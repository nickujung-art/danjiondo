# active-fix — 지번 충돌 14그룹 판정 및 전건 오연결 감사

> 착수 2026-08-24 · 분류 Small(확장) → **범위 재정의됨** · 루프 카운터: 0
> 상태: **1단계 완료(감사 도구 + 판정)**. DB 변경 0건 — 이동은 다음 세션.

## 문제 정의

`complex_canonical_jibun_collisions` 14그룹은 지번 게이트가 **꺼지는 구간**이다.
어제(08-21) 문서는 이를 "중복 등록 확인"으로 예상했다. **실측 결과 전제가 틀렸다.**

## 실행 결과

### 14그룹 전건 판정 완료
중복 등록 2(+1 보류) / 정상 3 / 재건축 1 / 지번무효 1 / **거래 오연결 5** / 재검토 1.
판정표는 `.planning/data-quality/wholesale-mislink-20260824.md` §8 — 다시 파지 말 것.

### 🔴 그 과정에서 훨씬 큰 것이 나왔다
기존 `relink-transactions-by-jibun.ts` 는 **소수 오염**만 잡는다. 거래가 **전건** 남의
지번이면 다수결 자체가 오염돼 벗어난 거래가 없고, 그래서 못 본다.
새 탐지 축(단지 등록 주소 대조)으로 운영권역을 재니:

```
거래_오연결      94곳 / 6,715건   ← 어제 정리한 1,659건의 4배
단지주소_오류    37곳 / 1,358건   ← 핸드오프 P2 계열. 거래를 건드리면 안 된다
판정불가         30곳 / 1,063건
```

동 불일치는 **두 결함을 한 바구니에 담는다.** 좌표 클러스터로 갈랐다(카카오 호출 0회).
3km 상한을 넣어 오확정을 막았다 — 첫 판에서 `경남`[마산합포구]을 12km 떨어졌는데도
"주소가 딴 도시"로 확정했다.

## 수정 범위 (실제)

| 파일 | 성격 |
|---|---|
| `scripts/audit-wholesale-mislink.ts` | 🆕 전건 오연결 감사 (읽기 전용) |
| `scripts/lib/name-similarity.ts` | 🆕 `nameSim` 공유 모듈 |
| `scripts/relink-transactions-by-jibun.ts` | import 로 교체 — **전후 dry-run 출력 완전 동일 실증** |
| `.planning/data-quality/wholesale-mislink-20260824.md` | 🆕 감사 결과 + 14그룹 판정표 + 인계 |
| `.planning/data-quality/wholesale-mislink-20260824.json` | 🆕 전건 판정 데이터 |
| `.planning/data-quality/matching-integrity-20260821.md` | §7 ① 결과·전제 뒤집힘 반영 |

`src/` 무접촉 · 마이그레이션 0건 · **DB 데이터 변경 0건**.

## 검증

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` | `✔ No ESLint warnings or errors` |
| 리팩터 무해성 | `relink` dry-run 전/후 diff **빈 출력** |
| 무결성 3축 | 7 / 33 / 61 = 기준선 (변동 없음 — 쓰기가 없었으므로 당연) |

## 남은 것 (다음 세션)

`wholesale-mislink-20260824.md` §7 참조. 요약:
1. 상위 10곳 카카오 지번 실측 — `verify-tx-jibun-kakao.ts` 입력 어댑터 필요
2. 이동 목표 없는 건(상위 9곳 중 5곳)의 처리 방침 — 끊기 / 신규 등록 / 별칭
3. 단지주소_오류 37곳 ↔ 핸드오프 P2 66곳 대조
4. `--busan` 재실행 + 0.6 임계 미달 단지를 세는 축 추가
5. 병합(신문동 1540 · 진동리 201) + 재건축 관계(남산동 601-26)

## 사용자 결정 이력

- 2026-08-24 ①(충돌 14그룹)부터 진행 — 승인
- 2026-08-24 A 5건 전부 + B 병합 2건 + D 재건축 — 승인
- 2026-08-24 🔴 **규모가 94곳/6,715건으로 드러난 뒤 범위 재정의** — 감사 도구와 판정만
  커밋하고 이동은 카카오 검증 후 별도 세션으로. 되돌리기 어려운 변경을 검증 없이 하지 않는다
