---
phase: 37-migration-drift
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260618085750_perf_review_avg_rpc.sql
  - supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql
  - supabase/migrations/20260618093403_fix_security_definer_search_path_v2.sql
  - supabase/migrations/20260625063824_cardnews_payloads_storage_policies.sql
  - supabase/migrations/20260728074553_realtrade_story_ads_admin.sql
autonomous: true
requirements: [DRIFT-01, DRIFT-02]

must_haves:
  truths:
    - "프로덕션 전용 스키마 5건이 remote 버전 파일명으로 supabase/migrations/에 존재한다"
    - "각 복원 파일의 본문이 원장 statements와 바이트 단위로 일치한다 (추가한 주석 헤더 제외)"
    - "복원 중 어떤 개선도 하지 않았다 — TO 절 추가·using(true) 변경·포맷 변경 0건"
    - "migration list --linked의 remote-only가 18건에서 13건으로 줄고, 그 13건이 D-04 표와 정확히 일치한다"
    - "스키마·데이터가 변경되지 않았다 (baseline 11개 항목 착수 전과 동일)"
    - "5개 파일이 git에 커밋되어 Wave 1이 안전하게 시작될 수 있다"
  artifacts:
    - path: "supabase/migrations/20260618085750_perf_review_avg_rpc.sql"
      provides: "get_complex_review_avg(uuid) 함수 + GRANT"
      contains: "CREATE OR REPLACE FUNCTION public.get_complex_review_avg"
    - path: "supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql"
      provides: "regional_income·complex_area_types RLS + ad_events INSERT 정책 수정본"
      contains: "ad_events: authenticated insert"
    - path: "supabase/migrations/20260618093403_fix_security_definer_search_path_v2.sql"
      provides: "SECURITY DEFINER 4함수 search_path='' 고정"
      contains: "SET search_path = ''"
    - path: "supabase/migrations/20260625063824_cardnews_payloads_storage_policies.sql"
      provides: "cardnews-payloads 버킷 정책 3개"
      contains: "cardnews-payloads public read"
    - path: "supabase/migrations/20260728074553_realtrade_story_ads_admin.sql"
      provides: "site_admin_roles 테이블 + RLS + realtrade-story-ad-images 버킷"
      contains: "create table public.site_admin_roles"
  key_links:
    - from: "supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql"
      to: "supabase/migrations/20260618000001_complex_area_types.sql"
      via: "complex_area_types 테이블이 더 앞 버전에서 생성됨 (reset 순서 성립)"
      pattern: "complex_area_types"
    - from: "supabase/migrations/20260728074553_realtrade_story_ads_admin.sql"
      to: "supabase/migrations/20260430000007_ads.sql"
      via: "ad_campaigns 테이블이 더 앞 버전에서 생성됨"
      pattern: "ad_campaigns"
---

<objective>
프로덕션에만 존재하고 로컬 저장소에는 없는 마이그레이션 5건을 원장
(`supabase_migrations.schema_migrations.statements`)에서 추출해 로컬 파일로 복원한다.

Purpose: `supabase db reset`이 프로덕션을 재현하지 못하는 상태를 끝낸다. 동시에 Wave 1이
원장 13건을 `reverted` 처리하기 전에 **복원 대상 5건의 유일한 기록을 파일로 확보**한다 —
순서가 뒤바뀌면 추출이 영구히 불가능해진다.

Output: 마이그레이션 파일 5개 (신규), 커밋 1건. 스키마 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-migration-drift/37-CONTEXT.md
@.planning/phases/36-db-supabase/36-00-SUMMARY.md
@CLAUDE.md

**Supabase 프로젝트**: `auoravdadyzvuoxunogh` (danjiondo + realtrade-story 공용)

🔴 **이 plan은 스키마를 변경하지 않는다.**
- `execute_sql`로 `create`/`alter`/`drop`/`insert` 실행 **금지**. `select` 읽기 조회만
- `npx supabase migration repair` 실행 **금지** (Wave 1 소관). D-01 덕분에 이 plan에는 repair가 필요 없다
- `npx supabase db push`를 `--dry-run`조차 이 plan에서 판정 근거로 쓰지 않는다 —
  remote-only 13건이 남아 있어 이 시점에는 실패하는 것이 정상이다
- `src/**` 무접촉

**CLI 인증**: `--linked` 명령이 DB 비밀번호를 대화형으로 물으면 임의 값을 넣지 말고
사용자에게 물어라. 비밀번호를 파일·커밋에 절대 남기지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 착수 기준값(baseline) 실측 + 원장 5건 원문 추출</name>

  <files>
  (저장소 파일 변경 없음 — 스크래치패드 임시 파일만 생성)
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` `<baseline>` 절 (L296–318) — 대조할 11개 항목
  - 같은 파일 D-01 표 (L43–57) — 추출할 5개 version과 기대 크기
  - 같은 파일 D-06 (L279–286), `<scope_fence>` 1번 (L354)
  </read_first>

  <action>
  **(A) baseline 11개 항목을 읽기 전용 조회로 실측한다.** 모두 `select`만 사용.
  CONTEXT `<baseline>` 표의 값과 하나씩 대조하고, 실제 출력값을 기록해 둔다
  (SUMMARY에 "착수 시 실측" 블록으로 남긴다).

  1. `select count(*) from public.site_admin_roles` → 1 / `select count(*) from pg_policies where tablename = 'site_admin_roles'` → 1
  2. `select count(*) from pg_proc p join pg_namespace n on n.oid = p.pronamespace where n.nspname = 'public' and p.proname = 'get_complex_review_avg'` → 1
  3. `select relrowsecurity from pg_class where relname = 'regional_income'` → `true` / `select count(*) from pg_policies where tablename = 'regional_income'` → 1
  4. `select count(*) from pg_policies where schemaname = 'storage' and tablename = 'objects' and policyname like 'cardnews-payloads%'` → 3
  5. `select proconfig from pg_proc where proname = 'check_gps_proximity'` → `search_path=""` 포함
  6. `select count(*), max(pronargs) from pg_proc where proname = 'recommend_hagwon_candidates'` → 1, 7
  7. `select roles, with_check from pg_policies where tablename = 'ad_events' and policyname = 'ad_events: authenticated insert'` → `{authenticated}` / `(auth.uid() IS NOT NULL)`
  8. `select count(*) from public.complexes` → 4285
  9. `select count(*) from public.hagwon_db` → 4601
  10. `select count(*) from public.complex_area_types` → 3472
  11. 창부레터 테이블 5개(`contents`, `content_complexes`, `content_votes`, `content_bookmarks`, `subscribers`): `relrowsecurity` 전부 `true`, `pg_policies` 합계 7, 각 테이블 0행

  **(B) 원장에서 5건의 원문을 추출한다.**
  `select version, name, array_length(statements, 1) as n, length(statements[1]) as len, md5(rtrim(statements[1], E'\n')) as body_md5 from supabase_migrations.schema_migrations where version in ('20260618085750','20260618085906','20260618093403','20260625063824','20260728074553') order by version`

  - `n`이 5건 모두 1이어야 한다. 1이 아닌 행이 있으면 그 version은 `statements`
    전체 원소를 순서대로 이어붙인 것이 원문이며, Task 2의 대조 기준도 그 이어붙인 값으로 바꾼다.
    (그 사실을 SUMMARY에 기록한다.)
  - `len`을 D-01 표의 크기(328B / 663B / 433B / 574B / 3.0KB)와 대조한다.
  - 각 version의 `statements[1]` 전문과 `body_md5`를 스크래치패드에
    `37-ledger-<version>.sql` / `37-ledger-md5.txt`로 저장한다. Task 2의 바이트 대조 기준값이다.

  **(C) 남은 drift 규모를 기록한다.** `npx supabase migration list --linked` 실행 →
  remote-only 18건 / local-only 4건임을 확인하고 raw 출력을 보관한다.
  local-only 4건의 정체(중복 3쌍 관련 3건 + 1건)를 목록으로 적어 둔다 — Wave 1의 입력이다.
  </action>

  <verify>
    <automated>npx supabase migration list --linked</automated>
  </verify>

  <acceptance_criteria>
  - baseline 11개 항목 실측값이 CONTEXT `<baseline>` 표와 **전부 일치**. 불일치가 1건이라도
    있으면 실행을 중단하고 사용자에게 보고한다 (Phase 착수 전제가 깨진 것)
  - `array_length(statements,1)` 조회 결과가 5개 version 모두 확인되었고, `n != 1`인 행이
    있으면 대조 기준을 이어붙인 값으로 변경했음을 SUMMARY에 명시
  - 스크래치패드에 `37-ledger-20260618085750.sql` 등 5개 파일이 존재하고 각 파일이 비어 있지 않음
  - 5건의 `md5(rtrim(statements[1], E'\n'))` 값이 기록되어 있음
  - `npx supabase migration list --linked` 출력에서 remote-only가 18건이고, 그 집합에
    `20260618085750`·`20260618085906`·`20260618093403`·`20260625063824`·`20260728074553`가 포함됨
  - 이 태스크에서 실행한 SQL이 전부 `select`. `create`/`alter`/`drop`/`insert`/`update` 0건
  - `git status --porcelain` 출력이 착수 시점과 동일 (파일 변경 0)
  </acceptance_criteria>

  <done>
  baseline 11개 항목이 CONTEXT와 일치함이 실측으로 확인되고, 5건의 원장 원문과 md5가
  스크래치패드에 확보되어 Task 2가 바이트 대조를 수행할 수 있다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 복원 파일 5개 생성 + 원장 바이트 대조</name>

  <files>
  supabase/migrations/20260618085750_perf_review_avg_rpc.sql,
  supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql,
  supabase/migrations/20260618093403_fix_security_definer_search_path_v2.sql,
  supabase/migrations/20260625063824_cardnews_payloads_storage_policies.sql,
  supabase/migrations/20260728074553_realtrade_story_ads_admin.sql
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` **D-02 전체 (L59–206)** — 복원할 SQL 5개
    코드블록 전문. 이것이 파일 본문의 유일한 원천이다
  - 같은 파일 **D-03 (L208–220)** — 개선 금지 이유와 "고치고 싶은 유혹" 3항목
  - 같은 파일 D-01 표 (L43–57) — 파일명 매핑
  - 같은 파일 `<scope_fence>` 2·3번 (L355–358)
  - `supabase/migrations/20260430000009_rls.sql` 의 `ad_events` 정책 부분 (L145–160 부근) —
    **읽기만 한다. 수정 금지.** 왜 이 버그 버전을 그대로 두는지 D-02 인용 블록(L95–99) 참조
  - Task 1이 스크래치패드에 남긴 `37-ledger-*.sql` 5개 — 대조 기준
  </read_first>

  <action>
  CONTEXT D-02의 5개 코드블록을 **그대로** 각 파일에 넣는다. 매핑은 D-01 표 그대로:
  `20260618085750` → `supabase/migrations/20260618085750_perf_review_avg_rpc.sql`,
  `20260618085906` → `..._rls_regional_income_area_types_ad_events.sql`,
  `20260618093403` → `..._fix_security_definer_search_path_v2.sql`,
  `20260625063824` → `..._cardnews_payloads_storage_policies.sql`,
  `20260728074553` → `..._realtrade_story_ads_admin.sql`.

  각 파일의 구조는 정확히 다음 3부분이다. 대조 가능성을 위해 헤더는 **딱 2줄**로 고정한다.
  1줄: `-- Phase 37 복원 — 원장 version <version>에서 추출, 프로덕션 그대로 재현 (개선 금지: D-03)`
  2줄: 빈 줄
  3줄부터: D-02 블록 본문 **한 글자도 바꾸지 않고**.

  **금지 사항 (D-03·Scope Fence 2번)** — 아래를 하면 이 Phase의 목적이 무너진다:
  `TO authenticated` / `TO anon` 절 추가, `using (true)`를 조건식으로 교체, 들여쓰기·대소문자·
  줄바꿈 정리, 주석 문구 수정, 세미콜론 추가·제거, `if not exists` 추가.
  `20260728074553` 파일의 정책 6개와 `20260625063824` 정책 3개에 `TO` 절이 없는 것,
  `regional_income`·`complex_area_types` 정책이 `USING (true)`인 것은 **프로덕션 현재 상태**다.

  **`supabase/migrations/20260430000009_rls.sql`을 수정하지 않는다** (Scope Fence 3번).
  `ad_events` 버그 버전은 그대로 두고, 복원 파일 `20260618085906`이 뒤 버전에서 덮게 한다 —
  그것이 프로덕션의 실제 이력이다.

  **파일 생성 후 원장과 바이트 대조한다.** 각 파일에 대해 헤더 2줄을 제거한 본문의 md5를
  구해(`tail -n +3` 후 후행 개선 제거 기준) Task 1이 기록한
  `md5(rtrim(statements[1], E'\n'))`와 비교한다. 예:
  `printf '%s' "$(tail -n +3 supabase/migrations/20260618085750_perf_review_avg_rpc.sql)" | md5sum`
  불일치가 나오면 **원장이 정답이다** — 스크래치패드의 `37-ledger-<version>.sql` 내용으로
  본문을 교체하고 다시 대조한다. CONTEXT D-02와 원장이 달랐다면 그 차이를 SUMMARY에 기록한다.

  **reset 순서 성립을 확인한다.** 복원 파일이 참조하는 객체가 더 **앞선 버전**의 마이그레이션에서
  생성되는지 grep으로 확인한다: `complex_reviews`(20260430000016), `regional_income`(20260602000001),
  `complex_area_types`(20260618000001), `ad_events`·`ad_campaigns`(20260430000007),
  `check_gps_proximity`·`new_listings`(20260507000004), `get_recent_complex_sales`(20260519000001),
  `get_hagwon_grade`(20260519000003), `get_schools_for_point`(20260526000002),
  `presale_discoveries`(20260530000002). 전부 복원 파일 version보다 작아야 한다.
  하나라도 뒤라면 `db reset`이 실패하므로 중단하고 보고한다.
  </action>

  <verify>
    <automated>ls -1 supabase/migrations/20260618085750_* supabase/migrations/20260618085906_* supabase/migrations/20260618093403_* supabase/migrations/20260625063824_* supabase/migrations/20260728074553_* && git diff --stat supabase/migrations/20260430000009_rls.sql</automated>
  </verify>

  <acceptance_criteria>
  - `ls supabase/migrations/` 에 D-01 표의 5개 파일명이 **정확히 그 이름으로** 존재 (각 1개)
  - 5개 파일 모두: 1행이 `-- Phase 37 복원 — 원장 version` 로 시작, 2행이 빈 줄
  - 5개 파일 모두 `tail -n +3` 본문의 md5가 원장 `md5(rtrim(statements[1], E'\n'))`와 **일치**
    (5/5 일치. 불일치 0건)
  - `git diff --stat supabase/migrations/20260430000009_rls.sql` 출력이 **빈 문자열**
    (`ad_events` 버그 버전 무변경)
  - `grep -c 'TO authenticated' supabase/migrations/20260625063824_cardnews_payloads_storage_policies.sql` → 0
  - `grep -c 'TO authenticated' supabase/migrations/20260728074553_realtrade_story_ads_admin.sql` → 0
  - `grep -c 'USING (true)' supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql` → 2
  - `grep -c 'TO authenticated' supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql` → 1
    (`ad_events` 정책의 것 하나. D-02 원문 그대로)
  - `git status --porcelain src/` 출력이 **빈 문자열**
  - 복원 파일이 참조하는 선행 객체 10종의 생성 마이그레이션 version이 전부 해당 복원 파일
    version보다 작음을 grep 근거와 함께 확인
  </acceptance_criteria>

  <done>
  5개 복원 파일이 원장과 바이트 일치하고, `20260430000009_rls.sql`은 무변경이며,
  `db reset` 순서상 선행 의존이 모두 앞 버전에서 충족된다.
  </done>
</task>

<task type="auto">
  <name>Task 3: remote-only 18→13 축소 확인 + 커밋</name>

  <files>
  (커밋만 — 파일 내용 변경 없음)
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` **D-04 (L222–248)** — reverted 대상 13건 표.
    이번 태스크가 대조할 정답 집합이다
  - 같은 파일 D-01 (L43–57) — 파일명=remote 버전이므로 repair가 불필요한 이유
  - `CLAUDE.md` 커밋 컨벤션
  </read_first>

  <action>
  `npx supabase migration list --linked`을 다시 실행해 방금 만든 5개 파일이 원장 기록과
  **매칭됐음**을 확인한다 (Local·Remote 양쪽에 같은 version이 표시됨). `migration repair`는
  실행하지 않는다 — D-01에 따라 파일명이 이미 remote 버전이므로 자동으로 매칭된다.

  remote-only 목록이 18건에서 **13건**으로 줄었는지 확인하고, 그 13건의 version 집합이
  CONTEXT D-04의 중복 12건 + 덮인 구버전 1건(`20260618075929`)과 **정확히 일치**하는지
  집합 비교한다. 여분이나 누락이 있으면 Wave 1을 시작하지 말고 사용자에게 보고한다.

  `npm run lint`를 실행한다 (마이그레이션 파일만 추가했으므로 통과해야 한다).

  5개 파일을 한 커밋으로 커밋한다. 커밋 메시지:
  `fix(37): 프로덕션 전용 마이그레이션 5건 로컬 복원 (DRIFT-01, DRIFT-02)`
  본문에 복원한 5개 version과 "원장 statements md5 일치 확인", "개선 없음 — 충실 재현(D-03)"을
  적는다. `supabase/migrations/` 5개 파일 외에는 스테이징하지 않는다.

  Wave 1이 이 커밋 존재를 게이트로 확인하므로 **커밋을 반드시 완료**한다.
  </action>

  <verify>
    <automated>npx supabase migration list --linked && npm run lint && git log -1 --name-only --oneline</automated>
  </verify>

  <acceptance_criteria>
  - `npx supabase migration list --linked` 출력에서 5개 version(`20260618085750`,
    `20260618085906`, `20260618093403`, `20260625063824`, `20260728074553`)이 remote-only가
    아니라 local·remote 양쪽에 표시됨
  - remote-only 항목 수가 **정확히 13**이고, 그 version 집합이 D-04의 13건과 완전 일치
    (차집합 양방향 0건). 실제 13개 version을 출력으로 남긴다
  - `npm run lint` 종료 코드 0
  - `git log -1 --name-only` 에 5개 마이그레이션 파일만 포함, `src/` 경로 0건
  - `git status --porcelain supabase/migrations/` 출력이 빈 문자열 (미커밋 잔여 없음)
  - `migration repair` 명령을 이 plan에서 **한 번도 실행하지 않음**
  </acceptance_criteria>

  <done>
  5개 복원 파일이 커밋되어 원장 13건의 안전한 정리(Wave 1)가 가능해졌고,
  remote-only 잔여가 D-04 표와 정확히 일치함이 확인됐다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 저장소 → 프로덕션 스키마 | 마이그레이션 파일이 향후 `db reset`·새 환경 구축 시 RLS 정책의 원천이 된다 |
| 원장(`supabase_migrations`) → CLI | CLI가 push/reset 대상을 판단하는 유일한 근거 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | 복원 파일 본문 | mitigate | 원장 `md5(rtrim(statements[1],E'\n'))`와 바이트 대조. 불일치 시 원장을 정답으로 채택 (Task 2) |
| T-37-02 | Elevation of Privilege | `regional_income`·`complex_area_types` `USING (true)` 정책 | accept | 프로덕션 현재 상태의 충실 재현이 이 Phase의 목적(D-03). 하드닝은 O-2 별도 Phase에서 프로덕션·로컬 동시 변경 |
| T-37-03 | Elevation of Privilege | `site_admin_roles`·`ad_campaigns`·`cardnews-payloads` 정책의 `TO` 절 누락 | accept | `auth.uid()` / `auth.role()` 조건이 `anon`을 실질 차단. O-2로 이월 (D-03) |
| T-37-04 | Tampering | `20260430000009_rls.sql` (`ad_events` 버그 버전) | mitigate | 수정 금지 + `git diff --stat` 빈 출력을 acceptance criterion으로 강제. 복원 파일이 뒤 버전에서 덮음 |
| T-37-05 | Denial of Service | 프로덕션 스키마 | mitigate | `execute_sql`은 `select`만. DDL 0건을 acceptance criterion으로 검증 + baseline 11개 항목 대조 |
| T-37-SC | Tampering | 패키지 설치 | N/A | 이 Phase는 npm/pip/cargo 설치가 없다. Package Legitimacy Gate 비적용 |
</threat_model>

<verification>
1. `npx supabase migration list --linked` — 5개 복원 version이 매칭, remote-only 정확히 13건
   (= D-04 표와 집합 일치), local-only 4건은 Wave 1에서 처리
2. 5개 파일 본문 md5 = 원장 `statements` md5 (5/5)
3. baseline 11개 항목이 CONTEXT `<baseline>`과 일치 (스키마 무변경)
4. `git diff --stat supabase/migrations/20260430000009_rls.sql` 빈 출력
5. `npm run lint` 통과, `git status --porcelain src/` 빈 출력
6. 커밋 1건 완료 (Wave 1의 선행 게이트)

**이 plan에서 판정하지 않는 것**: `db push --dry-run`. remote-only 13건이 남아 있어
이 시점에는 실패가 정상이다. Wave 1의 판정 항목이다.
</verification>

<success_criteria>
- D-01 표의 5개 파일이 정확한 파일명으로 존재하고 내용이 원장과 바이트 일치
- 복원 중 개선 0건 (`TO` 절 추가·`using(true)` 변경·포맷 변경 없음)
- `20260430000009_rls.sql` 무변경
- remote-only 18 → 13, 그 13건이 D-04와 일치
- baseline 11개 항목 무변경 (DDL 실행 0건)
- 5개 파일이 커밋됨
</success_criteria>

<output>
`.planning/phases/37-migration-drift/37-00-SUMMARY.md` 를 작성한다. 반드시 포함할 것:

- baseline 11개 항목 **착수 시 실측값** (Wave 1이 종료 시 대조할 기준)
- 5건의 원장 `md5` / `length` / `array_length(statements,1)` 값과 로컬 파일 md5 일치 여부
- CONTEXT D-02와 원장 원문이 달랐다면 그 차이 (없으면 "차이 0")
- `migration list --linked` remote-only 13건 목록과 local-only 4건의 정체 (Wave 1 입력)
- Wave 1 착수 전제: 5개 파일 커밋 SHA
</output>
