---
phase: 37-migration-drift
plan: "01"
type: execute
wave: 1
depends_on: [37-00]
files_modified:
  - supabase/migrations/20260618000001_complex_area_types.sql
  - supabase/migrations/20260618000001_fix_avg_sale_per_pyeong_formula.sql
  - supabase/migrations/20260618000002_area_type_chart_rpc.sql
  - supabase/migrations/20260618000002_fix_prediction_model_priority.sql
  - supabase/migrations/20260619000002_phase28_subject_v2.sql
  - supabase/migrations/20260619000002_recommend_hagwon_candidates_rpc.sql
autonomous: false
requirements: [DRIFT-03, DRIFT-04, DRIFT-05]

must_haves:
  truths:
    - "원장에서 remote 전용 13건이 제거되어 remote-only 0건이다"
    - "복원된 5건은 원장에 여전히 applied로 남아 있다"
    - "supabase/migrations/에 타임스탬프 중복이 0건이다"
    - "리네이밍이 의존 순서를 깨지 않았다 — 이동 파일의 객체를 참조하는 마이그레이션이 새 슬롯 앞에 없다"
    - "리네이밍한 새 버전 3건이 원장에 applied로 기록되어 push 대상이 아니다"
    - "npx supabase migration list --linked에서 local-only 0건, remote-only 0건이다"
    - "npx supabase db push --dry-run이 에러 없이 통과하고 적용 대상이 없다"
    - "baseline 11개 항목이 Phase 착수 전과 동일하다 (스키마 무변경 증명)"
    - "O-1(recommend_hagwons 오버로드)·O-2(TO 절 누락)가 SUMMARY에 별도 Phase 이월로 기록됐다"
  artifacts:
    - path: "supabase/migrations/"
      provides: "타임스탬프 중복 0건인 마이그레이션 디렉터리"
      contains: "중복 접두어 없음"
  key_links:
    - from: "supabase/migrations/20260619000002_recommend_hagwon_candidates_rpc.sql"
      to: "supabase/migrations/20260619000003_recommend_hagwon_candidates_v2.sql"
      via: "rpc 파일 version < v2 version — v2가 DROP·재생성하므로 순서 역전 금지"
      pattern: "recommend_hagwon_candidates"
    - from: "supabase/migrations/20260618000002_area_type_chart_rpc.sql"
      to: "supabase/migrations/20260624000003_rpc_add_exclusive_area.sql"
      via: "chart rpc 파일 version < 20260624000003 — 후자가 같은 함수를 재생성"
      pattern: "complex_transactions_for_chart"
    - from: "supabase/migrations/20260618000001_complex_area_types.sql"
      to: "supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql"
      via: "테이블 생성이 RLS 복원 파일보다 앞 version 유지"
      pattern: "complex_area_types"
---

<objective>
Wave 0이 복원 파일을 커밋한 뒤, 원장의 remote 전용 13건을 `reverted` 처리하고, 로컬
타임스탬프 중복 3쌍을 의존 순서를 보존하며 리네이밍하고, 새 버전을 원장에 기록해
**`migration list` 완전 매칭 + `db push --dry-run` 통과** 상태를 회복한다.

Purpose: `npm run db:push`가 정상 작동하고 `supabase db reset`이 프로덕션을 재현하는 상태로
돌아간다. 원장에 중복·구버전 기록이 남아 있는 한 push는 계속 거부되고, 타임스탬프 중복이
남아 있는 한 파일 한쪽이 영구히 추적 불가 상태가 된다.

Output: 리네이밍된 마이그레이션 파일 3개(git mv), 원장 정리(13건 삭제 + 새 버전 기록), 커밋 1건.
스키마 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-migration-drift/37-CONTEXT.md
@.planning/phases/37-migration-drift/37-00-SUMMARY.md
@CLAUDE.md

**Supabase 프로젝트**: `auoravdadyzvuoxunogh`

🔴 **이 plan도 스키마를 변경하지 않는다.**
- `execute_sql`은 `select` 읽기 조회만. DDL(`create`/`alter`/`drop`) 실행 금지
- `npx supabase db push`는 **`--dry-run`만** (검증용). 실제 적용 실행 금지 (Scope Fence 9번)
- `recommend_hagwons` 오버로드 정리 금지 (O-1 — 별도 Phase), `TO` 절 하드닝 금지 (O-2 — 별도 Phase)
- 복원 파일 5개와 `20260430000009_rls.sql` 수정 금지
- `src/**` 무접촉

⚠️ **`migration repair`는 스키마를 바꾸지 않지만 원장 행을 지운다.** `--status reverted` 13건은
되돌리기 어렵다. Task 2의 blocking 체크포인트 승인 전에 Task 3을 실행하지 않는다.
파일 리네이밍은 git으로 되돌릴 수 있어 승인 게이트가 없다.

**CLI 인증**: `--linked` 명령이 DB 비밀번호를 대화형으로 물으면 임의 값을 넣지 말고
사용자에게 물어라. 비밀번호를 파일·커밋에 남기지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 선행 게이트 — Wave 0 커밋 확인 + reverted 대상 13건 집합 재확인</name>

  <files>
  (읽기·조회만. 파일 변경 없음)
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-00-SUMMARY.md` — Wave 0 커밋 SHA, baseline 착수 실측값,
    remote-only 13건 목록, local-only 4건의 정체
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` **D-04 (L222–248)** — reverted 대상 13건 표
    (중복 12건 + 덮인 구버전 `20260618075929`)
  - 같은 파일 `<scope_fence>` 4번 (L359–360) — 순서 위반 시 무엇이 소실되는지
  - 같은 파일 `<baseline>` (L296–318)
  </read_first>

  <action>
  **(A) Wave 0 산출물이 커밋됐는지 확인한다.** 5개 파일 전부에 대해
  `git ls-files --error-unmatch <path>`가 성공하고
  `git log --oneline -1 -- <path>`가 커밋을 반환해야 한다. 대상 5개:
  `supabase/migrations/20260618085750_perf_review_avg_rpc.sql`,
  `supabase/migrations/20260618085906_rls_regional_income_area_types_ad_events.sql`,
  `supabase/migrations/20260618093403_fix_security_definer_search_path_v2.sql`,
  `supabase/migrations/20260625063824_cardnews_payloads_storage_policies.sql`,
  `supabase/migrations/20260728074553_realtrade_story_ads_admin.sql`.
  하나라도 미커밋(또는 파일 없음)이면 **즉시 중단**하고 사용자에게 보고한다 — 이 상태에서
  `reverted`를 실행하면 해당 마이그레이션의 유일한 기록이 사라진다.

  **(B) reverted 대상 집합을 다시 확정한다.** `npx supabase migration list --linked` 실행 →
  remote-only 목록을 추출하고, CONTEXT D-04의 13개 version과 집합 비교한다.
  13건: `20260618051341`, `20260618073750`, `20260618075929`, `20260619043107`, `20260619062830`,
  `20260619072547`, `20260619075829`, `20260624045555`, `20260624045621`, `20260624045635`,
  `20260707051809`, `20260709061130`, `20260715030221`.
  차집합이 양방향 0건이 아니면 중단하고 보고한다.

  **(C) 중복 12건의 대응 로컬 파일이 실제로 존재하는지 확인한다.** D-04 표의 "대응 로컬 파일"
  열 12개 경로에 대해 `ls`로 존재를 확인한다. `20260618075929`(덮인 구버전)는 대응 파일이
  없는 것이 정상이며, 대신 라이브 `recommend_hagwon_candidates` 인자 수가 7개
  (`select count(*), max(pronargs) from pg_proc where proname = 'recommend_hagwon_candidates'`)
  → v2 시그니처와 일치함을 재확인한다.

  **(D) 원장 행 수를 기록한다.**
  `select count(*) from supabase_migrations.schema_migrations` — Task 3의 감소량 검증 기준값.

  **(E) local-only 항목을 전수 열거한다.** `migration list --linked`의 local-only 목록을 그대로
  적고, 각 항목이 (i) 타임스탬프 중복 3쌍에 기인한 것인지 (ii) 그 외인지 분류한다.
  중복 3쌍 외의 항목이 있으면 그 version의 스키마가 실제로 프로덕션에 적용돼 있는지
  읽기 조회로 확인해 Task 4의 `repair --status applied` 대상에 포함시킬 근거를 만든다.
  적용돼 있지 않다면 **적용은 이 Phase 범위 밖**이므로 중단하고 사용자에게 보고한다.
  </action>

  <verify>
    <automated>npx supabase migration list --linked && git log --oneline -1 -- supabase/migrations/20260728074553_realtrade_story_ads_admin.sql</automated>
  </verify>

  <acceptance_criteria>
  - 5개 복원 파일 전부 `git ls-files --error-unmatch` 종료 코드 0 이고
    `git log --oneline -1 -- <path>` 출력이 비어 있지 않음 (5/5)
  - `git status --porcelain supabase/migrations/` 출력이 빈 문자열 (미커밋 잔여 없음)
  - remote-only 집합이 위 13개 version과 **완전 일치** (차집합 양방향 0건). 실제 목록을 출력으로 남김
  - D-04 "대응 로컬 파일" 12개 경로가 전부 `ls`로 확인됨
  - `recommend_hagwon_candidates` 조회 결과 count=1, pronargs=7
  - `select count(*) from supabase_migrations.schema_migrations` 값이 기록됨
  - local-only 항목이 전수 열거되고 각각 (i)/(ii)로 분류됨. (ii)에 해당하는 항목은
    프로덕션 적용 여부가 읽기 조회 근거와 함께 판정됨
  - 이 태스크에서 `repair`·DDL 실행 0건
  </acceptance_criteria>

  <done>
  Wave 0 커밋이 확인되고, reverted 대상 13건이 D-04와 일치하며, local-only 잔여의 정체가
  전부 분류되어 Task 2의 승인 요청에 필요한 근거가 갖춰졌다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] 원장 13건 reverted 처리 승인</name>

  <files>
  (파일 변경 없음 — 승인 게이트)
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` **D-04 (L222–248)** 및 Risk Summary (L381–390)
  - Task 1의 (A)~(E) 출력 전체
  </read_first>

  <action>
  아무것도 실행하지 않는다. Task 1의 (A)~(E) 근거를 아래 `<what-built>` 형식으로 사용자에게
  제시하고 승인을 기다린다. `repair --status reverted`는 **승인 이후 Task 3에서만** 실행한다.
  사용자가 이견을 제시하면 실행하지 않고 이견을 반영한 뒤 다시 승인을 요청한다.
  `workflow.auto_advance` 설정과 무관하게 이 게이트는 자동 통과하지 않는다.
  </action>

  <what-built>
  Task 1에서 아래를 **확인만** 했고, 원장은 아직 건드리지 않았다:

  1. Wave 0의 복원 파일 5개가 커밋됨 (커밋 SHA 제시)
  2. `migration list --linked` remote-only가 정확히 13건이고 CONTEXT D-04 표와 집합 일치
  3. 중복 12건의 대응 로컬 파일 12개가 모두 존재
  4. `20260618075929`(덮인 구버전)는 라이브 `recommend_hagwon_candidates`가 인자 7개
     (v2 시그니처)임이 실측돼 구버전이 이미 덮인 것으로 확인됨
  5. 현재 원장 행 수 (Task 3 이후 정확히 13 감소해야 함)

  실행하려는 명령 (아직 실행 안 함):
  `npx supabase migration repair --status reverted 20260618051341 20260618073750 20260618075929 20260619043107 20260619062830 20260619072547 20260619075829 20260624045555 20260624045621 20260624045635 20260707051809 20260709061130 20260715030221`

  이 명령은 **스키마를 변경하지 않는다.** 원장 테이블
  `supabase_migrations.schema_migrations`에서 13개 행을 삭제할 뿐이다. 다만 삭제된 기록은
  되돌리기 어렵다 — 그래서 승인을 받는다.
  </what-built>

  <how-to-verify>
  1. Task 1이 출력한 remote-only 13개 version 목록과 위 명령의 13개 version이 같은지 눈으로 대조
  2. 목록에 복원 대상 5건(`20260618085750`, `20260618085906`, `20260618093403`,
     `20260625063824`, `20260728074553`)이 **포함되어 있지 않은지** 확인 — 하나라도 있으면 중단
  3. Task 1이 제시한 5개 복원 파일 커밋 SHA로 `git show --stat <SHA>` 를 실행해 파일 5개가
     실제 커밋에 들어 있는지 확인 (원장 기록이 사라져도 파일이 남는다는 보장)
  4. 중복 12건에 대응하는 로컬 파일 12개 목록이 D-04 표와 일치하는지 확인
  </how-to-verify>

  <verify>
    <human-check>사용자가 위 4단계를 확인하고 "approved" 응답</human-check>
  </verify>

  <acceptance_criteria>
  - 사용자가 "approved"(또는 동등한 승인)를 명시적으로 응답했다
  - 승인 없이는 Task 3을 실행하지 않는다
  - 이 태스크에서 `repair`·DDL·파일 변경 0건
  </acceptance_criteria>

  <done>
  사용자가 13개 version 목록과 복원 파일 커밋을 확인하고 원장 정리를 승인했다.
  </done>

  <resume-signal>"approved" 입력, 또는 문제점 지적</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 원장 13건 reverted 실행 + 즉시 검증</name>

  <files>
  (원장 조작만. 저장소 파일 변경 없음)
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` **D-04 (L222–248)**, D-06 (L279–286)
  - Task 1의 (D) 원장 행 수 기준값, Task 2의 승인 내용
  </read_first>

  <action>
  Task 2에서 승인받은 명령을 **그대로** 실행한다 (version 13개, 추가·누락 없이):
  `npx supabase migration repair --status reverted 20260618051341 20260618073750 20260618075929 20260619043107 20260619062830 20260619072547 20260619075829 20260624045555 20260624045621 20260624045635 20260707051809 20260709061130 20260715030221`

  실행 직후 검증한다:
  - `select count(*) from supabase_migrations.schema_migrations` → Task 1 (D) 기준값보다
    **정확히 13 감소**
  - `select version, name from supabase_migrations.schema_migrations where version in (13개 version)`
    → **0행** (전부 제거됨)
  - `select version from supabase_migrations.schema_migrations where version in ('20260618085750','20260618085906','20260618093403','20260625063824','20260728074553')`
    → **5행** (복원 5건은 그대로 applied 유지)
  - `npx supabase migration list --linked` → remote-only **0건**

  DDL은 실행하지 않는다. `repair`는 지정한 13개 version 외에 어떤 인자도 받지 않는다.
  이 태스크에서는 커밋할 파일 변경이 없다 (원장은 git 대상이 아니다).
  </action>

  <verify>
    <automated>npx supabase migration list --linked</automated>
  </verify>

  <acceptance_criteria>
  - `npx supabase migration list --linked` 출력의 remote-only 항목 수가 **0**
  - `select count(*) from supabase_migrations.schema_migrations` 이 Task 1 (D) 값 − 13 과 정확히 일치
  - 13개 version 조회 결과 0행
  - 복원 5건 version 조회 결과 5행 (`20260618085750`, `20260618085906`, `20260618093403`,
    `20260625063824`, `20260728074553` 전부 존재)
  - 실행한 `repair` 명령이 1회이고, 인자가 승인받은 13개 version과 동일 (전문을 출력으로 남김)
  - `--status applied` 를 이 태스크에서 실행하지 않음 (Task 4 소관)
  - `git status --porcelain` 출력이 Task 1 시점과 동일
  </acceptance_criteria>

  <done>
  원장에서 remote 전용 13건이 제거되어 remote-only 0건이 되었고, 복원 5건은 applied로 남아 있다.
  </done>
</task>

<task type="auto">
  <name>Task 4: 타임스탬프 중복 3쌍 리네이밍 + 새 버전 repair applied + 최종 회복 검증 + 커밋</name>

  <files>
  supabase/migrations/20260618000001_complex_area_types.sql,
  supabase/migrations/20260618000001_fix_avg_sale_per_pyeong_formula.sql,
  supabase/migrations/20260618000002_area_type_chart_rpc.sql,
  supabase/migrations/20260618000002_fix_prediction_model_priority.sql,
  supabase/migrations/20260619000002_phase28_subject_v2.sql,
  supabase/migrations/20260619000002_recommend_hagwon_candidates_rpc.sql
  (위 6개 중 3개가 새 version으로 `git mv` 된다)
  </files>

  <read_first>
  - `.planning/phases/37-migration-drift/37-CONTEXT.md` **D-05 전체 (L250–277)** — 중복 3쌍,
    확인된 제약, 빈 슬롯, 새 버전 repair 필요성
  - 같은 파일 D-07 (L288–292) — 새 타임스탬프 값은 재량
  - 같은 파일 `<baseline>` (L296–318), `<observations>` O-1·O-2 (L320–346),
    `<scope_fence>` 5·6·7번 (L361–363)
  - **중복 3쌍 6개 파일 전문** (아래 6개). 각 쌍에서 어느 파일을 옮길지는 **읽고 판단한다**:
    - `supabase/migrations/20260618000001_complex_area_types.sql` (34줄)
    - `supabase/migrations/20260618000001_fix_avg_sale_per_pyeong_formula.sql` (87줄)
    - `supabase/migrations/20260618000002_area_type_chart_rpc.sql` (41줄)
    - `supabase/migrations/20260618000002_fix_prediction_model_priority.sql` (230줄)
    - `supabase/migrations/20260619000002_phase28_subject_v2.sql` (86줄)
    - `supabase/migrations/20260619000002_recommend_hagwon_candidates_rpc.sql` (64줄)
  - `supabase/migrations/20260619000003_recommend_hagwon_candidates_v2.sql` — `recommend_hagwon_candidates`를
    `DROP` 후 재생성한다. rpc 파일이 뒤로 가면 안 되는 이유
  - `supabase/migrations/20260624000003_rpc_add_exclusive_area.sql` — `complex_transactions_for_chart`를
    재생성한다. chart rpc 파일이 이 version보다 뒤로 가면 안 되는 이유
  </read_first>

  <action>
  **(A) 빈 슬롯을 재확인한다.** `ls -1 supabase/migrations/ | cut -c1-14 | sort | uniq -c`로
  6/18·6/19 대의 사용 중 version을 확인한다 (D-05는 6/18은 `000001`·`000002`, 6/19는
  `000000`~`000003`만 존재한다고 기록했다 — 실측으로 재확인).

  **(B) 각 쌍에서 옮길 파일을 읽고 판단한다.** 판단 규칙:
  각 파일이 **생성·변경하는 식별자**를 뽑고, 저장소 전체 마이그레이션에서 그 식별자를 grep해
  **다른 마이그레이션이 그것을 참조·재생성하는지** 확인한다. 선행 조건이 되는 쪽(다른 파일들이
  의존하는 쪽)은 **원래 자리에 남기고**, 아무도 의존하지 않는 쪽을 옮긴다.

  - `20260619000002` 쌍: **판단이 이미 확정돼 있다** (D-05). `_recommend_hagwon_candidates_rpc.sql`은
    **옮길 수 없다** — `20260619000003_..._v2.sql`이 이 함수를 DROP·재생성하므로 뒤로 가면
    구버전이 v2를 덮어 최종 상태가 틀어진다 (Scope Fence 5번).
    → 짝인 `_phase28_subject_v2.sql`을 옮긴다.
  - `20260618000001` 쌍: 두 파일을 읽고 판단하라. 확인할 식별자 —
    `_complex_area_types.sql`은 `complex_area_types` 테이블·`transactions.area_type_id`·
    `idx_cat_*`를 **생성**하고, `_fix_avg_sale_per_pyeong_formula.sql`은
    `refresh_complex_price_stats()` 함수를 `CREATE OR REPLACE` 한다.
    grep으로 두 식별자를 저장소 전체에서 검색해 어느 쪽이 후속 마이그레이션의 선행 조건인지
    직접 확인하라. (참고 가설: `complex_area_types`는 여러 후속 파일이 참조하고
    `refresh_complex_price_stats`는 이 파일에만 등장한다 — **반드시 grep으로 재검증**하라.)
  - `20260618000002` 쌍: 두 파일을 읽고 판단하라. 확인할 식별자 —
    `_area_type_chart_rpc.sql`은 `complex_transactions_for_chart`를 DROP·재생성하며
    `complex_area_types`에 의존하고, `_fix_prediction_model_priority.sql`은
    `invest_prediction_ranking`·`invest_regional_prediction_summary`를 DROP·재생성한다.
    `complex_transactions_for_chart`는 `20260624000003_rpc_add_exclusive_area.sql`이 다시
    재생성하므로 chart rpc 파일은 그 version보다 **앞**이어야 한다. grep으로 재검증하라.

  **(C) `git mv`로 리네이밍한다.** 새 version은 재량이되 아래를 만족해야 한다:
  - 기존 파일·원장의 어떤 version과도 충돌하지 않는다
  - 이동 파일이 생성·변경하는 식별자를 참조하는 마이그레이션이 **새 슬롯보다 앞에** 남는다
  - 이동 파일이 의존하는 선행 객체의 생성 마이그레이션이 **새 슬롯보다 앞에** 있다
  - 파일명의 이름 부분(`_` 뒤)은 바꾸지 않는다. 파일 **내용도 바꾸지 않는다** (git mv만)

  권장 슬롯(충족 시 그대로 사용 가능): 6/18 쌍의 이동 파일 2개는 `20260618000003`·`20260618000004`,
  `_phase28_subject_v2.sql`은 `20260619000004`. `20260619000004`가 안전한 근거는 D-05에
  기록돼 있다(`hagwon_db`(`20260619000001`)만 선행 조건이고 RPC 생성과 순서 무관).
  단, `20260619000003_..._v2.sql`이 `_phase28_subject_v2.sql`이 만드는 제약·데이터에
  의존하지 않는지 grep으로 확인하라.

  **(D) 새 version 3건을 원장에 기록한다.** 리네이밍한 새 version은 원장에 없어 push 대상이 된다.
  `npx supabase migration repair --status applied <새 version 3개>`를 실행한다.
  Task 1 (E)에서 분류한 "중복 3쌍 외 local-only 항목"이 있었고 프로덕션 적용이 확인됐다면
  그 version도 같은 방식으로 `--status applied` 처리한다 (적용이 확인되지 않은 것은 절대 포함하지 않는다).

  **(E) 최종 회복 검증.**
  - `ls -1 supabase/migrations/ | cut -c1-14 | sort | uniq -d` → 출력 없음 (중복 0건)
  - `npx supabase migration list --linked` → local-only 0건, remote-only 0건
  - `npx supabase db push --dry-run` → 에러 없이 통과, 적용 대상 없음.
    **`--dry-run` 없이 실행하지 않는다** (Scope Fence 9번)
  - baseline 11개 항목 재조회 → `37-00-SUMMARY.md`의 착수 실측값 및 CONTEXT `<baseline>`과
    **전부 동일**. 항목별로 전/후 값을 나란히 기록한다. 조회 목록은 37-00-PLAN Task 1 (A)의
    11개와 동일하다 (`site_admin_roles` 1행/정책1 · `get_complex_review_avg` 존재 ·
    `regional_income` RLS true/정책1 · `cardnews-payloads` 정책3 · `check_gps_proximity`
    `search_path=""` · `recommend_hagwon_candidates` count1/pronargs7 · `ad_events` 정책
    `{authenticated}`+`(auth.uid() IS NOT NULL)` · `complexes` 4285 · `hagwon_db` 4601 ·
    `complex_area_types` 3472 · 창부레터 5테이블 RLS on/정책7/0행)
  - `npm run lint` 통과, `git status --porcelain src/` 빈 출력

  **(F) 커밋한다.** `chore(37): 마이그레이션 타임스탬프 중복 3쌍 리네이밍 + 원장 정리 (DRIFT-03~05)`
  본문에 reverted 13건, 리네이밍 3건의 구/신 version, `repair --status applied` 대상,
  `migration list` 0/0 및 `db push --dry-run` 결과를 적는다. `supabase/migrations/` 외
  스테이징 금지.
  </action>

  <verify>
    <automated>ls -1 supabase/migrations/ | cut -c1-14 | sort | uniq -d; npx supabase migration list --linked && npx supabase db push --dry-run && npm run lint</automated>
  </verify>

  <acceptance_criteria>
  - `ls -1 supabase/migrations/ | cut -c1-14 | sort | uniq -d` 출력이 **빈 문자열** (중복 0건)
  - `supabase/migrations/20260619000002_recommend_hagwon_candidates_rpc.sql` 이 **그 파일명 그대로
    존재**하고, 그 version이 `20260619000003_recommend_hagwon_candidates_v2.sql` 의 version보다 작음
  - `complex_transactions_for_chart`를 DROP·재생성하는 파일의 version이
    `20260624000003_rpc_add_exclusive_area.sql` 의 version보다 작음
  - `complex_area_types` 테이블을 `CREATE TABLE`하는 파일의 version이 `20260618085906`
    (복원 RLS 파일) 및 `20260619000000`·`20260624000002`·`20260624000003`·`20260707000000`
    보다 작음
  - 이동한 3개 파일 각각: 구 version 초과 ~ 신 version 이하 구간의 모든 마이그레이션 파일에서
    그 파일이 생성·변경하는 식별자를 grep한 결과 **참조 0건** (grep 명령과 출력을 근거로 제시)
  - 이동한 3개 파일의 **내용 무변경**: `git diff -M --stat` 에서 3건이 rename(R100)으로 표시되고
    내용 변경 라인 0
  - `npx supabase migration list --linked` 출력에서 local-only **0건**, remote-only **0건**
  - `npx supabase db push --dry-run` 종료 코드 0, 출력에 적용 대상 마이그레이션 0건
    (`LegacyDbPushMissingLocalError` 등 에러 문자열 0건)
  - baseline 11개 항목 전/후 값이 **11/11 동일** (하나라도 다르면 실패로 보고)
  - `npm run lint` 종료 코드 0, `git status --porcelain src/` 빈 출력
  - `20260430000009_rls.sql` 및 복원 파일 5개의 `git diff --stat` 빈 출력 (무변경)
  - 이 태스크에서 DDL 실행 0건, `db push`를 `--dry-run` 없이 실행한 이력 0건
  - 커밋 1건 완료, `git log -1 --name-only` 에 `src/` 경로 0건
  </acceptance_criteria>

  <done>
  타임스탬프 중복이 0건이고 의존 순서가 보존되며, `migration list --linked`가 local-only 0건 /
  remote-only 0건이고 `db push --dry-run`이 통과하며, baseline 11개 항목이 착수 전과 동일하다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 원장(`supabase_migrations.schema_migrations`) → CLI | push/reset 판단의 유일한 근거. 행 삭제는 되돌리기 어렵다 |
| 로컬 마이그레이션 파일 순서 → `db reset` 최종 스키마 | 순서가 뒤바뀌면 구버전이 신버전을 덮어 RLS·함수 정의가 퇴행한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06 | Denial of Service | `repair --status reverted` 13건 | mitigate | Wave 0 커밋 확인 게이트(Task 1 A) + blocking human 체크포인트(Task 2) + 13건 집합 D-04 대조 + 실행 후 복원 5건 applied 잔존 확인 |
| T-37-07 | Tampering | 리네이밍이 의존 순서를 역전 | mitigate | `_recommend_hagwon_candidates_rpc.sql` 이동 금지(Scope Fence 5) + 이동 구간 식별자 grep 0건 + version 대소 비교를 acceptance criterion으로 강제 |
| T-37-08 | Repudiation | 새 version 원장 미기록 → push 대상 잔존 | mitigate | `repair --status applied` 실행 + `db push --dry-run` 적용 대상 0건 검증 |
| T-37-09 | Elevation of Privilege | `recommend_hagwons` 오버로드 2개 모호성 (O-1) | transfer | 함수 DROP은 스키마 변경이고 `src/lib/**/hagwon*` 호출부 확인이 선행돼야 함. SUMMARY에 별도 Phase 이월 기록 |
| T-37-10 | Elevation of Privilege | `TO` 절 누락 정책 다수 (O-2) | transfer | 프로덕션·로컬 동시 변경이 필요. SUMMARY에 별도 Phase 이월 기록 |
| T-37-11 | Denial of Service | 프로덕션 스키마 | mitigate | `execute_sql`은 `select`만, `db push`는 `--dry-run`만. baseline 11개 항목 전/후 대조 |
| T-37-SC | Tampering | 패키지 설치 | N/A | 이 Phase는 npm/pip/cargo 설치가 없다. Package Legitimacy Gate 비적용 |
</threat_model>

<verification>
Phase 37의 최종 판정은 CLI 출력 2개 + 스키마 무변경 증명이다.

1. `npx supabase migration list --linked` — local-only **0건**, remote-only **0건**
2. `npx supabase db push --dry-run` — 에러 없이 통과, 적용 대상 없음
3. `ls -1 supabase/migrations/ | cut -c1-14 | sort | uniq -d` — 빈 출력
4. baseline 11개 항목 전/후 **11/11 동일** (`37-00-SUMMARY.md` 착수 실측값 대조)
5. 원장 행 수가 Task 1 기준값 − 13 + (새로 기록한 applied 건수)와 일치
6. `npm run lint` 통과, `git status --porcelain src/` 빈 출력
7. 복원 파일 5개 및 `20260430000009_rls.sql` 무변경
</verification>

<success_criteria>
- remote 전용 13건이 원장에서 제거됨 (복원 5건은 applied 유지)
- 타임스탬프 중복 0건, 이동 파일 내용 무변경(rename만), 의존 순서 보존 근거 제시
- 새 version이 `repair --status applied`로 기록됨
- `migration list --linked` 0/0, `db push --dry-run` 통과
- baseline 11개 항목 무변경 (스키마 변경 0)
- `npm run lint` 통과, `src/**` 변경 0
- O-1·O-2가 SUMMARY에 별도 Phase 이월로 기록됨
</success_criteria>

<output>
`.planning/phases/37-migration-drift/37-01-SUMMARY.md` 를 작성한다. 반드시 포함할 것:

- `reverted` 처리한 13개 version 목록과 원장 행 수 전/후
- 리네이밍 3건의 **구 version → 신 version** 및 각 쌍에서 그 파일을 고른 근거
  (grep 결과 인용 — 어느 식별자가 어디서 참조되는지)
- `repair --status applied`로 기록한 version 목록 (중복 3쌍 외 항목이 있었다면 그 판정 근거 포함)
- `migration list --linked` 최종 출력 (0/0) 과 `db push --dry-run` 최종 출력
- **baseline 11개 항목 전/후 대조 표** (스키마 무변경 증명)
- 🔴 **별도 Phase 이월 기록**:
  - **O-1**: `recommend_hagwons` 오버로드 2개 공존 (`p_fee_tier text` / `p_fee_tiers text[]`).
    `phase28_subject_v2`가 구버전을 DROP하지 않아 인자 미명시 호출 시 모호성 에러 가능.
    선행 작업 — `src/lib/**/hagwon*` 호출 시그니처 확인
  - **O-2**: `TO` 절 누락 정책 (`regional_income`, `complex_area_types`, `site_admin_roles`,
    `ad_campaigns` realtrade-story 정책 3개, `presale_discoveries`, `new_listings`,
    `cardnews-payloads` 3개). 프로덕션·로컬 **동시** 변경 방식으로 처리해야 함
- `docs/FEATURES.json` 갱신 여부 판단 (마이그레이션 위생 작업이므로 해당 없으면 "해당 없음" 명시)
</output>
