---
phase: 31-admin-cardnews-builder
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/services/github-actions.ts
  - src/lib/cardnews/card-templates.ts
  - src/app/api/admin/cardnews/data/route.ts
  - src/app/api/admin/cardnews/generate-html/route.ts
  - src/app/api/admin/cardnews/ai-text/route.ts
autonomous: true
requirements: [BILD-01, BILD-02, BILD-03]

user_setup:
  - service: supabase-storage
    why: "HTML 페이로드를 GitHub Actions가 다운로드할 public URL로 저장 (Plan 03에서 사용)"
    dashboard_config:
      - task: "cardnews-payloads 버킷 생성 (Public: ON)"
        location: "Supabase Dashboard → Storage → New bucket → Name: cardnews-payloads, Public: 체크"
  - service: github
    why: "GitHub Actions workflow_dispatch 트리거 및 스케줄러 enable/disable"
    env_vars:
      - name: GITHUB_PAT
        source: "GitHub → Settings → Developer Settings → Personal access tokens → Fine-grained tokens. 권한: Actions (Read and write), Metadata (Read-only). Repository: nickujung-art/bds"
    dashboard_config:
      - task: "Fine-grained PAT 생성"
        location: "https://github.com/settings/personal-access-tokens/new"
      - task: "GITHUB_PAT를 Vercel 환경변수에 등록"
        location: "Vercel Dashboard → Project → Settings → Environment Variables → GITHUB_PAT"

must_haves:
  truths:
    - "POST /api/admin/cardnews/data가 기간·주제·지역·평형 옵션으로 랭킹 데이터를 반환한다 (BILD-01)"
    - "데이터 API가 최소 거래 3건 미만 단지를 제외하고 200% 이상치를 필터링한다 (D-04)"
    - "POST /api/admin/cardnews/generate-html가 4장 HTML을 CDN 폰트로 생성 반환한다 (BILD-02)"
    - "POST /api/admin/cardnews/ai-text가 Groq로 제목·캡션·SNS·해시태그를 생성한다 (BILD-03)"
    - "AI API가 Groq 실패 시 fallback:true를 반환하고 500을 throw하지 않는다 (D-05)"
    - "모든 API Route가 어드민 role 없는 요청에 401/403을 반환한다"
    - "GITHUB_PAT는 process.env로만 접근하고 응답 body에 포함되지 않는다"
  artifacts:
    - path: "src/services/github-actions.ts"
      provides: "GitHub Actions API 외부 어댑터 (CLAUDE.md CRITICAL)"
      exports: ["triggerWorkflow", "getLatestWorkflowRun", "getRunArtifacts", "getArtifactDownloadUrl", "setWorkflowEnabled", "getWorkflowState"]
    - path: "src/lib/cardnews/card-templates.ts"
      provides: "CDN 폰트 기반 카드 HTML 렌더 함수 (iframe 미리보기용)"
      exports: ["renderCoverPreview", "renderHighlightPreview", "renderRankingPreview", "renderClosingPreview"]
      contains: "cdn.jsdelivr.net/gh/orioncactus/pretendard"
    - path: "src/app/api/admin/cardnews/data/route.ts"
      provides: "실거래 집계 API (BILD-01)"
      exports: ["POST"]
    - path: "src/app/api/admin/cardnews/generate-html/route.ts"
      provides: "카드 HTML 생성 API (BILD-02)"
      exports: ["POST"]
    - path: "src/app/api/admin/cardnews/ai-text/route.ts"
      provides: "Groq AI 텍스트 생성 API (BILD-03)"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/admin/cardnews/data/route.ts"
      to: "Supabase transactions 테이블"
      via: "createSupabaseAdminClient().from('transactions')"
      pattern: "cancel_date.*null.*superseded_by.*null"
    - from: "src/app/api/admin/cardnews/generate-html/route.ts"
      to: "src/lib/cardnews/card-templates.ts"
      via: "renderCoverPreview() import"
      pattern: "from '@/lib/cardnews/card-templates'"
    - from: "src/app/api/admin/cardnews/ai-text/route.ts"
      to: "Groq API"
      via: "groq.chat.completions.create"
      pattern: "llama-3.3-70b-versatile"
---

<objective>
백엔드 서비스 어댑터와 핵심 API Route 3개를 구현한다.

Purpose: GitHub API 어댑터(src/services/github-actions.ts)와 카드 HTML 렌더 TypeScript 버전(src/lib/cardnews/card-templates.ts)을 먼저 정의해 하위 API들이 참조할 계약을 확립한다. 그 위에 집계 API(BILD-01), HTML 생성 API(BILD-02), AI 텍스트 API(BILD-03)를 구현한다.

Output:
- src/services/github-actions.ts: GitHub Actions API 외부 어댑터 (CLAUDE.md CRITICAL)
- src/lib/cardnews/card-templates.ts: CDN 폰트 기반 HTML 렌더 함수
- src/app/api/admin/cardnews/data/route.ts: 집계 API
- src/app/api/admin/cardnews/generate-html/route.ts: HTML 생성 API
- src/app/api/admin/cardnews/ai-text/route.ts: Groq AI 텍스트 API
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-admin-cardnews-builder/31-CONTEXT.md
@.planning/phases/31-admin-cardnews-builder/31-RESEARCH.md
@.planning/phases/31-admin-cardnews-builder/31-PATTERNS.md
@.planning/phases/31-admin-cardnews-builder/31-01-SUMMARY.md
</context>

<interfaces>
<!-- 기존 코드베이스 패턴 — Executor가 직접 참조해야 하는 계약 -->

어드민 API Route 권한 검증 패턴 (src/app/api/admin/gps-approve/route.ts 기준):
```typescript
export const runtime = 'nodejs'

export async function POST(request: Request): Promise<NextResponse> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { data: profile } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', user.id)
    .single()

  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }
  // ... 이후 로직
}
```

Groq SDK 패턴 (src/app/actions/hagwon.ts 기준):
```typescript
import Groq from 'groq-sdk'
const groq = new Groq({ apiKey: process.env.GROQ_API_KEY })
const res = await groq.chat.completions.create({
  model: 'llama-3.3-70b-versatile',
  messages: [{ role: 'user', content: prompt }],
  max_tokens: 400,
  temperature: 0.7,
})
const text = res.choices[0]?.message?.content?.trim() ?? FALLBACK
```

Supabase Admin Client 패턴:
```typescript
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { createSupabaseServerClient } from '@/lib/supabase/server'
```

거래 쿼리 필수 조건 (CLAUDE.md CRITICAL):
```typescript
.is('cancel_date', null)
.is('superseded_by', null)
```

Plan 01에서 생성한 card-news/scripts/fetch-data.js exports:
- fetchJeonseRanking, fetchAllTimeHighRanking, fetchPriceChangeRanking
- fetchAreaRanking, fetchCityRanking, fetchVolumeRanking, fetchValueRanking, fetchDistrictChampions
- getDateRange, filterOutliers, formatPrice
Note: 이 함수들은 Supabase 클라이언트를 직접 초기화하므로 API Route에서는 사용하지 않고 동일 로직을 TypeScript로 재구현한다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: github-actions.ts 서비스 어댑터 + card-templates.ts 렌더 함수</name>
  <files>
    src/services/github-actions.ts,
    src/lib/cardnews/card-templates.ts
  </files>
  <read_first>
    - src/services/molit.ts (얇은 래퍼 어댑터 패턴 — 구조 참조)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pattern 4~7, Pattern 8)
    - .planning/phases/31-admin-cardnews-builder/31-PATTERNS.md (github-actions.ts 패턴 섹션)
    - card-news/scripts/templates.js (renderXxx 함수 로직 — TypeScript로 포팅할 원본)
  </read_first>
  <behavior>
    - triggerWorkflow({ owner, repo, workflowId, ref, inputs }) → Promise<void>, 성공 시 void
    - GITHUB_PAT 미설정 시 throw new Error('GITHUB_PAT not configured')
    - getLatestWorkflowRun(owner, repo, workflowId) → WorkflowRun | null
    - getArtifactDownloadUrl(owner, repo, artifactId) → string | null (S3 redirect URL)
    - setWorkflowEnabled(owner, repo, workflowId, true/false) → Promise<void>
    - getWorkflowState(owner, repo, workflowId) → 'active' | 'disabled_manually' | string
    - renderCoverPreview(data) 반환 HTML에 cdn.jsdelivr.net URL 포함
    - renderClosingPreview 반환 HTML에 '출처: 국토교통부 실거래가 공개시스템' 포함
  </behavior>
  <action>
**src/services/github-actions.ts (신규):**

```typescript
/**
 * GitHub Actions API 어댑터
 * CLAUDE.md CRITICAL: 외부 API는 src/services/ 어댑터 전용 — 컴포넌트/Route 직접 호출 금지
 */

const GH_API = 'https://api.github.com'

function getToken(): string {
  const token = process.env.GITHUB_PAT
  if (!token) throw new Error('GITHUB_PAT not configured')
  return token
}

function ghHeaders(token: string): HeadersInit {
  return {
    Authorization: `Bearer ${token}`,
    Accept: 'application/vnd.github+json',
    'X-GitHub-Api-Version': '2022-11-28',
    'Content-Type': 'application/json',
  }
}

export interface WorkflowRun {
  id: number
  status: string
  conclusion: string | null
  created_at: string
  html_url: string
  name: string
}

export interface WorkflowArtifact {
  id: number
  name: string
  size_in_bytes: number
  created_at: string
}

export async function triggerWorkflow(params: {
  owner: string
  repo: string
  workflowId: string
  ref: string
  inputs: Record<string, string>
}): Promise<void> {
  const token = getToken()
  const res = await fetch(
    `${GH_API}/repos/${params.owner}/${params.repo}/actions/workflows/${params.workflowId}/dispatches`,
    {
      method: 'POST',
      headers: ghHeaders(token),
      body: JSON.stringify({ ref: params.ref, inputs: params.inputs }),
    },
  )
  if (!res.ok && res.status !== 204) {
    const body = await res.text().catch(() => '')
    throw new Error(`GitHub API dispatch error: ${res.status} ${body}`)
  }
}

export async function getLatestWorkflowRun(
  owner: string, repo: string, workflowId: string
): Promise<WorkflowRun | null> {
  const token = getToken()
  const res = await fetch(
    `${GH_API}/repos/${owner}/${repo}/actions/runs?workflow_id=${workflowId}&event=workflow_dispatch&per_page=5`,
    { headers: ghHeaders(token) },
  )
  if (!res.ok) return null
  const json = await res.json() as { workflow_runs: WorkflowRun[] }
  return json.workflow_runs[0] ?? null
}

export async function getRunArtifacts(
  owner: string, repo: string, runId: number
): Promise<WorkflowArtifact[]> {
  const token = getToken()
  const res = await fetch(
    `${GH_API}/repos/${owner}/${repo}/actions/runs/${runId}/artifacts`,
    { headers: ghHeaders(token) },
  )
  if (!res.ok) return []
  const json = await res.json() as { artifacts: WorkflowArtifact[] }
  return json.artifacts
}

export async function getArtifactDownloadUrl(
  owner: string, repo: string, artifactId: number
): Promise<string | null> {
  const token = getToken()
  const res = await fetch(
    `${GH_API}/repos/${owner}/${repo}/actions/artifacts/${artifactId}/zip`,
    { method: 'GET', headers: ghHeaders(token), redirect: 'manual' },
  )
  // GitHub returns 302/303 redirect → S3 pre-signed URL (인증 불필요)
  return res.headers.get('location')
}

export async function setWorkflowEnabled(
  owner: string, repo: string, workflowId: string, enabled: boolean
): Promise<void> {
  const token = getToken()
  const action = enabled ? 'enable' : 'disable'
  const res = await fetch(
    `${GH_API}/repos/${owner}/${repo}/actions/workflows/${workflowId}/${action}`,
    { method: 'PUT', headers: ghHeaders(token) },
  )
  if (!res.ok) throw new Error(`Failed to ${action} workflow: ${res.status}`)
}

export async function getWorkflowState(
  owner: string, repo: string, workflowId: string
): Promise<string> {
  const token = getToken()
  const res = await fetch(
    `${GH_API}/repos/${owner}/${repo}/actions/workflows/${workflowId}`,
    { headers: ghHeaders(token) },
  )
  if (!res.ok) throw new Error(`Failed to get workflow state: ${res.status}`)
  const data = await res.json() as { state: string }
  return data.state
}
```

**src/lib/cardnews/card-templates.ts (신규):**

card-news/scripts/templates.js의 렌더 로직을 TypeScript로 포팅. CDN 폰트 사용.
핵심 차이점: BASE_CSS를 CDN @import 방식으로 교체, LOGO_PATH를 빈 문자열로 (onerror 처리 있음).

```typescript
/**
 * card-templates.ts — 브라우저 iframe 미리보기용 카드 HTML 생성
 * card-news/scripts/templates.js의 TypeScript 포팅 (CDN 폰트 버전)
 */

// 브라우저 iframe에서 file:// 접근 불가 → CDN으로 교체 (PITFALL-2)
const BASE_CSS_PREVIEW = `
  @import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css');
  :root {
    --brand: #0066FF; --brand-tint: #EAF2FE; --ink: #152038; --ink-2: #5B6677;
    --ink-3: #8A93A3; --gold: #FFC93C; --gold-2: #FFAB00; --surface: #FFFFFF;
    --surface-2: #F7F8FA; --line: rgba(112,115,124,0.18); --placeholder: #C4CAD3;
  }
  * { margin: 0; padding: 0; box-sizing: border-box; }
  html, body { width: 1080px; height: 1080px; overflow: hidden; }
  body { font-family: 'Pretendard', -apple-system, 'Apple SD Gothic Neo', sans-serif; }
`

function buildHtml(body: string, css: string): string {
  return `<!DOCTYPE html><html lang="ko"><head><meta charset="UTF-8">
<style>${BASE_CSS_PREVIEW}${css}</style></head><body>${body}</body></html>`
}

// RankingRow 타입 (API response와 동일)
export interface RankingRow {
  rank: number
  name: string | null
  subtitle?: string | null
  price: string | null
  priceUnit?: string
}

export interface CoverData {
  week: string
  region: string
  area: string | null
  subCaption?: string
  period?: string
}

export interface CardSetData {
  week: string
  region: string
  area: string | null
  period: string
  source: string
  ranking: RankingRow[]
  seriesType?: string
  subCaption?: string
}

// templates.js의 pad10 함수
function pad10(ranking: RankingRow[]): RankingRow[] {
  return Array.from({ length: 10 }, (_, i) => ranking[i] ?? { rank: i + 1, name: null, price: null })
}

// BrandLockup HTML helper
function BrandLockup({ size = 46, fontSize = 30, color = 'var(--ink)' } = {}): string {
  return `<div style="display:flex;align-items:center;gap:12px;">
    <img src="" style="width:${size}px;height:${size}px;border-radius:10px;object-fit:cover;" onerror="this.style.display='none'">
    <span style="font:700 ${fontSize}px/1 'Pretendard';color:${color};">창원부동산랩</span>
  </div>`
}

export function renderCoverPreview(data: CoverData): string {
  // templates.js renderCover 로직과 동일 — 위치·CSS·HTML 구조 그대로
  // (구현은 card-news/scripts/templates.js의 renderCover 참조, BASE_CSS_PREVIEW로 교체)
  const { week, region, area, subCaption } = data
  const caption = subCaption ?? `이번 주 가장 비싸게 거래된\n국민평형 아파트는 어디일까요?`
  const css = `
    .card { width:1080px; height:1080px; background:#fff; position:relative; overflow:hidden; }
    .top-bar { width:100%; height:14px; background:var(--brand); }
    .brand { position:absolute; top:40px; left:80px; }
    .eyebrow { position:absolute; top:124px; left:80px; font:700 28px/1 'Pretendard'; color:var(--brand); letter-spacing:0.5px; }
    .title { position:absolute; top:172px; left:80px; z-index:1; }
    .title-line { font:900 100px/1.08 'Pretendard'; color:var(--ink); letter-spacing:-3.5px; display:block; }
    .title-blue { color:var(--brand); }
    .ghost { position:absolute; bottom:-120px; right:-30px; font:900 560px/1 'Pretendard'; color:var(--brand-tint); z-index:0; user-select:none; white-space:nowrap; }
    .caption { position:absolute; bottom:138px; left:80px; z-index:1; font:500 30px/1.6 'Pretendard'; color:var(--ink-2); white-space:pre-line; }
  `
  const body = `<div class="card">
    <div class="top-bar"></div>
    <div class="brand">${BrandLockup()}</div>
    <div class="eyebrow">WEEKLY REPORT · ${week}</div>
    <div class="title">
      <span class="title-line">${region}</span>
      <span class="title-line">${area ? `<span class="title-blue">${area}</span> 실거래가` : '전체 실거래가'}</span>
      <span class="title-line">랭킹 TOP 10</span>
    </div>
    <div class="ghost">10</div>
    <div class="caption">${caption}</div>
  </div>`
  return buildHtml(body, css)
}

export function renderHighlightPreview(data: CardSetData): string {
  // templates.js renderHighlight 로직과 동일
  // (상세 구현은 card-news/scripts/templates.js 참조)
  const { week, region, area, period, source, ranking } = data
  const r = pad10(ranking)
  const top3 = r.slice(0, 3)
  // ... (renderHighlight 동일 CSS + HTML 구현)
  return buildHtml(`<!-- highlight card for ${region} ${week} -->`, '')
}

export function renderRankingPreview(data: CardSetData): string {
  // templates.js renderRanking 로직과 동일
  return buildHtml(`<!-- ranking card -->`, '')
}

export function renderClosingPreview(data: Partial<CardSetData>): string {
  // templates.js renderClosing 로직과 동일
  // D-08 LOCKED: 법적 표기 2줄 반드시 포함
  const css = `
    .card { width:1080px; height:1080px; background:var(--ink); position:relative; display:flex; flex-direction:column; padding:80px; }
    .disclaimer { font:500 19px/1.6 'Pretendard'; color:rgba(255,255,255,0.3); border-top:1px solid rgba(255,255,255,0.1); padding-top:20px; }
  `
  const body = `<div class="card">
    <div style="margin-bottom:auto;padding-bottom:80px;">${BrandLockup({ size: 56, fontSize: 34, color: '#fff' })}</div>
    <div style="font:900 76px/1.15 'Pretendard';color:#fff;letter-spacing:-2.5px;margin-bottom:28px;">
      매주 업데이트되는<br>창원 <span style="color:var(--gold);">실거래가 리포트</span>
    </div>
    <div style="font:500 30px/1.7 'Pretendard';color:rgba(255,255,255,0.7);margin-bottom:48px;">
      우리 동네 아파트 시세가 궁금하다면<br>창원부동산랩을 팔로우하세요.
    </div>
    <div class="disclaimer">
      출처: 국토교통부 실거래가 공개시스템<br>
      본 데이터는 신고 기준이며 실제 거래와 차이가 있을 수 있습니다
    </div>
  </div>`
  return buildHtml(body, css)
}
```

Note: renderHighlightPreview, renderRankingPreview는 실제 구현 시 card-news/scripts/templates.js의 해당 함수를 그대로 TypeScript로 전사한다 (CSS + HTML 구조 완전 동일, BASE_CSS_PREVIEW만 교체).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "(src/services/github-actions|src/lib/cardnews)" | head -20; echo "Type check done"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function triggerWorkflow" src/services/github-actions.ts → 1
    - grep -c "export async function setWorkflowEnabled" src/services/github-actions.ts → 1
    - grep -c "export async function getWorkflowState" src/services/github-actions.ts → 1
    - grep -c "GITHUB_PAT not configured" src/services/github-actions.ts → 1
    - grep -c "export function renderCoverPreview" src/lib/cardnews/card-templates.ts → 1
    - grep -c "export function renderClosingPreview" src/lib/cardnews/card-templates.ts → 1
    - grep -c "cdn.jsdelivr.net/gh/orioncactus/pretendard" src/lib/cardnews/card-templates.ts → 1
    - grep -c "국토교통부 실거래가 공개시스템" src/lib/cardnews/card-templates.ts → 1
    - grep -c "신고 기준이며 실제 거래와 차이가 있을 수 있습니다" src/lib/cardnews/card-templates.ts → 1
  </acceptance_criteria>
  <done>github-actions.ts에 7개 함수가 export되고, card-templates.ts에 CDN 폰트 기반 미리보기 함수 4개가 export된다. 두 파일 모두 TypeScript 타입 검증을 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/admin/cardnews/data — 집계 API (BILD-01)</name>
  <files>src/app/api/admin/cardnews/data/route.ts</files>
  <read_first>
    - src/app/api/admin/gps-approve/route.ts (어드민 권한 검증 패턴)
    - src/app/api/admin/realtors/upload-image/route.ts (createSupabaseAdminClient 패턴)
    - .planning/phases/31-admin-cardnews-builder/31-CONTEXT.md (D-04, D-09, D-11)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (fetch-data.js 확장 설계, 기간 옵션 계산 로직)
  </read_first>
  <behavior>
    - 인증되지 않은 요청 → 401
    - admin/superadmin 이외 role → 403
    - 잘못된 body (sggCodes 빈 배열 등) → 400
    - topic='sale_top', sggCodes=['48123'], areaMin=80, areaMax=95, period='weekly' → { data: RankingRow[], from, to, warning: false }
    - 종료일 7일 이내 period → warning: true (D-04)
    - deal_type='jeonse' 지정 시 전세 거래만 집계
  </behavior>
  <action>
```typescript
// src/app/api/admin/cardnews/data/route.ts
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { z } from 'zod'

export const runtime = 'nodejs'

// D-09 Locked 옵션 스펙 기반 Zod 스키마
const RequestSchema = z.object({
  period: z.enum(['weekly', 'monthly', 'quarterly', 'yearly', 'custom']),
  topic: z.enum(['sale_top', 'jeonse_top', 'volume', 'value', 'alltime_high', 'price_change', 'district_champions']),
  sggCodes: z.array(z.string()).min(1),
  areaMin: z.number().min(0),
  areaMax: z.number().max(300),
  customFrom: z.string().optional(),
  customTo: z.string().optional(),
  dealType: z.enum(['sale', 'jeonse']).optional(),
})

type RankingRow = { rank: number; name: string | null; subtitle?: string | null; price: string | null; priceUnit?: string }

// 기간 계산 (RESEARCH.md 기간 옵션 계산 로직)
function getDateRange(period: string, customFrom?: string, customTo?: string): { from: string; to: string } {
  if (period === 'custom') return { from: customFrom ?? '', to: customTo ?? '' }
  const now = new Date()
  const yesterday = new Date(now.getTime() - 86400000).toISOString().slice(0, 10)
  if (period === 'weekly') {
    const dow = now.getDay()
    const daysToLastMon = dow === 0 ? 13 : dow + 6
    const mon = new Date(now); mon.setDate(now.getDate() - daysToLastMon); mon.setHours(0,0,0,0)
    const sun = new Date(mon); sun.setDate(mon.getDate() + 6)
    return { from: mon.toISOString().slice(0, 10), to: sun.toISOString().slice(0, 10) }
  }
  if (period === 'monthly') return { from: new Date(now.getFullYear(), now.getMonth(), 1).toISOString().slice(0, 10), to: yesterday }
  if (period === 'quarterly') {
    const q = Math.floor(now.getMonth() / 3)
    return { from: new Date(now.getFullYear(), q * 3, 1).toISOString().slice(0, 10), to: yesterday }
  }
  return { from: `${now.getFullYear()}-01-01`, to: yesterday }  // yearly
}

// 데이터 완결성 경고: 종료일 7일 이내 (D-04)
function isDataIncomplete(to: string): boolean {
  return Math.ceil((new Date().getTime() - new Date(to).getTime()) / (1000 * 60 * 60 * 24)) <= 7
}

// 가격 포맷 (formatPrice from fetch-data.js 동일)
function formatPrice(manwon: number): string {
  const eok = Math.floor(manwon / 10000), rem = manwon % 10000
  if (eok === 0) return `${manwon.toLocaleString('ko-KR')}만`
  if (rem === 0) return `${eok}억`
  return `${eok}억 ${rem.toLocaleString('ko-KR')}`
}

// 이상치 필터 (D-04 — 12개월 평균 200% 초과 제외 / PITFALL-6)
async function filterOutliers(
  transactions: Array<{ complex_id: string; price: number }>,
  dealType: string,
  adminClient: ReturnType<typeof createSupabaseAdminClient>
) {
  if (!transactions.length) return transactions
  const ids = [...new Set(transactions.map(t => t.complex_id).filter(Boolean))]
  const twelveMonthsAgo = new Date(); twelveMonthsAgo.setFullYear(twelveMonthsAgo.getFullYear() - 1)
  const { data: hist } = await adminClient.from('transactions')
    .select('complex_id, price')
    .is('cancel_date', null).is('superseded_by', null)
    .eq('deal_type', dealType)
    .gte('deal_date', twelveMonthsAgo.toISOString().slice(0, 10))
    .in('complex_id', ids).limit(50000)
  const sumMap = new Map<string, { sum: number; count: number }>()
  for (const t of hist ?? []) {
    const cur = sumMap.get(t.complex_id) ?? { sum: 0, count: 0 }
    sumMap.set(t.complex_id, { sum: cur.sum + t.price, count: cur.count + 1 })
  }
  const avgMap = new Map<string, number>()
  for (const [id, { sum, count }] of sumMap) avgMap.set(id, sum / count)
  return transactions.filter(t => { const avg = avgMap.get(t.complex_id); return !avg || t.price <= avg * 2 })
}

export async function POST(request: Request): Promise<NextResponse> {
  // 어드민 권한 검증 (gps-approve/route.ts 패턴)
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  let body: unknown
  try { body = await request.json() } catch { return NextResponse.json({ error: 'invalid body' }, { status: 400 }) }
  const parsed = RequestSchema.safeParse(body)
  if (!parsed.success) return NextResponse.json({ error: parsed.error.message }, { status: 400 })

  const { period, topic, sggCodes, areaMin, areaMax, customFrom, customTo, dealType = 'sale' } = parsed.data
  const { from, to } = getDateRange(period, customFrom, customTo)
  const warning = isDataIncomplete(to)
  const adminClient = createSupabaseAdminClient()

  try {
    // 주제별 집계 쿼리 (CLAUDE.md: cancel_date IS NULL AND superseded_by IS NULL 필수)
    let result: RankingRow[] = []

    if (topic === 'sale_top' || topic === 'jeonse_top') {
      const dt = topic === 'jeonse_top' ? 'jeonse' : (dealType as string)
      const { data: raw } = await adminClient.from('transactions')
        .select('complex_id, price, area_m2, sgg_code')
        .is('cancel_date', null).is('superseded_by', null)
        .eq('deal_type', dt)
        .gte('deal_date', from).lte('deal_date', to)
        .in('sgg_code', sggCodes)
        .gte('area_m2', areaMin).lte('area_m2', areaMax)
        .not('complex_id', 'is', null).limit(5000)

      const filtered = await filterOutliers(
        (raw ?? []).map(t => ({ complex_id: t.complex_id as string, price: t.price as number })),
        dt, adminClient
      )
      // 단지별 최고가 맵
      const map = new Map<string, number>()
      for (const t of filtered) {
        if (!map.has(t.complex_id) || t.price > (map.get(t.complex_id) ?? 0)) map.set(t.complex_id, t.price)
      }
      // 3건 미만 제외 (D-04)
      const countMap = new Map<string, number>()
      for (const t of filtered) countMap.set(t.complex_id, (countMap.get(t.complex_id) ?? 0) + 1)
      const sorted = [...map.entries()].filter(([id]) => (countMap.get(id) ?? 0) >= 3)
        .sort(([, a], [, b]) => b - a).slice(0, 10)
      // 단지명 조회
      const cmap = await fetchComplexNames(sorted.map(([id]) => id), adminClient)
      result = sorted.map(([id, price], i) => ({
        rank: i + 1,
        name: (cmap.get(id) as { canonical_name: string } | undefined)?.canonical_name ?? null,
        subtitle: (cmap.get(id) as { gu?: string; si?: string } | undefined)?.gu ?? null,
        price: formatPrice(price),
      }))
    }
    // volume, value, alltime_high, price_change, district_champions 주제도 동일 패턴으로 구현
    // (fetch-data.js의 해당 함수 로직을 TypeScript로 포팅, adminClient 사용)

    return NextResponse.json({ data: result, from, to, warning, topic })
  } catch (err) {
    return NextResponse.json({ error: err instanceof Error ? err.message : 'DB error' }, { status: 500 })
  }
}

async function fetchComplexNames(ids: string[], adminClient: ReturnType<typeof createSupabaseAdminClient>) {
  if (!ids.length) return new Map()
  const { data } = await adminClient.from('complexes').select('id, canonical_name, si, gu').in('id', ids)
  return new Map((data ?? []).map((c: { id: string }) => [c.id, c]))
}
```

Note: volume / value / alltime_high / price_change / district_champions 주제들도 fetch-data.js 로직을 동일하게 TypeScript로 포팅한다. 단 supabase 클라이언트 대신 `adminClient` 사용.
  </action>
  <verify>
    <automated>npm run lint -- --file src/app/api/admin/cardnews/data/route.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export const runtime = 'nodejs'" src/app/api/admin/cardnews/data/route.ts → 1
    - grep -c "export async function POST" src/app/api/admin/cardnews/data/route.ts → 1
    - grep -c "cancel_date.*null" src/app/api/admin/cardnews/data/route.ts → 존재
    - grep -c "superseded_by.*null" src/app/api/admin/cardnews/data/route.ts → 존재
    - grep -c "avg \* 2" src/app/api/admin/cardnews/data/route.ts → 1 (200% 이상치 필터)
    - grep -c "status: 401" src/app/api/admin/cardnews/data/route.ts → 1
    - grep -c "status: 403" src/app/api/admin/cardnews/data/route.ts → 1
    - grep -c "z.enum" src/app/api/admin/cardnews/data/route.ts → 2 이상 (Zod 검증)
    - grep -c "warning" src/app/api/admin/cardnews/data/route.ts → 존재 (D-04 경고)
  </acceptance_criteria>
  <done>POST /api/admin/cardnews/data가 이상치 필터·최소 거래 필터·권한 검증을 포함한 집계 API로 동작한다.</done>
</task>

<task type="auto">
  <name>Task 3: HTML 생성 API + AI 텍스트 API (BILD-02, BILD-03)</name>
  <files>
    src/app/api/admin/cardnews/generate-html/route.ts,
    src/app/api/admin/cardnews/ai-text/route.ts
  </files>
  <read_first>
    - src/app/actions/hagwon.ts (Groq SDK 사용 패턴 — 최우선)
    - src/app/api/admin/ad-copy-review/route.ts (JSON 파싱 + AI 호출 패턴)
    - src/lib/cardnews/card-templates.ts (Plan 02 Task 1에서 생성 — 사용할 함수 시그니처)
    - .planning/phases/31-admin-cardnews-builder/31-CONTEXT.md (D-03, D-05, D-09, D-10)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pattern 1 Groq 패턴)
  </read_first>
  <action>
**src/app/api/admin/cardnews/generate-html/route.ts:**

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { z } from 'zod'
import {
  renderCoverPreview, renderHighlightPreview,
  renderRankingPreview, renderClosingPreview,
} from '@/lib/cardnews/card-templates'

export const runtime = 'nodejs'

const RequestSchema = z.object({
  ranking: z.array(z.object({
    rank: z.number(),
    name: z.string().nullable(),
    subtitle: z.string().nullable().optional(),
    price: z.string().nullable(),
    priceUnit: z.string().optional(),
  })),
  week: z.string(),
  region: z.string(),
  area: z.string().nullable(),
  period: z.string(),
  source: z.string().default('국토교통부 실거래가 공개시스템'),
  topic: z.string().optional(),
})

export async function POST(request: Request): Promise<NextResponse> {
  // 어드민 권한 검증 (gps-approve/route.ts 패턴)
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  let body: unknown
  try { body = await request.json() } catch { return NextResponse.json({ error: 'invalid body' }, { status: 400 }) }
  const parsed = RequestSchema.safeParse(body)
  if (!parsed.success) return NextResponse.json({ error: parsed.error.message }, { status: 400 })

  const data = parsed.data
  try {
    const html = {
      cover:     renderCoverPreview({ week: data.week, region: data.region, area: data.area }),
      highlight: renderHighlightPreview({ ...data, ranking: data.ranking }),
      ranking:   renderRankingPreview({ ...data, ranking: data.ranking }),
      closing:   renderClosingPreview({}),
    }
    return NextResponse.json({ html })
  } catch (err) {
    return NextResponse.json({ error: err instanceof Error ? err.message : 'template error' }, { status: 500 })
  }
}
```

**src/app/api/admin/cardnews/ai-text/route.ts:**

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import Groq from 'groq-sdk'
import { z } from 'zod'

export const runtime = 'nodejs'

const RequestSchema = z.object({
  topic: z.string(),
  region: z.string(),
  area: z.string().nullable(),
  period: z.string(),
  ranking: z.array(z.object({
    rank: z.number(),
    name: z.string().nullable(),
    price: z.string().nullable(),
  })),
  options: z.object({
    cover_caption: z.boolean().default(false),
    insight: z.boolean().default(false),
    sns_caption: z.boolean().default(false),
    hashtags: z.boolean().default(false),
    title_mode: z.enum(['ai', 'manual']).default('ai'),
  }),
})

const FALLBACK = { title: null, caption: null, insight: null, sns: null, hashtags: null }

export async function POST(request: Request): Promise<NextResponse> {
  // 어드민 권한 검증
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  let body: unknown
  try { body = await request.json() } catch { return NextResponse.json({ error: 'invalid body' }, { status: 400 }) }
  const parsed = RequestSchema.safeParse(body)
  if (!parsed.success) return NextResponse.json({ error: parsed.error.message }, { status: 400 })

  const { topic, region, area, period, ranking, options } = parsed.data

  // 숫자는 코드로 계산 후 프롬프트에 주입 (D-03)
  const top3 = ranking.slice(0, 3).map(r => `${r.rank}위. ${r.name ?? '데이터없음'} — ${r.price ?? '-'}`).join('\n')
  const prompt = `당신은 창원·김해 부동산 전문 SNS 콘텐츠 작성자입니다. 친근하고 전문적인 한국어로 작성하세요.

지역: ${region}${area ? ` / 전용 ${area}` : ''}
주제: ${topic}
기간: ${period}
TOP 3 데이터:
${top3}

다음 항목을 JSON 형식으로 작성하세요:
${options.title_mode === 'ai' ? '- title: 카드뉴스 제목 (20자 이내)' : ''}
${options.cover_caption ? '- caption: 커버 캡션 (50자 이내, 흥미 유발)' : ''}
${options.insight ? '- insight: 시황 인사이트 (100자 이내, 전문적)' : ''}
${options.sns_caption ? '- sns: SNS 캡션 (150자 이내, 이모지 1개 포함)' : ''}
${options.hashtags ? '- hashtags: 해시태그 5개 (공백으로 구분, # 포함)' : ''}

JSON만 반환 (마크다운 코드 블록 없이)`

  try {
    const groq = new Groq({ apiKey: process.env.GROQ_API_KEY })
    const res = await groq.chat.completions.create({
      model: 'llama-3.3-70b-versatile',  // D-03 LOCKED
      messages: [{ role: 'user', content: prompt }],
      max_tokens: 500,
      temperature: 0.7,
    })
    const raw = res.choices[0]?.message?.content?.trim() ?? ''
    let parsed_result: typeof FALLBACK = { ...FALLBACK }
    try {
      // JSON 파싱 시도
      const json = JSON.parse(raw.replace(/```[a-z]*\n?/g, '').replace(/```/g, '').trim())
      parsed_result = {
        title: json.title ?? null,
        caption: json.caption ?? null,
        insight: json.insight ?? null,
        sns: json.sns ?? null,
        hashtags: json.hashtags ?? null,
      }
    } catch {
      // JSON 파싱 실패 시 raw text를 title에 넣음
      parsed_result = { ...FALLBACK, title: raw.slice(0, 50) || null }
    }
    return NextResponse.json({ ...parsed_result, fallback: false })
  } catch {
    // D-05: AI 실패 시 차단 안 함 — fallback:true 반환, 재시도 버튼만
    return NextResponse.json({ ...FALLBACK, fallback: true })
  }
}
```
  </action>
  <verify>
    <automated>npm run lint -- --file src/app/api/admin/cardnews/generate-html/route.ts src/app/api/admin/cardnews/ai-text/route.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function POST" src/app/api/admin/cardnews/generate-html/route.ts → 1
    - grep -c "renderCoverPreview" src/app/api/admin/cardnews/generate-html/route.ts → 1
    - grep -c "from '@/lib/cardnews/card-templates'" src/app/api/admin/cardnews/generate-html/route.ts → 1
    - grep -c "export async function POST" src/app/api/admin/cardnews/ai-text/route.ts → 1
    - grep -c "llama-3.3-70b-versatile" src/app/api/admin/cardnews/ai-text/route.ts → 1
    - grep -c "GROQ_API_KEY" src/app/api/admin/cardnews/ai-text/route.ts → 1
    - grep -c "fallback: true" src/app/api/admin/cardnews/ai-text/route.ts → 1 (D-05: 실패 시 차단 안 함)
    - grep -c "status: 401" src/app/api/admin/cardnews/ai-text/route.ts → 1
  </acceptance_criteria>
  <done>POST /api/admin/cardnews/generate-html가 CDN 폰트 HTML 4장을 반환하고, POST /api/admin/cardnews/ai-text가 Groq 실패 시에도 500을 throw하지 않고 fallback:true를 반환한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저→API | 어드민 사용자의 옵션 입력이 API Route로 진입 |
| API→Supabase | transactions 집계 쿼리 |
| API→Groq | AI 텍스트 생성 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Elevation of Privilege | /api/admin/cardnews/* | mitigate | createSupabaseServerClient() + profile.role admin/superadmin 이중 검증, 모든 API Route 적용 |
| T-31-05 | Information Disclosure | GROQ_API_KEY | mitigate | process.env로만 접근, 응답 body에 키 미포함, 서버 런타임 전용 |
| T-31-06 | Information Disclosure | GITHUB_PAT | mitigate | process.env.GITHUB_PAT 서버 전용, getToken() helper에서만 접근 |
| T-31-07 | Tampering | AI 텍스트 → HTML 카드 삽입 | mitigate | card-templates.ts에서 AI 텍스트를 textContent 방식으로만 삽입 (innerHTML 금지) — Executor가 renderXxxPreview 구현 시 반드시 준수 |
| T-31-08 | Spoofing | 비어드민 API 접근 | mitigate | RSC 페이지 redirect guard + API Route 이중 방어 |
</threat_model>

<verification>
1. `npm run lint` 통과 (src/services/github-actions.ts, src/lib/cardnews/card-templates.ts, 3개 route.ts)
2. `curl -X POST /api/admin/cardnews/data` (인증 없이) → 401 반환
3. `curl -X POST /api/admin/cardnews/ai-text` with admin session → { title, caption, ..., fallback: false }
4. grep -r "국토교통부 실거래가 공개시스템" src/lib/cardnews/card-templates.ts → 존재
</verification>

<success_criteria>
- src/services/github-actions.ts: 7개 GitHub API 함수 export, GITHUB_PAT 미설정 시 에러
- src/lib/cardnews/card-templates.ts: CDN 폰트 + D-08 법적 표기 포함 HTML 렌더 4개 함수
- POST /api/admin/cardnews/data: 기간·주제·지역·평형 파라미터로 이상치 필터된 랭킹 반환
- POST /api/admin/cardnews/generate-html: { html: { cover, highlight, ranking, closing } } 반환
- POST /api/admin/cardnews/ai-text: Groq 실패 시 fallback:true 반환 (500 없음)
- `npm run lint && npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/31-admin-cardnews-builder/31-02-SUMMARY.md` 생성.
</output>
