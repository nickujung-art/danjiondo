---
phase: 31-admin-cardnews-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - card-news/scripts/fetch-data.js
  - card-news/scripts/templates.js
  - card-news/scripts/generate-from-payload.js
  - .github/workflows/weekly-generate.yml
  - .github/workflows/custom-cardnews.yml
autonomous: true
requirements: [BILD-07, BILD-08, BILD-04]

must_haves:
  truths:
    - "fetchJeonseRanking, fetchMonthlyRanking, fetchAllTimeHighRanking, fetchPriceChangeRanking 함수가 export된다"
    - "getDateRange(type) 함수가 'weekly'|'monthly'|'quarterly'|'yearly'|'custom' 타입을 처리한다"
    - "filterOutliers()가 12개월 평균가 대비 200% 초과 거래를 제거한다 (D-04)"
    - "renderClosing() 하단에 '출처: 국토교통부 실거래가 공개시스템'과 면책 문구 2줄이 하드코딩된다 (D-08)"
    - "BASE_CSS_PREVIEW 상수가 Pretendard CDN URL을 사용한다 (Pitfall-2)"
    - ".github/workflows/weekly-generate.yml이 루트에 존재해 GitHub Actions가 인식한다 (Pitfall-1)"
    - ".github/workflows/custom-cardnews.yml이 payload_url + series_id 입력을 받아 PNG를 생성한다"
    - "generate-from-payload.js가 JSON 페이로드를 읽어 4개 PNG를 card-news/output/에 생성한다"
  artifacts:
    - path: "card-news/scripts/fetch-data.js"
      provides: "전세/신고가/변동률 신규 쿼리 + 이상치 필터 + getDateRange"
      exports: ["fetchJeonseRanking", "fetchMonthlyRanking", "fetchAllTimeHighRanking", "fetchPriceChangeRanking", "getDateRange", "filterOutliers"]
    - path: "card-news/scripts/templates.js"
      provides: "CDN 폰트 미리보기 CSS + 법적 표기 업데이트"
      exports: ["BASE_CSS_PREVIEW", "renderCoverPreview", "renderHighlightPreview", "renderRankingPreview", "renderClosingPreview"]
      contains: "출처: 국토교통부 실거래가 공개시스템"
    - path: "card-news/scripts/generate-from-payload.js"
      provides: "Actions 전용 payload→PNG 변환"
    - path: ".github/workflows/weekly-generate.yml"
      provides: "루트 위치 주간 자동 생성 워크플로우 (PITFALL-1 해결)"
    - path: ".github/workflows/custom-cardnews.yml"
      provides: "빌더 전용 workflow_dispatch PNG 생성 (D-02)"
  key_links:
    - from: ".github/workflows/custom-cardnews.yml"
      to: "card-news/scripts/generate-from-payload.js"
      via: "node scripts/generate-from-payload.js --payload=payload.json"
      pattern: "generate-from-payload"
    - from: "card-news/scripts/templates.js"
      to: "renderClosing()"
      via: "disclaimer 하드코딩"
      pattern: "국토교통부 실거래가 공개시스템"
---

<objective>
card-news 스크립트 기반을 강화하고 GitHub Actions 워크플로우를 정비한다.

Purpose: 빌더 API에서 호출할 신규 쿼리 함수를 fetch-data.js에 추가하고(BILD-07), 법적 표기를 D-08 요구사항에 맞게 templates.js에 하드코딩하며(BILD-08), custom-cardnews.yml을 루트에 신규 생성하고 weekly-generate.yml을 루트로 이동해 GitHub Actions가 인식하도록 한다(PITFALL-1, D-02).

Output:
- fetch-data.js: getDateRange / filterOutliers / fetchJeonseRanking / fetchMonthlyRanking / fetchAllTimeHighRanking / fetchPriceChangeRanking
- templates.js: BASE_CSS_PREVIEW / renderXxxPreview 함수들 / renderClosing 법적 표기 수정
- generate-from-payload.js: 신규 Actions 전용 PNG 생성 스크립트
- .github/workflows/weekly-generate.yml: 루트에 신규 (PITFALL-1)
- .github/workflows/custom-cardnews.yml: 신규 빌더 전용 워크플로우
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-admin-cardnews-builder/31-CONTEXT.md
@.planning/phases/31-admin-cardnews-builder/31-RESEARCH.md
@.planning/phases/31-admin-cardnews-builder/31-PATTERNS.md
</context>

<interfaces>
<!-- 기존 파일에서 추출한 핵심 패턴 — 읽기 전 반드시 확인 -->

card-news/scripts/fetch-data.js (기존):
- `getLastWeekRange()` → { from: 'YYYY-MM-DD', to: 'YYYY-MM-DD' }
- `fetchAreaRanking({ sggCode, areaMin, areaMax, dealType='sale', limit=10 })` → RankingRow[]
- `fetchCityRanking({ sggCodes, dealType='sale', limit=10 })` → RankingRow[]
- `fetchComplexNames(ids)` → Map<id, { canonical_name, si, gu }>
- `formatPrice(manwon)` → '16억 4,000'
- 모든 쿼리: `.is('cancel_date', null).is('superseded_by', null)` 필수 (CLAUDE.md CRITICAL)

card-news/scripts/templates.js (기존):
- `BASE_CSS` = file:// URL 폰트 (Puppeteer용)
- `renderCover(data)`, `renderHighlight(data)`, `renderRanking(data)`, `renderClosing(data)` → HTML string
- `html(body, css='')` → 전체 HTML 문서 생성 helper
- renderClosing() 현재 disclaimer: "본 자료는 국토교통부 실거래가 공개시스템 기준입니다. 투자 판단의 최종 책임은 본인에게 있습니다." (D-08에 맞게 교체 필요)

card-news/scripts/capture.js:
- `captureCard(htmlString, outputPath)` → Promise<void> (Puppeteer 캡처)
- `closeBrowser()` → Promise<void>

card-news/scripts/generate.js:
- `generateCardSet(seriesId, data, dryRun)` 패턴 참조 (Cards 4장 생성)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetch-data.js 확장 — 신규 쿼리 함수 + 유틸 추가</name>
  <files>card-news/scripts/fetch-data.js</files>
  <read_first>
    - card-news/scripts/fetch-data.js (전체 — 기존 패턴 파악 필수)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pattern 9~12, Pitfall 6)
    - .planning/phases/31-admin-cardnews-builder/31-CONTEXT.md (D-04, D-09, D-11)
  </read_first>
  <behavior>
    - getDateRange('weekly') → 지난 주 월~일 반환 (getLastWeekRange와 동일)
    - getDateRange('monthly') → 이번 달 1일 ~ 어제
    - getDateRange('quarterly') → 이번 분기 시작일 ~ 어제
    - getDateRange('yearly') → 올해 1월 1일 ~ 어제
    - getDateRange('custom', '2026-06-01', '2026-06-14') → { from: '2026-06-01', to: '2026-06-14' }
    - filterOutliers(transactions, dealType) → 12개월 평균 200% 초과 거래 제거 후 반환
    - fetchJeonseRanking({ sggCodes, areaMin, areaMax, from, to, limit=10 }) → RankingRow[] (deal_type='jeonse')
    - fetchMonthlyRanking({ sggCodes, areaMin, areaMax, from, to, limit=10 }) → RankingRow[] (deal_type='monthly', 보증금 기준 정렬)
    - fetchAllTimeHighRanking: 집계 기간 내 최고가 > 이전 전체 기간 최고가인 단지만 포함
    - fetchPriceChangeRanking: 현재 기간 평균가 vs 직전 동일 기간 평균가 비율, 3건 미만 제외
  </behavior>
  <action>
기존 fetch-data.js에 다음을 추가한다.

1. **getDateRange(type, customFrom?, customTo?) 함수 추가:**
   ```javascript
   export function getDateRange(type, customFrom, customTo) {
     if (type === 'custom') return { from: customFrom, to: customTo }
     const now = new Date()
     const yesterday = new Date(now - 86400000).toISOString().slice(0, 10)
     if (type === 'weekly') return getLastWeekRange()
     if (type === 'monthly') {
       return { from: new Date(now.getFullYear(), now.getMonth(), 1).toISOString().slice(0, 10), to: yesterday }
     }
     if (type === 'quarterly') {
       const q = Math.floor(now.getMonth() / 3)
       return { from: new Date(now.getFullYear(), q * 3, 1).toISOString().slice(0, 10), to: yesterday }
     }
     if (type === 'yearly') {
       return { from: `${now.getFullYear()}-01-01`, to: yesterday }
     }
     throw new Error(`Unknown period type: ${type}`)
   }
   ```

2. **filterOutliers(transactions, dealType) 함수 추가 (PITFALL-6 해결책 — 12개월 별도 쿼리):**
   ```javascript
   export async function filterOutliers(transactions, dealType) {
     if (!transactions.length) return transactions
     const complexIds = [...new Set(transactions.map(t => t.complex_id).filter(Boolean))]
     if (!complexIds.length) return transactions
     const twelveMonthsAgo = new Date()
     twelveMonthsAgo.setFullYear(twelveMonthsAgo.getFullYear() - 1)
     const { data: historical } = await supabase
       .from('transactions')
       .select('complex_id, price')
       .is('cancel_date', null)
       .is('superseded_by', null)
       .eq('deal_type', dealType)
       .gte('deal_date', twelveMonthsAgo.toISOString().slice(0, 10))
       .in('complex_id', complexIds)
       .limit(50000)
     const sumMap = new Map()
     for (const t of historical ?? []) {
       const cur = sumMap.get(t.complex_id) ?? { sum: 0, count: 0 }
       sumMap.set(t.complex_id, { sum: cur.sum + t.price, count: cur.count + 1 })
     }
     const avgMap = new Map()
     for (const [id, { sum, count }] of sumMap) avgMap.set(id, sum / count)
     return transactions.filter(t => {
       const avg = avgMap.get(t.complex_id)
       if (!avg) return true
       return t.price <= avg * 2  // 200% 초과 제외 (D-04)
     })
   }
   ```

3. **fetchJeonseRanking({ sggCodes, areaMin, areaMax, from, to, limit=10 }) 추가:**
   - fetchCityRanking과 동일하되 `.eq('deal_type', 'jeonse')`, `from`/`to` 파라미터 직접 사용
   - filterOutliers 적용 후 단지당 3건 미만 제외 (D-04)
   - 반환: `[{ rank, name, subtitle, price }]`

4. **fetchMonthlyRanking({ sggCodes, areaMin, areaMax, from, to, limit=10 }) 추가 (D-09 LOCKED "월세최고보증금", D-11):**
   - `.eq('deal_type', 'monthly')`, 보증금(deposit_amount) 기준 내림차순 정렬
   - deposit_amount 컬럼 없으면 price 컬럼 사용 (DB 확인 후 선택)
   - filterOutliers 적용 후 단지당 3건 미만 제외 (D-04)
   - 반환: `[{ rank, name, subtitle, price }]` (price = 보증금 formatPrice 결과)

5. **fetchAllTimeHighRanking({ sggCodes, areaMin, areaMax, from, to, dealType='sale', limit=10 }) 추가:**
   - RESEARCH.md Pattern 9 참조
   - 집계 기간 내 단지별 최고가 맵 생성
   - 해당 단지들의 from 이전 전체 역사 최고가 조회
   - 현재 최고가 > 역대 최고가인 단지만 필터링
   - 3건 미만 제외 (D-04), filterOutliers 적용
   - 반환: `[{ rank, name, subtitle, price }]`

5. **fetchPriceChangeRanking({ sggCodes, areaMin, areaMax, from, to, dealType='sale', limit=10 }) 추가:**
   - RESEARCH.md Pattern 10 참조
   - 현재 기간 평균가 + 직전 동일 기간 평균가 병렬 계산 (Promise.all)
   - 양쪽 모두 3건 이상인 단지만 포함 (D-04)
   - 변동률 = (현재평균 - 직전평균) / 직전평균 × 100
   - 반환: `[{ rank, name, subtitle, price: '+5.3%', priceUnit: '' }]`

모든 신규 함수: `.is('cancel_date', null).is('superseded_by', null)` 필수 (CLAUDE.md CRITICAL).
기존 함수(fetchAreaRanking, fetchCityRanking 등)는 `from`/`to` 파라미터를 선택적으로 추가해 빌더가 기간을 직접 전달할 수 있도록 한다 (기본값: getLastWeekRange() 결과).
  </action>
  <verify>
    <automated>cd card-news && node --input-type=module -e "import { getDateRange, filterOutliers, fetchJeonseRanking, fetchAllTimeHighRanking, fetchPriceChangeRanking } from './scripts/fetch-data.js'; const r = getDateRange('monthly'); if (!r.from || !r.to) throw new Error('getDateRange failed'); console.log('fetch-data OK:', r)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function fetchJeonseRanking" card-news/scripts/fetch-data.js → 1
    - grep -c "export async function fetchMonthlyRanking" card-news/scripts/fetch-data.js → 1
    - grep -c "export async function fetchAllTimeHighRanking" card-news/scripts/fetch-data.js → 1
    - grep -c "export async function fetchPriceChangeRanking" card-news/scripts/fetch-data.js → 1
    - grep -c "export function getDateRange" card-news/scripts/fetch-data.js → 1
    - grep -c "export async function filterOutliers" card-news/scripts/fetch-data.js → 1
    - grep -c "avg \* 2" card-news/scripts/fetch-data.js → 1 (200% 필터)
    - grep -c "cancel_date" card-news/scripts/fetch-data.js → 모든 신규 함수에 포함 (최소 6)
  </acceptance_criteria>
  <done>fetch-data.js에 신규 5개 함수가 export되고, 모든 쿼리에 cancel_date/superseded_by 조건이 포함된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: templates.js 수정 — 법적 표기 D-08 + CDN 폰트 미리보기 버전</name>
  <files>card-news/scripts/templates.js</files>
  <read_first>
    - card-news/scripts/templates.js (전체 — renderClosing 함수 위치 파악 필수)
    - .planning/phases/31-admin-cardnews-builder/31-CONTEXT.md (D-08 정확한 문구)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pitfall 1, Pitfall 7, Pattern 8)
    - card-news/CARDDESIGN.md (법적 표기 위치 확인)
  </read_first>
  <behavior>
    - renderClosing()의 disclaimer 클래스 요소가 "출처: 국토교통부 실거래가 공개시스템" 텍스트를 포함한다
    - renderClosing()의 disclaimer 클래스 요소가 "본 데이터는 신고 기준이며 실제 거래와 차이가 있을 수 있습니다" 텍스트를 포함한다
    - BASE_CSS_PREVIEW는 @font-face src가 없고 대신 @import CDN URL을 사용한다
    - renderCoverPreview(data) 반환 HTML에 CDN URL이 포함된다 (file:// 없음)
  </behavior>
  <action>
1. **renderClosing() 법적 표기 수정 (D-08 LOCKED — 제거 불가):**
   기존 disclaimer div 내용:
   ```
   "본 자료는 국토교통부 실거래가 공개시스템 기준입니다. 투자 판단의 최종 책임은 본인에게 있습니다."
   ```
   을 아래 두 줄로 교체:
   ```html
   <div class="disclaimer">
     출처: 국토교통부 실거래가 공개시스템<br>
     본 데이터는 신고 기준이며 실제 거래와 차이가 있을 수 있습니다
   </div>
   ```
   주석 추가: `// D-08 LOCKED: 이 두 줄은 제거 불가`

2. **BASE_CSS_PREVIEW 상수 추가 (Pitfall-1, Pitfall-2 해결):**
   파일 상단 BASE_CSS 선언 직후에 추가:
   ```javascript
   // 브라우저 iframe 미리보기용 — CDN 폰트 (file:// 경로 사용 불가)
   export const BASE_CSS_PREVIEW = `
     @import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css');
     :root {
       --brand: #0066FF; --brand-tint: #EAF2FE; --ink: #152038; --ink-2: #5B6677;
       --ink-3: #8A93A3; --gold: #FFC93C; --gold-2: #FFAB00; --surface: #FFFFFF;
       --surface-2: #F7F8FA; --line: rgba(112,115,124,0.18); --placeholder: #C4CAD3;
     }
     * { margin: 0; padding: 0; box-sizing: border-box; }
     html, body { width: 1080px; height: 1080px; overflow: hidden; }
     body { font-family: 'Pretendard', -apple-system, 'Apple SD Gothic Neo', sans-serif; -webkit-font-smoothing: antialiased; }
   `
   ```
   Note: 기존 BASE_CSS(file:// 방식)는 그대로 유지 — Puppeteer Actions 전용.

3. **html() 함수 리팩터 또는 htmlWithCss() 추가:**
   기존 `function html(body, css = '')` 유지. 미리보기 함수들은 BASE_CSS_PREVIEW를 추가 css 파라미터 없이 직접 사용.

4. **renderCoverPreview(), renderHighlightPreview(), renderRankingPreview(), renderClosingPreview() 추가:**
   각 함수는 기존 renderXxx() 로직과 동일하되 `BASE_CSS_PREVIEW`를 css 파라미터에 전달:
   ```javascript
   export function renderCoverPreview(data) {
     // renderCover와 동일한 body 생성 로직
     // html() 호출 시 BASE_CSS_PREVIEW 사용
     return `<!DOCTYPE html><html lang="ko"><head><meta charset="UTF-8">
       <style>${BASE_CSS_PREVIEW}${coverCss}</style></head><body>${body}</body></html>`
   }
   ```
   LOGO_PATH는 미리보기용으로 `/logo-cardnews.png` 또는 Supabase public URL로 교체 (Next.js public/ 경로).
   실제로는 이미지가 없어도 onerror="this.style.display='none'" 처리가 기존 템플릿에 있으므로 그대로.
  </action>
  <verify>
    <automated>cd card-news && node --input-type=module -e "import { renderClosing, renderCoverPreview, BASE_CSS_PREVIEW } from './scripts/templates.js'; const h = renderClosing({}); if (!h.includes('국토교통부 실거래가 공개시스템')) throw new Error('법적 표기 누락'); if (!h.includes('신고 기준이며')) throw new Error('면책 문구 누락'); const p = renderCoverPreview({ week:'테스트', region:'성산구', area:'84㎡' }); if (p.includes('file://')) throw new Error('CDN 폰트 미적용'); console.log('templates OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "출처: 국토교통부 실거래가 공개시스템" card-news/scripts/templates.js → 1
    - grep -c "신고 기준이며 실제 거래와 차이가 있을 수 있습니다" card-news/scripts/templates.js → 1
    - grep -c "D-08 LOCKED" card-news/scripts/templates.js → 1
    - grep -c "export const BASE_CSS_PREVIEW" card-news/scripts/templates.js → 1
    - grep -c "cdn.jsdelivr.net/gh/orioncactus/pretendard" card-news/scripts/templates.js → 1
    - grep -c "export function renderCoverPreview" card-news/scripts/templates.js → 1
    - grep -c "export function renderClosingPreview" card-news/scripts/templates.js → 1
  </acceptance_criteria>
  <done>renderClosing()에 D-08 법적 표기 2줄이 하드코딩되고, CDN 폰트 기반 미리보기 함수 4개가 export된다.</done>
</task>

<task type="auto">
  <name>Task 3: generate-from-payload.js 신규 + GitHub Actions 워크플로우 정비</name>
  <files>
    card-news/scripts/generate-from-payload.js,
    .github/workflows/weekly-generate.yml,
    .github/workflows/custom-cardnews.yml
  </files>
  <read_first>
    - card-news/.github/workflows/weekly-generate.yml (기존 구조 — 루트로 복사할 원본)
    - card-news/scripts/generate.js (generateCardSet 패턴 — generate-from-payload.js 참조)
    - card-news/scripts/capture.js (captureCard 시그니처 확인)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pattern 13, Critical Finding, Pitfall-2)
  </read_first>
  <action>
1. **card-news/scripts/generate-from-payload.js 신규 작성:**
   - argv 파싱: `--payload=payload.json`, `--series=custom`
   - payload.json 형식: `{ "cover": htmlStr, "highlight": htmlStr, "ranking": htmlStr, "closing": htmlStr }`
   - 각 HTML을 임시 파일로 쓴 뒤 captureCard()로 PNG 생성
   - output 경로: `card-news/output/{series}/01-cover.png`, `02-highlight.png`, `03-ranking.png`, `04-closing.png`
   ```javascript
   import { readFileSync, mkdirSync, writeFileSync } from 'fs'
   import { join, resolve, dirname } from 'path'
   import { fileURLToPath } from 'url'
   import { captureCard, closeBrowser } from './capture.js'
   import 'dotenv/config'
   
   const __dirname = dirname(fileURLToPath(import.meta.url))
   const OUTPUT_DIR = resolve(__dirname, '../output')
   
   const args = Object.fromEntries(
     process.argv.slice(2).map(a => { const [k,v] = a.replace('--','').split('='); return [k, v] })
   )
   const payloadPath = resolve(args.payload ?? 'payload.json')
   const seriesId = args.series ?? `custom-${Date.now()}`
   
   const payload = JSON.parse(readFileSync(payloadPath, 'utf-8'))
   const dir = join(OUTPUT_DIR, seriesId)
   mkdirSync(dir, { recursive: true })
   
   const cards = [
     { name: '01-cover',     html: payload.cover },
     { name: '02-highlight', html: payload.highlight },
     { name: '03-ranking',   html: payload.ranking },
     { name: '04-closing',   html: payload.closing },
   ]
   
   for (const card of cards) {
     if (!card.html) { console.warn(`  [skip] ${card.name} (no html)`); continue }
     await captureCard(card.html, join(dir, `${card.name}.png`))
     console.log(`  ✓ ${card.name}.png`)
   }
   await closeBrowser()
   console.log(`완료! → output/${seriesId}/`)
   ```

2. **.github/workflows/weekly-generate.yml (루트) 신규 작성 — PITFALL-1 해결:**
   card-news/.github/workflows/weekly-generate.yml의 내용을 그대로 복사해 루트 .github/workflows/weekly-generate.yml로 생성.
   변경사항: 없음 (내용 동일, 루트에 배치함으로써 GitHub Actions가 인식).
   Note: 기존 card-news/.github/workflows/weekly-generate.yml은 삭제하지 않음 (GitHub Actions가 서브디렉토리 파일을 무시하므로 무해).

3. **.github/workflows/custom-cardnews.yml (루트) 신규 작성 — PITFALL-3, D-02:**
   ```yaml
   name: Custom Card News (Builder)
   
   on:
     workflow_dispatch:
       inputs:
         payload_url:
           description: 'URL to JSON payload (Supabase Storage public URL)'
           type: string
           required: true
         series_id:
           description: 'Unique series identifier for artifact naming'
           type: string
           default: 'custom'
   
   jobs:
     generate:
       runs-on: ubuntu-latest
       defaults:
         run:
           working-directory: card-news
   
       steps:
         - uses: actions/checkout@v4
   
         - uses: actions/setup-node@v4
           with:
             node-version: '20'
             cache: 'npm'
             cache-dependency-path: card-news/package-lock.json
   
         - name: Install Chromium system dependencies
           run: sudo apt-get install -y libgbm-dev libxss1 libasound2t64 libatk-bridge2.0-0 libgtk-3-0
   
         - name: Install dependencies
           run: npm ci
   
         - name: Cache Pretendard fonts
           uses: actions/cache@v4
           with:
             path: card-news/fonts
             key: pretendard-1.3.9
   
         - name: Download Pretendard fonts
           run: node scripts/setup.js
   
         - name: Download HTML payload
           run: curl -f -o payload.json "${{ inputs.payload_url }}"
   
         - name: Generate PNG from payload
           env:
             SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
             SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
           run: node scripts/generate-from-payload.js --payload=payload.json --series=${{ inputs.series_id }}
   
         - name: Upload PNG artifacts
           uses: actions/upload-artifact@v4
           with:
             name: custom-card-${{ inputs.series_id }}-${{ github.run_id }}
             path: card-news/output/
             retention-days: 30
   ```
  </action>
  <verify>
    <automated>node -e "const { existsSync } = require('fs'); ['card-news/scripts/generate-from-payload.js', '.github/workflows/weekly-generate.yml', '.github/workflows/custom-cardnews.yml'].forEach(f => { if (!existsSync(f)) throw new Error('Missing: ' + f); }); console.log('All workflow files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/weekly-generate.yml 파일 존재
    - .github/workflows/custom-cardnews.yml 파일 존재
    - grep -c "workflow_dispatch" .github/workflows/custom-cardnews.yml → 1
    - grep -c "payload_url" .github/workflows/custom-cardnews.yml → 2 (inputs 정의 + run 단계 참조)
    - grep -c "generate-from-payload.js" .github/workflows/custom-cardnews.yml → 1
    - grep -c "retention-days: 30" .github/workflows/custom-cardnews.yml → 1
    - card-news/scripts/generate-from-payload.js 파일 존재
    - grep -c "captureCard" card-news/scripts/generate-from-payload.js → 1
  </acceptance_criteria>
  <done>루트 .github/workflows/에 두 워크플로우가 존재하고, generate-from-payload.js가 payload JSON을 PNG로 변환할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions 실행 | workflow_dispatch input으로 전달된 payload_url이 GitHub Actions 실행 컨텍스트에서 fetch됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | custom-cardnews.yml payload_url | accept | payload_url은 Supabase Storage public URL — 어드민만 업로드 가능, Actions가 JSON 형식 검증 |
| T-31-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | GitHub Secrets로 관리, Actions 로그에 마스킹됨 |
| T-31-03 | Tampering | renderClosing() 법적 표기 | mitigate | D-08 LOCKED 주석 + 하드코딩으로 코드 리뷰 시 가시적 |
</threat_model>

<verification>
1. `cd card-news && node --input-type=module -e "import { fetchJeonseRanking, fetchAllTimeHighRanking, fetchPriceChangeRanking, getDateRange, filterOutliers } from './scripts/fetch-data.js'; console.log('exports OK')"` → 에러 없음
2. `cd card-news && node --input-type=module -e "import { renderClosing, BASE_CSS_PREVIEW } from './scripts/templates.js'; const h = renderClosing({}); console.log(h.includes('국토교통부 실거래가 공개시스템'))"` → true
3. `node -e "require('fs').existsSync('.github/workflows/custom-cardnews.yml') && console.log('OK')"` → OK
4. `node -e "require('fs').existsSync('.github/workflows/weekly-generate.yml') && console.log('OK')"` → OK
</verification>

<success_criteria>
- fetch-data.js: fetchJeonseRanking / fetchMonthlyRanking / fetchAllTimeHighRanking / fetchPriceChangeRanking / getDateRange / filterOutliers export됨
- templates.js: renderClosing()에 D-08 문구 2줄 하드코딩, BASE_CSS_PREVIEW + renderXxxPreview 4개 export
- .github/workflows/weekly-generate.yml: 루트에 존재 (GitHub Actions 인식)
- .github/workflows/custom-cardnews.yml: payload_url 입력 받아 PNG 생성, artifact 30일 보존
- card-news/scripts/generate-from-payload.js: payload JSON → PNG 4장 변환 로직 완성
</success_criteria>

<output>
완료 후 `.planning/phases/31-admin-cardnews-builder/31-01-SUMMARY.md` 생성.
</output>
