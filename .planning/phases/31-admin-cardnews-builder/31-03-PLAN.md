---
phase: 31-admin-cardnews-builder
plan: 03
type: execute
wave: 3
depends_on: [31-02]
files_modified:
  - src/app/api/admin/cardnews/trigger-actions/route.ts
  - src/app/api/admin/cardnews/artifact/route.ts
  - src/app/api/admin/cardnews/scheduler/route.ts
autonomous: true
requirements: [BILD-04, BILD-06]

must_haves:
  truths:
    - "POST /api/admin/cardnews/trigger-actions가 HTML을 Supabase Storage에 업로드하고 custom-cardnews.yml을 트리거한다 (D-02, PITFALL-4)"
    - "GET /api/admin/cardnews/artifact?run_id=N이 artifact ZIP 다운로드 URL을 반환한다 (BILD-04)"
    - "GET /api/admin/cardnews/scheduler가 weekly-generate.yml의 state와 최근 실행 정보를 반환한다 (BILD-06)"
    - "PUT /api/admin/cardnews/scheduler가 weekly-generate.yml을 enable/disable한다 (BILD-06, D-07)"
    - "POST /api/admin/cardnews/scheduler가 weekly-generate.yml을 수동 트리거한다 (D-07)"
    - "모든 Route가 어드민 권한 없는 요청에 401/403을 반환한다"
    - "GITHUB_PAT는 서버 환경변수에서만 접근되고 클라이언트 응답 body에 포함되지 않는다"
  artifacts:
    - path: "src/app/api/admin/cardnews/trigger-actions/route.ts"
      provides: "GitHub Actions custom-cardnews.yml 트리거 (Supabase Storage HTML 전달)"
      exports: ["POST"]
    - path: "src/app/api/admin/cardnews/artifact/route.ts"
      provides: "artifact ZIP 다운로드 URL 조회"
      exports: ["GET"]
    - path: "src/app/api/admin/cardnews/scheduler/route.ts"
      provides: "weekly-generate.yml enable/disable/trigger/status"
      exports: ["GET", "PUT", "POST"]
  key_links:
    - from: "trigger-actions/route.ts"
      to: "Supabase Storage (cardnews-payloads)"
      via: "createSupabaseAdminClient().storage.from('cardnews-payloads').upload()"
      pattern: "cardnews-payloads"
    - from: "trigger-actions/route.ts"
      to: "GitHub Actions custom-cardnews.yml"
      via: "triggerWorkflow() from @/services/github-actions"
      pattern: "triggerWorkflow"
    - from: "scheduler/route.ts"
      to: "GitHub Actions weekly-generate.yml"
      via: "setWorkflowEnabled() / getWorkflowState() from @/services/github-actions"
      pattern: "setWorkflowEnabled|getWorkflowState"
---

<objective>
GitHub Actions 트리거·artifact 조회·스케줄러 관리 API Route를 구현한다.

Purpose: Plan 02에서 만든 src/services/github-actions.ts 어댑터를 활용해 빌더의 PNG 생성 흐름(BILD-04)과 자동 배포 스케줄러 관리(BILD-06)를 서버사이드에서 처리한다. HTML 페이로드는 Supabase Storage에 저장 후 public URL을 Actions에 전달 (PITFALL-4).

Output:
- trigger-actions/route.ts: HTML → Storage → workflow_dispatch
- artifact/route.ts: run_id → artifact ZIP S3 URL 조회
- scheduler/route.ts: weekly-generate.yml GET/PUT/POST (상태·enable/disable·수동 트리거)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-admin-cardnews-builder/31-CONTEXT.md
@.planning/phases/31-admin-cardnews-builder/31-RESEARCH.md
@.planning/phases/31-admin-cardnews-builder/31-PATTERNS.md
@.planning/phases/31-admin-cardnews-builder/31-02-SUMMARY.md
</context>

<interfaces>
<!-- Plan 02에서 생성된 계약 — 읽기 전 확인 필수 -->

src/services/github-actions.ts exports:
```typescript
export async function triggerWorkflow(params: {
  owner: string; repo: string; workflowId: string; ref: string; inputs: Record<string, string>
}): Promise<void>

export async function getLatestWorkflowRun(
  owner: string, repo: string, workflowId: string
): Promise<WorkflowRun | null>

export async function getRunArtifacts(
  owner: string, repo: string, runId: number
): Promise<WorkflowArtifact[]>

export async function getArtifactDownloadUrl(
  owner: string, repo: string, artifactId: number
): Promise<string | null>

export async function setWorkflowEnabled(
  owner: string, repo: string, workflowId: string, enabled: boolean
): Promise<void>

export async function getWorkflowState(
  owner: string, repo: string, workflowId: string
): Promise<string>

export interface WorkflowRun {
  id: number; status: string; conclusion: string | null; created_at: string; html_url: string
}
export interface WorkflowArtifact {
  id: number; name: string; size_in_bytes: number; created_at: string
}
```

Supabase Storage 업로드 패턴 (upload-image/route.ts 기반):
```typescript
const adminClient = createSupabaseAdminClient()
const { data, error } = await adminClient.storage
  .from('cardnews-payloads')  // Plan 02 user_setup: public 버킷 사전 생성 필요
  .upload(filename, buffer, { contentType: 'application/json', cacheControl: '3600', upsert: false })
if (error) return NextResponse.json({ error: error.message }, { status: 500 })
const { data: { publicUrl } } = adminClient.storage.from('cardnews-payloads').getPublicUrl(data.path)
```

GitHub Actions 상수:
- owner: 'nickujung-art'
- repo: 'bds'
- 빌더 워크플로우: 'custom-cardnews.yml'
- 주간 워크플로우: 'weekly-generate.yml'
- ref: 'main'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: trigger-actions/route.ts + artifact/route.ts (BILD-04)</name>
  <files>
    src/app/api/admin/cardnews/trigger-actions/route.ts,
    src/app/api/admin/cardnews/artifact/route.ts
  </files>
  <read_first>
    - src/app/api/admin/realtors/upload-image/route.ts (Supabase Storage 업로드 + 어드민 권한 패턴)
    - src/services/github-actions.ts (Plan 02 생성 — triggerWorkflow, getRunArtifacts, getArtifactDownloadUrl)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pitfall 2, 3, 4, 5, Pattern 4~6)
    - .planning/phases/31-admin-cardnews-builder/31-CONTEXT.md (D-02, D-06)
  </read_first>
  <action>
**src/app/api/admin/cardnews/trigger-actions/route.ts (신규):**

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { triggerWorkflow } from '@/services/github-actions'
import { z } from 'zod'

export const runtime = 'nodejs'

const GH_OWNER = 'nickujung-art'
const GH_REPO = 'bds'
const CUSTOM_WORKFLOW_ID = 'custom-cardnews.yml'

const RequestSchema = z.object({
  htmlCards: z.object({
    cover: z.string(),
    highlight: z.string(),
    ranking: z.string(),
    closing: z.string(),
  }),
  seriesId: z.string().regex(/^[a-z0-9-]+$/).default('custom'),
})

export async function POST(request: Request): Promise<NextResponse> {
  // 어드민 권한 검증
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  let body: unknown
  try { body = await request.json() } catch { return NextResponse.json({ error: 'invalid body' }, { status: 400 }) }
  const parsed = RequestSchema.safeParse(body)
  if (!parsed.success) return NextResponse.json({ error: parsed.error.message }, { status: 400 })

  const { htmlCards, seriesId } = parsed.data

  // 1. HTML 페이로드를 JSON으로 Supabase Storage에 업로드 (PITFALL-4)
  //    GitHub Actions가 public URL로 다운로드할 수 있어야 함 (PITFALL-5: public 버킷 필요)
  const adminClient = createSupabaseAdminClient()
  const filename = `payloads/${seriesId}-${Date.now()}.json`
  const buffer = Buffer.from(JSON.stringify(htmlCards))

  const { data: uploadData, error: uploadError } = await adminClient.storage
    .from('cardnews-payloads')
    .upload(filename, buffer, { contentType: 'application/json', cacheControl: '3600', upsert: false })

  if (uploadError) {
    return NextResponse.json({ error: `Storage upload failed: ${uploadError.message}` }, { status: 500 })
  }

  const { data: { publicUrl } } = adminClient.storage.from('cardnews-payloads').getPublicUrl(uploadData.path)

  // 2. GitHub Actions custom-cardnews.yml 트리거 (PITFALL-3: GITHUB_PAT 필요)
  //    204 반환, run ID 없음 — 클라이언트는 별도 polling으로 조회 (PITFALL-2)
  try {
    await triggerWorkflow({
      owner: GH_OWNER,
      repo: GH_REPO,
      workflowId: CUSTOM_WORKFLOW_ID,
      ref: 'main',
      inputs: { payload_url: publicUrl, series_id: seriesId },
    })
  } catch (err) {
    return NextResponse.json({
      error: err instanceof Error ? err.message : 'GitHub Actions trigger failed'
    }, { status: 502 })
  }

  const run_url = `https://github.com/${GH_OWNER}/${GH_REPO}/actions/workflows/${CUSTOM_WORKFLOW_ID}`

  return NextResponse.json({
    ok: true,
    payload_url: publicUrl,
    run_url,
    message: 'PNG 생성이 시작되었습니다. GitHub Actions에서 진행 상황을 확인하세요. (약 5~15분 소요)',
  })
}
```

**src/app/api/admin/cardnews/artifact/route.ts (신규):**

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { getRunArtifacts, getArtifactDownloadUrl, getLatestWorkflowRun } from '@/services/github-actions'

export const runtime = 'nodejs'

const GH_OWNER = 'nickujung-art'
const GH_REPO = 'bds'
const CUSTOM_WORKFLOW_ID = 'custom-cardnews.yml'

export async function GET(request: Request): Promise<NextResponse> {
  // 어드민 권한 검증
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  const url = new URL(request.url)
  const runIdParam = url.searchParams.get('run_id')

  try {
    let runId: number
    if (runIdParam) {
      runId = parseInt(runIdParam, 10)
    } else {
      // run_id 없으면 최근 workflow_dispatch run 조회
      const latestRun = await getLatestWorkflowRun(GH_OWNER, GH_REPO, CUSTOM_WORKFLOW_ID)
      if (!latestRun) return NextResponse.json({ status: 'pending', download_url: null })
      runId = latestRun.id
    }

    const artifacts = await getRunArtifacts(GH_OWNER, GH_REPO, runId)
    if (!artifacts.length) {
      return NextResponse.json({ status: 'pending', download_url: null })
    }

    // 첫 번째 artifact (custom-card-*) 의 다운로드 URL 획득
    const downloadUrl = await getArtifactDownloadUrl(GH_OWNER, GH_REPO, artifacts[0].id)

    return NextResponse.json({
      status: 'ready',
      download_url: downloadUrl,
      artifact_name: artifacts[0].name,
      size_bytes: artifacts[0].size_in_bytes,
      expires_info: 'artifact는 30일 후 만료됩니다',
    })
  } catch (err) {
    return NextResponse.json({
      error: err instanceof Error ? err.message : 'artifact fetch failed'
    }, { status: 500 })
  }
}
```
  </action>
  <verify>
    <automated>npm run lint -- --file src/app/api/admin/cardnews/trigger-actions/route.ts src/app/api/admin/cardnews/artifact/route.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function POST" src/app/api/admin/cardnews/trigger-actions/route.ts → 1
    - grep -c "cardnews-payloads" src/app/api/admin/cardnews/trigger-actions/route.ts → 2 이상
    - grep -c "triggerWorkflow" src/app/api/admin/cardnews/trigger-actions/route.ts → 1
    - grep -c "from '@/services/github-actions'" src/app/api/admin/cardnews/trigger-actions/route.ts → 1
    - grep -c "custom-cardnews.yml" src/app/api/admin/cardnews/trigger-actions/route.ts → 1
    - grep -c "payload_url" src/app/api/admin/cardnews/trigger-actions/route.ts → 1 이상
    - grep -c "export async function GET" src/app/api/admin/cardnews/artifact/route.ts → 1
    - grep -c "getArtifactDownloadUrl" src/app/api/admin/cardnews/artifact/route.ts → 1
    - grep -c "30일" src/app/api/admin/cardnews/artifact/route.ts → 1 (만료 안내)
    - grep -c "status: 401" src/app/api/admin/cardnews/trigger-actions/route.ts → 1
  </acceptance_criteria>
  <done>POST /api/admin/cardnews/trigger-actions가 HTML을 Storage에 올리고 Actions를 트리거하며, GET /api/admin/cardnews/artifact가 다운로드 URL을 반환한다.</done>
</task>

<task type="auto">
  <name>Task 2: scheduler/route.ts — 주간 워크플로우 관리 API (BILD-06, D-07)</name>
  <files>src/app/api/admin/cardnews/scheduler/route.ts</files>
  <read_first>
    - src/services/github-actions.ts (setWorkflowEnabled, getWorkflowState, triggerWorkflow, getLatestWorkflowRun)
    - src/app/api/admin/gps-approve/route.ts (GET + POST 메서드 혼용 패턴)
    - .planning/phases/31-admin-cardnews-builder/31-CONTEXT.md (D-07: enable/disable + 수동 트리거 + 마지막 실행 + 다음 예정 시각)
    - .planning/phases/31-admin-cardnews-builder/31-RESEARCH.md (Pattern 7)
  </read_first>
  <action>
```typescript
// src/app/api/admin/cardnews/scheduler/route.ts
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import {
  setWorkflowEnabled,
  getWorkflowState,
  getLatestWorkflowRun,
  triggerWorkflow,
} from '@/services/github-actions'

export const runtime = 'nodejs'

const GH_OWNER = 'nickujung-art'
const GH_REPO = 'bds'
const WEEKLY_WORKFLOW_ID = 'weekly-generate.yml'

// 다음 예정 실행 계산 (매주 월요일 00:10 KST = 일요일 15:10 UTC)
function nextScheduledRun(): string {
  const now = new Date()
  const utcSunday = new Date(now)
  // 다음 일요일(UTC) 15:10 찾기
  const day = now.getUTCDay()  // 0=Sun
  const daysUntilSunday = day === 0 ? 7 : 7 - day
  utcSunday.setUTCDate(now.getUTCDate() + daysUntilSunday)
  utcSunday.setUTCHours(15, 10, 0, 0)
  return utcSunday.toISOString()
}

async function getAdminGuard(request?: Request) {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Unauthorized', status: 401 } as const
  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return { error: 'Forbidden', status: 403 } as const
  }
  return null
}

// GET: 스케줄러 상태 + 최근 실행 정보 (D-07)
export async function GET(): Promise<NextResponse> {
  const authErr = await getAdminGuard()
  if (authErr) return NextResponse.json({ error: authErr.error }, { status: authErr.status })

  try {
    const [state, latestRun] = await Promise.all([
      getWorkflowState(GH_OWNER, GH_REPO, WEEKLY_WORKFLOW_ID),
      getLatestWorkflowRun(GH_OWNER, GH_REPO, WEEKLY_WORKFLOW_ID),
    ])
    return NextResponse.json({
      enabled: state === 'active',
      state,
      latestRun,
      nextScheduledRun: nextScheduledRun(),
    })
  } catch (err) {
    return NextResponse.json({ error: err instanceof Error ? err.message : 'GitHub API error' }, { status: 500 })
  }
}

// PUT: enable/disable (D-07)
export async function PUT(request: Request): Promise<NextResponse> {
  const authErr = await getAdminGuard()
  if (authErr) return NextResponse.json({ error: authErr.error }, { status: authErr.status })

  let body: unknown
  try { body = await request.json() } catch { return NextResponse.json({ error: 'invalid body' }, { status: 400 }) }
  const { enabled } = body as { enabled?: boolean }
  if (typeof enabled !== 'boolean') return NextResponse.json({ error: 'enabled must be boolean' }, { status: 400 })

  try {
    await setWorkflowEnabled(GH_OWNER, GH_REPO, WEEKLY_WORKFLOW_ID, enabled)
    return NextResponse.json({ ok: true, enabled })
  } catch (err) {
    return NextResponse.json({ error: err instanceof Error ? err.message : 'GitHub API error' }, { status: 502 })
  }
}

// POST: 수동 트리거 (D-07)
export async function POST(request: Request): Promise<NextResponse> {
  const authErr = await getAdminGuard()
  if (authErr) return NextResponse.json({ error: authErr.error }, { status: authErr.status })

  let body: unknown
  try { body = await request.json() } catch { body = {} }
  const { dry_run = false, series = '' } = (body as { dry_run?: boolean; series?: string }) ?? {}

  try {
    await triggerWorkflow({
      owner: GH_OWNER,
      repo: GH_REPO,
      workflowId: WEEKLY_WORKFLOW_ID,
      ref: 'main',
      inputs: {
        dry_run: String(dry_run),
        series: series,
      },
    })
    const run_url = `https://github.com/${GH_OWNER}/${GH_REPO}/actions/workflows/${WEEKLY_WORKFLOW_ID}`
    return NextResponse.json({ ok: true, run_url })
  } catch (err) {
    return NextResponse.json({ error: err instanceof Error ? err.message : 'trigger failed' }, { status: 502 })
  }
}
```
  </action>
  <verify>
    <automated>npm run lint -- --file src/app/api/admin/cardnews/scheduler/route.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function GET" src/app/api/admin/cardnews/scheduler/route.ts → 1
    - grep -c "export async function PUT" src/app/api/admin/cardnews/scheduler/route.ts → 1
    - grep -c "export async function POST" src/app/api/admin/cardnews/scheduler/route.ts → 1
    - grep -c "setWorkflowEnabled" src/app/api/admin/cardnews/scheduler/route.ts → 1
    - grep -c "getWorkflowState" src/app/api/admin/cardnews/scheduler/route.ts → 1
    - grep -c "weekly-generate.yml" src/app/api/admin/cardnews/scheduler/route.ts → 1
    - grep -c "nextScheduledRun" src/app/api/admin/cardnews/scheduler/route.ts → 2 이상 (함수 + 사용)
    - grep -c "from '@/services/github-actions'" src/app/api/admin/cardnews/scheduler/route.ts → 1
  </acceptance_criteria>
  <done>GET/PUT/POST /api/admin/cardnews/scheduler가 각각 상태 조회/enable-disable/수동 트리거를 처리하고 모두 어드민 권한 검증을 포함한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Supabase Storage | HTML 페이로드 업로드 (cardnews-payloads 버킷) |
| API→GitHub | GITHUB_PAT를 사용한 workflow_dispatch 및 enable/disable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Elevation of Privilege | /api/admin/cardnews/trigger-actions | mitigate | 어드민 role 검증 필수, 비인가 사용자가 GitHub Actions 트리거 불가 |
| T-31-10 | Tampering | Supabase Storage payload | mitigate | payload 크기는 4개 HTML 문자열로 제한 (~2MB max), JSON.stringify로 구조화 |
| T-31-11 | Information Disclosure | GitHub Actions run 상세 | accept | run_url만 반환 (GitHub 공개 링크), artifact S3 URL은 API 경유해서만 노출 |
| T-31-12 | Denial of Service | 반복 workflow_dispatch | accept | 어드민만 접근 가능, GitHub Actions 분당 요청 한도로 자체 제한됨 |
| T-31-13 | Elevation of Privilege | /api/admin/cardnews/scheduler PUT | mitigate | 어드민 role 검증 + setWorkflowEnabled는 서비스 어댑터 경유, 직접 GitHub API 호출 금지 |
</threat_model>

<verification>
1. `npm run lint` 통과 (3개 route.ts)
2. `npm run build` 통과 (타입 오류 없음)
3. grep -c "from '@/services/github-actions'" src/app/api/admin/cardnews/trigger-actions/route.ts → 1
4. grep -c "cardnews-payloads" src/app/api/admin/cardnews/trigger-actions/route.ts → 2 이상
5. 인증 없이 GET /api/admin/cardnews/scheduler → 401
</verification>

<success_criteria>
- POST /api/admin/cardnews/trigger-actions: HTML → cardnews-payloads Storage → custom-cardnews.yml dispatch → { ok: true, run_url }
- GET /api/admin/cardnews/artifact?run_id=N: artifact ZIP S3 URL 반환 (없으면 status:'pending')
- GET /api/admin/cardnews/scheduler: { enabled, state, latestRun, nextScheduledRun } 반환
- PUT /api/admin/cardnews/scheduler: weekly-generate.yml enable/disable
- POST /api/admin/cardnews/scheduler: weekly-generate.yml 수동 트리거
- `npm run lint && npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/31-admin-cardnews-builder/31-03-SUMMARY.md` 생성.
</output>
