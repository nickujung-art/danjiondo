---
plan_id: 23-01
phase: 23
wave: 1
depends_on: [23-00]
files_modified:
  - src/lib/utils/url-slug.ts
  - src/lib/utils/url-slug.test.ts
  - src/lib/data/seo-hierarchy.ts
  - src/lib/data/seo-hierarchy.test.ts
  - src/lib/data/complex-detail.ts
  - src/lib/data/sitemap.ts
autonomous: true
requirements: [SEO-01, SEO-02]
must_haves:
  goal: "한글 URL 처리 유틸 + 계층별 페이지 데이터 함수 + url_slug 인식 쿼리가 모두 준비된다"
  truths:
    - "buildUrlSlug('창원시','성산구','내동','대우2차') === '창원시/성산구/내동/대우2차'"
    - "buildUrlSlug('김해시', null, '내동', '리버사이드팰리스') === '김해시/내동/리버사이드팰리스'"
    - "getSiPageData('창원시') → guList 반환, getSiPageData('김해시') → dongList 반환"
    - "getComplexBySlug('창원시/성산구/내동/대우2차') → ComplexDetail 반환"
    - "getComplexById가 url_slug 필드를 포함한다"
    - "getComplexesForSitemap이 url_slug, si, gu, dong을 반환한다"
  artifacts:
    - path: "src/lib/utils/url-slug.ts"
      provides: "buildUrlSlug, classifySlug, buildCanonicalUrl 순수 함수"
      exports: ["buildUrlSlug", "classifySlug", "buildCanonicalUrl", "SlugPageType"]
    - path: "src/lib/data/seo-hierarchy.ts"
      provides: "계층별 페이지 데이터 함수 (getSiPageData, getGuPageData, getDongPageData, getComplexBySlug)"
      exports: ["getSiPageData", "getGuPageData", "getDongPageData", "getComplexBySlug", "SiPageData", "GuPageData", "DongPageData"]
  key_links:
    - from: "src/app/[...slug]/page.tsx"
      to: "src/lib/data/seo-hierarchy.ts"
      via: "getSiPageData / getGuPageData / getDongPageData / getComplexBySlug"
      pattern: "seo-hierarchy"
---

<objective>
SEO 계층 URL 처리에 필요한 데이터 레이어 전체를 구현한다.
Wave 2(라우팅)가 이 레이어에 의존하므로 Wave 1에서 완성되어야 한다.

Purpose: SEO-01 slug 유틸 + SEO-02 계층 페이지 데이터 함수
Output: url-slug.ts, seo-hierarchy.ts, 테스트 2종, complex-detail.ts/sitemap.ts 확장
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-seo-url-structure/23-CONTEXT.md
@.planning/phases/23-seo-url-structure/23-RESEARCH.md
@.planning/phases/23-seo-url-structure/23-PATTERNS.md
@.planning/phases/23-seo-url-structure/23-00-SUMMARY.md

<interfaces>
<!-- src/lib/data/complex-detail.ts 현재 ComplexDetail 타입 (url_slug 아직 없음) -->
```typescript
export interface ComplexDetail {
  id:              string
  canonical_name:  string
  road_address:    string | null
  si:              string | null
  gu:              string | null
  dong:            string | null
  built_year:      number | null
  household_count: number | null
  floors_above:    number | null
  heat_type:       string | null
  sgg_code:        string
  status:          string
  lat:             number | null
  lng:             number | null
}
```

<!-- src/lib/data/sitemap.ts 현재 SitemapEntry 타입 (url_slug 아직 없음) -->
```typescript
export interface SitemapEntry {
  id:         string
  updated_at: string
}
```

<!-- Supabase mock 패턴 (gap-label.test.ts 기반) -->
```typescript
function makeSupabaseMock(rows: SomeRow[]) {
  const query = {
    select: vi.fn().mockReturnThis(),
    eq:     vi.fn().mockReturnThis(),
    not:    vi.fn().mockReturnThis(),
    is:     vi.fn().mockReturnThis(),
    maybeSingle: vi.fn().mockResolvedValue({ data: rows[0] ?? null, error: null }),
  }
  return { from: vi.fn().mockReturnValue(query) } as unknown as SupabaseClient
}
```
</interfaces>
</context>

<tasks>

<task id="T-01" type="execute" tdd="true">
  <title>Task 1: url-slug.ts 유틸리티 + url-slug.test.ts (TDD)</title>
  <read_first>
    - src/lib/utils/facility-format.ts — JSDoc + export function 패턴, 순수 함수 구조
    - src/lib/data/gap-label.test.ts — describe/it/expect import 패턴
    - .planning/phases/23-seo-url-structure/23-RESEARCH.md (E-02 섹션) — buildUrlSlug, classifySlug, buildCanonicalUrl 구현
  </read_first>
  <behavior>
    - buildUrlSlug('창원시','성산구','내동','대우2차') → '창원시/성산구/내동/대우2차'
    - buildUrlSlug('김해시', null, '내동', '리버사이드팰리스') → '김해시/내동/리버사이드팰리스'
    - buildUrlSlug(null, null, '내동', '대우2차') → null (si 없으면 null)
    - buildUrlSlug('창원시', '성산구', null, '대우2차') → null (dong 없으면 null)
    - buildUrlSlug('창원시', '성산구', '내동', null) → null (canonical_name 없으면 null)
    - classifySlug(['창원시']) → 'si'
    - classifySlug(['창원시','성산구']) → 'gu'
    - classifySlug(['창원시','성산구','내동']) → 'dong-or-complex'
    - classifySlug(['창원시','성산구','내동','대우2차']) → 'complex'
    - classifySlug(['a','b','c','d','e']) → 'invalid'
    - buildCanonicalUrl('https://danjiondo.kr', ['창원시','성산구']) → 'https://danjiondo.kr/%EC%B0%BD%EC%9B%90%EC%8B%9C/%EC%84%B1%EC%82%B0%EA%B5%AC' (encodeURIComponent per segment)
  </behavior>
  <action>
    먼저 `src/lib/utils/url-slug.test.ts` 작성 (RED — import 대상 파일 없으므로 실패해야 정상):

    ```typescript
    /**
     * url-slug 유틸리티 테스트 — buildUrlSlug, classifySlug, buildCanonicalUrl
     * Phase 23 (TDD RED → GREEN)
     */
    import { describe, it, expect } from 'vitest'
    import { buildUrlSlug, classifySlug, buildCanonicalUrl } from './url-slug'

    describe('buildUrlSlug', () => {
      it('창원 4단계 — gu 있음', () =>
        expect(buildUrlSlug('창원시', '성산구', '내동', '대우2차')).toBe('창원시/성산구/내동/대우2차'))
      it('김해 3단계 — gu=null', () =>
        expect(buildUrlSlug('김해시', null, '내동', '리버사이드팰리스')).toBe('김해시/내동/리버사이드팰리스'))
      it('si=null → null', () =>
        expect(buildUrlSlug(null, null, '내동', '대우2차')).toBeNull())
      it('dong=null → null', () =>
        expect(buildUrlSlug('창원시', '성산구', null, '대우2차')).toBeNull())
      it('canonicalName=null → null', () =>
        expect(buildUrlSlug('창원시', '성산구', '내동', null)).toBeNull())
    })

    describe('classifySlug', () => {
      it('length 1 → si', () => expect(classifySlug(['창원시'])).toBe('si'))
      it('length 2 → gu', () => expect(classifySlug(['창원시', '성산구'])).toBe('gu'))
      it('length 3 → dong-or-complex', () =>
        expect(classifySlug(['창원시', '성산구', '내동'])).toBe('dong-or-complex'))
      it('length 4 → complex', () =>
        expect(classifySlug(['창원시', '성산구', '내동', '대우2차'])).toBe('complex'))
      it('length 5+ → invalid', () =>
        expect(classifySlug(['a', 'b', 'c', 'd', 'e'])).toBe('invalid'))
    })

    describe('buildCanonicalUrl', () => {
      it('한글 세그먼트 encodeURIComponent 인코딩', () => {
        const url = buildCanonicalUrl('https://danjiondo.kr', ['창원시', '성산구'])
        expect(url).toBe('https://danjiondo.kr/%EC%B0%BD%EC%9B%90%EC%8B%9C/%EC%84%B1%EC%82%B0%EA%B5%AC')
      })
      it('루트 site URL 유지', () => {
        const url = buildCanonicalUrl('https://danjiondo.kr', ['창원시'])
        expect(url.startsWith('https://danjiondo.kr/')).toBe(true)
      })
    })
    ```

    테스트 실행 (RED 확인):
    ```
    npx vitest run src/lib/utils/url-slug.test.ts
    ```
    → 파일 없으므로 실패해야 정상.

    다음으로 `src/lib/utils/url-slug.ts` 작성 (GREEN):

    ```typescript
    /**
     * SEO URL slug 유틸리티 — Phase 23
     *
     * D-01: 한글 그대로 사용 (로마자 변환 없음)
     * D-02: 창원시(gu 있음) → 4단계 / 김해시(gu 없음) → 3단계
     * import 'server-only' 없음 — scripts/backfill-url-slugs.ts에서도 직접 import 가능
     */

    /** DB url_slug 컬럼값 생성. 마이그레이션 SQL CASE 표현식과 동일 로직 */
    export function buildUrlSlug(
      si: string | null,
      gu: string | null,
      dong: string | null,
      canonicalName: string | null,
    ): string | null {
      if (!si || !dong || !canonicalName) return null
      return gu
        ? `${si}/${gu}/${dong}/${canonicalName}`
        : `${si}/${dong}/${canonicalName}`
    }

    /** catch-all slug 배열로 페이지 타입 판별 */
    export type SlugPageType = 'si' | 'gu' | 'dong-or-complex' | 'complex' | 'invalid'

    export function classifySlug(slug: string[]): SlugPageType {
      if (slug.length === 1) return 'si'
      if (slug.length === 2) return 'gu'
      if (slug.length === 3) return 'dong-or-complex'
      if (slug.length === 4) return 'complex'
      return 'invalid'
    }

    /**
     * 한글 URL을 canonical URL string으로 인코딩
     * sitemap.ts의 encodeSlug와 동일 목적 — 각 세그먼트 encodeURIComponent
     */
    export function buildCanonicalUrl(site: string, slug: string[]): string {
      return `${site}/${slug.map(encodeURIComponent).join('/')}`
    }
    ```

    GREEN 확인:
    ```
    npx vitest run src/lib/utils/url-slug.test.ts
    ```
    → 10개 테스트 모두 통과.
  </action>
  <acceptance_criteria>
    - npx vitest run src/lib/utils/url-slug.test.ts → 10개 PASS
    - grep -c "export function buildUrlSlug" src/lib/utils/url-slug.ts → 1
    - grep -c "export function classifySlug" src/lib/utils/url-slug.ts → 1
    - grep -c "export function buildCanonicalUrl" src/lib/utils/url-slug.ts → 1
    - grep -v "^//" src/lib/utils/url-slug.ts | grep -c "import 'server-only'" → 0 (scripts에서도 import 가능해야 함)
  </acceptance_criteria>
  <done>10개 Vitest 테스트 PASS, server-only import 없음</done>
</task>

<task id="T-02" type="execute">
  <title>Task 2: seo-hierarchy.ts 데이터 함수 + seo-hierarchy.test.ts</title>
  <read_first>
    - src/lib/data/invest.ts — import 'server-only', SupabaseClient<Database> 시그니처, error/data 패턴 (lines 1–10, 74–100)
    - src/lib/data/gap-label.test.ts — makeSupabaseMock 전체 패턴 (lines 1–99)
    - .planning/phases/23-seo-url-structure/23-RESEARCH.md (E-01, Pattern 1) — getSiPageData 구현 예시
  </read_first>
  <action>
    `src/lib/data/seo-hierarchy.ts` 생성. 계층 페이지(시/구/동) 데이터 함수 4종.

    ```typescript
    import 'server-only'
    import type { SupabaseClient } from '@supabase/supabase-js'
    import type { Database } from '@/types/database'
    import type { ComplexDetail } from './complex-detail'

    // ──────────────────────────────────────────
    // Types
    // ──────────────────────────────────────────

    export interface SiPageData {
      si:             string
      guList:         Array<{ gu: string; complexCount: number; avgPrice: number | null }>
      dongList:       Array<{ dong: string; complexCount: number }>  // 김해시용
      totalComplexes: number
    }

    export interface GuPageData {
      si:             string
      gu:             string
      dongList:       Array<{ dong: string; complexCount: number; avgPrice: number | null }>
      totalComplexes: number
    }

    export interface DongPageData {
      si:             string
      gu:             string | null
      dong:           string
      complexes:      Array<{
        id:              string
        canonical_name:  string
        url_slug:        string | null
        built_year:      number | null
        household_count: number | null
      }>
    }

    // ──────────────────────────────────────────
    // Functions
    // ──────────────────────────────────────────

    /** 시(시) 레벨 — 창원: gu별 집계 / 김해: dong별 집계 */
    export async function getSiPageData(
      si: string,
      supabase: SupabaseClient<Database>,
    ): Promise<SiPageData | null> {
      const { data, error } = await supabase
        .from('complexes')
        .select('gu, dong, avg_sale_per_pyeong')
        .eq('si', si)
        .eq('status', 'active')
        .not('url_slug', 'is', null)

      if (error || !data || data.length === 0) return null

      const hasGu = data.some(c => c.gu)

      if (hasGu) {
        // 창원시 — gu별 집계
        const byGu = new Map<string, number[]>()
        for (const c of data) {
          if (!c.gu) continue
          const arr = byGu.get(c.gu) ?? []
          if (c.avg_sale_per_pyeong) arr.push(c.avg_sale_per_pyeong)
          byGu.set(c.gu, arr)
        }
        return {
          si,
          guList: [...byGu.entries()].map(([gu, prices]) => ({
            gu,
            complexCount: prices.length,
            avgPrice: prices.length
              ? Math.round(prices.reduce((a, b) => a + b, 0) / prices.length)
              : null,
          })),
          dongList: [],
          totalComplexes: data.length,
        }
      } else {
        // 김해시 — dong별 집계
        const byDong = new Map<string, number>()
        for (const c of data) {
          if (!c.dong) continue
          byDong.set(c.dong, (byDong.get(c.dong) ?? 0) + 1)
        }
        return {
          si,
          guList: [],
          dongList: [...byDong.entries()].map(([dong, complexCount]) => ({ dong, complexCount })),
          totalComplexes: data.length,
        }
      }
    }

    /** 구(구) 레벨 — 창원시 전용 */
    export async function getGuPageData(
      si: string,
      gu: string,
      supabase: SupabaseClient<Database>,
    ): Promise<GuPageData | null> {
      const { data, error } = await supabase
        .from('complexes')
        .select('dong, avg_sale_per_pyeong')
        .eq('si', si)
        .eq('gu', gu)
        .eq('status', 'active')
        .not('url_slug', 'is', null)

      if (error || !data || data.length === 0) return null

      const byDong = new Map<string, number[]>()
      for (const c of data) {
        if (!c.dong) continue
        const arr = byDong.get(c.dong) ?? []
        if (c.avg_sale_per_pyeong) arr.push(c.avg_sale_per_pyeong)
        byDong.set(c.dong, arr)
      }

      return {
        si,
        gu,
        dongList: [...byDong.entries()].map(([dong, prices]) => ({
          dong,
          complexCount: prices.length,
          avgPrice: prices.length
            ? Math.round(prices.reduce((a, b) => a + b, 0) / prices.length)
            : null,
        })),
        totalComplexes: data.length,
      }
    }

    /** 동(동) 레벨 — 해당 동 단지 목록 */
    export async function getDongPageData(
      si: string,
      gu: string | null,
      dong: string,
      supabase: SupabaseClient<Database>,
    ): Promise<DongPageData | null> {
      let query = supabase
        .from('complexes')
        .select('id, canonical_name, url_slug, built_year, household_count')
        .eq('si', si)
        .eq('dong', dong)
        .eq('status', 'active')
        .not('url_slug', 'is', null)
        .order('household_count', { ascending: false })

      if (gu) query = query.eq('gu', gu)

      const { data, error } = await query
      if (error || !data || data.length === 0) return null

      return {
        si,
        gu,
        dong,
        complexes: data as DongPageData['complexes'],
      }
    }

    /**
     * url_slug로 단지 조회 — catch-all 라우트에서 단지 상세 판별용
     * D-09: url_slug=null인 143개 단지는 이 함수로 조회 불가 (빈 slug 쿼리 방지)
     */
    export async function getComplexBySlug(
      urlSlug: string,
      supabase: SupabaseClient<Database>,
    ): Promise<(ComplexDetail & { url_slug: string }) | null> {
      if (!urlSlug) return null
      const { data, error } = await supabase
        .from('complexes')
        .select(`
          id, canonical_name, road_address,
          si, gu, dong,
          built_year, household_count, floors_above, heat_type,
          sgg_code, status, lat, lng, url_slug
        `)
        .eq('url_slug', urlSlug)
        .eq('status', 'active')
        .maybeSingle()

      if (error || !data) return null
      return data as ComplexDetail & { url_slug: string }
    }
    ```

    다음으로 `src/lib/data/seo-hierarchy.test.ts` 생성:

    ```typescript
    /**
     * seo-hierarchy 데이터 함수 테스트 — Supabase mock
     * Phase 23
     */
    import { describe, it, expect, vi } from 'vitest'
    import { getSiPageData, getGuPageData, getDongPageData, getComplexBySlug } from './seo-hierarchy'
    import type { SupabaseClient } from '@supabase/supabase-js'

    function makeChainMock(returnValue: { data: unknown; error: null | { message: string } }) {
      const chain: Record<string, unknown> = {}
      const methods = ['select','eq','not','is','order','maybeSingle'] as const
      for (const m of methods) {
        chain[m] = vi.fn().mockReturnThis()
      }
      // terminal methods
      ;(chain['maybeSingle'] as ReturnType<typeof vi.fn>).mockResolvedValue(returnValue)
      // last method in chain for list queries — not/order resolves
      const lastChain = { ...chain }
      ;(lastChain['order'] as ReturnType<typeof vi.fn>).mockResolvedValue(returnValue)
      ;(lastChain['not'] as ReturnType<typeof vi.fn>).mockResolvedValue(returnValue)
      return lastChain
    }

    function makeSupabaseMock(tableData: { data: unknown; error: null }) {
      const chain = makeChainMock(tableData)
      return { from: vi.fn().mockReturnValue(chain) } as unknown as SupabaseClient
    }

    describe('getSiPageData', () => {
      it('창원시 — guList 반환', async () => {
        const rows = [
          { gu: '성산구', dong: '내동', avg_sale_per_pyeong: 1000 },
          { gu: '성산구', dong: '사파동', avg_sale_per_pyeong: 1100 },
          { gu: '의창구', dong: '팔용동', avg_sale_per_pyeong: 900 },
        ]
        const supabase = makeSupabaseMock({ data: rows, error: null })
        const result = await getSiPageData('창원시', supabase)
        expect(result).not.toBeNull()
        expect(result!.guList.length).toBe(2)
        expect(result!.dongList.length).toBe(0)
        expect(result!.totalComplexes).toBe(3)
      })

      it('데이터 없으면 null 반환', async () => {
        const supabase = makeSupabaseMock({ data: [], error: null })
        const result = await getSiPageData('없는시', supabase)
        expect(result).toBeNull()
      })
    })

    describe('getComplexBySlug', () => {
      it('빈 urlSlug → null (Pitfall 6 방어)', async () => {
        const supabase = makeSupabaseMock({ data: null, error: null })
        const result = await getComplexBySlug('', supabase)
        expect(result).toBeNull()
        expect((supabase.from as ReturnType<typeof vi.fn>).mock.calls.length).toBe(0)
      })

      it('유효한 slug → 복합 데이터 반환', async () => {
        const complexData = {
          id: 'uuid-1', canonical_name: '대우2차', url_slug: '창원시/성산구/내동/대우2차',
          si: '창원시', gu: '성산구', dong: '내동', status: 'active',
          road_address: null, built_year: 2005, household_count: 300,
          floors_above: 15, heat_type: '지역난방', sgg_code: '38110', lat: 35.2, lng: 128.6,
        }
        const supabase = makeSupabaseMock({ data: complexData, error: null })
        const result = await getComplexBySlug('창원시/성산구/내동/대우2차', supabase)
        expect(result).not.toBeNull()
        expect(result!.url_slug).toBe('창원시/성산구/내동/대우2차')
      })
    })
    ```

    테스트 실행:
    ```
    npx vitest run src/lib/data/seo-hierarchy.test.ts
    ```
  </action>
  <acceptance_criteria>
    - npx vitest run src/lib/data/seo-hierarchy.test.ts → 4개 이상 PASS
    - grep -c "import 'server-only'" src/lib/data/seo-hierarchy.ts → 1
    - grep -c "export async function getSiPageData" src/lib/data/seo-hierarchy.ts → 1
    - grep -c "export async function getGuPageData" src/lib/data/seo-hierarchy.ts → 1
    - grep -c "export async function getDongPageData" src/lib/data/seo-hierarchy.ts → 1
    - grep -c "export async function getComplexBySlug" src/lib/data/seo-hierarchy.ts → 1
    - grep -c "if (!urlSlug) return null" src/lib/data/seo-hierarchy.ts → 1 (Pitfall 6 방어)
  </acceptance_criteria>
  <done>테스트 4개 이상 PASS, 4개 데이터 함수 export 확인</done>
</task>

<task id="T-03" type="execute">
  <title>Task 3: complex-detail.ts url_slug 추가 + sitemap.ts 데이터 함수 확장</title>
  <read_first>
    - src/lib/data/complex-detail.ts — 전체 파일 (ComplexDetail 인터페이스 + getComplexById SELECT 쿼리)
    - src/lib/data/sitemap.ts — 전체 파일 (SitemapEntry 인터페이스 + getComplexesForSitemap 쿼리)
  </read_first>
  <action>
    **complex-detail.ts 수정** — `ComplexDetail` 인터페이스에 `url_slug` 추가 + `getComplexById` SELECT에 `url_slug` 포함:

    ```typescript
    // ComplexDetail 인터페이스에 추가 (기존 lng 다음 줄):
    url_slug: string | null   // SEO-01: 한글 URL 경로 (예: '창원시/성산구/내동/대우2차')

    // getComplexById SELECT 쿼리에 url_slug 추가:
    .select(`
      id, canonical_name, road_address,
      si, gu, dong,
      built_year, household_count, floors_above, heat_type,
      sgg_code, status, lat, lng, url_slug
    `)
    ```

    **sitemap.ts 수정** — `SitemapEntry` 인터페이스 확장 + `getComplexesForSitemap` SELECT 확장:

    ```typescript
    export interface SitemapEntry {
      id:         string
      updated_at: string
      url_slug:   string | null   // SEO-05: 한글 URL 사이트맵 포함
      si:         string | null   // 계층 URL 추출용
      gu:         string | null
      dong:       string | null
    }

    export async function getComplexesForSitemap(
      supabase: SupabaseClient<Database>,
    ): Promise<SitemapEntry[]> {
      const { data } = await supabase
        .from('complexes')
        .select('id, updated_at, url_slug, si, gu, dong')  // url_slug + 위치 추가
        .eq('status', 'active')
        .order('updated_at', { ascending: false })
        .limit(50000)
      return data ?? []
    }
    ```

    수정 완료 후 빌드 확인:
    ```
    npm run build 2>&1 | tail -20
    ```
    TypeScript 타입 에러 없어야 함.
  </action>
  <acceptance_criteria>
    - grep -c "url_slug" src/lib/data/complex-detail.ts → 2 이상 (인터페이스 + SELECT)
    - grep -c "url_slug" src/lib/data/sitemap.ts → 3 이상 (인터페이스 2곳 + SELECT)
    - grep -c "si:" src/lib/data/sitemap.ts → 1 (SitemapEntry에 si 추가)
    - npm run build → TypeScript 에러 0 (기존 페이지들이 ComplexDetail.url_slug를 사용할 때 optional 처리)
    - npm run test → 기존 테스트 PASS 유지
  </acceptance_criteria>
  <done>두 파일 모두 url_slug 포함, npm run build 통과, 기존 테스트 깨지지 않음</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → Supabase | seo-hierarchy.ts 함수들이 anon key로 complexes read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | getComplexBySlug | mitigate | Supabase .eq('url_slug', urlSlug) — parameterized query. `if (!urlSlug) return null` 빈 slug guard |
| T-23-01-02 | Information Disclosure | seo-hierarchy.ts | accept | complexes anon read RLS 기존 정책 유지. 공개 단지 데이터만 노출 |
| T-23-01-03 | Denial of Service | getDongPageData | accept | .not('url_slug', 'is', null) 필터로 143개 위치없는 단지 자동 제외. 결과 건수 상한은 동당 단지 수 (자연 제한) |
</threat_model>

<verification>
- npx vitest run src/lib/utils/url-slug.test.ts → 10개 PASS
- npx vitest run src/lib/data/seo-hierarchy.test.ts → 4개 이상 PASS
- npm run test → 전체 PASS
- npm run build → 에러 없음
</verification>

<success_criteria>
- url-slug.ts 3개 함수 export + 테스트 10개 GREEN
- seo-hierarchy.ts 4개 데이터 함수 export (getSiPageData, getGuPageData, getDongPageData, getComplexBySlug)
- complex-detail.ts ComplexDetail.url_slug 필드 존재
- sitemap.ts SitemapEntry에 url_slug, si, gu, dong 포함
- 기존 테스트 깨지지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/23-seo-url-structure/23-01-SUMMARY.md` 생성
</output>
