---
plan_id: 23-02
phase: 23
wave: 2
depends_on: [23-01]
files_modified:
  - src/app/[...slug]/page.tsx
  - src/app/[...slug]/opengraph-image.tsx
  - src/app/complexes/[id]/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [SEO-02, SEO-03, SEO-04]
must_haves:
  goal: "한글 계층 URL이 SSR로 응답하고, 기존 /complexes/[id]는 308 리다이렉트하며, 모든 페이지에 올바른 메타데이터와 JSON-LD가 포함된다"
  truths:
    - "/창원시 접속 시 SSR HTML에 창원시 구 목록이 포함된다 (D-03, D-05)"
    - "/창원시/성산구/내동/대우2차 접속 시 SSR HTML에 단지 정보가 포함된다 (D-05)"
    - "BreadcrumbList JSON-LD + 눈에 보이는 <nav> breadcrumb HTML이 모든 계층에 존재한다 (D-07)"
    - "/complexes/[uuid] 접속 시 308 → 한글 URL로 영구 리다이렉트된다 (SEO-03)"
    - "title ≤40자, description ≤80자 (D-06)"
    - "모든 페이지에 <meta httpEquiv='content-language' content='ko-kr'> 존재 (D-06)"
    - "5단계 이상 slug는 notFound() 반환 (Pitfall 1 방어)"
  artifacts:
    - path: "src/app/[...slug]/page.tsx"
      provides: "catch-all 라우트: si/gu/dong/complex 4단계 dispatch + generateMetadata + JSON-LD + breadcrumb"
      exports: ["default (SlugPage)", "generateMetadata"]
    - path: "src/app/[...slug]/opengraph-image.tsx"
      provides: "catch-all OG 이미지"
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "기존 페이지 + permanentRedirect (url_slug 있으면 308)"
    - path: "src/app/layout.tsx"
      provides: "content-language meta + RSS autodiscovery link"
  key_links:
    - from: "src/app/[...slug]/page.tsx"
      to: "src/lib/data/seo-hierarchy.ts"
      via: "getSiPageData / getGuPageData / getDongPageData / getComplexBySlug"
      pattern: "import.*seo-hierarchy"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/app/[...slug]/page.tsx"
      via: "permanentRedirect('/' + complex.url_slug)"
      pattern: "permanentRedirect"
---

<objective>
Next.js catch-all `[...slug]/page.tsx`로 한글 계층 URL 4단계를 SSR 구현하고,
기존 `/complexes/[id]` 페이지에 308 리다이렉트를 추가하며,
`layout.tsx`에 content-language meta를 삽입한다.

Purpose: SEO-02(계층 페이지), SEO-03(301 리다이렉트), SEO-04(메타데이터)
Output: 4개 파일 (catch-all page.tsx, opengraph-image.tsx, complexes page.tsx 수정, layout.tsx 수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-seo-url-structure/23-CONTEXT.md
@.planning/phases/23-seo-url-structure/23-RESEARCH.md
@.planning/phases/23-seo-url-structure/23-PATTERNS.md
@.planning/phases/23-seo-url-structure/23-01-SUMMARY.md

<interfaces>
<!-- Wave 1에서 생성된 인터페이스 -->
```typescript
// src/lib/utils/url-slug.ts
export function buildUrlSlug(si, gu, dong, canonicalName): string | null
export function classifySlug(slug: string[]): 'si' | 'gu' | 'dong-or-complex' | 'complex' | 'invalid'
export function buildCanonicalUrl(site: string, slug: string[]): string

// src/lib/data/seo-hierarchy.ts
export interface SiPageData { si: string; guList: Array<{gu, complexCount, avgPrice}>; dongList: Array<{dong, complexCount}>; totalComplexes: number }
export interface GuPageData { si: string; gu: string; dongList: Array<{dong, complexCount, avgPrice}>; totalComplexes: number }
export interface DongPageData { si: string; gu: string | null; dong: string; complexes: Array<{id, canonical_name, url_slug, built_year, household_count}> }
export async function getSiPageData(si, supabase): Promise<SiPageData | null>
export async function getGuPageData(si, gu, supabase): Promise<GuPageData | null>
export async function getDongPageData(si, gu | null, dong, supabase): Promise<DongPageData | null>
export async function getComplexBySlug(urlSlug, supabase): Promise<(ComplexDetail & { url_slug: string }) | null>

// src/lib/data/complex-detail.ts (Wave 1 수정 후)
export interface ComplexDetail {
  id, canonical_name, road_address, si, gu, dong,
  built_year, household_count, floors_above, heat_type,
  sgg_code, status, lat, lng,
  url_slug: string | null   // Wave 1에서 추가됨
}
export async function getComplexById(id, supabase): Promise<ComplexDetail | null>
```

<!-- 기존 catch-all 없음: src/app/ 바로 아래 단일 동적 라우트 없음 (Pitfall 2 안전) -->
<!-- 기존 complexes/[id]/page.tsx permanentRedirect import 위치: line 1 이후 추가 -->
<!-- layout.tsx 현재: <html lang="ko"><body>..., <head> 블록 없음 -->
</interfaces>
</context>

<tasks>

<task id="T-01" type="execute">
  <title>Task 1: src/app/[...slug]/page.tsx 전체 구현 + opengraph-image.tsx</title>
  <read_first>
    - src/app/complexes/[id]/page.tsx — 전체 파일 (imports, Props 타입, generateMetadata, 단지 상세 데이터 fetch 패턴, JSON-LD script 태그 패턴 lines 320-354, header 브레드크럼 HTML 패턴, 모든 data 함수 import 목록)
    - src/app/complexes/[id]/opengraph-image.tsx — 전체 파일 (runtime, alt, size, revalidate, Props, readFileSync TTF 패턴)
    - .planning/phases/23-seo-url-structure/23-RESEARCH.md (Pattern 1, Pattern 6, E-03) — catch-all dispatch 로직, BreadcrumbList/FAQ JSON-LD 헬퍼, generateMetadata 패턴
  </read_first>
  <action>
    `src/app/[...slug]/page.tsx` 파일 생성. 이 파일이 Phase 23의 핵심이다.

    **전체 구조 (순서대로 구현):**

    ```typescript
    import { notFound } from 'next/navigation'
    import type { Metadata } from 'next'
    import Link from 'next/link'
    import { createReadonlyClient } from '@/lib/supabase/readonly'
    import { classifySlug, buildCanonicalUrl } from '@/lib/utils/url-slug'
    import {
      getSiPageData, getGuPageData, getDongPageData, getComplexBySlug,
    } from '@/lib/data/seo-hierarchy'
    // 단지 상세 렌더링에 필요한 기존 data 함수 전부 import (아래 참고)
    import { getComplexById, getComplexTransactionSummary, getComplexRawTransactions } from '@/lib/data/complex-detail'
    // ... 나머지 imports는 src/app/complexes/[id]/page.tsx의 import 목록과 동일하게 유지

    export const revalidate = 3600  // 1시간 ISR (계층+단지 통합, D-04 우선순위)

    const SITE = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://danjiondo.kr'

    interface Props {
      params: Promise<{ slug: string[] }>
      searchParams: Promise<{ area_type?: string }>
    }
    ```

    **generateMetadata (D-06 title ≤40자, description ≤80자):**

    ```typescript
    export async function generateMetadata({ params }: Props): Promise<Metadata> {
      const { slug } = await params  // Next.js 15: await params 필수
      const type = classifySlug(slug)
      if (type === 'invalid') return { title: '페이지를 찾을 수 없습니다' }

      const canonical = buildCanonicalUrl(SITE, slug)
      const currentName = slug[slug.length - 1]

      // 단지 상세 메타데이터 (length >= 3이고 DB에서 complex 발견 시)
      if (type === 'complex' || type === 'dong-or-complex') {
        const supabase = createReadonlyClient()
        const urlSlug = slug.join('/')
        const complex = await getComplexBySlug(urlSlug, supabase)
        if (complex) {
          const location = slug.slice(0, -1).join(' ')
          const dong = slug[slug.length - 2] ?? ''
          // ≤40자: '내동 대우2차 아파트 실거래가' = 14자 (충분한 여유)
          const title = `${dong} ${complex.canonical_name} 아파트 실거래가`
          // ≤80자: '[창원시 성산구 내동] 대우2차 최근 실거래가 확인. 평형별 시세·관리비 정보.' = ~42자
          const description = `[${location}] ${complex.canonical_name} 최근 실거래가 확인. 평형별 시세·관리비 정보.`
          return {
            title,
            description,
            alternates: { canonical },
            openGraph: { title, description, url: canonical, siteName: '단지온도', locale: 'ko_KR', type: 'website' },
          }
        }
      }

      // 계층 페이지 메타데이터
      const title = `${currentName} 아파트 실거래가`  // ≤40자
      const description = `${currentName} 아파트 시세·거래량 현황. 단지별 실거래가 목록.`  // ≤80자
      return {
        title,
        description,
        alternates: { canonical },
        openGraph: { title, description, url: canonical, siteName: '단지온도', locale: 'ko_KR', type: 'website' },
      }
    }
    ```

    **BreadcrumbList + FAQ JSON-LD 헬퍼 함수 (D-07, D-12):**

    ```typescript
    function buildBreadcrumbJsonLd(slug: string[]) {
      return {
        '@context': 'https://schema.org',
        '@type': 'BreadcrumbList',
        itemListElement: [
          { '@type': 'ListItem', position: 1, name: '단지온도', item: SITE },
          ...slug.map((segment, i) => ({
            '@type': 'ListItem',
            position: i + 2,
            name: segment,
            // 마지막 항목(단지명)은 item URL 생략 (Google 권장)
            ...(i < slug.length - 1
              ? { item: buildCanonicalUrl(SITE, slug.slice(0, i + 1)) }
              : {}),
          })),
        ],
      }
    }

    // D-12: FAQ JSON-LD는 시·동 레벨만 (구·단지 상세 제외)
    function buildFaqJsonLd(name: string, avgPrice: number | null, totalComplexes: number) {
      return {
        '@context': 'https://schema.org',
        '@type': 'FAQPage',
        mainEntity: [
          {
            '@type': 'Question',
            name: `${name} 아파트 평균 매매가는?`,
            acceptedAnswer: {
              '@type': 'Answer',
              text: avgPrice
                ? `${name} 아파트 평균 매매가는 약 ${Math.round(avgPrice / 10000)}억원입니다. ${totalComplexes}개 단지 기준.`
                : `${name}에는 ${totalComplexes}개 아파트 단지가 있습니다. 개별 단지를 선택해 실거래가를 확인하세요.`,
            },
          },
        ],
      }
    }
    ```

    **눈에 보이는 <nav> 브레드크럼 HTML 컴포넌트 (D-07 필수):**

    ```tsx
    function BreadcrumbNav({ slug }: { slug: string[] }) {
      return (
        <nav aria-label="breadcrumb" style={{ padding: '8px 16px', fontSize: 13, color: 'var(--fg-tertiary, #888)' }}>
          <ol style={{ display: 'flex', flexWrap: 'wrap', gap: 4, listStyle: 'none', margin: 0, padding: 0 }}>
            <li><Link href="/" style={{ color: 'inherit' }}>단지온도</Link></li>
            {slug.map((segment, i) => (
              <li key={i} style={{ display: 'flex', alignItems: 'center', gap: 4 }}>
                <span aria-hidden="true">›</span>
                {i < slug.length - 1 ? (
                  <Link href={buildCanonicalUrl('', slug.slice(0, i + 1))} style={{ color: 'inherit' }}>
                    {segment}
                  </Link>
                ) : (
                  <span style={{ color: 'var(--fg-primary, #111)', fontWeight: 500 }}>{segment}</span>
                )}
              </li>
            ))}
          </ol>
        </nav>
      )
    }
    ```

    **계층 페이지 컴포넌트 (SiPage, GuPage, DongPage — 인라인 RSC):**

    시(시) 페이지 (slug.length === 1):
    ```tsx
    async function SiPage({ si, slug }: { si: string; slug: string[] }) {
      const supabase = createReadonlyClient()
      const data = await getSiPageData(si, supabase)
      if (!data) notFound()

      const breadcrumbJsonLd = buildBreadcrumbJsonLd(slug)
      // D-12: 시 레벨에 FAQ JSON-LD 포함
      const faqJsonLd = buildFaqJsonLd(si, null, data.totalComplexes)

      return (
        <main>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumbJsonLd) }} />
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd) }} />
          <BreadcrumbNav slug={slug} />
          <section aria-labelledby="si-heading">
            <h1 id="si-heading">{si} 아파트 실거래가</h1>
            <p>총 {data.totalComplexes}개 단지</p>
            {data.guList.length > 0 && (
              <ul>
                {data.guList.map(g => (
                  <li key={g.gu}>
                    <Link href={buildCanonicalUrl('', [...slug, g.gu])}>{g.gu}</Link>
                    <span> {g.complexCount}개 단지{g.avgPrice ? ` · 평균 ${Math.round(g.avgPrice / 10000)}억` : ''}</span>
                  </li>
                ))}
              </ul>
            )}
            {data.dongList.length > 0 && (
              <ul>
                {data.dongList.map(d => (
                  <li key={d.dong}>
                    <Link href={buildCanonicalUrl('', [...slug, d.dong])}>{d.dong}</Link>
                    <span> {d.complexCount}개 단지</span>
                  </li>
                ))}
              </ul>
            )}
          </section>
        </main>
      )
    }
    ```

    구(구) 페이지 (slug.length === 2, gu 페이지로 확정된 경우):
    ```tsx
    async function GuPage({ si, gu, slug }: { si: string; gu: string; slug: string[] }) {
      const supabase = createReadonlyClient()
      const data = await getGuPageData(si, gu, supabase)
      if (!data) notFound()

      const breadcrumbJsonLd = buildBreadcrumbJsonLd(slug)

      return (
        <main>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumbJsonLd) }} />
          <BreadcrumbNav slug={slug} />
          <section aria-labelledby="gu-heading">
            <h1 id="gu-heading">{si} {gu} 아파트 실거래가</h1>
            <p>총 {data.totalComplexes}개 단지</p>
            <ul>
              {data.dongList.map(d => (
                <li key={d.dong}>
                  <Link href={buildCanonicalUrl('', [...slug, d.dong])}>{d.dong}</Link>
                  <span> {d.complexCount}개 단지{d.avgPrice ? ` · 평균 ${Math.round(d.avgPrice / 10000)}억` : ''}</span>
                </li>
              ))}
            </ul>
          </section>
        </main>
      )
    }
    ```

    동(동) 페이지 (slug.length === 3 창원 or slug.length === 2 김해):
    ```tsx
    async function DongPage({ si, gu, dong, slug }: { si: string; gu: string | null; dong: string; slug: string[] }) {
      const supabase = createReadonlyClient()
      const data = await getDongPageData(si, gu, dong, supabase)
      if (!data) notFound()

      const breadcrumbJsonLd = buildBreadcrumbJsonLd(slug)
      // D-12: 동 레벨에 FAQ JSON-LD 포함
      const faqJsonLd = buildFaqJsonLd(dong, null, data.complexes.length)

      return (
        <main>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumbJsonLd) }} />
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd) }} />
          <BreadcrumbNav slug={slug} />
          <section aria-labelledby="dong-heading">
            <h1 id="dong-heading">{dong} 아파트 실거래가</h1>
            <p>총 {data.complexes.length}개 단지</p>
            <ul>
              {data.complexes.map(c => (
                <li key={c.id}>
                  {c.url_slug ? (
                    <Link href={`/${c.url_slug.split('/').map(encodeURIComponent).join('/')}`}>
                      {c.canonical_name}
                    </Link>
                  ) : (
                    <Link href={`/complexes/${c.id}`}>{c.canonical_name}</Link>
                  )}
                  <span>
                    {c.built_year ? ` ${c.built_year}년` : ''}
                    {c.household_count ? ` · ${c.household_count}세대` : ''}
                  </span>
                </li>
              ))}
            </ul>
          </section>
        </main>
      )
    }
    ```

    **단지 상세 컴포넌트 — ComplexDetailPage:**
    src/app/complexes/[id]/page.tsx의 렌더링 로직을 그대로 재사용.
    `complex.id`로 모든 기존 데이터 함수를 호출하고 같은 컴포넌트를 렌더.

    ```tsx
    async function ComplexDetailPage({
      complex,
      searchParams,
    }: {
      complex: ComplexDetail & { url_slug: string }
      searchParams: Promise<{ area_type?: string }>
    }) {
      const supabase = createReadonlyClient()
      const id = complex.id

      // src/app/complexes/[id]/page.tsx의 데이터 fetch 로직을 동일하게 복사
      // (아래는 핵심 fetch 예시 — 실제로는 page.tsx의 모든 fetch 포함)
      const { area_type: rawAreaType } = await searchParams
      // ... complex-detail.ts의 나머지 fetch + 컴포넌트 렌더링
      // IMPORTANT: ViewCountTracker import는 절대 경로로:
      //   import { ViewCountTracker } from '@/app/complexes/[id]/ViewCountTracker'
      //   (같은 디렉토리가 아니므로 상대 경로 불가)

      // BreadcrumbList JSON-LD (단지 상세)
      const slugParts = complex.url_slug.split('/')
      const breadcrumbJsonLd = buildBreadcrumbJsonLd(slugParts)

      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumbJsonLd) }} />
          <BreadcrumbNav slug={slugParts} />
          {/* 이하 src/app/complexes/[id]/page.tsx의 return JSX와 동일 */}
        </>
      )
    }
    ```

    **메인 dispatch 함수 (SlugPage):**

    ```typescript
    export default async function SlugPage({ params, searchParams }: Props) {
      const { slug } = await params
      const type = classifySlug(slug)

      // Pitfall 1 방어: 5단계 이상은 404
      if (type === 'invalid') notFound()

      const supabase = createReadonlyClient()

      if (type === 'si') {
        return <SiPage si={slug[0]} slug={slug} />
      }

      if (type === 'gu') {
        // slug.length === 2: 창원시(gu페이지) or 김해시(dong페이지)
        // getGuPageData 시도 → 없으면 getDongPageData (D-02)
        const guData = await getGuPageData(slug[0], slug[1], supabase)
        if (guData) return <GuPage si={slug[0]} gu={slug[1]} slug={slug} />
        const dongData = await getDongPageData(slug[0], null, slug[1], supabase)
        if (dongData) return <DongPage si={slug[0]} gu={null} dong={slug[1]} slug={slug} />
        notFound()
      }

      if (type === 'dong-or-complex') {
        // slug.length === 3: 김해 단지(url_slug 3개) or 창원 동 페이지
        const urlSlug = slug.join('/')
        const complex = await getComplexBySlug(urlSlug, supabase)
        if (complex) return <ComplexDetailPage complex={complex} searchParams={searchParams} />
        // 창원 동 페이지 (si/gu/dong)
        const dongData = await getDongPageData(slug[0], slug[1], slug[2], supabase)
        if (dongData) return <DongPage si={slug[0]} gu={slug[1]} dong={slug[2]} slug={slug} />
        notFound()
      }

      // type === 'complex': slug.length === 4 → 항상 창원 단지 상세
      const urlSlug = slug.join('/')
      const complex = await getComplexBySlug(urlSlug, supabase)
      if (!complex) notFound()
      return <ComplexDetailPage complex={complex} searchParams={searchParams} />
    }
    ```

    이후 `src/app/[...slug]/opengraph-image.tsx` 생성:
    - `src/app/complexes/[id]/opengraph-image.tsx` 전체 복사
    - `interface Props { params: Promise<{ id: string }> }` → `interface Props { params: Promise<{ slug: string[] }> }`
    - `const { id } = await params` → `const { slug } = await params`
    - 단지명: `slug[slug.length - 1] ?? '단지온도'`
    - 위치: `slug.slice(0, -1).join(' ')`
    - Supabase DB 쿼리 불필요 (params에서 직접 추출)
  </action>
  <acceptance_criteria>
    - src/app/[...slug]/page.tsx 파일이 존재한다
    - grep -c "export const revalidate = 3600" src/app/[...slug]/page.tsx → 1
    - grep -c "classifySlug" src/app/[...slug]/page.tsx → 1 이상
    - grep -c "BreadcrumbList" src/app/[...slug]/page.tsx → 1 이상 (JSON-LD)
    - grep -c "aria-label=\"breadcrumb\"" src/app/[...slug]/page.tsx → 1 (visible nav)
    - grep -c "FAQPage" src/app/[...slug]/page.tsx → 1 이상 (D-12)
    - grep -c "notFound()" src/app/[...slug]/page.tsx → 2 이상 (invalid + not found)
    - src/app/[...slug]/opengraph-image.tsx 파일이 존재한다
    - npm run build → 에러 없음
  </acceptance_criteria>
  <done>catch-all page.tsx 빌드 통과, BreadcrumbList + FAQ JSON-LD + visible nav 모두 포함</done>
</task>

<task id="T-02" type="execute">
  <title>Task 2: complexes/[id] permanentRedirect 추가 + layout.tsx content-language meta</title>
  <read_first>
    - src/app/complexes/[id]/page.tsx — lines 1-80 (imports, Props, generateMetadata, 함수 시작부. `if (!complex) notFound()` 위치 확인 — ~line 218)
    - src/app/layout.tsx — 전체 파일 (RootLayout 구조, <html lang="ko"> 확인, <head> 블록 없음 확인)
    - .planning/phases/23-seo-url-structure/23-RESEARCH.md (Pattern 3, Pattern 7) — permanentRedirect 삽입 위치, layout.tsx head 블록 패턴
  </read_first>
  <action>
    **1. src/app/complexes/[id]/page.tsx 수정 (SEO-03):**

    파일 상단 import 목록에 추가:
    ```typescript
    import { permanentRedirect } from 'next/navigation'
    ```

    `if (!complex) notFound()` 바로 다음 줄에 리다이렉트 로직 삽입:
    ```typescript
    if (!complex) notFound()

    // SEO-03: url_slug 있는 단지는 한글 URL로 영구 리다이렉트 (308)
    // D-09: url_slug=null인 ~143개 단지는 기존 페이지 그대로 렌더
    // permanentRedirect는 내부적으로 throw — 이후 코드 실행 안 됨
    if (complex.url_slug) {
      permanentRedirect('/' + complex.url_slug)
    }
    ```

    주의사항:
    - permanentRedirect는 catch-all page.tsx에 절대 추가 금지 (무한 루프, Pitfall 3)
    - url_slug 값은 DB에서 조회된 값 그대로 사용 — 사용자 입력 미포함 (Open redirect 방어)

    **2. src/app/layout.tsx 수정 (SEO-04 D-06):**

    `<html lang="ko" ...>` 바로 다음에 `<head>` 블록 추가:
    ```tsx
    <html lang="ko" className={pretendard.variable}>
      <head>
        {/* SEO-04: Naver Yeti 한국어 페이지 명시 (D-06) */}
        <meta httpEquiv="content-language" content="ko-kr" />
        {/* RSS autodiscovery — 네이버 서치어드바이저 RSS 등록용 (SEO-05 연계) */}
        <link
          rel="alternate"
          type="application/rss+xml"
          title="단지온도 최신 실거래가"
          href="/feed.xml"
        />
      </head>
      <body className="font-sans antialiased">
        <NuqsAdapter>
          {children}
        </NuqsAdapter>
        <Footer />
      </body>
    </html>
    ```

    수정 완료 후 빌드:
    ```
    npm run build
    ```
    기존 단지 상세 페이지에 permanentRedirect가 추가되어도 빌드 에러 없어야 함.
  </action>
  <acceptance_criteria>
    - grep -c "permanentRedirect" src/app/complexes/[id]/page.tsx → 2 (import + 호출)
    - grep -c "complex.url_slug" src/app/complexes/[id]/page.tsx → 1 이상
    - grep -c "permanentRedirect" src/app/[...slug]/page.tsx → 0 (catch-all에는 절대 없어야 함, Pitfall 3)
    - grep -c "httpEquiv=\"content-language\"" src/app/layout.tsx → 1
    - grep -c "content=\"ko-kr\"" src/app/layout.tsx → 1
    - grep -c "feed.xml" src/app/layout.tsx → 1 (RSS autodiscovery)
    - npm run build → 에러 없음
    - npm run test → 기존 테스트 PASS 유지
  </acceptance_criteria>
  <done>permanentRedirect 추가됨, layout.tsx head 블록 존재, npm run build 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저/Yeti 크롤러 → catch-all route | slug 파라미터가 URL에서 유입 |
| /complexes/[id] → permanentRedirect | url_slug는 DB에서만 조회, 사용자 제공값 미사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Spoofing | permanentRedirect | mitigate | url_slug는 DB에서 조회된 값만 사용. 사용자 입력 직접 redirect 없음. `complex.url_slug`가 null이 아닌 경우에만 redirect |
| T-23-02-02 | Tampering | slug parameter | mitigate | classifySlug()로 depth 검증: slug.length > 4 → notFound(). Next.js App Router가 `..` 세그먼트 자동 차단 |
| T-23-02-03 | Denial of Service | 계층 페이지 DB 쿼리 | mitigate | revalidate=3600으로 ISR 캐시. 동일 URL은 1시간 동안 DB 재쿼리 없음 |
| T-23-02-04 | Tampering | BreadcrumbList JSON-LD | accept | slug 값은 Next.js auto-decoded 한글 문자열. JSON.stringify로 출력 시 HTML escape 자동 처리 |
| T-23-02-05 | Spoofing | slug 기반 open redirect | mitigate | catch-all에는 permanentRedirect 없음. 리다이렉트는 /complexes/[id] 에서만, DB 조회 결과값으로만 |
</threat_model>

<verification>
- npm run build → 에러 없음
- npm run test → 기존 테스트 PASS
- 로컬 dev: curl -s http://localhost:3000/창원시 | grep "BreadcrumbList" (JSON-LD 포함 확인)
- 로컬 dev: curl -I http://localhost:3000/complexes/[uuid] → 308 Location: /창원시/... 확인
- 로컬 dev: curl -s http://localhost:3000/창원시/성산구/내동/대우2차 | grep "aria-label" (breadcrumb nav 확인)
</verification>

<success_criteria>
- /창원시, /창원시/성산구, /창원시/성산구/내동, /창원시/성산구/내동/대우2차 모두 SSR 200 응답
- BreadcrumbList JSON-LD + 눈에 보이는 <nav> breadcrumb가 모든 계층에 존재
- /complexes/[uuid] (url_slug 있는 단지) → 308 redirect to 한글 URL
- /complexes/[uuid] (url_slug=null인 143개) → 기존 렌더 유지
- layout.tsx에 content-language=ko-kr meta + RSS autodiscovery link 존재
- npm run build 통과
</success_criteria>

<output>
완료 후 `.planning/phases/23-seo-url-structure/23-02-SUMMARY.md` 생성
</output>
