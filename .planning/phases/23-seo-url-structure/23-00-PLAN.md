---
plan_id: 23-00
phase: 23
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260609000001_phase23_url_slug.sql
  - scripts/backfill-url-slugs.ts
autonomous: false
requirements: [SEO-01]
must_haves:
  goal: "complexes.url_slug 컬럼이 DB에 존재하고 ~1,706개 단지에 한글 경로값이 채워진다"
  truths:
    - "complexes 테이블에 url_slug TEXT 컬럼이 존재한다"
    - "UNIQUE PARTIAL INDEX (url_slug WHERE NOT NULL)가 존재한다"
    - "창원시 단지는 '창원시/구/동/단지명' 형태의 url_slug를 가진다 (D-01, D-02)"
    - "김해시 단지는 '김해시/동/단지명' 형태의 url_slug를 가진다 (D-01, D-02)"
    - "si/dong=null인 ~143개 단지는 url_slug=NULL로 유지된다 (D-09)"
  artifacts:
    - path: "supabase/migrations/20260609000001_phase23_url_slug.sql"
      provides: "url_slug 컬럼 + UNIQUE INDEX + 초기 backfill UPDATE"
    - path: "scripts/backfill-url-slugs.ts"
      provides: "미래 재실행용 idempotent backfill 스크립트"
  key_links:
    - from: "complexes.url_slug"
      to: "src/app/[...slug]/page.tsx"
      via: "getComplexBySlug() .eq('url_slug', urlSlug)"
      pattern: "url_slug"
---

<objective>
complexes 테이블에 url_slug 컬럼을 추가하고 초기 backfill을 실행한다.
이후 Wave 1(데이터 레이어)과 Wave 2(라우팅/SEO)의 모든 작업이 이 컬럼에 의존한다.

Purpose: SEO-01 — 한글 디렉토리 URL의 DB 기반 확립
Output: migration SQL 파일, backfill TypeScript 스크립트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-seo-url-structure/23-CONTEXT.md
@.planning/phases/23-seo-url-structure/23-RESEARCH.md
</context>

<tasks>

<task id="T-01" type="execute">
  <title>Task 1: url_slug 마이그레이션 SQL 작성</title>
  <read_first>
    - supabase/migrations/20260516000001_phase11_map_columns.sql — ADD COLUMN IF NOT EXISTS + CREATE UNIQUE INDEX IF NOT EXISTS 패턴
    - supabase/migrations/20260430000002_complexes.sql — complexes 테이블 스키마 (si, gu, dong, canonical_name 컬럼 이름 확인)
  </read_first>
  <action>
    `supabase/migrations/20260609000001_phase23_url_slug.sql` 파일 생성:

    ```sql
    -- Phase 23: SEO URL 구조 최적화 — complexes.url_slug 컬럼 추가 + 초기 백필
    -- 의존: public.complexes (20260430000002_complexes.sql 이후)
    -- D-01: 한글 URL — si/gu/dong/canonical_name 그대로, 로마자 변환 없음
    -- D-08: url_slug 사전 계산 — 이 마이그레이션이 초기 backfill 수행

    -- ============================================================
    -- 1. 컬럼 추가
    -- ============================================================
    ALTER TABLE public.complexes
      ADD COLUMN IF NOT EXISTS url_slug TEXT;

    -- ============================================================
    -- 2. UNIQUE INDEX (url_slug 기반 단지 조회 O(1) 보장)
    --    Partial index: NULL 제외 (위치 없는 ~143개 단지는 기존 UUID URL 유지, D-09)
    -- ============================================================
    CREATE UNIQUE INDEX IF NOT EXISTS complexes_url_slug_idx
      ON public.complexes(url_slug)
      WHERE url_slug IS NOT NULL;

    -- ============================================================
    -- 3. Backfill
    --    D-02: 창원시(gu 있음) → 4단계 si/gu/dong/name
    --          김해시(gu 없음) → 3단계 si/dong/name
    --    D-09: si/dong=null 단지는 url_slug=NULL 유지
    --    idempotent: url_slug IS NULL 조건으로 이미 채워진 행 건드리지 않음
    -- ============================================================
    UPDATE public.complexes
    SET url_slug = CASE
      WHEN gu IS NOT NULL
        THEN si || '/' || gu || '/' || dong || '/' || canonical_name
      ELSE
        si || '/' || dong || '/' || canonical_name
    END
    WHERE si IS NOT NULL
      AND dong IS NOT NULL
      AND canonical_name IS NOT NULL
      AND url_slug IS NULL;

    -- 결과 예상 (적용 후 확인):
    --   SELECT COUNT(*) FROM complexes WHERE url_slug IS NOT NULL;  -- ~1706
    --   SELECT COUNT(*) FROM complexes WHERE url_slug IS NULL;      -- ~143
    --   SELECT COUNT(*) FROM complexes;                             -- ~1849
    ```
  </action>
  <acceptance_criteria>
    - grep -c "ADD COLUMN IF NOT EXISTS url_slug TEXT" supabase/migrations/20260609000001_phase23_url_slug.sql → 1
    - grep -c "CREATE UNIQUE INDEX IF NOT EXISTS complexes_url_slug_idx" supabase/migrations/20260609000001_phase23_url_slug.sql → 1
    - grep -c "WHERE url_slug IS NOT NULL" supabase/migrations/20260609000001_phase23_url_slug.sql → 1 (partial index)
    - grep -c "url_slug IS NULL" supabase/migrations/20260609000001_phase23_url_slug.sql → 2 이상 (partial index + idempotent WHERE)
    - grep -c "WHEN gu IS NOT NULL" supabase/migrations/20260609000001_phase23_url_slug.sql → 1
  </acceptance_criteria>
  <done>마이그레이션 파일이 존재하고 위 grep 조건 모두 통과한다</done>
</task>

<task id="T-02" type="checkpoint:human-action" gate="blocking">
  <title>Task 2: [BLOCKING] supabase db push — url_slug 컬럼 원격 DB 적용</title>
  <action>
    T-01에서 작성한 마이그레이션을 원격 Supabase DB에 적용한다.
    이 체크포인트가 완료되어야 Wave 1 이후 작업이 시작된다.

    실행 명령:
    ```
    npx supabase db push
    ```

    적용 후 Supabase Studio SQL Editor 또는 psql에서 검증:
    ```sql
    -- 1. 컬럼 존재 확인
    SELECT column_name, data_type
    FROM information_schema.columns
    WHERE table_name = 'complexes' AND column_name = 'url_slug';
    -- 기대: url_slug | text

    -- 2. 백필 결과 확인
    SELECT
      COUNT(*) FILTER (WHERE url_slug IS NOT NULL) AS with_slug,
      COUNT(*) FILTER (WHERE url_slug IS NULL)     AS without_slug
    FROM complexes;
    -- 기대: with_slug >= 1700, without_slug ~143

    -- 3. 창원 4단계 예시
    SELECT url_slug FROM complexes WHERE si = '창원시' AND gu IS NOT NULL LIMIT 3;
    -- 기대: '창원시/성산구/내동/대우2차' 형태

    -- 4. 김해 3단계 예시
    SELECT url_slug FROM complexes WHERE si = '김해시' LIMIT 3;
    -- 기대: '김해시/내동/리버사이드팰리스' 형태

    -- 5. 충돌 없음 확인 (D-10)
    SELECT url_slug, COUNT(*) FROM complexes
    WHERE url_slug IS NOT NULL
    GROUP BY url_slug HAVING COUNT(*) > 1;
    -- 기대: 0 rows
    ```
  </action>
  <acceptance_criteria>
    - url_slug TEXT 컬럼이 complexes 테이블에 존재한다
    - url_slug IS NOT NULL 행이 1,700개 이상이다
    - url_slug IS NULL 행이 100~160개 범위이다
    - UNIQUE INDEX complexes_url_slug_idx가 존재한다 (pg_indexes 조회로 확인)
    - url_slug 중복 행 0건 (충돌 없음)
  </acceptance_criteria>
  <resume_signal>검증 SQL 결과가 기대값과 일치하면 "approved" 입력</resume_signal>
</task>

<task id="T-03" type="execute">
  <title>Task 3: backfill-url-slugs.ts 스크립트 작성</title>
  <read_first>
    - scripts/backfill-jibun-addr.ts — 전체 파일. loadEnvConfig(@next/env), createClient, --dry-run/--limit arg 파싱, process.stdout.write 진행 표시, .is('column', null) idempotent guard 패턴
  </read_first>
  <action>
    `scripts/backfill-url-slugs.ts` 파일 생성.
    마이그레이션 초기 backfill의 TypeScript 재구현 — 신규 단지 추가 후 재실행 또는 수동 점검용.
    인라인 `buildUrlSlug` 함수 포함 (Wave 1에서 src/lib/utils/url-slug.ts 생성 후에도 이 스크립트는 독립 실행 가능해야 함).

    ```typescript
    /**
     * complexes.url_slug 백필 스크립트
     *
     * 마이그레이션에서 초기 백필이 실행됨.
     * 이 스크립트는 신규 단지 추가 후 재실행용 (url_slug=NULL 행만 처리).
     *
     * 실행:
     *   npx tsx --env-file=.env.local scripts/backfill-url-slugs.ts
     *   npx tsx --env-file=.env.local scripts/backfill-url-slugs.ts --dry-run
     *   npx tsx --env-file=.env.local scripts/backfill-url-slugs.ts --limit=100
     *
     * 환경변수: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
     */
    import { loadEnvConfig } from '@next/env'
    import { createClient } from '@supabase/supabase-js'

    loadEnvConfig(process.cwd(), true)

    const SUPABASE_URL     = process.env.NEXT_PUBLIC_SUPABASE_URL
    const SUPABASE_SRV_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY
    if (!SUPABASE_URL)     { console.error('NEXT_PUBLIC_SUPABASE_URL 없음'); process.exit(1) }
    if (!SUPABASE_SRV_KEY) { console.error('SUPABASE_SERVICE_ROLE_KEY 없음'); process.exit(1) }

    const args     = process.argv.slice(2)
    const DRY_RUN  = args.includes('--dry-run')
    const limitArg = args.find(a => a.startsWith('--limit='))
    const LIMIT    = limitArg ? parseInt(limitArg.split('=')[1] ?? '10000', 10) : 10000

    const supabase = createClient(SUPABASE_URL, SUPABASE_SRV_KEY, {
      auth: { autoRefreshToken: false, persistSession: false },
    })

    // D-01, D-02: 한글 그대로, 창원 4단계/김해 3단계
    function buildUrlSlug(
      si: string | null,
      gu: string | null,
      dong: string | null,
      canonicalName: string | null,
    ): string | null {
      if (!si || !dong || !canonicalName) return null
      return gu
        ? `${si}/${gu}/${dong}/${canonicalName}`
        : `${si}/${dong}/${canonicalName}`
    }

    async function main(): Promise<void> {
      console.log(`[backfill-url-slugs] DRY_RUN=${DRY_RUN} LIMIT=${LIMIT}`)

      const { data: rows, error } = await supabase
        .from('complexes')
        .select('id, si, gu, dong, canonical_name')
        .is('url_slug', null)   // url_slug 없는 행만 처리 (idempotent)
        .limit(LIMIT)

      if (error) { console.error('조회 실패:', error.message); process.exit(1) }
      if (!rows || rows.length === 0) { console.log('처리할 행 없음'); return }

      console.log(`처리 대상: ${rows.length}건`)
      let updated = 0, skipped = 0, failed = 0

      for (let i = 0; i < rows.length; i++) {
        const c = rows[i] as { id: string; si: string | null; gu: string | null; dong: string | null; canonical_name: string | null }
        const slug = buildUrlSlug(c.si, c.gu, c.dong, c.canonical_name)

        if (!slug) { skipped++; continue }
        if (DRY_RUN) { updated++; continue }

        const { error: updateErr } = await supabase
          .from('complexes')
          .update({ url_slug: slug })
          .eq('id', c.id)
          .is('url_slug', null)  // 동시성 guard — 이미 채워진 경우 건드리지 않음

        if (updateErr) {
          failed++
          console.error(`\n실패 ${c.id} (${c.canonical_name}):`, updateErr.message)
        } else {
          updated++
        }

        process.stdout.write(
          `\r[${i + 1}/${rows.length}] 업데이트: ${updated} 스킵: ${skipped} 실패: ${failed}`
        )
      }

      console.log(`\n완료 — 업데이트: ${updated} 스킵: ${skipped} 실패: ${failed}`)
    }

    main().catch((err: unknown) => {
      console.error('스크립트 실행 실패:', err)
      process.exit(1)
    })
    ```
  </action>
  <acceptance_criteria>
    - grep -c "loadEnvConfig" scripts/backfill-url-slugs.ts → 1
    - grep -c "\-\-dry-run" scripts/backfill-url-slugs.ts → 1 이상
    - grep -c "is('url_slug', null)" scripts/backfill-url-slugs.ts → 2 (조회 filter + update idempotent guard)
    - grep -c "buildUrlSlug" scripts/backfill-url-slugs.ts → 2 이상 (정의 + 호출)
    - npx tsx --env-file=.env.local scripts/backfill-url-slugs.ts --dry-run --limit=5 → 에러 없이 실행, "DRY_RUN=true" 출력 확인
    <!-- RERUN NOTE (D-08): D-08에서 "ingest 시 자동 계산"이라 명시되었으나, ingest 서비스 수정은 이 Phase 범위 외.
         신규 단지 추가 시 이 스크립트를 재실행하여 url_slug를 채운다 (backfill 재실행 = D-08 대체 인정).
         RERUN 명령: npx tsx --env-file=.env.local scripts/backfill-url-slugs.ts -->
  </acceptance_criteria>
  <done>스크립트가 --dry-run 모드에서 에러 없이 실행되고 grep 조건 모두 통과한다. RERUN NOTE (D-08): 신규 단지 추가 시 재실행 필요</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 스크립트 환경 → DB | backfill-url-slugs.ts가 SUPABASE_SERVICE_ROLE_KEY로 DB 직접 접근 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-00-01 | Information Disclosure | backfill-url-slugs.ts | mitigate | SUPABASE_SERVICE_ROLE_KEY는 .env.local에서만 로드. console.error에 키 출력 금지. loadEnvConfig 사용 |
| T-23-00-02 | Tampering | migration backfill SQL | accept | canonical_name에 SQL injection 가능성 없음 — PostgreSQL || 연결 연산자는 타입 안전 |
| T-23-00-03 | Denial of Service | backfill 스크립트 대량 UPDATE | mitigate | --limit 파라미터로 배치 크기 제한. .is('url_slug', null) guard로 이미 채워진 행 skip |
</threat_model>

<verification>
- supabase/migrations/20260609000001_phase23_url_slug.sql 존재
- scripts/backfill-url-slugs.ts 존재
- T-02 체크포인트 완료 후: SELECT COUNT(*) FROM complexes WHERE url_slug IS NOT NULL → ~1706
- npm run build 통과 (마이그레이션 파일과 스크립트는 빌드에 영향 없음)
</verification>

<success_criteria>
- DB에 url_slug 컬럼이 존재하고 ~1,706개 단지에 한글 경로가 채워진다
- 충돌 없음 (UNIQUE INDEX 보장)
- url_slug=null인 ~143개 단지는 기존 URL 유지 (D-09)
- backfill 스크립트가 --dry-run 모드에서 정상 실행된다
</success_criteria>

<output>
완료 후 `.planning/phases/23-seo-url-structure/23-00-SUMMARY.md` 생성
</output>
