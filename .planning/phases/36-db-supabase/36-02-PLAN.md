---
phase: 36-db-supabase
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - supabase/migrations/20260730000003_cbl_content_rls.sql
  - scripts/verify-cbl-rls.ts
autonomous: false
requirements: [CBL-06, CBL-07]

must_haves:
  truths:
    - "anon 역할로 draft 상태 contents를 조회하면 0행이 반환된다"
    - "anon 역할로 scheduled 상태 contents를 조회하면 0행이 반환된다"
    - "anon 역할로 published이지만 published_at이 미래인 contents를 조회하면 0행이 반환된다"
    - "anon 역할로 published_at이 과거인 published contents는 조회된다 (positive control — 0행이 연결 오류가 아님을 증명)"
    - "anon 역할로 subscribers를 SELECT하면 어떤 행도 반환되지 않는다"
    - "anon 역할로 subscribers에 status='pending' INSERT는 성공하고 status='confirmed' INSERT는 거부된다"
    - "anon 역할로 draft 콘텐츠에 연결된 content_complexes를 조회하면 0행이 반환된다"
    - "신규 5개 테이블의 모든 RLS 정책에 TO 절이 명시되어 있다 (roles가 {public}인 정책이 없다)"
    - "검증에 사용한 테스트 데이터가 프로덕션 DB에서 전부 삭제됐다"
  artifacts:
    - path: "supabase/migrations/20260730000003_cbl_content_rls.sql"
      provides: "신규 5개 테이블 RLS 활성화 + 정책 7개 + 테이블 권한"
      contains: "to anon, authenticated"
      min_lines: 60
    - path: "scripts/verify-cbl-rls.ts"
      provides: "anon 역할 실측 권한 검증 + fixture cleanup"
      contains: "NEXT_PUBLIC_SUPABASE_ANON_KEY"
      min_lines: 80
  key_links:
    - from: "supabase/migrations/20260730000003_cbl_content_rls.sql"
      to: "public.profiles.role"
      via: "editor 정책의 exists 서브쿼리 (cbl_editor / superadmin)"
      pattern: "role in \\('cbl_editor', 'superadmin'\\)"
    - from: "supabase/migrations/20260730000003_cbl_content_rls.sql"
      to: "public.contents.status"
      via: "공개 읽기 정책의 published + published_at 조건"
      pattern: "status = 'published' and published_at <= now\\(\\)"
    - from: "scripts/verify-cbl-rls.ts"
      to: "PostgREST anon 역할"
      via: "createClient(url, anonKey)"
      pattern: "NEXT_PUBLIC_SUPABASE_ANON_KEY"
---

<objective>
신규 5개 테이블에 RLS를 활성화하고 정책 7개 + 테이블 권한을 적용한 뒤, **실제 `anon`
역할로 쿼리를 던져서** draft 유출·이메일 목록 노출이 차단됐는지 실측 검증한다.

Purpose: `TO` 절 누락 같은 버그는 DDL을 읽어서는 잡히지 않는다. 저장소의 유일한
anon-insert 선례(`20260430000009_rls.sql:151-153` `ad_events`)가 정확히 그 버그이며,
이름은 "authenticated insert"인데 실제로는 `anon`에도 적용된다. 이 Phase의 실질적
산출물은 **실측 권한 검증**이다.

Output: `supabase/migrations/20260730000003_cbl_content_rls.sql` +
`scripts/verify-cbl-rls.ts` + 6개 권한 항목 실측 통과 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-db-supabase/36-CONTEXT.md
@.planning/phases/36-db-supabase/36-01-SUMMARY.md
@CLAUDE.md

**적용할 RLS 전문은 `36-CONTEXT.md`의 D-04(182-246행)에 있다.** 원본
`.planning/vision/BRIEF.md` §25-6 / 창부레터 `docs/adr/ADR-003-rls-and-data-security.md`.
**정책 추가·삭제·조건 변경 금지.**

**의도된 저장소 관행 이탈 3건 (D-05) — 리뷰에서 "일관성 위반"으로 되돌리지 말 것:**
1. **모든 정책에 `TO` 절 명시.** PostgreSQL 기본값은 `TO public`이라 `TO` 절이 없으면
   `anon`에도 적용된다. `20260430000009_rls.sql:151-153`의 `ad_events` 정책이 그 버그다 —
   **패턴 복사 금지, 반례로만 참고**. (그 버그 자체 수정은 이 Phase 범위 밖)
2. **`contents`에 `using (true)` 금지.** 저장소에는 `using (true)` + 앱 레이어 필터 관행이
   있으나(`data_sources: public read` 등), `contents`에 적용하면 draft·예약발행 콘텐츠
   유출이 앱 코드 실수 하나에 달린다. 발행 상태를 DB 레벨에서 강제한다
3. **`subscribers`에 SELECT 정책 생성 금지.** 이메일 목록 덤프 + 이메일 존재 여부 오라클
   방지. 구독 중복 판정은 창부레터 Server Action(`service_role`)에서만 수행한다

**절대 하지 말 것:**
- 애플리케이션 코드(`src/**`) 변경 금지 — 이 plan은 `src/`를 전혀 만지지 않는다
  (`src/types/database.ts` 갱신은 36-01에서 이미 완료). RLS는 생성 타입에 영향이 없다
- `src/app/admin/layout.tsx` 수정 금지
- 기존 테이블의 정책 수정 금지 (`ad_events` 버그 포함)
- `card-news/`, `src/lib/cardnews/`, `complex_rankings` 관련 파일 접근 금지
- 검증 fixture를 프로덕션에 남기지 말 것 — cleanup까지 이 plan의 범위다

**적용할 정책 7개 (D-04):**

| 테이블 | 정책명 | cmd | TO | 조건 |
|--------|--------|-----|-----|------|
| `contents` | `contents: public read published` | select | `anon, authenticated` | `status='published' and published_at <= now()` |
| `contents` | `contents: editor all` | all | `authenticated` | `exists(profiles.role in ('cbl_editor','superadmin'))` |
| `subscribers` | `subscribers: anon subscribe` | insert | `anon, authenticated` | `with check (site_id='changbuletter' and status='pending')` |
| `content_bookmarks` | `content_bookmarks: own` | all | `authenticated` | `auth.uid() = user_id` (using + with check) |
| `content_votes` | `content_votes: own` | all | `authenticated` | `auth.uid() = user_id` (using + with check) |
| `content_complexes` | `content_complexes: public read` | select | `anon, authenticated` | `exists(contents published and published_at <= now())` |
| `content_complexes` | `content_complexes: editor write` | all | `authenticated` | `exists(profiles.role in ('cbl_editor','superadmin'))` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS 마이그레이션 파일 작성 (정책 7개 + 테이블 권한)</name>

  <read_first>
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` **D-04 전문** (182-246행) — 적용할 RLS 원본. 그대로 옮긴다
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` **D-05 전문** (248-278행) — 관행 이탈 3건의 근거. 파일 주석에 요약해 남긴다
    - `supabase/migrations/20260430000009_rls.sql` **149-153행** — 🔴 `ad_events`의 `TO` 절 누락 버그. **복사 금지**, 반례
    - `supabase/migrations/20260430000009_rls.sql` (61-188행 전체) — 저장소 RLS 작성 관행: 정책 명명 규칙(`"테이블: 설명"`), `exists (select 1 from public.profiles where id = auth.uid() and role in (...))` 서브쿼리 패턴
    - `supabase/migrations/20260601000004_regional_indicators.sql` (16-17행, 32-33행) — 신규 테이블에 `GRANT SELECT ... TO authenticated, anon` / `GRANT ALL ... TO service_role`를 명시하는 저장소 선례. 이 파일의 권한 블록이 따를 패턴
    - `supabase/migrations/20260730000002_cbl_content_schema.sql` — Wave 1이 만든 테이블·컬럼명 확인
  </read_first>

  <files>supabase/migrations/20260730000003_cbl_content_rls.sql</files>

  <action>
    `supabase/migrations/20260730000003_cbl_content_rls.sql`을 작성한다.

    (1) 파일 상단 주석: 창부레터 공유 스키마 0단계 0-3(RLS), 원본
    `.planning/vision/BRIEF.md` §25-6 / 창부레터 `docs/adr/ADR-003-rls-and-data-security.md`,
    그리고 D-05의 관행 이탈 3건(모든 정책에 `TO` 명시 / `contents`에 `using (true)` 금지 /
    `subscribers` SELECT 정책 미생성)과 각 근거를 요약한다. `ad_events` 선례가 버그라는
    사실도 주석으로 남겨서 후속 작업자가 다시 복사하지 않게 한다.

    (2) `36-CONTEXT.md` D-04의 SQL을 **그대로** 옮긴다: `alter table ... enable row level
    security` 5건 + `create policy` 7건. 위 `<context>` 표의 정책명·cmd·TO·조건이 정확히
    일치해야 한다. 모든 `create policy`에 `to` 절을 쓴다 — 예외 없음.

    (3) 테이블 권한 블록을 추가한다. RLS 정책은 테이블 권한(`GRANT`)이 있을 때만 의미가
    있고, `subscribers` SELECT 차단은 정책 부재만으로는 "0행 반환"에 그치므로 권한 자체를
    회수해서 이중으로 막는다. 저장소 선례는 `20260601000004_regional_indicators.sql`이다.
    순서와 내용:
    - 먼저 5개 테이블 전부에 대해 `revoke all on ... from anon, authenticated`
      (Supabase 기본 권한 상태와 무관하게 결정적으로 만든다)
    - `contents`: `grant select to anon, authenticated` + `grant insert, update, delete to authenticated`
    - `content_complexes`: `grant select to anon, authenticated` + `grant insert, update, delete to authenticated`
    - `content_votes`: `grant select, insert, update, delete to authenticated` (anon 권한 없음)
    - `content_bookmarks`: `grant select, insert, update, delete to authenticated` (anon 권한 없음)
    - `subscribers`: `grant insert to anon, authenticated` — **`select`는 주지 않는다**
    - 5개 테이블 전부: `grant all to service_role`
    - `subscribers`에 SELECT를 주지 않는 결과로 `anon`은 `insert ... returning`을 쓸 수
      없다는 점을 주석으로 명시한다(창부레터의 구독 폼은 `.select()` 체이닝 없이 insert해야
      한다). 이는 D-05 ③의 의도된 결과다

    금지: `subscribers`에 `for select` 정책, `using (true)`, `TO` 절 없는 `create policy`,
    기존 테이블(`ad_events` 포함) 정책 수정, `create table`/`alter table add column`,
    `UPDATE`/`DELETE`/`INSERT` 데이터 조작문.
  </action>

  <acceptance_criteria>
    - `supabase/migrations/20260730000003_cbl_content_rls.sql` 파일이 존재한다
    - `grep -c "enable row level security" supabase/migrations/20260730000003_cbl_content_rls.sql`가 5
    - `grep -c "^create policy" supabase/migrations/20260730000003_cbl_content_rls.sql`가 7
    - 주석을 제외한 `create policy` 개수와 `to ` 절 개수가 일치한다: `grep -v '^\s*--' supabase/migrations/20260730000003_cbl_content_rls.sql | grep -c "^\s*to "`가 7
    - `grep -v '^\s*--' supabase/migrations/20260730000003_cbl_content_rls.sql | grep -c "using (true)"`가 0
    - `grep -v '^\s*--' supabase/migrations/20260730000003_cbl_content_rls.sql | grep -c "public.subscribers for select"`가 0
    - 파일에 `status = 'published' and published_at <= now()`가 2회 등장 (`contents` 공개 읽기 + `content_complexes` 공개 읽기의 exists 서브쿼리)
    - 파일에 `role in ('cbl_editor', 'superadmin')`이 2회 등장 (`contents: editor all` + `content_complexes: editor write`)
    - 파일에 `with check (site_id = 'changbuletter' and status = 'pending')`가 등장
    - 파일에 `grant insert on public.subscribers to anon, authenticated`가 등장하고, `grant select on public.subscribers`는 등장하지 않는다
    - 파일에 `revoke all on`이 등장하고 `grant all on` + `to service_role`이 등장한다
    - `grep -v '^\s*--' supabase/migrations/20260730000003_cbl_content_rls.sql | grep -ciE "^\s*(update|delete|insert)\s|create table|ad_events"`가 0
    - `git status --porcelain src/`가 빈 출력
  </acceptance_criteria>

  <verify>
    <automated>F=supabase/migrations/20260730000003_cbl_content_rls.sql; grep -c "^create policy" $F | grep -qx 7 && grep -v '^\s*--' $F | grep -c "^\s*to " | grep -qx 7 && grep -v '^\s*--' $F | grep -c "using (true)" | grep -qx 0 && grep -v '^\s*--' $F | grep -ciE "create table|ad_events" | grep -qx 0 && echo OK</automated>
  </verify>

  <done>정책 7개 전부에 `TO` 절이 있고, `using (true)`·`subscribers` SELECT 정책이 없으며, 권한 블록이 `subscribers`의 anon SELECT를 회수한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] npm run db:push + 라이브 정책·권한 검증</name>

  <read_first>
    - `supabase/migrations/20260730000003_cbl_content_rls.sql` (Task 1 산출물 전문)
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` D-04 + D-05 (정답지)
    - `.planning/phases/36-db-supabase/36-01-SUMMARY.md` — Wave 1이 적용됐는지 확인. 테이블이 없으면 이 마이그레이션은 실패한다
  </read_first>

  <files>없음 (DB 적용 + 검증)</files>

  <action>
    (1) `npx supabase migration list --linked`로 `20260730000002`가 Remote에 적용됐고 pending이
    `20260730000003` 하나뿐인지 확인한다.
    (2) `revoke all` 대상 테이블명이 신규 5개(`contents`, `content_complexes`,
    `content_votes`, `content_bookmarks`, `subscribers`)뿐이고 기존 테이블이 섞이지 않았음을
    파일에서 직접 확인해 사용자에게 제시한다. 아래 `<how-to-verify>`의 4개 항목을 제시하고
    승인을 요청한다.
    (3) **승인 응답을 받은 뒤에만** `npm run db:push`를 실행한다.
    (4) push 성공 후 Supabase MCP `execute_sql`(project_id `auoravdadyzvuoxunogh`)로 검증한다:
    `pg_class.relrowsecurity`로 5개 테이블 RLS 활성화, `pg_policies`로 정책 7행 + `roles`
    컬럼에 `{public}`이 없음 + `subscribers`에 `cmd='SELECT'` 정책이 없음 + `qual`/`with_check`
    내용, `has_table_privilege`로 anon·authenticated 권한 8건. 마지막으로
    `pg_policies`에서 `tablename='ad_events'` 행을 조회해 기존 버그를 건드리지 않았음을
    기록한다(수정하지 않는다). 기대값은 acceptance criteria에 전부 명시되어 있다.
    (5) 사용자가 거부하면 push하지 않고 마이그레이션 파일만 커밋한 뒤 중단한다.
  </action>

  <what-built>
    `supabase/migrations/20260730000003_cbl_content_rls.sql` — 신규 5개 테이블의 RLS 활성화 +
    정책 7개 + 테이블 권한. 기존 테이블의 정책은 건드리지 않는다.
  </what-built>

  <how-to-verify>
    Claude가 승인 요청 시 제시할 것 (승인 전 `npm run db:push` 실행 금지):
    1. 마이그레이션 파일 전문
    2. `npx supabase migration list --linked` — pending이 `20260730000003` 하나뿐인지 확인
    3. 영향 고지: 대상은 **Wave 1에서 새로 만든 빈 테이블 5개뿐**이다. 기존
       danjiondo·realtrade-story 테이블의 정책·권한은 변경되지 않으므로 운영 서비스 영향 없음
    4. `revoke all ... from anon, authenticated`가 신규 5개 테이블에만 적용됨을 확인
       (기존 테이블명이 revoke 대상에 없는지 파일에서 직접 확인해 제시)

    승인 후 `npm run db:push` 실행 → Supabase MCP `execute_sql`로 acceptance criteria의
    라이브 조회 수행.
  </how-to-verify>

  <acceptance_criteria>
    - 승인 전에 `npm run db:push`가 실행되지 않았다
    - `npm run db:push`가 exit code 0으로 종료됐다
    - `npx supabase migration list --linked` 출력에 `20260730000003`이 Local·Remote 양쪽에 나타난다
    - `select relname, relrowsecurity from pg_class where relname in ('contents','content_complexes','content_votes','content_bookmarks','subscribers')`에서 5개 모두 `relrowsecurity=true`
    - `select tablename, policyname, cmd, roles from pg_policies where schemaname='public' and tablename in ('contents','content_complexes','content_votes','content_bookmarks','subscribers')`가 정확히 7행 반환 (`contents` 2, `content_complexes` 2, `content_votes` 1, `content_bookmarks` 1, `subscribers` 1)
    - 위 7행 중 `roles` 값이 `{public}`인 행이 **0건** (모든 정책에 TO 절 명시 — `ad_events` 버그 미재현)
    - 위 결과에 `tablename='subscribers' and cmd='SELECT'`인 행이 **0건**
    - `select qual from pg_policies where policyname='contents: public read published'` 결과에 `published` 및 `now()`가 포함되고, 값이 `true`가 아니다
    - `select with_check from pg_policies where policyname='subscribers: anon subscribe'` 결과에 `changbuletter`와 `pending`이 포함된다
    - `select has_table_privilege('anon','public.subscribers','select')`가 `false`
    - `select has_table_privilege('anon','public.subscribers','insert')`가 `true`
    - `select has_table_privilege('anon','public.contents','select')`가 `true`
    - `select has_table_privilege('anon','public.contents','insert')`가 `false`
    - `select has_table_privilege('anon','public.content_votes','insert')`가 `false`
    - `select has_table_privilege('anon','public.content_bookmarks','insert')`가 `false`
    - `select has_table_privilege('authenticated','public.contents','insert')`가 `true`
    - `select count(*) from pg_policies where schemaname='public' and tablename='ad_events'`가 1이고 그 정책의 `roles`가 여전히 `{public}` (기존 버그를 이 Phase에서 건드리지 않았음 = 범위 준수 확인)
  </acceptance_criteria>

  <resume-signal>"approved" 입력 시 `npm run db:push` 실행. 거부 시 파일만 커밋하고 중단.</resume-signal>

  <verify>
    <automated>npx supabase migration list --linked | grep 20260730000003</automated>
  </verify>

  <done>RLS가 5개 테이블에서 활성화되고, 정책 7개 전부 `roles`가 `{public}`이 아니며, `anon`의 `subscribers` SELECT 권한이 false다.</done>
</task>

<task type="auto">
  <name>Task 3: anon 실측 권한 검증 스크립트 작성·실행 + fixture cleanup</name>

  <read_first>
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` **Success Criteria 4번** (416-422행) — 검증해야 할 6개 항목의 원본
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` D-05 ③ — `subscribers` SELECT 차단 근거(이메일 존재 여부 오라클)
    - `scripts/detect-busan-dup-candidates.ts` (1-35행) — 이 저장소 독립 스크립트의 관행: 헤더 주석에 실행 커맨드 명시, `NEXT_PUBLIC_SUPABASE_URL` 필수 체크, localhost 경고, `createClient(..., { auth: { autoRefreshToken: false, persistSession: false } })`
    - `supabase/migrations/20260730000002_cbl_content_schema.sql` — fixture insert에 필요한 NOT NULL 컬럼(`contents`: `slug`,`type`,`category`,`title`)
    - `supabase/migrations/20260730000003_cbl_content_rls.sql` — 검증 대상 정책·권한
    - `tsconfig.json` — `exclude`에 `scripts`가 있어 `npm run lint`가 이 스크립트를 타입체크하지 않는다. 스크립트의 검증은 **실행 성공**으로만 확인된다
  </read_first>

  <files>scripts/verify-cbl-rls.ts</files>

  <action>
    `scripts/verify-cbl-rls.ts`를 작성하고 실행한다. 실행 커맨드는
    `npx tsx --env-file=.env.local scripts/verify-cbl-rls.ts`이며 헤더 주석에 명시한다.

    **클라이언트 2개** (`@supabase/supabase-js`의 `createClient`,
    `{ auth: { autoRefreshToken: false, persistSession: false } }`):
    - `admin`: `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` — fixture 생성·정리 전용
    - `anon`: `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` — 실제 `anon`
      역할로 PostgREST를 타는 검증 주체. 서비스 롤 키를 검증에 쓰면 RLS가 우회되므로
      **절대 혼용 금지**
    셋 중 하나라도 없으면 `process.exit(1)`. `NEXT_PUBLIC_SUPABASE_URL`이 localhost/127.0.0.1이면
    경고 출력(프로덕션 검증이 목적이다).

    **fixture** — 모든 식별자에 `zz-cbl-verify-` 접두어를 붙여 cleanup 대상을 명확히 한다.
    `admin` 클라이언트로 생성:
    1. `contents` slug `zz-cbl-verify-draft` — `status='draft'`
    2. `contents` slug `zz-cbl-verify-scheduled` — `status='scheduled'`, `scheduled_at`=now+1d
    3. `contents` slug `zz-cbl-verify-future` — `status='published'`, `published_at`=now+1d
    4. `contents` slug `zz-cbl-verify-live` — `status='published'`, `published_at`=now-1h
       (**positive control**)
    5. `content_complexes` — (1) draft 콘텐츠 ↔ 기존 단지, (4) live 콘텐츠 ↔ 같은 단지.
       단지는 `select id from complexes limit 1`로 조회한 기존 행을 쓴다. **`complexes`에
       새 행을 만들지 않는다** (Golden Record)
    6. `subscribers` — `zz-cbl-verify-seed@example.invalid`, `status='confirmed'`
       (SELECT 차단 테스트가 "숨길 대상"이 실제로 존재한 상태에서 검증되도록)
    공통 필드는 `type='article'`, `category='zz-verify'`, `title` 임의값.

    **anon 검증 항목** — 각 항목의 통과/실패를 배열에 누적하고 최종적으로 표로 출력.
    하나라도 실패면 `process.exitCode = 1`:
    1. `contents` where slug=`zz-cbl-verify-draft` → 0행
    2. `contents` where slug=`zz-cbl-verify-scheduled` → 0행
    3. `contents` where slug=`zz-cbl-verify-future` → 0행
    4. `contents` where slug=`zz-cbl-verify-live` → **1행** (positive control. 이게 0행이면
       위 3개의 0행은 차단 증거가 아니라 연결·권한 오류 증거다 — 반드시 실패로 처리)
    5. `subscribers` select `email` (필터 없음, limit 5) → 반환 데이터가 없어야 한다.
       권한 회수로 에러(코드 `42501`)가 나거나 빈 배열이 나오는 두 경우 모두 통과.
       `zz-cbl-verify-seed@example.invalid`가 결과에 포함되면 즉시 실패
    6. `subscribers` insert `{ site_id:'changbuletter', email:'zz-cbl-verify-anon@example.invalid',
       status:'pending' }` → 에러 없이 성공. **`.select()`를 체이닝하지 않는다** — anon은
       SELECT 권한이 없으므로 returning이 실패한다(D-05 ③의 의도된 결과)
    7. `subscribers` insert `{ site_id:'changbuletter',
       email:'zz-cbl-verify-anon-confirmed@example.invalid', status:'confirmed' }` → 거부.
       에러 코드가 `42501`(RLS 위반)이어야 하며 `23505`(unique 위반)이면 실패로 처리한다
       — 6번과 다른 이메일을 쓰는 이유가 이 구분이다
    8. `content_complexes` where content_id=(1)의 id → 0행
    9. `content_complexes` where content_id=(4)의 id → **1행** (positive control)

    **cleanup** — `try/finally`의 `finally`에서 `admin` 클라이언트로 무조건 실행한다
    (검증 실패로 중간에 빠져나가도 프로덕션에 잔여물이 남으면 안 된다):
    - `delete from contents where slug like 'zz-cbl-verify-%'` (FK cascade로
      `content_complexes` 연결 행도 삭제됨)
    - `delete from subscribers where email like 'zz-cbl-verify-%'`
    - 삭제 후 `admin`으로 두 테이블의 잔여 건수를 재조회해 0인지 확인하고 출력한다.
      0이 아니면 남은 식별자를 콘솔에 출력하고 `process.exitCode = 1`

    스크립트 실행 후 결과 표 전체를 SUMMARY에 붙인다.
  </action>

  <acceptance_criteria>
    - `scripts/verify-cbl-rls.ts` 파일이 존재하고 `NEXT_PUBLIC_SUPABASE_ANON_KEY`와 `SUPABASE_SERVICE_ROLE_KEY`를 각각 별도 클라이언트에 사용한다 (`grep -c "createClient" scripts/verify-cbl-rls.ts`가 2 이상)
    - `npx tsx --env-file=.env.local scripts/verify-cbl-rls.ts`가 exit code 0으로 종료된다
    - 출력에 9개 검증 항목이 모두 PASS로 표시된다
    - **항목 1**: anon `contents` slug=`zz-cbl-verify-draft` 조회 결과 0행
    - **항목 2**: anon `contents` slug=`zz-cbl-verify-scheduled` 조회 결과 0행
    - **항목 3**: anon `contents` slug=`zz-cbl-verify-future` (published_at 미래) 조회 결과 0행
    - **항목 4**: anon `contents` slug=`zz-cbl-verify-live` 조회 결과 1행 (positive control)
    - **항목 5**: anon `subscribers` SELECT가 에러(42501) 또는 0행이고, `zz-cbl-verify-seed@example.invalid`가 결과에 포함되지 않음
    - **항목 6**: anon `subscribers` INSERT `status='pending'`이 에러 없이 성공
    - **항목 7**: anon `subscribers` INSERT `status='confirmed'`가 에러 코드 `42501`로 거부 (`23505`가 아님)
    - **항목 8**: anon `content_complexes` (draft 콘텐츠 연결) 조회 결과 0행
    - **항목 9**: anon `content_complexes` (published 콘텐츠 연결) 조회 결과 1행 (positive control)
    - cleanup 후 service_role로 `select count(*) from contents where slug like 'zz-cbl-verify-%'`가 0
    - cleanup 후 service_role로 `select count(*) from subscribers where email like 'zz-cbl-verify-%'`가 0
    - cleanup 후 `select count(*) from content_complexes` 결과가 0 (이 Phase 이전 데이터가 없으므로 cascade 삭제 확인)
    - `select count(*) from public.complexes` 값이 스크립트 실행 전후 동일 (Golden Record 무영향)
    - `git status --porcelain src/`가 빈 출력 (애플리케이션 코드 무변경)
    - `git diff --quiet src/app/admin/layout.tsx` exit code 0
    - `npm run lint`가 exit code 0
    - `npm run test`가 exit code 0
  </acceptance_criteria>

  <verify>
    <automated>npx tsx --env-file=.env.local scripts/verify-cbl-rls.ts && npm run lint && npm run test && test -z "$(git status --porcelain src/)" && echo OK</automated>
  </verify>

  <done>9개 항목(차단 6 + positive control 3)이 실측 통과하고, fixture가 프로덕션에서 전부 삭제됐으며, lint·test가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 익명 방문자(`anon`) → `contents` | 미발행(draft·scheduled·예약) 콘텐츠가 있는 테이블을 공개 읽기시킨다 |
| 익명 방문자(`anon`) → `subscribers` | 이메일 PII 테이블에 INSERT 경로만 열어준다 |
| 로그인 사용자(`authenticated`) → `content_votes`/`content_bookmarks` | 타인 행 접근 가능성 |
| `cbl_editor` → bds 어드민 콘솔 | 공유 `profiles`를 쓰는 두 사이트 간 권한 격리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Information Disclosure | `contents` draft·scheduled 유출 | mitigate | `contents: public read published` 정책이 `status='published' and published_at <= now()`로 DB 레벨 강제. `using (true)` 금지. Task 3 항목 1·2·3이 실측 검증하고 항목 4가 false-negative를 배제 |
| T-36-02-02 | Information Disclosure | `subscribers` 이메일 목록 덤프 | mitigate | SELECT 정책 미생성 + `revoke all` 후 `grant insert`만 부여(anon SELECT 권한 false). Task 2가 `has_table_privilege`로, Task 3 항목 5가 실측으로 이중 검증 |
| T-36-02-03 | Information Disclosure | 이메일 존재 여부 오라클 | mitigate | anon이 조회·중복확인을 할 수 없으므로 특정 이메일의 구독 여부를 알 수 없다. 중복 판정은 창부레터 `service_role` Server Action 전용 |
| T-36-02-04 | Elevation of Privilege | `TO` 절 누락으로 `anon`이 editor 정책 획득 | mitigate | 정책 7개 전부 `to` 절 명시. Task 1의 `create policy` 개수 = `to` 절 개수 grep 게이트 + Task 2의 `pg_policies.roles != {public}` 라이브 검증 |
| T-36-02-05 | Tampering | anon이 `contents`·`content_complexes` 위조 삽입 | mitigate | `grant`에서 anon에 INSERT/UPDATE/DELETE 미부여 + editor 정책이 `authenticated` 한정. Task 2가 `has_table_privilege('anon','public.contents','insert')=false`로 검증 |
| T-36-02-06 | Tampering | 타인의 투표·북마크 조작 | mitigate | `auth.uid() = user_id`를 `using`과 `with check` 양쪽에 적용(D-04 그대로). anon에는 테이블 권한 자체가 없음 |
| T-36-02-07 | Spoofing | anon이 `status='confirmed'`로 더블 옵트인 우회 | mitigate | `with check (site_id='changbuletter' and status='pending')`. Task 3 항목 7이 `42501` 거부를 실측 확인(unique 위반과 구분) |
| T-36-02-08 | Elevation of Privilege | `cbl_editor`가 bds 어드민 접근 | mitigate | `src/app/admin/layout.tsx` 미변경(`git diff --quiet` 검증). 신규 정책은 bds 테이블을 참조하지 않는다 |
| T-36-02-09 | Tampering | 검증 fixture가 프로덕션에 잔존 | mitigate | `zz-cbl-verify-` 접두어 + `finally` 블록 무조건 cleanup + 삭제 후 잔여 건수 0 검증 |
| T-36-02-10 | Elevation of Privilege | 기존 `ad_events` `TO` 절 누락 버그 | accept | 이 Phase 범위 밖(별건). Task 2가 상태를 기록만 하고 수정하지 않음을 확인 |
</threat_model>

<verification>
1. `npx supabase migration list --linked`에 `20260730000003` Local·Remote 존재
2. `pg_class.relrowsecurity`가 신규 5개 테이블 전부 true
3. `pg_policies` 7행 + `roles`에 `{public}` 없음 + `subscribers` SELECT 정책 없음
4. `has_table_privilege` 8건(anon/authenticated × 테이블)이 기대값과 일치
5. `npx tsx --env-file=.env.local scripts/verify-cbl-rls.ts` exit 0 — 9개 항목 PASS
6. cleanup 후 fixture 잔여 0건, `complexes` 행수 무변화
7. `npm run lint` && `npm run test` 통과
</verification>

<success_criteria>
- 신규 5개 테이블 RLS 정책 7개가 전부 `TO` 절을 갖고 적용됐다 (CBL-06)
- `contents`에 `using (true)` 정책이 없고, `subscribers`에 SELECT 정책이 없다 (CBL-06)
- Success Criteria 4번의 6개 권한 항목이 `anon` 역할 실측으로 통과했다 (CBL-07)
- positive control 3건이 통과해 0행 결과가 연결 오류가 아님이 증명됐다 (CBL-07)
- 검증 fixture가 프로덕션 DB에서 전부 삭제됐다
- `npm run lint`·`npm run test` 통과, `src/**` 무변경
</success_criteria>

<output>
Create `.planning/phases/36-db-supabase/36-02-SUMMARY.md` when done.
반드시 포함할 것: `pg_policies` 7행 원본 출력(`roles` 컬럼 포함), `has_table_privilege`
결과 표, `scripts/verify-cbl-rls.ts` 실행 결과 9항목 표, cleanup 후 잔여 건수,
그리고 **창부레터 저장소에 전달할 사항**(신규 테이블·타입 사용 가능 / anon 구독 insert에
`.select()` 체이닝 금지 / `cbl_editor` 부여는 service_role로 수행 / 0-4~0-7은 미착수).
</output>
