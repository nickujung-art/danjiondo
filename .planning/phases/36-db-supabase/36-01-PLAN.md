---
phase: 36-db-supabase
plan: 01
type: execute
wave: 1
depends_on: [36-00]
files_modified:
  - supabase/migrations/20260730000002_cbl_content_schema.sql
  - src/types/database.ts
autonomous: false
requirements: [CBL-03, CBL-04, CBL-05]

must_haves:
  truths:
    - "public 스키마에 contents·content_complexes·content_votes·content_bookmarks·subscribers 5개 테이블이 존재한다"
    - "contents 테이블의 컬럼·기본값·CHECK가 36-CONTEXT.md D-03의 DDL과 일치한다"
    - "contents_feed_idx·contents_region_tags_idx·contents_category_idx·subscribers_send_idx 4개 인덱스가 존재한다"
    - "subscribers.confirm_token이 DB 기본값으로 자동 생성된다 (앱이 누락해도 확인 불가 레코드가 남지 않는다)"
    - "src/types/database.ts에 신규 5개 테이블 타입이 포함되고 기존 테이블 타입이 유실되지 않았다"
  artifacts:
    - path: "supabase/migrations/20260730000002_cbl_content_schema.sql"
      provides: "콘텐츠 스키마 5개 테이블 + 인덱스 4개"
      contains: "create table public.contents"
      min_lines: 60
    - path: "src/types/database.ts"
      provides: "생성된 Supabase 타입 — 창부레터가 import할 계약"
      contains: "contents:"
  key_links:
    - from: "supabase/migrations/20260730000002_cbl_content_schema.sql"
      to: "public.complexes"
      via: "content_complexes.complex_id FK (Golden Record 참조)"
      pattern: "references public\\.complexes\\(id\\)"
    - from: "supabase/migrations/20260730000002_cbl_content_schema.sql"
      to: "auth.users"
      via: "content_votes.user_id / content_bookmarks.user_id FK"
      pattern: "references auth\\.users\\(id\\)"
    - from: "src/types/database.ts"
      to: "public.contents"
      via: "supabase gen types 재생성"
      pattern: "contents:"
---

<objective>
창부레터 콘텐츠 스키마 5개 테이블(`contents`, `content_complexes`, `content_votes`,
`content_bookmarks`, `subscribers`) + 인덱스 4개를 공유 Supabase에 생성하고,
`src/types/database.ts` 생성 타입을 갱신한다.

Purpose: 창부레터(별도 저장소)가 실데이터로 개발을 시작할 수 있게 하는 이 Phase의 본체다.
타입 갱신이 없으면 창부레터가 타입 없이 개발해야 한다.

Output: `supabase/migrations/20260730000002_cbl_content_schema.sql` + 갱신된
`src/types/database.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-db-supabase/36-CONTEXT.md
@.planning/phases/36-db-supabase/36-00-SUMMARY.md
@CLAUDE.md

**설계는 잠겨 있다(LOCKED).** 적용할 DDL 전문은 `36-CONTEXT.md`의 **D-03**에 있다
(원본 `.planning/vision/BRIEF.md` §25-1, 동일 내용이 창부레터 저장소
`docs/adr/ADR-002-content-schema.md`에도 이관돼 있음). **필드 추가·제거·타입 변경 금지** —
두 저장소가 갈라진다.

**특히 하지 말 것:**
- `contents.updated_at`에 `public.set_updated_at()` 트리거 **추가 금지**. 저장소 관행이긴
  하지만 D-03의 DDL에 없다. 창부레터 ADR과 일치시켜야 하므로 앱 레이어에서 갱신한다
- `contents.site_id`에 CHECK 제약 추가 금지 (D-03에 없음)
- 인덱스에 `CONCURRENTLY` 사용 금지 — 신규 생성 테이블이라 락 대상 행이 0이다.
  (`20260728120000_transactions_dealtype_date_idx.sql`이 `CONCURRENTLY`를 쓴 건 운영 중
  테이블이었기 때문이며, 여기는 상황이 다르다. 또한 `CONCURRENTLY`는 트랜잭션 내에서
  실행할 수 없어 Supabase CLI 파일 단위 트랜잭션과 충돌한다)
- RLS 활성화·정책 생성 금지 — Wave 2(36-02) 담당이다. 이 마이그레이션은 DDL만
- 애플리케이션 코드(`src/**`) 변경 금지. **예외는 `src/types/database.ts` 재생성 1건뿐**
- `card-news/`, `src/lib/cardnews/`, `complex_rankings` 관련 파일 접근 금지

**Wave 0 의존 이유:** `contents.site_id`의 기본값이 `'changbuletter'`다. 36-00이
`favorites`/`ad_campaigns` CHECK를 확장하지 않아도 `contents` 자체 생성은 되지만,
Wave 순서를 어기면 Wave 2 RLS의 `role='cbl_editor'` 참조가 깨진다.

**기대 스키마 규모 (검증 기준값):**

| 테이블 | 컬럼 수 | PK | 비고 |
|--------|---------|-----|------|
| `contents` | 21 | `id` | `slug` unique, `type`·`status` CHECK |
| `content_complexes` | 2 | `(content_id, complex_id)` | FK 2개 모두 `on delete cascade` |
| `content_votes` | 4 | `(content_id, user_id)` | `choice` CHECK(`left`/`right`) |
| `content_bookmarks` | 3 | `(content_id, user_id)` | — |
| `subscribers` | 9 | `id` | `unique (site_id, email)`, `status` 4값 CHECK |
</context>

<tasks>

<task type="auto">
  <name>Task 1: 콘텐츠 스키마 마이그레이션 파일 작성</name>

  <read_first>
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` **D-03 전문** (87-164행) — 적용할 DDL 원본. 이 블록을 그대로 옮긴다
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` D-03 "필드 결정 근거" 표 — 변경 요청이 들어와도 이 근거를 먼저 확인
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` `<scope_fence>` 1·6·7·8번
    - `supabase/migrations/20260728120000_transactions_dealtype_date_idx.sql` — `CONCURRENTLY` 선례. **이 Phase는 쓰지 않는다**는 대조용
    - `supabase/migrations/20260430000009_rls.sql` (5-55행) — 저장소의 `create table` 작성 스타일(컬럼 정렬, 주석 위치, `create index ... where` 부분 인덱스 관행)
    - `supabase/migrations/20260430000005_users.sql` — `references public.profiles(id)` vs `auth.users(id)` 사용 구분 확인용. D-03은 `content_votes`/`content_bookmarks`에서 `auth.users(id)`를 쓴다 — **그대로 유지**
  </read_first>

  <files>supabase/migrations/20260730000002_cbl_content_schema.sql</files>

  <action>
    `supabase/migrations/20260730000002_cbl_content_schema.sql`을 작성한다. 내용은
    `36-CONTEXT.md` D-03의 SQL 블록(87-164행)을 **그대로** 옮긴 것이며, 파일 상단에만
    주석 헤더를 추가한다(창부레터 공유 스키마 0단계 0-3, 원본 `.planning/vision/BRIEF.md`
    §25-1 및 창부레터 `docs/adr/ADR-002-content-schema.md`, RLS는 후속 마이그레이션
    `20260730000003_cbl_content_rls.sql`에서 적용됨을 명시).

    포함 대상 (순서 유지 — `content_complexes`·`content_votes`·`content_bookmarks`가
    `contents`를 FK 참조하므로 `contents`가 먼저 와야 한다):
    1. `create table public.contents` — 21컬럼. `site_id` default `'changbuletter'`,
       `slug` unique, `type` CHECK(`card_news`/`article`), `region_tags text[]` default `'{}'`,
       `status` default `'draft'` CHECK(`draft`/`scheduled`/`published`),
       `is_featured` default `false`, `body jsonb`, VS 투표 3컬럼
       (`vote_question`/`vote_left`/`vote_right`), `created_at`/`updated_at` default `now()`
    2. 인덱스 3개: `contents_feed_idx` (`published_at desc`, `where status='published'`),
       `contents_region_tags_idx` (`using gin (region_tags)`),
       `contents_category_idx` (`(category, published_at desc)`, `where status='published'`)
    3. `create table public.content_complexes` — `content_id`→`public.contents(id)`,
       `complex_id`→`public.complexes(id)`, 둘 다 `on delete cascade`,
       PK `(content_id, complex_id)`
    4. `create table public.content_votes` — `content_id`, `user_id`→`auth.users(id)`,
       `choice` CHECK(`left`/`right`), `created_at`, PK `(content_id, user_id)`
    5. `create table public.content_bookmarks` — `content_id`, `user_id`→`auth.users(id)`,
       `created_at`, PK `(content_id, user_id)`
    6. `create table public.subscribers` — 9컬럼. `site_id` default `'changbuletter'`,
       `status` default `'pending'` CHECK 4값(`pending`/`confirmed`/`unsubscribed`/`bounced`),
       `confirm_token` default `encode(gen_random_bytes(24), 'hex')`,
       `requested_at` default `now()`, `confirmed_at`·`unsubscribed_at` nullable,
       `source` nullable, `unique (site_id, email)`
    7. 인덱스 1개: `subscribers_send_idx` (`(site_id)`, `where status='confirmed'`)

    D-03의 인라인 한글 주석은 유지한다(필드 의도가 창부레터 개발자에게 전달돼야 한다).
    `alter table ... enable row level security`, `create policy`, `create trigger`,
    `grant`/`revoke`, `CONCURRENTLY`, `UPDATE`/`DELETE`/`insert`는 넣지 않는다.
  </action>

  <acceptance_criteria>
    - `supabase/migrations/20260730000002_cbl_content_schema.sql` 파일이 존재한다
    - `grep -c "^create table public\." supabase/migrations/20260730000002_cbl_content_schema.sql`가 5
    - 파일에 `create table public.contents`·`create table public.content_complexes`·`create table public.content_votes`·`create table public.content_bookmarks`·`create table public.subscribers`가 각각 1회 등장
    - `grep -c "^create index" supabase/migrations/20260730000002_cbl_content_schema.sql`가 4
    - 파일에 `contents_feed_idx`·`contents_region_tags_idx`·`contents_category_idx`·`subscribers_send_idx` 4개 인덱스명이 모두 등장
    - `grep -v '^\s*--' supabase/migrations/20260730000002_cbl_content_schema.sql | grep -ciE "concurrently|enable row level security|create policy|create trigger|^\s*(grant|revoke|update|delete|insert)\s"`가 0
    - 파일에 `default 'changbuletter'`가 2회 등장 (`contents.site_id`, `subscribers.site_id`)
    - 파일에 `encode(gen_random_bytes(24), 'hex')`가 등장
    - 파일에 `using gin (region_tags)`가 등장
    - 파일에 `references public.complexes(id) on delete cascade`가 등장 (Golden Record 참조)
    - 파일에 `references auth.users(id) on delete cascade`가 2회 등장
    - `git status --porcelain src/`가 빈 출력 (이 태스크는 애플리케이션 코드를 만지지 않는다)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "^create table public\." supabase/migrations/20260730000002_cbl_content_schema.sql | grep -qx 5 && grep -c "^create index" supabase/migrations/20260730000002_cbl_content_schema.sql | grep -qx 4 && grep -v '^\s*--' supabase/migrations/20260730000002_cbl_content_schema.sql | grep -ciE "concurrently|enable row level security|create policy|create trigger" | grep -qx 0 && echo OK</automated>
  </verify>

  <done>5개 테이블 + 4개 인덱스 DDL이 D-03과 동일하게 작성되고, RLS·트리거·CONCURRENTLY가 섞여 들어가지 않았다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] npm run db:push + 라이브 스키마 일치 검증</name>

  <read_first>
    - `supabase/migrations/20260730000002_cbl_content_schema.sql` (Task 1 산출물 전문)
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` D-03 (기대 컬럼·기본값·CHECK의 정답지) + Success Criteria 3번
    - `.planning/phases/36-db-supabase/36-00-SUMMARY.md` — Wave 0이 실제로 적용됐는지 확인 (미적용 시 이 Wave를 진행하면 안 된다)
  </read_first>

  <files>없음 (DB 적용 + 검증)</files>

  <action>
    (1) `npx supabase migration list --linked`로 `20260730000001`이 Remote에 적용됐고 pending이
    `20260730000002` 하나뿐인지 확인한다. Wave 0이 Remote에 없으면 중단하고 보고한다.
    (2) 아래 `<how-to-verify>`의 4개 항목(파일 전문 · migration list · FK 생성 시 짧은
    `SHARE ROW EXCLUSIVE` 락 고지 · 디스크 영향)을 제시하고 승인을 요청한다.
    (3) **승인 응답을 받은 뒤에만** `npm run db:push`를 실행한다.
    (4) push 성공 후 Supabase MCP `execute_sql`(project_id `auoravdadyzvuoxunogh`)로 라이브
    스키마를 검증한다: `information_schema.tables`로 5개 테이블 존재,
    `information_schema.columns`로 테이블별 컬럼 수(21/2/4/3/9)와 `column_default`,
    `pg_constraint`(+`pg_get_constraintdef`)로 CHECK·unique·FK의 `confdeltype`,
    `pg_indexes`로 인덱스 4개와 `indexdef`의 `USING gin`·부분 조건,
    `pg_class.relrowsecurity`로 RLS가 아직 꺼져 있음을 확인한다. 기대값은 acceptance
    criteria에 전부 명시되어 있다.
    (5) 사용자가 거부하면 push하지 않고 마이그레이션 파일만 커밋한 뒤 중단한다.
  </action>

  <what-built>
    `supabase/migrations/20260730000002_cbl_content_schema.sql` — 신규 테이블 5개 + 인덱스 4개.
    전부 신규 생성(additive)이라 기존 테이블·데이터에 영향이 없다.
  </what-built>

  <how-to-verify>
    Claude가 승인 요청 시 제시할 것 (승인 전 `npm run db:push` 실행 금지):
    1. 마이그레이션 파일 전문
    2. `npx supabase migration list --linked` — `20260730000001`이 Remote에 적용된 상태이고,
       pending이 `20260730000002` 하나뿐인지 확인
    3. 영향 고지: 전부 `create table`/`create index`이므로 기존 테이블 락 없음. 단
       `content_complexes`가 `public.complexes`를, `content_votes`/`content_bookmarks`가
       `auth.users`를 FK 참조하므로 해당 테이블에 짧은 `SHARE ROW EXCLUSIVE` 락이 걸린다
       (FK 생성 시점, 밀리초 단위)
    4. 디스크 영향: 빈 테이블 5개 + 인덱스 4개로 무시 가능. Supabase Free tier 잔여 용량
       상황(`.planning/STATE.md` Decisions Log 2026-07-08·07-10 참고)에 영향 없음

    승인 후 `npm run db:push` 실행 → 아래 acceptance criteria의 라이브 조회를 Supabase MCP
    `execute_sql`로 수행한다.
  </how-to-verify>

  <acceptance_criteria>
    - 승인 전에 `npm run db:push`가 실행되지 않았다
    - `npm run db:push`가 exit code 0으로 종료됐다
    - `npx supabase migration list --linked` 출력에 `20260730000002`가 Local·Remote 양쪽에 나타난다
    - `select table_name from information_schema.tables where table_schema='public' and table_name in ('contents','content_complexes','content_votes','content_bookmarks','subscribers')`가 정확히 5행 반환
    - `select count(*) from information_schema.columns where table_schema='public' and table_name='contents'`가 21
    - `subscribers`=9, `content_votes`=4, `content_bookmarks`=3, `content_complexes`=2 컬럼 수 일치
    - `select column_name, column_default from information_schema.columns where table_schema='public' and table_name='contents'` 결과에서 `site_id` 기본값에 `'changbuletter'`, `status` 기본값에 `'draft'`, `is_featured` 기본값에 `false`, `region_tags` 기본값에 `'{}'`가 포함된다
    - `subscribers.confirm_token`의 `column_default`에 `gen_random_bytes(24)`가 포함되고 `subscribers.status` 기본값에 `'pending'`이 포함된다
    - `select conname, pg_get_constraintdef(oid) from pg_constraint where conrelid = 'public.contents'::regclass` 결과에 `type`의 CHECK(`card_news`,`article`)와 `status`의 CHECK(`draft`,`scheduled`,`published`)가 존재하고, `slug`에 unique 제약이 존재한다
    - `pg_constraint` 조회에서 `public.subscribers`에 `(site_id, email)` unique 제약이 존재하고 `status` CHECK에 4개 값(`pending`,`confirmed`,`unsubscribed`,`bounced`)이 모두 포함된다
    - `pg_constraint` 조회에서 `content_complexes`·`content_votes`·`content_bookmarks`의 모든 FK가 `confdeltype='c'`(ON DELETE CASCADE)이다
    - `select indexname, indexdef from pg_indexes where schemaname='public' and tablename in ('contents','subscribers')` 결과에 `contents_feed_idx`·`contents_region_tags_idx`·`contents_category_idx`·`subscribers_send_idx`가 모두 존재
    - `contents_region_tags_idx`의 `indexdef`에 `USING gin`이 포함된다
    - `contents_feed_idx`의 `indexdef`에 `WHERE (status = 'published'`가 포함되고, `subscribers_send_idx`의 `indexdef`에 `WHERE (status = 'confirmed'`가 포함된다
    - `select relname, relrowsecurity from pg_class where relname in ('contents','content_complexes','content_votes','content_bookmarks','subscribers')`에서 5개 모두 `relrowsecurity=false` (RLS는 Wave 2 담당 — 이 Wave에서 켜지지 않았음을 확인)
  </acceptance_criteria>

  <resume-signal>"approved" 입력 시 `npm run db:push` 실행. 거부 시 파일만 커밋하고 중단.</resume-signal>

  <verify>
    <automated>npx supabase migration list --linked | grep 20260730000002</automated>
  </verify>

  <done>5개 테이블·4개 인덱스가 프로덕션에 존재하고, 컬럼 수·기본값·CHECK·FK·부분 인덱스 조건이 D-03과 일치한다.</done>
</task>

<task type="auto">
  <name>Task 3: src/types/database.ts 재생성 + lint·test 회귀 확인</name>

  <read_first>
    - `src/types/database.ts` (1-30행) — 기존 생성 파일의 헤더 형태(`__InternalSupabase.PostgrestVersion`). 재생성 결과가 같은 형식이어야 한다. 전체 4,180행이므로 앞부분만 읽고 형식만 확인한다
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` D-07 — 타입 갱신은 **필수**이며 방법 선택은 재량
    - `CLAUDE.md` 명령어 섹션 — `npm run lint`(ESLint + tsc), `npm run test`(Vitest)
  </read_first>

  <files>src/types/database.ts</files>

  <action>
    (1) 생성 타입을 재생성해 `src/types/database.ts`를 **전체 덮어쓴다**. 우선 순위:
    `npx supabase gen types typescript --project-id auoravdadyzvuoxunogh --schema public`
    출력을 파일로 저장. CLI 인증 문제로 실패하면 Supabase MCP `generate_typescript_types`
    결과로 대체한다. 어느 경로를 썼는지 SUMMARY에 기록한다.

    (2) 재생성 결과를 검증한다. 신규 5개 테이블이 들어왔는지, **그리고 기존 테이블 타입이
    유실되지 않았는지** 양쪽을 확인한다 — 스키마 옵션을 잘못 주면 파일이 축소되어
    프로젝트 전역 타입이 깨진다. 재생성 전 행수(4,180)와 후 행수를 비교하고 감소했다면
    즉시 되돌린다(`git checkout -- src/types/database.ts`) 후 재시도.

    (3) `npm run lint` (ESLint + `tsc --noEmit`)과 `npm run test` (Vitest)를 실행한다.
    타입 재생성으로 기존 코드에 타입 에러가 생기면 **애플리케이션 코드를 고치지 말고**
    원인을 SUMMARY에 기록하고 보고한다 — 이 Phase는 `src/types/database.ts` 외
    애플리케이션 코드 변경이 금지돼 있다(`<scope_fence>` 7번). 실제 에러가 발생하면
    범위 판단이 필요한 사안이다.
  </action>

  <acceptance_criteria>
    - `grep -c "      contents: {" src/types/database.ts`가 1 이상 (신규 테이블 타입 유입)
    - `src/types/database.ts`에 `content_complexes`·`content_votes`·`content_bookmarks`·`subscribers`가 각각 등장
    - `src/types/database.ts`에 `region_tags`·`confirm_token`·`vote_question`·`cafe_post_url`·`is_featured` 필드명이 등장
    - `grep -c "      complexes: {" src/types/database.ts`가 1 이상 (기존 테이블 타입 보존)
    - `src/types/database.ts`에 `transactions`·`profiles`·`favorites`·`ad_campaigns`가 여전히 등장
    - `wc -l src/types/database.ts` 결과가 재생성 전(4,180행) 이상이다
    - `src/types/database.ts` 1행이 `export type Json =`로 시작한다 (CLI 생성 포맷 유지)
    - `npm run lint`가 exit code 0
    - `npm run test`가 exit code 0
    - `git status --porcelain src/`에 `src/types/database.ts` 외의 변경 파일이 없다
    - `git diff --quiet src/app/admin/layout.tsx` exit code 0
  </acceptance_criteria>

  <verify>
    <automated>npm run lint && npm run test && grep -q "      contents: {" src/types/database.ts && grep -q "      complexes: {" src/types/database.ts && test "$(git status --porcelain src/ | wc -l)" -eq 1 && echo OK</automated>
  </verify>

  <done>`src/types/database.ts`에 신규 5개 테이블 타입이 포함되고 기존 타입이 보존되며, lint·test가 모두 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 창부레터 앱(별도 저장소) → 공유 Supabase `contents`/`subscribers` | 신규 테이블에 다른 저장소 코드가 anon·authenticated로 접근한다 |
| 익명 방문자 → `subscribers` | 이메일 PII가 저장되는 유일한 신규 테이블 |
| 로컬 개발 머신 → 프로덕션 DB | `npm run db:push`로 DDL 적용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Information Disclosure | `contents` (draft·예약 콘텐츠) | mitigate | 이 Wave는 RLS 미적용 상태로 끝난다. Task 2가 `relrowsecurity=false`를 확인하고 **36-02가 즉시 후속 실행되어야 함**을 SUMMARY에 명시. 그 사이 테이블은 비어 있다(데이터 적재는 창부레터 소관) |
| T-36-01-02 | Information Disclosure | `subscribers.email` (PII) | mitigate | Wave 2에서 SELECT 정책 미생성 + anon SELECT 권한 회수. 이 Wave에서는 테이블만 생성하고 데이터를 넣지 않는다 |
| T-36-01-03 | Spoofing | `subscribers.confirm_token` | mitigate | 토큰을 앱이 아니라 DB 기본값 `encode(gen_random_bytes(24),'hex')`(192비트 CSPRNG)로 생성 — 앱 누락 시 확인 불가 레코드가 남는 문제와 약한 난수 사용을 동시에 차단. Task 2가 `column_default`로 검증 |
| T-36-01-04 | Tampering | `content_votes`/`content_bookmarks` 타인 데이터 | mitigate | PK `(content_id, user_id)` + Wave 2의 `auth.uid() = user_id` 정책. 이 Wave에서는 PK 구성만 검증 |
| T-36-01-05 | Repudiation | 구독 동의 증명 | mitigate | `requested_at`/`confirmed_at` 분리 유지 — `confirmed_at`이 정보통신망법 수신동의의 법적 근거. 필드 병합·삭제 금지 |
| T-36-01-06 | Denial of Service | FK 생성 시 `complexes`·`auth.users` 락 | accept | `create table` 시점의 FK는 참조 테이블에 밀리초 단위 `SHARE ROW EXCLUSIVE` 락만 유발. Task 2에서 사용자에게 고지 |
</threat_model>

<verification>
1. `npx supabase migration list --linked`에 `20260730000002` Local·Remote 존재
2. `information_schema.columns` 컬럼 수 5개 테이블 전부 일치 (21/2/4/3/9)
3. `pg_constraint`로 CHECK·unique·FK cascade 전수 확인
4. `pg_indexes`로 인덱스 4개 + gin/부분 조건 확인
5. `pg_class.relrowsecurity`가 5개 전부 false (RLS는 36-02 담당)
6. `npm run lint` && `npm run test` 통과
</verification>

<success_criteria>
- `contents` + 인덱스 3개가 D-03과 일치하게 존재한다 (CBL-03)
- `content_complexes`·`content_votes`·`content_bookmarks`가 D-03과 일치하게 존재한다 (CBL-04)
- `subscribers` + `subscribers_send_idx`가 D-03과 일치하게 존재한다 (CBL-05)
- `src/types/database.ts`가 신규 5개 테이블을 포함하고 기존 타입을 보존한다
- `npm run lint`·`npm run test` 통과 — danjiondo·realtrade-story 회귀 없음
</success_criteria>

<output>
Create `.planning/phases/36-db-supabase/36-01-SUMMARY.md` when done.
반드시 포함할 것: 컬럼 수 실측값 5개, 타입 재생성에 사용한 경로(CLI 또는 MCP),
`src/types/database.ts` 행수 전/후, **RLS가 아직 꺼져 있으므로 36-02를 즉시 이어서
실행해야 한다는 경고**.
</output>
