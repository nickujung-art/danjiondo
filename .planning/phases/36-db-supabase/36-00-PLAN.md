---
phase: 36-db-supabase
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260730000001_cbl_site_id_role_check.sql
autonomous: false
requirements: [CBL-01, CBL-02]

must_haves:
  truths:
    - "favorites 테이블에 site_id='changbuletter' 행을 insert해도 CHECK 제약 위반이 발생하지 않는다"
    - "ad_campaigns 테이블에 site_id='changbuletter' 행을 insert해도 CHECK 제약 위반이 발생하지 않는다"
    - "profiles.role을 'cbl_editor'로 설정할 수 있다"
    - "마이그레이션 전후 favorites·ad_campaigns·profiles의 기존 값별 행 수가 동일하다"
    - "src/app/admin/layout.tsx가 변경되지 않아 cbl_editor role은 bds 어드민 콘솔에 진입할 수 없다"
  artifacts:
    - path: "supabase/migrations/20260730000001_cbl_site_id_role_check.sql"
      provides: "site_id CHECK 2건 + profiles.role CHECK 1건 확장"
      contains: "changbuletter"
      min_lines: 20
  key_links:
    - from: "supabase/migrations/20260730000001_cbl_site_id_role_check.sql"
      to: "favorites_site_id_check"
      via: "drop constraint → add constraint"
      pattern: "favorites_site_id_check"
    - from: "supabase/migrations/20260730000001_cbl_site_id_role_check.sql"
      to: "ad_campaigns_site_id_check"
      via: "drop constraint → add constraint"
      pattern: "ad_campaigns_site_id_check"
    - from: "supabase/migrations/20260730000001_cbl_site_id_role_check.sql"
      to: "profiles_role_check"
      via: "drop constraint → add constraint"
      pattern: "profiles_role_check"
---

<objective>
공유 Supabase 프로젝트(`auoravdadyzvuoxunogh`)의 CHECK 제약 3건을 확장해서 창부레터가
쓸 수 있는 값(`site_id='changbuletter'`, `role='cbl_editor'`)을 허용한다.

Purpose: 현재 제약이 `('danjiondo','realtrade-story')`로 닫혀 있어 창부레터 insert가
제약 위반으로 **무조건 실패**한다. Wave 1의 `contents.site_id` 기본값
(`'changbuletter'`)과 Wave 2 RLS 정책의 `role='cbl_editor'` 참조가 이 Wave에 의존하므로
반드시 선행해야 한다.

Output: `supabase/migrations/20260730000001_cbl_site_id_role_check.sql` 1개 파일 +
프로덕션 DB 적용 + 제약·행수 검증 결과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-db-supabase/36-CONTEXT.md
@CLAUDE.md

**설계 원본은 36-CONTEXT.md의 D-01·D-02다. 즉석 설계 금지.**

이 Phase는 **운영 중인 서비스 2개(danjiondo·realtrade-story)가 쓰는 프로덕션 DB**를
변경한다. CHECK 제약은 `DROP CONSTRAINT` → `ADD CONSTRAINT` 순서라 대상 테이블에
**짧은 ACCESS EXCLUSIVE 락**이 걸린다. 대상은 `favorites`·`ad_campaigns`·`profiles`로
전부 작은 테이블이지만, `npm run db:push` 실행은 사용자 승인 후에만 한다.

**절대 하지 말 것 (36-CONTEXT.md `<scope_fence>`):**
- `src/app/admin/layout.tsx` 수정 금지 — 그 파일을 안 건드리는 것이 권한 격리의 구현이다
  (`role in ('admin','superadmin')` 게이트가 유지되어야 `cbl_editor`가 bds 어드민에서 차단됨)
- 마이그레이션에 `UPDATE`/`DELETE` 문 금지 — CHECK 확장은 허용 값을 넓히기만 하므로
  기존 행을 만질 이유가 없다. `UPDATE`가 들어가면 잘못 작성한 것이다
- 애플리케이션 코드(`src/**`) 변경 금지
- `card-news/`, `src/lib/cardnews/`, `complex_rankings` 관련 파일 접근 금지 (0-4~0-7은 별도 Phase)

**현재 제약 상태 (마이그레이션 파일 기준):**

| 제약명 | 테이블 | 현재 값 | 목표 값 |
|--------|--------|---------|---------|
| `favorites_site_id_check` | `favorites` | `('danjiondo','realtrade-story')` | `+ 'changbuletter'` |
| `ad_campaigns_site_id_check` | `ad_campaigns` | `('danjiondo','realtrade-story')` | `+ 'changbuletter'` |
| `profiles_role_check` | `profiles` | `('user','admin','superadmin','advertiser')` | `+ 'cbl_editor'` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: 제약 전수 재확인 + 마이그레이션 파일 작성 + 기준 행수 기록</name>

  <read_first>
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` (D-01, D-02, `<scope_fence>`) — 확장할 제약과 금지사항의 원본
    - `supabase/migrations/20260715000001_realtrade_story_site_scoping.sql` (11행, 43행) — `favorites_site_id_check` / `ad_campaigns_site_id_check`의 정확한 현재 정의
    - `supabase/migrations/20260723081433_fix_favorites_area_type_unique.sql` — `favorites` 제약을 나중에 또 만진 이력. `site_id` CHECK는 건드리지 않았음을 확인하기 위해 읽는다
    - `supabase/migrations/20260430000005_users.sql` (8-9행) — `profiles.role` 인라인 CHECK 원문
    - `supabase/migrations/20260430000009_rls.sql` (102-105행) — `profiles: owner update` 정책의 `with check (... role in ('user'))`. 사용자 자력 role 상승이 막혀 있음을 확인 (cbl_editor 부여는 service_role 전용)
  </read_first>

  <files>supabase/migrations/20260730000001_cbl_site_id_role_check.sql</files>

  <action>
    (1) `grep -rn "site_id" supabase/migrations/`를 실행해 `site_id` CHECK 제약이 D-01의
    2건(`favorites_site_id_check`, `ad_campaigns_site_id_check`) 외에 더 있는지 전수
    재확인한다. 3건 이상 발견되면 발견된 전부를 확장 대상에 포함하고 SUMMARY에 기록한다.

    (2) Supabase MCP `execute_sql`(project_id `auoravdadyzvuoxunogh`)로 라이브 제약명과
    현재 정의를 확인한다: `pg_constraint`를 `conname in ('favorites_site_id_check',
    'ad_campaigns_site_id_check','profiles_role_check')`로 조회하고 `pg_get_constraintdef(oid)`를
    출력한다. `profiles_role_check`는 인라인 CHECK의 자동 생성 이름이므로 라이브 이름이
    다를 수 있다 — 다르면 **라이브에서 확인된 실제 이름을 마이그레이션에 쓴다**.

    (3) 기준 행수를 기록한다(push 후 비교용). `favorites`를 `site_id`별로,
    `ad_campaigns`를 `site_id`별로, `profiles`를 `role`별로 `group by`+`count(*)` 조회한
    결과를 그대로 SUMMARY에 적을 수 있게 남긴다.

    (4) `supabase/migrations/20260730000001_cbl_site_id_role_check.sql`을 작성한다. 구성:
    파일 상단 주석(창부레터 공유 스키마 0단계 0-1·0-2임을 명시 + `.planning/vision/BRIEF.md`
    §25-7 참조 + ACCESS EXCLUSIVE 락 경고) → `favorites`의 `favorites_site_id_check`
    `drop constraint` 후 `add constraint`로 `check (site_id in ('danjiondo','realtrade-story','changbuletter'))`
    → `ad_campaigns`의 `ad_campaigns_site_id_check` 동일 처리 → `profiles`의
    `profiles_role_check` `drop constraint` 후 `add constraint`로
    `check (role in ('user','admin','superadmin','advertiser','cbl_editor'))`.
    `admin`을 재사용하지 않는 이유(`src/app/admin/layout.tsx:25` 게이트)를 주석으로 남긴다.

    금지: `UPDATE`·`DELETE` 문, 명시적 `begin`/`commit`(Supabase CLI가 파일 단위로 트랜잭션을
    감싼다 — 저장소 기존 마이그레이션도 쓰지 않음), 기존 값 제거, 다른 컬럼·인덱스 추가.
    `drop constraint`에는 `if exists`를 붙이지 않는다 — 제약이 없으면 즉시 실패해야 정상이다.
  </action>

  <acceptance_criteria>
    - `supabase/migrations/20260730000001_cbl_site_id_role_check.sql` 파일이 존재한다
    - `grep -c "changbuletter" supabase/migrations/20260730000001_cbl_site_id_role_check.sql`가 2 이상
    - `grep -v '^--' supabase/migrations/20260730000001_cbl_site_id_role_check.sql | grep -c "cbl_editor"`가 1 이상
    - `grep -v '^--' supabase/migrations/20260730000001_cbl_site_id_role_check.sql | grep -ciE "^\s*(update|delete)\s"`가 0
    - 파일에 `favorites_site_id_check`·`ad_campaigns_site_id_check`·`profiles_role_check`(또는 (2)에서 확인된 실제 이름)이 각각 `drop constraint`와 `add constraint` 양쪽에 등장한다
    - 확장된 3개 CHECK 식에 기존 값이 모두 보존되어 있다: `favorites`/`ad_campaigns`는 `'danjiondo'`와 `'realtrade-story'`, `profiles`는 `'user'`·`'admin'`·`'superadmin'`·`'advertiser'`
    - `grep -rn "site_id" supabase/migrations/` 실행 결과가 SUMMARY에 기록되고, CHECK 제약이 2건임(또는 발견된 건수)이 명시된다
    - `favorites`(site_id별)·`ad_campaigns`(site_id별)·`profiles`(role별) 기준 행수가 숫자로 기록된다
    - `git status --porcelain`에 `supabase/migrations/20260730000001_cbl_site_id_role_check.sql` 외의 신규/변경 파일이 없다 (`.planning/` 문서 제외)
  </acceptance_criteria>

  <verify>
    <automated>test -f supabase/migrations/20260730000001_cbl_site_id_role_check.sql && grep -c "changbuletter" supabase/migrations/20260730000001_cbl_site_id_role_check.sql && grep -v '^--' supabase/migrations/20260730000001_cbl_site_id_role_check.sql | grep -ciE "^\s*(update|delete)\s" | grep -qx 0 && echo OK</automated>
  </verify>

  <done>마이그레이션 파일이 3개 제약을 `drop`→`add`로 확장하고, `UPDATE`/`DELETE`가 없으며, 기준 행수가 기록됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] npm run db:push — 프로덕션 DB 적용 승인</name>

  <read_first>
    - `supabase/migrations/20260730000001_cbl_site_id_role_check.sql` (Task 1 산출물 전문)
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` `<scope_fence>` "프로덕션 DB 주의" 절
    - `CLAUDE.md` 명령어 섹션 — `npm run db:push`가 이 저장소의 마이그레이션 적용 명령
  </read_first>

  <files>없음 (DB 적용만)</files>

  <action>
    (1) `npx supabase migration list --linked`로 pending 마이그레이션을 확인한다.
    `20260730000001` 외에 예상치 못한 pending이 있으면 push하지 않고 사용자에게 보고한다.
    (2) 아래 `<how-to-verify>`의 4개 항목(파일 전문 · pending 목록 · 락 영향 + Task 1에서
    기록한 실제 행수 · 롤백 동작)을 사용자에게 제시하고 승인을 요청한다.
    (3) **승인 응답을 받은 뒤에만** `npm run db:push`를 실행한다. 승인 전 실행 절대 금지.
    (4) push 성공 후 Supabase MCP `execute_sql`(project_id `auoravdadyzvuoxunogh`)로
    `select conname, pg_get_constraintdef(oid) from pg_constraint where conname in
    ('favorites_site_id_check','ad_campaigns_site_id_check','profiles_role_check')`를 실행해
    3행의 정의 문자열을 확인한다. Task 1 (2)에서 라이브 제약명이 달랐다면 그 이름을 쓴다.
    (5) 사용자가 거부하면 push하지 않고 마이그레이션 파일만 커밋한 뒤 Phase를 중단한다.
  </action>

  <what-built>
    `supabase/migrations/20260730000001_cbl_site_id_role_check.sql` — CHECK 제약 3건 확장
    마이그레이션. 아직 프로덕션에 적용되지 않았다.
  </what-built>

  <how-to-verify>
    Claude가 **먼저** 아래를 사용자에게 제시한다(승인 전 `npm run db:push` 실행 금지):
    1. 마이그레이션 파일 전문
    2. `npx supabase migration list --linked`로 확인한 미적용(pending) 마이그레이션 목록 —
       이 파일 외에 예상치 못한 pending 마이그레이션이 함께 올라가지 않는지 확인
    3. 락 영향 고지: `favorites`·`ad_campaigns`·`profiles` 3개 테이블에 `DROP CONSTRAINT`
       → `ADD CONSTRAINT` 동안 짧은 ACCESS EXCLUSIVE 락. `ADD CONSTRAINT`는 기존 행 전체를
       검증 스캔하므로 행수만큼의 시간이 걸린다(Task 1에서 기록한 실제 행수를 함께 제시).
       운영 중인 danjiondo·realtrade-story 요청이 그 순간 대기할 수 있다
    4. 롤백 방법: 실패 시 Supabase CLI가 파일 단위 트랜잭션을 롤백하므로 부분 적용은 없다

    사용자가 승인하면 Claude가 `npm run db:push`를 실행하고, 이어서 Task 3의 검증으로 넘어간다.
    사용자가 거부하면 마이그레이션 파일만 커밋하고 Phase를 중단한다.
  </how-to-verify>

  <acceptance_criteria>
    - 승인 전에 `npm run db:push`가 실행되지 않았다 (승인 응답이 대화에 존재)
    - `npm run db:push`가 exit code 0으로 종료됐다
    - `npx supabase migration list --linked` 출력에서 `20260730000001`이 Local·Remote 양쪽에 나타난다
    - Supabase MCP `execute_sql`로 `select conname, pg_get_constraintdef(oid) from pg_constraint where conname in ('favorites_site_id_check','ad_campaigns_site_id_check','profiles_role_check')` 조회 시 3행이 반환되고, `favorites_site_id_check`·`ad_campaigns_site_id_check` 정의 문자열에 `'changbuletter'`가 포함된다
    - 같은 조회에서 `profiles_role_check` 정의 문자열에 `'cbl_editor'`가 포함된다
    - 같은 조회에서 3개 정의 모두 기존 허용 값(`'danjiondo'`,`'realtrade-story'` / `'user'`,`'admin'`,`'superadmin'`,`'advertiser'`)을 여전히 포함한다
  </acceptance_criteria>

  <resume-signal>"approved" 입력 시 `npm run db:push` 실행. 거부·보류 시 이유를 남기고 중단.</resume-signal>

  <verify>
    <automated>npx supabase migration list --linked | grep 20260730000001</automated>
  </verify>

  <done>마이그레이션이 프로덕션에 적용되고, 라이브 `pg_constraint` 정의 3건에 신규 값이 포함됐다.</done>
</task>

<task type="auto">
  <name>Task 3: 제약 동작 검증 + 기존 데이터 무영향 확인</name>

  <read_first>
    - `.planning/phases/36-db-supabase/36-CONTEXT.md` Success Criteria 1·2번 — 검증해야 할 항목의 원본
    - `supabase/migrations/20260430000007_ads.sql` (6-21행) — `ad_campaigns`의 NOT NULL 컬럼 목록. 테스트 insert에 필요한 최소 컬럼 집합을 여기서 확정한다
    - `supabase/migrations/20260430000005_users.sql` (38-45행) — `favorites`의 NOT NULL 컬럼(`user_id`, `complex_id`) + FK 대상
    - `src/app/admin/layout.tsx` — 25행 게이트가 `role in ('admin','superadmin')`인지 확인만 한다. **수정 금지**
  </read_first>

  <files>없음 (검증 전용 — 파일 생성 없음)</files>

  <action>
    Supabase MCP `execute_sql`(project_id `auoravdadyzvuoxunogh`)로 아래 4단계를 수행한다.

    (1) **행수 비교**: Task 1에서 기록한 기준 행수와 동일한 3개 쿼리(`favorites` site_id별,
    `ad_campaigns` site_id별, `profiles` role별 `count(*)`)를 재실행하고 값이 완전히 동일한지
    비교한다. 두 숫자 세트를 SUMMARY에 병기한다.

    (2) **`favorites` 동작 검증**: `do $$ ... $$` 블록 안에서 `public.favorites`에
    `user_id`=`(select id from public.profiles limit 1)`, `complex_id`=`(select id from
    public.complexes limit 1)`, `site_id='changbuletter'`로 insert한 직후
    `raise exception 'CBL_CHECK_OK_favorites'`를 던진다. 예외가 블록 전체를 롤백하므로
    **행이 남지 않는다**. 반환된 에러 메시지가 `CBL_CHECK_OK_favorites`면 CHECK 통과,
    `violates check constraint "favorites_site_id_check"`면 실패로 판정한다.

    (3) **`ad_campaigns` 동작 검증**: 동일한 `do $$ ... $$` + `raise exception
    'CBL_CHECK_OK_ad_campaigns'` 패턴. `ad_campaigns`는 `advertiser_id`가 nullable이므로 FK
    서브쿼리 없이 NOT NULL 컬럼(`advertiser_name`, `title`, `image_url`, `link_url`,
    `placement`는 `'banner_top'`, `starts_at`=`now()`, `ends_at`=`now() + interval '1 day'`)만
    리터럴로 채우고 `site_id='changbuletter'`를 준다.

    (4) **`profiles.role` 동작 검증**: 동일 패턴으로 `update public.profiles set
    role='cbl_editor' where id = (select id from public.profiles limit 1)` 후
    `raise exception 'CBL_CHECK_OK_profiles'`. 실제 role 변경은 롤백된다 — 이 Phase는
    **실제 사용자에게 cbl_editor를 부여하지 않는다**(창부레터 저장소 소관).

    MCP 세션이 read-only여서 (2)~(4)의 insert/update가 `cannot execute ... in a read-only
    transaction`으로 거부되면, Task 2의 `pg_get_constraintdef` 문자열 검증을 최종 근거로
    채택하고 read-only 제약 때문에 동작 검증을 생략했음을 SUMMARY에 명시한다.
  </action>

  <acceptance_criteria>
    - `favorites` site_id별 count가 Task 1 기록값과 동일 (두 값을 SUMMARY에 병기)
    - `ad_campaigns` site_id별 count가 Task 1 기록값과 동일 (두 값을 SUMMARY에 병기)
    - `profiles` role별 count가 Task 1 기록값과 동일하고, `cbl_editor` role을 가진 행이 0건
    - `favorites` 테스트 블록이 `CBL_CHECK_OK_favorites` 에러로 종료됐다 (CHECK 위반 에러가 아니다)
    - `ad_campaigns` 테스트 블록이 `CBL_CHECK_OK_ad_campaigns` 에러로 종료됐다
    - `profiles` 테스트 블록이 `CBL_CHECK_OK_profiles` 에러로 종료됐다
    - 테스트 후 `select count(*) from public.ad_campaigns where site_id='changbuletter'`가 0 (롤백 확인)
    - `select count(*) from public.favorites where site_id='changbuletter'`가 0 (롤백 확인)
    - `git diff --quiet src/app/admin/layout.tsx` exit code 0 (게이트 미변경 = cbl_editor의 bds 어드민 차단 유지)
    - `git status --porcelain src/`가 빈 출력 (애플리케이션 코드 무변경)
  </acceptance_criteria>

  <verify>
    <automated>git diff --quiet src/app/admin/layout.tsx && test -z "$(git status --porcelain src/)" && echo OK</automated>
  </verify>

  <done>CHECK 제약 3건이 신규 값을 허용하고, 기존 행수가 전후 동일하며, 애플리케이션 코드와 어드민 게이트가 변경되지 않았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 창부레터 앱(별도 저장소) → 공유 Supabase | 다른 저장소의 코드가 같은 `profiles`·`favorites`·`ad_campaigns`를 쓴다 |
| 로컬 개발 머신 → 프로덕션 DB (`npm run db:push`) | 운영 중 서비스 2개가 쓰는 DB에 DDL이 적용된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-00-01 | Elevation of Privilege | `profiles.role` 확장 | mitigate | `admin` 재사용 금지, `cbl_editor` 신규 값 추가. `src/app/admin/layout.tsx:25`의 `role in ('admin','superadmin')` 게이트를 **수정하지 않음**으로써 cbl_editor의 bds 어드민 접근을 원천 차단. Task 3에서 `git diff --quiet`로 미변경 검증 |
| T-36-00-02 | Elevation of Privilege | 사용자 자력 role 상승 | accept | `20260430000009_rls.sql:102-105`의 `profiles: owner update` 정책이 `with check (... role in ('user'))`로 이미 self-escalation을 차단. 이 Phase는 그 정책을 건드리지 않는다 |
| T-36-00-03 | Denial of Service | `ADD CONSTRAINT` 검증 스캔 락 | mitigate | 대상 3개 테이블 전부 소규모. Task 2에서 실제 행수와 락 구간을 사용자에게 고지하고 승인 후에만 push |
| T-36-00-04 | Tampering | 마이그레이션이 기존 행을 변경 | mitigate | 파일에 `UPDATE`/`DELETE` 금지 (Task 1 grep 게이트) + Task 3의 전후 행수 동일 검증 |
</threat_model>

<verification>
1. `npx supabase migration list --linked`에 `20260730000001`이 Local·Remote 양쪽 존재
2. `pg_get_constraintdef`로 3개 제약 정의에 신규 값 + 기존 값 모두 존재
3. `do $$ ... raise exception $$` 롤백 테스트 3건이 CHECK 위반이 아닌 마커 에러로 종료
4. `favorites`·`ad_campaigns`·`profiles` 행수 전후 동일
5. `git status --porcelain src/` 빈 출력
</verification>

<success_criteria>
- `site_id='changbuletter'`가 `favorites`·`ad_campaigns`에서 CHECK 제약을 통과한다 (CBL-01)
- `role='cbl_editor'`가 `profiles`에서 CHECK 제약을 통과한다 (CBL-02)
- 기존 `danjiondo`·`realtrade-story` 행 수가 마이그레이션 전후 동일하다
- `src/app/admin/layout.tsx`와 `src/**` 전체가 변경되지 않았다
- Wave 1(36-01)의 `contents.site_id default 'changbuletter'`가 적용 가능한 상태가 됐다
</success_criteria>

<output>
Create `.planning/phases/36-db-supabase/36-00-SUMMARY.md` when done.
반드시 포함할 것: `grep -rn "site_id"` 전수 확인 결과, 라이브 제약명(자동 생성 이름이
예상과 달랐는지), push 전/후 행수 두 세트, 롤백 테스트 3건의 에러 메시지.
</output>
