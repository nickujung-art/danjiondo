---
phase: 32
plan: "32-01"
title: 카드뉴스 관리 대시보드 통합
wave: 1
autonomous: true
depends_on: []
files_modified:
  - src/components/admin/AdminSidebar.tsx
  - src/components/admin/cardnews/CardnewsDashboardClient.tsx
  - src/app/admin/cardnews/page.tsx
  - src/app/admin/cardnews/scheduler/page.tsx
requirements:
  - CARDNEWS-ADMIN-DASHBOARD-01
must_haves:
  truths:
    - "/admin/cardnews 가 스케줄러 상태 + 빠른 액션 + 신고가 TOP5 텍스트를 한 페이지에서 보여준다"
    - "AdminSidebar에 '스케줄러' 항목이 없고 '카드뉴스 관리'로 표시된다"
    - "/admin/cardnews/scheduler 접근 시 /admin/cardnews 로 redirect 된다"
    - "기존 SchedulerPanel, CardnewsDownloadButton, AdminCardnewsCopyButton 컴포넌트가 재사용된다"
  artifacts:
    - path: "src/components/admin/cardnews/CardnewsDashboardClient.tsx"
      provides: "4-섹션 통합 대시보드 클라이언트 컴포넌트"
    - path: "src/app/admin/cardnews/page.tsx"
      provides: "RSC — auth + topDeals fetch + CardnewsDashboardClient 렌더"
    - path: "src/app/admin/cardnews/scheduler/page.tsx"
      provides: "/admin/cardnews/scheduler → /admin/cardnews redirect"
    - path: "src/components/admin/AdminSidebar.tsx"
      provides: "사이드바 메뉴 업데이트"
  key_links:
    - from: "src/app/admin/cardnews/page.tsx"
      to: "src/components/admin/cardnews/CardnewsDashboardClient.tsx"
      via: "topDeals prop"
    - from: "src/components/admin/cardnews/CardnewsDashboardClient.tsx"
      to: "src/components/admin/cardnews/SchedulerPanel.tsx"
      via: "직접 import 재사용"
---

# Phase 32-01: 카드뉴스 관리 대시보드 통합

## Goal

`/admin/cardnews`를 자동화 상태·빠른 액션·신고가 데이터를 한 화면에서 관리하는 통합 대시보드로 재구성하고, 분리된 스케줄러 페이지를 제거한다.

## Context

```
@src/app/admin/cardnews/page.tsx
@src/components/admin/AdminSidebar.tsx
@src/components/admin/cardnews/SchedulerPanel.tsx
@src/components/admin/CardnewsDownloadButton.tsx
@src/components/admin/AdminCardnewsCopyButton.tsx
```

<interfaces>
<!-- 재사용 컴포넌트 인터페이스 — 변경 없이 그대로 import -->

SchedulerPanel.tsx:
  export function SchedulerPanel()  // no props — fetches /api/admin/cardnews/scheduler internally

CardnewsDownloadButton.tsx:
  export function CardnewsDownloadButton()  // no props — calls /api/cardnews/generate internally

AdminCardnewsCopyButton.tsx:
  interface AdminCardnewsCopyButtonProps { text: string }
  export function AdminCardnewsCopyButton({ text }: AdminCardnewsCopyButtonProps)

AdminSidebar.tsx:
  function buildNavItems(pendingCounts: PendingCounts): NavItem[]
  // NavItem: { label: string, href: string, pendingCount?: number }
  // 현재 항목 순서: 대시보드, 회원관리, 신고관리, 광고관리, 중개사관리, GPS검증,
  //   '카드뉴스 목록'(href:/admin/cardnews), '카드뉴스 빌더', '스케줄러', 시세입력, 재개발관리, 분양검수
</interfaces>

## Wave 1: 통합 + 정리

---

### Task 1: AdminSidebar 메뉴 업데이트

**Type:** code
**Files:**
- `src/components/admin/AdminSidebar.tsx`

**Implementation:**

`buildNavItems` 함수에서 두 가지만 변경한다:

1. `{ label: '카드뉴스 목록', href: '/admin/cardnews' }` → `{ label: '카드뉴스 관리', href: '/admin/cardnews' }`
2. `{ label: '스케줄러', href: '/admin/cardnews/scheduler' }` 항목 전체 삭제

다른 항목·순서·컴포넌트 구조는 건드리지 않는다.

변경 후 `buildNavItems` 결과:
```
대시보드, 회원 관리, 신고 관리, 광고 관리, 중개사 관리, GPS 검증,
카드뉴스 관리 (/admin/cardnews), 카드뉴스 빌더 (/admin/cardnews/builder),
시세 입력, 재개발 관리, 분양 검수
```

**Verify:** `grep -n "스케줄러" src/components/admin/AdminSidebar.tsx` 결과 없음.
`grep -n "카드뉴스 관리" src/components/admin/AdminSidebar.tsx` 결과 1건.

---

### Task 2: CardnewsDashboardClient 신규 작성

**Type:** code
**Files:**
- `src/components/admin/cardnews/CardnewsDashboardClient.tsx` (신규)

**Implementation:**

`'use client'` 클라이언트 컴포넌트. props:

```typescript
interface TopDeal {
  rank: number        // 1-based index
  name: string        // complex canonical_name (없으면 '(단지명 없음)')
  priceEok: number    // Math.round(price / 10000)
  areaSqm: string     // Number(area_m2).toFixed(0)
}

interface CardnewsDashboardClientProps {
  topDeals: TopDeal[]
  cardnewsText: string   // 이미 조합된 SNS 공유 텍스트 (500자 이내)
  periodLabel: string    // 예: "2026.05.27 ~ 2026.06.26"
}
```

**레이아웃 — 4 섹션, 세로 스택, `gap: 24px`:**

전체 wrapper: `maxWidth: 720, margin: '0 auto', padding: '40px 32px'`

**페이지 헤더:**
```tsx
<h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 4px' }}>
  카드뉴스 관리
</h1>
<p style={{ font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-sec)', margin: '0 0 32px' }}>
  주간 신고가 카드뉴스 자동화 상태 및 즉시 실행
</p>
```

**섹션 1 — 자동화 상태 (SchedulerPanel 재사용):**
```tsx
<section>
  <h2 style={{ font: '600 13px/1.4 var(--font-sans)', color: 'var(--fg-sec)',
                textTransform: 'uppercase', letterSpacing: '0.06em', margin: '0 0 12px' }}>
    자동화 상태
  </h2>
  {/* SchedulerPanel 내부에 자체 카드 스타일(.bg-white.border-gray-200.rounded-lg)이 있음.
      감싸는 추가 카드 없이 그대로 렌더한다. */}
  <SchedulerPanel />
</section>
```

**섹션 2 — 빠른 액션:**
```tsx
<section>
  <h2 style={{ /* 섹션 1과 동일 레이블 스타일 */ }}>빠른 액션</h2>
  <div className="card" style={{ padding: '24px', display: 'flex', gap: 12, flexWrap: 'wrap', alignItems: 'flex-start' }}>
    <CardnewsDownloadButton />
    {/* CardnewsDownloadButton은 자체 flex column wrapper를 가지므로 바로 배치 */}
  </div>
</section>
```

**섹션 3 — 이번 주 데이터 (신고가 TOP5 + 복사):**
```tsx
<section>
  <h2 style={{ /* 섹션 1과 동일 레이블 스타일 */ }}>이번 주 데이터</h2>
  <div className="card" style={{ padding: '24px' }}>
    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', gap: 16, marginBottom: 16 }}>
      <div>
        <div style={{ font: '600 14px/1.4 var(--font-sans)', marginBottom: 4 }}>
          최근 30일 신고가 TOP 5
        </div>
        <div style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
          {periodLabel} · 창원·김해 실거래가 기준
        </div>
      </div>
      <AdminCardnewsCopyButton text={cardnewsText} />
    </div>
    <ol style={{ margin: 0, padding: '0 0 0 20px', display: 'flex', flexDirection: 'column', gap: 8 }}>
      {topDeals.map((d) => (
        <li key={d.rank} style={{ font: '500 14px/1.5 var(--font-sans)', color: 'var(--fg-pri)' }}>
          <span style={{ fontVariantNumeric: 'tabular-nums' }}>
            {d.name} — {d.priceEok.toLocaleString()}억원
          </span>
          <span style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', marginLeft: 6 }}>
            ({d.areaSqm}㎡)
          </span>
        </li>
      ))}
      {topDeals.length === 0 && (
        <li style={{ color: 'var(--fg-tertiary)', font: '500 14px/1.5 var(--font-sans)', listStyle: 'none' }}>
          최근 30일 거래 데이터 없음
        </li>
      )}
    </ol>
  </div>
</section>
```

**섹션 4 — GitHub Actions 로그 링크:**
```tsx
<section>
  <h2 style={{ /* 섹션 1과 동일 레이블 스타일 */ }}>실행 로그</h2>
  <div className="card" style={{ padding: '20px 24px' }}>
    <a
      href="https://github.com/nickujung-art/bds/actions/workflows/weekly-cardnews.yml"
      target="_blank"
      rel="noopener noreferrer"
      style={{
        font: '500 14px/1.5 var(--font-sans)',
        color: 'var(--fg-brand)',
        textDecoration: 'none',
      }}
    >
      GitHub Actions — weekly-cardnews 워크플로우 로그 보기 →
    </a>
    <p style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '6px 0 0' }}>
      실행 이력·에러 로그는 GitHub Actions에서 직접 확인
    </p>
  </div>
</section>
```

**Import 정리:**
```typescript
'use client'
import { SchedulerPanel } from '@/components/admin/cardnews/SchedulerPanel'
import { CardnewsDownloadButton } from '@/components/admin/CardnewsDownloadButton'
import { AdminCardnewsCopyButton } from '@/components/admin/AdminCardnewsCopyButton'
```

**Verify:** `npx tsc --noEmit 2>&1 | grep CardnewsDashboardClient` 출력 없음.

---

### Task 3: /admin/cardnews/page.tsx 재작성

**Type:** code
**Files:**
- `src/app/admin/cardnews/page.tsx` (전체 재작성)

**Implementation:**

현재 page.tsx의 auth 로직과 topDeals fetch 로직은 유지하되, 렌더 부분을 `CardnewsDashboardClient`로 교체한다.

```typescript
import { redirect } from 'next/navigation'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { CardnewsDashboardClient } from '@/components/admin/cardnews/CardnewsDashboardClient'

export const revalidate = 0

export const metadata = {
  title: '카드뉴스 관리 — 단지온도 관리자',
}

export default async function AdminCardnewsPage() {
  // 인증/권한 확인 (기존 로직 그대로)
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login?next=/admin/cardnews')

  const { data: profile } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', user.id)
    .single()

  if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
    redirect('/')
  }

  // topDeals fetch (기존 쿼리 그대로 유지 — cancel_date IS NULL, superseded_by IS NULL 필수)
  const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000)
    .toISOString().slice(0, 10).replaceAll('-', '')

  const { data: rawDeals } = await supabase
    .from('transactions')
    .select('complex_id, price, area_m2, deal_date, complexes(canonical_name, sgg_code)')
    .gte('deal_date', thirtyDaysAgo)
    .is('cancel_date', null)
    .is('superseded_by', null)
    .eq('deal_type', 'sale')
    .order('price', { ascending: false })
    .limit(5)

  // TopDeal 변환
  const topDeals = (rawDeals ?? []).map((d, i) => {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const c = (d as any).complexes as { canonical_name: string } | null
    return {
      rank: i + 1,
      name: c?.canonical_name ?? '(단지명 없음)',
      priceEok: Math.round(Number(d.price) / 10000),
      areaSqm: Number(d.area_m2).toFixed(0),
    }
  })

  // SNS 공유 텍스트 생성 (기존 로직 유지)
  const periodStart = new Date(thirtyDaysAgo.replace(/(\d{4})(\d{2})(\d{2})/, '$1-$2-$3'))
  const periodEnd   = new Date()
  const fmtDate = (d: Date) =>
    `${d.getFullYear()}.${String(d.getMonth() + 1).padStart(2, '0')}.${String(d.getDate()).padStart(2, '0')}`

  const lines = topDeals.map((d) =>
    `${d.rank}위. ${d.name} — ${d.priceEok.toLocaleString()}억원 (${d.areaSqm}㎡)`
  )
  const cardnewsText = [
    '📊 이번 주 창원·김해 신고가 TOP 5',
    '',
    ...lines,
    '',
    `📅 집계 기간: ${fmtDate(periodStart)} ~ ${fmtDate(periodEnd)}`,
    '🔗 단지온도에서 상세 보기: https://danjiondo.kr',
  ].join('\n').slice(0, 500)

  const periodLabel = `${fmtDate(periodStart)} ~ ${fmtDate(periodEnd)}`

  return (
    <CardnewsDashboardClient
      topDeals={topDeals}
      cardnewsText={cardnewsText}
      periodLabel={periodLabel}
    />
  )
}
```

**Verify:** `npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules"` 출력 없음.

---

### Task 4: /admin/cardnews/scheduler/page.tsx → redirect 처리

**Type:** redirect
**Files:**
- `src/app/admin/cardnews/scheduler/page.tsx` (전체 교체)

**Implementation:**

기존 파일 내용을 아래 최소 redirect 페이지로 전체 교체한다. (파일 삭제 대신 redirect를 선택하는 이유: Next.js App Router에서 디렉터리를 유지하면 중간에 layout이 있어도 안전하게 처리됨. `next.config.js` redirects 대신 page-level redirect를 사용해 auth 없이도 즉시 적용됨.)

```typescript
import { redirect } from 'next/navigation'

export default function CardNewsSchedulerRedirectPage() {
  redirect('/admin/cardnews')
}
```

메타데이터, auth 검사, SchedulerPanel import 전부 제거한다.

**Verify:** 브라우저 또는 curl로 `/admin/cardnews/scheduler` 접근 시 `/admin/cardnews`로 307 redirect 확인.

---

## Verification

```bash
# 1. 타입 체크 통과
npx tsc --noEmit

# 2. 빌드 통과
npm run build

# 3. 사이드바에 '스케줄러' 항목 없음
grep -n "스케줄러" src/components/admin/AdminSidebar.tsx

# 4. 사이드바에 '카드뉴스 관리' 항목 있음
grep -n "카드뉴스 관리" src/components/admin/AdminSidebar.tsx

# 5. scheduler page가 redirect만 포함
grep -n "redirect" src/app/admin/cardnews/scheduler/page.tsx
```

## Success Criteria

- `/admin/cardnews` 접근 시 자동화 상태(SchedulerPanel) + 빠른 액션(다운로드) + 신고가 TOP5(+복사) + 실행 로그 링크 4섹션이 렌더됨
- `/admin/cardnews/scheduler` 접근 시 `/admin/cardnews`로 즉시 redirect
- AdminSidebar에 '스케줄러' 항목 없음, '카드뉴스 관리' 레이블 표시
- `npm run build` 오류 없음
- 기존 SchedulerPanel·CardnewsDownloadButton·AdminCardnewsCopyButton 내부 로직 변경 없음

## Output

완료 후 `.planning/phases/32-cardnews-admin-dashboard/32-01-SUMMARY.md` 생성.
요약: 변경 파일 목록, 주요 결정사항, 검증 결과.
