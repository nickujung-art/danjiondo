---
phase: 41-busan-recollect
plan: "03"
type: execute
wave: 2
depends_on: ["41-01", "41-02"]
files_modified:
  - scripts/seed-complexes.ts
autonomous: true
requirements:
  - BUSAN-04
must_haves:
  truths:
    - "부산 16개 구의 complexes Golden Record 가 K-apt 단지목록 API 로 재시딩되고 16개 구 전부 1행 이상을 갖는다"
    - "재시딩이 기존 경남 22개 지역의 built_year 를 null 로 리셋하지 않는다 (Phase 34 에서 실제로 발생한 부작용)"
    - "부산 단지의 si/gu/household_count/built_year 가 K-apt 상세정보로 보강되고 잔여 null 이 API 데이터 갭으로 특정된다"
  artifacts:
    - path: "scripts/seed-complexes.ts"
      provides: "--sgg= 지역 한정 시딩 (전체 활성 지역 재시딩의 부작용 회피)"
      contains: "parseSggCodes"
  key_links:
    - from: "scripts/seed-complexes.ts"
      to: "src/lib/data/backfill-args.ts (parseSggCodes)"
      via: "--sgg= 인자 검증·분해를 41-02 모듈과 공유"
      pattern: "parseSggCodes"
    - from: "scripts/seed-complexes.ts"
      to: "complexes 테이블"
      via: "fetchComplexList(sggCode) → seedComplex upsert"
      pattern: "seedComplex"
---

<objective>
🔴 **D-03 의 첫 단계.** `src/lib/data/realprice.ts` 의 `ingestMonth`/`ingestMonthVilla` 는 `createComplexIdLookup()` 으로 `complex_id` 를 **조회만** 한다 — 단지를 만들지 않는다. 부산 단지 0개 상태로 백필하면 28만건이 전부 `complex_id=null` 이 되고, 그것이 2026-05-26 사고(연결률 19.1%, 마린애시앙 "4개월째 거래 없음")의 정확한 재현이다.

이 plan 은 백필 이전에 Golden Record 를 되돌린다. `scripts/seed-complexes.ts`·`scripts/kapt-enrich.ts` 는 이미 `regions.is_active` 를 동적으로 읽으므로(41-01 의 스위치만으로 부산이 자동 포함) **로직 변경은 필요 없다.** 대신 한 가지 실측된 부작용을 막아야 한다:

⚠️ **Phase 34 실측 부작용** — `seedComplex()` 의 UPDATE 경로가 `built_year` 를 무조건 덮어쓴다(`raw.kaptUseApproveYmd` 가 `KaptComplex` 스키마에 없어 항상 null). 지금 `seed-complexes.ts` 를 그대로 돌리면 활성 38개 지역 전부가 대상이 되어 **기존 경남 22개 지역의 이미 보강된 `built_year` 가 null 로 리셋된다.** Phase 34 는 이걸 겪고 `kapt-enrich.ts` 를 10회 반복 실행해 되돌렸다. 이번에는 애초에 부산만 건드린다.

Purpose: 백필 진입 전제인 Golden Record 확보 + 기존 운영권역 데이터 무회귀.
Output: `--sgg` 를 받는 `seed-complexes.ts`, 부산 `complexes` 재시딩·보강 실측값.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-03-PLAN.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-03-SUMMARY.md
@C:\Users\jung\coding\bds\scripts\seed-complexes.ts
@C:\Users\jung\coding\bds\scripts\kapt-enrich.ts
@C:\Users\jung\coding\bds\.github\workflows\kapt-enrich-once.yml

<interfaces>
<!-- scripts/seed-complexes.ts 현재 구조 -->
loadEnvConfig(process.cwd())                         // @next/env — .env.production.local 이 .env.local 보다 먼저 로드된다
async function getSggCodes(): Promise<string[]>      // regions.is_active=true 전체 (현재 38개)
async function runWithApi(sggCodes: string[])        // fetchComplexList → seedComplex, 실패는 scripts/seed-failures.csv
async function runBootstrap()                        // KAPT_API_KEY 없으면 자동 전환 — 부산 시딩에서는 절대 타면 안 되는 경로
const useBootstrap = process.argv.includes('--bootstrap') || !process.env.KAPT_API_KEY

<!-- Phase 34 실측 (34-03-SUMMARY / 34-05-SUMMARY) -->
부산 K-apt 시딩량        1,463건        ← 이번 기대값의 기준
si 보강 후 잔여 null      11건 (0.75%)   ← KAPT API 데이터 갭, 재실행으로 해결 불가 (2회 연속 동일 확인)
지오코딩 커버리지        1,462/1,463 = 99.9%
2026-08-10 삭제분         1,594건        ← 시딩량보다 131 많다 (시딩 이후 한 달간 다른 경로로 증가)
seed-complexes.ts 실행    부산 16개 완료 직후 10분 Bash 타임아웃 (경남 22개는 별도 스크립트로 완주)
kapt-enrich.ts 실행       PostgREST 1,000행 캡 → 10회 반복 실행으로 1,310 → 11 수렴

<!-- src/lib/data/backfill-args.ts (41-02 산출물) -->
export function parseSggCodes(raw: string | undefined): string[] | undefined   // 빈 문자열이면 throw
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seed-complexes.ts 에 --sgg= 지역 한정 추가</name>
  <files>scripts/seed-complexes.ts</files>
  <read_first>
    - scripts/seed-complexes.ts (`getSggCodes()`·`runWithApi()`·`useBootstrap` 판정·상단 사용법 주석)
    - src/lib/data/backfill-args.ts (41-02 의 `parseSggCodes` 시그니처)
    - scripts/backfill-realprice.ts (`--sgg=` 인자 형식의 기존 선례 — 같은 표기를 쓴다)
    - .planning/phases/34-db-2/34-03-SUMMARY.md ("Deviations from Plan" 절 — built_year 리셋 부작용과 env 우선순위 함정의 원문)
  </read_first>
  <action>
`scripts/seed-complexes.ts` 를 수정한다. 목표 상태: `npx tsx scripts/seed-complexes.ts --sgg=26110,26140` 이 **지정한 지역만** 시딩하고, `--sgg` 없이 돌리면 지금과 똑같이 `regions.is_active=true` 전체를 대상으로 한다.

- `../src/lib/data/backfill-args` 에서 `parseSggCodes` 를 import 해 `--sgg=` 값을 검증·분해한다. 자체 파싱을 새로 쓰지 않는다 — `--sgg=` 빈 값이 전체 지역으로 조용히 확장되는 것을 41-02 가 이미 막아뒀다
- `getSggCodes()` 가 `parseSggCodes(sggArg)` 결과가 있으면 그것을 반환하고, 없으면 기존 `regions` 조회 경로를 그대로 쓰도록 바꾼다 (`backfill-realprice.ts` 의 `getSggCodes()` 와 동일한 형태)
- 상단 사용법 주석에 `--sgg=` 예시를 추가하고, **왜 이 플래그가 생겼는지**를 남긴다: `seedComplex()` 의 UPDATE 경로가 `built_year` 를 무조건 null 로 덮어쓰므로(`KaptComplex` 에 `kaptUseApproveYmd` 가 없다) 전체 지역 재시딩은 이미 보강된 `built_year` 를 리셋한다. Phase 34 가 `kapt-enrich.ts` 10회 반복으로 복구해야 했다
- ⚠️ `seedComplex()` 자체는 **수정하지 않는다.** `built_year` 무조건 덮어쓰기를 고치는 것은 이 Phase 범위 밖이다(운영권역 2,035건에 영향이 가는 변경). `--sgg` 로 사정거리를 좁히는 것이 이 plan 의 대응이다
- `useBootstrap` 판정은 그대로 둔다. 다만 상단 주석에 🔴 "`KAPT_API_KEY` 가 없으면 `runBootstrap()` 으로 자동 전환되어 **창원·김해 샘플 10건을 upsert** 한다 — 부산 시딩에서 이 경로를 타면 조용한 오작동이다" 를 경고로 남긴다
  </action>
  <acceptance_criteria>
    - `grep -c "parseSggCodes" scripts/seed-complexes.ts` >= 1
    - `grep -c "\-\-sgg" scripts/seed-complexes.ts` >= 1
    - `npx tsx scripts/seed-complexes.ts --sgg=` 가 exit 1 (빈 값 차단이 상속됨)
    - `git diff src/lib/data/complex-matching.ts | wc -l` == 0 (`seedComplex` 무변경)
    - `npm run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "parseSggCodes" scripts/seed-complexes.ts && npm run lint</automated>
  </verify>
  <done>지역 한정 시딩이 가능하고, 경남 22개 지역을 건드리지 않는 경로가 생겼다.</done>
</task>

<task type="auto">
  <name>Task 2: 부산 16개 구 K-apt 재시딩 + kapt-enrich 보강 실행</name>
  <files>없음 (스크립트·워크플로 실행. 실측값은 SUMMARY.md 에 기록)</files>
  <read_first>
    - scripts/seed-complexes.ts (Task 1 수정 후 상태)
    - scripts/kapt-enrich.ts (대상 조건 `kapt_code is not null and si is null`, `--debug` 플래그, KAPT 일 100,000회 한도)
    - .github/workflows/kapt-enrich-once.yml (`timeout-minutes: 60` — 로컬 10분 Bash 한계를 넘기는 경로)
    - scripts/busan-status.ts (41-01 산출물 — 이 태스크의 모든 측정을 여기로 한다)
    - .planning/phases/34-db-2/34-03-SUMMARY.md (env 우선순위 함정 + 반복 실행 수렴 이력)
  </read_first>
  <action>
**순서를 지킬 것 — 시딩 완주 → 그 다음 enrich.** 뒤집거나 섞으면 `seedComplex()` 의 UPDATE 가 방금 보강한 `built_year` 를 다시 null 로 만든다.

**(1) env 우선순위 함정 회피** — `seed-complexes.ts` 는 `@next/env` 의 `loadEnvConfig()` 를 쓰므로 `.env.production.local` 이 `.env.local` 보다 먼저 적용된다. 실측: 두 파일의 `KAPT_API_KEY` 값 길이가 다르다(`.env.local` 64자 / `.env.production.local` 66자). Phase 34 는 이 우선순위 때문에 빈 `SUPABASE_SERVICE_ROLE_KEY` 를 집어 실패했다.
→ 실행 전에 `KAPT_API_KEY`·`NEXT_PUBLIC_SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY` 를 셸에서 **명시적으로 export** 한 뒤 스크립트를 돌린다(`loadEnvConfig` 는 이미 설정된 값을 덮어쓰지 않는다). 어느 파일의 값을 썼는지 SUMMARY.md 에 기록한다.

**(2) 시딩 프리플라이트** — 전체 루프 전에 **한 지역만** 돌려 K-apt API 가 실제로 응답하는지 확인한다: `npx tsx scripts/seed-complexes.ts --sgg=26110`. 출력의 `→ N건 수신` 이 0 이면 즉시 중단한다. 🔴 0건인데 계속 돌리면 16개 구를 다 돌고도 `✅ 총 0건 upsert 완료` 로 끝난다. 그리고 출력에 `🔧 Bootstrap 모드` 가 보이면 `KAPT_API_KEY` 가 안 잡힌 것이므로 **즉시 중단**한다 — 그 경로는 창원·김해 샘플 10건을 쓴다.

**(3) 부산 16개 구 시딩** — 나머지 15개 코드를 `--sgg=` 로 넘긴다: `26140,26170,26200,26230,26260,26290,26320,26350,26380,26410,26440,26470,26500,26530,26710`. Phase 34 실측으로 부산 16개는 10분 안에 끝났으므로 한 번에 시도하고, 타임아웃되면 **아직 `complexes` 행이 없는 구만** 골라 재실행한다(`busan-status.ts` 의 구별 breakdown 으로 판별). `scripts/seed-failures.csv` 가 생성되면 내용을 SUMMARY.md 에 옮긴다.

**(4) K-apt 상세정보 보강** — `gh workflow run kapt-enrich-once.yml` 로 dispatch 한다(로컬 실행은 10분 Bash 한계 + PostgREST 1,000행 캡에 걸린다. 워크플로는 60분이다). `gh run watch` 로 완료를 기다린 뒤 `busan-status.ts` 로 부산 `si is null` 수를 측정하고, **수가 줄어드는 동안 재dispatch 한다.** Phase 34 실측 수렴 경로는 1,310 → 1,175 → 1,091 → 874 → 563 → 301 → 11 → 11(안정)이다. 연속 2회 동일하면 수렴으로 판정하고 멈춘다.
- ⚠️ `kapt-enrich.ts` 에는 지역 필터가 없어 기존 경남의 잔여 null 행도 같이 채운다 — **null 만 채우므로 무해하다**(리셋을 일으키는 것은 `seedComplex` 쪽이다)
- 잔여 null 은 KAPT API 자체 데이터 갭이다. Phase 34 가 특정한 11건 목록(34-03-SUMMARY "Issues Encountered")과 대조해 **같은 단지들인지** 확인하고 결과를 기록한다

**(5) 실측 보고** — `npx tsx scripts/busan-status.ts --json` 출력을 SUMMARY.md 에 붙이고 다음 4숫자를 대조한다: 이번 실측값 / **1,463**(Phase 34 K-apt 시딩) / **1,594**(2026-08-10 삭제분) / **1,500**(ROADMAP·41-CONTEXT 요구치). 🔴 실측값이 1,500 미만이면 그것을 실패로 처리하지 말고 **차이의 방향과 원인을 설명**한다 — 삭제분 1,594 는 시딩량 1,463 위에 한 달간 다른 경로(일배치 매칭·match queue 승인 등)로 131건이 얹힌 값이며, K-apt 시딩만으로는 1,463 근방이 정상이다. 백필 진입 차단선은 **1,400** 이다(41-01 Task 3 근거).
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/busan-status.ts --json` 에서 부산 `complexes` >= **1400**
    - 16개 구 breakdown 에서 `complexes` == 0 인 구가 **0개**
    - 부산 `si is null` 행 수 <= **20** 이고, 연속 2회 측정에서 동일(수렴 확인)
    - 경남 22개 지역 무회귀: `complexes where sgg_code like '48%' and built_year is null` 카운트가 시딩 전 대비 **증가하지 않았다** (시딩 전·후 두 수치를 SUMMARY.md 에 나란히 기록)
    - `scripts/seed-failures.csv` 가 존재하면 그 행 수와 사유가 SUMMARY.md 에 기록됨
    - SUMMARY.md 에 실측값 / 1,463 / 1,594 / 1,500 / 1,400 다섯 숫자와 그 관계 설명이 있다
    - 실행 로그에 `🔧 Bootstrap 모드` 가 나타난 적이 없다
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --json</automated>
  </verify>
  <done>부산 16개 구 전부에 Golden Record 가 존재하고 K-apt 보강이 수렴했으며, 경남 built_year 무회귀가 수치로 확인된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| K-apt 공공 API → `complexes`(Golden Record) | 외부 응답이 그대로 Golden Record 가 된다. 빈 응답이 "0건 성공"으로 보인다 |
| 로컬 실행자 → 프로덕션 Supabase (service_role) | upsert 로 2,000+ 행을 쓴다. 사정거리를 좁히는 것이 유일한 방어다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-08 | Tampering (기존 데이터 회귀) | `seedComplex()` 의 `built_year` 무조건 덮어쓰기 | mitigate | `--sgg=` 로 부산 16개만 대상. 경남 `built_year is null` 카운트를 시딩 전·후로 대조해 증가 0 을 단언 |
| T-41-09 | Repudiation (조용한 성공) | `fetchComplexList` 빈 응답 / `runBootstrap()` 자동 전환 | mitigate | 1지역 프리플라이트로 `→ N건 수신` 확인 후 진행. `🔧 Bootstrap 모드` 출현 시 즉시 중단을 acceptance 로 못박음 |
| T-41-10 | Denial of Service (도구 타임아웃) | 10분 Bash 한계 / PostgREST 1,000행 캡 | mitigate | 시딩은 부산만(Phase 34 실측 10분 내). enrich 는 `kapt-enrich-once.yml`(60분) dispatch 반복, 연속 2회 동일로 수렴 판정 |
| T-41-11 | Information Disclosure | `scripts/seed-failures.csv` 커밋 | accept | 실패 사유·단지명만 담긴다. 비밀값 없음. Phase 34 에도 동일 산출물이 있었다 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
npx tsx scripts/busan-status.ts
npx tsx scripts/busan-status.ts --json
npm run lint
```
</verification>

<success_criteria>
1. `seed-complexes.ts` 가 `--sgg=` 를 받고, 없을 때 기존 전체 지역 동작을 유지한다
2. 부산 `complexes` >= 1,400 이고 16개 구 전부 1행 이상 (BUSAN-04 전반부)
3. 부산 `si is null` <= 20 으로 수렴, 잔여분이 Phase 34 의 KAPT 데이터 갭과 대조됨
4. 경남 22개 지역 `built_year is null` 증가 0 (무회귀)
5. 실측 시딩량이 1,463·1,594·1,500·1,400 과 대조 보고됨
6. `npm run lint` exit 0 / 테스트 실패 이름 집합 불변
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-03-SUMMARY.md` 를 생성한다. `busan-status.ts --json` 원본, 구별 breakdown, 경남 무회귀 전·후 수치, enrich 수렴 경로, 어느 env 파일의 KAPT 키를 썼는지를 기록할 것.
</output>
</content>
