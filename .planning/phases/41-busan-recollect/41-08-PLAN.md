---
phase: 41-busan-recollect
plan: "08"
type: execute
wave: 7
depends_on: ["41-07"]
files_modified: []
autonomous: true
requirements:
  - BUSAN-06
must_haves:
  truths:
    - "가격 파생값·갭 통계·랭킹·가격예측 배치가 부산까지 돌아 부산 행이 생성됨이 실측된다"
    - "복원 규모가 2026-08-10 삭제분과 대조 보고되고, D-04 로 기간이 18개월 넓어진 초과분이 정상으로 설명된다"
    - "복원되지 않는 삭제분(facility_kapt·cafe_articles)이 범위 밖으로 명시 기록되어 미복원이 조용히 남지 않는다"
  artifacts: []
  key_links:
    - from: "src/lib/data/rankings.ts (computeRankings)"
      to: "regions.is_active (getActiveSggCodes)"
      via: "활성 지역 목록을 동적으로 읽어 .in('sgg_code', activeSggCodes) 로 집계 — 41-01 스위치의 결과로 부산 포함"
      pattern: "getActiveSggCodes"
    - from: "refresh_complex_price_stats()"
      to: "complexes 의 avg_sale_per_pyeong / price_change_30d / tx_count_30d / is_new_record_30d"
      via: "psql 직결 호출 (PostgREST 8초 상한 회피)"
      pattern: "refresh_complex_price_stats"
---

<objective>
부산 거래가 적재됐으므로 그 위에 얹히는 파생값을 되살린다. 🔴 **이 단계에 코드 변경은 없다** — `getActiveSggCodes()` 호출부 14곳이 `regions.is_active` 를 동적으로 읽으므로 41-01 의 스위치가 이미 부산을 포함시켰다(ROADMAP Notes 의 ✅ 항목). 이 plan 이 하는 일은 **배치를 실제로 돌려 부산 행이 생기는지 실측하고, 복원 규모를 삭제분과 대조 보고**하는 것이다.

🔴 **ROADMAP·41-CONTEXT 의 "파생값 4테이블" 표현을 정정한다.** `complex_price_stats` 라는 테이블은 존재하지 않는다 — 가격 파생값은 `complexes` 의 4개 컬럼(`avg_sale_per_pyeong`·`price_change_30d`·`tx_count_30d`·`is_new_record_30d`)이며 `refresh_complex_price_stats()` 가 갱신한다(`20260806000000_refresh_complex_price_stats_setbased.sql`). 따라서 측정 대상은 **3개 테이블 + `complexes` 4컬럼**이다.

그리고 이 plan 은 **다운스트림 영향 하나를 반드시 기록해야 한다**: `complex_rankings` 의 `price_change` 는 전국(활성 지역 전체) 집계이고, 2026-08-03 실측에서 22행 중 **12행이 부산**이었으며 **TOP3 가 전부 부산**이었다. 부산을 다시 켜면 그 상태로 돌아간다 — 창부레터가 `si in ('창원시','김해시')` 필터 없이 `hotArea` 를 읽으면 "동래구"가 나간다(Phase 40-04 인계 ①).

Purpose: 파생값 복원 실측 + 규모·용량 대조 보고 + 다운스트림 영향 기록.
Output: 부산 파생값 행 수 실측, 삭제분 대조표, 용량 보고.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-07-SUMMARY.md
@C:\Users\jung\coding\bds\supabase\migrations\20260810060000_drop_busan_scope.sql.applied-manually
@C:\Users\jung\coding\bds\src\lib\data\rankings.ts
@C:\Users\jung\coding\bds\.github\workflows\refresh-price-stats.yml
@C:\Users\jung\coding\bds\.github\workflows\refresh-gap-stats.yml
@C:\Users\jung\coding\bds\.github\workflows\rankings-cron.yml
@C:\Users\jung\coding\bds\.github\workflows\compute-predictions.yml

<interfaces>
<!-- 파생값 배치 4종 — 전부 workflow_dispatch 가능. 지역 필터 코드 변경 불필요 -->
refresh-price-stats.yml   psql 직결 → refresh_complex_price_stats()   실측 41~46초   → complexes 4컬럼
refresh-gap-stats.yml     psql 직결 → refresh_complex_gap_stats()     실측 cold 11.45초 / warm 0.35~5.1초 → complex_gap_stats
rankings-cron.yml         curl → /api/cron/rankings (CRON_SECRET)     timeout-minutes: 3 → complex_rankings
compute-predictions.yml   npx tsx scripts/compute-predictions.ts      timeout-minutes: 30 → complex_price_predictions

<!-- 지역 스코프 -->
src/lib/data/rankings.ts    computeRankings → getActiveSggCodes(supabase) → .in('sgg_code', activeSggCodes)  ← 부산 자동 포함
refresh_complex_price_stats / refresh_complex_gap_stats   지역 필터 없음 (전 단지 집합 기반)
scripts/compute-predictions.ts   지역 필터 없음 (전 단지 × 4 area_bucket)

<!-- 2026-08-10 삭제분 (ADR-062 기록) — 이번 복원과 대조할 기준 -->
transactions               282,444    (기간 201607~202608)
complex_price_predictions   11,611
cafe_articles                1,654    ← 이 Phase 범위 밖 (카페 수집은 창원 카페 대상)
complexes                    1,594
facility_kapt                1,463    ← 이 Phase 범위 밖 (34-07 enrichment 계열)
complex_rankings               330
complex_gap_stats              299

<!-- Phase 40-04 인계 ① (다운스트림 영향) -->
2026-08-03 실측: complex_rankings rank_type='price_change' 전체 22행 = 부산 12 / 창원 6 / 김해 2 / 양산 2
rank 1 = 동래래미안아이파크아파트 (metadata.region = '동래구')
창부레터는 si in ('창원시','김해시') 로 필터한 뒤 최상위 행의 metadata.region 을 hotArea 로 써야 한다
getRankingsByType() (src/lib/data/rankings.ts:27-63) 은 metadata.region 을 노출하지 않고 si/gu 필터도 하지 않는다
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 파생값 배치 4종 재실행 + 부산 행 생성 실측</name>
  <files>없음 (워크플로 dispatch. 실측값은 SUMMARY.md 에 기록)</files>
  <read_first>
    - .github/workflows/refresh-price-stats.yml (psql 직결 이유 — PostgREST 8초 상한. 이 경로로만 완주한다)
    - .github/workflows/refresh-gap-stats.yml (cold 11.45초. 크론 시각이 정확히 cold 라 실패했던 이력)
    - .github/workflows/rankings-cron.yml (CRON_SECRET 헤더 + NEXT_PUBLIC_SITE_URL)
    - .github/workflows/compute-predictions.yml (timeout-minutes: 30)
    - src/lib/data/rankings.ts (`computeRankings` 의 5종 rank_type 과 `getActiveSggCodes` 사용부)
    - scripts/busan-status.ts (41-01 산출물 — 파생값 부산 행 측정)
  </read_first>
  <action>
**(1) 착수 전 기준값** — `npx tsx scripts/busan-status.ts --json` 을 "파생값 배치 전"으로 기록한다. 이 시점의 부산 파생값 행 수는 대부분 0 이어야 한다(2026-08-10 에 지웠고 그 후 배치가 부산을 대상에 넣지 않았다).

**(2) 순서대로 dispatch 하고 각각 완료를 확인한다.** `gh workflow run <파일명>` + `gh run watch` 를 쓴다. 순서 근거: 가격 파생값이 `complexes` 컬럼을 채우고 랭킹·예측이 그 위에서 돈다.
- `refresh-price-stats.yml` (약 1분)
- `refresh-gap-stats.yml` (약 1분)
- `rankings-cron.yml` (약 3분, `timeout-minutes: 3` 이라 여유가 없다 — 초과하면 부산 유입으로 집계량이 늘어난 것이므로 그 사실을 기록한다)
- `compute-predictions.yml` (최대 30분)
각 run 의 id·소요시간·결론을 SUMMARY.md 에 적는다. 🔴 실패한 것을 "다음에 크론이 돌면 될 것"으로 넘기지 않는다 — 실패 사유를 로그에서 확인해 기록한다.

**(3) 부산 행 생성 실측** — `npx tsx scripts/busan-status.ts --json` 으로 4항목을 측정한다:
- `complexes` 부산 행 중 `avg_sale_per_pyeong is not null` 카운트 (= 가격 파생값. 🔴 테이블이 아니라 컬럼이다)
- `complex_gap_stats` 부산 행 수
- `complex_rankings` 부산 행 수
- `complex_price_predictions` 부산 행 수
각 값이 **0 보다 커야** 한다. 0 인 항목이 있으면 그 배치의 run 로그와 `data_sources` 의 해당 행(`last_status`·`error_message`)을 확인해 사유를 기록한다.

**(4) 🔴 `price_change` 랭킹의 지역 분포를 실측한다** — `complex_rankings` 에서 `rank_type='price_change'` 행을 `rank` 순으로 전부 뽑아 각 행의 `metadata.region` 과 단지의 `si`/`sgg_code` 를 함께 적는다. 2026-08-03 실측(22행 = 부산 12 / 창원 6 / 김해 2 / 양산 2, TOP3 전부 부산)과 대조한다.
- 부산이 상위를 차지하는 상태로 돌아갔다면 그것이 **정상 동작**이다(ADR-005 의 의도 — `실거래이야기`가 같은 배치를 재사용한다). 창원·김해로 배치를 좁히지 **않는다**
- 🔴 대신 **창부레터 인계 사항으로 명시 기록**한다: 창부레터가 `si in ('창원시','김해시')` 필터 없이 `hotArea` 를 읽으면 홈 히어로에 부산 지역명이 나간다. 필터 후 최상위 행의 `metadata.region` 을 써야 한다(Phase 40-04 인계 ①). 이 저장소에서 고칠 것은 없고 **realtrade-story/창부레터 쪽 확인이 필요한 사항**임을 SUMMARY.md 에 남긴다
  </action>
  <acceptance_criteria>
    - 4개 워크플로의 run id·상태·소요시간이 SUMMARY.md 에 기록되고, 실패한 run 이 있으면 사유가 로그 근거와 함께 기록됨
    - `complexes` 부산 행 중 `avg_sale_per_pyeong is not null` 카운트 > 0
    - `complex_gap_stats` 부산 행 수 > 0
    - `complex_rankings` 부산 행 수 > 0
    - `complex_price_predictions` 부산 행 수 > 0
    - `rank_type='price_change'` 전체 행의 지역 분포가 실측 열거되고 2026-08-03 실측(22행 / 부산 12 / TOP3 부산)과 대조됨
    - SUMMARY.md 에 창부레터 `hotArea` 필터 인계 사항이 명시됨
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --json</automated>
  </verify>
  <done>파생값 배치 4종이 실행되고 부산 행 생성이 4항목 모두 0 초과로 실측된다.</done>
</task>

<task type="auto">
  <name>Task 2: 삭제분 대조 보고 + 용량 보고 + 미복원 항목 명시</name>
  <files>없음 (보고 전용. SUMMARY.md 에 기록)</files>
  <read_first>
    - supabase/migrations/20260810060000_drop_busan_scope.sql.applied-manually (삭제분 7행 표 — 대조 기준)
    - .planning/phases/41-busan-recollect/41-07-SUMMARY.md (4열 추이 표 + 최종 부산 거래 수·연결률·용량)
    - .planning/phases/41-busan-recollect/41-CONTEXT.md (D-04 — 기간이 18개월 넓어진 근거, Success Criteria 6·8)
    - scripts/busan-status.ts
  </read_first>
  <action>
**(1) 삭제분 대조표를 만든다.** 행은 삭제분 7항목, 열은 `2026-08-10 삭제분` / `복원 후 실측` / `차이` / `해석`.

| 항목 | 삭제분 | 기대 |
|---|---|---|
| `transactions` | 282,444 | 🔴 **초과가 정상.** 삭제분은 201607~202608 이고 복원은 D-04 로 **201501~** 이라 **576 지역-월(약 18개월 × 16구 × 2종)이 더 넓다.** 초과분의 크기가 2015-01~2016-06 구간의 거래량과 정합하는지 확인해 적는다 — 즉 `deal_date < 2016-07` 인 부산 거래 수를 따로 세서 "초과분 ≈ 그 수" 를 보인다 |
| `complexes` | 1,594 | 미달 가능. 41-03 이 기록한 1,463(Phase 34 K-apt 시딩량)과 대조. 차이 131 은 시딩 이후 한 달간 다른 경로로 늘어난 분으로 설명된다 |
| `complex_price_predictions` | 11,611 | 단지 수 × area_bucket 수에 비례. 단지가 1,594 → 약 1,46x 로 줄었으면 예측 행도 비례해 줄어드는 것이 정합이다 |
| `complex_rankings` | 330 | 랭킹은 top-N 이라 단지 수와 선형이 아니다. 값만 기록 |
| `complex_gap_stats` | 299 | 전세 거래가 있는 단지만 대상 |
| `facility_kapt` | 1,463 | 🔴 **복원하지 않는다** — K-apt 시설 enrichment(34-07 계열)는 이 Phase `<scope_fence>` 밖이다. 미복원임을 명시 기록 |
| `cafe_articles` | 1,654 | 🔴 **복원하지 않는다** — 카페 수집은 창원 카페(`NAVER_TARGET_CAFE=xxdkd`) 대상이고 부산 아티클을 되살릴 경로가 없다. 미복원임을 명시 기록 |

🔴 **미복원 2항목을 표에서 빼지 말 것.** 빼면 "전부 복원됨"으로 읽히고, 그것이 이 저장소가 반복해 겪은 실패 모양이다. 미복원 사유와 어느 Phase 소관인지를 적는다.

**(2) 용량 보고 (Success Criteria 8)** — 41-07 의 4열 추이 표에 "파생값 후" 열을 더해 5열로 완성한다. 그리고:
- 최종 `db_size_bytes` 를 MB 로, Pro 한도 **8,192MB** 대비 % 로 적는다
- 41-CONTEXT 의 사용자 산정(**650MB = 8%**)과 대조한다. 크게 다르면(예: 900MB 초과) 어느 테이블이 예상보다 큰지 `transactions` 행 수 × 545 bytes/행(41-CONTEXT 실측 원단위)로 역산해 설명한다
- 🔴 `VACUUM FULL` 은 **범위 밖**이다(데이터가 늘어나는 방향이라 불필요). 용량이 예상보다 크더라도 이 Phase 에서 실행하지 않는다

**(3) 연결률 최종 보고 (Success Criteria 5)** — 부산 전체 연결률 실측값을 **운영권역 94.6% / 경남 기타 64.7% / 임계 65%** 세 기준과 나란히 적는다. 부산이 세 값 사이 어디에 위치하는지와, 65% 를 넘겼다면 **90% 는 별칭 보정 후속 작업의 목표이며 이 Phase 범위 밖**임을 명시한다(D-07).

**(4) 회귀 확인** — `npm run lint` exit 0, `npm run test` 실행 후 **실패 테스트 이름 집합**이 Phase 시작 시점과 동일한지 확인한다(개수가 아니라 이름 집합이다). 부산 데이터 유입으로 라이브 DB 를 읽는 테스트(`src/__tests__/admin-status.test.ts` 등)의 결과가 달라질 수 있으므로 달라진 항목이 있으면 그 사실을 기록한다.
  </action>
  <acceptance_criteria>
    - SUMMARY.md 에 7행 4열 삭제분 대조표가 있고, `facility_kapt`·`cafe_articles` 2항목이 "복원하지 않음 + 사유 + 소관"으로 표에 남아 있다
    - `transactions` 초과분이 `deal_date < 2016-07` 인 부산 거래 수와 대조되어 "초과가 정상"임이 수치로 설명됨
    - 5열 용량 추이 표(착수 전 / A 후 / B 후 / C 후 / 파생값 후)와 최종 MB·8,192MB 대비 % 가 기록됨
    - 최종 용량이 41-CONTEXT 의 650MB 산정과 대조되고, 차이가 크면 545 bytes/행 역산으로 설명됨
    - 부산 연결률이 94.6% / 64.7% / 65% 세 기준과 함께 기록되고, 90% 가 범위 밖임이 명시됨
    - `npm run lint` exit 0
    - `npm run test` 의 실패 테스트 **이름 집합**이 Phase 시작 시점과 동일 (다르면 차이가 열거됨)
    - `git status --short` 에 소스 변경이 없다 (이 plan 은 코드를 바꾸지 않는다 — `.planning/` 문서 제외)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run test 2>&1 | tail -30</automated>
  </verify>
  <done>복원 규모·용량·연결률이 삭제분과 대조 보고되고 미복원 항목이 명시된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → 프로덕션 DB (psql 직결 / CRON_SECRET) | `BACKUP_AGENT_PASSWORD`·`CRON_SECRET` 시크릿으로 집계 함수를 트리거한다 |
| `complex_rankings` (전국 집계) → 창부레터·실거래이야기 | 같은 테이블을 여러 사이트가 읽는다. 지역 필터 책임이 읽는 쪽에 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-28 | Repudiation (조용한 실패) | 배치가 실패했는데 "크론이 다음에 돌면 됨"으로 넘김 | mitigate | 4개 run 의 id·상태·소요시간 기록 + 부산 행 수 4항목 모두 > 0 단언 + `data_sources.error_message` 확인 |
| T-41-29 | Information Disclosure (오표시) | 부산 `price_change` 1위가 창부레터 홈 히어로 `hotArea` 로 나감 | mitigate | 지역 분포를 실측 열거하고 창부레터 필터 필요를 인계 기록. 🔴 배치를 창원·김해로 좁혀 해결하지 않는다(ADR-005 의도 위반) |
| T-41-30 | Repudiation (미복원 은폐) | `facility_kapt`·`cafe_articles` 를 대조표에서 생략 | mitigate | 두 항목을 표에 남기고 "복원하지 않음 + 사유 + 소관"을 acceptance 로 못박음 |
| T-41-31 | Denial of Service (타임아웃) | `rankings-cron.yml` `timeout-minutes: 3` 이 부산 유입으로 초과 | accept | 초과 사실을 기록하고 41-09 로 인계. 상한 조정은 실측 근거가 쌓인 뒤 판단 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
gh run list --limit=10
npx tsx scripts/busan-status.ts
npm run lint
npm run test
```
</verification>

<success_criteria>
1. 파생값 배치 4종이 실행되고 부산 행이 4항목 모두 > 0 (BUSAN-06) — 단, `complex_price_stats` 는 테이블이 아니라 `complexes` 4컬럼임이 정정 기록됨
2. 삭제분 7항목 대조표 완성. `transactions` 초과가 D-04 기간 확장으로 수치 설명됨 (Success Criteria 6)
3. `facility_kapt`·`cafe_articles` 미복원이 사유·소관과 함께 명시됨
4. DB 용량 최종 실측 + 8,192MB 대비 % + 650MB 산정 대조 (Success Criteria 8)
5. 부산 연결률이 94.6%/64.7%/65% 기준과 함께 보고됨 (Success Criteria 5)
6. `price_change` 랭킹 지역 분포 실측 + 창부레터 `hotArea` 필터 인계 기록
7. `npm run lint` exit 0 / 테스트 실패 이름 집합 불변
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-08-SUMMARY.md` 를 생성한다. 4개 run 기록, 부산 파생값 4항목 실측, 7행 삭제분 대조표(미복원 2항목 포함), 5열 용량 추이 표, 연결률 최종 보고, `price_change` 지역 분포와 창부레터 인계 사항을 기록할 것.
</output>
</content>
