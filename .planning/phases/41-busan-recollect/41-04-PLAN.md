---
phase: 41-busan-recollect
plan: "04"
type: execute
wave: 3
depends_on: ["41-03"]
files_modified:
  - scripts/busan-dup-candidates.csv
autonomous: false
requirements:
  - BUSAN-04
must_haves:
  truths:
    - "부산 complexes 의 lat/lng 좌표 커버리지가 95% 이상이고, 미확보분이 카카오 미등록 데이터 갭으로 특정된다"
    - "백필 진입 게이트(busan-status.ts --assert-seed-gate)가 exit 0 으로 통과한다 — 미달이면 백필을 시작하지 않는다"
    - "좌표+이름유사 중복 후보가 병합 없이 로그로 남아 연결률 해석의 근거가 된다"
  artifacts:
    - path: "scripts/busan-dup-candidates.csv"
      provides: "부산 잠재 중복 단지 후보 (log-only, 병합 없음)"
      contains: "dist_m"
  key_links:
    - from: "scripts/geocode-complexes.ts"
      to: "complexes.lat / complexes.lng"
      via: "카카오 키워드 검색 → 실패 시 road_address 주소 검색 폴백"
      pattern: "searchKakaoAddress"
    - from: "scripts/busan-status.ts --assert-seed-gate"
      to: "41-05/41-06/41-07 백필 dispatch"
      via: "exit 0 이 백필 진입 조건. exit 1 이면 백필 태스크를 시작하지 않는다"
      pattern: "assert-seed-gate"
---

<objective>
D-03 의 두 번째 단계이자 **백필 진입 게이트**다. 좌표가 없으면 `match_complex_by_coord` 경로가 죽고 이름 단독 매칭만 남는다 — CLAUDE.md 가 금지한 상태(단지명 단독 매칭 금지, 항상 좌표+이름 복합 매칭)이고, 그대로 백필하면 부산 거래가 대량 미연결로 쌓인다.

그리고 이 plan 은 **다음 3개 plan(백필 그룹 A·B·C)의 진입을 막는 문지기**다. `scripts/busan-status.ts --assert-seed-gate` 가 exit 0 이 되기 전에는 백필을 dispatch 하지 않는다. 41-CONTEXT Risk Summary 가 이것을 명시적 게이트로 요구했다.

⚠️ **`geocode-complexes.ts` 는 페이지네이션이 없다** — `.is('lat', null)` 쿼리가 PostgREST 1,000행 캡에 걸려 1회 실행이 최대 1,000건만 처리한다(Phase 34 실측: 3회 반복 필요). 멱등하므로(매 실행마다 남은 null 만 재조회) 반복 실행으로 완주한다.

Purpose: 좌표 확보 + 백필 진입 판정.
Output: 좌표 커버리지 실측, 게이트 통과(또는 차단) 판정, 중복 후보 CSV.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-05-PLAN.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-05-SUMMARY.md
@C:\Users\jung\coding\bds\scripts\geocode-complexes.ts
@C:\Users\jung\coding\bds\scripts\detect-busan-dup-candidates.ts

<interfaces>
<!-- scripts/geocode-complexes.ts -->
필요 env: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, KAKAO_REST_API_KEY
대상: complexes where lat is null (전역 — sgg_code 필터 없음), .range() 없음 → 1회 최대 1,000행
경로: searchKakao(키워드) → 실패 시 searchKakaoAddress(road_address) 폴백
출력: "✅ 완료: N개 성공(주소 검색 폴백 M건 포함), K개 실패"

<!-- Phase 34 실측 (34-05-SUMMARY) -->
3회 반복 수렴: 1,000건 전량 성공 → 507건 중 462성공/45실패 → 45건 재시도 0성공/45실패(안정)
부산 최종 커버리지 1,462/1,463 = 99.9%   (미확보 1건: 광안비치올리브씨아파트 — road_address 자체가 null)
비-부산 잔여 44건은 카카오 DB 미등록 (빌라·소규모) — 이 Phase 범위 밖의 기존 갭
부산 좌표 실측 범위 lat 35.05~35.34 / lng 128.83~129.28
complexes-map.ts BBOX 는 34-05 에서 이미 lng 129.4 로 확장됨 — 재조정 불필요

<!-- scripts/detect-busan-dup-candidates.ts -->
find_nearby_similar_complexes RPC (ST_DWithin 30m + trigram similarity >= 0.4) 를 순회 호출
writeFileSync 로 scripts/busan-dup-candidates.csv 를 덮어쓴다 (append 아님 — 중복 누적 없음)
.range() 페이지네이션 있음. 처리 건수 N==0 이면 exit 1 가드
Phase 34 결과: 30건 (대부분 dist_m=0.0 — 동일 좌표에 다른 KAPT 코드로 시딩된 이름 변형체)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 부산 단지 좌표 지오코딩 반복 실행 (1,000행 캡 대응)</name>
  <files>없음 (스크립트 실행. 실측값은 SUMMARY.md 에 기록)</files>
  <read_first>
    - scripts/geocode-complexes.ts (전역 `lat is null` 대상, `.range()` 없음, 100ms rate limit)
    - .planning/phases/34-db-2/34-05-SUMMARY.md ("Deviations from Plan" 절 — 3회 반복 수렴 실측 경로)
    - scripts/busan-status.ts (41-01 산출물 — 커버리지 측정)
  </read_first>
  <action>
`geocode-complexes.ts` 를 **코드 수정 없이** 반복 실행한다. 이 스크립트는 지역 필터가 없어 비-부산 잔여 44건(카카오 DB 미등록 빌라·소규모)도 매번 함께 시도하지만, 100ms 대기 × 44건이라 비용이 무의미하다 — 페이지네이션 추가나 `--sgg` 도입은 이 plan 범위 밖이다.

실행 전 env: 이 스크립트는 `@next/env` 를 쓰지 않고 `process.env` 를 직접 읽으며, 로컬 URL 이면 경고를 찍는다. `NEXT_PUBLIC_SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`·`KAKAO_REST_API_KEY` 를 셸에서 export 한 뒤 실행하고, 출력 첫 줄의 `🔗 연결 대상:` 이 프로덕션 URL 인지 확인한다.

반복 규칙: 매 실행 후 `npx tsx scripts/busan-status.ts` 로 부산 `lat is null` 수를 측정하고, **줄어드는 동안 재실행**한다. 연속 2회 동일하면 카카오 미등록 데이터 갭으로 판정하고 멈춘다(Phase 34 는 3회에 수렴했다). 각 회차의 `성공/폴백/실패` 3수치를 SUMMARY.md 에 회차별로 기록한다.

미확보분 처리: Phase 34 가 특정한 1건(광안비치올리브씨아파트, `road_address` 자체가 null)과 대조한다. ⚠️ `scripts/enrich-apt-unmatched.ts` 로 재시도하지 말 것 — 이 스크립트는 `src/services/bld-rgst.ts` 의 `import 'server-only'` 때문에 `tsx` 직접 실행 시 모듈 로드 시점에 크래시한다(Phase 34 확인, 이 Phase 범위 밖의 기존 버그).

⚠️ `complexes-map.ts` 의 BBOX 는 34-05 에서 이미 부산 실측 범위(lng 상한 129.4)를 포함하도록 확장돼 있다. **다시 손대지 않는다.** 확장 여부만 `grep` 으로 확인해 SUMMARY.md 에 기록한다.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/busan-status.ts --json` 에서 부산 좌표 커버리지 >= **95%**
    - 부산 `lat is null` 수가 연속 2회 측정에서 동일 (수렴 확인)
    - SUMMARY.md 에 회차별 `성공/주소폴백/실패` 3수치와 최종 커버리지가 기록됨
    - `grep -c "129.4" src/lib/data/complexes-map.ts` >= 1 이고 `git diff --stat src/lib/data/complexes-map.ts` 가 공백 (무변경)
    - 미확보 단지 목록이 SUMMARY.md 에 열거되고 Phase 34 의 1건과 대조됨
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --json && git diff --stat --exit-code src/lib/data/complexes-map.ts</automated>
  </verify>
  <done>부산 좌표 커버리지가 95% 이상으로 수렴하고 미확보분이 데이터 갭으로 특정된다.</done>
</task>

<task type="auto">
  <name>Task 2: 중복 후보 재탐지 + 백필 진입 게이트 판정</name>
  <files>scripts/busan-dup-candidates.csv</files>
  <read_first>
    - scripts/detect-busan-dup-candidates.ts (`writeFileSync` 덮어쓰기, N==0 가드, RPC 인자)
    - scripts/busan-dup-candidates.csv (Phase 34 결과 30건 — 이번 결과와 대조할 기준)
    - scripts/busan-status.ts (`--assert-seed-gate` 3조건: complexes >= 1400 / 좌표 >= 95% / 16개 구 전부 >= 1행)
    - .planning/phases/41-busan-recollect/41-CONTEXT.md (Risk Summary — 게이트 요구 근거)
  </read_first>
  <action>
**(1) 중복 후보 재탐지** — `npx tsx scripts/detect-busan-dup-candidates.ts` 를 실행한다. `find_nearby_similar_complexes` RPC(ST_DWithin 30m + trigram 0.4)는 34-03 이 프로덕션에 적용했으므로 그대로 있다. 결과는 `scripts/busan-dup-candidates.csv` 를 **덮어쓴다**(append 아님).
- 🔴 **병합하지 않는다.** D-10 의 defer 를 유지한다 — 이 태스크는 로그만 남긴다. 연결률이 65% 를 밑돌 때 원인 후보로 쓰기 위한 자료다
- Phase 34 결과(30건)와 건수를 대조한다. 크게 다르면(예: 60건 이상) K-apt 등록 상태가 바뀐 것이므로 그 사실을 기록한다
- RPC 가 없어 호출이 실패하면(마이그레이션 이력 손실 등) CSV 를 갱신하지 못한 사유를 기록하고 게이트 판정으로 넘어간다 — 중복 탐지는 게이트 조건이 **아니다**

**(2) 🔴 백필 진입 게이트 판정** — `npx tsx scripts/busan-status.ts --assert-seed-gate` 를 실행한다.
- **exit 0** → 게이트 통과. 41-05(그룹 A 백필)로 진행 가능
- **exit 1** → 🔴 **백필을 시작하지 않는다.** 어느 조건이 미달인지(단지 수 / 좌표 커버리지 / 빈 구)와 실측값을 SUMMARY.md 에 적고 아래 체크포인트에서 사용자에게 보고한다. 41-03 으로 되돌아가 시딩을 보완하는 것이 정답이며, 게이트를 낮춰 통과시키는 것은 **금지**다 — 그 결과가 2026-05-26 사고(연결률 19.1%)다

**(3) D-05 dispatch 계획 확정** — 다음 3개 plan 이 쓸 그룹 분할을 실측값으로 검토해 SUMMARY.md 에 인계한다. 계획된 분할(41-CONTEXT D-05, 지역-월 균등이 아니라 거래량 균등을 노린 것):
- 그룹 A (5구, 대형): `26230,26260,26320,26350,26380` (부산진·동래·북·해운대·사하)
- 그룹 B (5구, 중대형): `26290,26410,26440,26470,26500` (남·금정·강서·연제·수영)
- 그룹 C (6구, 소형): `26110,26140,26170,26200,26530,26710` (중·서·동·영도·사상·기장)
각 그룹의 지역-월 수(그룹당 5×140×2=1,400 / 6×140×2=1,680)와 D-05 실측 산정(5시간 job ≈ 1,560 지역-월)을 나란히 적고, 그룹 C 가 산정을 초과하는 것과 그때 `--resume` 이 재개 경로가 된다는 점을 명시한다.
  </action>
  <acceptance_criteria>
    - `test -f scripts/busan-dup-candidates.csv` 성공, 헤더에 `dist_m` 포함, 행 수가 SUMMARY.md 에 Phase 34 의 30건과 나란히 기록됨
    - `npx tsx scripts/busan-status.ts --assert-seed-gate` 의 exit 코드와 그때의 3조건 실측값이 SUMMARY.md 에 기록됨
    - 게이트가 exit 1 인 경우 SUMMARY.md 에 "백필 미진입" 이 명시되고 41-05 로 진행하지 않았음이 확인됨
    - `git diff --stat src/lib/data/complex-matching.ts supabase/migrations/` 가 공백 (병합·스키마 변경 없음 — D-10 defer 유지)
    - SUMMARY.md 에 3그룹의 sgg_code 목록·지역-월 수·5시간 job 산정 대조가 표로 있다
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --assert-seed-gate</automated>
  </verify>
  <done>중복 후보가 로그로 남고, 백필 진입 여부가 실측 3조건으로 판정된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] 백필 진입 승인 — 게이트 실측 보고</name>
  <files>없음 (사용자 확인 대기)</files>
  <read_first>
    - .planning/phases/41-busan-recollect/41-CONTEXT.md (Risk Summary — 게이트 요구 근거와 D-05 분할)
    - scripts/busan-status.ts (게이트 3조건의 실제 판정 구현)
    - scripts/busan-dup-candidates.csv (중복 후보 결과)
  </read_first>
  <what-built>
부산 16개 구의 Golden Record 시딩(41-03)과 좌표 지오코딩(Task 1)을 완료하고, 중복 후보를 로그로 남긴 뒤 **백필 진입 게이트를 실측 판정**했다. 다음 단계는 MOLIT 실거래 백필 3그룹 dispatch 이고, 이것은 **여러 날에 걸친 GitHub Actions 실행**이라 사용자 확인 후에 시작해야 한다.
  </what-built>
  <how-to-verify>
1. 게이트·실측값 확인:
   `npx tsx scripts/busan-status.ts`
   - 부산 `complexes` >= 1,400 / 좌표 커버리지 >= 95% / 16개 구 전부 1행 이상
   - `npx tsx scripts/busan-status.ts --assert-seed-gate` 가 exit 0
2. 시딩 규모 해석 확인 — SUMMARY.md 의 실측값 vs **1,463**(Phase 34 K-apt 시딩) vs **1,594**(2026-08-10 삭제분) vs **1,500**(ROADMAP 요구치) 대조 설명이 납득 가능한지
3. 중복 후보 확인: `wc -l scripts/busan-dup-candidates.csv` — Phase 34 의 30건과 비슷한 규모인지
4. 3그룹 분할(그룹 A/B/C sgg_code 목록)과 **하루 한 그룹** dispatch 계획을 승인할지 결정
5. Supabase Dashboard → Database → Usage 에서 현재 DB 용량을 확인해 알려주면, 그룹별 용량 추이 대조의 기준선으로 쓴다 (`busan-status.ts` 의 `db_size_bytes` 값과 대조)
  </how-to-verify>
  <resume-signal>게이트 통과 + 3그룹 분할 승인 시 "승인 — 그룹 A 시작, DB N MB" 로 응답. 게이트 미달이면 "게이트 미달: <조건>" 으로 알려주면 41-03 시딩 보완으로 되돌아간다. 그룹 분할을 바꾸려면 원하는 분할을 알려주면 된다.</resume-signal>
  <action>Task 1·2 의 실측값(부산 complexes / 좌표 커버리지 / 16개 구 breakdown / --assert-seed-gate exit 코드 / 중복 후보 건수 / 3그룹 분할표)을 정리해 사용자에게 제시하고, 백필 3그룹 dispatch 진입 승인과 Supabase Dashboard DB 용량 기준선을 받는다. 게이트가 exit 1 이면 승인을 요청하지 말고 미달 조건을 보고한 뒤 41-03 시딩 보완으로 되돌린다.</action>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --assert-seed-gate</automated>
  </verify>
  <acceptance_criteria>
    - 사용자 응답에 백필 진입 승인 여부가 명시됨
    - 사용자가 알려준 Supabase Dashboard DB 용량 수치가 SUMMARY.md 에 기록되고 `busan-status.ts` 의 `db_size_bytes` 와 대조됨
    - 게이트 exit 1 이었다면 41-05 로 진행하지 않았음이 SUMMARY.md 에 기록됨
  </acceptance_criteria>
  <done>사용자가 백필 진입을 승인하고 DB 용량 기준선을 알려준다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 카카오 로컬 API → `complexes.lat/lng` | 외부 좌표가 Golden Record 매칭의 기준이 된다. 오좌표는 거래를 엉뚱한 단지에 붙인다 |
| 게이트 판정 → 백필 3그룹 dispatch | 여기를 잘못 통과시키면 28만건이 미연결로 쌓인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-12 | Tampering (오매칭) | 카카오 키워드 검색의 잘못된 1순위 결과 | mitigate | 좌표 실측 범위(lat 35.05~35.34 / lng 128.83~129.28)를 벗어나는 행이 있는지 `busan-status.ts` 측정에 포함해 확인. 34-05 가 같은 방식으로 검증했다 |
| T-41-13 | Repudiation (게이트 우회) | 게이트 미달인데 백필을 진행 | mitigate | `--assert-seed-gate` exit 코드를 acceptance 로 못박고, 임계 하향을 명문 금지. 체크포인트에서 사용자 확인 |
| T-41-14 | Tampering (중복 병합 사고) | `detect-busan-dup-candidates.ts` 를 병합 스크립트로 오용 | mitigate | 이 스크립트는 SELECT RPC + CSV 쓰기만 한다. `complex-matching.ts`·마이그레이션 diff 공백을 acceptance 로 단언 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
npx tsx scripts/busan-status.ts
npx tsx scripts/busan-status.ts --assert-seed-gate
wc -l scripts/busan-dup-candidates.csv
```
</verification>

<success_criteria>
1. 부산 좌표 커버리지 >= 95%, 미확보분이 카카오 미등록 갭으로 특정됨 (BUSAN-04 후반부)
2. `--assert-seed-gate` 판정 결과와 3조건 실측값이 기록됨
3. `scripts/busan-dup-candidates.csv` 갱신, 병합 0건 (D-10 defer 유지)
4. `complexes-map.ts`·`complex-matching.ts`·마이그레이션 무변경
5. 3그룹 dispatch 계획이 지역-월 수·5시간 산정과 함께 인계됨
6. 사용자가 백필 진입을 승인함
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-04-SUMMARY.md` 를 생성한다. 회차별 지오코딩 수치, 게이트 3조건 실측값과 exit 코드, 중복 후보 건수 대조, 3그룹 분할 표, 사용자 승인 내용을 기록할 것.
</output>
</content>
