---
phase: 41-busan-recollect
plan: "07"
type: execute
wave: 6
depends_on: ["41-06"]
files_modified: []
autonomous: false
requirements:
  - BUSAN-05
must_haves:
  truths:
    - "그룹 C 6개 구(26110·26140·26170·26200·26530·26710)의 201501~202608 실거래가 transactions 에 적재된다"
    - "부산 16개 구 전부의 ingest_runs success 가 (140개월 × apt/villa) 에 수렴하고, 미완료 (지역,월) 이 0 또는 사유와 함께 특정된다"
    - "백필 중단 후 재개 지점이 ingest_runs 로 판정 가능함이 실제 재dispatch 로 증명된다 (Success Criteria 10)"
  artifacts: []
  key_links:
    - from: ".github/workflows/molit-backfill-once.yml (workflow_dispatch)"
      to: "scripts/backfill-realprice.ts"
      via: "sgg_codes=그룹C 6코드 / from=201501 / resume=true / min_linked_pct=65"
      pattern: "sgg_codes"
    - from: "scripts/backfill-realprice.ts (getCompletedRuns/getCompletedVillaRuns)"
      to: "ingest_runs (status='success')"
      via: "--resume 이 완료된 (source_id, sgg_code, year_month) 을 skip — 재개 지점 판정 근거"
      pattern: "getCompletedRuns"
---

<objective>
MOLIT 실거래 백필 **그룹 C** — 부산 소형 6개 구(중·서·동·영도·사상·기장)를 `201501~` 로 백필하고, **부산 16개 구 전체의 완료를 확정**한다.

🔴 **그룹 C 는 D-05 산정을 초과한다.** 6구 × 140개월 × 2종 = **1,680 지역-월** vs 5시간 job 산정 ≈ 1,560 지역-월. 다만 그룹 C 는 소형 구라 월당 거래가 적고 페이지 초과분이 거의 없어 건당 소요가 중간값(11.5초)보다 짧을 가능성이 높다. **한 번에 시도하되 타임아웃을 정상 경로로 취급한다** — 그리고 그때의 재dispatch 가 **Success Criteria 10("백필 중단 시 재개 지점이 `ingest_runs` 로 판정 가능")의 실증 기회**다. 타임아웃 없이 완주하면 별도로 재개 경로를 검증한다(Task 3).

Purpose: 그룹 C 적재 + 부산 전체 완료 확정 + 재개 메커니즘 실증.
Output: 부산 16개 구 전량 적재, 미완료 (지역,월) 전수 특정, 재개 증명.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-05-SUMMARY.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-06-SUMMARY.md
@C:\Users\jung\coding\bds\.github\workflows\molit-backfill-once.yml
@C:\Users\jung\coding\bds\scripts\backfill-realprice.ts

<interfaces>
<!-- 그룹 C dispatch 입력 -->
sgg_codes      = 26110,26140,26170,26200,26530,26710   (중구·서구·동구·영도구·사상구·기장군)
from           = 201501
to             = (공백 — 당월 자동)
resume         = true
min_linked_pct = 65

<!-- 규모 -->
지역-월 = 6구 × 140개월 × 2종 = 1,680   (5시간 job 산정 1,560 을 초과)
부산 전체 = 16구 × 140 × 2 = 4,480 지역-월

<!-- 재개 메커니즘 (Success Criteria 10 의 검증 대상) -->
getCompletedRuns(sggCode)       source_id='molit_trade',       status='success' 인 year_month 집합
getCompletedVillaRuns(sggCode)  source_id='molit_villa_trade', status='success' 인 year_month 집합
cleanupStuckRuns()              status='running' 이고 started_at 이 30분 초과인 행을 'failed' 로 마감 (다음 실행이 재시도한다)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 그룹 C dispatch (6개 구 = 산정 초과 구간)</name>
  <files>없음 (워크플로 dispatch)</files>
  <read_first>
    - .planning/phases/41-busan-recollect/41-06-SUMMARY.md (그룹 B 실측 소요시간·타임아웃 여부·분할 여부 — 이번 판단의 1차 근거)
    - .planning/phases/41-busan-recollect/41-05-SUMMARY.md (그룹 A 실측)
    - .github/workflows/molit-backfill-once.yml (입력 이름과 조건부 조립)
    - scripts/busan-status.ts
  </read_first>
  <action>
**(1) dispatch 형태 결정** — 41-05·41-06 의 **실제 건당 소요시간**을 계산한다(실제 소요시간 ÷ 실제 처리 지역-월 수). 그 값으로 1,680 지역-월의 예상 소요를 다시 산정한다.
- 예상 소요 < 4.5시간 → 6코드 한 번에 dispatch
- 예상 소요 >= 4.5시간 → 3코드 + 3코드로 나눠 두 번 dispatch. 🔴 5시간에 아슬아슬하게 넣어 잘리는 것보다 나누는 편이 낫다 — 잘린 job 은 5시간을 태우고 부분 적재로 끝난다
결정과 산정 근거(건당 초 × 1,680)를 SUMMARY.md 에 먼저 적는다.

**(2) 착수 전 기준값 고정** — `npx tsx scripts/busan-status.ts --json` 을 "그룹 C 착수 전"으로 기록한다.

**(3) dispatch** — `gh workflow run molit-backfill-once.yml` 에 `<interfaces>` 의 그룹 C 입력을 전달한다. 04:00 KST(19:00 UTC) 전후 1시간 회피, 그룹 B dispatch 로부터 **최소 24시간 경과** 후.

**(4) 초기 로그 확인** — `🔌 MOLIT 연결 확인: HTTP 200` / `📅 기간: 201501` / `📍 지역:` 에 그룹 C 코드만. 그룹 C 첫 run 이므로 skip 은 0 이어야 한다.
  </action>
  <acceptance_criteria>
    - SUMMARY.md 에 "실측 건당 초 × 1,680 = 예상 소요" 산정과 dispatch 형태 결정 근거가 기록됨
    - 그룹 B dispatch 시각과의 간격 >= 24시간이 기록됨
    - `gh run list --workflow=molit-backfill-once.yml --limit=1` 에 새 run 이 있고 run id 가 기록됨
    - run 로그에 `📅 기간: 201501` 이 나타나고 `📍 지역:` 이 그룹 C 코드만 담는다
    - SUMMARY.md 에 착수 전 `busan-status.ts --json` 기준값이 있다
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=3</automated>
  </verify>
  <done>실측 산정에 근거한 형태로 그룹 C 가 실행 중이고 기준값이 고정됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [CHECKPOINT] 그룹 C 백필 완료 대기 (다세션, 타임아웃 정상)</name>
  <files>없음 (사용자 확인 대기)</files>
  <read_first>
    - .github/workflows/molit-backfill-once.yml
    - scripts/backfill-realprice.ts (`getCompletedRuns`/`getCompletedVillaRuns` — skip 건수의 의미)
    - .planning/phases/41-busan-recollect/41-06-SUMMARY.md (그룹 B 실측)
  </read_first>
  <what-built>
그룹 C(중·서·동·영도·사상·기장 6개 구)의 MOLIT 실거래 백필을 `201501~` 로 dispatch 했다. 6개 구 = 1,680 지역-월로 D-05 의 5시간 산정(≈1,560)을 초과하는 구간이라 **타임아웃 후 재dispatch 가 필요할 수 있다 — 그것은 실패가 아니라 정상 설계다.**
  </what-built>
  <how-to-verify>
1. 실행 상태: `gh run list --workflow=molit-backfill-once.yml --limit=3`
   - `success` → 3번으로
   - **타임아웃/`cancelled`(5시간)** → 🔴 **정상.** 같은 입력으로 재dispatch 하면 `--resume` 이 완료분을 skip 한다. **몇 건이 skip 됐는지 로그에서 확인해 알려주면** 그것이 재개 메커니즘의 증거가 된다
   - **exit 75**(`🚫 이 러너에서 MOLIT 에 연결할 수 없습니다` / `연결 불가가 연속 3회`) → 즉시 재dispatch
   - **exit 1 + `실패율이 임계(30%)를 넘어`** → 로그의 실패 사유 확인 후 재실행
   - **`Check linkage rate` 단계만 실패** → 적재는 됐고 연결률 65% 미만. 3번을 진행한 뒤 보고
2. 부산 전체 잔여 확인: `npx tsx scripts/busan-status.ts`
   16개 구 전부의 `ingest_runs` success 가 각각 280(=140개월 × apt/villa)에 가까운지 본다. 크게 모자란 구가 있으면 그 구만 `sgg_codes` 에 넣어 재dispatch 한다
3. **[필수] Supabase Dashboard → Database → Usage 의 최종 DB 용량**을 확인해 알려준다 (부산 전체 예상 약 650MB / Pro 한도 8,192MB = 8%)
4. 완료 후에는 파생값 배치(41-08)로 넘어간다 — 이때는 API 한도와 무관하므로 하루를 기다릴 필요가 없다
  </how-to-verify>
  <resume-signal>"그룹 C 완료, DB N MB" 로 응답. 타임아웃 후 재dispatch 를 했다면 **재dispatch 로그의 skip 건수**를 함께 알려주면 재개 메커니즘 증거로 기록한다. 특정 구가 모자라면 "26xxx 미완료" 로 알려주면 그 구만 재dispatch 한다.</resume-signal>
  <action>그룹 C run 의 완료를 사용자가 확인해 알려줄 때까지 대기한다. 1,680 지역-월은 5시간 산정을 초과하므로 타임아웃을 정상 경로로 안내하고, 재dispatch 시 로그의 skip 건수를 받아 재개 메커니즘 증거로 쓴다. 최종 DB 용량 수치도 받는다.</action>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=5</automated>
  </verify>
  <acceptance_criteria>
    - 그룹 C run 의 최종 conclusion 이 확인됨
    - 타임아웃 후 재dispatch 가 있었다면 그 run 의 skip 건수가 기록됨 (재개 메커니즘 증거)
    - 최종 DB 용량 수치가 기록됨
  </acceptance_criteria>
  <done>그룹 C 가 완주했고 재개 증거·최종 용량을 확보했다.</done>
</task>

<task type="auto">
  <name>Task 3: 부산 16개 구 완료 확정 + 재개 메커니즘 증명</name>
  <files>없음 (측정 전용. 실측값은 SUMMARY.md 에 기록)</files>
  <read_first>
    - scripts/busan-status.ts
    - scripts/check-ingest-linkage.ts
    - scripts/backfill-realprice.ts (`getCompletedRuns`/`getCompletedVillaRuns`/`cleanupStuckRuns` — 재개 판정의 실제 구현)
    - .planning/phases/41-busan-recollect/41-05-SUMMARY.md, 41-06-SUMMARY.md (그룹 A·B 실측)
  </read_first>
  <action>
**(1) 그룹 C 실측 5종** — 41-05·41-06 Task 3 과 동일한 항목을 그룹 C 기준으로 측정한다: 첫 run skip==0 / `rows_upserted` 합계 vs `transactions` 증가분 / 6개 구 각각 `transactions` > **1,000** / `deal_date` 최소값 2015-01 대 / `check-ingest-linkage.ts --since-hours=48 --sgg=26110,26140,26170,26200,26530,26710 --min-linked-pct=65`.

**(2) 부산 16개 구 완료 확정** — `busan-status.ts` 의 `ingest_runs` 교차 집계로 **구별 미완료 (지역,월) 수**를 만든다. 기대값은 구당 `molit_trade` 140 + `molit_villa_trade` 140 = 280, 합계 4,480 이다.
- `status='success'` 합계가 4,480 의 **95% 이상**이어야 한다
- 미완료분(`failed` 또는 행 없음)은 🔴 **구·월 단위로 전수 열거**한다. "대체로 됐다"로 넘기지 않는다. 남은 것이 특정 월에 몰려 있으면(예: 202608 = 당월, 아직 신고 데이터가 없다) 그 사유를 적는다
- `status='running'` 으로 남은 행이 있으면 `cleanupStuckRuns()`(30분 초과 시 `failed` 마감)가 다음 실행에서 정리함을 근거로 기록한다

**(3) 🔴 재개 메커니즘 증명 (Success Criteria 10)** — 두 경로 중 하나로 증명한다:
- **(a) 타임아웃이 실제로 발생한 경우**: 재dispatch run 로그의 `✅ 완료: N건 처리 (M건 skip)` 에서 **M > 0** 이고, 그 M 이 앞 run 의 성공 건수와 일치(또는 근사)함을 대조한다. 이것이 가장 강한 증거다
- **(b) 타임아웃 없이 완주한 경우**: 이미 완료된 임의의 1개 구를 `resume=true` 로 **의도적으로 재dispatch** 한다. 로그가 `280건 처리 (280건 skip), 0건 upsert` 로 끝나야 한다 — 즉 **완료 상태에서 재실행하면 아무 일도 하지 않는다**는 것이 재개 판정이 동작한다는 증거다. 이 실행은 API 호출을 거의 쓰지 않으므로(각 지역별 `ingest_runs` 조회 2회 + preflight 1회) 한도에 무해하다
어느 경로를 썼는지와 로그 인용을 SUMMARY.md 에 남긴다.

**(4) 부산 전체 4열 추이 표** — SUMMARY.md 에 착수 전 / A 후 / B 후 / C 후 4열 표를 완성한다. 행은 부산 `transactions` 총 수, 부산 연결률, `db_size_bytes`(MB), 8,192MB 대비 %. 41-08 이 이 표를 인용한다.
  </action>
  <acceptance_criteria>
    - 그룹 C 첫 run skip == 0 (또는 사유 규명), `rows_upserted` 합계 > 0, `transactions` 증가분 > 0
    - 그룹 C 6개 구 각각 `transactions` > 1,000, `deal_date` 최소값 2015-01 대
    - 부산 16개 구 `ingest_runs` success 합계 >= 4,480 × 0.95 = **4,256**
    - 미완료 (지역,월) 이 0 이거나, 0 이 아니면 구·월 단위로 전수 열거되고 각각 사유가 적혀 있다
    - 재개 증명이 (a) 또는 (b) 경로로 수행되고 로그 인용이 SUMMARY.md 에 있다. (b) 의 경우 `0건 upsert` 가 확인된다
    - 4열 추이 표(착수 전 / A 후 / B 후 / C 후 × 4행)가 완성됨
    - `check-ingest-linkage.ts --sgg=<부산16코드> --min-linked-pct=65` 의 부산 전체 연결률이 기록됨
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/check-ingest-linkage.ts --since-hours=168 --sgg=26110,26140,26170,26200,26230,26260,26290,26320,26350,26380,26410,26440,26470,26500,26530,26710 --min-linked-pct=65</automated>
  </verify>
  <done>부산 16개 구 백필이 완료 확정되고, 재개 메커니즘이 로그로 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.go.kr(MOLIT) → `transactions` | 외부 응답이 Zod 검증을 지나 적재된다 |
| `ingest_runs` → 재개 판정 | 원장이 실제 데이터와 어긋나면 재개가 조용히 건너뛴다 (D-02 가 닫은 문) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-24 | Repudiation (부분 완료를 완료로 보고) | 16개 구 중 일부 월이 빠진 상태 | mitigate | Task 3(2) — success 합계 >= 4,256 단언 + 미완료 (지역,월) 전수 열거를 acceptance 로 못박음 |
| T-41-25 | Denial of Service (타임아웃 낭비) | 1,680 지역-월을 5시간에 밀어넣어 잘림 | mitigate | Task 1(1) — 실측 건당 초로 재산정, >= 4.5시간이면 3+3 분할 |
| T-41-26 | Repudiation (재개 미검증) | 재개가 동작한다고 가정만 하고 넘어감 | mitigate | Task 3(3) — 타임아웃 미발생 시 완료 구를 의도적으로 재dispatch 해 `0건 upsert` 를 확인 |
| T-41-27 | Denial of Service (stuck running) | `status='running'` 잔존 행이 재개를 막음 | mitigate | 기존 `cleanupStuckRuns()`(30분 초과 → failed)가 다음 실행에서 정리. 잔존 수를 측정·기록 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
gh run list --workflow=molit-backfill-once.yml --limit=8
npx tsx scripts/busan-status.ts
npx tsx scripts/check-ingest-linkage.ts --since-hours=168 --sgg=26110,26140,26170,26200,26230,26260,26290,26320,26350,26380,26410,26440,26470,26500,26530,26710 --min-linked-pct=65
```
</verification>

<success_criteria>
1. 그룹 C 6개 구의 201501~ 실거래가 적재됨 (BUSAN-05 3/3)
2. 부산 16개 구 `ingest_runs` success 합계 >= 4,256 (4,480 의 95%), 미완료분 전수 특정
3. 6개 구 각각 `transactions` > 1,000, `deal_date` 최소값 2015-01 대
4. 재개 메커니즘이 로그로 증명됨 (Success Criteria 10)
5. 부산 전체 연결률 실측값 기록 (임계 65%)
6. 4열 추이 표 완성 (착수 전 / A 후 / B 후 / C 후)
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-07-SUMMARY.md` 를 생성한다. 그룹 C run id·재dispatch 이력, 부산 16개 구 완료 집계표(구별 success/미완료), 재개 증명 로그 인용, 4열 추이 표를 기록할 것. 41-08 이 인용할 최종 수치(부산 `transactions` 총 수, 연결률, DB 용량)를 명확히 표시할 것.
</output>
</content>
