---
phase: 41-busan-recollect
plan: "05"
type: execute
wave: 4
depends_on: ["41-02", "41-04"]
files_modified: []
autonomous: false
requirements:
  - BUSAN-05
must_haves:
  truths:
    - "그룹 A 5개 구(26230·26260·26320·26350·26380)의 201501~202608 실거래(아파트+연립다세대)가 transactions 에 적재된다"
    - "첫 dispatch 의 skip 건수가 0 이다 — D-02 의 ingest_runs 무효화가 실제로 --resume 함정을 닫았음이 로그로 증명된다"
    - "적재 성공이 워크플로 초록불이 아니라 ingest_runs.rows_upserted 합계와 transactions 증가분 대조로 판정된다"
  artifacts: []
  key_links:
    - from: ".github/workflows/molit-backfill-once.yml (workflow_dispatch)"
      to: "scripts/backfill-realprice.ts"
      via: "sgg_codes=그룹A 5코드 / from=201501 / resume=true / min_linked_pct=65"
      pattern: "sgg_codes"
    - from: "scripts/backfill-realprice.ts"
      to: "ingest_runs.rows_upserted"
      via: "ingestMonth / ingestMonthVilla 가 (source_id, sgg_code, year_month) 단위로 기록"
      pattern: "rows_upserted"
---

<objective>
MOLIT 실거래 백필 **그룹 A** — 부산 대형 5개 구(부산진·동래·북·해운대·사하)를 `201501~` 로 백필한다.

🔴 **이 plan 은 백필 3그룹 중 첫 실행이므로 메커니즘 전체의 검증 지점이다.** 특히 D-02(부산 `ingest_runs` 무효화)가 실제로 `--resume` 함정을 닫았는지는 **첫 dispatch 로그의 `skip` 건수로만 증명된다.** 백필 전에 3,756건이 남아 있었다면 로그가 `(1400건 skip), 0건 upsert` 로 끝나면서 워크플로는 초록불이 된다 — 이 저장소가 반복해 겪은 실패 모양이다(ADR-063).

D-05 실측 산정: 지역-월 1건당 중간값 11.5초 → 그룹 A 는 5구 × 140개월 × 2종 = **1,400 지역-월 ≈ 4.5시간**. 워크플로 `timeout-minutes: 300`(5시간)에 **간신히** 들어간다. 초과하면 실패가 아니라 정상 설계이며 `--resume` 으로 재개한다.

Purpose: 그룹 A 적재 + 백필 메커니즘(무효화·기간·연결률 임계·재개) 실증.
Output: 그룹 A 거래 적재, skip=0 증명, 연결률·용량 실측.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-06-PLAN.md
@C:\Users\jung\coding\bds\.github\workflows\molit-backfill-once.yml
@C:\Users\jung\coding\bds\scripts\backfill-realprice.ts
@C:\Users\jung\coding\bds\scripts\check-ingest-linkage.ts

<interfaces>
<!-- 그룹 A dispatch 입력 (41-02 가 노출한 4개 입력 사용) -->
sgg_codes      = 26230,26260,26320,26350,26380      (부산진구·동래구·북구·해운대구·사하구)
from           = 201501                              (D-04 — 창원·김해와 대칭)
to             = (공백 — 당월 자동)
resume         = true                                (기본값. ingest_runs 가 비어 있으므로 skip 대상 없음)
min_linked_pct = 65                                  (D-07 — 경남 기타 실측 64.7% 기준선)

<!-- 예상 규모 -->
지역-월 = 5구 × 140개월 × 2종(apt+villa) = 1,400
소요 추정 = 1,400 × 11.5초 ≈ 4.5시간   vs   timeout-minutes: 300 (5시간)
API 호출 추정 = 1,400 + 페이지 초과분 ≈ 1,800~2,700   vs   일 10,000 한도

<!-- 스크립트 종료 코드 -->
exit 0   정상 (실패율 < 30%)
exit 1   실패율 >= FAILURE_ABORT_RATE(0.3) — 개별 지역 문제가 아니라 공통 원인
exit 75  EXIT_BLOCKED_RUNNER — data.go.kr 이 이 러너 IP 를 TCP 차단. 새 러너에서 재시도할 것

<!-- 크론 충돌 회피 (KST) -->
02:00 compute-predictions / 04:00 molit-daily + Vercel daily / 05:00 price-stats / 05:30 gap-stats / 06:00 complex-integrity
→ 09:00 KST 전후 dispatch 가 안전하다 (5시간 job 이 14:00 KST 에 끝난다)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 사전 재확인 후 그룹 A dispatch</name>
  <files>없음 (워크플로 dispatch)</files>
  <read_first>
    - .github/workflows/molit-backfill-once.yml (41-02 수정 후 상태 — 입력 4개의 실제 이름과 조건부 조립 로직)
    - scripts/busan-status.ts (`--assert-runs-purged`·`--assert-seed-gate`)
    - .planning/phases/41-busan-recollect/41-04-SUMMARY.md (게이트 판정 결과와 그룹 분할 확정본)
  </read_first>
  <action>
**(1) 진입 조건 2개를 dispatch 직전에 다시 실측한다.** 41-04 에서 통과했더라도 그 사이 시간이 흘렀다:
- `npx tsx scripts/busan-status.ts --assert-seed-gate` → exit 0 이어야 한다. exit 1 이면 🔴 **dispatch 하지 않고** 중단해 사용자에게 보고한다
- `npx tsx scripts/busan-status.ts --assert-runs-purged` → exit 0 이어야 한다. 부산 `ingest_runs` 가 0행이 아니면 D-02 가 되돌아간 것이므로 🔴 **dispatch 하지 않는다**

**(2) 백필 전 기준값을 고정한다.** `npx tsx scripts/busan-status.ts --json` 출력을 "그룹 A 착수 전"으로 SUMMARY.md 에 붙인다. 최소한 부산 `transactions` 총 수, 부산 `ingest_runs` 총 수, `db_size_bytes` 세 값이 있어야 Task 3 의 증가분 대조가 가능하다.

**(3) dispatch** — `gh workflow run molit-backfill-once.yml` 로 `<interfaces>` 의 그룹 A 입력 5개를 전달한다(`-f sgg_codes=...` `-f from=201501` `-f resume=true` `-f min_linked_pct=65`, `to` 는 생략하거나 공백).
- 🔴 **04:00 KST(19:00 UTC) 전후 1시간을 피한다.** `molit-daily.yml` 이 같은 MOLIT API 일 10,000회 한도를 쓴다. 09:00 KST 전후가 안전하다
- dispatch 후 `gh run list --workflow=molit-backfill-once.yml --limit=1` 로 run id 를 확보해 SUMMARY.md 에 기록한다
- **초기 로그 확인** — 시작 후 몇 분 안에 `gh run view <id> --log` 로 다음 3줄을 확인한다:
  - `🔌 MOLIT 연결 확인: HTTP 200` (없고 exit 75 로 끝났으면 러너 IP 차단 → **즉시 재dispatch**)
  - `📅 기간: 201501 ~ <당월>` 과 개월 수 (기간 인자가 실제로 전달됐는지. `201608` 로 나오면 `from` 입력이 새어나가지 않은 것 → 중단하고 41-02 를 점검)
  - `📍 지역:` 에 그룹 A 5코드만 (38개 전체가 나오면 `--sgg` 가 전달되지 않은 것 → **즉시 중단**)
  </action>
  <acceptance_criteria>
    - dispatch 직전 `--assert-seed-gate` 와 `--assert-runs-purged` 가 모두 exit 0 이었음이 SUMMARY.md 에 기록됨
    - `gh run list --workflow=molit-backfill-once.yml --limit=1` 에 방금 트리거한 run 이 나타나고 run id 가 기록됨
    - run 로그에 `📅 기간: 201501` 이 나타난다 (D-04 기간이 실제로 전달됨)
    - run 로그의 `📍 지역:` 줄에 코드가 정확히 5개이고 전부 `26` 으로 시작한다
    - SUMMARY.md 에 착수 전 `busan-status.ts --json` 기준값(transactions·ingest_runs·db_size)이 있다
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=3</automated>
  </verify>
  <done>그룹 A 가 올바른 기간·지역 인자로 실행 중이고 착수 전 기준값이 고정됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [CHECKPOINT] 그룹 A 백필 완료 대기 (다세션)</name>
  <files>없음 (사용자 확인 대기)</files>
  <read_first>
    - .github/workflows/molit-backfill-once.yml (`timeout-minutes: 300` 과 연결률 검사 스텝)
    - scripts/backfill-realprice.ts (`EXIT_BLOCKED_RUNNER=75` · `FAILURE_ABORT_RATE=0.3` · `cleanupStuckRuns()` — 종료 코드 해석 근거)
    - scripts/busan-status.ts (구별 `ingest_runs` success 집계)
  </read_first>
  <what-built>
그룹 A(부산진·동래·북·해운대·사하 5개 구)의 MOLIT 실거래 백필을 `201501~` 기간으로 dispatch 했다. D-05 실측 산정으로 **약 4.5시간**이 걸리고 워크플로 상한은 5시간이다 — 한 세션 안에 끝나지 않으므로 사람이 완료를 확인해야 한다. 초과로 잘려도 실패가 아니며 `--resume` 으로 재개한다.
  </what-built>
  <how-to-verify>
1. 실행 상태: `gh run list --workflow=molit-backfill-once.yml --limit=3`
   - `success` → 3번으로
   - `failure` → `gh run view <id> --log` 마지막 부분 확인. 🔴 종료 코드별 대응:
     - **exit 75**(`🚫 이 러너에서 MOLIT 에 연결할 수 없습니다` 또는 `연결 불가가 연속 3회`) → 러너 IP 차단. 같은 입력으로 **즉시 재dispatch**(회당 성공 확률 약 2/3)
     - **exit 1 + `실패율이 임계(30%)를 넘어`** → 공통 원인(API 키·네트워크). 재실행 전에 로그의 실패 사유를 확인
     - **`Check linkage rate` 단계에서만 실패** → 적재는 됐고 연결률이 65% 미만이다. 3번의 실측을 진행한 뒤 보고
   - `cancelled`/타임아웃(5시간) → **정상 설계.** 같은 입력으로 재dispatch 하면 `--resume` 이 완료분을 건너뛴다
2. 잔여 확인:
   `npx tsx scripts/busan-status.ts`
   그룹 A 5개 구의 `ingest_runs` success 수가 각각 280(=140개월×2종)에 가까워졌는지 본다. 크게 모자라면 재dispatch 한다
3. **[필수] Supabase Dashboard → Database → Usage 에서 DB 용량**을 확인한다. `busan-status.ts` 의 `db_size_bytes` 값과 대조한다 (예상: 착수 전 약 450MB → 그룹 A 후 약 500MB. Pro 8GB 대비 6% 수준)
4. **다음 그룹은 하루 뒤에** 시작한다 — MOLIT API 일 10,000회 한도를 `molit-daily.yml` 과 공유한다(D-05: 최소 2일 분산)
  </how-to-verify>
  <resume-signal>그룹 A 가 완주하면 "그룹 A 완료, DB N MB" 로 응답. 재dispatch 가 필요했으면 횟수와 사유(exit 75 / 타임아웃 / 실패율)를 함께 알려주면 SUMMARY 에 기록한다. 진행 중이면 "진행 중, ingest_runs success M건" 으로 알려주면 다음 세션에서 이어서 확인한다.</resume-signal>
  <action>그룹 A run 의 완료를 사용자가 확인해 알려줄 때까지 대기한다. 종료 코드별 대응(exit 75 = 러너 IP 차단 → 즉시 재dispatch / exit 1 + 실패율 30% 초과 → 사유 확인 후 재실행 / 5시간 타임아웃 → --resume 재dispatch / Check linkage rate 단계만 실패 → Task 3 실측 후 보고)을 안내하고, Supabase Dashboard DB 용량 수치를 받는다.</action>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=3</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow=molit-backfill-once.yml` 에서 그룹 A run 의 최종 conclusion 이 확인됨
    - 재dispatch 가 있었다면 횟수와 사유(exit 75 / 타임아웃 / 실패율)가 SUMMARY.md 에 기록됨
    - 사용자가 알려준 DB 용량 수치가 기록됨
  </acceptance_criteria>
  <done>그룹 A 가 완주했고 DB 용량 수치를 확보했다.</done>
</task>

<task type="auto">
  <name>Task 3: 그룹 A 적재 실측 단언 — 조용한 성공 차단</name>
  <files>없음 (측정 전용. 실측값은 SUMMARY.md 에 기록)</files>
  <read_first>
    - scripts/busan-status.ts (측정 진입점)
    - scripts/check-ingest-linkage.ts (연결률 판정 기준과 MIN_SAMPLE 가드)
    - .planning/phases/41-busan-recollect/41-CONTEXT.md (D-02·D-04·D-07 — 아래 단언의 근거)
  </read_first>
  <action>
🔴 **"워크플로가 초록불이다" 를 성공 근거로 쓰지 않는다.** 아래 5개를 실측해 SUMMARY.md 에 수치로 남기고, 하나라도 미달이면 사유를 규명한다.

**(1) skip=0 증명 (D-02 검증)** — 첫 dispatch 의 run 로그에서 완료 요약 줄(`✅ 완료: N건 처리 (M건 skip), K건 upsert, ...`)을 찾아 **M(skip) 이 0** 임을 확인한다. 0 이 아니면 `ingest_runs` 무효화가 불완전했던 것이므로 어느 (지역,월) 이 skip 됐는지 규명한다. 🔴 재dispatch 한 run 의 skip 은 정상이다(앞 run 의 성공분) — **첫 run 의 skip 만** 판정 대상이다.

**(2) `rows_upserted` 합계 vs `transactions` 증가분** — 그룹 A 5코드의 `ingest_runs.rows_upserted` 합계와, Task 1 이 고정한 착수 전 기준값 대비 부산 `transactions` 증가분을 나란히 적는다. `upsert` 는 `dedupe_key` 기반이라 재적재분이 있으면 `rows_upserted >= 증가분` 이 정상이다. 🔴 **증가분이 0 이면 적재가 없었던 것**이므로 초록불과 무관하게 실패로 처리한다.

**(3) 지역별 최저선** — 그룹 A 5개 구 각각 `transactions` > **1,000**. 140개월 창에서 부산 어느 구도 1,000건 미만일 수 없다. 한 구라도 미달이면 그 구의 `ingest_runs` 를 status 별로 집계해 원인(failed / 미실행)을 찾는다.

**(4) 기간 검증 (D-04)** — 그룹 A 5코드의 `transactions.deal_date` 최소값이 **2015-01** 대(2015-01-01 ~ 2015-01-31)임을 확인한다. `2016-07` 이면 `from=201501` 이 전달되지 않은 것이다.

**(5) 연결률 (D-07)** — `npx tsx scripts/check-ingest-linkage.ts --since-hours=48 --sgg=26230,26260,26320,26350,26380 --min-linked-pct=65` 를 직접 실행한다(워크플로의 `--since-hours=8` 은 재dispatch 로 여러 날에 걸쳤을 때 앞부분을 놓친다). 실측 연결률을 **운영권역 94.6% / 경남 기타 64.7%** 와 나란히 적는다.
- 65% 미달이면 🔴 매칭 경로를 의심한다(2026-05-26 사고 부류). 확인 순서: ① `busan-dup-candidates.csv` 의 중복이 거래를 분산시키는지 ② 미연결 거래의 `apt_name` 상위 빈도가 `complexes` 에 없는 이름인지 ③ `ingest_runs.error_message` 에 RPC 실패가 있는지. **복구는 `scripts/link-unmatched-safe.ts`(기본 dry-run)** 이며, 별칭 보정으로 90% 를 만드는 작업은 이 Phase 범위 밖이다
- 65% 이상이면 실측값만 기록하고 넘어간다 — **90% 를 요구하지 않는다**(D-07)

**(6) 용량** — `busan-status.ts` 의 `db_size_bytes` 와 체크포인트에서 사용자가 알려준 Dashboard 수치를 나란히 적고 8,192MB 대비 % 를 계산한다.
  </action>
  <acceptance_criteria>
    - 첫 dispatch run 로그의 완료 요약에서 skip 건수 == **0** (또는 0 이 아닌 사유가 (지역,월) 단위로 규명됨)
    - 그룹 A 5코드 `ingest_runs.rows_upserted` 합계 > 0 이고, 부산 `transactions` 증가분 > 0 이며 두 값이 SUMMARY.md 에 나란히 기록됨
    - 그룹 A 5개 구 각각 `transactions` > 1,000 (미달 구가 있으면 그 구의 `ingest_runs` status 집계가 기록됨)
    - 그룹 A 5코드 `transactions.deal_date` 최소값이 2015-01 대
    - `check-ingest-linkage.ts --sgg=<그룹A> --min-linked-pct=65` 의 출력(연결률 %)이 기록되고, 미달 시 원인 조사 3항목의 결과가 기록됨
    - `db_size_bytes` 와 Dashboard 수치, 8,192MB 대비 % 가 기록됨
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/check-ingest-linkage.ts --since-hours=48 --sgg=26230,26260,26320,26350,26380 --min-linked-pct=65</automated>
  </verify>
  <done>그룹 A 적재가 실측 수치 5종으로 증명되고, 연결률·용량이 기록된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.go.kr(MOLIT) → `transactions` | 외부 응답이 Zod 검증(`MolitSaleItemSchema`)을 지나 적재된다. `ZOD_FAILURE_THRESHOLD`(0.05)가 이미 있다 |
| GitHub Actions 러너 → 프로덕션 Supabase | `SUPABASE_SERVICE_ROLE_KEY` 시크릿으로 대량 upsert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-15 | Repudiation (조용한 성공) | `--resume` skip → 0건 적재 초록불 | mitigate | Task 3(1) 첫 run 의 skip==0 단언 + (2) `rows_upserted` 합계와 `transactions` 증가분 대조 |
| T-41-16 | Denial of Service (API 한도) | MOLIT 일 10,000회를 `molit-daily.yml` 과 공유 | mitigate | 04:00 KST ±1h 회피, 하루 한 그룹, 그룹당 호출 추정 1,800~2,700 |
| T-41-17 | Denial of Service (러너 IP 차단) | data.go.kr 의 Actions IP TCP 차단 | mitigate | 기존 `preflight()` + `EXIT_BLOCKED_RUNNER=75` + 연속 3회 중단. exit 75 면 즉시 재dispatch (성공 확률 회당 약 2/3) |
| T-41-18 | Tampering (오적재) | `--sgg`/`from` 이 전달되지 않아 전국·잘못된 기간 적재 | mitigate | Task 1 의 초기 로그 3줄 확인(`📅 기간: 201501`, `📍 지역:` 5코드)을 acceptance 로 못박음 |
| T-41-19 | Denial of Service (용량) | 대량 적재로 Pro 8GB 소모 | accept | 부산 전체 예상 650MB = 한도의 8%. 그룹별 실측 체크포인트로 추이 확인 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 (`npm ci` 는 기존 lockfile) |
</threat_model>

<verification>
```bash
gh run list --workflow=molit-backfill-once.yml --limit=5
npx tsx scripts/busan-status.ts
npx tsx scripts/check-ingest-linkage.ts --since-hours=48 --sgg=26230,26260,26320,26350,26380 --min-linked-pct=65
```
</verification>

<success_criteria>
1. 그룹 A 5개 구의 201501~ 실거래가 적재됨 (BUSAN-05 1/3)
2. 첫 dispatch 의 skip 건수 0 — D-02 가 `--resume` 함정을 닫았음이 증명됨
3. `rows_upserted` 합계와 `transactions` 증가분이 대조 기록됨 (초록불을 근거로 쓰지 않았다)
4. 5개 구 각각 `transactions` > 1,000, `deal_date` 최소값 2015-01 대
5. 연결률 실측값이 65% 임계 및 94.6%/64.7% 기준선과 함께 기록됨
6. DB 용량 실측 + 8GB 대비 % 기록
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-05-SUMMARY.md` 를 생성한다. run id 목록·재dispatch 이력과 사유, skip=0 로그 인용, 실측 5종 수치, 용량 추이를 기록할 것. 다음 그룹(41-06)에 인계할 사항(실제 소요시간, 실제 API 호출 규모, 타임아웃 여부)을 명시할 것.
</output>
</content>
