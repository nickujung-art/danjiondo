---
phase: 41-busan-recollect
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819080000_reactivate_busan_scope.sql
  - scripts/busan-status.ts
autonomous: true
requirements:
  - BUSAN-01
  - BUSAN-02
must_haves:
  truths:
    - "regions 부산 16행(26110~26710)이 is_active=true 이고 이 변경이 마이그레이션 원장에 등록돼 db reset 으로 재현된다"
    - "부산 ingest_runs 가 0행이라 --resume 이 아무것도 skip 하지 않는다 (조용한 성공 경로 차단)"
    - "부산 진행 상황(단지·좌표·거래·연결률·파생값·DB 용량)을 한 명령으로 실측할 수 있고, 시딩 게이트 미달 시 exit 1 로 판정된다"
  artifacts:
    - path: "supabase/migrations/20260819080000_reactivate_busan_scope.sql"
      provides: "regions 부산 활성화 + 부산 ingest_runs 무효화 + db_size_bytes() RPC"
      contains: "is_active = true"
    - path: "scripts/busan-status.ts"
      provides: "부산 복원 진행 실측 단일 진입점 + 시딩 게이트 판정(--assert-seed-gate)"
      exports: []
  key_links:
    - from: "supabase/migrations/20260819080000_reactivate_busan_scope.sql"
      to: "src/lib/data/regions.ts (getActiveSggCodes)"
      via: "regions.is_active=true 를 14곳 호출부가 동적으로 읽는다 — 코드 변경 없이 부산 포함"
      pattern: "is_active"
    - from: "scripts/busan-status.ts"
      to: "public.db_size_bytes() RPC"
      via: "supabase.rpc('db_size_bytes')"
      pattern: "db_size_bytes"
---

<objective>
2026-08-10 에 끈 스위치를 되돌린다 — `regions` 부산 16행을 `is_active=true` 로 올리고, **데이터 없이 기록만 남아 있는 부산 `ingest_runs` 4,006행을 지워 원장을 실제 상태와 일치시킨다.** 그리고 이후 6개 plan 이 전부 의존할 **실측 단일 진입점**(`scripts/busan-status.ts`)을 만든다.

🔴 **이 plan 의 존재 이유는 D-02 다.** `molit-backfill-once.yml` 이 `--resume` 를 하드코딩하고 `backfill-realprice.ts` 의 `getCompletedRuns()`/`getCompletedVillaRuns()` 가 `status='success'` 인 `(source_id, sgg_code, year_month)` 를 건너뛴다. 삭제 때 `ingest_runs` 는 지우지 않았으므로, 이 plan 없이 백필을 돌리면 **apt 1,903 + villa 1,853 = 3,756건이 전부 skip 되고 0건 적재로 초록불이 된다.**

Purpose: 이후 모든 plan 의 전제(스위치 ON + 원장 정합 + 측정 가능성) 확보.
Output: 마이그레이션 1건(원장 등록), `scripts/busan-status.ts`, 실측 기준값 기록.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\supabase\migrations\20260810060000_drop_busan_scope.sql.applied-manually
@C:\Users\jung\coding\bds\supabase\migrations\20260819030000_fix_unqualified_refs_in_secdef_rpcs.sql
@C:\Users\jung\coding\bds\src\lib\data\regions.ts
@C:\Users\jung\coding\bds\scripts\backfill-realprice.ts
@C:\Users\jung\coding\bds\scripts\check-ingest-linkage.ts

<interfaces>
<!-- 부산 16개 sgg_code (src/app/admin/region-expansion/page.tsx NEW_CODES 와 동일 집합) -->
26110 26140 26170 26200 26230 26260 26290 26320 26350 26380 26410 26440 26470 26500 26530 26710

<!-- ingest_runs 소비처 전수 (grep 결과) — 부산 행 삭제 영향 -->
scripts/backfill-realprice.ts       getCompletedRuns / getCompletedVillaRuns / cleanupStuckRuns  ← 이 plan 이 노리는 대상
scripts/backfill-officetel.ts       동일 resume 구조 (molit_offi_trade)
scripts/check-ingest-linkage.ts     transactions 만 읽는다 — ingest_runs 미참조 (영향 없음)
scripts/check-data-freshness.ts     source_run_id → ingest_runs.source_id 임베디드 필터, 지역 무관 (영향 없음)
src/app/admin/region-expansion/page.tsx  부산 진행률이 0 으로 리셋 — 데이터가 실제로 0 이므로 정직한 상태
src/app/api/cron/rankings/route.ts  source_id='rankings' 로 INSERT/UPDATE 만 한다. sgg_code 가 NULL 이라 '26%' LIKE 에 걸리지 않는다
transactions.source_run_id          references ingest_runs(id) on delete set null (20260430000008)

<!-- SECURITY DEFINER 함수 규약 (20260819030000 의 교훈) -->
SET search_path TO '' 이면 public 조차 탐색되지 않는다 → 본문의 모든 참조를 스키마 수식할 것 (pg_catalog.* 포함)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: regions 부산 활성화 + 부산 ingest_runs 무효화 마이그레이션 작성·적용</name>
  <files>supabase/migrations/20260819080000_reactivate_busan_scope.sql</files>
  <read_first>
    - supabase/migrations/20260810060000_drop_busan_scope.sql.applied-manually (되돌리는 대상. "되돌리려면" 절이 이 파일의 명세다)
    - supabase/seed.sql (🔴 부산 행이 **없다** — 창원 5구 + 김해 6행만 있다. 아래 금지사항의 근거)
    - supabase/migrations/20260819030000_fix_unqualified_refs_in_secdef_rpcs.sql (SECURITY DEFINER + search_path 규약)
    - supabase/migrations/20260819060000_restore_function_grants_lost_in_migration.sql (grant 복원 규약)
    - CLAUDE.md (마이그레이션 CRITICAL 규약)
  </read_first>
  <action>
`supabase/migrations/20260819080000_reactivate_busan_scope.sql` 을 신규 작성한다. D-01 에 따라 **정식 `.sql` 마이그레이션**이며 `.applied-manually` 확장자를 쓰지 않는다 — 원장에 등록돼야 `db reset` 재현성이 유지된다(Phase 37 이 회복한 것).

이 파일이 하는 일 3가지:

**(1) `public.regions` 부산 활성화** — `update public.regions set is_active = true where sgg_code like '26%'`. 조건을 `sgg_code like '26%'` 로 두는 근거를 주석에 남긴다: 법정동코드 `26` 은 부산광역시 전용이고 현재 16행 전수가 부산이다.

**(2) 부산 `ingest_runs` 무효화** — `delete from public.ingest_runs where sgg_code like '26%'`. 대상은 4,006행(molit_trade 1,975 / molit_villa_trade 1,919 / molit_offi_trade 112). 주석에 D-02 의 메커니즘(= `--resume` 이 3,756건을 skip 해 0건 적재 초록불이 된다)과 소비처 전수 조사 결과(`<interfaces>` 표)를 남긴다. `molit_offi_trade` 112행도 함께 지운다 — 오피스텔 **재수집은 이 Phase 범위 밖**이지만 기록만 남고 데이터가 없는 상태를 남겨두지 않는다.
- `source_id='rankings'` 행은 `sgg_code` 가 NULL 이라 `like '26%'` 에 걸리지 않는다. 주석에 명시할 것.

**(3) `public.db_size_bytes()` RPC 신설** — Task 2 의 `busan-status.ts` 와 이후 6개 plan 의 용량 체크포인트가 쓴다. PostgREST 로는 `pg_database_size` 를 부를 방법이 없고, `psql` 직결 경로는 `BACKUP_AGENT_PASSWORD`(GH secret)가 있어야 해서 로컬 실행자가 쓸 수 없다.
- `returns bigint`, `language sql`, `stable`, `security definer`, `set search_path to ''`
- 본문은 `select pg_catalog.pg_database_size(pg_catalog.current_database())` — 🔴 `search_path` 가 빈 상태이므로 **반드시 `pg_catalog.` 로 수식**한다(20260819030000 이 세 번째로 겪은 함정)
- `revoke all on function public.db_size_bytes() from public;` 후 `grant execute on function public.db_size_bytes() to service_role;` — anon/authenticated 에는 주지 않는다

🔴 **절대 넣지 말 것 — 행 수 단언(`raise exception if count <> 16`).** `supabase/seed.sql` 에는 부산 행이 없다(창원 5구 + 김해 6행뿐). 행 수를 단언하면 `supabase db reset` 이 깨진다 — Phase 38 이 "hollow dependency 클래스 A"로 분류해 고친 것과 정확히 같은 실수다. 이 마이그레이션은 **조건 없는 멱등 UPDATE/DELETE** 여야 하며, 16행 검증은 Task 3(프로덕션 쿼리)에서 한다.

작성 후 `npm run db:push` 로 적용한다(`supabase db push`). `CREATE INDEX CONCURRENTLY` 가 없으므로 정상 경로로 적용된다 — `.applied-manually` 우회나 `migration repair` 가 필요 없다.
  </action>
  <acceptance_criteria>
    - `test -f supabase/migrations/20260819080000_reactivate_busan_scope.sql` 이 성공
    - `grep -c "is_active = true" supabase/migrations/20260819080000_reactivate_busan_scope.sql` >= 1
    - `grep -c "delete from public.ingest_runs" supabase/migrations/20260819080000_reactivate_busan_scope.sql` >= 1 (대소문자 무관 매칭 허용)
    - `grep -c "pg_catalog.pg_database_size" supabase/migrations/20260819080000_reactivate_busan_scope.sql` >= 1
    - `grep -c "raise exception" supabase/migrations/20260819080000_reactivate_busan_scope.sql` == 0 (주석 제외 후 판정: `grep -v '^--' | grep -ci 'raise exception'` == 0)
    - `npx supabase migration list --linked` 출력에서 `20260819080000` 이 Local·Remote 양쪽에 나타나고 한쪽만 있는 엔트리(drift)가 0건
  </acceptance_criteria>
  <verify>
    <automated>npx supabase migration list --linked</automated>
  </verify>
  <done>마이그레이션이 원장에 등록된 상태로 적용되고 drift 0 이다.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/busan-status.ts — 부산 복원 실측 단일 진입점 + 시딩 게이트</name>
  <files>scripts/busan-status.ts</files>
  <read_first>
    - scripts/check-ingest-linkage.ts (인자 파싱 `arg()` 패턴, 임계 미달 시 exit 1 관례, MIN_SAMPLE 가드)
    - src/app/admin/region-expansion/page.tsx (`complexes!inner(sgg_code)` 임베디드 필터로 자식 테이블을 지역별로 세는 패턴, NEW_CODES 상수)
    - scripts/geocode-complexes.ts (프로덕션 URL 오연결 경고 패턴 — 로컬 Supabase 를 가리킬 때 경고)
  </read_first>
  <action>
`scripts/busan-status.ts` 를 신규 작성한다. 이후 6개 plan 의 `acceptance_criteria` 가 전부 이 스크립트 출력을 근거로 삼으므로, **각 실행자가 쿼리를 재발명하지 않도록** 측정을 한곳에 모은다.

환경: `dotenv` 로 `.env.local` 로드 후 `createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY)`. `geocode-complexes.ts` 처럼 URL 에 `127.0.0.1`/`localhost` 가 들어 있으면 경고를 찍는다.

측정 항목(전부 `sgg_code like '26%'` 또는 `complexes!inner(sgg_code)` 임베디드 필터 기준):
- `regions`: 부산 활성 수(기대 16), 전체 활성 수(기대 38)
- `complexes`: 부산 총 행 수 / `lat is not null` 행 수 / 좌표 커버리지 % / **16개 구별 breakdown**
- `ingest_runs`: 부산 행 수를 `source_id`(`molit_trade`·`molit_villa_trade`·`molit_offi_trade`) × `status` 로 교차 집계, `rows_upserted` 합계
- `transactions`: 부산 총 행 수(🔴 CLAUDE.md 규약대로 `cancel_date is null` + `superseded_by is null`), `complex_id is null` 행 수, 연결률 %, `deal_date` 최소·최대
- 파생값 — 🔴 **`complex_price_stats` 라는 테이블은 존재하지 않는다.** 가격 파생값은 `complexes` 의 4개 컬럼(`avg_sale_per_pyeong`·`price_change_30d`·`tx_count_30d`·`is_new_record_30d`)이다(`20260806000000_refresh_complex_price_stats_setbased.sql`). 따라서:
  - `complexes` 부산 행 중 `avg_sale_per_pyeong is not null` 카운트
  - `complex_rankings` / `complex_gap_stats` / `complex_price_predictions` 각각 부산 행 수 (`complexes!inner(sgg_code)` + `like '26%'` 가 임베디드 필터에서 동작하지 않으면 `in('complexes.sgg_code', BUSAN_SGG_CODES)` 로 16개 코드를 명시 열거한다 — 후자가 admin 페이지의 검증된 패턴이다)
- `db_size_bytes`: `supabase.rpc('db_size_bytes')` → MB 로 환산해 8,192MB(Pro 8GB) 대비 % 를 함께 찍는다. RPC 가 없으면(마이그레이션 미적용) 경고만 찍고 나머지 측정을 계속한다

플래그:
- 기본: 사람이 읽는 표 형태 출력, 항상 exit 0
- `--json`: 위 전 항목을 단일 JSON 객체로 출력(다운스트림 plan 이 파싱)
- `--assert-seed-gate`: 🔴 **백필 진입 게이트.** 아래 3조건을 모두 만족해야 exit 0, 하나라도 미달이면 실패 항목을 열거하고 exit 1
  - 부산 `complexes` >= **1400** (근거는 Task 3 주석 참조 — Phase 34 의 K-apt 실측 시딩량이 1,463 이었다)
  - 좌표 커버리지 >= **95%**
  - 16개 구 전부 `complexes` >= 1행 (한 구라도 0 이면 그 구의 거래가 전량 미연결로 쌓인다)
- `--assert-runs-purged`: 부산 `ingest_runs` 총 행 수가 0 이 아니면 exit 1 (백필 직전 D-02 재확인용)

기존 관례를 따를 것: 임계 미달 시 stderr 에 **무엇을 의심해야 하는지**를 적고 exit 1 한다(`check-ingest-linkage.ts` 의 마지막 블록이 기준).
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/busan-status.ts` 가 exit 0 으로 끝나고 출력에 `regions`·`complexes`·`ingest_runs`·`transactions`·`db_size` 다섯 섹션이 모두 나타난다
    - `npx tsx scripts/busan-status.ts --json | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8'));process.exit(j.regions_busan_active===16?0:1)"` 가 exit 0 (키 이름은 구현에 맞추되 JSON 이 파싱 가능하고 부산 활성 수가 16 이어야 한다)
    - Task 1 적용 직후 시점에 `npx tsx scripts/busan-status.ts --assert-seed-gate` 는 **exit 1** 이다 (부산 complexes 0행 — 게이트가 실제로 막는다는 양성 신호)
    - `npx tsx scripts/busan-status.ts --assert-runs-purged` 가 exit 0
    - `npm run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --assert-runs-purged && npm run lint</automated>
  </verify>
  <done>한 명령으로 부산 복원 상태 전체를 실측할 수 있고, 시딩 게이트가 미달 상태에서 exit 1 로 막는다.</done>
</task>

<task type="auto">
  <name>Task 3: 실행 전 기준값 실측 기록 + D-08 감시 스코프 프로덕션 대조</name>
  <files>없음 (측정 전용 — SUMMARY.md 에 기록)</files>
  <read_first>
    - .github/workflows/complex-integrity.yml (`complex_integrity_counts(ARRAY[...])` 호출부 + `BASE_MULTI_JIBUN`/`BASE_TURNOVER`/`BASE_EMPTY_KAPT` 기준선)
    - .planning/phases/41-busan-recollect/41-CONTEXT.md (`<baseline>` 절 — 오케스트레이터 실측값)
    - .planning/phases/34-db-2/34-03-SUMMARY.md (K-apt 시딩 실측량 1,463)
    - .planning/phases/34-db-2/34-05-SUMMARY.md (지오코딩 실측 커버리지 1,462/1,463 = 99.9%)
  </read_first>
  <action>
**(1) 기준값 실측** — `npx tsx scripts/busan-status.ts --json` 을 실행해 출력 전체를 SUMMARY.md 에 "Task 1 적용 직후 기준값"으로 붙인다. 41-CONTEXT `<baseline>` 의 값(운영권역 거래 300,811 / 연결률 94.6% / 경남 기타 64.7% / 부산 0 / DB 448MB)과 대조해 **차이가 있으면 차이를 그대로 기록**한다(오케스트레이터 측정 시점은 2026-08-19 이고 그 사이 일배치가 돌았다).

**(2) 🔴 D-08 감시 스코프 프로덕션 대조** — `complex_integrity_counts(text[])` 는 **저장소에 `CREATE FUNCTION` 이 없다**(발견 5, 프로덕션 전용 = 마이그레이션 drift). 배열 인자가 실제로 필터로 쓰이는지 정적으로 알 수 없으므로 **호출 결과로 판정한다.** 부산 데이터가 아직 0행인 지금이 이 판정에 가장 좋은 시점이다:
- `supabase.rpc('complex_integrity_counts', { ... })` 를 두 번 호출한다 — ① `complex-integrity.yml` 이 넘기는 운영권역 6코드 배열 ② 부산 단일 코드 배열(`26110` 하나) 또는 빈 배열
- 인자 이름은 프로덕션 함수 시그니처를 그대로 써야 한다. 이름을 모르면 `select pg_get_function_arguments(oid) from pg_proc where proname='complex_integrity_counts'` 에 해당하는 조회를 PostgREST 로 할 수 없으므로, `rpc` 호출을 후보 이름(`p_sgg_codes`·`sgg_codes`·`codes`)으로 순차 시도하고 성공한 이름을 기록한다
- **판정**: ① 과 ② 의 세 수치(`multi_jibun`·`turnover_anomaly`·`empty_kapt`)가 **다르면** 배열이 필터로 쓰이는 것 → 부산 유입이 `complex-integrity` 기준선에 영향을 주지 않는다. **같으면** 배열이 무시되는 것(= 전역 집계) → 부산 유입이 `BASE_EMPTY_KAPT=59` 를 즉시 깨뜨릴 수 있다
- 두 경우 모두 결과를 SUMMARY.md 에 실측값과 함께 기록한다. 🔴 **D-08 에 따라 기준선을 지금 바꾸지 않는다** — 예방적 하향은 감시를 무력화한다. 상향 판단은 41-09 가 부산 데이터가 실제로 들어온 뒤에 한다
- RPC 를 service_role 로도 호출할 수 없으면(grant 부재) "PostgREST 경로로 확인 불가 — 41-09 에서 `complex-integrity.yml` 실제 dispatch 로 판정"이라고 기록하고 넘어간다. 이 경우 `complex_integrity_counts` 의 마이그레이션 파일화는 이 Phase 범위 밖(Phase 37 계열)임을 함께 남긴다

**(3) 시딩 목표치 근거 기록** — 🔴 ROADMAP Success Criteria 2 와 41-CONTEXT Success Criteria 3 은 부산 `complexes` **≥ 1,500** 을 요구하지만, Phase 34 의 K-apt 실측 시딩량은 **1,463** 이었고 2026-08-10 삭제분은 **1,594** 였다(차이 131 = 시딩 이후 한 달간 다른 경로로 늘어난 행). 즉 **K-apt 시딩만으로는 1,500 에 닿지 않을 가능성이 높다.** 이 사실과 그래서 차단 게이트를 1,400 으로 둔 근거를 SUMMARY.md 에 명시하고, 41-03·41-04 가 실측값을 1,463·1,594 양쪽과 대조 보고하도록 인계 사항으로 남긴다.
  </action>
  <acceptance_criteria>
    - SUMMARY.md 에 `busan-status.ts --json` 원본 출력이 "기준값"으로 포함되고, 부산 `complexes`=0 / 부산 `ingest_runs`=0 / 부산 활성 16 / 전체 활성 38 이 확인된다
    - SUMMARY.md 에 `complex_integrity_counts` 두 인자 호출의 세 수치가 나란히 기록되고, "배열이 필터로 쓰인다 / 쓰이지 않는다 / PostgREST 로 확인 불가" 중 하나로 명시 판정된다
    - `git diff --stat .github/workflows/complex-integrity.yml` 이 공백 (기준선을 이 plan 에서 바꾸지 않았다는 양성 신호)
    - SUMMARY.md 에 1,463(K-apt 실측)·1,594(삭제분)·1,500(ROADMAP 요구치)·1,400(차단 게이트) 네 숫자와 그 관계가 기록된다
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/busan-status.ts --json > /dev/null && git diff --stat --exit-code .github/workflows/complex-integrity.yml</automated>
  </verify>
  <done>기준값이 실측 기록되고, 감시 스코프 판정과 시딩 목표치 근거가 남는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 실행자 → 프로덕션 Supabase (service_role) | `.env.local` 의 service_role 키로 UPDATE/DELETE 를 수행한다. RLS 를 우회하는 최고 권한 |
| `db_size_bytes()` RPC → PostgREST 호출자 | SECURITY DEFINER 함수를 어떤 롤이 부를 수 있는지가 곧 노출 범위다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering (과삭제) | `delete from ingest_runs where sgg_code like '26%'` | mitigate | `26` 은 부산 전용 법정동코드이고 16행 전수 확인됨. `source_id='rankings'` 행은 `sgg_code` NULL 로 매칭되지 않음. `transactions.source_run_id` 는 `on delete set null` 이고 부산 거래가 0행이라 참조 행 자체가 없다 |
| T-41-02 | Information Disclosure | `public.db_size_bytes()` | mitigate | `revoke all from public` + `grant execute to service_role` 만. anon/authenticated 는 호출 불가 |
| T-41-03 | Elevation of Privilege | SECURITY DEFINER + `search_path` | mitigate | `set search_path to ''` + 본문 전체를 `pg_catalog.` 로 수식 (20260819030000 의 세 번째 재발 방지) |
| T-41-04 | Denial of Service (재현성 파괴) | 마이그레이션에 행 수 단언 삽입 | mitigate | `seed.sql` 에 부산 행이 없으므로 단언 금지를 명문화. 검증은 Task 3 의 프로덕션 쿼리로 분리 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 (`dotenv`·`@supabase/supabase-js` 는 기존 의존성) |
</threat_model>

<verification>
```bash
npx supabase migration list --linked
npx tsx scripts/busan-status.ts
npx tsx scripts/busan-status.ts --assert-runs-purged
npm run lint
```
</verification>

<success_criteria>
1. `regions` 부산 16행 `is_active=true`, `migration list --linked` drift 0 (BUSAN-01)
2. 부산 `ingest_runs` 0행 — `--resume` 이 skip 할 대상이 없다 (BUSAN-02)
3. `scripts/busan-status.ts` 가 5개 섹션 + `--json` + `--assert-seed-gate` + `--assert-runs-purged` 를 제공하고, 현재 상태에서 seed-gate 가 exit 1 로 막는다
4. `complex_integrity_counts` 배열 인자의 실제 스코프가 프로덕션 대조로 판정·기록됨 (D-08)
5. `.github/workflows/complex-integrity.yml` 무변경 (기준선 예방적 하향 금지)
6. `npm run lint` exit 0 / 테스트 실패 이름 집합 불변
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-01-SUMMARY.md` 를 생성한다. 기준값 JSON 원본, `complex_integrity_counts` 스코프 판정, 시딩 목표치 4숫자 근거를 반드시 포함할 것.
</output>
