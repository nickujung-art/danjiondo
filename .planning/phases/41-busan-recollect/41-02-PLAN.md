---
phase: 41-busan-recollect
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/backfill-args.ts
  - src/lib/data/backfill-args.test.ts
  - scripts/backfill-realprice.ts
  - .github/workflows/molit-backfill-once.yml
autonomous: true
requirements:
  - BUSAN-03
must_haves:
  truths:
    - "molit-backfill-once.yml 을 dispatch 할 때 resume·from·to·min_linked_pct 를 입력으로 지정할 수 있다"
    - "입력을 비워두면 워크플로가 현재와 동일하게 동작한다 (--resume 유지, 기간 기본값 10년, 연결률 임계 80)"
    - "--from/--to/--sgg 가 비었거나 형식이 틀리면 백필이 0건 적재 후 exit 0 하지 않고 즉시 exit 1 한다"
  artifacts:
    - path: "src/lib/data/backfill-args.ts"
      provides: "백필 인자 검증·기간 전개 순수 함수 (assertYearMonth / monthRange / parseSggCodes)"
      exports: ["assertYearMonth", "monthRange", "parseSggCodes"]
    - path: "src/lib/data/backfill-args.test.ts"
      provides: "빈 --from 이 조용히 0개월로 전개되던 경로를 잡는 유닛 테스트"
    - path: ".github/workflows/molit-backfill-once.yml"
      provides: "resume / from / to / min_linked_pct workflow_dispatch 입력"
      contains: "min_linked_pct"
  key_links:
    - from: ".github/workflows/molit-backfill-once.yml"
      to: "scripts/backfill-realprice.ts"
      via: "조건부로 조립한 인자 배열 (--resume / --from= / --to= / --sgg=)"
      pattern: "--from="
    - from: ".github/workflows/molit-backfill-once.yml"
      to: "scripts/check-ingest-linkage.ts"
      via: "min_linked_pct 입력이 있으면 --min-linked-pct= 전달"
      pattern: "min-linked-pct"
    - from: "scripts/backfill-realprice.ts"
      to: "src/lib/data/backfill-args.ts"
      via: "preflight() 앞에서 인자 검증 호출"
      pattern: "assertYearMonth"
---

<objective>
`molit-backfill-once.yml` 은 지금 `sgg_codes` 하나만 받고 `--resume` 를 하드코딩한다. D-04(기간 `201501~`)와 D-05(3그룹 분할 dispatch)를 실행하려면 `from`/`to` 가 필요하고, D-07(부산 연결률 임계 65%)을 실행하려면 연결률 임계도 입력이어야 한다 — 현재 워크플로는 `check-ingest-linkage.ts` 를 임계 인자 없이 부르므로 기본값 **80%** 가 적용되고, 부산 예상 연결률(≈65%)에서는 **적재가 성공해도 매번 빨간불**이 된다.

그리고 인자를 노출하면 새 함정이 열린다: `--from=` 을 빈 값으로 넘기면 `fromArg` 가 `''` 가 되어 `?? defaultFrom` 을 통과하고(`''` 는 nullish 가 아니다), `monthRange('', '')` 가 `NaN` 비교로 **빈 배열**을 돌려주고, `total=0` → `done=0` → 실패율 0 → **`✅ 완료: 0건 upsert` 로 exit 0 한다.** 이 저장소가 반복해 겪은 조용한 성공의 정확한 재판이다(ADR-063). 입력을 노출하기 전에 그 경로를 막는다.

Purpose: 3그룹 분할 dispatch 를 실행 가능하게 만들고, 그 과정에서 열리는 조용한 성공 경로를 함께 닫는다.
Output: 검증 모듈 + 유닛 테스트, 스크립트 검증 배선, 워크플로 입력 4개.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\scripts\backfill-realprice.ts
@C:\Users\jung\coding\bds\.github\workflows\molit-backfill-once.yml
@C:\Users\jung\coding\bds\scripts\check-ingest-linkage.ts
@C:\Users\jung\coding\bds\.github\workflows\molit-daily.yml

<interfaces>
<!-- scripts/backfill-realprice.ts 현재 인자 파싱 (이동/재사용 대상) -->
const useResume = args.includes('--resume')
const fromArg   = args.find(a => a.startsWith('--from='))?.split('=')[1]
const toArg     = args.find(a => a.startsWith('--to='))?.split('=')[1]
const sggArg    = args.find(a => a.startsWith('--sgg='))?.split('=')[1]
function monthRange(from: string, to: string): string[]   // ← 이 함수가 NaN 입력에 빈 배열을 돌려준다

<!-- scripts/check-ingest-linkage.ts 기본값 -->
DEFAULT_SGG_CODES = ['48121','48123','48125','48127','48129','48250']   // 창원 5구 + 김해
DEFAULT_MIN_LINKED_PCT = 80
MIN_SAMPLE = 50
--sgg=all 이면 전국. --min-linked-pct 로 임계 재지정 가능

<!-- molit-daily.yml 의 연결률 검사는 인자 없이 부른다 (기본 창원·김해 6코드 / 임계 80) — 이 plan 에서 바꾸지 않는다 -->
npx tsx scripts/check-ingest-linkage.ts --since-hours=6
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/data/backfill-args.ts — 인자 검증·기간 전개 순수 함수 + 유닛 테스트</name>
  <files>src/lib/data/backfill-args.ts, src/lib/data/backfill-args.test.ts</files>
  <read_first>
    - scripts/backfill-realprice.ts (현재 `monthRange`·인자 파싱 구현. 계산 정의를 바꾸지 말고 그대로 옮긴다)
    - src/lib/data/regions.ts (`server-only` 를 넣지 않는 이유가 최상단 주석에 있다 — 스크립트가 tsx 로 직접 import 하는 모듈은 이 규약을 따른다)
    - src/lib/data/complex-matching.test.ts (이 저장소의 vitest 유닛 테스트 형식)
  </read_first>
  <behavior>
    - monthRange('201501','201503') === ['201501','201502','201503'] (기존 동작 보존)
    - monthRange('202512','202602') === ['202512','202601','202602'] (연도 넘김 보존)
    - assertYearMonth('from','') → throw (빈 문자열이 0개월 전개로 흐르던 경로)
    - assertYearMonth('from','2015') → throw (6자리 아님)
    - assertYearMonth('from','201513') → throw (월 13)
    - assertYearMonth('from','201500') → throw (월 00)
    - assertYearMonth('from','201501') → 통과, '201501' 반환
    - assertYearMonth 는 인자 이름을 에러 메시지에 포함한다 (어느 인자가 틀렸는지 로그로 판별 가능)
    - parseSggCodes('26230, 26260') === ['26230','26260'] (공백 trim)
    - parseSggCodes('') → throw (빈 --sgg= 가 regions 전체 38개 지역으로 조용히 확장되는 것을 막는다)
    - parseSggCodes('26230,abc') → throw (5자리 숫자가 아닌 코드)
  </behavior>
  <action>
`src/lib/data/backfill-args.ts` 를 신규 작성한다. `import 'server-only'` 를 **넣지 않는다** — `scripts/backfill-realprice.ts` 가 `tsx` 로 직접 import 하므로 `regions.ts` 최상단 주석과 같은 이유로 금지다. 그 이유를 파일 최상단 주석에 남긴다.

export 3개:
- `monthRange(from: string, to: string): string[]` — `scripts/backfill-realprice.ts` 의 현재 구현을 **계산 정의 변경 없이** 그대로 옮긴다
- `assertYearMonth(label: string, value: string | undefined): string` — `undefined` 는 "인자 미지정"이라 그대로 통과시켜 호출부가 기본값을 쓰게 하고(`undefined` 반환 대신 오버로드/조건 처리는 구현 재량), **문자열이면** `/^\d{6}$/` + 월 01~12 를 강제한다. 위반 시 `label` 을 포함한 메시지로 `throw`
- `parseSggCodes(raw: string | undefined): string[] | undefined` — `undefined` 는 그대로 통과(호출부가 `regions` 조회로 폴백), 문자열이면 콤마 분리 + trim 후 **빈 배열이면 throw**, 각 코드가 `/^\d{5}$/` 가 아니면 throw

핵심은 **`''`(빈 문자열)와 `undefined` 를 구분**하는 것이다. 현재 코드의 버그는 `--from=` 이 `''` 를 만들고 `?? ` 가 그것을 통과시키는 데 있다.

`src/lib/data/backfill-args.test.ts` 를 신규 작성해 `<behavior>` 전 케이스를 검증한다. 🔴 특히 `assertYearMonth('from','')` 가 throw 하는 케이스는 **이 plan 의 존재 이유**이므로 테스트 이름에 "빈 값이 0개월로 조용히 전개되던 것" 이라는 취지를 남긴다.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/backfill-args.test.ts` 통과, 테스트 케이스 수 >= 10
    - `grep -c "server-only" src/lib/data/backfill-args.ts` == 0
    - `grep -c "export function monthRange\|export function assertYearMonth\|export function parseSggCodes" src/lib/data/backfill-args.ts` == 3
    - `npm run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/data/backfill-args.test.ts</automated>
  </verify>
  <done>빈/형식 위반 인자가 throw 되고 기존 기간 전개 동작이 테스트로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 2: backfill-realprice.ts 에 인자 검증 배선 — preflight 앞에서 exit 1</name>
  <files>scripts/backfill-realprice.ts</files>
  <read_first>
    - scripts/backfill-realprice.ts (`monthRange` 정의, `main()` 의 `preflight()` 호출 위치, `MOLIT_API_KEY` 검증 블록, 상단 사용법 주석)
    - src/lib/data/backfill-args.ts (Task 1 산출물 — 시그니처를 그대로 쓴다)
  </read_first>
  <action>
`scripts/backfill-realprice.ts` 를 수정한다.

**(1) 로컬 `monthRange` 정의를 제거**하고 `../src/lib/data/backfill-args` 에서 `monthRange`·`assertYearMonth`·`parseSggCodes` 를 import 한다. 다른 import(`ingestMonth`·`describeError` 등)와 같은 상대경로 형식을 따른다.

**(2) 검증을 `main()` 의 최상단 — `preflight()` **앞**에 배치한다.** 순서가 중요하다: 인자가 틀렸는데 preflight 로 MOLIT API 를 찔러 20초를 태우거나, 최악의 경우 그대로 진행해 0건 적재로 끝나는 것을 막아야 한다. `MOLIT_API_KEY` 검증 블록 바로 뒤가 적절하다.
- `assertYearMonth('--from', fromArg)` / `assertYearMonth('--to', toArg)` / `parseSggCodes(sggArg)` 를 호출하고, throw 를 잡아 `console.error` 로 메시지를 찍은 뒤 `process.exit(1)` 한다
- `getSggCodes()` 는 `parseSggCodes` 의 결과를 쓰도록 바꾼다 (기존 `sggArg.split(',')` 인라인 파싱 제거)

**(3) 기간 전개 결과가 비었으면 실패시킨다.** `months = monthRange(from, to)` 직후 `months.length === 0` 이면 `from`·`to` 실제 값을 포함한 메시지로 `console.error` 후 `process.exit(1)`. 🔴 이것이 마지막 방어선이다 — `from > to` 처럼 개별 인자는 형식이 맞는데 범위가 역전된 경우를 잡는다(예: `--from=202608 --to=201501`).

**(4) 상단 사용법 주석 갱신** — `--from`/`--to`/`--sgg` 가 빈 값이나 잘못된 형식이면 즉시 exit 1 한다는 것과, 그 방어가 왜 있는지(빈 `--from=` → 0개월 → `✅ 0건 upsert` → exit 0 이라는 조용한 성공)를 남긴다.

⚠ `FAILURE_ABORT_RATE`·`preflight()`·`EXIT_BLOCKED_RUNNER`·`cleanupStuckRuns()`·`getCompletedRuns()`/`getCompletedVillaRuns()` 의 동작은 **일절 바꾸지 않는다.** 이 plan 은 인자 경계만 손댄다.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/backfill-realprice.ts --from= --sgg=26110` 가 **exit 1** 로 끝나고 stderr 에 `--from` 이 언급된다 (MOLIT API 호출 없이 즉시 종료 — 실행 시간 < 10초)
    - `npx tsx scripts/backfill-realprice.ts --from=202608 --to=201501 --sgg=26110` 가 **exit 1** 로 끝나고 stderr 에 기간이 비었다는 취지의 메시지가 나온다
    - `npx tsx scripts/backfill-realprice.ts --sgg=` 가 **exit 1** 로 끝난다 (regions 전체 38지역 확장 차단)
    - `grep -c "function monthRange" scripts/backfill-realprice.ts` == 0 (모듈로 이동 완료)
    - `grep -c "assertYearMonth" scripts/backfill-realprice.ts` >= 1
    - `grep -c "FAILURE_ABORT_RATE = 0.3" scripts/backfill-realprice.ts` == 1 (실패 임계 무변경)
    - `npm run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/backfill-realprice.ts --from= --sgg=26110; test $? -eq 1</automated>
  </verify>
  <done>형식 위반·범위 역전 인자가 API 호출 전에 exit 1 로 잡힌다.</done>
</task>

<task type="auto">
  <name>Task 3: molit-backfill-once.yml 에 resume·from·to·min_linked_pct 입력 노출</name>
  <files>.github/workflows/molit-backfill-once.yml</files>
  <read_first>
    - .github/workflows/molit-backfill-once.yml (현재 입력 1개 + `--resume` 하드코딩 + 연결률 검사 단계 + `timeout-minutes: 300`)
    - .github/workflows/molit-daily.yml (`prepare` job 이 빈 입력을 shell 에서 처리하는 기존 패턴 — `if [ -z "$FROM" ]`)
    - scripts/check-ingest-linkage.ts (`--min-linked-pct` 인자명과 기본값 80)
  </read_first>
  <action>
`.github/workflows/molit-backfill-once.yml` 의 `workflow_dispatch.inputs` 에 3+1개를 추가한다. 🔴 **기본값은 현재 동작을 그대로 유지해야 한다** — 이 워크플로의 기존 용도(창원·김해 재개)가 깨지면 안 된다(D-06).

추가할 입력:
- `resume` — `type: boolean`, `default: true`, description 에 "완료된 (지역,월) 을 skip 한다. 부산 최초 재수집처럼 ingest_runs 를 비운 직후에는 켜둬도 무해하다" 취지를 적는다
- `from` — `type: string`, `required: false`, `default: ''`, description 에 "YYYYMM. 미입력 시 스크립트 기본값(현재로부터 10년)" + "부산 재수집은 **201501**(창원·김해와 대칭, D-04)" 을 명시
- `to` — `type: string`, `required: false`, `default: ''`, description 에 "YYYYMM. 미입력 시 당월"
- `min_linked_pct` — `type: string`, `required: false`, `default: ''`, description 에 "연결률 검사 임계. 미입력 시 스크립트 기본값 80. **부산은 65**(경남 기타 실측 64.7%, D-07)" 를 명시

`Run MOLIT backfill` 스텝의 `run:` 을 조건부 인자 조립으로 바꾼다:
- 입력을 셸 변수로 받고 `ARGS` 배열(또는 문자열)을 만든다
- `--sgg=` 는 항상 붙인다
- `resume` 이 `'true'` 일 때만 `--resume` 를 붙인다 (`false` 면 붙이지 않는다)
- `from`/`to` 가 **비어 있지 않을 때만** `--from=<값>`/`--to=<값>` 을 붙인다 — 🔴 빈 값을 `--from=` 형태로 넘기면 Task 2 의 검증이 exit 1 로 잡아 dispatch 가 전부 실패한다. `molit-daily.yml` 의 `if [ -z "$FROM" ]` 패턴을 따를 것
- 조립된 최종 명령을 `echo` 로 로그에 남긴다 (무엇이 실행됐는지 run 로그만 보고 판별 가능해야 한다)

`Check linkage rate` 스텝도 같은 방식으로 바꾼다:
- `--since-hours=8` 과 `--sgg=<입력>` 은 유지
- `min_linked_pct` 가 비어 있지 않을 때만 `--min-linked-pct=<값>` 을 붙인다

`sgg_codes` 의 `default: '48121,48123'` 과 `timeout-minutes: 300`, 연결률 검사 스텝 위의 2026-05-26 사고 주석 블록은 **그대로 둔다**. 헤더 주석에 D-05(한 job 5시간 = 약 5~6개 구, 부산 전체는 14.3시간 추정이라 3그룹 분할 dispatch 가 필요하다)를 한 단락으로 추가한다.
  </action>
  <acceptance_criteria>
    - `grep -c "min_linked_pct" .github/workflows/molit-backfill-once.yml` >= 2 (입력 정의 + 사용부)
    - `grep -c "type: boolean" .github/workflows/molit-backfill-once.yml` >= 1
    - `grep -vE '^\s*#' .github/workflows/molit-backfill-once.yml | grep -c '\-\-resume'` >= 1 그리고 그 줄이 조건 블록 안에 있다 (하드코딩 제거)
    - `grep -c "timeout-minutes: 300" .github/workflows/molit-backfill-once.yml` == 1 (무변경)
    - `node -e "const y=require('fs').readFileSync('.github/workflows/molit-backfill-once.yml','utf8'); if(!/inputs:/.test(y)) process.exit(1)"` 및 YAML 파싱 성공 (`npx js-yaml` 또는 `python -c "import yaml,sys;yaml.safe_load(open(...))"` 중 사용 가능한 것)
    - 입력 4개(`resume`·`from`·`to`·`min_linked_pct`)의 description 에 각각 기본값 동작이 문장으로 적혀 있다
  </acceptance_criteria>
  <verify>
    <automated>grep -c "min_linked_pct" .github/workflows/molit-backfill-once.yml && grep -c "timeout-minutes: 300" .github/workflows/molit-backfill-once.yml</automated>
  </verify>
  <done>4개 입력이 노출되고, 빈 입력이 플래그로 새어나가지 않으며, 미입력 시 현재 동작이 보존된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `workflow_dispatch` 입력 → 셸 명령 조립 | repo write 권한자만 트리거하지만 입력 문자열이 셸에 들어간다 |
| 조립된 CLI 인자 → `backfill-realprice.ts` | 신뢰 경계. 형식 위반이 조용한 0건 적재로 이어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | Tampering (명령 주입) | `${{ inputs.* }}` 를 `run:` 에 직접 보간 | mitigate | 입력을 먼저 셸 변수로 받고 `"$VAR"` 로 인용해 쓴다. `from`/`to` 는 Task 2 의 `/^\d{6}$/`, `sgg` 는 `/^\d{5}$/` 검증을 통과해야 스크립트가 진행한다 |
| T-41-06 | Repudiation (조용한 성공) | 빈 `--from=` → 0개월 → exit 0 | mitigate | Task 1 유닛 테스트 + Task 2 의 `months.length===0` exit 1 + Task 3 의 빈 입력 플래그 생략 3중 방어 |
| T-41-07 | Denial of Service (감시 무력화) | 연결률 임계 80 고정으로 부산 그룹이 상시 빨간불 | mitigate | `min_linked_pct` 입력 노출. 기본값 공백으로 기존 용도(창원·김해 임계 80)는 불변 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
npx vitest run src/lib/data/backfill-args.test.ts
npx tsx scripts/backfill-realprice.ts --from= --sgg=26110   # exit 1 이어야 한다
npm run lint
```
</verification>

<success_criteria>
1. `molit-backfill-once.yml` 이 `resume`·`from`·`to`·`min_linked_pct` 4개 입력을 받는다 (BUSAN-03)
2. 4개 입력을 모두 비워두면 조립 명령이 현재와 동일하다 (`--resume --sgg=<코드>` + 연결률 검사 임계 미지정)
3. 빈/형식 위반/범위 역전 기간 인자가 MOLIT API 호출 전에 exit 1 로 잡힌다
4. `src/lib/data/backfill-args.test.ts` 가 10개 이상 케이스로 통과
5. `FAILURE_ABORT_RATE`·`preflight()`·`EXIT_BLOCKED_RUNNER`·resume 조회 로직 무변경
6. `npm run lint` exit 0 / 테스트 실패 이름 집합 불변
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-02-SUMMARY.md` 를 생성한다. 4개 입력의 기본값과 "미입력 시 현재 동작"의 대조표, 그리고 3그룹 dispatch 에 실제로 넣을 입력값 예시(그룹 A 5코드 + `from=201501` + `min_linked_pct=65`)를 인계 사항으로 남길 것.
</output>
</content>
