---
phase: 41-busan-recollect
plan: "09"
type: execute
wave: 8
depends_on: ["41-08"]
files_modified:
  - .github/workflows/complex-integrity.yml
  - scripts/check-ingest-linkage.ts
  - docs/ADR.md
  - docs/FEATURES.json
autonomous: false
requirements:
  - BUSAN-07
  - BUSAN-08
must_haves:
  truths:
    - "complex-integrity·data-freshness 워크플로가 부산 유입 후에도 초록불이거나, 움직인 기준선이 부산 유입을 인지한 값으로 상향 기록된다"
    - "부산 연결률의 실측 기준선이 check-ingest-linkage.ts 에 문서화되어 다음 사람이 재발견하지 않는다"
    - "DB 용량이 complex-integrity 실행마다 로그에 남아 다음 확장 판단의 상시 근거가 된다"
    - "ADR-062 의 되돌리기를 실제로 실행한 결과가 ADR 로 기록되고 FEATURES.json 이 갱신된다"
  artifacts:
    - path: ".github/workflows/complex-integrity.yml"
      provides: "부산 유입을 인지한 BASE_* 기준선 + pg_database_size 상시 로깅"
      contains: "pg_database_size"
    - path: "scripts/check-ingest-linkage.ts"
      provides: "부산 실측 연결률 기준선 문서화 + 기본 범위 유지 근거"
      contains: "26"
    - path: "docs/ADR.md"
      provides: "ADR-064 — 부산 16개 구 수집 재개"
      contains: "ADR-064"
  key_links:
    - from: ".github/workflows/complex-integrity.yml"
      to: "public.complex_integrity_counts(text[])"
      via: "psql 직결 DO 블록에서 호출. 배열 인자의 실제 스코프는 41-01 Task 3 이 실측 판정했다"
      pattern: "complex_integrity_counts"
    - from: "docs/ADR.md (ADR-064)"
      to: "docs/ADR.md (ADR-062)"
      via: "ADR-062 의 '되돌리려면' 절차를 실행한 결과를 역방향 기록으로 연결"
      pattern: "ADR-062"
---

<objective>
부산 데이터가 실제로 들어온 뒤 **감시가 여전히 유효한지 확인하고**, 이 작업의 결정과 실측을 영구 기록으로 남긴다.

🔴 **D-08 의 순서를 지킨다: 미리 바꾸지 않고, 움직인 뒤 상향만.** 예방적 하향은 감시를 무력화한다. `complex-integrity.yml` 의 `BASE_EMPTY_KAPT` 는 바로 직전 커밋(`bf09835`)에서 60 → 59 로 내려 잠근 값이다 — 부산 유입으로 움직였다면 **부산을 인지한 값으로 올리고 그 근거를 주석에 남긴다.** 안 움직였으면 손대지 않는다.

그리고 이 Phase 가 발견한 사실 두 개를 감시 코드에 문서로 심는다: **부산 연결률의 실측 기준선**(다음 사람이 90% 를 요구하지 않도록)과 **DB 용량 상시 로깅**(다음 확장 판단이 Dashboard 를 사람이 들여다보는 것에 의존하지 않도록).

Purpose: 감시 유효성 확인 + 기준선 정직한 갱신 + 영구 기록.
Output: 기준선 갱신(또는 무변경 근거), 연결률 문서화, 용량 상시 로깅, ADR-064, FEATURES.json 갱신.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-01-SUMMARY.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-07-SUMMARY.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-08-SUMMARY.md
@C:\Users\jung\coding\bds\.github\workflows\complex-integrity.yml
@C:\Users\jung\coding\bds\.github\workflows\data-freshness-check.yml
@C:\Users\jung\coding\bds\scripts\check-data-freshness.ts
@C:\Users\jung\coding\bds\scripts\check-ingest-linkage.ts
@C:\Users\jung\coding\bds\docs\ADR.md

<interfaces>
<!-- complex-integrity.yml 현재 기준선 (2026-08-06 측정, empty_kapt 는 bf09835 에서 59 로 하향 잠금) -->
BASE_MULTI_JIBUN: 9
BASE_TURNOVER: 23
BASE_EMPTY_KAPT: 59
호출: public.complex_integrity_counts(ARRAY['48121','48123','48125','48127','48129','48250'])
판정: 절대 수치가 아니라 **기준선 대비 증가**만 RAISE EXCEPTION. 낮아지면 RAISE INFO 로 "내려 잠글 것" 안내
psql: backup_agent 롤 (읽기 전용, BYPASSRLS), ON_ERROR_STOP=1, statement_timeout 없음

<!-- check-ingest-linkage.ts 기본값 (변경하지 않는다 — 문서만 추가) -->
DEFAULT_SGG_CODES = 창원 5구 + 김해 6코드
DEFAULT_MIN_LINKED_PCT = 80
주석 근거(2026-08-06 실측): 운영권역 88.7% / 기타 지역 50.5% — 전국 한 임계로 재면 상시 빨간불

<!-- molit-daily.yml 의 연결률 검사 (이 plan 에서 바꾸지 않는다) -->
npx tsx scripts/check-ingest-linkage.ts --since-hours=6      ← 인자 없음 = 기본 범위(창원·김해) / 임계 80

<!-- ADR 번호 -->
마지막 = ADR-063 (ad_campaigns DELETE 정책 누락) → 신규 = ADR-064
헤더 형식: ### ADR-064 — <한 줄 요지>

<!-- FEATURES.json 구조 -->
top keys: _schema, _usage, lastUpdated, summary, features
summary: { done, in_progress, todo, deferred, blocked }   ← 카운트 재계산 대상
features[]: { id, category, name, status, priority, notes, checklist[{ layer, name, status }] }
현재 features 33개 / summary done=28, deferred=5, blocked=2 / "부산" 문자열 0건
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 감시 실행·기준선 판정 + DB 용량 상시 로깅 추가</name>
  <files>.github/workflows/complex-integrity.yml</files>
  <read_first>
    - .github/workflows/complex-integrity.yml (`env:` 기준선 3개, psql DO 블록의 `RAISE INFO`/`RAISE EXCEPTION` 구조, 헤더 주석의 "기준선 대비 증가만 실패" 원칙)
    - .planning/phases/41-busan-recollect/41-01-SUMMARY.md (🔴 `complex_integrity_counts` 배열 인자가 실제로 필터인지 판정한 결과 — 이 태스크의 해석 전제)
    - scripts/check-data-freshness.ts (`CHECKS` 배열의 `embeddedFilter`·`pausedReason` — 부산 유입이 왜 신선도에 무해한지)
    - .github/workflows/data-freshness-check.yml
  </read_first>
  <action>
**(1) 두 워크플로를 실제로 dispatch 해 결과를 본다.**
- `gh workflow run complex-integrity.yml` → `gh run watch` → 로그의 `RAISE INFO` 3줄(`다중 지번` / `회전율 이상` / `K-apt 등록인데 거래 0건`) 실측값을 기준선(9 / 23 / 59)과 나란히 기록한다
- `gh workflow run data-freshness-check.yml` → 결과 기록. 부산 적재는 신선도를 **높이는** 방향이라 초록불이 기대값이다. 빨간불이면 어느 `CHECKS` 항목인지와 사유를 적는다

**(2) 기준선 판정** — 41-01 Task 3 의 스코프 판정과 이번 실측을 함께 읽는다.
- **실측값 <= 기준선** → 🔴 **아무것도 바꾸지 않는다.** "부산 유입 후에도 기준선 이내"를 SUMMARY.md 에 실측값과 함께 기록한다. 41-01 이 "배열이 필터로 쓰인다"고 판정했다면 이것이 예상 결과다
- **실측값 > 기준선** → 워크플로의 `BASE_*` 를 **실측값으로 올린다.** 그리고 `env:` 블록 위 주석에 🔴 **왜 올렸는지**를 남긴다: "2026-08-19 부산 16개 구 수집 재개(Phase 41 / ADR-064)로 단지 약 1,46x곳이 유입됐다. 기준선 N → M 은 부산 유입을 인지한 값이며, 부산 오염을 줄이면 다시 내려 잠근다." 🔴 **여유를 얹어 올리지 않는다** — 실측값 그대로 잠근다. 여유를 주면 그만큼 감시가 눈을 감는다
- **실측값 < 기준선** (부산 유입에도 낮아진 경우) → 워크플로의 `RAISE INFO` 안내대로 **내려 잠근다**

**(3) DB 용량 상시 로깅 추가** — psql DO 블록에 `RAISE INFO` 한 줄을 더한다: `pg_size_pretty(pg_database_size(current_database()))` 와 Pro 한도 8,192MB 대비 % 를 찍는다. 이 잡은 이미 `backup_agent` 로 psql 직결하고 있어 추가 비용이 사실상 0 이고, 매일 06:00 KST 로그에 용량이 남으면 **다음 지역 확장 판단이 Dashboard 를 사람이 들여다보는 것에 의존하지 않게 된다** — 2026-08-10 삭제의 원인이 용량이었으므로 이 관측이 없는 상태를 유지할 이유가 없다.
- 🔴 **실패 조건으로 만들지 않는다.** `RAISE INFO` 만 추가하고 `RAISE EXCEPTION` 조건이나 `worse` 배열에는 넣지 않는다. 용량 임계를 판정하는 것은 이 Phase 범위 밖이다
- `pg_database_size`/`pg_size_pretty` 는 `pg_catalog` 함수이고 이 블록은 `search_path` 를 비우지 않으므로 수식이 필수는 아니지만, 명시적으로 수식해 두면 안전하다
  </action>
  <acceptance_criteria>
    - `complex-integrity.yml` run 의 `RAISE INFO` 3줄 실측값이 기준선 9/23/59 와 나란히 SUMMARY.md 에 기록됨
    - `data-freshness-check.yml` run 의 상태가 기록되고, 빨간불이면 해당 `CHECKS` 항목과 사유가 적혀 있다
    - 기준선을 바꿨다면 `git diff .github/workflows/complex-integrity.yml` 에 `BASE_*` 변경과 **그 이유를 적은 주석**이 함께 나타난다. 바꾸지 않았다면 `BASE_MULTI_JIBUN: 9`·`BASE_TURNOVER: 23`·`BASE_EMPTY_KAPT: 59` 세 줄이 그대로다
    - `grep -c "pg_database_size" .github/workflows/complex-integrity.yml` >= 1
    - 용량 줄이 `RAISE INFO` 이고 `worse`/`RAISE EXCEPTION` 조건에 들어가지 않았음이 diff 로 확인된다
    - 수정 후 `complex-integrity.yml` 을 **한 번 더 dispatch** 해 초록불 + 로그에 용량 줄이 나타남을 확인 (기준선을 올렸다면 그 값으로 통과하는지 실증)
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=complex-integrity.yml --limit=3 && grep -c "pg_database_size" .github/workflows/complex-integrity.yml</automated>
  </verify>
  <done>감시 2종의 결과가 실측되고, 기준선이 정직하게 유지/상향/하향되며, 용량이 상시 로깅된다.</done>
</task>

<task type="auto">
  <name>Task 2: 부산 연결률 실측 기준선을 check-ingest-linkage.ts 에 문서화</name>
  <files>scripts/check-ingest-linkage.ts</files>
  <read_first>
    - scripts/check-ingest-linkage.ts (`DEFAULT_SGG_CODES` 위 주석 — 왜 범위를 좁혔는지 설명하는 기존 문체와 형식)
    - .planning/phases/41-busan-recollect/41-07-SUMMARY.md, 41-08-SUMMARY.md (부산 전체 연결률 실측값)
    - .github/workflows/molit-daily.yml (`연결률 검사` 스텝 — 인자 없이 호출한다)
  </read_first>
  <action>
`scripts/check-ingest-linkage.ts` 의 `DEFAULT_SGG_CODES`/`DEFAULT_MIN_LINKED_PCT` 주석 블록을 **확장**한다. 🔴 **기본값 자체는 바꾸지 않는다** — 창원·김해 6코드 / 임계 80 을 유지한다.

주석에 추가할 사실 3개(전부 이 Phase 실측):
- 부산 16개 구를 2026-08-19 에 재개했고(Phase 41 / ADR-064) **부산 실측 연결률은 <41-08 실측값>%** 이다. 대조군: 운영권역 94.6% / 경남 기타 64.7%
- 운영권역 94.6% 는 **수동 별칭 보정이 누적된 결과**(`complex_aliases`, 매칭 로직 개선, 토월성원 병합 등)다. K-apt 시딩만 받은 지역의 자연 수준은 60% 대이므로 **부산에 90% 를 요구하면 상시 빨간불이 되고 그러면 아무도 안 본다**
- 부산은 기본 범위에 넣지 **않는다.** 임계가 다르기 때문이다(부산 65 vs 운영권역 80). 부산을 재는 방법은 `--sgg=<부산 16코드> --min-linked-pct=65` 로 **명시 호출**이며, `molit-backfill-once.yml` 의 `min_linked_pct` 입력이 그 경로다. ⚠️ `molit-daily.yml` 의 연결률 검사는 인자 없이 호출하므로 **일배치의 부산 적재는 이 감시의 판정 대상이 아니다** — 알고 남긴 공백이며, 좁히려면 임계가 다른 두 범위를 각각 재는 스텝 분리가 필요하다(별칭 보정 후속 작업과 함께 판단할 사항)

부산 16개 코드를 주석 안에 문자열로 남겨 다음 사람이 찾아 헤매지 않게 한다.
  </action>
  <acceptance_criteria>
    - `grep -c "26110" scripts/check-ingest-linkage.ts` >= 1 (부산 코드가 주석에 기록됨)
    - `grep -c "ADR-064" scripts/check-ingest-linkage.ts` >= 1
    - `grep -vE '^\s*(\*|//|/\*)' scripts/check-ingest-linkage.ts | grep -c "DEFAULT_MIN_LINKED_PCT = 80"` == 1 (기본 임계 무변경)
    - `grep -vE '^\s*(\*|//|/\*)' scripts/check-ingest-linkage.ts | grep -c "'48121'"` == 1 (기본 범위 무변경)
    - 주석에 실측 부산 연결률 수치와 94.6%·64.7% 대조군, `molit-daily.yml` 판정 공백이 모두 문장으로 적혀 있다
    - `npm run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ADR-064" scripts/check-ingest-linkage.ts && npm run lint</automated>
  </verify>
  <done>부산 연결률 기준선과 감시 범위 결정이 코드 주석으로 남아 재발견이 불필요해진다.</done>
</task>

<task type="auto">
  <name>Task 3: ADR-064 작성 + FEATURES.json 갱신</name>
  <files>docs/ADR.md, docs/FEATURES.json</files>
  <read_first>
    - docs/ADR.md (🔴 ADR-062 전문 = 이 작업의 역방향 기록. ADR-060~063 의 헤더 형식·서술 문체·분량)
    - docs/FEATURES.json (`_schema`·`_usage`·`summary`·`features[]` 구조와 `checklist` layer 값들)
    - .planning/phases/41-busan-recollect/41-01-SUMMARY.md ~ 41-08-SUMMARY.md (인용할 실측값 전부)
    - CLAUDE.md (FEATURES.json 업데이트 규칙 — status·checklist 갱신 후 summary 카운트 재계산)
  </read_first>
  <action>
**(1) `docs/ADR.md` 에 `### ADR-064` 를 파일 마지막 ADR(ADR-063) 다음에 추가한다.** 기존 ADR 의 문체를 따른다 — 결정만 적지 않고 **무엇을 실측해 알았는지**를 적는다. 반드시 담을 것:
- **결정**: 2026-08-10 에 용량(Free 500MB / 실제 565MB) 때문에 끈 부산 16개 구 수집을 Pro 전환(8GB)으로 전제가 사라져 재개했다. ADR-062 의 "되돌리려면" 2단계 절차를 실제로 실행한 결과다. `regions.is_active` 스위치는 이번엔 **정식 마이그레이션**(`20260819080000_reactivate_busan_scope.sql`)으로 원장에 남겼다 — ADR-062 의 삭제는 statement timeout 때문에 `.applied-manually` 였다
- 🔴 **`--resume` 함정과 그 발견**: 삭제 때 `ingest_runs` 를 지우지 않아 부산 4,006행(success 3,868 = apt 1,903 + villa 1,853 + offi 112)이 남아 있었다. `molit-backfill-once.yml` 이 `--resume` 를 하드코딩하므로 그대로 돌리면 **3,756건이 skip 되고 0건 적재로 초록불**이 됐을 것이다. 원장을 실제 데이터 상태와 일치시키는 것(삭제)을 근본 해결로 택했고, `--resume` 을 끄는 방식은 재개 시 같은 문제가 다시 나므로 배제했다. **첫 dispatch 로그의 skip=0** 으로 실증했다(41-05)
- **기간을 201501 로 넓힌 결정(D-04)**: 부산은 원래 201607 부터였다(`ingest_runs.year_month` 실측). 창원·김해(201501)와 대칭으로 맞춰 삭제분보다 576 지역-월 넓게 수집했다 — 랭킹·갭·예측의 시계열 길이가 지역별로 달라지는 것을 피하기 위함
- **분할 dispatch(D-05)**: 4,480 지역-월 × 11.5초(실측 중간값) ≈ 14.3시간 vs `timeout-minutes: 300`. 3그룹(5·5·6)으로 나눠 하루 한 그룹씩 실행했다. **실제 소요시간과 그룹별 실측**을 41-05~41-07 에서 인용해 적는다
- **연결률 기준을 65% 로 정한 근거(D-07)**: 운영권역 94.6% 는 별칭 보정 누적 결과이고, K-apt 시딩만 받은 경남 기타 16곳은 64.7% 다. 부산 실측은 <41-08 값>% 였다. 90% 를 요구하면 상시 빨간불이 되어 감시가 무력화된다
- **감시 기준선(D-08)**: 미리 바꾸지 않고 부산 유입 후 실측해 <유지/상향/하향> 했다. `complex_integrity_counts(text[])` 는 **저장소에 `CREATE FUNCTION` 이 없다**(프로덕션 전용 = 마이그레이션 drift). 배열 스코프를 프로덕션 호출 대조로 판정했고, **파일화는 Phase 37 계열 후속 과제로 남는다**
- **미복원 항목**: `facility_kapt` 1,463 / `cafe_articles` 1,654 는 복원하지 않았다. 사유와 소관 Phase 를 적는다
- **다운스트림**: `complex_rankings.price_change` 는 전국 집계이고 부산이 상위를 차지하는 상태로 돌아갔다. 창부레터·실거래이야기는 `si` 필터 후 최상위 행의 `metadata.region` 을 `hotArea` 로 써야 한다(Phase 40-04 인계 ①). **bds 코드 변경 0건**이며 realtrade-story 소관이다
- **범위 밖으로 남긴 것**: 부산 화면 노출·도메인·`site_id`(realtrade-story ADR-012 의 미정 4건) / 네이버 호가 크롤링 부산 확장 / 부산 미분양 / 오피스텔 부산 재수집 / 별칭 보정으로 90% 달성 / `complex_integrity_counts` 파일화
- **용량 실측**: 최종 DB 용량과 8,192MB 대비 %, 41-CONTEXT 의 650MB 산정과의 차이

**(2) `docs/FEATURES.json` 갱신** — 현재 `features` 33개에 "부산" 문자열이 0건이다. 수집 권역은 기존 feature 중 실거래·지역 관련 항목의 `notes`·`checklist` 로 표현하는 것이 자연스러우므로, **새 feature 를 만들지 판단해 결정하고 그 근거를 SUMMARY.md 에 남긴다.** 어느 쪽이든:
- 관련 feature 의 `notes` 에 "수집 권역 = 경남 22개 시군 + 부산 16개 구(2026-08-19 재개, ADR-064)" 를 반영한다
- `lastUpdated` 를 갱신한다
- 🔴 **`summary` 카운트를 재계산한다** (CLAUDE.md 규칙). `features[].status` 를 실제로 집계해 `done`/`in_progress`/`todo`/`deferred`/`blocked` 를 맞춘다 — 손으로 숫자를 고치지 말고 집계 결과와 일치시킨다
- `deferred`·`blocked` 항목은 조건 충족 전까지 건드리지 않는다

**(3) `.planning/STATE.md` 갱신** — Phase 41 완료 상태와 핵심 실측(부산 거래 수·연결률·DB 용량·기준선 판정)을 `Current focus` 에 반영한다.

🔴 `git push` 하지 않는다 — 배포는 별도 사용자 결정이다(`<scope_fence>`).
  </action>
  <acceptance_criteria>
    - `grep -c "### ADR-064" docs/ADR.md` == 1 이고 `grep -c "### ADR-063" docs/ADR.md` == 1 (기존 ADR 무손실)
    - ADR-064 본문에 다음 문자열이 모두 등장한다: `ADR-062`, `--resume`, `201501`, `65`, `complex_integrity_counts`, `facility_kapt`, `price_change`
    - ADR-064 에 실측 수치(부산 `transactions` 총 수 / 연결률 % / 최종 DB 용량 MB / 그룹별 소요시간)가 인용돼 있다
    - `node -e "const j=require('./docs/FEATURES.json'); const c={}; for(const f of j.features) c[f.status]=(c[f.status]||0)+1; const s=j.summary; process.exit(JSON.stringify(c)===JSON.stringify({done:s.done,in_progress:s.in_progress,todo:s.todo,deferred:s.deferred,blocked:s.blocked})||Object.keys(c).every(k=>c[k]===s[k])?0:1)"` 가 exit 0 (summary 카운트가 실제 집계와 일치)
    - `node -e "JSON.parse(require('fs').readFileSync('docs/FEATURES.json','utf8'))"` 가 exit 0 (JSON 유효)
    - `grep -c "ADR-064" docs/FEATURES.json` >= 1
    - `.planning/STATE.md` 의 `Current focus` 에 Phase 41 실측이 반영됨
    - `git log --oneline -1` 이 이 Phase 커밋이고 `git status --short` 에 미커밋 변경이 없다. `git push` 는 실행되지 않았다 (`git log origin/main..HEAD --oneline` 에 커밋이 남아 있음)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "### ADR-064" docs/ADR.md && node -e "JSON.parse(require('fs').readFileSync('docs/FEATURES.json','utf8'))" && npm run lint</automated>
  </verify>
  <done>ADR-064 가 실측 근거와 함께 기록되고 FEATURES.json·STATE.md 가 갱신된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Phase 41 마감 확인 + push 여부 결정</name>
  <files>없음 (사용자 확인 대기)</files>
  <read_first>
    - docs/ADR.md (ADR-064 작성 결과)
    - .github/workflows/complex-integrity.yml (기준선 diff 와 용량 로깅 추가분)
    - scripts/busan-status.ts (최종 실측)
  </read_first>
  <what-built>
Phase 41 을 마감했다. 부산 16개 구 수집 재개의 전 과정(스위치 → 원장 정합 → 시딩·지오코딩 → 3그룹 백필 → 파생값 → 감시 확인)을 실행하고, 감시 기준선을 실측으로 판정하고, ADR-064 로 기록했다. `git push` 는 하지 않았다.
  </what-built>
  <how-to-verify>
1. 최종 상태 한눈에: `npx tsx scripts/busan-status.ts`
   - 부산 `complexes` / 좌표 커버리지 / `transactions` 총 수 / 연결률 / 파생값 4항목 / DB 용량
2. 원장: `npx supabase migration list --linked` — drift 0
3. 감시: `gh run list --workflow=complex-integrity.yml --limit=2` 와 `gh run list --workflow=data-freshness-check.yml --limit=2` 가 초록불
   - `complex-integrity` 로그에 DB 용량 줄이 새로 나타나는지 확인
4. 기준선: `git diff HEAD~<N> -- .github/workflows/complex-integrity.yml` — `BASE_*` 가 바뀌었다면 **왜 올렸는지 주석이 함께 있는지** 확인 (여유를 얹지 않고 실측값으로 잠갔는지)
5. 기록: `docs/ADR.md` 의 ADR-064 를 읽고 실측 수치와 범위 밖 항목이 납득 가능한지 확인
6. 🔴 **후속 확인이 필요한 인계 사항 2건**:
   - 창부레터/실거래이야기가 `complex_rankings.price_change` 를 `si` 필터 없이 읽으면 `hotArea` 에 부산 지역명이 나간다 (41-08 실측 참조)
   - `complex_integrity_counts(text[])` 는 프로덕션에만 있는 함수다 (마이그레이션 drift, Phase 37 계열 후속)
7. `git push` 여부를 결정한다 — 이 Phase 는 push 하지 않았다
  </how-to-verify>
  <resume-signal>확인 후 "승인" 또는 수정 요청 사항을 알려주면 된다. push 를 원하면 "push" 라고 알려주면 실행한다.</resume-signal>
  <action>부산 복원 최종 실측(busan-status.ts), 원장 drift 0, 감시 2종 초록불, 기준선 판정 diff, ADR-064 내용, 후속 과제 2건을 사용자에게 제시하고 승인 또는 수정 요청을 받는다. git push 는 사용자가 명시적으로 요청할 때만 실행한다.</action>
  <verify>
    <automated>npx tsx scripts/busan-status.ts && npx supabase migration list --linked</automated>
  </verify>
  <acceptance_criteria>
    - 사용자 응답에 승인 또는 수정 요청이 명시됨
    - `git push` 를 실행했는지 여부가 SUMMARY.md 에 기록됨 (요청 없으면 미실행)
    - 후속 과제 2건(창부레터 `hotArea` 필터 / `complex_integrity_counts` 파일화)이 인계 사항으로 기록됨
  </acceptance_criteria>
  <done>사용자가 Phase 41 결과를 승인하고 push 여부를 결정한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `complex-integrity.yml` (psql `backup_agent`) → 프로덕션 DB | 읽기 전용 BYPASSRLS 롤. 여기서 실패를 만들지 못하면 오염이 조용히 쌓인다 |
| 감시 기준선(`BASE_*`) → 사람의 주의 | 기준선을 느슨하게 하면 감시가 형식만 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-32 | Repudiation (감시 무력화) | 부산 유입에 맞춰 `BASE_*` 를 여유 있게 올림 | mitigate | 실측값 그대로 잠그도록 명문화 + 변경 시 이유 주석을 acceptance 로 요구 + 수정 후 재dispatch 로 실증 |
| T-41-33 | Repudiation (감시 무력화) | `DEFAULT_MIN_LINKED_PCT` 를 65 로 낮춰 부산을 기본 범위에 넣음 | mitigate | 기본값 무변경을 `grep` 단언으로 고정(주석 제외 필터 사용). 부산은 `--sgg` + `min_linked_pct` 명시 호출 경로 |
| T-41-34 | Denial of Service (오탐으로 잡 실패) | 용량 로깅을 실패 조건에 넣음 | mitigate | `RAISE INFO` 만 추가하고 `worse`/`RAISE EXCEPTION` 에 넣지 않음을 diff 로 확인 |
| T-41-35 | Information Disclosure | ADR/SUMMARY 에 실측 수치 기록 | accept | 행 수·용량·연결률은 비밀값이 아니다. 프로젝트 ref·키·비밀번호는 적지 않는다 |
| T-41-36 | Tampering (문서 손실) | `FEATURES.json` 수동 편집으로 JSON 깨짐·카운트 불일치 | mitigate | `JSON.parse` 성공 + `summary` 카운트가 실제 집계와 일치함을 단언 |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
gh run list --workflow=complex-integrity.yml --limit=3
gh run list --workflow=data-freshness-check.yml --limit=3
grep -c "### ADR-064" docs/ADR.md
node -e "JSON.parse(require('fs').readFileSync('docs/FEATURES.json','utf8'))"
npx supabase migration list --linked
npm run lint
```
</verification>

<success_criteria>
1. `complex-integrity`·`data-freshness` 실행 결과가 실측 기록되고, 기준선이 유지/상향/하향 중 하나로 **근거와 함께** 처리됨 (BUSAN-07, Success Criteria 9)
2. `BASE_*` 를 올린 경우 실측값 그대로이고 이유 주석이 함께 있으며, 수정 후 재실행으로 초록불이 실증됨
3. `complex-integrity` 로그에 DB 용량이 상시 남는다 (`RAISE INFO`, 실패 조건 아님)
4. `check-ingest-linkage.ts` 기본값 무변경 + 부산 실측 기준선·감시 공백이 주석으로 문서화됨
5. `docs/ADR.md` 에 ADR-064 가 실측 수치·범위 밖 항목·다운스트림 영향과 함께 기록됨 (BUSAN-08)
6. `docs/FEATURES.json` JSON 유효 + `summary` 카운트가 실제 집계와 일치, `.planning/STATE.md` 갱신
7. `npm run lint` exit 0 / 테스트 실패 이름 집합 불변 / `git push` 미실행
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-09-SUMMARY.md` 를 생성한다. 감시 2종 실측값과 기준선 판정(유지/상향/하향 + 근거), 용량 로깅 추가 diff 요지, ADR-064 요지, FEATURES.json 갱신 내용, 후속 과제 2건(창부레터 `hotArea` 필터 / `complex_integrity_counts` 파일화)을 기록할 것.
</output>
</content>
