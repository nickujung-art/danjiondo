---
phase: 41-busan-recollect
plan: "06"
type: execute
wave: 5
depends_on: ["41-05"]
files_modified: []
autonomous: false
requirements:
  - BUSAN-05
must_haves:
  truths:
    - "그룹 B 5개 구(26290·26410·26440·26470·26500)의 201501~202608 실거래(아파트+연립다세대)가 transactions 에 적재된다"
    - "그룹 A 실측 소요시간·API 호출량을 반영해 dispatch 시각·분할이 조정된다 (추정이 아니라 실측 기반)"
    - "그룹 B 적재가 rows_upserted 합계와 transactions 증가분 대조로 판정된다"
  artifacts: []
  key_links:
    - from: ".github/workflows/molit-backfill-once.yml (workflow_dispatch)"
      to: "scripts/backfill-realprice.ts"
      via: "sgg_codes=그룹B 5코드 / from=201501 / resume=true / min_linked_pct=65"
      pattern: "sgg_codes"
---

<objective>
MOLIT 실거래 백필 **그룹 B** — 부산 중대형 5개 구(남·금정·강서·연제·수영)를 `201501~` 로 백필한다.

그룹 A(41-05)와 절차는 같지만 **입력이 실측으로 갱신된다.** 41-05 SUMMARY 가 남긴 실제 소요시간·실제 API 호출 규모·타임아웃 여부를 근거로 dispatch 시각과(필요하면) 분할을 조정한다 — D-05 의 11.5초/건 산정은 최근 성공 40건의 중간값이고, 그룹 A 가 그보다 느렸다면 그룹 B 는 한 job 에 들어가지 않는다.

🔴 **그룹 A 와 최소 하루 간격을 둔다** (D-05 — MOLIT API 일 10,000회 한도를 `molit-daily.yml` 과 공유).

Purpose: 그룹 B 적재 + 실측 기반 계획 보정.
Output: 그룹 B 거래 적재, 연결률·용량 실측, 누적 추이.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\41-busan-recollect\41-05-SUMMARY.md
@C:\Users\jung\coding\bds\.github\workflows\molit-backfill-once.yml
@C:\Users\jung\coding\bds\scripts\backfill-realprice.ts

<interfaces>
<!-- 그룹 B dispatch 입력 -->
sgg_codes      = 26290,26410,26440,26470,26500      (남구·금정구·강서구·연제구·수영구)
from           = 201501
to             = (공백 — 당월 자동)
resume         = true
min_linked_pct = 65

<!-- 예상 규모 (41-05 실측으로 갱신할 대상) -->
지역-월 = 5구 × 140개월 × 2종 = 1,400
소요 추정 = 그룹 A 실측 소요시간을 1차 근거로 쓴다. D-05 원 산정은 1,400 × 11.5초 ≈ 4.5시간
timeout-minutes: 300 (5시간)

<!-- 종료 코드 (41-05 와 동일) -->
exit 0 정상 / exit 1 실패율 >= 30% / exit 75 러너 IP 차단 → 재dispatch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 41-05 실측 반영 후 그룹 B dispatch</name>
  <files>없음 (워크플로 dispatch)</files>
  <read_first>
    - .planning/phases/41-busan-recollect/41-05-SUMMARY.md (🔴 실제 소요시간·API 호출 규모·재dispatch 횟수·타임아웃 여부 — 이 태스크의 판단 근거)
    - .github/workflows/molit-backfill-once.yml (입력 이름과 조건부 조립)
    - scripts/busan-status.ts (측정 진입점)
  </read_first>
  <action>
**(1) 41-05 실측을 근거로 dispatch 형태를 결정한다.**
- 그룹 A 가 **타임아웃 없이** 완주했다면 그룹 B 도 5코드 한 번에 dispatch 한다
- 그룹 A 가 **타임아웃돼 재dispatch 가 필요했다면** 그룹 B 를 3코드 + 2코드로 쪼개 두 번 dispatch 한다. `--resume` 이 있으므로 쪼개는 것에 위험은 없다. 🔴 반대로 "한 번에 되겠지" 로 밀어붙이면 5시간을 태우고 부분 적재로 끝난다
- 그룹 A 의 실제 API 호출 규모가 4,000회를 넘었다면 그룹 B 를 그룹 A 다음날이 아니라 **이틀 뒤**로 미룬다
결정과 그 근거를 SUMMARY.md 에 먼저 적는다.

**(2) 착수 전 기준값 고정** — `npx tsx scripts/busan-status.ts --json` 을 "그룹 B 착수 전"으로 기록한다. 이 값은 그룹 A 적재가 반영된 상태이며, Task 3 의 증가분 대조 기준이 된다.

**(3) dispatch** — `gh workflow run molit-backfill-once.yml` 에 `<interfaces>` 의 그룹 B 입력을 전달한다. 🔴 04:00 KST(19:00 UTC) 전후 1시간을 피하고, **그룹 A dispatch 로부터 최소 24시간 경과** 후에 실행한다.

**(4) 초기 로그 확인** — `gh run view <id> --log` 로 세 줄을 확인한다:
- `🔌 MOLIT 연결 확인: HTTP 200` (없이 exit 75 면 즉시 재dispatch)
- `📅 기간: 201501 ~ <당월>`
- `📍 지역:` 에 그룹 B 코드만 (5개 또는 쪼갠 수만큼)
⚠️ 이번 run 은 그룹 B 가 처음이므로 **skip 은 0 이어야 한다.** 로그에 skip 이 잡히면 그룹 A 코드가 섞여 들어간 것이므로 즉시 중단하고 입력을 확인한다.
  </action>
  <acceptance_criteria>
    - SUMMARY.md 에 "41-05 실측 → 그룹 B dispatch 형태" 결정 근거(완주/타임아웃, API 호출 규모, 간격)가 기록됨
    - 그룹 A dispatch 시각과 그룹 B dispatch 시각의 간격이 >= 24시간임이 기록됨
    - `gh run list --workflow=molit-backfill-once.yml --limit=1` 에 새 run 이 있고 run id 가 기록됨
    - run 로그에 `📅 기간: 201501` 이 나타나고 `📍 지역:` 이 그룹 B 코드만 담는다
    - SUMMARY.md 에 착수 전 `busan-status.ts --json` 기준값이 있다
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=3</automated>
  </verify>
  <done>실측 기반으로 결정된 형태로 그룹 B 가 실행 중이고 기준값이 고정됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [CHECKPOINT] 그룹 B 백필 완료 대기 (다세션)</name>
  <files>없음 (사용자 확인 대기)</files>
  <read_first>
    - .github/workflows/molit-backfill-once.yml
    - scripts/backfill-realprice.ts (종료 코드 해석 근거)
    - .planning/phases/41-busan-recollect/41-05-SUMMARY.md (그룹 A 실측 — 비교 기준)
  </read_first>
  <what-built>
그룹 B(남·금정·강서·연제·수영 5개 구)의 MOLIT 실거래 백필을 `201501~` 로 dispatch 했다(41-05 실측에 따라 1회 또는 2회로 분할). 그룹 A 와 동일하게 여러 시간이 걸리므로 사람이 완료를 확인해야 한다.
  </what-built>
  <how-to-verify>
1. 실행 상태: `gh run list --workflow=molit-backfill-once.yml --limit=3`
   - `success` → 3번으로
   - **exit 75**(로그에 `🚫 이 러너에서 MOLIT 에 연결할 수 없습니다` 또는 `연결 불가가 연속 3회`) → 같은 입력으로 즉시 재dispatch
   - **exit 1 + `실패율이 임계(30%)를 넘어`** → 로그의 실패 사유 확인 후 재실행
   - **`Check linkage rate` 단계만 실패** → 적재는 됐고 연결률이 65% 미만. 3번을 진행한 뒤 보고
   - 타임아웃(5시간) → 정상 설계. 같은 입력으로 재dispatch 하면 `--resume` 이 완료분을 skip 한다
2. 잔여 확인: `npx tsx scripts/busan-status.ts`
   그룹 B 5개 구의 `ingest_runs` success 수가 각각 280(=140×2)에 가까운지 본다
3. **[필수] Supabase Dashboard → Database → Usage 의 DB 용량**을 확인해 알려준다. 그룹 A 후 수치와 비교해 **증가분**을 본다 — 부산 전체 예상은 약 +200MB(650MB 도달)이고 Pro 한도는 8,192MB 다
4. **다음 그룹(C, 6개 구)은 하루 뒤에** 시작한다
  </how-to-verify>
  <resume-signal>"그룹 B 완료, DB N MB" 로 응답. 재dispatch 나 분할이 있었으면 횟수와 사유를 알려주면 SUMMARY 에 기록한다. 진행 중이면 "진행 중, ingest_runs success M건" 으로 알려주면 다음 세션에서 이어서 확인한다.</resume-signal>
  <action>그룹 B run 의 완료를 사용자가 확인해 알려줄 때까지 대기한다. 종료 코드별 대응을 안내하고 Supabase Dashboard DB 용량 수치를 받아 그룹 A 후 수치와의 증가분을 확인한다.</action>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=3</automated>
  </verify>
  <acceptance_criteria>
    - 그룹 B run 의 최종 conclusion 이 확인됨
    - 재dispatch·분할이 있었다면 횟수와 사유가 기록됨
    - DB 용량 수치와 그룹 A 후 대비 증가분이 기록됨
  </acceptance_criteria>
  <done>그룹 B 가 완주했고 용량 증가분을 확보했다.</done>
</task>

<task type="auto">
  <name>Task 3: 그룹 B 적재 실측 단언 + 누적 추이</name>
  <files>없음 (측정 전용. 실측값은 SUMMARY.md 에 기록)</files>
  <read_first>
    - scripts/busan-status.ts
    - scripts/check-ingest-linkage.ts
    - .planning/phases/41-busan-recollect/41-05-SUMMARY.md (그룹 A 실측 — 이번 수치와 나란히 놓을 기준)
  </read_first>
  <action>
🔴 **워크플로 초록불을 성공 근거로 쓰지 않는다.** 41-05 Task 3 과 같은 5종을 그룹 B 기준으로 실측한다.

**(1) skip 검증** — 그룹 B 첫 run 의 완료 요약 줄(`✅ 완료: N건 처리 (M건 skip), K건 upsert`)에서 skip == 0. 분할 dispatch 했다면 각 분할의 **첫 run** 이 판정 대상이다.

**(2) `rows_upserted` 합계 vs `transactions` 증가분** — 그룹 B 5코드의 `ingest_runs.rows_upserted` 합계와 Task 1 이 고정한 착수 전 기준값 대비 증가분을 나란히 적는다. 증가분 0 이면 실패로 처리한다.

**(3) 지역별 최저선** — 그룹 B 5개 구 각각 `transactions` > **1,000**.

**(4) 기간 검증** — 그룹 B 5코드의 `deal_date` 최소값이 2015-01 대.

**(5) 연결률** — `npx tsx scripts/check-ingest-linkage.ts --since-hours=48 --sgg=26290,26410,26440,26470,26500 --min-linked-pct=65`. 실측값을 **그룹 A 실측값**과 나란히 적는다. 두 그룹이 크게 다르면(예: 10%p 이상) 지역 특성이 아니라 매칭 경로 문제일 수 있으므로 차이가 큰 구를 특정해 기록한다.

**(6) 누적 추이 표** — SUMMARY.md 에 3열 표를 만든다: 착수 전(41-05 착수 전) / 그룹 A 후 / 그룹 B 후. 행은 부산 `transactions` 총 수, 부산 연결률, `db_size_bytes`(MB), 8,192MB 대비 %. 🔴 이 표가 41-08 의 규모·용량 보고의 원자료이므로 반드시 남긴다.
  </action>
  <acceptance_criteria>
    - 그룹 B 첫 run(분할 시 각 분할의 첫 run)의 skip 건수 == 0 또는 사유 규명
    - 그룹 B 5코드 `rows_upserted` 합계 > 0, 부산 `transactions` 증가분 > 0, 두 값이 나란히 기록됨
    - 그룹 B 5개 구 각각 `transactions` > 1,000
    - 그룹 B 5코드 `deal_date` 최소값이 2015-01 대
    - `check-ingest-linkage.ts --sgg=<그룹B> --min-linked-pct=65` 출력이 기록되고 그룹 A 실측값과 대조됨
    - SUMMARY.md 에 3열 누적 추이 표(착수 전 / A 후 / B 후 × 4행)가 있다
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/check-ingest-linkage.ts --since-hours=48 --sgg=26290,26410,26440,26470,26500 --min-linked-pct=65</automated>
  </verify>
  <done>그룹 B 적재가 실측 수치로 증명되고 누적 추이 표가 만들어진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.go.kr(MOLIT) → `transactions` | 외부 응답이 Zod 검증을 지나 적재된다 |
| GitHub Actions 러너 → 프로덕션 Supabase | service_role 시크릿으로 대량 upsert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-20 | Repudiation (조용한 성공) | 그룹 B 가 0건 적재로 초록불 | mitigate | Task 3(1)(2) — skip==0 + `rows_upserted`/`transactions` 증가분 대조 |
| T-41-21 | Denial of Service (API 한도) | 그룹 A 와 같은 날 실행 시 일 10,000회 초과 | mitigate | dispatch 간격 >= 24시간을 acceptance 로 단언. 그룹 A 실측 호출량이 4,000 초과면 이틀 간격 |
| T-41-22 | Denial of Service (타임아웃 반복) | 그룹 A 가 5시간을 넘겼는데 같은 형태로 밀어붙임 | mitigate | Task 1(1) — 41-05 실측에 따라 3+2 분할로 전환 |
| T-41-23 | Denial of Service (용량) | 누적 적재로 Pro 8GB 소모 | accept | 그룹별 Dashboard 실측 + 누적 추이 표. 부산 전체 예상 650MB = 8% |
| T-41-SC | Tampering | 신규 패키지 설치 | accept | 이 plan 은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
```bash
gh run list --workflow=molit-backfill-once.yml --limit=5
npx tsx scripts/busan-status.ts
npx tsx scripts/check-ingest-linkage.ts --since-hours=48 --sgg=26290,26410,26440,26470,26500 --min-linked-pct=65
```
</verification>

<success_criteria>
1. 그룹 B 5개 구의 201501~ 실거래가 적재됨 (BUSAN-05 2/3)
2. 첫 run skip 0, `rows_upserted` 합계와 `transactions` 증가분 대조 기록
3. 5개 구 각각 `transactions` > 1,000, `deal_date` 최소값 2015-01 대
4. 연결률 실측값이 그룹 A 와 대조 기록됨
5. 3열 누적 추이 표(착수 전 / A 후 / B 후)가 존재
6. 그룹 A 와의 dispatch 간격 >= 24시간이 기록됨
</success_criteria>

<output>
`.planning/phases/41-busan-recollect/41-06-SUMMARY.md` 를 생성한다. dispatch 형태 결정 근거, run id·재dispatch 이력, 실측 5종, 누적 추이 표를 기록하고, 그룹 C(6개 구 = 1,680 지역-월로 5시간 산정 초과)에 인계할 판단 근거를 명시할 것.
</output>
</content>
