---
phase: 18-realtor-recommendation
plan: "03"
type: execute
wave: 3
depends_on: ["18-01"]
files_modified:
  - src/components/realtors/RealtorCard.tsx
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - PHASE18-UI
must_haves:
  truths:
    - "단지 상세 페이지에서 '이 지역 관련 광고(in_feed)' 섹션이 완전히 제거된다"
    - "배정된 공인중개사가 있는 단지에서 '이 단지 담당 공인중개사' 섹션이 표시된다"
    - "RealtorCard가 이름, 사무소명, 전화 문의 버튼을 표시한다"
    - "전화 문의 버튼이 tel: URI로 동작한다"
    - "이미지가 없는 공인중개사에게 이니셜 기반 아바타가 표시된다"
    - "배정된 공인중개사가 없는 단지에서는 섹션 자체가 숨겨진다"
  artifacts:
    - path: "src/components/realtors/RealtorCard.tsx"
      provides: "공인중개사 카드 RSC 컴포넌트"
      contains: "tel:"
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "수정된 단지 상세 페이지"
      contains: "getRealtorsByComplexId"
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/lib/data/realtors.ts"
      via: "getRealtorsByComplexId(id, supabase)"
      pattern: "getRealtorsByComplexId"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/components/realtors/RealtorCard.tsx"
      via: "JSX <RealtorCard realtor={r} />"
      pattern: "RealtorCard"
---

<objective>
단지 상세 페이지의 `in_feed` 광고 섹션을 제거하고 공인중개사 카드 섹션으로 교체한다.

Purpose: 방문자가 단지 담당 공인중개사에게 직접 전화 문의할 수 있는 UI를 제공한다.
Output: RealtorCard 컴포넌트, 수정된 complexes/[id]/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-realtor-recommendation/18-CONTEXT.md
@.planning/phases/18-realtor-recommendation/18-RESEARCH.md
@.planning/phases/18-realtor-recommendation/18-PATTERNS.md
@src/app/complexes/[id]/page.tsx
</context>

<interfaces>
<!-- Wave 1에서 생성된 인터페이스 -->

From src/lib/data/realtors.ts:
```typescript
export type Realtor = Database['public']['Tables']['realtors']['Row']
// {
//   id: string
//   name: string
//   agency_name: string
//   phone: string
//   description: string | null
//   image_url: string | null
//   is_active: boolean
//   ...
// }

export async function getRealtorsByComplexId(
  complexId: string,
  supabase: SupabaseClient<Database>,
): Promise<Realtor[]>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RealtorCard RSC 컴포넌트 구현</name>
  <files>src/components/realtors/RealtorCard.tsx</files>
  <action>
`src/components/realtors/` 디렉터리를 생성하고 `RealtorCard.tsx`를 작성한다.

CRITICAL:
- `'use client'` 지시어 없음 — 순수 RSC 컴포넌트 (D-05)
- 이미지 없을 때 이니셜 기반 텍스트 아바타 (D-06)
- 전화 링크: `<a href="tel:...">` (D-07, Next.js Link 불필요)
- tel: href에서 하이픈 제거: `phone.replace(/[^0-9+]/g, '')` (Pitfall 5 방지)
- BANNED 스타일: backdrop-blur, gradient-text, glow, purple/indigo 색상

```typescript
// src/components/realtors/RealtorCard.tsx
// RSC — 'use client' 없음 (D-05)

interface RealtorCardProps {
  realtor: {
    id: string
    name: string
    agency_name: string
    phone: string
    description: string | null
    image_url: string | null
  }
}

export function RealtorCard({ realtor }: RealtorCardProps) {
  // 이니셜: 이름 첫 두 글자 (D-06)
  const initials = realtor.name.slice(0, 2)

  return (
    <div
      className="card"
      style={{ padding: 16, marginBottom: 8 }}
    >
      <div style={{ display: 'flex', gap: 12, alignItems: 'flex-start' }}>

        {/* 프로필 이미지 또는 이니셜 아바타 (D-06) */}
        <div
          style={{
            width: 48,
            height: 48,
            borderRadius: '50%',
            flexShrink: 0,
            overflow: 'hidden',
            background: 'var(--bg-surface-2)',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          {realtor.image_url ? (
            // eslint-disable-next-line @next/next/no-img-element
            <img
              src={realtor.image_url}
              alt={realtor.name}
              style={{ width: '100%', height: '100%', objectFit: 'cover' }}
            />
          ) : (
            <span
              style={{
                font: '600 14px/1 var(--font-sans)',
                color: 'var(--fg-sec)',
              }}
            >
              {initials}
            </span>
          )}
        </div>

        {/* 정보 영역 */}
        <div style={{ flex: 1, minWidth: 0 }}>
          <div
            style={{
              font: '700 14px/1.3 var(--font-sans)',
              color: 'var(--fg-pri)',
              marginBottom: 2,
            }}
          >
            {realtor.name}
          </div>
          <div
            style={{
              font: '500 12px/1.3 var(--font-sans)',
              color: 'var(--fg-sec)',
              marginBottom: 6,
            }}
          >
            {realtor.agency_name}
          </div>
          {realtor.description && (
            <div
              style={{
                font: '400 12px/1.5 var(--font-sans)',
                color: 'var(--fg-tertiary)',
                marginBottom: 8,
                display: '-webkit-box',
                WebkitLineClamp: 2,
                WebkitBoxOrient: 'vertical',
                overflow: 'hidden',
              }}
            >
              {realtor.description}
            </div>
          )}
          {/* tel: 링크 (D-07) — 하이픈 제거 (Pitfall 5) */}
          <a
            href={`tel:${realtor.phone.replace(/[^0-9+]/g, '')}`}
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 4,
              padding: '5px 12px',
              borderRadius: 6,
              background: 'var(--dj-orange)',
              color: '#fff',
              font: '600 12px/1 var(--font-sans)',
              textDecoration: 'none',
            }}
          >
            전화 문의 &nbsp;{realtor.phone}
          </a>
        </div>

      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 2>&1 | grep "src/components/realtors" | head -10 && npm run test -- --run src/__tests__/realtors.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
- src/components/realtors/RealtorCard.tsx 생성됨
- src/components/realtors/RealtorCard.test.tsx 생성됨 (tel: href 형식 + 이니셜 폴백 렌더 테스트)
- 'use client' 없음 (RSC)
- tel: href에 phone.replace(/[^0-9+]/g, '') 적용됨
- `npm run lint` 오류 없음
- 기존 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: complexes/[id]/page.tsx — in_feed 제거 및 공인중개사 섹션 교체</name>
  <files>src/app/complexes/[id]/page.tsx</files>
  <action>
CRITICAL: 이 파일은 대용량(1000줄 이상 추정)이므로 **반드시 먼저 Read 툴로 파일 전체를 읽고** 정확한 라인 위치를 파악한 후 Edit 툴을 사용한다.

수행할 변경 4단계:

**Step 1 — import 제거:**
```typescript
// 제거 (D-01):
import { getActiveAds } from '@/lib/data/ads'
```
`getActiveAds` 임포트 라인을 제거한다. `AdBanner` 임포트는 sidebarAds에도 사용되므로 **유지**한다.

**Step 2 — import 추가:**
`getActiveAds` 임포트 제거 라인 위치에, 또는 ads.ts 관련 import 다음에 추가:
```typescript
import { getRealtorsByComplexId } from '@/lib/data/realtors'
import type { Realtor } from '@/lib/data/realtors'
import { RealtorCard } from '@/components/realtors/RealtorCard'
```

**Step 3 — Promise.all 수정:**

RESEARCH.md Q1의 정확한 위치(lines 208-224)를 확인 후 편집:
```typescript
// 제거:
inFeedAds,          // 구조분해에서 제거
// ...
getActiveAds('in_feed', supabase, complex.sgg_code ?? undefined),  // 제거

// 추가:
complexRealtors,    // 구조분해에 추가
// ...
getRealtorsByComplexId(id, supabase),  // 추가 (D-02: createReadonlyClient는 page에서 이미 사용)
```

CRITICAL: `supabase` 클라이언트 변수명은 이 page에서 이미 사용 중인 변수명과 일치시킨다.
`createReadonlyClient()`를 사용하는 경우 해당 클라이언트를 전달해야 한다.

**Step 4 — JSX 섹션 교체:**

RESEARCH.md Q1 기준 lines 811-833의 `{/* 이 지역 관련 광고 */}` 블록을 완전히 제거하고,
right rail의 `sidebarAds` 렌더링 다음에 새 섹션 삽입:

```tsx
{/* 이 단지 담당 공인중개사 (D-01, D-05) */}
{complexRealtors.length > 0 && (
  <section style={{ marginTop: 32, paddingBottom: 40 }}>
    <h2
      style={{
        font: '600 14px/1 var(--font-sans)',
        color: 'var(--fg-sec)',
        margin: '0 0 12px',
        letterSpacing: '-0.01em',
      }}
    >
      이 단지 담당 공인중개사
    </h2>
    <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
      {complexRealtors.map(r => (
        <RealtorCard key={r.id} realtor={r} />
      ))}
    </div>
  </section>
)}
```

삽입 위치: in_feed 섹션이 있던 자리 (right rail 내부, sidebarAds 다음).

**추가 확인:**
- `inFeedAds` 변수명이 파일 어디에도 남아 있지 않아야 한다
- TypeScript 오류: `Realtor` 타입 임포트가 필요한 경우 사용, 아니면 생략 가능
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 2>&1 | grep "complexes" | head -20</automated>
  </verify>
  <done>
- src/app/complexes/[id]/page.tsx에서 getActiveAds('in_feed', ...) 호출이 제거됨
- inFeedAds 변수가 파일에 존재하지 않음
- getRealtorsByComplexId import 및 호출이 추가됨
- RealtorCard 컴포넌트가 JSX에 사용됨
- `npm run lint` 오류 없음
- `npm run build` 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공개 사용자 → 단지 상세 페이지 | 인증 없이 공인중개사 정보 조회 가능 (의도된 공개 데이터) |
| tel: 링크 → 전화 앱 | 사용자 디바이스 전화 앱 호출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | getRealtorsByComplexId (공개 노출) | accept | 공인중개사 정보는 의도적 공개 디렉터리 데이터. is_active=true만 노출 (Wave 1에서 구현) |
| T-18-03-02 | Tampering | RealtorCard image_url | mitigate | `<img src={image_url}>` — Supabase Storage URL만 허용 권장. 현재 URL 입력 방식이므로 HTTPS 확인은 어드민 책임. next.config.ts에 허용 도메인 추가 권장 |
| T-18-03-03 | Spoofing | tel: URI 인젝션 | mitigate | `phone.replace(/[^0-9+]/g, '')` 로 숫자/+ 외 문자 제거 |
</threat_model>

<verification>
1. `npm run lint` 통과 — inFeedAds 관련 미사용 변수 오류 없음
2. `npm run build` 통과 — 단지 상세 페이지 서버 빌드 성공
3. `npm run test` 전체 스위트 통과 (기존 테스트 깨지지 않음)
4. 수동 확인: 공인중개사가 배정된 단지 상세 페이지에서 RealtorCard 표시
5. 수동 확인: 공인중개사가 없는 단지에서 섹션 숨김
6. 수동 확인: 전화 문의 버튼 클릭 시 tel: 링크 동작
</verification>

<success_criteria>
- 단지 상세 페이지에 '이 지역 관련 광고(in_feed)' 섹션이 완전히 없음
- 공인중개사 배정된 단지: '이 단지 담당 공인중개사' 섹션 + RealtorCard 표시
- 배정 없는 단지: 섹션 숨김 (조건부 렌더링)
- RealtorCard: 이름, 사무소명, 전화 문의 tel: 링크 정상 동작
- 이미지 없는 공인중개사: 이니셜 아바타 표시
- `npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/18-realtor-recommendation/18-03-SUMMARY.md`를 생성한다.
</output>
