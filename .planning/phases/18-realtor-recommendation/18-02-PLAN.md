---
phase: 18-realtor-recommendation
plan: "02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/admin/realtors/page.tsx
  - src/app/admin/realtors/new/page.tsx
  - src/app/admin/realtors/[id]/edit/page.tsx
  - src/components/admin/RealtorCreateForm.tsx
  - src/components/admin/RealtorEditForm.tsx
  - src/components/admin/RealtorActions.tsx
autonomous: true
requirements:
  - PHASE18-ADMIN
must_haves:
  truths:
    - "관리자가 /admin/realtors에서 전체 공인중개사 목록을 볼 수 있다"
    - "관리자가 새 공인중개사를 등록할 수 있다 (이름, 사무소명, 전화번호 필수)"
    - "관리자가 공인중개사 정보를 수정하고 is_active 상태를 변경할 수 있다"
    - "관리자가 공인중개사를 삭제할 수 있다 (확인 다이얼로그 포함)"
    - "관리자가 수정 페이지에서 공인중개사에 단지를 배정할 수 있다 (display_order 1 또는 2)"
    - "관리자가 배정된 단지를 해제할 수 있다"
    - "비어 있는 목록 화면에서 '등록된 공인중개사가 없습니다' 빈 상태 메시지가 표시된다"
  artifacts:
    - path: "src/app/admin/realtors/page.tsx"
      provides: "어드민 목록 RSC 페이지"
      contains: "getAllRealtors"
    - path: "src/app/admin/realtors/new/page.tsx"
      provides: "어드민 등록 RSC 페이지"
      contains: "RealtorCreateForm"
    - path: "src/app/admin/realtors/[id]/edit/page.tsx"
      provides: "어드민 수정 RSC 페이지"
      contains: "RealtorEditForm"
    - path: "src/components/admin/RealtorCreateForm.tsx"
      provides: "등록 폼 클라이언트 컴포넌트"
      contains: "'use client'"
    - path: "src/components/admin/RealtorEditForm.tsx"
      provides: "수정 폼 + 배정 UI 클라이언트 컴포넌트"
      contains: "assignRealtorToComplex"
    - path: "src/components/admin/RealtorActions.tsx"
      provides: "목록 페이지 삭제 버튼 클라이언트 컴포넌트"
      contains: "deleteRealtor"
  key_links:
    - from: "src/app/admin/realtors/page.tsx"
      to: "src/lib/data/realtors.ts"
      via: "getAllRealtors(adminClient)"
      pattern: "getAllRealtors"
    - from: "src/components/admin/RealtorEditForm.tsx"
      to: "src/lib/auth/realtor-actions.ts"
      via: "Server Action calls"
      pattern: "assignRealtorToComplex|removeRealtorAssignment"
---

<objective>
공인중개사 어드민 CRUD UI를 구현한다: 목록 페이지, 등록 페이지, 수정 페이지(단지 배정 인라인 포함).

Purpose: 운영자가 공인중개사를 관리하고 단지별 배정을 설정할 수 있어야 Wave 3의 단지 상세 페이지에 실제 데이터가 표시된다.
Output: 6개 파일 — RSC 라우트 3개, 클라이언트 컴포넌트 3개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-realtor-recommendation/18-CONTEXT.md
@.planning/phases/18-realtor-recommendation/18-RESEARCH.md
@.planning/phases/18-realtor-recommendation/18-PATTERNS.md
@src/app/admin/ads/page.tsx
@src/app/admin/ads/new/page.tsx
@src/app/admin/ads/[id]/edit/page.tsx
@src/components/admin/AdCreateForm.tsx
</context>

<interfaces>
<!-- Wave 1에서 생성된 인터페이스 -->

From src/lib/data/realtors.ts:
```typescript
export type Realtor = Database['public']['Tables']['realtors']['Row']
export type RealtorAssignment = Database['public']['Tables']['realtor_assignments']['Row']

export async function getAllRealtors(supabase: SupabaseClient<Database>): Promise<Realtor[]>
export async function getRealtorById(id: string, supabase: SupabaseClient<Database>): Promise<Realtor | null>
export async function getAssignmentsByRealtor(realtorId: string, supabase: SupabaseClient<Database>): Promise<RealtorAssignment[]>
```

From src/lib/auth/realtor-actions.ts:
```typescript
export async function createRealtor(formData: FormData): Promise<{ error: string | null }>
export async function updateRealtor(id: string, formData: FormData): Promise<{ error: string | null }>
export async function deleteRealtor(id: string): Promise<{ error: string | null }>
export async function assignRealtorToComplex(realtorId: string, complexId: string, displayOrder: 1 | 2): Promise<{ error: string | null }>
export async function removeRealtorAssignment(assignmentId: string, complexId: string): Promise<{ error: string | null }>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 어드민 목록 RSC 페이지 + 공통 클라이언트 컴포넌트</name>
  <files>
    src/app/admin/realtors/page.tsx,
    src/components/admin/RealtorActions.tsx
  </files>
  <action>
**src/app/admin/realtors/page.tsx:**

src/app/admin/ads/page.tsx 구조를 따른다. 핵심 포인트:

1. `export const revalidate = 0` — 어드민 페이지 캐시 비활성화
2. 권한 체크: createSupabaseServerClient() → auth.getUser() → profiles.role 검사 → redirect('/')
3. 데이터: `createSupabaseAdminClient()`로 getAllRealtors() 호출 (RLS 우회 필요)
4. 헤더: 자급자족 sticky 헤더, `관리자 · 공인중개사` 텍스트 (RESEARCH.md Q2 패턴)

테이블 컬럼: 이름 | 사무소명 | 전화번호 | 자격번호 | 상태 | 액션

상태 뱃지: is_active=true → `활성` (var(--fg-positive)), false → `비활성` (var(--fg-tertiary))

빈 목록: `등록된 공인중개사가 없습니다.`

이름 셀: `/admin/realtors/${r.id}/edit` 링크로 처리

액션 열: `<RealtorActions id={r.id} isActive={r.is_active} />`

"+ 새 중개사 등록" 버튼: `/admin/realtors/new` 링크, `background: 'var(--dj-orange)'`

BANNED 스타일: backdrop-blur, gradient-text, glow, purple/indigo 색상 금지

---

**src/components/admin/RealtorActions.tsx:**

src/components/ads/AdminCampaignActions.tsx 패턴을 따른다. `'use client'` 포함.

```typescript
'use client'

import { useTransition } from 'react'
import { deleteRealtor } from '@/lib/auth/realtor-actions'

interface Props {
  id: string
  isActive: boolean
}

export function RealtorActions({ id }: Props) {
  const [pending, startTransition] = useTransition()

  function handleDelete() {
    if (!confirm('이 공인중개사를 삭제하면 단지 배정도 모두 삭제됩니다. 삭제하시겠습니까?')) return
    startTransition(async () => {
      const result = await deleteRealtor(id)
      if (result.error) alert(result.error)
    })
  }

  return (
    <div style={{ display: 'flex', gap: 6 }}>
      <button
        className="btn btn-sm btn-ghost"
        style={{ fontSize: 11, color: '#dc2626' }}
        disabled={pending}
        onClick={handleDelete}
      >
        삭제
      </button>
    </div>
  )
}
```

주의: CLAUDE.md "Supabase 쿼리는 서버 컴포넌트 또는 API Route에서만" — RealtorActions에서 직접 DB 쿼리 절대 금지.
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 2>&1 | grep -E "src/app/admin/realtors|src/components/admin/Realtor" | head -20</automated>
  </verify>
  <done>
- src/app/admin/realtors/page.tsx 생성됨
- src/components/admin/RealtorActions.tsx 생성됨
- `npm run lint` 해당 파일 오류 없음
- /admin/realtors 라우트가 Next.js 라우팅에 인식됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 등록/수정 RSC 페이지 + 폼 컴포넌트 (배정 UI 포함)</name>
  <files>
    src/app/admin/realtors/new/page.tsx,
    src/app/admin/realtors/[id]/edit/page.tsx,
    src/components/admin/RealtorCreateForm.tsx,
    src/components/admin/RealtorEditForm.tsx
  </files>
  <action>
**src/app/admin/realtors/new/page.tsx:**

src/app/admin/ads/new/page.tsx 구조를 따른다:
- `export const revalidate = 0`
- 권한 체크 (동일 패턴)
- `<RealtorCreateForm />` 렌더링

---

**src/app/admin/realtors/[id]/edit/page.tsx:**

Next.js 15 async params 패턴 사용:
```typescript
export default async function AdminRealtorEditPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params   // CRITICAL: Next.js 15에서 params는 Promise
  // ...
  const adminClient = createSupabaseAdminClient()
  const [realtor, assignments] = await Promise.all([
    getRealtorById(id, adminClient),
    getAssignmentsByRealtor(id, adminClient),
  ])
  if (!realtor) notFound()

  return <RealtorEditForm realtor={realtor} assignments={assignments} />
}
```

서버에서 전체 단지 목록을 미리 로드해 edit form에 전달 (D-04 인라인 배정 UI):
```typescript
const { data: complexes } = await adminClient
  .from('complexes')
  .select('id, canonical_name, si, gu')
  .in('status', ['active', 'in_redevelopment'])
  .order('canonical_name')
  .limit(500)
```

컴포넌트 시그니처: `<RealtorEditForm realtor={realtor} assignments={assignments} complexes={complexes ?? []} />`

---

**src/components/admin/RealtorCreateForm.tsx:**

`'use client'` 포함. AdCreateForm.tsx 패턴을 따른다.

폼 필드:
- name (필수): 중개사 이름
- agency_name (필수): 사무소명
- phone (필수, type="tel"): 전화번호
- description (선택, textarea): 소개글
- license_no (선택): 자격번호
- image_url (선택, type="url"): 프로필 이미지 URL

submit handler:
```typescript
async function handleSubmit(formData: FormData) {
  setIsSubmitting(true)
  setSubmitError(null)
  try {
    const result = await createRealtor(formData)
    if (result.error) setSubmitError(result.error)
    else router.push('/admin/realtors')
  } catch {
    setSubmitError('등록 중 오류가 발생했습니다.')
  } finally {
    setIsSubmitting(false)
  }
}
```

---

**src/components/admin/RealtorEditForm.tsx:**

`'use client'` 포함.

Props:
```typescript
interface Props {
  realtor: Realtor
  assignments: RealtorAssignment[]
  complexes: Array<{ id: string; canonical_name: string; si: string | null; gu: string | null }>
}
```

두 섹션으로 구성:

**섹션 1 — 기본 정보 수정:**
- name, agency_name, phone, description, license_no, image_url 필드
- is_active 토글 (checkbox 또는 select)
- 저장/취소 버튼

**섹션 2 — 단지 배정 (D-04 인라인):**
현재 배정 목록 + 새 배정 추가 폼.

단지 검색: complexes 배열로 `<datalist>` 또는 client-side filter `<input>` + `<select>` 조합.
단순 구현: `<select>` + JavaScript filter로 최대 500개 단지 처리.

```tsx
{/* 현재 배정 목록 */}
<div>
  {localAssignments.map(a => (
    <div key={a.id} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '8px 0', borderBottom: '1px solid var(--line-subtle)' }}>
      <span style={{ font: '500 13px/1 var(--font-sans)' }}>
        {complexes.find(c => c.id === a.complex_id)?.canonical_name ?? a.complex_id}
        <span style={{ color: 'var(--fg-sec)', marginLeft: 8 }}>({a.display_order}번)</span>
      </span>
      <button
        type="button"
        className="btn btn-sm btn-ghost"
        style={{ fontSize: 11, color: '#dc2626' }}
        onClick={() => handleRemoveAssignment(a.id, a.complex_id)}
      >
        해제
      </button>
    </div>
  ))}
</div>

{/* 새 배정 추가 */}
<div style={{ marginTop: 12, display: 'flex', gap: 8 }}>
  <input
    type="text"
    value={complexSearch}
    onChange={e => setComplexSearch(e.target.value)}
    placeholder="단지명 검색..."
    className="input"
    style={{ flex: 1, height: 36, fontSize: 13 }}
  />
  <select
    value={selectedComplexId}
    onChange={e => setSelectedComplexId(e.target.value)}
    className="input"
    style={{ flex: 2, height: 36, fontSize: 13 }}
  >
    <option value="">단지 선택</option>
    {filteredComplexes.map(c => (
      <option key={c.id} value={c.id}>
        {c.canonical_name} ({c.gu ?? c.si ?? ''})
      </option>
    ))}
  </select>
  <select value={displayOrder} onChange={e => setDisplayOrder(Number(e.target.value) as 1 | 2)} className="input" style={{ width: 80, height: 36, fontSize: 13 }}>
    <option value={1}>1번</option>
    <option value={2}>2번</option>
  </select>
  <button type="button" className="btn btn-md btn-orange" style={{ height: 36, whiteSpace: 'nowrap', fontSize: 13 }} onClick={handleAddAssignment}>
    배정
  </button>
</div>
```

handler 구현:
```typescript
async function handleAddAssignment() {
  if (!selectedComplexId) { setAssignError('단지를 선택하세요.'); return }
  setAssignError(null)
  const result = await assignRealtorToComplex(realtor.id, selectedComplexId, displayOrder)
  if (result.error) { setAssignError(result.error); return }
  // optimistic update
  const found = complexes.find(c => c.id === selectedComplexId)
  if (found) {
    setLocalAssignments(prev => [
      ...prev.filter(a => !(a.complex_id === selectedComplexId && a.display_order === displayOrder)),
      { id: `temp-${Date.now()}`, realtor_id: realtor.id, complex_id: selectedComplexId, display_order: displayOrder, created_at: new Date().toISOString() }
    ])
  }
  setSelectedComplexId('')
}

async function handleRemoveAssignment(assignmentId: string, complexId: string) {
  if (!confirm('이 단지 배정을 해제하시겠습니까?')) return
  const result = await removeRealtorAssignment(assignmentId, complexId)
  if (result.error) { alert(result.error); return }
  setLocalAssignments(prev => prev.filter(a => a.id !== assignmentId))
}
```

filteredComplexes 계산:
```typescript
const filteredComplexes = complexSearch.trim()
  ? complexes.filter(c =>
      c.canonical_name.includes(complexSearch) ||
      (c.gu ?? '').includes(complexSearch) ||
      (c.si ?? '').includes(complexSearch)
    ).slice(0, 50)
  : complexes.slice(0, 50)
```

BANNED: backdrop-blur, gradient-text, glow, purple/indigo 색상.
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 2>&1 | grep -E "src/app/admin/realtors|src/components/admin/Realtor" | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- src/app/admin/realtors/new/page.tsx 생성됨
- src/app/admin/realtors/[id]/edit/page.tsx 생성됨 (Next.js 15 async params 포함)
- src/components/admin/RealtorCreateForm.tsx 생성됨 ('use client' 포함)
- src/components/admin/RealtorEditForm.tsx 생성됨 ('use client', 배정 UI 포함)
- 모든 파일 `npm run lint` 오류 없음
- `npm run build` RSC 컴파일 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → RSC page | 어드민 페이지 접근 요청 |
| 클라이언트 컴포넌트 → Server Action | 폼 submit, 버튼 클릭 이벤트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Elevation of Privilege | /admin/realtors/* RSC pages | mitigate | 모든 페이지 최상단에서 createSupabaseServerClient() → profiles.role 검사 → 미인증/비권한 redirect('/') |
| T-18-02-02 | Elevation of Privilege | RealtorActions (클라이언트) | mitigate | deleteRealtor Server Action 내부에서 requireAdmin() 재검증 — 클라이언트 우회 불가 |
| T-18-02-03 | Tampering | complex 배정 (selectedComplexId) | mitigate | assignRealtorToComplex Server Action에서 복합 ID 타입 검증, DB FK 제약으로 이중 방어 |
</threat_model>

<verification>
1. `npm run lint` 전체 통과 (타입 오류 없음)
2. `npm run build` 통과 (RSC 파일 서버 빌드 오류 없음)
3. /admin/realtors 페이지를 비인증 상태에서 접근 시 /login으로 redirect (수동 확인)
4. 새 중개사 등록 → 목록 페이지에 표시됨 (수동 확인)
</verification>

<success_criteria>
- /admin/realtors: 공인중개사 목록 표시, 빈 상태 메시지, 새 중개사 등록 버튼
- /admin/realtors/new: 등록 폼, 성공 시 /admin/realtors로 redirect
- /admin/realtors/[id]/edit: 기존 데이터 편집, is_active 토글, 단지 배정/해제 UI
- 비인증 접근 → /login redirect
- `npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/18-realtor-recommendation/18-02-SUMMARY.md`를 생성한다.
</output>
