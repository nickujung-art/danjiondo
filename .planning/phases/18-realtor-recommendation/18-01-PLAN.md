---
phase: 18-realtor-recommendation
plan: "01"
type: execute
wave: 1
depends_on: ["18-00"]
files_modified:
  - src/lib/data/realtors.ts
  - src/lib/auth/realtor-actions.ts
  - src/__tests__/realtors.test.ts
autonomous: true
requirements:
  - PHASE18-DATA
  - PHASE18-ACTIONS
must_haves:
  truths:
    - "getRealtorsByComplexId가 배정된 공인중개사 목록을 display_order 순으로 반환한다"
    - "getRealtorsByComplexId가 is_active=false 공인중개사를 제외한다"
    - "배정이 없는 단지에 대해 getRealtorsByComplexId가 빈 배열을 반환한다"
    - "createRealtor가 미인증 호출 시 error를 반환한다"
    - "assignRealtorToComplex가 UNIQUE 충돌 시 upsert로 처리한다"
    - "Server Action이 complex detail 캐시를 revalidate한다"
  artifacts:
    - path: "src/lib/data/realtors.ts"
      provides: "공인중개사 데이터 레이어 함수"
      exports: ["getRealtorsByComplexId", "getAllRealtors", "getRealtorById", "getAssignmentsByRealtor", "Realtor", "RealtorAssignment"]
    - path: "src/lib/auth/realtor-actions.ts"
      provides: "공인중개사 Server Actions"
      exports: ["createRealtor", "updateRealtor", "deleteRealtor", "assignRealtorToComplex", "removeRealtorAssignment"]
    - path: "src/__tests__/realtors.test.ts"
      provides: "유닛 테스트"
      contains: "getRealtorsByComplexId"
  key_links:
    - from: "src/lib/data/realtors.ts"
      to: "src/types/database.ts"
      via: "Database type import"
      pattern: "from '@/types/database'"
    - from: "src/lib/auth/realtor-actions.ts"
      to: "src/lib/supabase/admin"
      via: "createSupabaseAdminClient()"
      pattern: "requireAdmin"
---

<objective>
공인중개사 데이터 레이어 함수(`src/lib/data/realtors.ts`)와 Server Actions(`src/lib/auth/realtor-actions.ts`)를 구현하고 테스트를 작성한다.

Purpose: Wave 2(어드민 UI)와 Wave 3(단지 상세 페이지) 모두 이 레이어에 의존한다.
Output: realtors.ts, realtor-actions.ts, realtors.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-realtor-recommendation/18-CONTEXT.md
@.planning/phases/18-realtor-recommendation/18-RESEARCH.md
@.planning/phases/18-realtor-recommendation/18-PATTERNS.md
@src/lib/data/ads.ts
@src/lib/auth/ad-actions.ts
@src/__tests__/ads.test.ts
</context>

<interfaces>
<!-- 이 Plan이 의존하는 기존 인터페이스 -->

From src/types/database.ts (Wave 0에서 추가됨):
```typescript
// Database['public']['Tables']['realtors']['Row']
{
  id: string
  name: string
  agency_name: string
  phone: string
  description: string | null
  license_no: string | null
  image_url: string | null
  is_active: boolean
  created_at: string
  updated_at: string
}

// Database['public']['Tables']['realtor_assignments']['Row']
{
  id: string
  realtor_id: string
  complex_id: string
  display_order: number
  created_at: string
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient<Database>>
```

From src/lib/supabase/admin.ts:
```typescript
export function createSupabaseAdminClient(): SupabaseClient<Database>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 테스트 파일 작성 (RED 단계)</name>
  <files>src/__tests__/realtors.test.ts</files>
  <behavior>
    - Test 1: getRealtorsByComplexId — 배정된 is_active=true 공인중개사를 display_order 순으로 반환
    - Test 2: getRealtorsByComplexId — is_active=false 공인중개사는 제외
    - Test 3: getRealtorsByComplexId — 배정 없는 단지에 대해 빈 배열 반환
    - Test 4: getAllRealtors — 전체 공인중개사 목록 반환 (created_at 역순)
    - Test 5: getRealtorById — 존재하는 ID → Realtor 반환, 없는 ID → null 반환
    - Test 6: createRealtor — 미인증 호출 시 { error: '로그인이 필요합니다' } 반환
    - Test 7: deleteRealtor — 미인증 호출 시 error 반환
    - Test 8: assignRealtorToComplex — 미인증 호출 시 error 반환
  </behavior>
  <action>
ads.test.ts 패턴을 따라 테스트를 작성한다.

파일 상단 mock 설정:
```typescript
import { describe, it, expect, vi, beforeAll } from 'vitest'
import { URL_, SKEY, AKEY, admin } from './helpers/db'

vi.mock('server-only', () => ({}))
vi.mock('next/headers', () => ({
  cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
}))
vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
vi.mock('@/lib/supabase/server', () => ({
  createSupabaseServerClient: vi.fn().mockResolvedValue({
    auth: { getUser: vi.fn().mockResolvedValue({ data: { user: null } }) },
  }),
}))
```

getRealtorsByComplexId 통합 테스트는 `describe.skipIf(!SKEY)(...)` 내에 작성 (로컬 Supabase 없을 때 스킵).

미인증 가드 테스트(createRealtor, deleteRealtor, assignRealtorToComplex)는 `vi.mock('@/lib/supabase/server')` mock이 user: null을 반환하므로 skipIf 없이 작성 가능.

테스트 실행 후 실패(RED) 상태여야 한다 (구현 파일이 아직 없으므로).
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/realtors.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
- src/__tests__/realtors.test.ts 파일 생성됨
- 테스트가 "FAIL" 또는 "cannot find module" 상태 (RED 단계 확인)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 데이터 레이어 및 Server Actions 구현 (GREEN 단계)</name>
  <files>src/lib/data/realtors.ts, src/lib/auth/realtor-actions.ts</files>
  <behavior>
    - getRealtorsByComplexId: realtor_assignments JOIN realtors, is_active=true 필터, display_order ASC 정렬
    - getAllRealtors: 전체 공인중개사, created_at DESC
    - getRealtorById: maybeSingle() 사용, null 안전
    - getAssignmentsByRealtor: realtor_id로 배정 목록 조회
    - createRealtor: requireAdmin 검사, 필수 필드 검증, insert, revalidatePath
    - updateRealtor: requireAdmin 검사, 필드 업데이트, revalidatePath
    - deleteRealtor: requireAdmin 검사, hard delete, revalidatePath
    - assignRealtorToComplex: upsert onConflict:'complex_id,display_order', complexId로 단지 상세 캐시 무효화
    - removeRealtorAssignment: assignmentId로 삭제, complexId 파라미터 받아 revalidatePath
  </behavior>
  <action>
**src/lib/data/realtors.ts 구현:**

ads.ts 패턴을 그대로 따른다.

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export type Realtor = Database['public']['Tables']['realtors']['Row']
export type RealtorAssignment = Database['public']['Tables']['realtor_assignments']['Row']

export async function getRealtorsByComplexId(
  complexId: string,
  supabase: SupabaseClient<Database>,
): Promise<Realtor[]> {
  const { data } = await supabase
    .from('realtor_assignments')
    .select('display_order, realtors(*)')
    .eq('complex_id', complexId)
    .order('display_order', { ascending: true })

  return (data ?? [])
    .map(row => (row as unknown as { display_order: number; realtors: Realtor | null }).realtors)
    .filter((r): r is Realtor => r !== null && r.is_active)
}

export async function getAllRealtors(
  supabase: SupabaseClient<Database>,
): Promise<Realtor[]> {
  const { data } = await supabase
    .from('realtors')
    .select('*')
    .order('created_at', { ascending: false })
  return data ?? []
}

export async function getRealtorById(
  id: string,
  supabase: SupabaseClient<Database>,
): Promise<Realtor | null> {
  const { data } = await supabase
    .from('realtors')
    .select('*')
    .eq('id', id)
    .maybeSingle()
  return data ?? null
}

export async function getAssignmentsByRealtor(
  realtorId: string,
  supabase: SupabaseClient<Database>,
): Promise<RealtorAssignment[]> {
  const { data } = await supabase
    .from('realtor_assignments')
    .select('*')
    .eq('realtor_id', realtorId)
    .order('created_at', { ascending: true })
  return data ?? []
}
```

**is_active 필터 주의사항:** PostgREST의 외래 테이블 필터(`.eq('realtors.is_active', true)`)가 현재 Supabase 버전에서 동작하지 않을 수 있다. 따라서 JOIN 후 앱 레벨에서 `.filter((r): r is Realtor => r !== null && r.is_active)` 방식을 사용한다.

---

**src/lib/auth/realtor-actions.ts 구현:**

`'use server'` 지시어 포함. ad-actions.ts의 requireAdmin 패턴을 복사한다.

```typescript
'use server'

import { createSupabaseServerClient } from '@/lib/supabase/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { revalidatePath } from 'next/cache'

type AdminClient = ReturnType<typeof createSupabaseAdminClient>

async function requireAdmin(): Promise<{ error: string | null; admin: AdminClient | null }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다', admin: null }

  const { data: profile } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', user.id)
    .single()

  if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
    return { error: '관리자 권한이 필요합니다', admin: null }
  }

  return { error: null, admin: createSupabaseAdminClient() }
}

export async function createRealtor(
  formData: FormData,
): Promise<{ error: string | null }> {
  const { error, admin } = await requireAdmin()
  if (error || !admin) return { error: error! }

  const name = formData.get('name')
  const agencyName = formData.get('agency_name')
  const phone = formData.get('phone')
  const description = formData.get('description')
  const licenseNo = formData.get('license_no')
  const imageUrl = formData.get('image_url')

  if (
    typeof name !== 'string' || !name.trim() ||
    typeof agencyName !== 'string' || !agencyName.trim() ||
    typeof phone !== 'string' || !phone.trim()
  ) {
    return { error: '이름, 사무소명, 전화번호는 필수 항목입니다.' }
  }

  const { error: dbErr } = await admin.from('realtors').insert({
    name: name.trim(),
    agency_name: agencyName.trim(),
    phone: phone.trim(),
    description: typeof description === 'string' && description.trim() ? description.trim() : null,
    license_no: typeof licenseNo === 'string' && licenseNo.trim() ? licenseNo.trim() : null,
    image_url: typeof imageUrl === 'string' && imageUrl.trim() ? imageUrl.trim() : null,
  })

  if (dbErr) return { error: dbErr.message }
  revalidatePath('/admin/realtors')
  return { error: null }
}

export async function updateRealtor(
  id: string,
  formData: FormData,
): Promise<{ error: string | null }> {
  const { error, admin } = await requireAdmin()
  if (error || !admin) return { error: error! }

  const name = formData.get('name')
  const agencyName = formData.get('agency_name')
  const phone = formData.get('phone')
  const description = formData.get('description')
  const licenseNo = formData.get('license_no')
  const imageUrl = formData.get('image_url')
  const isActiveRaw = formData.get('is_active')

  if (
    typeof name !== 'string' || !name.trim() ||
    typeof agencyName !== 'string' || !agencyName.trim() ||
    typeof phone !== 'string' || !phone.trim()
  ) {
    return { error: '이름, 사무소명, 전화번호는 필수 항목입니다.' }
  }

  const { error: dbErr } = await admin.from('realtors').update({
    name: name.trim(),
    agency_name: agencyName.trim(),
    phone: phone.trim(),
    description: typeof description === 'string' && description.trim() ? description.trim() : null,
    license_no: typeof licenseNo === 'string' && licenseNo.trim() ? licenseNo.trim() : null,
    image_url: typeof imageUrl === 'string' && imageUrl.trim() ? imageUrl.trim() : null,
    is_active: isActiveRaw === 'true',
  }).eq('id', id)

  if (dbErr) return { error: dbErr.message }
  revalidatePath('/admin/realtors')
  revalidatePath(`/admin/realtors/${id}/edit`)
  return { error: null }
}

export async function deleteRealtor(id: string): Promise<{ error: string | null }> {
  const { error, admin } = await requireAdmin()
  if (error || !admin) return { error: error! }

  const { error: dbErr } = await admin
    .from('realtors')
    .delete()
    .eq('id', id)

  if (dbErr) return { error: dbErr.message }
  revalidatePath('/admin/realtors')
  return { error: null }
}

export async function assignRealtorToComplex(
  realtorId: string,
  complexId: string,
  displayOrder: 1 | 2,
): Promise<{ error: string | null }> {
  const { error, admin } = await requireAdmin()
  if (error || !admin) return { error: error! }

  // UNIQUE(complex_id, display_order) 충돌 시 upsert로 교체 (Pitfall 3 방지)
  const { error: dbErr } = await admin
    .from('realtor_assignments')
    .upsert(
      { realtor_id: realtorId, complex_id: complexId, display_order: displayOrder },
      { onConflict: 'complex_id,display_order' },
    )

  if (dbErr) return { error: dbErr.message }
  revalidatePath('/admin/realtors')
  // 단지 상세 페이지 캐시 무효화 (revalidate=86400 ISR 우회, Pitfall 4 방지)
  revalidatePath(`/complexes/${complexId}`)
  return { error: null }
}

export async function removeRealtorAssignment(
  assignmentId: string,
  complexId: string,
): Promise<{ error: string | null }> {
  const { error, admin } = await requireAdmin()
  if (error || !admin) return { error: error! }

  const { error: dbErr } = await admin
    .from('realtor_assignments')
    .delete()
    .eq('id', assignmentId)

  if (dbErr) return { error: dbErr.message }
  revalidatePath('/admin/realtors')
  revalidatePath(`/complexes/${complexId}`)
  return { error: null }
}
```

구현 완료 후 테스트가 GREEN이 되는지 확인한다.
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/realtors.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
- src/lib/data/realtors.ts 생성됨, getRealtorsByComplexId/getAllRealtors/getRealtorById/getAssignmentsByRealtor 모두 export
- src/lib/auth/realtor-actions.ts 생성됨, 'use server' 포함, 5개 Server Action export
- 모든 테스트 GREEN (미인증 가드 테스트 포함)
- `npm run lint` 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData → Server Action | 어드민 폼 입력이 Server Action으로 전달됨 |
| Server Action → adminClient | RLS 우회 클라이언트가 DB에 직접 접근 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Elevation of Privilege | createRealtor / updateRealtor / deleteRealtor | mitigate | requireAdmin() — profiles.role 체크, 미인증 즉시 error 반환 |
| T-18-01-02 | Tampering | assignRealtorToComplex (display_order) | mitigate | displayOrder 파라미터는 Server Action에서 `1 \| 2` 타입으로 강제, DB CHECK 제약으로 이중 방어 |
| T-18-01-03 | Information Disclosure | getRealtorsByComplexId (is_active=false 노출) | mitigate | 앱 레벨 `.filter(r => r.is_active)` 명시 — RLS SELECT USING(true)를 보완 |
</threat_model>

<verification>
1. `npm run test -- src/__tests__/realtors.test.ts` — 전체 통과
2. `npm run lint` — 타입 오류 없음
3. realtors.ts에서 export된 Realtor 타입이 Database['public']['Tables']['realtors']['Row']와 일치
</verification>

<success_criteria>
- getRealtorsByComplexId: is_active=true 필터, display_order 순 정렬, 빈 배열 안전
- createRealtor/updateRealtor/deleteRealtor: 미인증 시 error 반환 (테스트 통과)
- assignRealtorToComplex: upsert onConflict 처리, complexId로 revalidatePath 호출
- removeRealtorAssignment: complexId 파라미터 포함, 단지 상세 캐시 무효화
- `npm run test` 전체 스위트 통과 (기존 테스트 깨지지 않음)
</success_criteria>

<output>
완료 후 `.planning/phases/18-realtor-recommendation/18-01-SUMMARY.md`를 생성한다.
</output>
