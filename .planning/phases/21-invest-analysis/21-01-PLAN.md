---
phase: 21-invest-analysis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/invest.ts
  - src/components/invest/RegionalPriceChart.tsx
  - src/components/invest/RegionalPriceChartWrapper.tsx
  - src/components/invest/ComplexPriceChart.tsx
  - src/components/invest/ComplexPriceChartWrapper.tsx
autonomous: true
requirements:
  - INVEST-01
  - INVEST-02
  - INVEST-03
  - INVEST-04

must_haves:
  truths:
    - "getRegionalPriceHistory()가 sgg_code + area_bucket 필터로 지역 시세 배열을 반환한다"
    - "getComplexAreaTypes()가 단지의 거래 가능 타입 목록(tx_count >= 3)을 반환한다"
    - "getComplexPriceByType()이 단지+타입별 24개월 시세 배열을 반환한다"
    - "RegionalPriceChartWrapper는 RSC이며 dynamic(ssr:false)로 RegionalPriceChart(use client)를 래핑한다"
    - "ComplexPriceChartWrapper가 'use client' + dynamic(ssr:false)로 렌더된다"
    - "차트 색상이 24개월 첫값 대비 마지막값 기준으로 초록(상승)/빨강(하락)으로 결정된다"
  artifacts:
    - path: "src/lib/data/invest.ts"
      provides: "getRegionalPriceHistory, getComplexAreaTypes, getComplexPriceByType"
      exports: ["getRegionalPriceHistory", "getComplexAreaTypes", "getComplexPriceByType", "RegionalPricePoint", "AreaType"]
    - path: "src/components/invest/RegionalPriceChart.tsx"
      provides: "Recharts AreaChart 컬러 영역 차트 (지역 단위)"
    - path: "src/components/invest/RegionalPriceChartWrapper.tsx"
      provides: "dynamic(ssr:false) 래퍼"
    - path: "src/components/invest/ComplexPriceChart.tsx"
      provides: "Recharts AreaChart 컬러 영역 차트 (단지 단위)"
    - path: "src/components/invest/ComplexPriceChartWrapper.tsx"
      provides: "dynamic(ssr:false) 래퍼"
  key_links:
    - from: "src/lib/data/invest.ts"
      to: "invest_regional_price_history RPC"
      via: "supabase.rpc('invest_regional_price_history', ...)"
      pattern: "invest_regional_price_history"
    - from: "RegionalPriceChartWrapper"
      to: "RegionalPriceChart"
      via: "dynamic(ssr:false)"
      pattern: "dynamic.*ssr.*false"
---

<objective>
Phase 21 Wave 1-B: 데이터 레이어 + 차트 컴포넌트 (Wave 1 병렬 실행, 21-00과 독립적).

`src/lib/data/invest.ts` 데이터 함수 3종과 Recharts AreaChart 기반 차트 컴포넌트 4파일을 작성한다.
Wave 2 (21-02, 21-03)가 이 파일들을 import해 사용한다.

Purpose: 데이터 fetch 로직과 차트 렌더링을 분리하여 RSC-first 원칙을 지킨다.
Output: invest.ts (서버 전용 데이터 함수), 차트 컴포넌트 4파일
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-CONTEXT.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-RESEARCH.md

<interfaces>
<!-- 검증된 기존 패턴 -->

<!-- src/app/compare/CompareChart.tsx (검증됨) — dynamic(ssr:false) 래퍼 패턴 참조 -->
'use client'
import { Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, LineChart, Legend } from 'recharts'

<!-- src/lib/data/gap-analysis.ts (검증됨) — 서버 전용 함수 패턴 -->
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

<!-- src/lib/format.ts — formatPrice 시그니처 (검증됨) -->
// formatPrice(price: number): string  — 만원 입력 → "N억 M만" 출력
// import { formatPrice } from '@/lib/format'

<!-- src/lib/supabase/readonly.ts — createReadonlyClient() (검증됨) -->
// import { createReadonlyClient } from '@/lib/supabase/readonly'
// createReadonlyClient(): SupabaseClient<Database>

<!-- invest_regional_price_history RPC 반환값 (21-00 마이그레이션 기준) -->
// { year_month: string, avg_price: numeric, tx_count: bigint }[]

<!-- ALLOWED_SGG_CODES (gap-analysis.ts에서 검증됨) -->
const ALLOWED_SGG_CODES = ['48121', '48123', '48125', '48127', '48128', '48129', '48250']
const ALLOWED_AREA_BUCKETS = ['소형', '59', '84', '대형'] as const
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: src/lib/data/invest.ts — 서버 전용 데이터 함수 3종</name>
  <files>src/lib/data/invest.ts</files>
  <read_first>
    - src/lib/data/gap-analysis.ts (서버 전용 함수 패턴, SupabaseClient 타입 사용법)
    - src/lib/format.ts (formatPrice 확인)
  </read_first>
  <action>
`src/lib/data/invest.ts`를 신규 생성한다. `'server-only'` 지시어로 시작해 클라이언트 번들에서 완전히 제외한다.

**타입 정의:**
```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export const ALLOWED_SGG_CODES = ['48121', '48123', '48125', '48127', '48128', '48129', '48250'] as const
export const ALLOWED_AREA_BUCKETS = ['소형', '59', '84', '대형'] as const
export type AreaBucket = typeof ALLOWED_AREA_BUCKETS[number]

export interface RegionalPricePoint {
  yearMonth: string
  avgPrice:  number
  txCount:   number
}

export interface AreaType {
  bucket:  AreaBucket
  txCount: number
}
```

**함수 1: getRegionalPriceHistory**
```typescript
export async function getRegionalPriceHistory(
  supabase:    SupabaseClient<Database>,
  sggCode:     string | undefined,
  areaBucket:  AreaBucket | undefined,
  months = 24,
): Promise<RegionalPricePoint[]> {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase as any).rpc('invest_regional_price_history', {
    p_sgg_code:    sggCode    ?? null,
    p_area_bucket: areaBucket ?? null,
    p_months:      months,
  })
  if (error || !data) return []
  return (data as Array<{ year_month: string; avg_price: number; tx_count: number }>).map(r => ({
    yearMonth: r.year_month,
    avgPrice:  Number(r.avg_price),
    txCount:   Number(r.tx_count),
  }))
}
```

**함수 2: getComplexAreaTypes** — 단지의 tx_count >= 3 타입 목록

NOTE: 함수는 버킷 정의로 시작하고, transactions 직접 쿼리를 사용한다. 결과가 없는 경우 빈 배열을 반환한다. 초기에 결과를 버리는 RPC 호출 블록을 포함하지 않는다.

```typescript
export async function getComplexAreaTypes(
  supabase:   SupabaseClient<Database>,
  complexId:  string,
  months = 24,
): Promise<AreaType[]> {
  const buckets: AreaBucket[] = ['소형', '59', '84', '대형']
  const cutoff = new Date()
  cutoff.setMonth(cutoff.getMonth() - months)
  const cutoffStr = cutoff.toISOString().slice(0, 10)

  const result: AreaType[] = []
  for (const bucket of buckets) {
    const bucketFilter = bucket === '소형'
      ? { lt: 50 }
      : bucket === '59'
      ? { gte: 50, lt: 66 }
      : bucket === '84'
      ? { gte: 66, lt: 95 }
      : { gte: 95 }

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    let q = (supabase as any)
      .from('transactions')
      .select('id', { count: 'exact', head: true })
      .eq('complex_id', complexId)
      .eq('deal_type', 'sale')
      .gte('deal_date', cutoffStr)
      .is('cancel_date', null)
      .is('superseded_by', null)

    if ('lt' in bucketFilter && 'gte' in bucketFilter) {
      q = q.gte('area_m2', (bucketFilter as { gte: number; lt: number }).gte)
        .lt('area_m2', (bucketFilter as { gte: number; lt: number }).lt)
    } else if ('lt' in bucketFilter) {
      q = q.lt('area_m2', (bucketFilter as { lt: number }).lt)
    } else {
      q = q.gte('area_m2', (bucketFilter as { gte: number }).gte)
    }

    const { count } = await q
    if ((count ?? 0) >= 3) {
      result.push({ bucket, txCount: count ?? 0 })
    }
  }
  return result
}
```

**함수 3: getComplexPriceByType** — 단지+타입별 시세 배열
```typescript
export async function getComplexPriceByType(
  supabase:    SupabaseClient<Database>,
  complexId:   string,
  areaBucket:  AreaBucket | undefined,
  months = 24,
): Promise<RegionalPricePoint[]> {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase as any).rpc('invest_price_history', {
    p_complex_id:  complexId,
    p_deal_type:   'sale',
    p_months:      months,
    p_area_bucket: areaBucket ?? null,
  })
  if (error || !data) return []
  return (data as Array<{ year_month: string; avg_price: number; tx_count: number }>).map(r => ({
    yearMonth: r.year_month,
    avgPrice:  Number(r.avg_price),
    txCount:   Number(r.tx_count),
  }))
}
```

**주의사항:**
- `'server-only'` 반드시 첫 줄에 (클라이언트 번들 유입 방지)
- getComplexAreaTypes의 직접 supabase 쿼리: `cancel_date IS null`, `superseded_by IS null`  
- 모든 함수는 error 시 빈 배열 반환 (절대 throw하지 않음 — 페이지 레벨에서 `.catch(() => [])`)
  </action>
  <verify>
    <automated>grep -c "server-only" "C:/Users/jung/coding/bds/src/lib/data/invest.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `import 'server-only'`
    - `grep -c "getRegionalPriceHistory\|getComplexAreaTypes\|getComplexPriceByType" src/lib/data/invest.ts` returns 6 (export + definition each)
    - `grep -c "cancel_date\|superseded_by" src/lib/data/invest.ts` returns >= 2
    - `grep "formatPrice" src/lib/data/invest.ts` returns nothing (서버 함수에서 formatPrice 사용 금지)
    - `getComplexAreaTypes` function body starts directly with `const buckets` — no dead RPC call block before it
    - `npm run lint` passes on this file (no TypeScript errors)
  </acceptance_criteria>
  <done>invest.ts가 존재하고 3개 함수가 export되며, 'server-only' 지시어와 cancel_date 필터가 존재한다. getComplexAreaTypes는 죽은 RPC 블록 없이 buckets 정의로 시작한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Recharts AreaChart 차트 컴포넌트 4파일 작성</name>
  <files>
    src/components/invest/RegionalPriceChart.tsx,
    src/components/invest/RegionalPriceChartWrapper.tsx,
    src/components/invest/ComplexPriceChart.tsx,
    src/components/invest/ComplexPriceChartWrapper.tsx
  </files>
  <read_first>
    - src/app/compare/CompareChart.tsx (Recharts 패턴, dynamic ssr:false 패턴)
    - src/lib/format.ts (formatPrice 시그니처)
  </read_first>
  <action>
4개 파일을 작성한다. 지역 차트와 단지 차트는 동일한 차트 컴포넌트 구조를 공유한다.

**파일 1: src/components/invest/RegionalPriceChart.tsx**
```tsx
'use client'
import {
  AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer
} from 'recharts'
import type { RegionalPricePoint } from '@/lib/data/invest'

// 차트 내부 전용 로컬 포맷 함수 (server-only import 금지)
function fmtPrice(v: unknown): string {
  const n = Number(v)
  if (n >= 10000) return `${(n / 10000).toFixed(1)}억`
  return `${n.toLocaleString()}만`
}

interface RegionalPriceChartProps {
  data:  RegionalPricePoint[]
  title: string
}

export function RegionalPriceChart({ data, title }: RegionalPriceChartProps) {
  if (data.length < 2) {
    return (
      <div style={{ padding: '32px 0', textAlign: 'center', color: 'var(--fg-tertiary)', font: '500 13px/1.4 var(--font-sans)' }}>
        데이터가 부족합니다 (거래 3건 미만)
      </div>
    )
  }

  const first    = data[0]?.avgPrice ?? 0
  const last     = data[data.length - 1]?.avgPrice ?? 0
  const isRising = last >= first
  const color    = isRising ? '#16a34a' : '#dc2626'
  const gradId   = `priceGrad-${title.replace(/\s/g, '')}`

  return (
    <div>
      {title && (
        <p style={{ font: '500 12px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 8px' }}>
          {title}
        </p>
      )}
      <ResponsiveContainer width="100%" height={220}>
        <AreaChart data={data} margin={{ top: 4, right: 16, left: 8, bottom: 4 }}>
          <defs>
            <linearGradient id={gradId} x1="0" y1="0" x2="0" y2="1">
              <stop offset="5%"  stopColor={color} stopOpacity={0.25} />
              <stop offset="95%" stopColor={color} stopOpacity={0.03} />
            </linearGradient>
          </defs>
          <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
          <XAxis
            dataKey="yearMonth"
            tick={{ fontSize: 11 }}
            tickFormatter={(v: string) => v.slice(2)}
            interval="preserveStartEnd"
          />
          <YAxis tick={{ fontSize: 11 }} tickFormatter={fmtPrice} width={60} />
          <Tooltip
            formatter={(v) => [fmtPrice(v), '월평균']}
            labelFormatter={(l) => String(l)}
            contentStyle={{ fontSize: 12 }}
          />
          <Area
            type="monotone"
            dataKey="avgPrice"
            stroke={color}
            strokeWidth={2}
            fill={`url(#${gradId})`}
            dot={false}
            activeDot={{ r: 4 }}
          />
        </AreaChart>
      </ResponsiveContainer>
    </div>
  )
}
```

**파일 2: src/components/invest/RegionalPriceChartWrapper.tsx**
```tsx
import dynamic from 'next/dynamic'
import type { RegionalPricePoint } from '@/lib/data/invest'

const RegionalPriceChart = dynamic(
  () => import('./RegionalPriceChart').then(m => m.RegionalPriceChart),
  {
    ssr:     false,
    loading: () => (
      <div style={{ height: 220, background: 'var(--bg-surface-2)', borderRadius: 6, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>차트 로딩 중…</span>
      </div>
    ),
  }
)

interface Props {
  data:  RegionalPricePoint[]
  title: string
}

export function RegionalPriceChartWrapper({ data, title }: Props) {
  return <RegionalPriceChart data={data} title={title} />
}
```

**파일 3: src/components/invest/ComplexPriceChart.tsx**
- RegionalPriceChart.tsx와 동일한 구조. props 이름만 다름:
```tsx
'use client'
// ... (동일한 AreaChart 구현)
interface ComplexPriceChartProps {
  data:  RegionalPricePoint[]  // import from '@/lib/data/invest'
  title: string
}
export function ComplexPriceChart({ data, title }: ComplexPriceChartProps) {
  // RegionalPriceChart와 동일한 구현
  // gradId = `complexPriceGrad-${title.replace(/\s/g, '')}`
}
```

**파일 4: src/components/invest/ComplexPriceChartWrapper.tsx**
- RegionalPriceChartWrapper.tsx와 동일한 구조. ComplexPriceChart를 import.

**공통 규칙 (AI 슬롭 금지 — CLAUDE.md, D-08):**
- backdrop-blur, gradient-text, glow, 보라/인디고 없음
- linearGradient는 상승(초록 #16a34a) / 하락(빨강 #dc2626) 두 가지만
- 색상 결정: `isRising = data[last].avgPrice >= data[0].avgPrice` (24개월 첫값 대비 마지막값)
- 'use client'는 RegionalPriceChart.tsx, ComplexPriceChart.tsx에만 (Wrapper는 일반 서버 컴포넌트)
  </action>
  <verify>
    <automated>grep -c "use client" "C:/Users/jung/coding/bds/src/components/invest/RegionalPriceChart.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ssr.*false" src/components/invest/RegionalPriceChartWrapper.tsx` returns 1
    - `grep -c "ssr.*false" src/components/invest/ComplexPriceChartWrapper.tsx` returns 1
    - `grep -c "'use client'" src/components/invest/RegionalPriceChart.tsx` returns 1
    - `grep -c "'use client'" src/components/invest/ComplexPriceChart.tsx` returns 1
    - `grep "'use client'" src/components/invest/RegionalPriceChartWrapper.tsx` returns nothing (서버 컴포넌트)
    - `grep "backdrop-blur\|gradient-text\|glow\|indigo\|violet\|purple" src/components/invest/*.tsx` returns nothing
    - `npm run build` passes (Recharts SSR 오류 없음)
  </acceptance_criteria>
  <done>4개 차트 파일이 존재하고, Wrapper 2개는 dynamic(ssr:false) 패턴을 사용하며, 'use client'는 Chart 컴포넌트에만 존재한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → 차트 컴포넌트 | 서버에서 fetch한 데이터를 props로 전달 |
| 'use client' 경계 | RegionalPriceChart, ComplexPriceChart만 클라이언트 컴포넌트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | server-only 누락 | mitigate | invest.ts 첫 줄 `import 'server-only'` — 클라이언트 번들 유입 시 빌드 오류 발생 |
| T-21-05 | Tampering | 차트 데이터 조작 | accept | 공개 통계 데이터, 조작 시 UI 렌더만 영향; 저가치 위협 |
</threat_model>

<verification>
```bash
# 파일 존재 확인
ls src/lib/data/invest.ts
ls src/components/invest/RegionalPriceChart.tsx
ls src/components/invest/RegionalPriceChartWrapper.tsx
ls src/components/invest/ComplexPriceChart.tsx
ls src/components/invest/ComplexPriceChartWrapper.tsx

# server-only 확인
grep "server-only" src/lib/data/invest.ts

# ssr:false 확인
grep "ssr.*false" src/components/invest/RegionalPriceChartWrapper.tsx
grep "ssr.*false" src/components/invest/ComplexPriceChartWrapper.tsx

# AI 슬롭 없음 확인
grep -r "backdrop-blur\|gradient-text\|glow\|indigo\|violet" src/components/invest/

# 빌드 성공
npm run build
```
</verification>

<success_criteria>
1. `src/lib/data/invest.ts`가 'server-only'로 시작하고 3개 함수를 export한다
2. `getRegionalPriceHistory`가 invest_regional_price_history RPC를 호출한다
3. `getComplexAreaTypes`가 transactions 직접 쿼리로 cancel_date/superseded_by 필터를 포함한다 (죽은 RPC 블록 없음)
4. `getComplexPriceByType`이 invest_price_history RPC를 호출한다
5. RegionalPriceChart.tsx + ComplexPriceChart.tsx: 'use client', AreaChart, linearGradient 포함
6. RegionalPriceChartWrapper.tsx + ComplexPriceChartWrapper.tsx: dynamic(ssr:false) 포함, 'use client' 없음
7. 차트 색상은 #16a34a (상승) / #dc2626 (하락) 두 가지만 사용
8. AI 슬롭 없음 (backdrop-blur, gradient-text, glow, 보라/인디고 없음)
9. `npm run build` 통과
</success_criteria>

<output>
After completion, create `.planning/phases/21-invest-analysis/21-01-SUMMARY.md`
</output>
