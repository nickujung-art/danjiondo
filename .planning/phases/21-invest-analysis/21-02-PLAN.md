---
phase: 21-invest-analysis
plan: "02"
type: execute
wave: 2
depends_on:
  - "21-00"
  - "21-01"
files_modified:
  - src/app/invest/page.tsx
autonomous: true
requirements:
  - INVEST-01
  - INVEST-02
  - INVEST-03
  - INVEST-04

must_haves:
  truths:
    - "/invest 페이지가 존재하고 상단에 지역별 시세 흐름 AreaChart가 표시된다"
    - "타입 탭(전체|59㎡|84㎡)이 URL searchParam area_type으로 동작한다 (D-03/D-09: 정확히 3개 탭)"
    - "지역 필터 탭(전체|창원 의창|...| 김해)이 URL searchParam sgg_code로 동작한다"
    - "하단에 갭투자 랭킹 테이블이 표시되고 단지 클릭 시 /complexes/[id]로 이동한다"
    - "법적 면책 문구가 페이지에 존재한다"
    - "ISR revalidate = 3600이 설정되어 있다"
    - "/gap-analysis는 /invest로 301 redirect된다"
  artifacts:
    - path: "src/app/invest/page.tsx"
      provides: "투자 분석 통합 RSC 페이지"
      contains: "export const revalidate = 3600"
    - path: "src/app/invest/page.tsx"
      contains: "투자 결정에 직접 활용하지 마세요"
  key_links:
    - from: "src/app/invest/page.tsx"
      to: "getRegionalPriceHistory"
      via: "import from @/lib/data/invest"
      pattern: "getRegionalPriceHistory"
    - from: "src/app/invest/page.tsx"
      to: "getGapRankings"
      via: "import from @/lib/data/gap-analysis"
      pattern: "getGapRankings"
    - from: "src/app/invest/page.tsx"
      to: "RegionalPriceChartWrapper"
      via: "props 전달"
      pattern: "RegionalPriceChartWrapper"
---

<objective>
Phase 21 Wave 2-A: /invest 통합 RSC 페이지.

기존 `/gap-analysis/page.tsx`의 필터 탭 + 갭 랭킹 테이블 패턴을 이식하고,
상단에 지역 시세 흐름 차트 섹션을 추가한다.

Purpose: D-03 결정 — 상단 차트 + 하단 랭킹 테이블 통합 투자 분석 페이지.
Output: src/app/invest/page.tsx (신규 RSC 페이지)
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-CONTEXT.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-RESEARCH.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-01-SUMMARY.md

<interfaces>
<!-- 재사용 기존 코드 (gap-analysis/page.tsx에서 직접 검증됨) -->

// ─── allowlists ───────────────────────────────────────────────────────────
const ALLOWED_SGG_CODES = ['48121', '48123', '48125', '48127', '48128', '48129', '48250']
const ALLOWED_RISK_LEVELS = ['safe', 'caution', 'danger'] as const
const ALLOWED_AREA_TYPES  = ['소형', '59', '84', '대형'] as const   // 신규 (입력 검증용)

// ─── SGG 레이블 (기존 동일) ────────────────────────────────────────────────
const SGG_LABEL: Record<string, string> = {
  '48121': '창원 의창구', '48123': '창원 창원구', '48125': '창원 성산구',
  '48127': '창원 마산합포구', '48128': '창원 마산회원구', '48129': '창원 진해구',
  '48250': '김해시',
}

// ─── getGapRankings 시그니처 (gap-analysis.ts에서 검증됨) ─────────────────
export async function getGapRankings(
  filter: GapRankingFilter,     // { sggCode?: string; riskLevel?: 'safe'|'caution'|'danger' }
  supabase: SupabaseClient<Database>,
): Promise<GapRankingRow[]>    // 갭 비율 내림차순 최대 200건

// ─── getRegionalPriceHistory 시그니처 (21-01 invest.ts) ───────────────────
export async function getRegionalPriceHistory(
  supabase: SupabaseClient<Database>,
  sggCode: string | undefined,
  areaBucket: AreaBucket | undefined,
  months?: number,
): Promise<RegionalPricePoint[]>   // { yearMonth: string; avgPrice: number; txCount: number }[]

// ─── RegionalPriceChartWrapper props (21-01) ──────────────────────────────
// interface Props { data: RegionalPricePoint[]; title: string }

// ─── formatPrice (src/lib/format.ts — 검증됨) ────────────────────────────
import { formatPrice } from '@/lib/format'
// formatPrice(price: number): string → "N억 M만"

// ─── filterTab 헬퍼 (gap-analysis 패턴 이식, URL만 /invest로 변경) ─────────
function filterTab(
  key: string, value: string,
  currentSgg: string | undefined, currentRisk: string | undefined,
  currentArea: string | undefined,  // 신규 파라미터
): string
// → `/invest?sgg_code=...&risk_level=...&area_type=...`

// ─── RISK_BADGE (기존 동일) ───────────────────────────────────────────────
const RISK_BADGE: Record<'safe'|'caution'|'danger', { bg: string; label: string }> = {
  safe:    { bg: '#16a34a', label: '안전' },
  caution: { bg: '#d97706', label: '주의' },
  danger:  { bg: '#dc2626', label: '위험' },
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: src/app/invest/page.tsx — 투자 분석 통합 RSC 페이지</name>
  <files>src/app/invest/page.tsx</files>
  <read_first>
    - src/app/gap-analysis/page.tsx (필터 탭 + 테이블 전체 패턴 이식 대상)
    - src/lib/data/invest.ts (21-01에서 생성한 getRegionalPriceHistory 시그니처)
    - src/components/invest/RegionalPriceChartWrapper.tsx (21-01에서 생성한 Wrapper props)
    - src/lib/format.ts (formatPrice — 반드시 여기서 import, 로컬 복사 금지)
  </read_first>
  <action>
`src/app/invest/page.tsx`를 신규 생성한다. 기존 `/gap-analysis/page.tsx`를 베이스로,
상단 시세 차트 섹션을 추가하고 URL을 `/invest`로 변경한다.

**중요 규칙:**
1. `export const revalidate = 3600` — ISR 1시간 (쿠키 사용 금지)
2. `createReadonlyClient()` — Supabase 쿼리 (cookies() 없음)
3. `import { formatPrice } from '@/lib/format'` — 로컬 복사 절대 금지
4. filterTab URL: `/invest` (기존 `/gap-analysis` 모두 교체)
5. `area_type` searchParam 신규 추가 (기존 sgg_code + risk_level에 추가)

**페이지 구조:**
```tsx
import type { Metadata } from 'next'
import Link from 'next/link'
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getGapRankings } from '@/lib/data/gap-analysis'
import type { GapRankingRow } from '@/lib/data/gap-analysis'
import { getRegionalPriceHistory, ALLOWED_SGG_CODES, ALLOWED_AREA_BUCKETS } from '@/lib/data/invest'
import type { AreaBucket, RegionalPricePoint } from '@/lib/data/invest'
import { RegionalPriceChartWrapper } from '@/components/invest/RegionalPriceChartWrapper'
import { formatPrice } from '@/lib/format'

export const revalidate = 3600

export const metadata: Metadata = {
  title: '투자 분석 — 창원·김해 | 단지온도',
  description: '창원·김해 아파트 지역별 시세 흐름 + 갭투자 위험도 랭킹. 실거래 데이터 기반 참고 지수.',
}

interface Props {
  searchParams: Promise<{ sgg_code?: string; risk_level?: string; area_type?: string }>
}

// ─── allowlists ──────────────────────────────────────────────────────────
// ALLOWED_SGG_CODES는 @/lib/data/invest에서 import (기존 gap-analysis 로컬 상수 제거)
const ALLOWED_RISK_LEVELS = ['safe', 'caution', 'danger'] as const
type AllowedRiskLevel = typeof ALLOWED_RISK_LEVELS[number]

// ─── 레이블 ───────────────────────────────────────────────────────────────
const SGG_LABEL: Record<string, string> = { /* 기존 동일 */ }

const REGION_OPTIONS = [{ label: '전체', value: '' }, ...ALLOWED_SGG_CODES.map(c => ({ label: SGG_LABEL[c] ?? c, value: c }))]
const RISK_OPTIONS = [{ label: '전체', value: '' }, { label: '안전', value: 'safe' }, { label: '주의', value: 'caution' }, { label: '위험', value: 'danger' }]

// ─── D-03/D-09: 타입 탭은 정확히 3개만 (전체|59㎡|84㎡) ──────────────────
// ALLOWED_AREA_BUCKETS에는 4값이 있지만 표시 탭은 D-03/D-09에 의해 3개로 고정
// 입력 검증(areaBucket allowlist)은 여전히 ALLOWED_AREA_BUCKETS 사용
const AREA_OPTIONS = [
  { label: '전체', value: '' },
  { label: '59㎡', value: '59' },
  { label: '84㎡', value: '84' },
] as const

// ─── filterTab 헬퍼 (URL = /invest) ──────────────────────────────────────
function filterTab(
  key: string, value: string,
  currentSgg: string | undefined,
  currentRisk: string | undefined,
  currentArea: string | undefined,
): string {
  const p = new URLSearchParams()
  if (key !== 'sgg_code'   && currentSgg)  p.set('sgg_code',   currentSgg)
  if (key !== 'risk_level' && currentRisk) p.set('risk_level', currentRisk)
  if (key !== 'area_type'  && currentArea) p.set('area_type',  currentArea)
  if (value) p.set(key, value)
  const s = p.toString()
  return `/invest${s ? `?${s}` : ''}`
}

// ─── RSC 컴포넌트 ─────────────────────────────────────────────────────────
export default async function InvestPage({ searchParams }: Props) {
  const params     = await searchParams
  const rawSgg     = typeof params.sgg_code   === 'string' ? params.sgg_code   : ''
  const rawRisk    = typeof params.risk_level === 'string' ? params.risk_level : ''
  const rawArea    = typeof params.area_type  === 'string' ? params.area_type  : ''
  const sggCode    = (ALLOWED_SGG_CODES as ReadonlyArray<string>).includes(rawSgg) ? rawSgg : undefined
  const riskLevel  = (ALLOWED_RISK_LEVELS as ReadonlyArray<string>).includes(rawRisk)
    ? (rawRisk as AllowedRiskLevel) : undefined
  // 입력 검증은 ALLOWED_AREA_BUCKETS (4값) 사용 — 탭 표시(AREA_OPTIONS)와 별개
  const areaBucket = (ALLOWED_AREA_BUCKETS as ReadonlyArray<string>).includes(rawArea)
    ? (rawArea as AreaBucket) : undefined

  const supabase = createReadonlyClient()

  // 병렬 fetch (Promise.all)
  const [priceHistory, rows] = await Promise.all([
    getRegionalPriceHistory(supabase, sggCode, areaBucket, 24).catch((): RegionalPricePoint[] => []),
    getGapRankings({ sggCode, riskLevel }, supabase).catch((): GapRankingRow[] => []),
  ])

  // 탭 active 헬퍼
  function isSggActive(v: string) { return v === '' ? !sggCode  : sggCode === v }
  function isRiskActive(v: string) { return v === '' ? !riskLevel : riskLevel === v }
  function isAreaActive(v: string) { return v === '' ? !areaBucket : areaBucket === v }

  // 차트 제목
  const regionLabel = sggCode ? (SGG_LABEL[sggCode] ?? sggCode) : '창원·김해 전체'
  const areaLabel   = areaBucket ? (areaBucket === '59' ? '59㎡' : areaBucket === '84' ? '84㎡' : areaBucket) : '전체 타입'
  const chartTitle  = `${regionLabel} ${areaLabel} 아파트 매매 시세 흐름 (최근 24개월)`

  const tabStyle = (active: boolean): React.CSSProperties => ({
    display: 'inline-block', padding: '5px 12px', borderRadius: 6,
    font: '500 12px/1 var(--font-sans)', textDecoration: 'none',
    background: active ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
    color: active ? '#fff' : 'var(--fg-sec)',
    border: '1px solid var(--line-subtle)', whiteSpace: 'nowrap' as const,
  })

  return (
    <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
      {/* Header — 동일 패턴 */}
      <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)', display: 'flex', alignItems: 'center', padding: '0 32px', gap: 16, position: 'sticky', top: 0, zIndex: 50 }}>
        <Link href="/" className="dj-logo">
          <span className="mark">단</span>
          <span>단지온도</span>
        </Link>
        <span style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
          투자 분석
        </span>
      </header>

      <main style={{ maxWidth: 1040, margin: '0 auto', padding: '24px 32px' }}>
        {/* 페이지 제목 */}
        <div style={{ marginBottom: 20 }}>
          <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 6px' }}>
            투자 분석
          </h1>
          <p style={{ font: '500 13px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
            실거래 흐름 기반 참고 지수 — 창원·김해 아파트 시세 흐름 + 갭투자 위험도
          </p>
        </div>

        {/* ─── 지역 필터 탭 ─────────────────────────────────────────────── */}
        <div style={{ display: 'flex', gap: 4, flexWrap: 'wrap', marginBottom: 8 }}>
          {REGION_OPTIONS.map(opt => (
            <Link key={`sgg-${opt.value}`}
              href={filterTab('sgg_code', opt.value, sggCode, riskLevel, areaBucket)}
              style={tabStyle(isSggActive(opt.value))}>
              {opt.label}
            </Link>
          ))}
        </div>

        {/* ─── 시세 흐름 차트 섹션 ──────────────────────────────────────── */}
        <div className="card" style={{ padding: 20, marginBottom: 24 }}>
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 12, flexWrap: 'wrap', gap: 8 }}>
            <h2 style={{ font: '700 15px/1.4 var(--font-sans)', margin: 0 }}>시세 흐름</h2>
            {/* 타입 탭 — D-03/D-09: 전체|59㎡|84㎡ 3개 고정 */}
            <div style={{ display: 'flex', gap: 4 }}>
              {AREA_OPTIONS.map(opt => (
                <Link key={`area-${opt.value}`}
                  href={filterTab('area_type', opt.value, sggCode, riskLevel, areaBucket)}
                  style={tabStyle(isAreaActive(opt.value))}>
                  {opt.label}
                </Link>
              ))}
            </div>
          </div>

          <RegionalPriceChartWrapper data={priceHistory} title={chartTitle} />

          {/* 법적 면책 문구 (D-01) */}
          <p style={{ font: '400 11px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '12px 0 0' }}>
            * 본 데이터는 국토교통부 실거래가 공개시스템 기반입니다.<br />
            * 투자 결정에 직접 활용하지 마세요. 부동산 전문가와 상담하시기 바랍니다.
          </p>
        </div>

        {/* ─── 갭투자 랭킹 섹션 ────────────────────────────────────────── */}
        <div style={{ marginBottom: 16 }}>
          <h2 style={{ font: '700 18px/1.3 var(--font-sans)', letterSpacing: '-0.01em', margin: '0 0 12px' }}>
            갭투자 랭킹
          </h2>
          {/* 위험도 필터 */}
          <div style={{ display: 'flex', gap: 4 }}>
            {RISK_OPTIONS.map(opt => (
              <Link key={`risk-${opt.value}`}
                href={filterTab('risk_level', opt.value, sggCode, riskLevel, areaBucket)}
                style={tabStyle(isRiskActive(opt.value))}>
                {opt.label}
              </Link>
            ))}
          </div>
        </div>

        <p style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 12 }}>
          {rows.length}개 단지
        </p>

        {/* 갭투자 랭킹 테이블 — 기존 gap-analysis 패턴 완전 이식 */}
        {rows.length === 0 ? (
          <div className="card" style={{ padding: 48, textAlign: 'center' }}>
            <p style={{ font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
              갭투자 통계 데이터가 아직 없습니다.
            </p>
            <p style={{ font: '400 12px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '8px 0 0' }}>
              일배치 cron이 실행된 후 데이터가 표시됩니다.
            </p>
          </div>
        ) : (
          <div className="card" style={{ overflow: 'hidden' }}>
            <table style={{ width: '100%', borderCollapse: 'collapse' }}>
              <thead>
                <tr style={{ background: 'var(--bg-surface-2)', borderBottom: '1px solid var(--line-default)' }}>
                  {['#', '단지명', '갭 비율', '갭 금액', '전세가율', '위험도', '매매 건수'].map(h => (
                    <th key={h} style={{ padding: '10px 12px', font: '600 11px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: h === '#' || h === '매매 건수' ? 'center' : 'left', whiteSpace: 'nowrap' }}>
                      {h}
                    </th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {rows.map((row, idx) => {
                  const RISK_BADGE = {
                    safe:    { bg: '#16a34a', label: '안전' },
                    caution: { bg: '#d97706', label: '주의' },
                    danger:  { bg: '#dc2626', label: '위험' },
                  }
                  const badge = RISK_BADGE[row.riskLevel] ?? RISK_BADGE['caution']
                  const ratioColor = row.riskLevel === 'danger' ? '#dc2626' : row.riskLevel === 'caution' ? '#d97706' : '#16a34a'
                  return (
                    <tr key={row.complexId} style={{ borderBottom: '1px solid var(--line-subtle)' }}>
                      <td style={{ padding: '10px 12px', font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)', textAlign: 'center', width: 36 }}>{idx + 1}</td>
                      <td style={{ padding: '10px 12px' }}>
                        {/* D-06: 단지 클릭 → /complexes/[id] (기존 상세 페이지) */}
                        <Link href={`/complexes/${row.complexId}`}
                          style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none' }}>
                          {row.complexName}
                        </Link>
                        {(row.si ?? row.gu) && (
                          <div style={{ font: '400 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginTop: 2 }}>
                            {[row.si, row.gu].filter(Boolean).join(' ')}
                          </div>
                        )}
                      </td>
                      <td className="tnum" style={{ padding: '10px 12px', font: '700 14px/1 var(--font-sans)', color: ratioColor }}>
                        {row.gapRatio.toFixed(1)}%
                      </td>
                      <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)' }}>
                        {formatPrice(row.gapAmount)}
                      </td>
                      <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
                        {row.jeonseRatio.toFixed(1)}%
                      </td>
                      <td style={{ padding: '10px 12px' }}>
                        <span style={{ display: 'inline-flex', alignItems: 'center', background: badge.bg, color: '#fff', font: '600 11px/1 var(--font-sans)', borderRadius: 4, padding: '3px 8px', whiteSpace: 'nowrap' }}>
                          {badge.label}
                        </span>
                      </td>
                      <td className="tnum" style={{ padding: '10px 12px', font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)', textAlign: 'center' }}>
                        {row.saleCount}건
                      </td>
                    </tr>
                  )
                })}
              </tbody>
            </table>
          </div>
        )}

        {/* 하단 데이터 출처 */}
        <p style={{ font: '400 11px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', marginTop: 12 }}>
          국토부 실거래 기준 · 매매·전세 각 3건 이상 단지만 표시 · 매일 새벽 갱신
        </p>
      </main>
    </div>
  )
}
```

**금지 사항 (AI 슬롭, D-08):**
- backdrop-blur, gradient-text, glow, 보라/인디고 색상 금지
- formatPrice 로컬 정의 금지 — 반드시 `import { formatPrice } from '@/lib/format'`
- `'use client'` 추가 금지 — 이 파일은 RSC
- cookies() 호출 금지 (revalidate 비활성화됨)
  </action>
  <verify>
    <automated>grep -c "revalidate = 3600" "C:/Users/jung/coding/bds/src/app/invest/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "revalidate = 3600" src/app/invest/page.tsx` returns 1
    - `grep -c "createReadonlyClient" src/app/invest/page.tsx` returns 1
    - `grep -c "투자 결정에 직접 활용하지 마세요" src/app/invest/page.tsx` returns 1
    - `grep -c "RegionalPriceChartWrapper" src/app/invest/page.tsx` returns >= 1
    - `grep -c "getGapRankings" src/app/invest/page.tsx` returns >= 1
    - `grep -c "getRegionalPriceHistory" src/app/invest/page.tsx` returns >= 1
    - `grep "formatPrice" src/app/invest/page.tsx | grep -v "import"` — only import line, no local definition
    - `grep "'use client'" src/app/invest/page.tsx` returns nothing
    - File contains `/complexes/${row.complexId}` (D-06)
    - AREA_OPTIONS contains exactly 3 entries (전체|59㎡|84㎡) — verify with `grep -c "AREA_OPTIONS" src/app/invest/page.tsx` and inspect the literal array
    - `grep -c "'소형'\|'대형'" src/app/invest/page.tsx` confirms 소형/대형 do NOT appear in AREA_OPTIONS (only in ALLOWED_AREA_BUCKETS for validation)
    - `npm run lint` passes
    - `npm run test` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>
    /invest 페이지가 존재하고:
    - revalidate = 3600, createReadonlyClient(), formatPrice from @/lib/format 사용
    - 상단 차트 + 하단 갭 랭킹 테이블 구조
    - 법적 면책 문구 존재
    - sgg_code + area_type(전체|59㎡|84㎡ 3탭) + risk_level URL 필터 탭
    - 단지 클릭 → /complexes/[id] 이동
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → RSC | sgg_code, area_type, risk_level 파라미터가 RSC에서 검증됨 |
| RSC → Supabase RPC | 검증된 값만 쿼리에 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering | area_type searchParam | mitigate | ALLOWED_AREA_BUCKETS allowlist 검증(4값); 미포함 값 → undefined → 전체 집계. 탭 표시는 3개지만 다른 값도 안전하게 처리됨 |
| T-21-07 | Tampering | sgg_code searchParam | mitigate | ALLOWED_SGG_CODES allowlist 검증 (invest.ts에서 가져온 동일 상수); 미포함 값 → undefined |
| T-21-08 | Tampering | risk_level searchParam | mitigate | ALLOWED_RISK_LEVELS 검증; getGapRankings 내부도 중복 검증 (이중 방어) |
| T-21-09 | Information Disclosure | ISR + cookies() 충돌 | mitigate | createReadonlyClient() 사용 (cookies() 없음) → revalidate 정상 동작 보장 |
</threat_model>

<verification>
```bash
# 파일 존재
ls src/app/invest/page.tsx

# 필수 패턴 확인
grep -c "revalidate = 3600" src/app/invest/page.tsx      # → 1
grep -c "createReadonlyClient" src/app/invest/page.tsx    # → 1
grep -c "투자 결정에 직접 활용" src/app/invest/page.tsx   # → 1
grep -c "RegionalPriceChartWrapper" src/app/invest/page.tsx  # → >= 1
grep -c "/complexes/" src/app/invest/page.tsx              # → >= 1

# AREA_OPTIONS 3탭 확인 (D-03/D-09)
grep -A5 "AREA_OPTIONS" src/app/invest/page.tsx            # → 전체|59㎡|84㎡ 3개만

# 금지 패턴 없음
grep "'use client'" src/app/invest/page.tsx               # → 없음
grep "backdrop-blur\|gradient-text\|glow" src/app/invest/page.tsx  # → 없음

# formatPrice import 확인 (로컬 정의 금지)
grep "function formatPrice" src/app/invest/page.tsx       # → 없음
grep "import.*formatPrice" src/app/invest/page.tsx        # → 있음

# 빌드 + 테스트
npm run lint && npm run test && npm run build
```
</verification>

<success_criteria>
1. `src/app/invest/page.tsx` 존재
2. `export const revalidate = 3600`
3. `createReadonlyClient()` + 쿠키 없음
4. `formatPrice` import from `@/lib/format` (로컬 복사 없음)
5. 지역 필터 탭 7개(전체+6구) + 타입 탭 정확히 3개(전체|59㎡|84㎡, D-03/D-09) + 위험도 탭 4개
6. `RegionalPriceChartWrapper` props: data + title
7. 법적 면책 문구 ("투자 결정에 직접 활용하지 마세요") 존재
8. 갭 랭킹 테이블: 단지 클릭 → `/complexes/${row.complexId}`
9. `npm run lint && npm run test && npm run build` 통과
</success_criteria>

<output>
After completion, create `.planning/phases/21-invest-analysis/21-02-SUMMARY.md`
</output>
