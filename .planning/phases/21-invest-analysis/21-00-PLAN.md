---
phase: 21-invest-analysis
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260529000001_invest_price_history.sql
  - next.config.ts
autonomous: true
requirements:
  - INVEST-01
  - INVEST-02

must_haves:
  truths:
    - "invest_regional_price_history RPC가 sgg_code + area_bucket + months 파라미터를 받아 연도-월별 평균 매매가를 반환한다"
    - "invest_price_history RPC가 complex_id + area_bucket + months 파라미터를 받아 연도-월별 평균 매매가를 반환한다"
    - "/gap-analysis URL 접근 시 /invest로 301 redirect된다"
  artifacts:
    - path: "supabase/migrations/20260529000001_invest_price_history.sql"
      provides: "invest_regional_price_history + invest_price_history RPC 두 함수"
      contains: "invest_regional_price_history"
    - path: "next.config.ts"
      provides: "gap-analysis → invest 301 redirect"
      contains: "gap-analysis"
  key_links:
    - from: "invest_regional_price_history"
      to: "transactions"
      via: "SQL GROUP BY sgg_code, deal_date"
      pattern: "cancel_date.*IS NULL.*superseded_by.*IS NULL"
    - from: "invest_price_history"
      to: "transactions"
      via: "SQL GROUP BY complex_id, deal_date"
      pattern: "cancel_date.*IS NULL.*superseded_by.*IS NULL"
---

<objective>
Phase 21 Wave 1-A: DB 마이그레이션 + Redirect 설정.

두 개의 Supabase RPC 함수와 next.config.ts의 301 redirect를 작성한다.
이 플랜은 Wave 2 (21-02, 21-03)의 데이터 기반이다.

Purpose: 시세 집계는 DB에서 SQL GROUP BY로 처리한다. 클라이언트 집계 금지.
Output: 마이그레이션 파일 1개, next.config.ts 수정 1건
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/ROADMAP.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-CONTEXT.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-RESEARCH.md

<interfaces>
<!-- 기존 RPC 참조 패턴 (supabase/migrations/20260430000013_complex_detail_functions.sql 검증됨) -->
-- 기존 complex_monthly_prices RPC 시그니처 (참조용):
create or replace function public.complex_monthly_prices(
  p_complex_id uuid,
  p_deal_type  text,
  p_months     int default 120
) returns table (year_month text, avg_price numeric, count bigint, avg_area numeric)

-- 기존 transactions 컬럼 (검증됨):
-- complex_id uuid, deal_type deal_type, deal_date date, price int4,
-- area_m2 numeric, cancel_date date, superseded_by uuid, sgg_code text (via complexes JOIN)

-- next.config.ts 현재 구조 (검증됨):
-- const nextConfig: NextConfig = { async headers() { ... } }
-- export default withSentryConfig(withSerwist(nextConfig), ...)
-- redirects() 메서드 현재 없음 → 추가 필요
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: invest_price_history + invest_regional_price_history SQL 마이그레이션 작성</name>
  <files>supabase/migrations/20260529000001_invest_price_history.sql</files>
  <read_first>
    - supabase/migrations/20260430000013_complex_detail_functions.sql (RPC 작성 패턴 + deal_type enum 캐스트 확인)
    - supabase/migrations/20260514000002_phase9_transactions_for_chart.sql (area_m2 컬럼 사용 확인)
  </read_first>
  <action>
신규 마이그레이션 파일 `supabase/migrations/20260529000001_invest_price_history.sql`을 작성한다.

**함수 1: invest_price_history (단지별 + 타입별 집계)**
```sql
-- 단지 상세 페이지 (/complexes/[id]) 시세 차트용
-- area_m2 컬럼 기반 버킷화 (transactions.area_m2 사용 — exclusive_area 아님)
CREATE OR REPLACE FUNCTION public.invest_price_history(
  p_complex_id   uuid,
  p_deal_type    text    DEFAULT 'sale',
  p_months       int     DEFAULT 24,
  p_area_bucket  text    DEFAULT NULL
)
RETURNS TABLE (year_month text, avg_price numeric, tx_count bigint)
LANGUAGE sql STABLE
AS $$
  SELECT
    to_char(deal_date, 'YYYY-MM')  AS year_month,
    ROUND(AVG(price))              AS avg_price,
    COUNT(*)                       AS tx_count
  FROM public.transactions
  WHERE
    complex_id    = p_complex_id
    AND deal_type = p_deal_type::public.deal_type
    AND deal_date >= (CURRENT_DATE - (p_months || ' months')::interval)::date
    AND cancel_date   IS NULL
    AND superseded_by IS NULL
    AND (
      p_area_bucket IS NULL
      OR CASE
           WHEN area_m2 < 50 THEN '소형'
           WHEN area_m2 < 66 THEN '59'
           WHEN area_m2 < 95 THEN '84'
           ELSE '대형'
         END = p_area_bucket
    )
  GROUP BY to_char(deal_date, 'YYYY-MM')
  ORDER BY year_month ASC;
$$;

GRANT EXECUTE ON FUNCTION public.invest_price_history(uuid, text, int, text)
  TO authenticated, anon;
```

**함수 2: invest_regional_price_history (지역 전체 집계)**
```sql
-- /invest 페이지 상단 시세 차트용
-- sgg_code 기반으로 해당 지역 모든 complexes의 transactions 집계
CREATE OR REPLACE FUNCTION public.invest_regional_price_history(
  p_sgg_code     text    DEFAULT NULL,
  p_months       int     DEFAULT 24,
  p_area_bucket  text    DEFAULT NULL
)
RETURNS TABLE (year_month text, avg_price numeric, tx_count bigint)
LANGUAGE sql STABLE
AS $$
  SELECT
    to_char(t.deal_date, 'YYYY-MM') AS year_month,
    ROUND(AVG(t.price))             AS avg_price,
    COUNT(*)                        AS tx_count
  FROM public.transactions t
  JOIN public.complexes c ON c.id = t.complex_id
  WHERE
    t.deal_type     = 'sale'::public.deal_type
    AND t.deal_date >= (CURRENT_DATE - (p_months || ' months')::interval)::date
    AND t.cancel_date   IS NULL
    AND t.superseded_by IS NULL
    AND (p_sgg_code IS NULL OR c.sgg_code = p_sgg_code)
    AND (
      p_area_bucket IS NULL
      OR CASE
           WHEN t.area_m2 < 50 THEN '소형'
           WHEN t.area_m2 < 66 THEN '59'
           WHEN t.area_m2 < 95 THEN '84'
           ELSE '대형'
         END = p_area_bucket
    )
  GROUP BY to_char(t.deal_date, 'YYYY-MM')
  ORDER BY year_month ASC;
$$;

GRANT EXECUTE ON FUNCTION public.invest_regional_price_history(text, int, text)
  TO authenticated, anon;
```

**CRITICAL 검증사항:**
- 두 함수 모두 `cancel_date IS NULL AND superseded_by IS NULL` 필수 (CLAUDE.md)
- `deal_type = p_deal_type::public.deal_type` enum 캐스트 (기존 패턴 동일)
- 지역 집계 함수는 `p_sgg_code IS NULL` 시 전체 지역 집계 (필터 미적용)
- `area_m2`를 버킷화할 때 CASE 값은 '소형'/'59'/'84'/'대형' (숫자 문자열 아닌 레이블)
  — 참고: '59'는 `59㎡`급을 의미하는 내부 코드값
  
파일 헤더 주석:
```sql
-- Phase 21: 투자 분석 통합 페이지
-- invest_price_history: 단지별 타입별 시세 집계 (단지 상세 페이지 차트)
-- invest_regional_price_history: 지역 전체 타입별 시세 집계 (/invest 페이지 차트)
```
  </action>
  <verify>
    <automated>grep -c "invest_regional_price_history" "C:/Users/jung/coding/bds/supabase/migrations/20260529000001_invest_price_history.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^--" supabase/migrations/20260529000001_invest_price_history.sql | grep -c "cancel_date.*IS NULL"` returns 2 (both functions have the filter)
    - `grep -c "invest_price_history" supabase/migrations/20260529000001_invest_price_history.sql` returns >= 2
    - `grep -c "GRANT EXECUTE" supabase/migrations/20260529000001_invest_price_history.sql` returns 2
    - File contains `area_m2` (not `exclusive_area`)
  </acceptance_criteria>
  <done>마이그레이션 파일이 존재하고 두 RPC 함수와 GRANT 문이 모두 포함되며, cancel_date/superseded_by 필터가 두 함수 모두에 존재한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: supabase db push + next.config.ts 301 redirect 추가</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (현재 전체 구조 확인 — withSentryConfig + withSerwist 래퍼)
  </read_first>
  <action>
**Step A: Supabase 마이그레이션 적용**

```bash
cd C:/Users/jung/coding/bds
npx supabase db push
```

성공 후 다음 메시지 확인: `Applying migration 20260529000001_invest_price_history.sql`

**Step B: next.config.ts에 redirects() 추가**

`nextConfig` 객체에 `async redirects()` 메서드를 추가한다. `async headers()` 메서드 바로 앞에 삽입.

```typescript
const nextConfig: NextConfig = {
  async redirects() {
    return [
      {
        source:      '/gap-analysis',
        destination: '/invest',
        permanent:   true,
      },
      {
        source:      '/gap-analysis/:path*',
        destination: '/invest/:path*',
        permanent:   true,
      },
    ]
  },
  async headers() {
    // 기존 CSP 헤더 — 변경 없음
    return [ /* ... 기존 내용 유지 ... */ ]
  },
}
```

**CRITICAL:** `withSentryConfig(withSerwist(nextConfig), ...)` 래퍼 구조를 절대 변경하지 않는다. `nextConfig` 객체만 수정한다.

**Step C: 빌드 검증**
```bash
npm run build
```
  </action>
  <verify>
    <automated>grep -c "gap-analysis" "C:/Users/jung/coding/bds/next.config.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "permanent.*true" next.config.ts` returns >= 2 (두 redirect 규칙 모두)
    - `grep -c "withSentryConfig" next.config.ts` returns 1 (래퍼 구조 유지)
    - `npm run build` exits with code 0
    - `npx supabase db push` succeeded (마이그레이션 적용됨)
  </acceptance_criteria>
  <done>마이그레이션이 원격 DB에 적용되었고, /gap-analysis → /invest 301 redirect가 next.config.ts에 설정되어 있으며, npm run build가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → RSC | searchParams 경유 sgg_code, area_bucket 값이 RPC에 전달됨 |
| RPC parameter → SQL | p_sgg_code, p_area_bucket이 SQL WHERE절에 사용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | invest_regional_price_history p_sgg_code | mitigate | 호출 측(RSC page.tsx)에서 ALLOWED_SGG_CODES allowlist 검증 후 전달; RPC 내부는 NULL 처리로 최악의 경우 전체 집계 |
| T-21-02 | Tampering | invest_price_history p_area_bucket | mitigate | 호출 측에서 ALLOWED_AREA_BUCKETS allowlist 검증; RPC CASE 버킷화가 이중 방어 제공 |
| T-21-03 | Information Disclosure | cancel_date/superseded_by 필터 누락 | mitigate | 두 RPC 모두 WHERE cancel_date IS NULL AND superseded_by IS NULL 필수 포함 |
</threat_model>

<verification>
```bash
# 마이그레이션 파일 검증
grep -c "invest_regional_price_history" supabase/migrations/20260529000001_invest_price_history.sql
# → 3 이상 (함수명, GRANT, 주석)

grep -c "invest_price_history" supabase/migrations/20260529000001_invest_price_history.sql
# → 3 이상

# cancel_date 필터 존재 확인 (두 함수 모두)
grep -v "^--" supabase/migrations/20260529000001_invest_price_history.sql | grep -c "cancel_date.*IS NULL"
# → 2

# redirect 확인
grep -c "gap-analysis" next.config.ts
# → 2

# 빌드 성공
npm run build
```
</verification>

<success_criteria>
1. `supabase/migrations/20260529000001_invest_price_history.sql` 파일 존재
2. `invest_regional_price_history(p_sgg_code, p_months, p_area_bucket)` RPC 정의됨
3. `invest_price_history(p_complex_id, p_deal_type, p_months, p_area_bucket)` RPC 정의됨
4. 두 함수 모두 `cancel_date IS NULL AND superseded_by IS NULL` 포함
5. GRANT EXECUTE TO authenticated, anon 두 함수 모두 적용
6. `supabase db push` 성공적으로 실행됨
7. `next.config.ts`에 `/gap-analysis` → `/invest` 301 redirect 두 규칙 존재
8. `npm run build` 통과
</success_criteria>

<output>
After completion, create `.planning/phases/21-invest-analysis/21-00-SUMMARY.md`
</output>
