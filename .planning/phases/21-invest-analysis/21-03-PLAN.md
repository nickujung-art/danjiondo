---
phase: 21-invest-analysis
plan: "03"
type: execute
wave: 2
depends_on:
  - "21-00"
  - "21-01"
files_modified:
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - INVEST-02
  - INVEST-03
  - INVEST-04

must_haves:
  truths:
    - "단지 상세 페이지에 '시세 흐름' 섹션이 GapAnalysisCard 아래 존재한다"
    - "타입 탭(전체+존재 타입)이 URL area_type searchParam으로 동작한다"
    - "ComplexPriceChartWrapper가 타입별 24개월 매매 시세 데이터를 받아 렌더된다"
    - "법적 면책 문구가 단지 상세 차트 섹션 하단에 존재한다"
    - "기존 revalidate = 86400과 모든 기존 쿼리가 유지된다"
  artifacts:
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "단지 상세 시세 흐름 차트 섹션 추가"
      contains: "ComplexPriceChartWrapper"
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "getComplexAreaTypes"
      via: "import from @/lib/data/invest"
      pattern: "getComplexAreaTypes"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "getComplexPriceByType"
      via: "import from @/lib/data/invest"
      pattern: "getComplexPriceByType"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "ComplexPriceChartWrapper"
      via: "props: data + title"
      pattern: "ComplexPriceChartWrapper"
---

<objective>
Phase 21 Wave 2-B: 단지 상세 페이지 시세 차트 섹션 추가.

기존 `/complexes/[id]/page.tsx`에 시세 흐름 차트 섹션을 추가한다.
21-02와 병렬 실행 가능 — 수정 파일이 겹치지 않음.

Purpose: D-02 결정 — 단지 상세 페이지에도 시세 차트 섹션 추가 (GapAnalysisCard 아래).
Output: 기존 src/app/complexes/[id]/page.tsx 수정 (시세 차트 섹션 + 타입 탭 추가)
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-CONTEXT.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-RESEARCH.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/21-01-SUMMARY.md

<interfaces>
<!-- src/app/complexes/[id]/page.tsx 현재 상태 (직접 검증됨) -->

// 기존 import 목록 (모두 유지, 추가만 함):
import { getComplexGapStats } from '@/lib/data/gap-analysis'
import type { ComplexGapStatsResult } from '@/lib/data/gap-analysis'
import { GapAnalysisCard } from '@/components/complex/GapAnalysisCard'

// 추가할 imports:
import { getComplexAreaTypes, getComplexPriceByType, ALLOWED_AREA_BUCKETS } from '@/lib/data/invest'
import type { AreaBucket, AreaType, RegionalPricePoint } from '@/lib/data/invest'
import { ComplexPriceChartWrapper } from '@/components/invest/ComplexPriceChartWrapper'

// 현재 Props 타입 (확인됨):
interface Props {
  params: Promise<{ id: string }>
}

// 신규 필요: searchParams 추가
interface Props {
  params:       Promise<{ id: string }>
  searchParams: Promise<{ area_type?: string }>  // 신규 추가
}

// 기존 revalidate (유지 필수):
export const revalidate = 86400

// 기존 Promise.all 패턴 (이 안에 신규 쿼리 2개 추가):
// const [complex, summary, ...] = await Promise.all([...])
// → getComplexAreaTypes, getComplexPriceByType 을 Promise.all에 추가

// GapAnalysisCard 위치 (기존 유지, 아래에 시세 차트 삽입):
// <GapAnalysisCard gapStats={gapStats} />
// {/* 여기에 ComplexPriceSection 삽입 */}

// getComplexAreaTypes 시그니처 (21-01 invest.ts):
// async function getComplexAreaTypes(supabase, complexId, months?): Promise<AreaType[]>
// AreaType = { bucket: AreaBucket; txCount: number }

// getComplexPriceByType 시그니처 (21-01 invest.ts):
// async function getComplexPriceByType(supabase, complexId, areaBucket?, months?): Promise<RegionalPricePoint[]>

// ComplexPriceChartWrapper props (21-01):
// { data: RegionalPricePoint[]; title: string }

// ALLOWED_AREA_BUCKETS (21-01):
// ['소형', '59', '84', '대형'] as const
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 단지 상세 페이지 — 시세 차트 섹션 추가</name>
  <files>src/app/complexes/[id]/page.tsx</files>
  <read_first>
    - src/app/complexes/[id]/page.tsx (전체 파일 읽기 필수 — 기존 import, Props 타입, Promise.all 위치, GapAnalysisCard 위치 파악)
    - src/lib/data/invest.ts (21-01에서 생성한 함수 시그니처)
    - src/components/invest/ComplexPriceChartWrapper.tsx (21-01에서 생성한 Wrapper)
  </read_first>
  <action>
기존 `src/app/complexes/[id]/page.tsx` 파일을 **최소한으로** 수정한다.
기존 코드를 삭제하거나 리팩터링하지 않는다. 추가(add)만 수행한다.

**수정 1: import 추가 (파일 상단 import 블록 마지막에 추가)**
```typescript
import { getComplexAreaTypes, getComplexPriceByType, ALLOWED_AREA_BUCKETS } from '@/lib/data/invest'
import type { AreaBucket, AreaType, RegionalPricePoint } from '@/lib/data/invest'
import { ComplexPriceChartWrapper } from '@/components/invest/ComplexPriceChartWrapper'
```

**수정 2: Props 타입 변경 — searchParams 추가**

기존:
```typescript
interface Props {
  params: Promise<{ id: string }>
}
```
변경:
```typescript
interface Props {
  params:       Promise<{ id: string }>
  searchParams: Promise<{ area_type?: string }>
}
```

**수정 3: 컴포넌트 함수 시그니처 변경**

기존:
```typescript
export default async function ComplexDetailPage({ params }: Props) {
  const { id } = await params
```
변경:
```typescript
export default async function ComplexDetailPage({ params, searchParams }: Props) {
  const { id } = await params
  const sp         = await searchParams
  const rawArea    = typeof sp.area_type === 'string' ? sp.area_type : ''
  const areaBucket = (ALLOWED_AREA_BUCKETS as ReadonlyArray<string>).includes(rawArea)
    ? (rawArea as AreaBucket) : undefined
```

**수정 4: Promise.all에 신규 쿼리 2개 추가**

기존 Promise.all 블록을 찾아 신규 쿼리를 추가한다.
기존 쿼리 결과 변수들은 그대로 유지한다.
CRITICAL: 기존 변수 순서를 절대 교란하지 않는다. `getComplexGapStats` 호출 결과 변수(gapStats)를 그대로 두고, 마지막에만 추가한다:

```typescript
// 기존 Promise.all 내에 다음 두 항목 추가:
const [
  complex,
  summary,
  // ... 기존 항목들 ...
  gapStats,
  // ↓ 신규 추가
  areaTypes,
  priceHistory,
] = await Promise.all([
  // ... 기존 항목들 ...
  getComplexGapStats(id, supabase),
  // ↓ 신규 추가
  getComplexAreaTypes(supabase, id, 24).catch((): AreaType[] => []),
  getComplexPriceByType(supabase, id, areaBucket, 24).catch((): RegionalPricePoint[] => []),
])
```

주의: Promise.all의 순서는 반드시 기존 변수 순서를 유지. 기존 변수들을 교란하지 않도록 마지막에 추가.

**수정 5: JSX에 시세 차트 섹션 추가**

GapAnalysisCard 렌더 위치를 찾아 그 바로 아래에 추가:

```tsx
{/* 기존 GapAnalysisCard — 유지 */}
{gapStats && <GapAnalysisCard gapStats={gapStats} />}

{/* 신규: 시세 흐름 차트 섹션 (D-02, D-04) */}
{(areaTypes.length > 0 || priceHistory.length > 0) && (
  <div className="card" style={{ padding: 20 }}>
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 12, flexWrap: 'wrap', gap: 8 }}>
      <h3 style={{ font: '700 15px/1.4 var(--font-sans)', margin: 0 }}>시세 흐름</h3>
      {/* 타입 탭 — URL searchParam area_type */}
      {areaTypes.length > 0 && (
        <div style={{ display: 'flex', gap: 4, flexWrap: 'wrap' }}>
          {/* '전체' 탭 */}
          <a href={`/complexes/${id}`}
            style={{
              display: 'inline-block', padding: '4px 10px', borderRadius: 5,
              font: '500 11px/1 var(--font-sans)', textDecoration: 'none',
              background: !areaBucket ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
              color: !areaBucket ? '#fff' : 'var(--fg-sec)',
              border: '1px solid var(--line-subtle)', whiteSpace: 'nowrap',
            }}>전체</a>
          {areaTypes.map(t => {
            const AREA_LABEL: Record<string, string> = { '소형': '소형', '59': '59㎡', '84': '84㎡', '대형': '대형' }
            const isActive = areaBucket === t.bucket
            return (
              <a key={t.bucket} href={`/complexes/${id}?area_type=${t.bucket}`}
                style={{
                  display: 'inline-block', padding: '4px 10px', borderRadius: 5,
                  font: '500 11px/1 var(--font-sans)', textDecoration: 'none',
                  background: isActive ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
                  color: isActive ? '#fff' : 'var(--fg-sec)',
                  border: '1px solid var(--line-subtle)', whiteSpace: 'nowrap',
                }}>
                {AREA_LABEL[t.bucket] ?? t.bucket}
              </a>
            )
          })}
        </div>
      )}
    </div>
    <ComplexPriceChartWrapper
      data={priceHistory}
      title={`최근 24개월 매매 실거래 월평균${areaBucket ? ` (${areaBucket === '59' ? '59㎡' : areaBucket === '84' ? '84㎡' : areaBucket})` : ''}`}
    />
    {/* 법적 면책 문구 (D-01) */}
    <p style={{ font: '400 11px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '12px 0 0' }}>
      * 실거래 흐름 기반 참고 지수입니다. 투자 결정에 직접 활용하지 마세요.
    </p>
  </div>
)}
```

**CRITICAL 규칙:**
- `revalidate = 86400` 유지 (변경 금지)
- 기존 모든 import, 함수, JSX 유지 (삭제 금지)
- `'use client'` 추가 금지 (이 파일은 RSC)
- searchParams를 Props에 추가하면 ISR revalidate에 영향이 있을 수 있으므로:
  - `createReadonlyClient()` 유지 (cookies() 없음)
  - searchParams는 Next.js 15 RSC에서 `revalidate`와 공존 가능 (cookies()를 사용하지 않는 한)
- AI 슬롭 금지: backdrop-blur, gradient-text, glow, 보라/인디고 없음
  </action>
  <verify>
    <automated>grep -c "ComplexPriceChartWrapper" "C:/Users/jung/coding/bds/src/app/complexes/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ComplexPriceChartWrapper" "src/app/complexes/[id]/page.tsx"` returns >= 1
    - `grep -c "getComplexAreaTypes" "src/app/complexes/[id]/page.tsx"` returns >= 1
    - `grep -c "getComplexPriceByType" "src/app/complexes/[id]/page.tsx"` returns >= 1
    - `grep -c "revalidate = 86400" "src/app/complexes/[id]/page.tsx"` returns 1 (기존 유지)
    - `grep -c "투자 결정에 직접 활용하지 마세요" "src/app/complexes/[id]/page.tsx"` returns >= 1
    - `grep "'use client'" "src/app/complexes/[id]/page.tsx"` returns nothing
    - `grep "backdrop-blur\|gradient-text\|glow" "src/app/complexes/[id]/page.tsx"` returns nothing
    - `grep -c "getComplexGapStats" "src/app/complexes/[id]/page.tsx"` returns 1 (기존 쿼리 보존)
    - `npm run lint` passes
    - `npm run build` passes
    - `npm run test` passes (기존 테스트 깨지지 않음)
  </acceptance_criteria>
  <done>
    단지 상세 페이지에 시세 흐름 차트 섹션이 추가되어:
    - GapAnalysisCard 아래에 '시세 흐름' 카드 섹션 존재
    - 타입 탭이 areaTypes 목록 기반으로 렌더 (거래 없는 타입 숨김)
    - ComplexPriceChartWrapper가 priceHistory 데이터 수신
    - 법적 면책 문구 존재
    - 기존 revalidate = 86400, getComplexGapStats 쿼리 보존, 기존 기능 전부 유지됨
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → RSC | area_type 파라미터 |
| RSC → Supabase | getComplexAreaTypes, getComplexPriceByType 호출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Tampering | area_type searchParam (단지 상세) | mitigate | ALLOWED_AREA_BUCKETS allowlist 검증; 미포함 값 → undefined → 전체 조회 |
| T-21-11 | Denial of Service | Promise.all 추가로 쿼리 증가 | accept | 2개 추가 쿼리는 병렬 실행 + revalidate=86400 ISR 캐시로 보호됨; 저위험 |
</threat_model>

<verification>
```bash
# 파일 수정 확인
grep -c "ComplexPriceChartWrapper" "src/app/complexes/[id]/page.tsx"  # → >= 1
grep -c "getComplexAreaTypes"       "src/app/complexes/[id]/page.tsx"  # → >= 1
grep -c "revalidate = 86400"        "src/app/complexes/[id]/page.tsx"  # → 1

# 기존 쿼리 보존 확인
grep -c "getComplexGapStats"        "src/app/complexes/[id]/page.tsx"  # → 1

# 법적 면책 문구
grep -c "투자 결정에 직접 활용" "src/app/complexes/[id]/page.tsx"   # → >= 1

# 금지 패턴
grep "'use client'" "src/app/complexes/[id]/page.tsx"   # → 없음
grep "backdrop-blur" "src/app/complexes/[id]/page.tsx"  # → 없음

# 빌드 + 테스트
npm run lint && npm run build && npm run test
```
</verification>

<success_criteria>
1. `src/app/complexes/[id]/page.tsx`에 `ComplexPriceChartWrapper` import + 사용
2. `getComplexAreaTypes` + `getComplexPriceByType` import + Promise.all에 포함
3. GapAnalysisCard 아래에 시세 흐름 섹션이 렌더됨 (areaTypes.length > 0 또는 priceHistory.length > 0 조건)
4. 타입 탭: `href="/complexes/${id}?area_type=${bucket}"` 형식
5. 법적 면책 문구 ("투자 결정에 직접 활용하지 마세요") 존재
6. `export const revalidate = 86400` 유지됨
7. `getComplexGapStats` 쿼리 보존 (기존 갭 분석 기능 정상 유지)
8. 기존 모든 기능 (GapAnalysisCard, TransactionChart, etc.) 정상 작동
9. `npm run lint && npm run build && npm run test` 통과
</success_criteria>

<output>
After completion, create `.planning/phases/21-invest-analysis/21-03-SUMMARY.md`
</output>
