---
phase: 39-onconflict-constraint-fix
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - supabase/migrations/20260731110000_favorites_drop_legacy_partial_unique_indexes.sql
  - supabase/migrations/20260731110001_redevelopment_projects_complex_id_unique.sql
  - supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql
  - scripts/verify-onconflict-probe.ts
autonomous: false
requirements: [F-02, F-04, F-05]

must_haves:
  truths:
    - "favorites 구 부분 UNIQUE 인덱스 2개가 제거되고 통합 인덱스 1개만 남는다"
    - "redevelopment_projects에 UNIQUE (complex_id)가 생겨 어드민 재건축 저장이 동작한다"
    - "public.unique_indexes_for_table(text)가 존재하고 service_role만 실행할 수 있다"
    - "그 함수가 부분 인덱스를 is_partial=true로 구분해 돌려준다 (F-05 게이트의 판정 근거)"
    - "고장 4건 전부 EXPLAIN·PostgREST 프로브에서 통과한다"
    - "정상 판정된 13개 upsert 지점이 여전히 통과한다 — 무회귀"
  artifacts:
    - path: "supabase/migrations/20260731110000_favorites_drop_legacy_partial_unique_indexes.sql"
      provides: "구 부분 UNIQUE 인덱스 2개 DROP (롤아웃 ③단계)"
      contains: "favorites_complex_favorite_unique_idx"
    - path: "supabase/migrations/20260731110001_redevelopment_projects_complex_id_unique.sql"
      provides: "redevelopment_projects UNIQUE (complex_id) 신설 — 코드 의도에 스키마를 맞춤"
      contains: "unique (complex_id)"
    - path: "supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql"
      provides: "UNIQUE 인덱스 카탈로그 조회 RPC + is_partial/is_expression 플래그, service_role 전용"
      contains: "unique_indexes_for_table"
  key_links:
    - from: "src/lib/actions/redevelopment-actions.ts:77"
      to: "redevelopment_projects_complex_id_key"
      via: "onConflict: 'complex_id' (앱 코드 무변경)"
      pattern: "unique \\(complex_id\\)"
    - from: "supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql"
      to: "pg_index.indpred"
      via: "is_partial 플래그 — 부분 인덱스를 추론 가능으로 오판하지 않게 하는 근거"
      pattern: "indpred is not null"
---

<objective>
스키마 쪽 정리를 한 번의 `db push`로 끝낸다: **favorites 롤아웃 ③단계(구 인덱스 DROP)** +
**F-04 UNIQUE 신설** + **F-05 게이트가 쓸 카탈로그 RPC**.

Purpose:
- **F-02 마무리** — 39-01이 통합 인덱스를 만들고 앱을 배포했다. 이제 구 부분 인덱스 2개를 지워
  `favorites`에 **추론 가능한 UNIQUE 하나만** 남긴다. 부분 인덱스가 남아 있으면 F-05 게이트가
  "어느 게 진짜 계약인가"를 계속 헷갈릴 여지를 준다.
- **F-04** — `redevelopment-actions.ts:77`은 `onConflict: 'complex_id'`에 *"complex_id 기준 —
  단지당 1개 row"*라는 주석까지 달려 있는데 **실제 제약은 PK `(id)` 뿐**이라 어드민 재건축 저장이
  실패한다. 나머지 3건과 방향이 반대다 — 여기는 **코드가 아니라 스키마가 의도에서 벗어나 있다.**
  제약이 처음부터 없었을 뿐 코드 의도는 명확하므로 **앱 코드는 그대로 두고 UNIQUE를 신설**한다.
  중복 `complex_id` 그룹 **0건** 실측 확인 완료 → 안전.
- **F-05 준비** — 게이트 테스트가 "이 테이블의 **추론 가능한** UNIQUE 인덱스가 뭔가"를 알아야 한다.
  이 저장소에는 `pg` 클라이언트 의존성도 DB 접속 문자열도 없고 테스트는 supabase-js만 쓴다.
  따라서 카탈로그를 읽는 **읽기 전용 RPC**를 하나 만든다. 🔴 **필터링(`is_partial` 제외)은 SQL이
  아니라 TypeScript에서 한다** — 그래야 "부분 인덱스를 일치로 판정하면 안 된다"는 규칙이
  **DB 없이도 단위 테스트로 검증 가능한 코드**가 된다. 이게 39-03 음성 대조의 전제다.

Output: 마이그레이션 3개 + 한 번의 `db push` + 고장 4건 전수 재판정 + 정상 13건 무회귀 확인.

⚠️ **앱 코드를 바꾸지 않는다.** `redevelopment-actions.ts`는 한 글자도 손대지 않는다 —
제약이 생기면 기존 `onConflict: 'complex_id'`가 의도대로 동작한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-onconflict-constraint-fix/39-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않도록 확정 사실을 여기 박아둔다. 전부 실측값이다. -->

**F-04 대상 — `src/lib/actions/redevelopment-actions.ts` 63~78행 (읽기만, 수정 금지)**

```
// 3. upsert (complex_id 기준 — 단지당 1개 row)
const { error: dbErr } = await (admin as any)
  .from('redevelopment_projects')
  .upsert({ complex_id, phase, notes, project_name: `재건축-${complexId}`, created_by: userId },
          { onConflict: 'complex_id' })
```
`requireAdmin()` 게이트 → zod 검증 → service_role upsert 순서. 실측 제약은 `redevelopment_projects_pkey (id)` 뿐.
중복 `complex_id` 그룹 **0건**.

**적용할 DDL 3종**

```sql
-- ① 20260731110000 — favorites 롤아웃 ③단계
drop index if exists public.favorites_complex_favorite_unique_idx;
drop index if exists public.favorites_area_type_alert_unique_idx;

-- ② 20260731110001 — F-04
alter table public.redevelopment_projects
  add constraint redevelopment_projects_complex_id_key unique (complex_id);

-- ③ 20260731110002 — F-05 카탈로그 RPC (본문은 아래 검증된 SQL)
```

**③ 함수 본문 — 프로덕션에서 실행 검증 완료한 SQL 그대로 쓸 것**

```sql
create or replace function public.unique_indexes_for_table(p_table text)
returns table (index_name text, columns text[], is_partial boolean, is_expression boolean, is_valid boolean)
language sql
stable
security invoker
set search_path = public, pg_catalog
as $$
  select
    i.relname::text,
    (select array_agg(a.attname::text order by a.attname)
       from unnest(x.indkey::int2[]) with ordinality as k(attnum, ord)
       join pg_attribute a on a.attrelid = t.oid and a.attnum = k.attnum
      where k.ord <= x.indnkeyatts),
    x.indpred   is not null,
    x.indexprs  is not null,
    x.indisvalid
  from pg_index x
  join pg_class     i on i.oid = x.indexrelid
  join pg_class     t on t.oid = x.indrelid
  join pg_namespace n on n.oid = t.relnamespace
  where n.nspname = 'public'
    and t.relname = p_table
    and x.indisunique
  order by i.relname;
$$;

revoke execute on function public.unique_indexes_for_table(text) from public, anon, authenticated;
grant  execute on function public.unique_indexes_for_table(text) to service_role;

notify pgrst, 'reload schema';
```

동작 확인된 출력 형태 (`p_table = 'favorites'`, DROP 적용 **전** 프로덕션 실측):
```
favorites_area_type_alert_unique_idx  {area_type_id,complex_id,site_id,user_id}  is_partial=true
favorites_complex_favorite_unique_idx {complex_id,site_id,user_id}               is_partial=true
favorites_pkey                        {id}                                       is_partial=false
```
`columns`는 **attname 알파벳 정렬**로 나온다 (ON CONFLICT 추론은 집합 기준이므로 순서 무관 —
39-CONTEXT F-05 요구사항 3번). `indnkeyatts` 제한으로 INCLUDE 컬럼은 제외된다.

**설계 결정 (F-06 재량 확정)** — 함수는 **필터링하지 않고 모든 UNIQUE 인덱스를 플래그와 함께**
돌려준다. `is_partial`/`is_expression` 제외 판정은 39-03의 TypeScript(`toInferrable`)가 한다.
근거: 39-CONTEXT가 "부분 인덱스를 일치로 판정하면 안 된다"를 F-05의 **핵심 함정**으로 지목했는데,
그 규칙이 SQL 안에 숨어 있으면 **DB 없이는 검증할 수 없다.** TS로 올려야 39-03의 음성 대조
(수정 전 `favorites` 형태 → FAIL 판정)가 라이브 DB 없이도 항상 실행된다.

**권한 결정** — `security invoker` + `revoke from public, anon, authenticated` + `grant to service_role`.
`SECURITY DEFINER`는 불필요하다(`pg_index`·`pg_class`는 기본적으로 모든 롤이 읽을 수 있다).
불필요한 권한 승격을 만들지 않는 쪽을 택한다. 테스트는 `SUPABASE_SERVICE_ROLE_KEY` 클라이언트로
호출하므로 `service_role` 권한으로 충분하다.

**마이그레이션 타임스탬프**: 39-01이 `20260731100000`을 썼다. 이 plan은 `20260731110000`·
`110001`·`110002`를 쓴다. 파일명 순서가 곧 적용 순서다.

**판정 수단**: `scripts/verify-onconflict-probe.ts` (39-00 Task 1 산출, 39-01 Task 1에서 확장) +
`npx supabase db query --linked "explain insert … on conflict (…)"`.
`db query --linked`는 **읽기 검증 전용**이며 DDL 적용은 `npm run db:push`만 쓴다 (Scope Fence 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 마이그레이션 3개 작성 + 적용 직전 안전성 재확인(읽기 전용) + 프로브 대상 확장</name>

  <files>
    supabase/migrations/20260731110000_favorites_drop_legacy_partial_unique_indexes.sql
    supabase/migrations/20260731110001_redevelopment_projects_complex_id_unique.sql
    supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql
    scripts/verify-onconflict-probe.ts
  </files>

  <read_first>
    - `.planning/phases/39-onconflict-constraint-fix/39-CONTEXT.md` — **F-04 절 전체**(DDL 원문 +
      "방향이 반대인 이유"), **F-05 절 전체**(부분 인덱스 오판 금지 + 컬럼 순서 무관),
      **F-02의 롤백 순서 문단**, `<scope_fence>` **1·6·7번**
    - `.planning/phases/39-onconflict-constraint-fix/39-01-SUMMARY.md` — 39-01이 만든 통합 인덱스가
      실제로 프로덕션에 있는지, `addFavorite`이 통과 상태인지 확인. **통과가 아니면 이 plan을 시작하지 않는다**
    - `src/lib/actions/redevelopment-actions.ts` 40~85행 — `onConflict: 'complex_id'` 위치와
      주석. **읽기만, 수정 금지**
    - `supabase/migrations/20260731100000_favorites_unified_unique_index.sql` (39-01 산출물) —
      DROP 마이그레이션이 어떤 인덱스를 남기는지 대조
    - `CLAUDE.md` `## 아키텍처 규칙` — RLS `TO` 절 규약, `CREATE INDEX CONCURRENTLY` 규약
  </read_first>

  <action>
  **(0) 선행 확인** — 39-01이 끝났는지 라이브로 본다 (읽기 전용):
  ```
  npx supabase db query --linked "select indexname from pg_indexes where schemaname='public' and tablename='favorites' order by indexname"
  ```
  `favorites_user_complex_area_site_key`가 **없으면 중단하고 보고**한다. 통합 인덱스 없이 구 인덱스를
  지우면 `favorites`에 유일성 보장이 사라진다.

  **(1) `20260731110000_favorites_drop_legacy_partial_unique_indexes.sql`**

  `<interfaces>` ①의 `drop index if exists` 2문장. 주석에 남길 것: 이것이 39-CONTEXT F-02
  롤아웃의 **③단계**(신규 인덱스 생성 → 앱 배포 → 구 인덱스 DROP)이고, `20260731100000`의 통합
  인덱스가 두 부분 인덱스와 **의미 등가**임(39-CONTEXT 등가 표)이며, `NULLS NOT DISTINCT`가
  `area_type_id IS NULL` 중복 차단을 대신한다는 사실.

  ⛔ `favorites` 컬럼·CHECK·RLS를 손대지 않는다. `site_id`·`area_type_id`를 제거하거나
  NOT NULL로 바꾸지 않는다 (Scope Fence 2·3).

  **(2) `20260731110001_redevelopment_projects_complex_id_unique.sql`**

  `<interfaces>` ②의 `alter table … add constraint … unique (complex_id)` 1문장.
  주석에 남길 것: 앱 코드(`redevelopment-actions.ts:77`)가 `onConflict: 'complex_id'`와
  *"단지당 1개 row"* 주석으로 의도를 명시했는데 **제약이 처음부터 없었다**는 사실,
  중복 `complex_id` 그룹 0건 실측 확인,
  그리고 나머지 3건과 달리 **코드가 아니라 스키마를 의도에 맞추는** 방향이라는 판단 근거.

  ⛔ `CREATE INDEX CONCURRENTLY`를 쓰지 않는다. `add constraint … unique`는 일반 인덱스를 만들고
  `db push` 정상 경로로 적용된다. `CONCURRENTLY`를 쓰면 `db push` 자체가 불가능해지고
  `migration repair --status applied <version>`이 필수가 된다 (CLAUDE.md 규약 / Scope Fence 7) —
  대상 테이블이 소량이라 그럴 이유가 없다.

  **(3) `20260731110002_unique_indexes_for_table_fn.sql`**

  `<interfaces>` ③의 SQL을 **그대로** 옮긴다. 이 본문은 프로덕션에서 실행해 출력까지 확인한 것이다.
  임의로 최적화하거나 `indpred is null` 필터를 SQL 안으로 옮기지 말 것 — 필터링을 TS로 올리는 것이
  설계 결정이다(위 `<interfaces>` "설계 결정" 참조). 주석에 그 근거를 남긴다.
  `notify pgrst, 'reload schema';`를 **반드시 마지막에 포함**한다 (PostgREST 스키마 캐시 갱신).

  ⛔ `security definer`를 쓰지 않는다. `grant execute … to public`·`anon`·`authenticated`를
  남기지 않는다 — `revoke … from public, anon, authenticated` 후 `service_role`에만 grant한다.

  **(4) 적용 직전 안전성 재확인** (읽기 전용, 3건 전부 실행하고 출력을 보관):
  ```
  npx supabase db query --linked "select complex_id, count(*) from public.redevelopment_projects group by 1 having count(*) > 1"
  npx supabase db query --linked "select count(*) as total from public.redevelopment_projects"
  npx supabase db query --linked "select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname='unique_indexes_for_table'"
  ```
  1번이 **0행**이 아니면 `add constraint`가 실패하므로 **중단·보고**한다.
  3번이 이미 1행이면 같은 이름의 함수가 존재하는 것이므로 시그니처를 확인하고 보고한다.

  **(5) 프로브 대상 확장** — `scripts/verify-onconflict-probe.ts`에 `redevelopment_projects`
  (`onConflict: 'complex_id'`, payload `{ complex_id: randomUUID(), phase: <유효 enum 또는 text 값>, project_name: 'ZZ-PROBE' }`)를
  추가한다. payload는 **FK 위반(23503)으로 반드시 실패**해야 한다 — `complex_id`는 FK다.
  NOT NULL 컬럼이 더 있으면 더미 값을 채워 23503까지 도달하게 만든다. `phase`가 CHECK/enum이면
  유효값을 넣어 **23503이 나오도록** 한다(22P02/23514가 나오면 UNKNOWN 판정이 되어 판정이 흐려진다).
  넣기 전에 컬럼 정의를 읽기 쿼리로 확인한다:
  ```
  npx supabase db query --linked "select column_name, is_nullable, data_type, column_default from information_schema.columns where table_schema='public' and table_name='redevelopment_projects' order by ordinal_position"
  ```

  ⛔ 이 태스크는 **DDL을 실행하지 않는다.** 파일 작성 + 읽기 조회 + 스크립트 수정만 한다.
  `execute_sql`·MCP `apply_migration`·대시보드 금지 (Scope Fence 6).
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && test -f supabase/migrations/20260731110000_favorites_drop_legacy_partial_unique_indexes.sql && test -f supabase/migrations/20260731110001_redevelopment_projects_complex_id_unique.sql && test -f supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql && grep -v '^--' supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql | grep -ci "security definer" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>

  <acceptance_criteria>
    - 마이그레이션 3개 파일 존재, 타임스탬프가 `20260731100000`보다 뒤
    - `110000` 본문에 `favorites_complex_favorite_unique_idx`·`favorites_area_type_alert_unique_idx`
      두 이름이 모두 등장하고, `favorites_user_complex_area_site_key`는 **등장하지 않음**
      (통합 인덱스를 실수로 지우지 않는다는 증거)
    - `110001` 본문에 `unique (complex_id)` 등장, `concurrently` **0회**
      (`grep -ci concurrently` == 0)
    - `110002` 본문에 `security definer` **0회**, `indpred is not null` **1회**,
      `revoke execute` 1회, `grant  execute … to service_role` 1회,
      `notify pgrst` 1회
    - `110002` 본문에 `indpred is null` 형태의 **필터**가 없음
      (`grep -v '^--' <file> | grep -c "indpred is null"` == 0) — 필터링은 TS 담당
    - 안전성 조회: `redevelopment_projects` 중복 `complex_id` **0행**,
      `unique_indexes_for_table` 기존 함수 **0행**
    - 선행 확인: `favorites_user_complex_area_site_key`가 라이브에 **존재**
    - `scripts/verify-onconflict-probe.ts`에 `redevelopment_projects` 대상이 추가됨
    - `npx tsc --noEmit` exit 0
    - `git status --porcelain src/` 빈 출력 (앱 코드 무변경 — `redevelopment-actions.ts` 포함)
  </acceptance_criteria>

  <done>
    마이그레이션 3개가 준비됐고, 적용 전 안전 조건(중복 0건·통합 인덱스 존재·동명 함수 없음)이
    라이브 조회로 확인됐으며, 프로덕션은 아직 무변경이다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] npm run db:push로 마이그레이션 3개 일괄 적용</name>

  <files>
    supabase/migrations/20260731110000_favorites_drop_legacy_partial_unique_indexes.sql
    supabase/migrations/20260731110001_redevelopment_projects_complex_id_unique.sql
    supabase/migrations/20260731110002_unique_indexes_for_table_fn.sql
    (전부 적용 대상, 수정 없음)
  </files>

  <read_first>
    - `.planning/phases/39-onconflict-constraint-fix/39-CONTEXT.md` — `<scope_fence>` **6·7번**,
      `## Risk Summary`의 "마이그레이션이 원장 drift 유발" 행
    - `.planning/phases/38-security-reset-fix/38-01-PLAN.md` Task 4 — 이 저장소의 `db push`
      체크포인트 절차 원형(dry-run 게이트 → push → 라이브 확인 → 원장 확인)
    - Task 1의 실행 로그 — 안전성 조회 3건 출력
  </read_first>

  <what-built>
    Task 1이 마이그레이션 3개(구 부분 인덱스 DROP / `redevelopment_projects` UNIQUE 신설 /
    카탈로그 RPC)를 작성했고, 적용 전 안전 조건을 라이브 조회로 확인했다.
    이 태스크는 셋을 **한 번의 `db push`로** 프로덕션에 적용한다. 프로덕션 스키마 변경이므로
    사람 확인이 필요하다.
  </what-built>

  <how-to-verify>
    실행자가 아래를 순서대로 수행하고 각 단계 출력을 그대로 보고한다.

    1. **사전 확인** — `npx supabase db push --dry-run`
       - 기대: 대기 목록이 `20260731110000_*`, `20260731110001_*`, `20260731110002_*` **3건뿐**
       - 🔴 `20260731100000_*`(39-01)이 대기 목록에 뜨면 39-01의 push가 안 된 것이므로 **중단·보고**
       - 🔴 그 밖의 마이그레이션이 뜨거나 원장 불일치 에러가 나면 **drift 재발**이므로 push하지 말고
         전체 출력과 함께 보고한다

    2. **적용** — `npm run db:push`
       - 🔴 실패 시 재시도하거나 `execute_sql`·MCP `apply_migration`·대시보드로 우회하지 않는다.
         그게 Phase 37이 청소한 원장 drift의 원인이다 (Scope Fence 6). 에러 전문을 보고하고 중단한다

    3. **favorites 인덱스 확인** (읽기 전용)
       ```
       npx supabase db query --linked "select indexname, indexdef from pg_indexes where schemaname='public' and tablename='favorites' order by indexname"
       ```
       기대: `favorites_user_complex_area_site_key`(NULLS NOT DISTINCT) + `favorites_pkey` +
       비유니크 3개. **`favorites_complex_favorite_unique_idx`·`favorites_area_type_alert_unique_idx`는
       사라져야 한다.**

    4. **redevelopment 제약 확인** (읽기 전용)
       ```
       npx supabase db query --linked "select conname, pg_get_constraintdef(oid) as def from pg_constraint where conrelid='public.redevelopment_projects'::regclass and contype='u'"
       ```
       기대: `redevelopment_projects_complex_id_key` = `UNIQUE (complex_id)` 1행

    5. **RPC 존재·권한 확인** (읽기 전용)
       ```
       npx supabase db query --linked "select p.oid::regprocedure as sig, p.prosecdef as is_definer, p.proacl::text as acl from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname='unique_indexes_for_table'"
       ```
       기대: 1행, `is_definer = false`, `acl`에 `service_role=X` 포함이고 `anon`·`authenticated`·
       `=X/` (public grant) 는 **없음**

    6. **RPC 동작 확인** (읽기 전용) — 부분 인덱스 플래그가 실제로 살아 있는지 본다
       ```
       npx supabase db query --linked "select * from public.unique_indexes_for_table('new_listings') order by index_name"
       ```
       기대: `new_listings_molit_name_region_idx` 행의 `is_partial = true`,
       `new_listings_pblanc_no_key` 행의 `is_partial = false`.
       **이 한 줄이 F-05 게이트 전체의 근거다** — 부분 인덱스를 구분하지 못하면 게이트가 무의미해진다

    7. **데이터 불변** (읽기 전용)
       ```
       npx supabase db query --linked "select (select count(*) from public.favorites) as fav, (select count(*) from public.redevelopment_projects) as redev"
       ```
       기대: `fav = 4`, `redev`는 Task 1에서 실측한 값과 동일

    8. **원장 정합성** — `npx supabase migration list --linked`
       기대: local-only 0건, remote-only 0건
  </how-to-verify>

  <acceptance_criteria>
    - `db push --dry-run` 대기 목록이 `20260731110000/110001/110002` **3건뿐**
    - `npm run db:push` exit 0
    - `pg_indexes`에서 `favorites_complex_favorite_unique_idx`·`favorites_area_type_alert_unique_idx`
      **부재**, `favorites_user_complex_area_site_key` **존재**(`NULLS NOT DISTINCT` 포함)
    - `pg_constraint`에 `redevelopment_projects_complex_id_key = UNIQUE (complex_id)` 1행
    - `pg_proc`에 `unique_indexes_for_table(text)` 1행, `prosecdef = false`,
      `proacl`에 `service_role` 포함 / `anon`·`authenticated` 미포함
    - `unique_indexes_for_table('new_listings')` 출력에서 `new_listings_molit_name_region_idx`의
      `is_partial = true`
    - `favorites` 4행 불변, `redevelopment_projects` 행 수 불변
    - `npx supabase migration list --linked` local-only 0 / remote-only 0
    - `git status --porcelain src/` 빈 출력
  </acceptance_criteria>

  <action>
  Task 1이 작성한 마이그레이션 3개를 `npm run db:push`로 프로덕션에 적용한다.
  위 `<how-to-verify>` 1~8단계를 순서대로 실행하고 각 단계 출력을 그대로 사용자에게 보고한 뒤
  승인을 받는다.
  🔴 dry-run 대기 목록이 3건이 아니면 push하지 않는다.
  🔴 push 실패 시 재시도·우회 없이 **drift 재발로 간주하고 즉시 중단·보고**한다 (Scope Fence 6).
  🔴 3단계에서 통합 인덱스까지 사라졌다면 `110000`이 잘못 작성된 것이므로 즉시 보고한다 —
  임의로 인덱스를 재생성하지 말 것(원장과 파일이 어긋난다).
  </action>

  <verify>
    <automated>npx supabase migration list --linked</automated>
  </verify>

  <done>
    구 부분 인덱스 2개가 제거되고 통합 인덱스만 남았으며, `redevelopment_projects`에 UNIQUE가
    생겼고, 카탈로그 RPC가 service_role 전용으로 존재하며 부분 인덱스를 `is_partial=true`로
    구분함이 확인됐고, 원장 0/0과 데이터 불변을 확인하고 사용자가 승인했다.
  </done>

  <resume-signal>
    위 8단계 출력을 모두 확인한 뒤 "approved" 입력. 어느 단계든 기대와 다르면 그 출력을 그대로 붙여넣기.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: 고장 4건 전수 재판정 + 정상 13건 무회귀 확인 + 커밋</name>

  <files>scripts/verify-onconflict-probe.ts (실행만, 수정 없음)</files>

  <read_first>
    - `.planning/phases/39-onconflict-constraint-fix/39-CONTEXT.md` — `<baseline>`의
      **onConflict 판정 전수 표(17행)**. 이 표가 이번 재판정의 대조 기준이다.
      `<scope_fence>` **1번**(OK 13건 무접촉), `## Success Criteria` **1번**
    - `.planning/phases/39-onconflict-constraint-fix/39-00-SUMMARY.md`·`39-01-SUMMARY.md` —
      이미 확인된 F-01·F-02·F-03의 판정 결과
  </read_first>

  <action>
  **(1) 고장 4건 재판정 — raw EXPLAIN (39-CONTEXT가 지정한 판정 수단)**

  4건 전부 `npx supabase db query --linked "explain insert into … on conflict (…) do nothing"`으로
  판정하고 **출력 전문을 SUMMARY에 남긴다.** 코드를 읽고 "맞는 것 같다"는 증거가 아니다.

  | # | 테이블 | ON CONFLICT |
  |---|---|---|
  | 1 | `public.facility_kapt` | `(complex_id, data_month)` |
  | 2 | `public.favorites` | `(user_id, complex_id, area_type_id, site_id)` |
  | 3 | `public.redevelopment_projects` | `(complex_id)` |
  | 4 | `public.new_listings` | — **해당 없음.** F-03은 upsert를 제거했으므로 ON CONFLICT 판정 대상이 아니다. 대신 `grep -c "onConflict: 'name,region'" src/app/api/cron/daily/route.ts` == 0 으로 판정한다 |

  값은 존재하지 않는 UUID/더미를 쓴다. EXPLAIN은 플래닝만 하므로 데이터가 변하지 않는다.
  🔴 `explain analyze`를 쓰지 않는다 — 실제로 실행되어 데이터가 쓰인다.
  🔴 그리고 39-CONTEXT `<baseline>` 마지막 절의 교훈대로, 이 프로젝트에서 성능 판정에
  `EXPLAIN ANALYZE`의 `Execution Time`을 믿지 않는다(이 인스턴스는 `clock_gettime`이 비싸다).
  이 태스크는 **성능이 아니라 플래닝 성공 여부만** 본다.

  **(2) PostgREST 프로브 전수 실행**
  ```
  npx tsx --env-file=.env.local scripts/verify-onconflict-probe.ts
  ```
  기대: `facility_kapt (complex_id,data_month)` OK / `favorites (user_id,complex_id,area_type_id,site_id)` OK /
  `redevelopment_projects (complex_id)` OK / 음성 대조 항목들(구 컬럼 목록)은 BROKEN 유지.
  "에러 없음" 케이스 **0건**.

  **(3) 정상 13건 무회귀 확인 (Scope Fence 1)**

  39-CONTEXT `<baseline>` 표의 ✅ 13건이 여전히 통과하는지 확인한다. **RPC를 쓰면 EXPLAIN 없이
  일괄 확인이 가능하다** — 각 (테이블, 컬럼목록)에 대해
  `unique_indexes_for_table(<table>)` 결과 중 `is_partial=false and is_expression=false and is_valid`인
  행의 `columns` 집합과 일치하는 것이 하나 이상 있는지 본다:
  ```
  npx supabase db query --linked "select 'complex_gap_stats' t, * from public.unique_indexes_for_table('complex_gap_stats') union all select 'presale_transactions', * from public.unique_indexes_for_table('presale_transactions') union all select 'new_listings', * from public.unique_indexes_for_table('new_listings') union all select 'transactions', * from public.unique_indexes_for_table('transactions') union all select 'complex_rankings', * from public.unique_indexes_for_table('complex_rankings') union all select 'cafe_posts', * from public.unique_indexes_for_table('cafe_posts') union all select 'cafe_articles', * from public.unique_indexes_for_table('cafe_articles') union all select 'push_subscriptions', * from public.unique_indexes_for_table('push_subscriptions') union all select 'notification_topics', * from public.unique_indexes_for_table('notification_topics') union all select 'realtor_assignments', * from public.unique_indexes_for_table('realtor_assignments') union all select 'complex_aliases', * from public.unique_indexes_for_table('complex_aliases') union all select 'listing_prices', * from public.unique_indexes_for_table('listing_prices') union all select 'kakao_channel_subscriptions', * from public.unique_indexes_for_table('kakao_channel_subscriptions')"
  ```
  출력을 39-CONTEXT `<baseline>` 표의 13개 onConflict 문자열과 **한 줄씩 대조한 표**를 SUMMARY에
  만든다. 불일치가 1건이라도 나오면 **중단하고 보고**한다 — 이 Phase가 뭔가를 깼거나
  baseline 표가 틀렸다는 뜻이며, 어느 쪽이든 사람 판단이 필요하다.
  (이 대조는 39-03의 F-05 게이트가 자동화할 작업의 **수동 예행**이다. 여기서 한 번 손으로 맞춰두면
  39-03에서 게이트가 오탐인지 진짜인지 즉시 구분된다.)

  ⛔ 13개 지점의 **앱 코드를 고치지 않는다** (Scope Fence 1). 이 태스크는 조회·대조만 한다.

  **(4) 커밋**
  ```
  npm run lint
  git add -A supabase/ scripts/
  git commit -m "fix(db): favorites 구 부분 인덱스 제거 + redevelopment_projects UNIQUE 신설 + onConflict 검증용 카탈로그 RPC"
  ```
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && npx tsx --env-file=.env.local scripts/verify-onconflict-probe.ts && npm run lint && npx supabase migration list --linked</automated>
  </verify>

  <acceptance_criteria>
    - raw EXPLAIN 3건(`facility_kapt`·`favorites`·`redevelopment_projects`)이 **에러 없이 플랜 출력**,
      출력 전문이 SUMMARY에 기록됨
    - `grep -c "onConflict: 'name,region'" src/app/api/cron/daily/route.ts` == 0 (F-03 판정)
    - 프로브 전수 실행 exit 0, "에러 없음" 케이스 0건, 음성 대조 항목이 여전히 BROKEN
    - 정상 13건 대조 표가 SUMMARY에 존재하고 **불일치 0건**
    - `npx supabase migration list --linked` 0/0
    - `npm run lint` exit 0
    - `git status --porcelain src/` 빈 출력 (이 plan 전체에서 앱 코드 무변경)
    - 커밋 메시지가 `fix(` 로 시작
  </acceptance_criteria>

  <done>
    고장 4건이 전부 통과 판정을 받았고(EXPLAIN + 프로브 이중 확인), 정상 13건이 한 줄씩 대조돼
    무회귀임이 확인됐으며, 원장 0/0을 유지한 채 커밋됐다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 마이그레이션 파일 → 프로덕션 스키마 (`db push`) | DDL의 유일한 승인 경로. 우회가 Phase 36/37 drift의 원인 |
| PostgREST 호출자(anon/authenticated/service_role) → `public.unique_indexes_for_table` | 신규 RPC. `public` 스키마 함수는 기본적으로 PostgREST에 노출된다 — grant를 좁히지 않으면 anon도 카탈로그를 읽는다 |
| 어드민 브라우저 → `upsertRedevelopment` Server Action | `requireAdmin()` + zod 후 service_role upsert. 이 plan은 그 경로를 바꾸지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Information Disclosure | `unique_indexes_for_table` RPC | mitigate | `security invoker` + `revoke execute from public, anon, authenticated` + `grant to service_role`. Task 2 5단계에서 `proacl` 실측 확인. `SECURITY DEFINER` 명시 금지 |
| T-39-02-02 | Tampering | `favorites` 유일성 보장 | mitigate | DROP 전 통합 인덱스 존재를 Task 1 (0)·Task 2 3단계에서 라이브 확인. 통합 인덱스까지 지워지면 즉시 중단(임의 재생성 금지 — 파일↔원장 drift가 된다) |
| T-39-02-03 | Tampering | 프로덕션 스키마 (우회 적용) | mitigate | `npm run db:push`만 사용 + dry-run 대기 3건 게이트 + 실패 시 재시도·우회 금지 (Scope Fence 6) |
| T-39-02-04 | Denial of Service | `redevelopment_projects` UNIQUE 추가 실패 | mitigate | 적용 직전 중복 `complex_id` 조회 0행 확인 (Task 1 (4)). 0행이 아니면 push 중단 |
| T-39-02-05 | Repudiation | F-05 게이트의 부분 인덱스 판정 근거 | mitigate | 필터링을 SQL이 아닌 TS로 올려 DB 없이 단위 테스트 가능하게 함. Task 2 6단계에서 `is_partial=true` 실측으로 플래그가 실제로 동작함을 확인 |
| T-39-02-06 | Tampering | ✅ 13개 정상 upsert 지점 | mitigate | Task 3 (3)에서 조회·대조만 수행, 코드 무변경을 `git status --porcelain src/` 빈 출력로 강제 (Scope Fence 1) |
| T-39-02-07 | Repudiation | 원장 ↔ 프로덕션 정합성 | mitigate | `CONCURRENTLY` 미사용으로 `db push` 정상 경로 유지. 적용 후 `migration list --linked` 0/0 확인 (Scope Fence 7 / CLAUDE.md 규약) |
| T-39-02-SC | Tampering | npm/pip/cargo installs | N/A | 이 plan은 패키지를 설치하지 않는다 (신규 의존성 0건) |
</threat_model>

<verification>
1. 마이그레이션 3개 존재, 타임스탬프가 `20260731100000` 뒤
2. `110000` 본문에 구 인덱스 2개 이름 등장 / 통합 인덱스 이름 미등장
3. `110001` 본문에 `unique (complex_id)` / `concurrently` 0회
4. `110002` 본문에 `security definer` 0회 / `indpred is not null` 1회 / `indpred is null` 필터 0회 / `notify pgrst` 1회
5. 적용 전 `redevelopment_projects` 중복 `complex_id` 0행
6. `db push --dry-run` 대기 3건 → `npm run db:push` exit 0
7. `pg_indexes`: 구 부분 인덱스 2개 부재 + 통합 인덱스 존재
8. `pg_constraint`: `redevelopment_projects_complex_id_key = UNIQUE (complex_id)`
9. `pg_proc`: `prosecdef=false`, `proacl`에 `service_role` 포함 / `anon`·`authenticated` 미포함
10. `unique_indexes_for_table('new_listings')`의 `new_listings_molit_name_region_idx.is_partial = true`
11. raw EXPLAIN 3건 정상 플랜 + 프로브 전수 exit 0 ("에러 없음" 0건)
12. `grep -c "onConflict: 'name,region'" src/app/api/cron/daily/route.ts` == 0
13. 정상 13건 대조 표 불일치 0건
14. `migration list --linked` 0/0, `npm run lint` exit 0, `git status --porcelain src/` 빈 출력
</verification>

<success_criteria>
1. `favorites`에 추론 가능한 UNIQUE 인덱스가 통합본 **1개만** 남는다 (구 부분 인덱스 2개 제거)
2. `redevelopment_projects`에 `UNIQUE (complex_id)`가 생기고 앱 코드는 무변경이다
3. `public.unique_indexes_for_table(text)`가 service_role 전용으로 존재하고
   부분 인덱스를 `is_partial=true`로 구분한다
4. 고장 4건이 전부 통과 판정 — raw EXPLAIN + PostgREST 프로브 이중 확인
5. 정상 13건이 한 줄씩 대조돼 무회귀임이 확인됐고 앱 코드는 무변경이다
6. `migration list --linked` 0/0 유지, `npm run lint` 통과
</success_criteria>

<output>
Create `.planning/phases/39-onconflict-constraint-fix/39-02-SUMMARY.md` when done.

SUMMARY에 반드시 포함:
- `db push --dry-run` → `db push` 출력
- `pg_indexes`(favorites) / `pg_constraint`(redevelopment) / `pg_proc`(RPC acl) 조회 출력
- `unique_indexes_for_table('new_listings')` 출력 — `is_partial=true` 증거
- **고장 4건 재판정 표** (raw EXPLAIN 출력 + 프로브 결과)
- **정상 13건 대조 표** (baseline onConflict 문자열 ↔ 라이브 추론 가능 인덱스 컬럼 집합)
- RPC를 `SECURITY INVOKER` + service_role 전용으로 한 결정과 근거
- 필터링을 SQL이 아닌 TS로 올린 결정과 근거 (39-03 음성 대조의 전제)
</output>
