---
phase: 25-naver-listing-crawl
plan: "03"
type: execute
wave: 2
depends_on:
  - "25-01"
  - "25-02"
files_modified:
  - src/lib/data/listing-history.ts
  - src/components/complex/ListingPriceSection.tsx
  - src/components/complex/ListingPriceSectionWrapper.tsx
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - LISTING-04

must_haves:
  truths:
    - "단지 상세 페이지 '시세 흐름' 카드 아래에 '호가 vs 실거래' 섹션이 표시된다"
    - "listing_prices.source='naver' 데이터가 없는 단지는 섹션 전체가 숨겨진다"
    - "차트에 호가 평당가(주황 실선)와 실거래 평당가(파랑 점선)가 함께 표시된다"
    - "Supabase 쿼리는 서버 컴포넌트(page.tsx)에서만 수행된다"
  artifacts:
    - path: "src/lib/data/listing-history.ts"
      provides: "getListingPriceHistory 서버 데이터 함수"
      exports: ["getListingPriceHistory", "ListingPricePoint"]
    - path: "src/components/complex/ListingPriceSection.tsx"
      provides: "Recharts 차트 클라이언트 컴포넌트"
      contains: "use client"
    - path: "src/components/complex/ListingPriceSectionWrapper.tsx"
      provides: "next/dynamic SSR false 래퍼"
      contains: "dynamic"
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "ListingPriceSectionWrapper + getListingPriceHistory 통합"
      contains: "getListingPriceHistory"
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/lib/data/listing-history.ts"
      via: "import { getListingPriceHistory }"
      pattern: "getListingPriceHistory"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/components/complex/ListingPriceSectionWrapper.tsx"
      via: "import { ListingPriceSectionWrapper }"
      pattern: "ListingPriceSectionWrapper"
    - from: "src/components/complex/ListingPriceSectionWrapper.tsx"
      to: "src/components/complex/ListingPriceSection.tsx"
      via: "next/dynamic SSR:false"
      pattern: "ListingPriceSection"
---

<objective>
호가 히스토리 데이터 레이어(listing-history.ts) + 차트 컴포넌트(ListingPriceSection.tsx + Wrapper) +
단지 상세 페이지 통합을 구현한다.

Purpose: 수집된 호가 데이터를 사용자에게 실거래 대비 차트로 시각화 (LISTING-04)
Output:
  - src/lib/data/listing-history.ts
  - src/components/complex/ListingPriceSection.tsx
  - src/components/complex/ListingPriceSectionWrapper.tsx
  - src/app/complexes/[id]/page.tsx (getListingPriceHistory 추가, ListingPriceSectionWrapper 삽입)
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/CLAUDE.md
@C:/Users/jung/coding/bds/.planning/phases/25-naver-listing-crawl/25-RESEARCH.md
@C:/Users/jung/coding/bds/src/lib/data/gap-label.ts
@C:/Users/jung/coding/bds/src/components/invest/ComplexPriceChartWrapper.tsx
@C:/Users/jung/coding/bds/src/app/complexes/[id]/page.tsx

<interfaces>
<!-- gap-label.ts 패턴 — listing_prices 서버 쿼리 방식 -->
```typescript
// src/lib/data/gap-label.ts (기존)
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'
export async function getGapLabelData(complexId: string, supabase: SupabaseClient<Database>): Promise<GapLabelData>
```

<!-- ComplexPriceChartWrapper 패턴 — 'use client' + next/dynamic SSR:false -->
```typescript
// 'use client'
// import dynamic from 'next/dynamic'
// const Chart = dynamic(() => import('./ChartInner').then(m => m.ChartInner), { ssr: false, loading: ... })
// export function ChartWrapper(props) { return <Chart {...props} /> }
```

<!-- page.tsx Promise.all 패턴 — 새 데이터 함수 추가 위치 -->
// page.tsx: const [..., listingHistory] = await Promise.all([..., getListingPriceHistory(id, supabase).catch(()=>[])])
// 삽입 위치: ComplexPriceChartWrapper 아래 (시세 흐름 섹션 다음)

<!-- rawSaleData 타입 (page.tsx에서 기존 fetch) -->
// rawSaleData: Array<{ yearMonth: string; price: number; area: number; ... }>
// getComplexRawTransactions(id, 'sale', supabase) → RawTransaction[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listing-history.ts 데이터 레이어 구현</name>
  <files>src/lib/data/listing-history.ts</files>
  <read_first>
    - src/lib/data/gap-label.ts — SupabaseClient 타입, listing_prices 쿼리 패턴
    - .planning/phases/25-naver-listing-crawl/25-RESEARCH.md §5.2 — getListingPriceHistory 명세
  </read_first>
  <behavior>
    - getListingPriceHistory(complexId, supabase) → source='naver' 레코드만 반환
    - 날짜 범위: 오늘로부터 months(기본 12)개월 이전 이후 데이터만
    - recorded_date 오름차순 정렬
    - 데이터 없으면 빈 배열 반환
    - source 필터: .eq('source', 'naver') 적용
  </behavior>
  <action>
src/lib/data/listing-history.ts를 아래 명세대로 작성한다:

```typescript
// src/lib/data/listing-history.ts
// CLAUDE.md: Supabase 쿼리 → 서버 컴포넌트·API Route 전용
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export interface ListingPricePoint {
  recorded_date: string   // 'YYYY-MM-DD'
  price_per_py:  number   // 만원/평
}

/**
 * 호가 히스토리 조회 (source='naver' 전용, 최근 N개월)
 *
 * RESEARCH.md §5.2: .eq('source', 'naver') 필터
 * CLAUDE.md: 거래 조회는 서버 컴포넌트에서만
 */
export async function getListingPriceHistory(
  complexId: string,
  supabase:  SupabaseClient<Database>,
  months = 12,
): Promise<ListingPricePoint[]> {
  const cutoff = new Date(Date.now() - months * 30 * 24 * 60 * 60 * 1000)
    .toISOString()
    .slice(0, 10)

  const { data } = await supabase
    .from('listing_prices')
    .select('recorded_date, price_per_py')
    .eq('complex_id', complexId)
    .eq('source', 'naver')
    .gte('recorded_date', cutoff)
    .order('recorded_date', { ascending: true })

  return (data ?? []).map(r => ({
    recorded_date: r.recorded_date as string,
    price_per_py:  r.price_per_py as number,
  }))
}
```
  </action>
  <verify>
    <automated>grep -c "source.*naver" "C:/Users/jung/coding/bds/src/lib/data/listing-history.ts"</automated>
  </verify>
  <done>
    - src/lib/data/listing-history.ts 존재
    - export function getListingPriceHistory 포함
    - .eq('source', 'naver') 쿼리 포함
    - export interface ListingPricePoint 포함
  </done>
</task>

<task type="auto">
  <name>Task 2: ListingPriceSection 차트 컴포넌트 + Wrapper 구현</name>
  <files>src/components/complex/ListingPriceSection.tsx, src/components/complex/ListingPriceSectionWrapper.tsx</files>
  <read_first>
    - src/components/invest/ComplexPriceChartWrapper.tsx — next/dynamic SSR:false 래퍼 패턴
    - .planning/phases/25-naver-listing-crawl/25-RESEARCH.md §5.3, §5.4 — 차트 설계, 실거래 병합
    - src/app/complexes/[id]/page.tsx line 1~50 — import 패턴, rawSaleData 타입 확인
  </read_first>
  <action>
1. src/components/complex/ListingPriceSection.tsx (클라이언트 차트 컴포넌트):

```typescript
'use client'

import {
  ComposedChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  Legend,
} from 'recharts'
import type { ListingPricePoint } from '@/lib/data/listing-history'

interface TxPricePoint {
  yearMonth: string    // 'YYYY-MM'
  price:     number    // 만원
  area:      number    // m²
}

interface MergedPoint {
  yearMonth:  string
  listingPy?: number   // 호가 평당가 (만원)
  txPy?:      number   // 실거래 평당가 월평균 (만원)
}

interface Props {
  listingHistory: ListingPricePoint[]   // from listing_prices source='naver'
  rawSaleData:    TxPricePoint[]        // from getComplexRawTransactions 'sale'
}

/**
 * 호가(listing) + 실거래(tx) 병합 데이터 생성
 * RESEARCH.md §5.3
 */
function mergeData(listingHistory: ListingPricePoint[], rawSaleData: TxPricePoint[]): MergedPoint[] {
  // 호가: recorded_date(YYYY-MM-DD) → yearMonth(YYYY-MM)
  const listingByYm: Record<string, number> = {}
  for (const p of listingHistory) {
    const ym = p.recorded_date.slice(0, 7)
    // 같은 월 여러 건이면 최신값 유지 (오름차순 정렬이므로 마지막 값)
    listingByYm[ym] = p.price_per_py
  }

  // 실거래: 월별 평균 평당가
  const txByYm: Record<string, number[]> = {}
  for (const t of rawSaleData) {
    const py = t.area > 0 ? Math.round(t.price / (t.area / 3.3058)) : null
    if (!py || py < 100 || py > 99999) continue
    if (!txByYm[t.yearMonth]) txByYm[t.yearMonth] = []
    txByYm[t.yearMonth].push(py)
  }
  const txAvgByYm: Record<string, number> = {}
  for (const [ym, prices] of Object.entries(txByYm)) {
    txAvgByYm[ym] = Math.round(prices.reduce((s, v) => s + v, 0) / prices.length)
  }

  // 월 집합: 호가 + 실거래 합집합
  const allYm = Array.from(new Set([...Object.keys(listingByYm), ...Object.keys(txAvgByYm)])).sort()

  return allYm.map(ym => ({
    yearMonth:  ym,
    listingPy:  listingByYm[ym],
    txPy:       txAvgByYm[ym],
  }))
}

function formatPy(value: number): string {
  return `${value.toLocaleString()}만`
}

export function ListingPriceSection({ listingHistory, rawSaleData }: Props) {
  // RESEARCH.md §6 Pitfall 5: 양쪽 데이터 중 하나라도 없으면 섹션 숨김
  // (page.tsx에서 listingHistory.length === 0 이면 렌더하지 않음)
  const data = mergeData(listingHistory, rawSaleData)

  if (data.length === 0) return null

  return (
    <div>
      <ResponsiveContainer width="100%" height={220}>
        <ComposedChart data={data} margin={{ top: 4, right: 4, left: 0, bottom: 0 }}>
          <CartesianGrid strokeDasharray="3 3" stroke="var(--line-subtle)" />
          <XAxis
            dataKey="yearMonth"
            tick={{ fontSize: 11, fill: 'var(--fg-tertiary)' }}
            tickLine={false}
            axisLine={false}
          />
          <YAxis
            tick={{ fontSize: 11, fill: 'var(--fg-tertiary)' }}
            tickLine={false}
            axisLine={false}
            tickFormatter={formatPy}
            width={60}
          />
          <Tooltip
            formatter={(v: number) => [`${v.toLocaleString()}만원/평`]}
            labelStyle={{ fontSize: 11 }}
            contentStyle={{ fontSize: 12, border: '1px solid var(--line-default)' }}
          />
          <Legend
            wrapperStyle={{ fontSize: 11, paddingTop: 8 }}
          />
          {/* 호가 평당가 — 주황 실선 */}
          <Line
            dataKey="listingPy"
            stroke="#ea580c"
            name="호가(평당)"
            dot={false}
            connectNulls
            strokeWidth={2}
          />
          {/* 실거래 평당가 — 파랑 점선 */}
          <Line
            dataKey="txPy"
            stroke="#1d4ed8"
            name="실거래(평당)"
            dot={false}
            connectNulls
            strokeDasharray="4 2"
            strokeWidth={2}
          />
        </ComposedChart>
      </ResponsiveContainer>
    </div>
  )
}
```

2. src/components/complex/ListingPriceSectionWrapper.tsx (next/dynamic 래퍼):

```typescript
'use client'

import dynamic from 'next/dynamic'
import type { ListingPricePoint } from '@/lib/data/listing-history'

interface TxPricePoint {
  yearMonth: string
  price:     number
  area:      number
}

const ListingPriceSectionInner = dynamic(
  () => import('./ListingPriceSection').then(m => m.ListingPriceSection),
  {
    ssr:     false,
    loading: () => (
      <div
        style={{
          height:         220,
          background:     'var(--bg-surface-2)',
          borderRadius:   6,
          display:        'flex',
          alignItems:     'center',
          justifyContent: 'center',
        }}
      >
        <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
          차트 로딩 중…
        </span>
      </div>
    ),
  },
)

interface Props {
  listingHistory: ListingPricePoint[]
  rawSaleData:    TxPricePoint[]
}

export function ListingPriceSectionWrapper({ listingHistory, rawSaleData }: Props) {
  return <ListingPriceSectionInner listingHistory={listingHistory} rawSaleData={rawSaleData} />
}
```

UI 금지 사항 준수 (CLAUDE.md):
- backdrop-blur, gradient-text, glow 없음
- 보라/인디고 아닌 #ea580c(주황), #1d4ed8(파랑) 사용
- 애니메이션 없음 (Recharts 기본 애니메이션은 CSS transform 기반으로 허용)
  </action>
  <verify>
    <automated>grep -c "use client" "C:/Users/jung/coding/bds/src/components/complex/ListingPriceSection.tsx"</automated>
  </verify>
  <done>
    - src/components/complex/ListingPriceSection.tsx: 'use client', ComposedChart, Line 두 개 포함
    - src/components/complex/ListingPriceSectionWrapper.tsx: 'use client', dynamic, ssr: false 포함
    - 차트 색상: #ea580c(호가), #1d4ed8(실거래) — 보라/인디고 없음
  </done>
</task>

<task type="auto">
  <name>Task 3: 단지 상세 페이지 통합</name>
  <files>src/app/complexes/[id]/page.tsx</files>
  <read_first>
    - src/app/complexes/[id]/page.tsx — Promise.all 블록(line ~241~314), ComplexPriceChartWrapper 삽입 위치(line ~742~787)
    - src/lib/data/listing-history.ts (Task 1 결과물) — getListingPriceHistory import
    - src/components/complex/ListingPriceSectionWrapper.tsx (Task 2 결과물) — ListingPriceSectionWrapper import
  </read_first>
  <action>
src/app/complexes/[id]/page.tsx를 다음 3곳 수정한다.

**수정 1: import 추가** (파일 상단 import 블록에 추가)
```typescript
import { getListingPriceHistory } from '@/lib/data/listing-history'
import type { ListingPricePoint } from '@/lib/data/listing-history'
import { ListingPriceSectionWrapper } from '@/components/complex/ListingPriceSectionWrapper'
```

**수정 2: Promise.all 블록에 getListingPriceHistory 추가**
기존 `nearbyComplexes` 항목 바로 다음에 추가:
```typescript
// 호가 히스토리 (source='naver', 12개월) — LISTING-04
getListingPriceHistory(id, supabase).catch((): ListingPricePoint[] => []),
```
그리고 구조분해 최상단의 배열에 `listingHistory` 변수 추가:
```typescript
const [
  saleData,
  // ... (기존 항목 유지)
  nearbyComplexes,
  listingHistory,  // ← 추가
] = await Promise.all([
  // ... (기존 항목 유지)
  getNearbyComplexPrices(supabase, id).catch(() => []),
  getListingPriceHistory(id, supabase).catch((): ListingPricePoint[] => []),  // ← 추가
])
```

**수정 3: JSX에 ListingPriceSectionWrapper 삽입**
ComplexPriceChartWrapper 블록 (시세 흐름 카드 `</div>`) 바로 다음, NearbyCompareCard 블록 바로 앞에 삽입:

```tsx
{/* 호가 vs 실거래 히스토리 — LISTING-04 */}
{/* RESEARCH.md §6 Pitfall 5: source='naver' 데이터 없으면 섹션 숨김 */}
{listingHistory.length > 0 && (
  <div className="card" style={{ padding: 20 }}>
    <h3 style={{ font: '700 15px/1.4 var(--font-sans)', margin: '0 0 12px' }}>
      호가 vs 실거래
    </h3>
    <p style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 16, margin: '0 0 16px' }}>
      네이버 부동산 매물 호가 중앙값(평당) vs 실거래 평균(평당)
    </p>
    <ListingPriceSectionWrapper
      listingHistory={listingHistory}
      rawSaleData={rawSaleData.map(t => ({
        yearMonth: t.yearMonth,
        price:     t.price,
        area:      t.area,
      }))}
    />
    <p style={{ font: '400 11px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '12px 0 0' }}>
      * 호가는 수집 시점 기준이며 실제 거래가와 다를 수 있습니다.
    </p>
  </div>
)}
```

CRITICAL:
- rawSaleData의 타입을 확인하여 { yearMonth, price, area } 필드명이 일치하는지 검증.
  page.tsx의 `getComplexRawTransactions` 반환 타입에 따라 필드명을 맞출 것.
  실제 필드명이 다르면 (예: `area_m2` 등) 그에 맞게 조정.
- `export const revalidate = 86400` 유지 — ISR 재검증 주기 변경 금지.
- 기존 Promise.all 항목 순서 변경 금지 — 맨 마지막에 추가.
  </action>
  <verify>
    <automated>cd "C:/Users/jung/coding/bds" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - page.tsx에 import getListingPriceHistory 포함
    - page.tsx에 import ListingPriceSectionWrapper 포함
    - page.tsx Promise.all에 getListingPriceHistory 추가됨
    - page.tsx JSX에 listingHistory.length > 0 조건부 렌더 포함
    - npm run build 성공 (타입 에러 없음)
    - npm run lint 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listing_prices→클라이언트 | RSC에서 fetch, 클라이언트 컴포넌트에 props로 전달 |
| rawSaleData→차트 | 기존 거래 데이터 재사용, 별도 신뢰 경계 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Information Disclosure | listing_prices 공개 조회 | accept | 기존 "public read" 정책 그대로 (Phase 5 설계 의도) |
| T-25-03-02 | Tampering | rawSaleData 차트 표시 | accept | 서버에서 fetch한 데이터만 props로 전달, 클라이언트 조작 불가 |
| T-25-03-03 | Denial of Service | Recharts SSR 비용 | mitigate | next/dynamic ssr:false로 SSR 제외, 클라이언트 렌더만 |
</threat_model>

<verification>
```bash
cd "C:/Users/jung/coding/bds"

# 파일 존재 확인
ls src/lib/data/listing-history.ts
ls src/components/complex/ListingPriceSection.tsx
ls src/components/complex/ListingPriceSectionWrapper.tsx

# 핵심 패턴 확인
grep "getListingPriceHistory" src/app/complexes/[id]/page.tsx
grep "ListingPriceSectionWrapper" src/app/complexes/[id]/page.tsx
grep "listingHistory.length > 0" src/app/complexes/[id]/page.tsx
grep "source.*naver" src/lib/data/listing-history.ts
grep "use client" src/components/complex/ListingPriceSection.tsx

# 빌드 + 린트
npm run build 2>&1 | tail -10
npm run lint
npm run test -- --reporter=verbose listing
```
</verification>

<success_criteria>
1. npm run build 성공 (타입 에러 없음)
2. npm run lint 통과
3. 단지 상세 페이지 page.tsx: getListingPriceHistory import + Promise.all 포함
4. listingHistory.length > 0 조건부로 섹션 숨김 처리 포함
5. ListingPriceSection: 'use client', 호가(#ea580c), 실거래(#1d4ed8) Line 두 개
6. listing-history.ts: .eq('source', 'naver') 쿼리 포함
7. export const revalidate = 86400 유지
</success_criteria>

<output>
완료 후 .planning/phases/25-naver-listing-crawl/25-03-SUMMARY.md 작성
</output>
