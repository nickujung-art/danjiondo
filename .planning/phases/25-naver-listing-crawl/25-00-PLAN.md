---
phase: 25-naver-listing-crawl
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260617000001_phase25_naver_complex_no.sql
autonomous: false
requirements:
  - LISTING-01

must_haves:
  truths:
    - "complexes 테이블에 naver_complex_no TEXT NULLABLE 컬럼이 존재한다"
    - "listing_prices 테이블에 service_role이 INSERT/UPDATE할 수 있음을 주석으로 문서화한다"
  artifacts:
    - path: "supabase/migrations/20260617000001_phase25_naver_complex_no.sql"
      provides: "naver_complex_no 컬럼 마이그레이션"
      contains: "naver_complex_no"
  key_links:
    - from: "scripts/map-naver-complexes.ts"
      to: "complexes.naver_complex_no"
      via: "UPDATE WHERE id = ..."
      pattern: "naver_complex_no"
---

<objective>
complexes 테이블에 naver_complex_no TEXT NULLABLE 컬럼을 추가한다.
listing_prices는 service_role(RLS 우회)로 INSERT 가능하므로 별도 write 정책은 불필요하나,
이 사실을 마이그레이션 주석으로 명시한다.

Purpose: Wave 1 스크립트(map-naver-complexes.ts, crawl-naver-listings.ts)가 의존하는 DB 스키마 확립
Output: supabase/migrations/20260617000001_phase25_naver_complex_no.sql
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/ROADMAP.md
@C:/Users/jung/coding/bds/CLAUDE.md
@C:/Users/jung/coding/bds/supabase/migrations/20260507000005_phase5_listing_prices.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: naver_complex_no 마이그레이션 작성</name>
  <files>supabase/migrations/20260617000001_phase25_naver_complex_no.sql</files>
  <read_first>
    - supabase/migrations/20260507000005_phase5_listing_prices.sql — 기존 listing_prices 스키마 확인
  </read_first>
  <action>
파일 내용을 정확히 아래와 같이 작성한다:

```sql
-- Phase 25: complexes.naver_complex_no 컬럼 추가
-- 네이버 부동산 단지 ID(예: "19672") 매핑용 — map-naver-complexes.ts 스크립트가 채운다.
-- NULL = 아직 매핑 안 됨. IS NOT NULL = 크롤링 대상.

ALTER TABLE public.complexes
  ADD COLUMN IF NOT EXISTS naver_complex_no TEXT;

-- 동일 naver_complex_no가 두 단지에 매핑되는 걸 방지 (부분 유니크: null 제외)
CREATE UNIQUE INDEX IF NOT EXISTS complexes_naver_complex_no_unique_idx
  ON public.complexes (naver_complex_no)
  WHERE naver_complex_no IS NOT NULL;

-- listing_prices: service_role 클라이언트는 RLS를 우회하므로 크롤링 스크립트용 정책 추가 불필요.
-- (src/lib/supabase/admin.ts의 createClient(url, SERVICE_ROLE_KEY) 패턴으로 RLS bypass)
-- 기존 "listing_prices: public read" + "listing_prices: admin write" 정책은 유지됨.
-- 이 주석은 나중에 읽는 개발자를 위한 의도 문서화.
COMMENT ON COLUMN public.complexes.naver_complex_no IS
  '네이버 부동산 단지 코드(hscpNo). scripts/map-naver-complexes.ts로 매핑. NULL=미매핑.';
```
  </action>
  <verify>
    <automated>grep -c "naver_complex_no" "C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase25_naver_complex_no.sql"</automated>
  </verify>
  <done>마이그레이션 파일이 존재하고 naver_complex_no 키워드가 3회 이상 포함된다</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: supabase db push 실행</name>
  <what-built>20260617000001_phase25_naver_complex_no.sql 마이그레이션 파일</what-built>
  <how-to-verify>
1. 터미널에서 프로젝트 루트로 이동 후 실행:
   ```
   npx supabase db push
   ```
2. 성공 메시지 확인 후 다음을 실행:
   ```
   npx supabase db diff --use-migra
   ```
   → 출력에 `naver_complex_no` 관련 diff 없으면 마이그레이션 완료.
3. 또는 Supabase 대시보드 Table Editor → complexes 테이블에서 naver_complex_no 컬럼 존재 확인.
  </how-to-verify>
  <resume-signal>"db push 완료" 또는 에러 메시지를 입력해주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 마이그레이션 파일→Supabase | SQL DDL이 프로덕션 DB에 직접 적용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-00-01 | Tampering | complexes.naver_complex_no | mitigate | UNIQUE INDEX (null 제외)로 동일 complexNo 중복 매핑 방지 |
| T-25-00-02 | Information Disclosure | service_role 키 | accept | .env.local gitignore 적용됨 (기존 패턴), 스크립트는 로컬 전용 실행 |
</threat_model>

<verification>
```bash
# 마이그레이션 파일 존재 확인
ls C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase25_naver_complex_no.sql

# 파일 내용 핵심 키워드 확인
grep -c "naver_complex_no" C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase25_naver_complex_no.sql
# 출력 >= 3

grep "ADD COLUMN" C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase25_naver_complex_no.sql
# "ADD COLUMN IF NOT EXISTS naver_complex_no TEXT" 포함 확인
```
</verification>

<success_criteria>
1. supabase/migrations/20260617000001_phase25_naver_complex_no.sql 파일 존재
2. ALTER TABLE complexes ADD COLUMN naver_complex_no TEXT 구문 포함
3. UNIQUE INDEX (naver_complex_no IS NOT NULL 조건) 포함
4. supabase db push 성공 완료
</success_criteria>

<output>
완료 후 .planning/phases/25-naver-listing-crawl/25-00-SUMMARY.md 작성
</output>
