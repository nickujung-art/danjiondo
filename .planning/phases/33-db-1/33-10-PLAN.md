---
phase: 33-db-1
plan: "10"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/lib/data/regions.ts
  - src/services/molit-unsold.ts
  - src/services/molit-unsold.test.ts
  - scripts/fetch-regional-unsold.ts
  - src/lib/data/realprice-officetel.ts
  - scripts/backfill-officetel.ts
autonomous: true
requirements:
  - REGION-11
must_haves:
  truths:
    - "molit-unsold.ts의 resolveSggCode가 CHANGWON_GU_MAP 정적 배열과 명시적 null 폴백(진주시·통영시 등) 대신 regions 테이블 기반 동적 조회로 시군구를 판별하여, 경남 신규 16개 시군구의 월별 미분양현황이 자동으로 sgg_code에 매칭된다"
    - "realprice-officetel.ts의 getOrCreateOffiComplex가 SGG_TO_ADDR 6행 고정 배열 대신 regions 테이블 기반 동적 조회로 si/gu를 채워, 경남 신규 시군구 오피스텔 자동 생성 시 si/gu가 null로 떨어지지 않는다"
  artifacts:
    - path: "src/lib/data/regions.ts"
      provides: "getActiveRegionAddrs — regions 테이블 is_active=true 전체 (sgg_code, si, gu) 조회, molit-unsold.ts/realprice-officetel.ts 공용"
      exports: ["getActiveRegionAddrs"]
    - path: "src/services/molit-unsold.ts"
      provides: "resolveSggCode(item, regions) — regions 배열 파라미터 기반 동적 시군구 역매칭 (CHANGWON_GU_MAP 제거)"
    - path: "src/lib/data/realprice-officetel.ts"
      provides: "getOrCreateOffiComplex가 regionAddrMap(Map<sgg_code, RegionAddr>) 파라미터 기반으로 si/gu 조회 (SGG_TO_ADDR 제거)"
  key_links:
    - from: "scripts/fetch-regional-unsold.ts"
      to: "src/lib/data/regions.ts"
      via: "await getActiveRegionAddrs(supabase)"
      pattern: "getActiveRegionAddrs"
    - from: "src/lib/data/realprice-officetel.ts"
      to: "src/lib/data/regions.ts"
      via: "await getActiveRegionAddrs(supabase)"
      pattern: "getActiveRegionAddrs"
---

<objective>
**(리비전 추가, 2026-07-03 plan-checker 2차 검증 대응 — blocker 2건):** plan-checker의 2차 검증 패스가 기존 grep 패턴(`ALLOWED_SGG_CODES|ACTIVE_SGG_CODES|TARGET_SGG|VALID_SGG_CODES|LAWD_CODES|offiSggCodes`)에 걸리지 않는 다른 명명 규칙을 쓰는 하드코딩 지점 2곳을 발견했다 — `src/services/molit-unsold.ts`의 `resolveSggCode`/`CHANGWON_GU_MAP`과 `src/lib/data/realprice-officetel.ts`의 `SGG_TO_ADDR`이다.

**Fix 1 (molit-unsold.ts):** 이미 경상남도 전체를 조회하는 `fetchGyeongnamUnsold()`(province-wide API) 결과를, `resolveSggCode()`가 창원 5개 구 + 김해만 하드코딩된 `CHANGWON_GU_MAP`으로 판별하고 `return null // 서비스 범위 외 (진주시, 통영시 등)`로 나머지 16개 신규 시군구를 전부 버리고 있었다. 매월 1일 GitHub Actions(`fetch-regional-unsold.yml`, `cron: '0 15 1 * *'`)가 `scripts/fetch-regional-unsold.ts`를 통해 이 함수를 호출하여 `regional_unsold` 테이블에 적재한다.

**Fix 2 (realprice-officetel.ts):** 33-03이 활성화하는 오피스텔 ingest 루프(`cron/daily/route.ts`의 offi 루프가 `activeSggCodes` 22개 전체를 순회하도록 전환됨)의 직접적인 결과로 발생하는 갭이다. K-apt 매칭에 실패한 신규 지역 오피스텔은 `getOrCreateOffiComplex()`가 `complexes`에 자동 INSERT하는데, `SGG_TO_ADDR`이 6개 지역만 정의되어 있어 나머지 16개 신규 시군구 오피스텔은 `si: null, gu: null`로 저장된다.

두 파일 모두 `src/lib/data/regions.ts`에 신설하는 공용 헬퍼 `getActiveRegionAddrs()`(regions 테이블 `is_active=true` 행의 `sgg_code, si, gu`를 반환)를 사용하여, 정적 배열을 완전히 대체한다.

Purpose: 경남 신규 16개 시군구의 월별 미분양현황 집계와, 오피스텔 자동 생성 시 주소(si/gu) 정합성을 regions 테이블 단일 소스로 통일한다.
Output: `CHANGWON_GU_MAP`/`SGG_TO_ADDR` 정적 배열 2곳이 제거되고 `getActiveRegionAddrs()` 동적 조회로 전환됨. `resolveSggCode`의 신규 유닛 테스트 추가.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
<!-- src/lib/data/regions.ts (33-00에서 생성된 기존 함수 — 이 plan은 건드리지 않고, 아래에 함수 하나만 추가) -->
```typescript
export async function getActiveSggCodes(supabase: SupabaseClient<Database>): Promise<string[]>
export async function getActiveCityNames(supabase: SupabaseClient<Database>): Promise<string[]>
```

<!-- regions 테이블 실제 컬럼 타입 (src/types/database.ts) — si는 NOT NULL이므로 매칭만 되면 si는 항상 문자열 -->
```typescript
regions.Row = {
  created_at: string
  gu:         string | null
  is_active:  boolean
  sgg_code:   string
  sgg_name:   string
  si:         string
}
```

<!-- src/services/molit-unsold.ts 현재 전체 (라인 70~91, 변경 대상) -->
```typescript
// ── 시군구명 → sgg_code 매핑 ────────────────────────────────────
// 창원시는 rdnmadr에서 구 이름 추출 필요

const CHANGWON_GU_MAP: Record<string, string> = {
  '의창구': '48121',
  '성산구': '48123',
  '마산합포구': '48125',
  '마산회원구': '48127',
  '진해구': '48129',
}

export function resolveSggCode(item: UnsoldItem): string | null {
  const sgg = item.signgunm
  if (sgg.includes('김해')) return '48250'
  if (sgg.includes('창원')) {
    for (const [guName, code] of Object.entries(CHANGWON_GU_MAP)) {
      if (item.rdnmadr.includes(guName)) return code
    }
    return null // 창원시이지만 구 파악 불가
  }
  return null // 서비스 범위 외 (진주시, 통영시 등)
}
```

<!-- scripts/fetch-regional-unsold.ts 현재 전체 (변경 대상) -->
```typescript
import { createClient } from '@supabase/supabase-js'
import { fetchGyeongnamUnsold, resolveSggCode } from '../src/services/molit-unsold'

// ... currentYearMonth() ...

async function main() {
  const supabase: any = createClient(/* ... */)
  const yearMonth = currentYearMonth()
  console.log(`[fetch-regional-unsold] year_month=${yearMonth}`)

  const { items, totalCount } = await fetchGyeongnamUnsold(1, 1000)

  const agg: Record<string, number> = {}
  let skipped = 0
  for (const item of items) {
    const sggCode = resolveSggCode(item)
    if (!sggCode) { skipped++; continue }
    agg[sggCode] = (agg[sggCode] ?? 0) + item.unsoldcnt_this
  }
  // ... upsert regional_unsold ...
}
```

<!-- src/lib/data/realprice-officetel.ts 현재 관련 부분 (변경 대상) -->
```typescript
// ── 지역코드 → 시·구 매핑 (창원·김해 한정) ──────────────────
const SGG_TO_ADDR: Record<string, { si: string; gu: string | null }> = {
  '48121': { si: '창원시', gu: '의창구' },
  '48123': { si: '창원시', gu: '성산구' },
  '48125': { si: '창원시', gu: '마산합포구' },
  '48127': { si: '창원시', gu: '마산회원구' },
  '48129': { si: '창원시', gu: '진해구' },
  '48250': { si: '김해시', gu: null },
}

async function getOrCreateOffiComplex(
  supabase: SupabaseClient,
  offiNm: string,
  sggCd: string,
  umdNm?: string,
  buildYear?: number,
  cache: Map<string, string | null> = new Map(),
): Promise<string | null> {
  // ... 1단계 매칭 시도 ...
  // 2단계: 매칭 실패 → 오피스텔 단지 자동 생성
  const addr = SGG_TO_ADDR[sggCd]
  const { data: newComplex, error } = await supabase
    .from('complexes')
    .insert({
      // ...
      si: addr?.si ?? null,
      gu: addr?.gu ?? null,
      // ...
    })
  // ...
}

export async function ingestOffiMonth(
  sggCode: string,
  yearMonth: string,
  supabase: SupabaseClient,
): Promise<IngestResult> {
  // ... runId 생성 ...
  let rowsFetched = 0
  // ... 카운터 5개 ...
  let totalRows = 0

  // 단지 캐시 (ingestOffiMonth 호출 당 — 중복 INSERT 방지)
  const complexCache = new Map<string, string | null>()

  async function processSaleItem(raw: unknown): Promise<void> {
    // ...
    const complexId = await getOrCreateOffiComplex(
      supabase, item.offiNm, String(item.sggCd),
      item.umdNm, item.buildYear, complexCache,
    )
    // ...
  }

  async function processRentItem(raw: unknown): Promise<void> {
    // ...
    const complexId = await getOrCreateOffiComplex(
      supabase, item.offiNm, String(item.sggCd),
      item.umdNm, item.buildYear, complexCache,
    )
    // ...
  }
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: molit-unsold.ts + fetch-regional-unsold.ts regions 테이블 동적 조회 전환 (CHANGWON_GU_MAP 제거) — Fix 1</name>
  <files>src/lib/data/regions.ts, src/services/molit-unsold.ts, src/services/molit-unsold.test.ts, scripts/fetch-regional-unsold.ts</files>
  <read_first>src/lib/data/regions.ts, src/services/molit-unsold.ts, scripts/fetch-regional-unsold.ts</read_first>
  <behavior>
    - Test 1: 창원시 — `signgunm`이 "창원시"를 포함하고 `rdnmadr`에 "성산구"가 포함되면 `48123`을 반환한다 (다중 gu 후보를 rdnmadr로 좁힘)
    - Test 2: 김해시 — `signgunm`이 "김해시"이고 regions에 gu=null인 단일 후보만 있으면 rdnmadr 확인 없이 바로 `48250`을 반환한다
    - Test 3: 경남 신규 시군구(진주시) — regions 배열에 `{sgg_code:'48170', si:'진주시', gu:null}`이 있으면 `CHANGWON_GU_MAP`에 없던 코드도 동적으로 `48170`을 반환한다 (이번 Fix의 핵심 회귀 방지 테스트)
    - Test 4: regions 배열에 없는 시군구(예: 부산광역시)는 `null`을 반환한다
    - Test 5: 창원시이지만 rdnmadr에서 구를 특정할 수 없으면 `null`을 반환한다 (기존 "구 파악 불가" 동작 보존)
  </behavior>
  <action>
**1. `src/lib/data/regions.ts`** — 파일 끝(기존 `getActiveCityNames` 다음)에 신규 함수를 추가한다. 기존 두 함수는 전혀 수정하지 않는다:

```typescript
/**
 * regions 테이블에서 is_active=true인 전체 (sgg_code, si, gu) 행을 조회한다.
 * CHANGWON_GU_MAP(molit-unsold.ts)/SGG_TO_ADDR(realprice-officetel.ts) 등
 * 정적 시군구 주소 매핑 배열을 대체하는 유일한 지역 주소 마스터 접근 경로 (Phase 33 2차 리비전).
 */
export interface RegionAddr {
  sgg_code: string
  si: string
  gu: string | null
}

export async function getActiveRegionAddrs(
  supabase: SupabaseClient<Database>,
): Promise<RegionAddr[]> {
  const { data, error } = await supabase
    .from('regions')
    .select('sgg_code, si, gu')
    .eq('is_active', true)
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  return data ?? []
}
```

**2. `src/services/molit-unsold.ts`** — 라인 70~91 전체(주석 헤더 + `CHANGWON_GU_MAP` + `resolveSggCode`)를 아래로 완전히 교체한다:

```typescript
// ── 시군구명 → sgg_code 매핑 (regions 테이블 기반 동적 역매칭) ──────
// CHANGWON_GU_MAP 등 정적 배열 대신 활성 regions 행(si/gu)을 역매칭한다.
// signgunm으로 si 후보를 좁히고, 창원처럼 si가 같은 복수 gu 후보가 있으면
// rdnmadr에서 gu 이름을 찾아 단일 코드로 좁힌다.

export interface RegionAddrEntry {
  sgg_code: string
  si: string
  gu: string | null
}

export function resolveSggCode(item: UnsoldItem, regions: RegionAddrEntry[]): string | null {
  const candidates = regions.filter(r => item.signgunm.includes(r.si))
  if (candidates.length === 0) return null // regions 테이블에 없는 시군구 (서비스 범위 외)
  if (candidates.length === 1) return candidates[0].sgg_code
  const guMatch = candidates.find(r => r.gu != null && item.rdnmadr.includes(r.gu))
  return guMatch?.sgg_code ?? null // si는 같으나 구(rdnmadr) 파악 불가
}
```

이 파일은 `src/services/` 어댑터이므로 `@/lib/data/regions`를 직접 import하지 않는다 (CLAUDE.md 계층 규칙 — 어댑터는 순수 함수로 유지, DB 조회는 호출자가 주입). `RegionAddrEntry`는 `regions.ts`의 `RegionAddr`와 구조적으로 동일하되 이 파일 로컬 타입으로 독립 정의한다.

**3. `src/services/molit-unsold.test.ts` 신규 생성** — Vitest, mock 없이 순수 함수 테스트 (behavior 섹션의 5개 케이스 그대로 구현). `UnsoldItem`의 모든 필수 필드를 채우는 `makeItem(overrides)` 헬퍼를 파일 내부에 둔다.

**4. `scripts/fetch-regional-unsold.ts`** 수정:
- import에 `import { getActiveRegionAddrs } from '../src/lib/data/regions'` 추가 (`resolveSggCode` import 다음 줄)
- `main()` 함수 내부, `console.log(\`[fetch-regional-unsold] year_month=${yearMonth}\`)` 다음 줄에 `const regionAddrs = await getActiveRegionAddrs(supabase)` 추가
- 루프 내부 `const sggCode = resolveSggCode(item)`를 `const sggCode = resolveSggCode(item, regionAddrs)`로 변경
- 이 스크립트가 `regions` 테이블 조회와 `molit-unsold.ts` 어댑터를 함께 조립하는 composition root 역할을 한다 (CLAUDE.md 계층 규칙 준수 — script 레이어에서 서비스 어댑터 + 데이터 레이어를 조립)
  </action>
  <acceptance_criteria>
    - `grep -c "CHANGWON_GU_MAP" src/services/molit-unsold.ts` == 0
    - `grep -c "getActiveRegionAddrs" src/lib/data/regions.ts` == 1
    - `grep -c "getActiveRegionAddrs" scripts/fetch-regional-unsold.ts` == 2 (import 1 + 호출 1)
    - `grep -c "resolveSggCode(item, regionAddrs)" scripts/fetch-regional-unsold.ts` == 1
    - `npx vitest run src/services/molit-unsold.test.ts` 통과 (5개 테스트 전부)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/molit-unsold.test.ts && npx tsc --noEmit --pretty false 2>&1 | grep -i "molit-unsold\|fetch-regional-unsold\|lib/data/regions" | head -20</automated>
  </verify>
  <done>CHANGWON_GU_MAP이 molit-unsold.ts에서 완전히 사라졌고, resolveSggCode가 regions 배열 파라미터 기반으로 동적 판별하며(경남 신규 16개 시군구 포함), fetch-regional-unsold.ts가 getActiveRegionAddrs()로 조회한 regions를 주입한다. molit-unsold.test.ts 5개 테스트 통과, tsc 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 2: realprice-officetel.ts regions 테이블 동적 조회 전환 (SGG_TO_ADDR 제거) — Fix 2</name>
  <files>src/lib/data/realprice-officetel.ts</files>
  <read_first>src/lib/data/realprice-officetel.ts</read_first>
  <action>
**`src/lib/data/realprice-officetel.ts`** 수정 (Task 1에서 이미 존재하는 `src/lib/data/regions.ts`의 `getActiveRegionAddrs`/`RegionAddr`를 재사용):

1. import 목록에 `import { getActiveRegionAddrs, type RegionAddr } from './regions'` 추가 (`import { nameNormalize } from './name-normalize'` 다음 줄)

2. 라인 19~27의 주석 헤더 + `const SGG_TO_ADDR: Record<string, { si: string; gu: string | null }> = { ... }` 전체를 완전히 삭제한다.

3. `getOrCreateOffiComplex` 함수 시그니처(라인 47~54)에 파라미터를 추가한다:
```typescript
async function getOrCreateOffiComplex(
  supabase: SupabaseClient,
  offiNm: string,
  sggCd: string,
  umdNm?: string,
  buildYear?: number,
  cache: Map<string, string | null> = new Map(),
  regionAddrMap: Map<string, RegionAddr> = new Map(),
): Promise<string | null> {
```
(기존 `cache` 파라미터처럼 `regionAddrMap`도 안전 기본값 `new Map()`을 둔다 — 실제 호출부는 항상 채워진 Map을 전달하지만, 시그니처 자체는 방어적으로 유지)

4. 함수 본문의 `const addr = SGG_TO_ADDR[sggCd]`를 `const addr = regionAddrMap.get(sggCd)`로 변경한다. 이후 `si: addr?.si ?? null, gu: addr?.gu ?? null` 로직은 그대로 유지한다 (regions 테이블의 `si`는 NOT NULL이므로, `activeSggCodes`에서 온 sggCd는 이제 항상 addr을 찾고 si가 채워진다 — 매칭 실패 폴백만 방어적으로 남겨둔다).

5. `ingestOffiMonth` 함수 내부, `let totalRows = 0` 선언 다음 줄이자 `// 단지 캐시` 주석 이전에 추가:
```typescript
  // sgg_code → si/gu 매핑 (regions 테이블 기반, ingestOffiMonth 호출당 1회 조회 — SGG_TO_ADDR 정적 배열 대체)
  const regionAddrsList = await getActiveRegionAddrs(supabase)
  const regionAddrMap = new Map(regionAddrsList.map(r => [r.sgg_code, r]))
```

6. `processSaleItem`과 `processRentItem` 내부 두 곳의 `getOrCreateOffiComplex(...)` 호출에 각각 `regionAddrMap`을 6번째 인자로 추가한다:
```typescript
const complexId = await getOrCreateOffiComplex(
  supabase, item.offiNm, String(item.sggCd),
  item.umdNm, item.buildYear, complexCache, regionAddrMap,
)
```
(processSaleItem, processRentItem 두 곳 모두 동일하게 수정)
  </action>
  <acceptance_criteria>
    - `grep -c "SGG_TO_ADDR" src/lib/data/realprice-officetel.ts` == 0
    - `grep -c "getActiveRegionAddrs" src/lib/data/realprice-officetel.ts` == 2 (import 1 + 호출 1)
    - `grep -c "regionAddrMap" src/lib/data/realprice-officetel.ts` >= 4 (선언 2 + getOrCreateOffiComplex 호출 2곳)
    - `grep -c "regionAddrMap.get(sggCd)" src/lib/data/realprice-officetel.ts` == 1
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "realprice-officetel" | head -20</automated>
  </verify>
  <done>SGG_TO_ADDR 정적 배열이 realprice-officetel.ts에서 완전히 사라졌고, getOrCreateOffiComplex가 ingestOffiMonth당 1회 조회한 regionAddrMap을 통해 si/gu를 결정하며, 경남 신규 시군구 오피스텔 자동 생성 시 si/gu가 null로 떨어지지 않는다. tsc 에러 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub Actions cron(`fetch-regional-unsold.yml`) → 경상남도 미분양현황 API | 외부 API 응답(시군구명·도로명주소 문자열)을 신뢰하여 substring 매칭 — 기존 패턴과 동일 신뢰 수준, 변경 없음 |
| cron/daily/route.ts offi 루프(33-03) → getOrCreateOffiComplex → complexes INSERT | service_role 전용 내부 파이프라인, 사용자 입력 경로 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-15 | Tampering | molit-unsold.ts resolveSggCode 입력(signgunm/rdnmadr) | accept | 국토부 산하 data.go.kr 공식 API 응답, 사용자 입력 아님 — 기존 CHANGWON_GU_MAP 방식과 동일한 신뢰 수준 유지 |
| T-33-16 | Information Disclosure | realprice-officetel.ts regionAddrMap | accept | 공개 정보(지역 시/구 이름)를 regions 테이블에서 조회 — 민감정보 아님, service_role 전용 파이프라인 내부 사용 |
</threat_model>

<verification>
```bash
npx vitest run src/services/molit-unsold.test.ts
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. molit-unsold.ts에서 CHANGWON_GU_MAP이 제거되고 resolveSggCode가 regions 배열 파라미터 기반으로 경남 전체 22개 시군구를 동적 판별
2. fetch-regional-unsold.ts가 getActiveRegionAddrs()로 조회한 regions를 resolveSggCode에 주입, 매월 미분양현황 적재 시 신규 16개 시군구도 자동 포함
3. realprice-officetel.ts에서 SGG_TO_ADDR가 제거되고 getOrCreateOffiComplex가 regionAddrMap 기반으로 si/gu 결정, 신규 지역 오피스텔 자동 생성 시 si/gu null 문제 해소
4. molit-unsold.test.ts 5개 테스트 통과, tsc/lint 통과
</success_criteria>

<addendum>
**Task 3 (2026-07-03, 오케스트레이터 직접 처리 — plan-checker 3차 검증에서 발견):**
`scripts/backfill-officetel.ts:30`의 `SGG_CODES` 6개 하드코딩 기본값을, `scripts/backfill-realprice.ts`의 `getSggCodes()`와 동일한 패턴(regions 테이블 `is_active=true` 동적 조회, `--sgg` 있으면 우선)으로 교체 완료. 동일 버그 클래스의 4번째 발생이라 별도 planner 리비전 라운드 없이 직접 수정 — `grep -n "SGG_CODES" scripts/backfill-officetel.ts`가 함수 선언(`getSggCodes`)만 매치하고 정적 배열은 남아있지 않음, `npx tsc --noEmit`으로 타입 에러 없음 확인.
</addendum>

<output>
완료 후 `.planning/phases/33-db-1/33-10-SUMMARY.md` 생성.
</output>
