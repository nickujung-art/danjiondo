---
phase: 33-db-1
plan: "02"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/lib/data/rankings.ts
  - src/lib/data/rankings-page.ts
autonomous: true
requirements:
  - REGION-03
must_haves:
  truths:
    - "랜딩 페이지 랭킹(신고가·거래량·평당가·관심도) 계산이 경남 신규 시군구 거래도 포함한다"
    - "구별 챔피언·주간 하이라이트·전일 신고건수 등 rankings-page.ts 기반 집계가 경남 신규 시군구를 포함한다"
    - "ACTIVE_SGG_CODES 인라인 배열 상수가 rankings.ts, rankings-page.ts에서 제거되었다"
  artifacts:
    - path: "src/lib/data/rankings.ts"
      provides: "computeRankings 집계 함수 4종이 regions 동적 조회 기반으로 sgg_code 필터링"
    - path: "src/lib/data/rankings-page.ts"
      provides: "7개 export 함수가 regions 동적 조회 기반으로 sgg_code 필터링"
  key_links:
    - from: "src/lib/data/rankings.ts"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
    - from: "src/lib/data/rankings-page.ts"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
---

<objective>
RESEARCH.md가 발견한 Pitfall 1(사전 조사 목록 누락)의 핵심 대상인 `rankings.ts`와, 이미 함께 감사된 `rankings-page.ts`의 `ACTIVE_SGG_CODES` 하드코딩(각 6개 고정, 총 11개 사용처)을 `getActiveSggCodes()` 동적 조회로 교체한다.

Purpose: 랜딩 페이지 랭킹, 구별 챔피언, 주간 하이라이트 등 이 두 파일에 의존하는 모든 노출 지점이 경남 전체 데이터를 반영하게 한다.
Output: 두 파일에서 정적 ACTIVE_SGG_CODES 상수가 사라지고 모든 `.in('sgg_code', ...)` 호출이 동적 목록을 사용.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
```typescript
// src/lib/data/regions.ts (33-00에서 생성)
export async function getActiveSggCodes(supabase: SupabaseClient<Database>): Promise<string[]>
```

<!-- rankings.ts computeRankings의 현재 aggregator 배열 구조 (fn 시그니처 변경 대상) -->
```typescript
const aggregators: Array<{
  type: RankType
  fn: (s: SupabaseClient<Database>) => Promise<AggRow[]>
}> = [
  { type: 'high_price', fn: aggregateHighPrice },
  { type: 'volume', fn: aggregateVolume },
  { type: 'price_per_pyeong', fn: aggregatePricePerPyeong },
  { type: 'interest', fn: aggregateInterest },
]
for (const { type, fn } of aggregators) {
  const rows = await fn(supabase)
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rankings.ts 집계 함수 4종 동적 지역 필터 전환</name>
  <files>src/lib/data/rankings.ts</files>
  <read_first>src/lib/data/rankings.ts</read_first>
  <action>
`src/lib/data/rankings.ts`:
- 라인 5~7의 주석 + `const ACTIVE_SGG_CODES = [...] as const`를 제거하고 `import { getActiveSggCodes } from './regions'` 추가
- 4개 내부 집계 함수(`aggregateHighPrice`, `aggregateVolume`, `aggregatePricePerPyeong`, `aggregateInterest`)의 시그니처를 `(supabase: SupabaseClient<Database>, activeSggCodes: string[]): Promise<AggRow[]>`로 변경 (두 번째 파라미터 추가)
- 각 함수 내부의 `.in('sgg_code', ACTIVE_SGG_CODES)` / `.in('complexes.sgg_code', ACTIVE_SGG_CODES)`를 `.in('sgg_code', activeSggCodes)` / `.in('complexes.sgg_code', activeSggCodes)`로 변경 (해당 라인: 86, 125, 156, 189)
- `computeRankings` 함수 내부:
  - 함수 시작부(라인 214 직후)에 `const activeSggCodes = await getActiveSggCodes(supabase)` 추가
  - `aggregators` 배열의 `fn` 타입을 `(s: SupabaseClient<Database>, codes: string[]) => Promise<AggRow[]>`로 변경
  - 루프(라인 228~229)의 `const rows = await fn(supabase)`를 `const rows = await fn(supabase, activeSggCodes)`로 변경
- `getRankingsByType`은 `complex_rankings` 사전 계산 테이블만 읽으므로 수정하지 않는다 (ACTIVE_SGG_CODES를 참조하지 않음 — 이미 확인됨)
  </action>
  <acceptance_criteria>
    - `grep -c "ACTIVE_SGG_CODES" src/lib/data/rankings.ts` == 0
    - `grep -c "activeSggCodes" src/lib/data/rankings.ts` >= 5 (computeRankings 1회 선언 + 4개 함수 파라미터)
    - `grep -c "getActiveSggCodes" src/lib/data/rankings.ts` == 1 (import 후 단일 호출 지점)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "rankings.ts" | head -20</automated>
  </verify>
  <done>rankings.ts의 4개 집계 함수가 activeSggCodes 파라미터를 받아 필터링하고, computeRankings가 요청당 1회만 getActiveSggCodes를 호출하며, tsc 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: rankings-page.ts 7개 함수 동적 지역 필터 전환</name>
  <files>src/lib/data/rankings-page.ts</files>
  <read_first>src/lib/data/rankings-page.ts</read_first>
  <action>
`src/lib/data/rankings-page.ts`:
- 라인 5~6의 주석 + `const ACTIVE_SGG_CODES = [...] as const`를 제거하고 `import { getActiveSggCodes } from './regions'` 추가
- `CHAMPION_REGIONS`(라인 9~) export 상수는 그대로 유지한다 — 이것은 지역 필터 allowlist가 아니라 "대장단지 구별 정의"용 6개 sub-region UI 표시 목록이며, CONTEXT.md의 "UI 구조 변경 없음" 원칙에 따라 창원 5구+김해로 유지 (경남 신규 시군구를 이 챔피언 UI에 추가하는 것은 범위 밖 — 라벨 기계적 추가 대상도 아님, 별도 UI 구조 변경이 필요하므로 33-05에도 포함하지 않는다)
- 아래 7개 export 함수 각각의 본문 시작부에 `const activeSggCodes = await getActiveSggCodes(supabase)`를 추가하고, 함수 내 모든 `[...ACTIVE_SGG_CODES]` 참조를 `activeSggCodes`로 치환한다 (각 함수는 이미 `supabase`를 첫 파라미터로 받으므로 시그니처 변경 불필요):
  - `getRecentDailyFeed` (라인 145, `.in('complexes.sgg_code', [...ACTIVE_SGG_CODES])` at 160)
  - `getChampionComplexes` (라인 273, `.in('sgg_code', [...ACTIVE_SGG_CODES])` at 280)
  - `getRegionalPriceRanking` (라인 343)
  - `getWeeklyHighlights` (라인 412, 3곳: 427/443/455)
  - `getNewRecordCount` (라인 521, at 528)
  - `getRegionalTradingHeat` (라인 537, at 544)
  - `getRegionalAllTimeHighs` (라인 562)
  - 그 외 라인 160/280/427에서 함수 밖에서 직접 `[...ACTIVE_SGG_CODES]`를 사용하는 곳이 있으면(예: 라인 160이 `getRecentDailyFeed` 함수 밖일 경우) 해당 함수 스코프 안으로 `activeSggCodes` 선언을 옮긴다 — 정확한 함수 경계는 read_first 결과로 확인 후 처리
  </action>
  <acceptance_criteria>
    - `grep -c "ACTIVE_SGG_CODES" src/lib/data/rankings-page.ts` == 0
    - `grep -c "getActiveSggCodes(supabase)" src/lib/data/rankings-page.ts` >= 7
    - `grep -c "CHAMPION_REGIONS" src/lib/data/rankings-page.ts` >= 1 (변경 없이 유지 확인)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "rankings-page" | head -20</automated>
  </verify>
  <done>rankings-page.ts의 7개 export 함수 모두 regions 테이블 기반 동적 조회로 sgg_code를 필터링하고, CHAMPION_REGIONS는 변경 없이 유지되며, tsc 에러가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| cron/RSC 내부 집계 로직 → transactions 테이블 | 사용자 입력 없음, 순수 내부 데이터 파이프라인 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Information Disclosure | rankings.ts/rankings-page.ts 지역 범위 | accept | 사용자 입력 없이 서버 내부에서만 결정되는 필터 — 인젝션 경로 없음 |
</threat_model>

<verification>
```bash
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. rankings.ts, rankings-page.ts에서 ACTIVE_SGG_CODES 정적 배열이 완전히 제거됨
2. 랭킹 cron 실행 시(computeRankings) 경남 신규 시군구 거래도 집계 대상에 포함됨
3. 랜딩 페이지 관련 함수(getRecentDailyFeed 등) 7종 모두 동적 지역 필터 사용
4. tsc/lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-02-SUMMARY.md` 생성.
</output>
