---
phase: 33-db-1
plan: "03"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/services/molit-presale.ts
  - src/services/cheongyak/client.ts
  - src/services/cheongyak/client.test.ts
  - src/app/api/cron/daily/route.ts
autonomous: true
requirements:
  - REGION-04
must_haves:
  truths:
    - "일배치 cron이 분양권전매(molit-presale) 수집을 경남 신규 시군구 전체에 대해서도 수행한다"
    - "일배치 cron이 오피스텔(offi) 수집을 경남 신규 시군구 전체에 대해서도 수행한다"
    - "청약홈 분양 공고 수집이 창원·김해뿐 아니라 경남 신규 시군구 주소도 포함하도록 필터링된다"
    - "LAWD_CODES, offiSggCodes 인라인 배열이 cron/daily/route.ts와 molit-presale.ts에서 제거되었다"
  artifacts:
    - path: "src/services/cheongyak/client.ts"
      provides: "fetchCheongyakList/fetchRemndrList가 cities 파라미터를 받아 동적 필터링"
  key_links:
    - from: "src/app/api/cron/daily/route.ts"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase), await getActiveCityNames(supabase)"
      pattern: "getActiveSggCodes|getActiveCityNames"
---

<objective>
크론(`cron/daily/route.ts`)과 그 하위 어댑터(`molit-presale.ts`, `cheongyak/client.ts`)의 지역 하드코딩 3곳을 정리한다. CONTEXT.md canonical_refs가 지목한 `offiSggCodes`(cron 파일 내 inline)와 `LAWD_CODES`(molit-presale.ts) 외에, 코드베이스 직접 감사로 실제 청약홈 필터링 로직 위치(`src/services/cheongyak/client.ts`의 `CHEONGYAK_CITIES`)를 확인하여 함께 처리한다 — CONTEXT.md는 이 필터링 로직이 "cron/daily/route.ts 내부"에 있다고 서술했으나 실제로는 서비스 어댑터 파일에 있다.

Purpose: 경남 신규 시군구의 분양권전매·오피스텔·청약홈 데이터가 일배치로 수집되게 한다.
Output: 3개 하드코딩 지점이 regions 테이블 기반 동적 조회로 전환되고, cron/daily/route.ts가 offi·presale 루프에서 동일한 activeSggCodes를 재사용(중복 제거).
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
```typescript
// src/lib/data/regions.ts (33-00에서 생성)
export async function getActiveSggCodes(supabase: SupabaseClient<Database>): Promise<string[]>
export async function getActiveCityNames(supabase: SupabaseClient<Database>): Promise<string[]>
```

<!-- 현재 cheongyak/client.ts 필터링 로직 (변경 대상) -->
```typescript
// 경남 지역코드 (odcloud.kr 기준 — 창원·김해 포함)
const GYEONGNAM_CODE = '621'
// 창원·김해만 필터 — HSSPLY_ADRES 주소 기반
export const CHEONGYAK_CITIES = ['창원', '김해'] as const

export async function fetchCheongyakList(_sggCode?: string): Promise<CheongyakItem[]> {
  // ... 경남(621) 전체 조회 ...
  return all.filter(item =>
    CHEONGYAK_CITIES.some(city => item.HSSPLY_ADRES?.includes(city)),
  )
}
export async function fetchRemndrList(): Promise<CheongyakRemndrItem[]> { /* 동일 패턴 */ }
```

<!-- 기존 client.test.ts가 인자 없는 호출을 검증하므로 기본값 유지 필수 -->
```typescript
// 인자 없이 호출 시 CHEONGYAK_CITIES(창원·김해) 기본값으로 필터링되는 기존 동작을 그대로 보존해야 함
await expect(fetchCheongyakList()).rejects.toThrow('MOLIT_API_KEY not set')
const result = await fetchCheongyakList()  // 기본값 필터로 창원/김해 2건만 반환되는 기존 테스트
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: molit-presale.ts + cron/daily/route.ts 동적 지역 필터 전환 (LAWD_CODES + offiSggCodes 통합)</name>
  <files>src/services/molit-presale.ts, src/app/api/cron/daily/route.ts</files>
  <read_first>src/services/molit-presale.ts, src/app/api/cron/daily/route.ts</read_first>
  <action>
**1. `src/services/molit-presale.ts`**:
- 라인 6~7의 주석 + `export const LAWD_CODES = ['48125', '48250'] as const`를 완전히 제거한다. (참고: 이 상수는 원래 창원 5개 구 전체가 아닌 마산합포구(48125)+김해시(48250) 2개만 포함하는 실수가 있었음 — 동적 전환으로 이 문제도 함께 해결됨을 SUMMARY에 기록할 것)

**2. `src/app/api/cron/daily/route.ts`**:
- import에서 `LAWD_CODES`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 파일 상단, `supabase` 클라이언트 생성 직후(현재 라인 34 부근)에 `const activeSggCodes = await getActiveSggCodes(supabase)`를 1회만 선언한다
- 라인 71의 `for (const lawdCd of LAWD_CODES) {` (분양권전매 루프)를 `for (const lawdCd of activeSggCodes) {`로 변경
- 라인 230의 `const offiSggCodes = ['48121', '48123', '48125', '48127', '48129', '48250']`를 완전히 삭제하고, 라인 233의 `for (const sggCode of offiSggCodes) {` (오피스텔 루프)를 `for (const sggCode of activeSggCodes) {`로 변경
- 라인 247의 `offiErrors < offiSggCodes.length`도 `offiErrors < activeSggCodes.length`로 변경
- 이 두 루프(분양권전매·오피스텔)는 이제 파일 상단에서 1회 조회한 동일한 `activeSggCodes`를 공유하므로, 기존에 각각 하드코딩되어 있던 두 배열이 하나의 소스로 통합된다
  </action>
  <acceptance_criteria>
    - `grep -c "LAWD_CODES" src/services/molit-presale.ts src/app/api/cron/daily/route.ts` == 0 각각
    - `grep -c "offiSggCodes = \[" src/app/api/cron/daily/route.ts` == 0
    - `grep -c "activeSggCodes" src/app/api/cron/daily/route.ts` >= 4 (선언 1 + 사용 3곳 이상)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "cron/daily\|molit-presale" | head -20</automated>
  </verify>
  <done>molit-presale.ts에서 LAWD_CODES export가 사라지고, cron/daily/route.ts가 activeSggCodes 단일 소스로 분양권전매·오피스텔 루프를 실행하며 tsc 에러가 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 청약홈 어댑터 CHEONGYAK_CITIES 동적 파라미터화</name>
  <files>src/services/cheongyak/client.ts, src/services/cheongyak/client.test.ts, src/app/api/cron/daily/route.ts</files>
  <read_first>src/services/cheongyak/client.ts, src/services/cheongyak/client.test.ts</read_first>
  <behavior>
    - 기존 동작 보존: `fetchCheongyakList()` 인자 없이 호출 시 기존 `CHEONGYAK_CITIES = ['창원','김해']` 기본값으로 필터링 (client.test.ts의 "filters 창원·김해 by HSSPLY_ADRES" 테스트가 그대로 통과해야 함)
    - 신규 동작: `fetchCheongyakList(['진주'])`처럼 커스텀 city 배열을 전달하면 해당 도시만 필터링 — 진주 항목만 반환하고 창원/김해 항목은 제외
  </behavior>
  <action>
**1. `src/services/cheongyak/client.ts`**:
- `export const CHEONGYAK_CITIES = ['창원', '김해'] as const`는 하위 호환 기본값으로 유지한다 (제거하지 않음 — client.test.ts가 인자 없는 호출에서 이 값에 의존)
- `fetchCheongyakList(_sggCode?: string)`의 시그니처를 `fetchCheongyakList(cities: readonly string[] = CHEONGYAK_CITIES): Promise<CheongyakItem[]>`로 변경 (미사용 `_sggCode` 파라미터 제거, `cities` 파라미터로 대체)
- 함수 본문의 `CHEONGYAK_CITIES.some(city => item.HSSPLY_ADRES?.includes(city))`를 `cities.some(city => item.HSSPLY_ADRES?.includes(city))`로 변경
- `fetchRemndrList()`도 동일하게 `fetchRemndrList(cities: readonly string[] = CHEONGYAK_CITIES): Promise<CheongyakRemndrItem[]>`로 변경하고 내부 필터를 `cities`로 교체

**2. `src/services/cheongyak/client.test.ts`**:
- 기존 4개 테스트는 수정 없이 그대로 유지 (인자 없이 호출 → CHEONGYAK_CITIES 기본값 적용, 하위 호환 확인)
- 신규 테스트 1개 추가 — "filters" describe 블록 안에 `it('커스텀 cities 배열로 필터링 가능', ...)`: 진주/창원/김해 3건 mock 응답에서 `fetchCheongyakList(['진주'])` 호출 시 진주 1건만 반환되는지 확인 (기존 44~55라인 테스트와 동일한 mock 패턴 재사용)

**3. `src/app/api/cron/daily/route.ts`**:
- import에 `import { getActiveCityNames } from '@/lib/data/regions'` 추가 (`getActiveSggCodes`와 함께 import)
- `fetchCheongyakList`/`fetchRemndrList` import 유지, `CHEONGYAK_CITIES` 관련 import는 없었으므로 추가 변경 불필요
- 청약홈 수집 섹션(현재 라인 118의 `const items = await fetchCheongyakList()`) 직전에 `const activeCityNames = await getActiveCityNames(supabase)`를 추가하고, 호출을 `await fetchCheongyakList(activeCityNames)`로 변경
- 잔여세대 수집 섹션(현재 라인 158의 `const remndrItems = await fetchRemndrList()`)도 `await fetchRemndrList(activeCityNames)`로 변경
  </action>
  <acceptance_criteria>
    - `npx vitest run src/services/cheongyak/client.test.ts` 통과 (기존 4개 + 신규 1개 = 5개 테스트)
    - `grep -c "cities: readonly string\[\]" src/services/cheongyak/client.ts` == 2 (두 함수 시그니처)
    - `grep -c "getActiveCityNames" src/app/api/cron/daily/route.ts` >= 1
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/cheongyak/client.test.ts</automated>
  </verify>
  <done>fetchCheongyakList/fetchRemndrList가 cities 파라미터를 받고 기본값(창원·김해)은 하위 호환 유지하며, cron/daily/route.ts가 regions 기반 동적 도시명 목록을 전달한다. client.test.ts 5개 테스트 모두 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub Actions cron (x-cron-secret) → 외부 API 호출 | 이미 CRON_SECRET 헤더 검증 유지 (변경 없음) |
| 청약홈 API 응답 → HSSPLY_ADRES 문자열 매칭 | 외부 API 응답 데이터를 신뢰하여 substring 매칭 — 기존 패턴과 동일한 신뢰 수준 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | cron/daily/route.ts activeSggCodes/activeCityNames | accept | 사용자 입력 경로 없음, cron 내부에서만 regions 테이블 조회 후 사용 |
</threat_model>

<verification>
```bash
npx vitest run src/services/cheongyak/client.test.ts
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. molit-presale.ts에서 LAWD_CODES가 제거되고 cron이 activeSggCodes로 분양권전매를 수집
2. cron/daily/route.ts의 offiSggCodes 인라인 배열이 제거되고 activeSggCodes로 통합됨
3. cheongyak/client.ts가 cities 파라미터를 받아 동적 필터링 가능하며 기존 테스트(인자 없는 호출) 하위 호환 유지
4. cron이 경남 신규 시군구 청약홈 공고도 수집 대상에 포함
5. tsc/lint/vitest 통과
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-03-SUMMARY.md` 생성.
</output>
