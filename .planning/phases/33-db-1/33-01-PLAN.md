---
phase: 33-db-1
plan: "01"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/lib/data/invest.ts
  - src/app/invest/page.tsx
  - src/app/invest/region/[sggCode]/page.tsx
  - src/lib/data/gap-analysis.ts
  - src/app/gap-analysis/page.tsx
autonomous: true
requirements:
  - REGION-02
must_haves:
  truths:
    - "/invest 페이지의 지역 필터 드롭다운에 경남 신규 시군구가 선택 가능한 옵션으로 나타난다 (regions 테이블 기반)"
    - "/gap-analysis 페이지의 지역 필터도 동일하게 regions 테이블 기반으로 동작한다"
    - "/invest/region/[sggCode] 상세 페이지가 신규 시군구 코드에 대해 404를 반환하지 않는다"
    - "ALLOWED_SGG_CODES 인라인 배열 상수가 invest.ts, gap-analysis.ts, gap-analysis/page.tsx에서 제거되었다"
  artifacts:
    - path: "src/lib/data/invest.ts"
      provides: "ALLOWED_SGG_CODES 정적 배열 제거, getActiveSggCodes 재사용"
    - path: "src/lib/data/gap-analysis.ts"
      provides: "getGapRankings 내부 allowlist가 regions 동적 조회로 전환"
  key_links:
    - from: "src/app/invest/page.tsx"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
    - from: "src/app/gap-analysis/page.tsx"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
---

<objective>
`/invest`, `/invest/region/[sggCode]`, `/gap-analysis` 3개 페이지와 그 데이터 레이어(`invest.ts`, `gap-analysis.ts`)의 인라인 `ALLOWED_SGG_CODES` 하드코딩 배열(6개 고정)을 33-00에서 만든 `getActiveSggCodes()` 동적 조회로 교체한다. CONTEXT.md 2026-07-03 추가 결정에 따라, 라벨 맵(`SGG_LABEL`)은 정적 유지하되 경남 신규 16개 항목을 기계적으로 추가한다 (라벨은 UI 구조 변경이 아닌 데이터 추가로 취급).

Purpose: 투자분석·갭투자분석 페이지가 경남 전체 시군구 데이터를 필터링/탐색할 수 있게 한다.
Output: 5개 파일의 allowlist가 regions 테이블 기반 동적 조회로 전환되고, SGG_LABEL에 경남 16개 라벨이 추가됨.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
<!-- 33-00에서 생성된 공용 헬퍼 (이 plan은 이것만 import) -->
```typescript
// src/lib/data/regions.ts
export async function getActiveSggCodes(supabase: SupabaseClient<Database>): Promise<string[]>
```

<!-- 경남 16개 신규 라벨 — SGG_LABEL Record에 추가할 항목 (33-00 REGIONS와 동일 sgg_name) -->
```typescript
'48170': '진주시', '48220': '통영시', '48240': '사천시', '48270': '밀양시',
'48310': '거제시', '48330': '양산시', '48720': '의령군', '48730': '함안군',
'48740': '창녕군', '48820': '고성군', '48840': '남해군', '48850': '하동군',
'48860': '산청군', '48870': '함양군', '48880': '거창군', '48890': '합천군',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /invest 페이지 동적 지역 필터 전환</name>
  <files>src/lib/data/invest.ts, src/app/invest/page.tsx, src/app/invest/region/[sggCode]/page.tsx</files>
  <read_first>src/lib/data/invest.ts, src/app/invest/page.tsx, src/app/invest/region/[sggCode]/page.tsx</read_first>
  <action>
**1. `src/lib/data/invest.ts`** — 라인 20의 `export const ALLOWED_SGG_CODES = ['48121', ...] as const`를 완전히 제거한다. `ALLOWED_AREA_BUCKETS`는 그대로 유지한다.

**2. `src/app/invest/page.tsx`**:
- import에서 `ALLOWED_SGG_CODES`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 라인 34~41의 `SGG_LABEL` Record에 경남 16개 항목을 기계적으로 추가 (interfaces 블록 값 그대로, `'48250': '김해시',` 다음 줄에 삽입)
- 컴포넌트 함수 내부: `const supabase = createReadonlyClient()`를 라인 108(sggCode 검증) 이전으로 이동, 그 직후 `const allowedSggCodes = await getActiveSggCodes(supabase)` 추가
- 라인 108의 `const sggCode = (ALLOWED_SGG_CODES as ReadonlyArray<string>).includes(rawSgg) ? rawSgg : undefined`를 `const sggCode = allowedSggCodes.includes(rawSgg) ? rawSgg : undefined`로 변경
- 라인 43~46의 module-level `const REGION_OPTIONS = [...]`를 제거하고, 컴포넌트 함수 내부(`sggCode` 계산 직후)에 지역 옵션으로 동일 로직을 로컬 변수로 재정의:
  ```typescript
  const REGION_OPTIONS: Array<{ label: string; value: string }> = [
    { label: '전체', value: '' },
    ...allowedSggCodes.map((code) => ({ label: SGG_LABEL[code] ?? code, value: code })),
  ]
  ```
- JSX에서 `REGION_OPTIONS.map(...)` 참조는 이제 로컬 변수를 가리키므로 수정 불필요

**3. `src/app/invest/region/[sggCode]/page.tsx`**:
- import에서 `ALLOWED_SGG_CODES`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 라인 36~43의 `SGG_LABEL` Record에 경남 16개 항목 동일하게 추가
- 컴포넌트 함수(`RegionDetailPage`) 내부: `const supabase = createReadonlyClient()`(현재 라인 182)를 라인 176의 `notFound()` 체크보다 위로 이동
- `const allowedSggCodes = await getActiveSggCodes(supabase)` 추가
- `if (!(ALLOWED_SGG_CODES as ReadonlyArray<string>).includes(sggCode)) notFound()`를 `if (!allowedSggCodes.includes(sggCode)) notFound()`로 변경
- `generateMetadata` 함수는 수정하지 않는다 (static SGG_LABEL fallback으로 충분, DB 접근 없이 메타데이터 생성 유지가 성능상 유리)
  </action>
  <acceptance_criteria>
    - `grep -c "ALLOWED_SGG_CODES" src/lib/data/invest.ts src/app/invest/page.tsx "src/app/invest/region/[sggCode]/page.tsx"` == 0 (전부 제거)
    - `grep -c "getActiveSggCodes" src/app/invest/page.tsx "src/app/invest/region/[sggCode]/page.tsx"` >= 1 각각
    - `grep -c "48170" src/app/invest/page.tsx "src/app/invest/region/[sggCode]/page.tsx"` == 1 각각
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "invest" | head -20</automated>
  </verify>
  <done>invest.ts에서 ALLOWED_SGG_CODES export가 사라졌고, 두 invest 페이지 모두 regions 테이블 기반 동적 조회로 sgg_code를 검증하며, SGG_LABEL에 경남 16개 라벨이 추가되어 tsc 에러 없이 빌드된다.</done>
</task>

<task type="auto">
  <name>Task 2: /gap-analysis 페이지 동적 지역 필터 전환</name>
  <files>src/lib/data/gap-analysis.ts, src/app/gap-analysis/page.tsx</files>
  <read_first>src/lib/data/gap-analysis.ts, src/app/gap-analysis/page.tsx</read_first>
  <action>
**1. `src/lib/data/gap-analysis.ts`**:
- 라인 29의 `const ALLOWED_SGG_CODES = ['48121', ...]`를 제거하고 파일 상단에 `import { getActiveSggCodes } from './regions'` 추가
- `getGapRankings` 함수(라인 37) 내부에서 `if (filter.sggCode && ALLOWED_SGG_CODES.includes(filter.sggCode))` 체크 직전에 `const allowedSggCodes = await getActiveSggCodes(supabase)`를 추가하고, 조건을 `if (filter.sggCode && allowedSggCodes.includes(filter.sggCode))`로 변경 (defense-in-depth 유지 — page.tsx에서도 검증하지만 데이터 레이어에서도 재검증, Decisions Log 2026-05-28 패턴 계승)

**2. `src/app/gap-analysis/page.tsx`**:
- 라인 21의 `const ALLOWED_SGG_CODES = [...]`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 라인 26~33의 `SGG_LABEL` Record에 경남 16개 항목 추가 (Task 1과 동일 값)
- 컴포넌트 함수(`GapAnalysisPage`) 내부: `const supabase = createReadonlyClient()`(현재 라인 86)를 라인 81(sggCode 검증)보다 위로 이동
- `const allowedSggCodes = await getActiveSggCodes(supabase)` 추가
- 라인 81 `const sggCode = ALLOWED_SGG_CODES.includes(rawSgg) ? rawSgg : undefined`를 `const sggCode = allowedSggCodes.includes(rawSgg) ? rawSgg : undefined`로 변경
- 라인 35~38의 module-level `const REGION_OPTIONS = [...]`를 제거하고 컴포넌트 함수 내부 로컬 변수로 재정의 (Task 1과 동일 패턴):
  ```typescript
  const REGION_OPTIONS: Array<{ label: string; value: string }> = [
    { label: '전체', value: '' },
    ...allowedSggCodes.map((code) => ({ label: SGG_LABEL[code] ?? code, value: code })),
  ]
  ```
  </action>
  <acceptance_criteria>
    - `grep -c "ALLOWED_SGG_CODES" src/lib/data/gap-analysis.ts src/app/gap-analysis/page.tsx` == 0 각각
    - `grep -c "getActiveSggCodes" src/lib/data/gap-analysis.ts src/app/gap-analysis/page.tsx` >= 1 각각
    - `grep -c "48170" src/app/gap-analysis/page.tsx` == 1
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "gap-analysis" | head -20</automated>
  </verify>
  <done>gap-analysis.ts와 gap-analysis/page.tsx 모두 regions 테이블 기반 동적 조회로 전환되었고 tsc 에러 없이 빌드된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| URL 쿼리 `sgg_code` → DB 쿼리 필터 | 사용자가 임의 문자열 전달 가능, allowlist 대조 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering | invest/page.tsx, gap-analysis/page.tsx `sgg_code` 파라미터 | mitigate | `allowedSggCodes.includes(rawSgg)` 검증 유지 (Decisions Log 2026-05-28 패턴 계승, 동적 소스로 전환됐을 뿐 검증 로직 자체는 동일) |
| T-33-04 | Tampering | getGapRankings 데이터 레이어 재검증 | mitigate | page.tsx 검증을 우회해 데이터 레이어 함수를 직접 호출해도 내부 allowlist 재검증으로 방어 (defense-in-depth) |
</threat_model>

<verification>
```bash
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. invest.ts, gap-analysis.ts에서 정적 ALLOWED_SGG_CODES export가 사라짐
2. /invest, /invest/region/[sggCode], /gap-analysis 3개 페이지가 regions 테이블 기반으로 sgg_code를 동적 검증
3. SGG_LABEL 맵에 경남 16개 신규 항목이 추가되어 신규 지역 선택 시 원본 코드가 아닌 한글 라벨이 표시됨
4. tsc/lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-01-SUMMARY.md` 생성.
</output>
