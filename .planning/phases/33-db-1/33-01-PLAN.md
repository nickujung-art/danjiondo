---
phase: 33-db-1
plan: "01"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/lib/data/invest.ts
  - src/app/invest/page.tsx
  - src/app/invest/region/[sggCode]/page.tsx
  - src/lib/data/gap-analysis.ts
  - src/app/gap-analysis/page.tsx
  - scripts/seed-kosis-population.ts
autonomous: true
requirements:
  - REGION-02
  - REGION-10
must_haves:
  truths:
    - "/invest 페이지의 지역 필터 드롭다운에 경남 신규 시군구가 선택 가능한 옵션으로 나타난다 (regions 테이블 기반)"
    - "/gap-analysis 페이지의 지역 필터도 동일하게 regions 테이블 기반으로 동작한다"
    - "/invest/region/[sggCode] 상세 페이지가 신규 시군구 코드에 대해 404를 반환하지 않는다"
    - "ALLOWED_SGG_CODES 인라인 배열 상수가 invest.ts, gap-analysis.ts, gap-analysis/page.tsx에서 제거되었다"
    - "scripts/seed-kosis-population.ts가 --sgg CLI 인자 없이 실행되면 regions 테이블 is_active=true 전체 22개 코드를 기본 대상으로 사용한다 (기존 6개 고정 배열 제거)"
  artifacts:
    - path: "src/lib/data/invest.ts"
      provides: "ALLOWED_SGG_CODES 정적 배열 제거, getActiveSggCodes 재사용"
    - path: "src/lib/data/gap-analysis.ts"
      provides: "getGapRankings 내부 allowlist가 regions 동적 조회로 전환"
    - path: "scripts/seed-kosis-population.ts"
      provides: "--sgg CLI 오버라이드 + regions is_active=true 동적 기본값 (backfill-realprice.ts와 동일 패턴)"
  key_links:
    - from: "src/app/invest/page.tsx"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
    - from: "src/app/gap-analysis/page.tsx"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
    - from: "scripts/seed-kosis-population.ts"
      to: "regions 테이블"
      via: "REST fetch regions?select=sgg_code&is_active=eq.true"
      pattern: "is_active=eq.true"
---

<objective>
`/invest`, `/invest/region/[sggCode]`, `/gap-analysis` 3개 페이지와 그 데이터 레이어(`invest.ts`, `gap-analysis.ts`)의 인라인 `ALLOWED_SGG_CODES` 하드코딩 배열(6개 고정)을 33-00에서 만든 `getActiveSggCodes()` 동적 조회로 교체한다. CONTEXT.md 2026-07-03 추가 결정에 따라, 라벨 맵(`SGG_LABEL`)은 정적 유지하되 경남 신규 16개 항목을 기계적으로 추가한다 (라벨은 UI 구조 변경이 아닌 데이터 추가로 취급).

**(리비전 추가, 2026-07-03 plan-checker Gap 2 대응):** `scripts/seed-kosis-population.ts`가 `SGG_CODES`를 6개로 하드코딩하고 있어 `region_population_cache`가 경남 신규 16개 시군구에 대해 사전 적재되지 않는 문제를 함께 해결한다. 이 캐시는 `src/lib/data/invest.ts`가 `fetchPopulationBySgg`(`src/services/kosis.ts`)를 통해 조회하여 `/invest` 페이지의 "지역 인구" 투자 통계에 사용한다. `scripts/backfill-realprice.ts`와 동일한 `--sgg=` CLI 오버라이드 + `regions.is_active=true` 동적 기본값 패턴을 적용한다 (단, 이 파일의 기존 REST fetch 스타일은 유지 — Task 3 참고).

Purpose: 투자분석·갭투자분석 페이지가 경남 전체 시군구 데이터를 필터링/탐색할 수 있게 하고, 그 기반이 되는 인구 통계 캐시도 경남 전체를 커버할 수 있게 한다.
Output: 5개 파일의 allowlist가 regions 테이블 기반 동적 조회로 전환되고, SGG_LABEL에 경남 16개 라벨이 추가되며, 인구 시딩 스크립트가 --sgg 오버라이드 + regions 동적 기본값을 지원한다.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
<!-- 33-00에서 생성된 공용 헬퍼 (Task 1/2는 이것만 import; Task 3은 아래 설명대로 별도 구현) -->
```typescript
// src/lib/data/regions.ts
export async function getActiveSggCodes(supabase: SupabaseClient<Database>): Promise<string[]>
```

<!-- 경남 16개 신규 라벨 — SGG_LABEL Record에 추가할 항목 (33-00 REGIONS와 동일 sgg_name) -->
```typescript
'48170': '진주시', '48220': '통영시', '48240': '사천시', '48270': '밀양시',
'48310': '거제시', '48330': '양산시', '48720': '의령군', '48730': '함안군',
'48740': '창녕군', '48820': '고성군', '48840': '남해군', '48850': '하동군',
'48860': '산청군', '48870': '함양군', '48880': '거창군', '48890': '합천군',
```

<!-- scripts/seed-kosis-population.ts 현재 전체 내용 (Task 3 시작 상태) -->
```typescript
/**
 * KOSIS 인구 데이터 → Supabase region_population_cache 초기 적재 스크립트
 *
 * 실행: npx dotenv-cli -e .env.local npx tsx scripts/seed-kosis-population.ts
 * 연간 1회 재실행 권장 (매년 새 통계 발표 후)
 */

const KOSIS_BASE = 'https://kosis.kr/openapi/Param/statisticsParameterData.do'
const ORG_ID     = '101'
const TBL_ID     = 'DT_1B040A3'
const ITM_ID     = 'T20'

const SGG_CODES = ['48121', '48123', '48125', '48127', '48129', '48250']

interface Row {
  sgg_code:   string
  year:       number
  sgg_name:   string
  population: number
  fetched_at: string
}

async function fetchFromKosis(): Promise<Row[]> {
  const key = process.env.KOSIS_API_KEY
  if (!key) throw new Error('KOSIS_API_KEY not set')

  const objL1 = SGG_CODES.join('+') + '+'
  const url =
    `${KOSIS_BASE}?method=getList` +
    `&apiKey=${encodeURIComponent(key)}` +
    `&orgId=${ORG_ID}&tblId=${TBL_ID}` +
    `&itmId=${ITM_ID}+` +
    `&objL1=${objL1}` +
    `&objL2=&objL3=&objL4=&objL5=&objL6=&objL7=&objL8=` +
    `&format=json&jsonVD=Y` +
    `&prdSe=Y&newEstPrdCnt=10`

  console.log('KOSIS API 호출 중...')
  const res = await fetch(url)
  if (!res.ok) throw new Error(`KOSIS API ${res.status}: ${await res.text()}`)

  const json = await res.json()
  if (!Array.isArray(json)) {
    console.error('응답:', JSON.stringify(json).slice(0, 300))
    throw new Error('KOSIS 응답이 배열이 아님')
  }

  const now = new Date().toISOString()
  return (json as Record<string, string>[])
    .filter(r => r['ITM_ID'] === 'T20' && r['DT'] && r['C1'] && r['C1_NM'])
    .map(r => ({
      sgg_code:   r['C1']!,
      year:       Number(r['PRD_DE']),
      sgg_name:   r['C1_NM']!,
      population: Number(r['DT']),
      fetched_at: now,
    }))
    .filter(r => !isNaN(r.year) && !isNaN(r.population))
    .sort((a, b) => a.sgg_code.localeCompare(b.sgg_code) || a.year - b.year)
}

async function upsertToSupabase(rows: Row[]): Promise<void> {
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
  const serviceKey  = process.env.SUPABASE_SERVICE_ROLE_KEY

  if (!supabaseUrl) throw new Error('NEXT_PUBLIC_SUPABASE_URL not set')
  if (!serviceKey)  throw new Error('SUPABASE_SERVICE_ROLE_KEY not set')

  const res = await fetch(`${supabaseUrl}/rest/v1/region_population_cache`, {
    method:  'POST',
    headers: {
      apikey:          serviceKey,
      Authorization:   `Bearer ${serviceKey}`,
      'Content-Type':  'application/json',
      Prefer:          'resolution=merge-duplicates',
    },
    body: JSON.stringify(rows),
  })

  if (!res.ok) {
    const text = await res.text()
    throw new Error(`Supabase upsert ${res.status}: ${text}`)
  }
}

async function main() {
  const rows = await fetchFromKosis()
  console.log(`KOSIS에서 ${rows.length}건 수신`)
  rows.forEach(r => console.log(`  ${r.sgg_code} ${r.sgg_name} ${r.year}: ${r.population.toLocaleString('ko-KR')}명`))

  await upsertToSupabase(rows)
  console.log(`✓ Supabase region_population_cache에 ${rows.length}건 저장 완료`)
}

main().catch(err => {
  console.error('실패:', err)
  process.exit(1)
})
```

<!-- backfill-realprice.ts의 --sgg CLI 오버라이드 패턴 (Task 3이 이식할 참조 패턴, supabase-js 대신 REST로 이식) -->
```typescript
// Source: scripts/backfill-realprice.ts
const args = process.argv.slice(2)
const sggArg = args.find(a => a.startsWith('--sgg='))?.split('=')[1]

async function getSggCodes(): Promise<string[]> {
  if (sggArg) return sggArg.split(',').map(s => s.trim())
  // ... regions 테이블 is_active=true 조회 ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /invest 페이지 동적 지역 필터 전환</name>
  <files>src/lib/data/invest.ts, src/app/invest/page.tsx, src/app/invest/region/[sggCode]/page.tsx</files>
  <read_first>src/lib/data/invest.ts, src/app/invest/page.tsx, src/app/invest/region/[sggCode]/page.tsx</read_first>
  <action>
**1. `src/lib/data/invest.ts`** — 라인 20의 `export const ALLOWED_SGG_CODES = ['48121', ...] as const`를 완전히 제거한다. `ALLOWED_AREA_BUCKETS`는 그대로 유지한다.

**2. `src/app/invest/page.tsx`**:
- import에서 `ALLOWED_SGG_CODES`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 라인 34~41의 `SGG_LABEL` Record에 경남 16개 항목을 기계적으로 추가 (interfaces 블록 값 그대로, `'48250': '김해시',` 다음 줄에 삽입)
- 컴포넌트 함수 내부: `const supabase = createReadonlyClient()`를 라인 108(sggCode 검증) 이전으로 이동, 그 직후 `const allowedSggCodes = await getActiveSggCodes(supabase)` 추가
- 라인 108의 `const sggCode = (ALLOWED_SGG_CODES as ReadonlyArray<string>).includes(rawSgg) ? rawSgg : undefined`를 `const sggCode = allowedSggCodes.includes(rawSgg) ? rawSgg : undefined`로 변경
- 라인 43~46의 module-level `const REGION_OPTIONS = [...]`를 제거하고, 컴포넌트 함수 내부(`sggCode` 계산 직후)에 지역 옵션으로 동일 로직을 로컬 변수로 재정의:
  ```typescript
  const REGION_OPTIONS: Array<{ label: string; value: string }> = [
    { label: '전체', value: '' },
    ...allowedSggCodes.map((code) => ({ label: SGG_LABEL[code] ?? code, value: code })),
  ]
  ```
- JSX에서 `REGION_OPTIONS.map(...)` 참조는 이제 로컬 변수를 가리키므로 수정 불필요

**3. `src/app/invest/region/[sggCode]/page.tsx`**:
- import에서 `ALLOWED_SGG_CODES`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 라인 36~43의 `SGG_LABEL` Record에 경남 16개 항목 동일하게 추가
- 컴포넌트 함수(`RegionDetailPage`) 내부: `const supabase = createReadonlyClient()`(현재 라인 182)를 라인 176의 `notFound()` 체크보다 위로 이동
- `const allowedSggCodes = await getActiveSggCodes(supabase)` 추가
- `if (!(ALLOWED_SGG_CODES as ReadonlyArray<string>).includes(sggCode)) notFound()`를 `if (!allowedSggCodes.includes(sggCode)) notFound()`로 변경
- `generateMetadata` 함수는 수정하지 않는다 (static SGG_LABEL fallback으로 충분, DB 접근 없이 메타데이터 생성 유지가 성능상 유리)
  </action>
  <acceptance_criteria>
    - `grep -c "ALLOWED_SGG_CODES" src/lib/data/invest.ts src/app/invest/page.tsx "src/app/invest/region/[sggCode]/page.tsx"` == 0 (전부 제거)
    - `grep -c "getActiveSggCodes" src/app/invest/page.tsx "src/app/invest/region/[sggCode]/page.tsx"` >= 1 각각
    - `grep -c "48170" src/app/invest/page.tsx "src/app/invest/region/[sggCode]/page.tsx"` == 1 각각
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "invest" | head -20</automated>
  </verify>
  <done>invest.ts에서 ALLOWED_SGG_CODES export가 사라졌고, 두 invest 페이지 모두 regions 테이블 기반 동적 조회로 sgg_code를 검증하며, SGG_LABEL에 경남 16개 라벨이 추가되어 tsc 에러 없이 빌드된다.</done>
</task>

<task type="auto">
  <name>Task 2: /gap-analysis 페이지 동적 지역 필터 전환</name>
  <files>src/lib/data/gap-analysis.ts, src/app/gap-analysis/page.tsx</files>
  <read_first>src/lib/data/gap-analysis.ts, src/app/gap-analysis/page.tsx</read_first>
  <action>
**1. `src/lib/data/gap-analysis.ts`**:
- 라인 29의 `const ALLOWED_SGG_CODES = ['48121', ...]`를 제거하고 파일 상단에 `import { getActiveSggCodes } from './regions'` 추가
- `getGapRankings` 함수(라인 37) 내부에서 `if (filter.sggCode && ALLOWED_SGG_CODES.includes(filter.sggCode))` 체크 직전에 `const allowedSggCodes = await getActiveSggCodes(supabase)`를 추가하고, 조건을 `if (filter.sggCode && allowedSggCodes.includes(filter.sggCode))`로 변경 (defense-in-depth 유지 — page.tsx에서도 검증하지만 데이터 레이어에서도 재검증, Decisions Log 2026-05-28 패턴 계승)

**2. `src/app/gap-analysis/page.tsx`**:
- 라인 21의 `const ALLOWED_SGG_CODES = [...]`를 제거하고 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가
- 라인 26~33의 `SGG_LABEL` Record에 경남 16개 항목 추가 (Task 1과 동일 값)
- 컴포넌트 함수(`GapAnalysisPage`) 내부: `const supabase = createReadonlyClient()`(현재 라인 86)를 라인 81(sggCode 검증)보다 위로 이동
- `const allowedSggCodes = await getActiveSggCodes(supabase)` 추가
- 라인 81 `const sggCode = ALLOWED_SGG_CODES.includes(rawSgg) ? rawSgg : undefined`를 `const sggCode = allowedSggCodes.includes(rawSgg) ? rawSgg : undefined`로 변경
- 라인 35~38의 module-level `const REGION_OPTIONS = [...]`를 제거하고 컴포넌트 함수 내부 로컬 변수로 재정의 (Task 1과 동일 패턴):
  ```typescript
  const REGION_OPTIONS: Array<{ label: string; value: string }> = [
    { label: '전체', value: '' },
    ...allowedSggCodes.map((code) => ({ label: SGG_LABEL[code] ?? code, value: code })),
  ]
  ```
  </action>
  <acceptance_criteria>
    - `grep -c "ALLOWED_SGG_CODES" src/lib/data/gap-analysis.ts src/app/gap-analysis/page.tsx` == 0 각각
    - `grep -c "getActiveSggCodes" src/lib/data/gap-analysis.ts src/app/gap-analysis/page.tsx` >= 1 각각
    - `grep -c "48170" src/app/gap-analysis/page.tsx` == 1
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "gap-analysis" | head -20</automated>
  </verify>
  <done>gap-analysis.ts와 gap-analysis/page.tsx 모두 regions 테이블 기반 동적 조회로 전환되었고 tsc 에러 없이 빌드된다.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/seed-kosis-population.ts --sgg CLI 오버라이드 + regions 동적 기본값 (리비전 Gap 2 대응)</name>
  <files>scripts/seed-kosis-population.ts</files>
  <read_first>scripts/backfill-realprice.ts</read_first>
  <action>
`scripts/seed-kosis-population.ts`의 라인 13 `const SGG_CODES = ['48121', '48123', '48125', '48127', '48129', '48250']`를 제거하고, `backfill-realprice.ts`(라인 29~33, 70~76)와 동일한 `--sgg=` CLI 오버라이드 패턴을 적용한다. 이 스크립트는 기존에 supabase-js 클라이언트가 아니라 REST fetch(`SUPABASE_SERVICE_ROLE_KEY` 헤더)로 Supabase에 접근하는 자체 스타일을 이미 갖고 있으므로(현재 `upsertToSupabase` 함수), regions 조회도 동일한 REST fetch 스타일로 구현한다 — `src/lib/data/regions.ts`의 `getActiveSggCodes`를 import하지 않는다 (`server-only` 패키지 의존성과 `@/` 경로 별칭을 scripts/ 독립 실행 환경에 새로 끌어들이지 않기 위함, Decisions Log 2026-06-10 "scripts/ tsconfig exclude" 원칙 유지 — 이 디렉토리는 Next.js 빌드 대상이 아니며 지금까지 모든 scripts/*.ts가 상대 경로 import만 사용해 왔다).

**1. 파일 상단(현재 라인 8~13 부근)에 CLI 인자 파싱 추가, 기존 `const SGG_CODES = [...]` 라인은 완전히 삭제:**
```typescript
const args = process.argv.slice(2)
const sggArg = args.find(a => a.startsWith('--sgg='))?.split('=')[1]
```

**2. `fetchFromKosis` 함수 바로 위에 `getSggCodes()` 함수 신규 추가:**
```typescript
async function getSggCodes(): Promise<string[]> {
  if (sggArg) return sggArg.split(',').map(s => s.trim())

  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
  const serviceKey  = process.env.SUPABASE_SERVICE_ROLE_KEY
  if (!supabaseUrl) throw new Error('NEXT_PUBLIC_SUPABASE_URL not set')
  if (!serviceKey)  throw new Error('SUPABASE_SERVICE_ROLE_KEY not set')

  const res = await fetch(
    `${supabaseUrl}/rest/v1/regions?select=sgg_code&is_active=eq.true&order=sgg_code`,
    { headers: { apikey: serviceKey, Authorization: `Bearer ${serviceKey}` } },
  )
  if (!res.ok) throw new Error(`regions 조회 실패: ${res.status} ${await res.text()}`)
  const data = (await res.json()) as Array<{ sgg_code: string }>
  return data.map(r => r.sgg_code)
}
```

**3. `fetchFromKosis` 함수 시그니처를 `sggCodes` 파라미터를 받도록 변경** — 함수 본문 내부의 모듈 레벨 `SGG_CODES` 참조(`const objL1 = SGG_CODES.join('+') + '+'`)를 파라미터로 치환:
```typescript
async function fetchFromKosis(sggCodes: string[]): Promise<Row[]> {
  const key = process.env.KOSIS_API_KEY
  if (!key) throw new Error('KOSIS_API_KEY not set')

  const objL1 = sggCodes.join('+') + '+'
  // ... 이하 기존 로직 완전히 동일 (url 조립, fetch, 파싱, 정렬) ...
}
```

**4. `main()` 함수 수정** — `getSggCodes()` 호출 후 결과를 `fetchFromKosis`에 전달:
```typescript
async function main() {
  const sggCodes = await getSggCodes()
  console.log(`대상 시군구: ${sggCodes.length}개 (${sggCodes.join(', ')})`)

  const rows = await fetchFromKosis(sggCodes)
  console.log(`KOSIS에서 ${rows.length}건 수신`)
  rows.forEach(r => console.log(`  ${r.sgg_code} ${r.sgg_name} ${r.year}: ${r.population.toLocaleString('ko-KR')}명`))

  await upsertToSupabase(rows)
  console.log(`✓ Supabase region_population_cache에 ${rows.length}건 저장 완료`)
}
```

`upsertToSupabase` 함수는 변경하지 않는다 (이미 `rows` 파라미터만 받아 동작하고 `SGG_CODES` 모듈 상수를 참조하지 않음).
  </action>
  <acceptance_criteria>
    - `grep -c "const SGG_CODES = \[" scripts/seed-kosis-population.ts` == 0
    - `grep -c "sggArg" scripts/seed-kosis-population.ts` >= 1
    - `grep -c "is_active=eq.true" scripts/seed-kosis-population.ts` == 1
    - `grep -c "async function getSggCodes" scripts/seed-kosis-population.ts` == 1
    - `grep -c "async function fetchFromKosis(sggCodes: string\[\])" scripts/seed-kosis-population.ts` == 1
  </acceptance_criteria>
  <verify>
    <automated>npx dotenv-cli -e .env.local npx tsx scripts/seed-kosis-population.ts --sgg=48170,48250 2>&1 | tail -10</automated>
  </verify>
  <done>seed-kosis-population.ts가 --sgg=48170,48250 인자로 실행 시 두 코드만 대상으로 KOSIS 조회·upsert하고, 인자 없이 실행하면 regions 테이블 is_active=true 전체(33-00 완료 후 22개)를 기본값으로 사용한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| URL 쿼리 `sgg_code` → DB 쿼리 필터 | 사용자가 임의 문자열 전달 가능, allowlist 대조 필수 |
| CLI 인자(`--sgg=`, seed-kosis-population.ts) → KOSIS/Supabase REST 파라미터 | 로컬/CI 실행자만 트리거 가능, 사용자 입력 아님 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering | invest/page.tsx, gap-analysis/page.tsx `sgg_code` 파라미터 | mitigate | `allowedSggCodes.includes(rawSgg)` 검증 유지 (Decisions Log 2026-05-28 패턴 계승, 동적 소스로 전환됐을 뿐 검증 로직 자체는 동일) |
| T-33-04 | Tampering | getGapRankings 데이터 레이어 재검증 | mitigate | page.tsx 검증을 우회해 데이터 레이어 함수를 직접 호출해도 내부 allowlist 재검증으로 방어 (defense-in-depth) |
| T-33-14 | Tampering | seed-kosis-population.ts `--sgg=` CLI 인자 | accept | git 관리 파일, service_role 키로만 실행 가능, 로컬/CI 실행자 전용(사용자 입력 경로 없음) — backfill-realprice.ts와 동일 신뢰 수준 |
</threat_model>

<verification>
```bash
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. invest.ts, gap-analysis.ts에서 정적 ALLOWED_SGG_CODES export가 사라짐
2. /invest, /invest/region/[sggCode], /gap-analysis 3개 페이지가 regions 테이블 기반으로 sgg_code를 동적 검증
3. SGG_LABEL 맵에 경남 16개 신규 항목이 추가되어 신규 지역 선택 시 원본 코드가 아닌 한글 라벨이 표시됨
4. tsc/lint 통과
5. scripts/seed-kosis-population.ts가 --sgg CLI 오버라이드를 지원하고, 미지정 시 regions.is_active=true 전체를 기본값으로 사용 (경남 신규 16개 시군구 인구 데이터 사전 적재 가능)
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-01-SUMMARY.md` 생성.
</output>
</output>
