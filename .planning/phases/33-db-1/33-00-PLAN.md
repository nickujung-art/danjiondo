---
phase: 33-db-1
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/seed.ts
  - src/lib/data/regions.ts
  - src/lib/data/regions.test.ts
  - src/__tests__/seed-region.test.ts
autonomous: true
requirements:
  - REGION-01
must_haves:
  truths:
    - "regions 테이블에 경남 16개 신규 시군구가 존재하고 is_active=true이다"
    - "신규 시군구 sgg_code가 국토부 실거래가 API에서 유효한 코드로 확인된다 (빈 응답만 반복되는 오탐 코드가 아님)"
    - "regions 테이블 동적 조회를 위한 공용 헬퍼 함수가 존재하고 다른 Wave 1 plan들이 즉시 import해서 쓸 수 있다"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "REGIONS 배열에 경남 16개 신규 시군구 행 추가"
      contains: "48170"
    - path: "src/lib/data/regions.ts"
      provides: "getActiveSggCodes / getActiveCityNames 공용 동적 조회 함수"
      exports: ["getActiveSggCodes", "getActiveCityNames"]
    - path: "src/lib/data/regions.test.ts"
      provides: "getActiveSggCodes/getActiveCityNames 유닛 테스트 (mock supabase)"
    - path: "src/__tests__/seed-region.test.ts"
      provides: "경남 전체 22개 시군구 기준으로 갱신된 통합 테스트"
  key_links:
    - from: "scripts/seed.ts"
      to: "regions 테이블"
      via: "supabase.from('regions').upsert(REGIONS, { onConflict: 'sgg_code' })"
      pattern: "upsert.*regions"
    - from: "src/lib/data/regions.ts"
      to: "regions 테이블"
      via: "supabase.from('regions').select().eq('is_active', true)"
      pattern: "is_active"
---

<objective>
`regions` 테이블에 경남 전체 나머지 16개 시군구를 시딩하고, Wave 1의 모든 하드코딩 필터 리팩터 plan(33-01~33-05)이 공통으로 사용할 동적 조회 헬퍼(`src/lib/data/regions.ts`)를 먼저 정의한다. 시딩 직후 국토부 API로 신규 코드의 유효성을 단발 검증하여, 잘못된 법정동코드로 대규모 백필(33-07)이 낭비되는 것을 방지한다.

Purpose: 이 phase의 모든 후속 plan(33-01~33-08)이 의존하는 지역 마스터 데이터 + 인터페이스 계약을 Wave 0에서 확정한다.
Output: regions 테이블 22행(기존 6 + 신규 16), `getActiveSggCodes()`/`getActiveCityNames()` 공용 함수, 갱신된 seed-region 통합 테스트.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
<!-- scripts/seed.ts 현재 REGIONS 배열 (기존 6행, 이 형식 그대로 16행 추가) -->
```typescript
const REGIONS = [
  { sgg_code: '48121', sgg_name: '창원시 의창구',    si: '창원시', gu: '의창구' },
  { sgg_code: '48123', sgg_name: '창원시 성산구',    si: '창원시', gu: '성산구' },
  { sgg_code: '48125', sgg_name: '창원시 마산합포구', si: '창원시', gu: '마산합포구' },
  { sgg_code: '48127', sgg_name: '창원시 마산회원구', si: '창원시', gu: '마산회원구' },
  { sgg_code: '48129', sgg_name: '창원시 진해구',    si: '창원시', gu: '진해구' },
  { sgg_code: '48250', sgg_name: '김해시',           si: '김해시', gu: null },
] as const
```

<!-- regions 테이블 실제 컬럼 타입 (src/types/database.ts) -->
```typescript
regions.Row = {
  created_at: string
  gu:         string | null
  is_active:  boolean
  sgg_code:   string
  sgg_name:   string
  si:         string
}
```

<!-- backfill-realprice.ts에 이미 구현된 동일 패턴 (regions.ts 헬퍼가 이 패턴을 재사용) -->
```typescript
async function getSggCodes(): Promise<string[]> {
  const { data, error } = await supabase
    .from('regions')
    .select('sgg_code')
    .eq('is_active', true)
    .order('sgg_code')
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  return (data ?? []).map((r: { sgg_code: string }) => r.sgg_code)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 경남 16개 신규 시군구 regions 시딩</name>
  <files>scripts/seed.ts</files>
  <read_first>scripts/seed.ts</read_first>
  <action>
scripts/seed.ts의 `REGIONS` 배열(라인 12~19)에 아래 16개 행을 기존 6행 뒤에 추가한다 (형식은 기존 6행과 동일하게 유지, `as const` 유지):

```typescript
  { sgg_code: '48170', sgg_name: '진주시', si: '진주시', gu: null },
  { sgg_code: '48220', sgg_name: '통영시', si: '통영시', gu: null },
  { sgg_code: '48240', sgg_name: '사천시', si: '사천시', gu: null },
  { sgg_code: '48270', sgg_name: '밀양시', si: '밀양시', gu: null },
  { sgg_code: '48310', sgg_name: '거제시', si: '거제시', gu: null },
  { sgg_code: '48330', sgg_name: '양산시', si: '양산시', gu: null },
  { sgg_code: '48720', sgg_name: '의령군', si: '의령군', gu: null },
  { sgg_code: '48730', sgg_name: '함안군', si: '함안군', gu: null },
  { sgg_code: '48740', sgg_name: '창녕군', si: '창녕군', gu: null },
  { sgg_code: '48820', sgg_name: '고성군', si: '고성군', gu: null },
  { sgg_code: '48840', sgg_name: '남해군', si: '남해군', gu: null },
  { sgg_code: '48850', sgg_name: '하동군', si: '하동군', gu: null },
  { sgg_code: '48860', sgg_name: '산청군', si: '산청군', gu: null },
  { sgg_code: '48870', sgg_name: '함양군', si: '함양군', gu: null },
  { sgg_code: '48880', sgg_name: '거창군', si: '거창군', gu: null },
  { sgg_code: '48890', sgg_name: '합천군', si: '합천군', gu: null },
```

⚠ 이 16개 법정동코드는 RESEARCH.md에서 MEDIUM confidence로 표시됨 (2개 독립 웹소스 교차검증, 정부 원본 표 미확인). 스키마 제약(`sgg_code ~ '^\d{5}$'`)은 형식만 검증하고 실제 유효성은 검증 못하므로, Task 2에서 반드시 API 단발 검증을 수행한다.

수정 후 `npm run db:seed` 실행 (내부적으로 `tsx scripts/seed.ts` → `regions` upsert, `onConflict: 'sgg_code'`라 기존 6행은 그대로 유지되고 신규 16행만 추가됨 — 스키마 변경 없는 순수 데이터 upsert이므로 `supabase db push` 불필요).
  </action>
  <acceptance_criteria>
    - `grep -c "sgg_code: '48" scripts/seed.ts` == 22
    - `npm run db:seed` 실행 로그에 `✓ regions: 22건` 출력
  </acceptance_criteria>
  <verify>
    <automated>npm run db:seed 2>&1 | grep "regions:"</automated>
  </verify>
  <done>regions 테이블에 22행(기존 6 + 신규 16)이 존재하고 신규 16행 모두 is_active=true, gu=null이다.</done>
</task>

<task type="auto">
  <name>Task 2: 신규 시군구 법정동코드 단발 검증 (Pitfall 4 방어)</name>
  <files>없음 (검증 전용, 코드 변경 없음)</files>
  <read_first>scripts/backfill-realprice.ts</read_first>
  <action>
신규 16개 sgg_code가 실제로 유효한 국토부 LAWD_CD인지, 10년 전체 백필(33-07)을 트리거하기 전에 최근 1개월치만으로 확인한다. 국토부 API는 잘못된 코드에도 200 OK + 빈 items를 반환할 수 있어(Pitfall 4), HTTP 에러가 아니라 `ingest_runs.rowsUpserted` 실측으로 판단해야 한다.

```bash
npx tsx scripts/backfill-realprice.ts \
  --sgg=48170,48220,48240,48270,48310,48330,48720,48730,48740,48820,48840,48850,48860,48870,48880,48890 \
  --from=202606 --to=202606
```

실행 후 결과를 `ingest_runs` 테이블에서 확인한다 (Supabase SQL 또는 admin 클라이언트로):
```sql
select sgg_code, status, rows_upserted from ingest_runs
where source_id in ('molit_trade','molit_villa_trade')
  and year_month = '202606'
  and sgg_code in ('48170','48220','48240','48270','48310','48330','48720','48730','48740','48820','48840','48850','48860','48870','48880','48890')
order by sgg_code;
```

판단 기준:
- 인구가 많은 시(진주·통영·사천·밀양·거제·양산)는 `rows_upserted > 0`이 기대됨 — 0이면 코드 오류 의심, land.koreacharts.com/land/list/<code>.html 재확인 필요
- 인구 3만 미만 군(의령·산청·함양·거창·합천 등)은 해당 월 거래가 0건일 수 있음 (Open Question 3 — 정상 상황, 백필 우선순위만 낮추면 됨)
- **전체 16개 코드에서 전부 0건이면 시스템적 오류(코드 형식·API 파라미터 문제)일 가능성이 높음 — 이 경우 Task 1로 돌아가 코드를 재검증한다**

이 태스크는 코드를 수정하지 않는다. 검증 결과는 실행 로그로 남기고 다음 태스크(Task 3)와 이후 33-07 백필 실행 판단의 근거로 사용한다.
  </action>
  <acceptance_criteria>
    - `ingest_runs`에 신규 16개 sgg_code × 202606 조합의 status='success' 레코드가 존재한다 (일부 rows_upserted=0은 허용, 전체 0건은 재검증 필요)
    - 진주시(48170)·통영시(48220)·거제시(48310)·양산시(48330) 중 최소 1곳은 rows_upserted > 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/backfill-realprice.ts --sgg=48170,48310,48330 --from=202606 --to=202606 --apt 2>&1 | tail -5</automated>
  </verify>
  <done>16개 신규 sgg_code에 대한 최근 1개월 백필이 실행되었고, 주요 시(진주·통영·거제·양산 등)에서 0건이 아닌 실거래 데이터가 확인되어 법정동코드가 유효함이 검증된다.</done>
</task>

<task type="auto">
  <name>Task 3: 지역 마스터 동적 조회 공용 헬퍼 + 테스트 정합성</name>
  <files>src/lib/data/regions.ts, src/lib/data/regions.test.ts, src/__tests__/seed-region.test.ts</files>
  <read_first>src/__tests__/seed-region.test.ts, src/lib/data/seo-hierarchy.ts</read_first>
  <action>
**1. `src/lib/data/regions.ts` 신규 생성** — Wave 1의 33-01~33-05가 공통으로 import할 인터페이스 계약. `server-only` + `SupabaseClient<Database>` 파라미터를 받는 순수 함수 2개:

```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

/**
 * regions 테이블에서 is_active=true인 sgg_code 목록을 동적으로 조회한다.
 * ALLOWED_SGG_CODES/ACTIVE_SGG_CODES/LAWD_CODES/offiSggCodes 등
 * 인라인 하드코딩 배열을 대체하는 유일한 지역 마스터 접근 경로 (Phase 33).
 */
export async function getActiveSggCodes(
  supabase: SupabaseClient<Database>,
): Promise<string[]> {
  const { data, error } = await supabase
    .from('regions')
    .select('sgg_code')
    .eq('is_active', true)
    .order('sgg_code')
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  return (data ?? []).map((r) => r.sgg_code)
}

/**
 * regions 테이블에서 is_active=true인 시/군 이름을 '시'/'군' 접미사 제거 후 중복 제거하여 반환한다.
 * 청약홈(cheongyak) HSSPLY_ADRES 주소 문자열 매칭용 — '창원시'→'창원', '의령군'→'의령'.
 */
export async function getActiveCityNames(
  supabase: SupabaseClient<Database>,
): Promise<string[]> {
  const { data, error } = await supabase
    .from('regions')
    .select('si')
    .eq('is_active', true)
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  const names = new Set((data ?? []).map((r) => r.si.replace(/(시|군)$/, '')))
  return [...names]
}
```

**2. `src/lib/data/regions.test.ts` 신규 생성** — `seo-hierarchy.test.ts`의 mock 패턴(`makeSupabaseMock`)을 참고하여 두 함수를 유닛 테스트:
- `getActiveSggCodes`: mock select 결과 `[{sgg_code:'48121'},{sgg_code:'48250'}]` → `['48121','48250']` 반환 확인
- `getActiveSggCodes`: error 발생 시 throw 확인
- `getActiveCityNames`: mock select 결과 `[{si:'창원시'},{si:'창원시'},{si:'의령군'}]` → `['창원','의령']` (중복 제거) 반환 확인

**3. `src/__tests__/seed-region.test.ts` 갱신** — 현재 `TARGET_SGG_CODES`(6개, 창원·김해)는 `.in('sgg_code', TARGET_SGG_CODES)`로 필터링되므로 신규 16행 추가와 무관하게 그대로 통과하지만(허위 경보), 경남 전체 시딩을 검증하는 테스트가 없는 것이 실제 갭이다. 다음을 변경한다:
- `TARGET_SGG_CODES`를 `CHANGWON_GIMHAE_SGG_CODES`로 이름 변경(기존 6개, 하위 호환 유지)
- `GYEONGNAM_EXPANSION_SGG_CODES` 신규 상수 추가 (Task 1의 16개 코드)
- 기존 `it('regions: 창원·김해 sgg_code 6건 존재', ...)`는 이름 변경한 상수로 유지
- 신규 `it('regions: 경남 전체 22개 시군구 존재', ...)` 테스트 추가 — `admin.from('regions').select('sgg_code', {count:'exact', head:true})`로 `count >= 22` 확인 (upsert이므로 이후 재확장 대비 `>=` 사용)
- 신규 `it('regions: 경남 확장 16개 시군구 gu=null', ...)` 테스트 추가 — `GYEONGNAM_EXPANSION_SGG_CODES`로 `.in()` 조회 후 모든 행 `gu === null` 확인 (창원 5구 패턴과 구분되는 무구 시군 패턴 회귀 방지)
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/regions.test.ts` 통과
    - `npx vitest run src/__tests__/seed-region.test.ts` 통과 (SKEY 있는 로컬/CI 환경에서 integration describe 포함)
    - `grep -c "GYEONGNAM_EXPANSION_SGG_CODES" src/__tests__/seed-region.test.ts` >= 1
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/data/regions.test.ts src/__tests__/seed-region.test.ts</automated>
  </verify>
  <done>regions.ts가 존재하고 유닛 테스트를 통과하며, seed-region.test.ts가 경남 전체 22개 시군구 기준으로 갱신되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CLI 인자(`--sgg=`) → 국토부 API 파라미터 | 로컬/CI 실행자만 트리거 가능, 사용자 입력 아님 |
| regions.sgg_code → 하위 plan들의 allowlist 소스 | DB 값이 이후 모든 지역 필터링의 신뢰 기준점이 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | scripts/seed.ts REGIONS 배열 | accept | git 관리 파일, service_role 키로만 실행 가능, RLS write 정책 없음(=service_role 전용) |
| T-33-02 | Information Disclosure | regions.ts 함수 | accept | public read 정책 이미 존재하는 데이터(지역 코드), 민감정보 아님 |
</threat_model>

<verification>
```bash
npm run db:seed 2>&1 | grep "regions: 22건"
npx vitest run src/lib/data/regions.test.ts src/__tests__/seed-region.test.ts
```
</verification>

<success_criteria>
1. regions 테이블에 22개 시군구가 존재하고 신규 16개는 is_active=true, gu=null
2. 신규 sgg_code 중 주요 시(진주·통영·거제·양산) 최소 1곳에서 국토부 API 실거래 데이터가 확인됨
3. `getActiveSggCodes()`/`getActiveCityNames()` 공용 함수가 존재하고 유닛 테스트 통과
4. seed-region.test.ts가 경남 전체 확장을 반영하여 갱신됨
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-00-SUMMARY.md` 생성.
</output>
