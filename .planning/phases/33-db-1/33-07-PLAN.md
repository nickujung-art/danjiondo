---
phase: 33-db-1
plan: "07"
type: execute
wave: 2
depends_on: ["33-06"]
files_modified: []
autonomous: false
requirements:
  - REGION-07
must_haves:
  truths:
    - "경남 신규 16개 시군구의 10년치 국토부 실거래가(아파트+연립다세대)가 transactions 테이블에 적재된다"
    - "ingest_runs 테이블에서 신규 16개 시군구 × 전체 기간 조합이 모두 status='success'로 기록된다"
  artifacts: []
  key_links:
    - from: ".github/workflows/molit-backfill-once.yml"
      to: "scripts/backfill-realprice.ts"
      via: "workflow_dispatch inputs.sgg_codes"
      pattern: "sgg_codes"
---

<objective>
경남 신규 16개 시군구에 대해 국토부 실거래가 10년 백필을 실행한다. 05-00 phase 선례(창원+김해 6개 시군구, 3일 분할, timeout-minutes: 300)를 그대로 재사용한다 — `.github/workflows/molit-backfill-once.yml`은 이미 임의 `sgg_codes` 입력을 받도록 범용 설계되어 있어 코드 변경이 필요 없다.

**왜 checkpoint:human-action인가:** 국토부 API는 일 10,000회 한도가 있고 `molit-daily.yml` 일배치 cron과 키를 공유한다(Pitfall 3). 16개 신규 시군구 × 10년 × (아파트+연립다세대) 조합은 한 번의 300분 실행으로 끝나지 않을 가능성이 높다. `--resume` 플래그로 재개 가능하게 설계되어 있지만, "다음 날 재실행" 자체는 이 세션 안에서 자동화할 수 없는 다일(多日) 실물 시간 경과가 필요한 작업이므로, Claude가 1일차를 자동 트리거한 뒤 완료 여부를 사용자가 확인하고 필요 시 재트리거하는 방식으로 진행한다.

Purpose: 경남 신규 지역의 실거래가 데이터를 확보하여 33-08의 용량 실측·비용 결정 게이트에 필요한 실제 데이터를 만든다.
Output: transactions 테이블에 경남 신규 16개 시군구 10년치 거래 데이터, ingest_runs에 완료 기록.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\05-data-expansion-ops\COMPLETE.md
@C:\Users\jung\coding\bds\.github\workflows\molit-backfill-once.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 1일차 백필 트리거 (경남 신규 16개 시군구)</name>
  <files>없음 (기존 워크플로 트리거, 코드 변경 없음)</files>
  <read_first>.github/workflows/molit-backfill-once.yml</read_first>
  <action>
`gh` CLI로 기존 `molit-backfill-once.yml` workflow_dispatch를 트리거한다 (워크플로 자체는 이미 임의 `sgg_codes` 입력을 받도록 범용 설계되어 있어 수정 불필요):

```bash
gh workflow run molit-backfill-once.yml \
  -f sgg_codes=48170,48220,48240,48270,48310,48330,48720,48730,48740,48820,48840,48850,48860,48870,48880,48890
```

일배치 cron(`molit-daily.yml`, 04:00 KST 실행)과 API 한도(일 10,000회)를 공유하므로(Pitfall 3), 04:00 KST 전후 1시간은 피해서 트리거한다. 트리거 후 실행 상태를 확인한다:

```bash
gh run list --workflow=molit-backfill-once.yml --limit=1
gh run watch  # 또는 --exit-status로 완료까지 대기 (최대 300분 timeout)
```

`timeout-minutes: 300` 제한으로 1회 실행이 도중에 끊길 수 있다 — 이는 실패가 아니라 정상적인 설계이며, `ingest_runs` 테이블의 `status='success'` 기록을 기준으로 재개 가능하다(스크립트 내부 `--resume` 플래그).
  </action>
  <acceptance_criteria>
    - `gh run list --workflow=molit-backfill-once.yml --limit=1` 출력에 방금 트리거한 run이 나타남
    - 실행 완료(성공 또는 timeout) 후 `select count(*) from ingest_runs where source_id in ('molit_trade','molit_villa_trade') and sgg_code in ('48170','48220','...') and status='success'` 결과 > 0
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=molit-backfill-once.yml --limit=3</automated>
  </verify>
  <done>1일차 백필 workflow_dispatch가 트리거되고 실행이 시작되었다 (완료 여부는 Task 2에서 확인).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
경남 신규 16개 시군구 국토부 실거래가 10년 백필을 위한 1일차 GitHub Actions 실행을 자동 트리거했다. API 일 10,000회 한도로 인해 전체 백필은 여러 날에 걸쳐 나뉘어 실행되어야 한다 (05-00 phase 선례: 창원+김해 6개 시군구도 3일 소요됨 — 이번은 16개 시군구로 규모가 더 크므로 RESEARCH.md 추정상 5~8영업일 예상, LOW confidence — 실제 신규 지역 거래량에 따라 변동).
  </what-built>
  <how-to-verify>
1. 오늘 실행 상태 확인: `gh run list --workflow=molit-backfill-once.yml --limit=1`
2. 완료 여부와 무관하게, 남은 미완료 조합이 있는지 확인:
   ```sql
   -- 예상 총 조합 수 대비 완료 수 비교 (16개 시군구 × 120개월 × 2모드 = 3840, 실제로는 --resume이 건너뛴 기존 성공 건 제외)
   select sgg_code, count(*) filter (where status='success') as done
   from ingest_runs
   where source_id in ('molit_trade','molit_villa_trade')
     and sgg_code in ('48170','48220','48240','48270','48310','48330','48720','48730','48740','48820','48840','48850','48860','48870','48880','48890')
   group by sgg_code order by sgg_code;
   ```
3. 미완료 시군구가 남아있으면, **다음 영업일** (04:00 KST 일배치 cron과 겹치지 않는 시간대) 아래 명령으로 재트리거한다 — `--resume`이 자동 적용되므로 이미 완료된 월은 건너뛴다:
   ```bash
   gh workflow run molit-backfill-once.yml \
     -f sgg_codes=48170,48220,48240,48270,48310,48330,48720,48730,48740,48820,48840,48850,48860,48870,48880,48890
   ```
4. 모든 시군구가 완료될 때까지 이 재트리거를 반복한다 (인구가 적은 군 지역은 애초에 거래가 희소해 빠르게 완료됨 — Open Question 3, 정상 상황)
  </how-to-verify>
  <resume-signal>모든 16개 시군구의 ingest_runs가 완료 상태(성공 또는 해당 월 데이터 없음으로 확인됨)가 되면 "백필 완료"라고 응답. 진행 중이면 "N일차 진행 중, M개 시군구 남음"으로 진행 상황을 알려주면 다음 세션에서 이어서 재트리거한다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub Actions workflow_dispatch → 국토부 API | repo write 권한 있는 관리자만 트리거 가능 (기존 워크플로와 동일 신뢰 수준) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Denial of Service (자원 고갈) | 국토부 API 일 10,000회 한도 | mitigate | 04:00 KST 일배치 cron과 겹치지 않는 시간대 트리거 + `--resume` 재개 가능 설계로 한도 소진 시 자연스럽게 다음 날 이어서 실행 |
</threat_model>

<verification>
```bash
gh run list --workflow=molit-backfill-once.yml --limit=5
```
</verification>

<success_criteria>
1. 경남 신규 16개 시군구의 10년치 국토부 실거래가(아파트+연립다세대)가 모두 적재됨
2. ingest_runs에 신규 16개 시군구 × 전체 기간 조합이 status='success'로 기록됨
3. 인구가 적은 군 지역의 낮은/0건 거래량은 정상으로 간주됨 (오류 아님)
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-07-SUMMARY.md` 생성. 여러 날에 걸쳐 진행된 경우 각 재트리거 일자와 최종 완료 상태를 SUMMARY에 기록한다.
</output>
