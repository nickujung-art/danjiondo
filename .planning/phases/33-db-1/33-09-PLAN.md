---
phase: 33-db-1
plan: "09"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/app/map/page.tsx
  - src/app/api/ads/sidebar/route.ts
autonomous: true
requirements:
  - REGION-09
must_haves:
  truths:
    - "/map 페이지가 TARGET_SGG 하드코딩 배열 대신 regions 테이블(is_active=true) 기반 동적 조회로 지도에 표시할 단지·검색 결과를 결정한다"
    - "사이드바 광고 API(/api/ads/sidebar)가 VALID_SGG_CODES 하드코딩 배열 대신 regions 테이블 기반 동적 조회로 sgg_code 쿼리 파라미터를 검증한다"
    - "TARGET_SGG/VALID_SGG_CODES 정적 배열이 두 파일에서 완전히 제거되었다"
    - "/map, /api/ads/sidebar의 UI 구조·레이아웃·응답 스키마는 변경되지 않는다 (기계적 데이터 소스 교체만, CONTEXT.md UI 동결 원칙 준수)"
  artifacts:
    - path: "src/app/map/page.tsx"
      provides: "getActiveSggCodes(supabase) 기반 동적 지도 단지·검색 조회"
    - path: "src/app/api/ads/sidebar/route.ts"
      provides: "getActiveSggCodes(supabase) 기반 동적 sgg_code allowlist 검증"
  key_links:
    - from: "src/app/map/page.tsx"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
    - from: "src/app/api/ads/sidebar/route.ts"
      to: "src/lib/data/regions.ts"
      via: "await getActiveSggCodes(supabase)"
      pattern: "getActiveSggCodes"
---

<objective>
**(리비전 추가, 2026-07-03 plan-checker Gap 1 대응 — 최우선순위 blocker):** plan-checker 1차 검토에서 CONTEXT.md/RESEARCH.md 사전 조사와 기존 9개 plan 모두 놓친 2곳의 실사용 하드코딩 지역 필터를 발견했다. `src/app/map/page.tsx`의 `TARGET_SGG`(사이트의 대표 기능 "지도 검색"의 데이터 소스)와 `src/app/api/ads/sidebar/route.ts`의 `VALID_SGG_CODES`(사이드바 광고 지역 타겟팅 검증)가 여전히 6~7개 창원·김해 코드로 고정되어 있어, 33-00~33-08이 모두 완료되어도 지도에는 경남 신규 16개 시군구 단지가 전혀 표시되지 않는 문제가 남는다.

두 배열 모두 33-00에서 생성되는 `getActiveSggCodes(supabase)` 공용 헬퍼로 교체하는 기계적 데이터 소스 전환이며, `/map`·`/api/ads/sidebar` 어느 쪽도 UI 구조·레이아웃·응답 스키마를 변경하지 않는다 (CONTEXT.md "UI 변경 없음" 원칙 준수 — 33-01/33-02/33-03과 동일한 패턴).

**참고 (조사 완료, 실행 불필요):** `git log -p -- src/app/map/page.tsx`로 확인한 결과 `TARGET_SGG`는 현재 7개 코드(`48121,48123,48125,48127,48129,48250,48720`)를 갖고 있으며, 마지막 추가분 `48720`은 커밋 메시지에 "김해 단지가 지도에서 누락된 문제" 수정으로 기록되어 있다. 그러나 `48720`은 실제로는 의령군의 sgg_code이고 김해시는 이미 `48250`으로 포함되어 있었다 — 즉 원래 커밋 메시지의 의도(김해 누락 수정)와 실제 추가된 코드가 어긋난 것으로 보이는 이력상의 불일치이다. 이 phase의 동적 전환 이후에는 `48250`(김해)과 `48720`(의령군) 모두 `regions` 테이블에 정상적으로 포함되어 있으므로(33-00에서 의령군도 신규 시딩됨) 기능 손실은 없다 — 별도 수정 태스크 불필요, 기록만 남긴다.

Purpose: 사이트의 플래그십 기능인 지도 검색과 광고 지역 타겟팅이 경남 전체 데이터를 반영하게 한다.
Output: map/page.tsx와 ads/sidebar/route.ts 두 파일의 하드코딩 배열이 regions 테이블 기반 동적 조회로 전환됨.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
<!-- 33-00에서 생성된 공용 헬퍼 (이 plan은 이것만 import) -->
```typescript
// src/lib/data/regions.ts
export async function getActiveSggCodes(supabase: SupabaseClient<Database>): Promise<string[]>
```

<!-- src/lib/data/complexes-map.ts / src/lib/data/complex-search.ts — 이미 sggCodes: string[] 파라미터를 받는 범용 시그니처, 변경 불필요 -->
```typescript
export async function getComplexesForMap(sggCodes: string[], supabase: SupabaseClient): Promise<ComplexMapItem[]>
export async function searchComplexes(query: string, sggCodes: string[], supabase: SupabaseClient, limit?: number): Promise<ComplexSearchResult[]>
```

<!-- src/app/map/page.tsx 현재 전체 내용 (라인 9: TARGET_SGG 정의, 라인 22/26: 사용처) -->
```typescript
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getComplexesForMap } from '@/lib/data/complexes-map'
import { getPresalePinsForMap } from '@/lib/data/presale-pins'
import { searchComplexes } from '@/lib/data/complex-search'
import { getActiveAds } from '@/lib/data/ads'
import { MapView } from '@/components/map/MapView'
import { SidePanel } from '@/components/search/SidePanel'

const TARGET_SGG = ['48121', '48123', '48125', '48127', '48129', '48250', '48720']

export const revalidate = 0

interface Props {
  searchParams: Promise<{ q?: string }>
}

export default async function MapPage({ searchParams }: Props) {
  const { q = '' } = await searchParams
  const supabase = createReadonlyClient()

  const [complexes, searchResults, presalePins, mapPopupAds, inFeedAds] = await Promise.all([
    getComplexesForMap(TARGET_SGG, supabase).catch((err: unknown) => {
      console.error('[map] getComplexesForMap failed:', err)
      return []
    }),
    searchComplexes(q, TARGET_SGG, supabase).catch(() => []),
    getPresalePinsForMap(supabase).catch(() => []),
    getActiveAds('map_popup', supabase).catch(() => []),
    getActiveAds('in_feed', supabase).catch(() => []),
  ])
  return (
    <main className="flex overflow-hidden h-[calc(100dvh-120px)] sm:h-[calc(100dvh-56px)]">
      <h1 className="sr-only">단지온도 지도 — 창원·김해 아파트 실거래가</h1>
      <div className="hidden sm:block flex-shrink-0">
        <SidePanel query={q} complexes={searchResults} inFeedAds={inFeedAds} />
      </div>
      <div className="flex-1">
        <MapView complexes={complexes} presalePins={presalePins} mapPopupAds={mapPopupAds} />
      </div>
    </main>
  )
}
```

<!-- src/app/api/ads/sidebar/route.ts 현재 전체 내용 -->
```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getActiveAds } from '@/lib/data/ads'
import type { AdCampaign } from '@/lib/data/ads'

export const dynamic = 'force-dynamic'

const VALID_SGG_CODES = new Set(['48121', '48123', '48125', '48127', '48129', '48250', '48720'])

export async function GET(request: NextRequest): Promise<NextResponse> {
  const { searchParams } = request.nextUrl
  const raw = searchParams.get('sgg_code')
  const sggCode = raw && VALID_SGG_CODES.has(raw) ? raw : undefined

  const supabase = createReadonlyClient()

  // CRITICAL: getActiveAds enforces now() BETWEEN starts_at AND ends_at AND status='approved'
  const ads: AdCampaign[] = await getActiveAds('sidebar', supabase, sggCode)

  return NextResponse.json(
    { ads },
    { headers: { 'Cache-Control': 'no-store' } },
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /map 페이지 TARGET_SGG 동적 지역 필터 전환</name>
  <files>src/app/map/page.tsx</files>
  <read_first>src/app/map/page.tsx</read_first>
  <action>
`src/app/map/page.tsx`를 다음과 같이 수정한다 (interfaces 블록의 "현재 전체 내용"이 정확한 시작 상태):

1. import 목록에 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가 (다른 `@/lib/data/*` import들과 같은 위치, `getActiveAds` import 다음 줄에 삽입)
2. 라인 9 `const TARGET_SGG = ['48121', '48123', '48125', '48127', '48129', '48250', '48720']` 모듈 레벨 상수를 완전히 삭제한다
3. `MapPage` 컴포넌트 함수 내부, `const supabase = createReadonlyClient()` 바로 다음 줄에 `const activeSggCodes = await getActiveSggCodes(supabase)`를 추가한다 (33-01의 invest/page.tsx, 33-02의 rankings.ts와 동일하게 — 에러 발생 시 try/catch로 감싸지 않고 자연스럽게 전파시킨다. Wave 1의 다른 모든 plan이 동일한 무-try/catch 패턴을 사용하므로 일관성 유지)
4. `Promise.all` 배열 내부 두 곳의 `TARGET_SGG` 참조를 `activeSggCodes`로 치환한다:
   - `getComplexesForMap(TARGET_SGG, supabase)` → `getComplexesForMap(activeSggCodes, supabase)`
   - `searchComplexes(q, TARGET_SGG, supabase)` → `searchComplexes(q, activeSggCodes, supabase)`
5. JSX(`<main>` 이하)와 `<h1>` 텍스트("단지온도 지도 — 창원·김해 아파트 실거래가")는 전혀 변경하지 않는다 — CONTEXT.md "UI 변경 없음" 원칙에 따라 이번 태스크는 데이터 소스 교체만 수행하며, 헤딩 문구·레이아웃 변경은 범위 밖(별도 phase의 UI 재기획 대상)이다.
  </action>
  <acceptance_criteria>
    - `grep -c "TARGET_SGG" src/app/map/page.tsx` == 0
    - `grep -c "getActiveSggCodes" src/app/map/page.tsx` >= 1
    - `grep -c "activeSggCodes" src/app/map/page.tsx` >= 3 (선언 1 + getComplexesForMap 1 + searchComplexes 1)
    - `grep -c "import { getActiveSggCodes } from '@/lib/data/regions'" src/app/map/page.tsx` == 1
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "app/map/page" | head -20</automated>
  </verify>
  <done>map/page.tsx에서 TARGET_SGG 정적 배열이 사라졌고, 지도 단지 조회·검색 모두 regions 테이블 기반 동적 조회(activeSggCodes)를 사용하며, JSX/레이아웃은 변경 없이 tsc 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: 사이드바 광고 API VALID_SGG_CODES 동적 지역 필터 전환</name>
  <files>src/app/api/ads/sidebar/route.ts</files>
  <read_first>src/app/api/ads/sidebar/route.ts</read_first>
  <action>
`src/app/api/ads/sidebar/route.ts`를 다음과 같이 수정한다 (interfaces 블록의 "현재 전체 내용"이 정확한 시작 상태):

1. import 목록에 `import { getActiveSggCodes } from '@/lib/data/regions'` 추가 (`getActiveAds` import 다음 줄)
2. 라인 8 `const VALID_SGG_CODES = new Set(['48121', '48123', '48125', '48127', '48129', '48250', '48720'])` 모듈 레벨 상수를 완전히 삭제한다
3. `GET` 함수 내부에서 기존 순서(`raw` 파싱 → `sggCode` 계산 → `supabase` 생성)를 다음처럼 재배치한다:
   ```typescript
   export async function GET(request: NextRequest): Promise<NextResponse> {
     const { searchParams } = request.nextUrl
     const raw = searchParams.get('sgg_code')

     const supabase = createReadonlyClient()
     const activeSggCodes = new Set(await getActiveSggCodes(supabase))
     const sggCode = raw && activeSggCodes.has(raw) ? raw : undefined

     // CRITICAL: getActiveAds enforces now() BETWEEN starts_at AND ends_at AND status='approved'
     const ads: AdCampaign[] = await getActiveAds('sidebar', supabase, sggCode)

     return NextResponse.json(
       { ads },
       { headers: { 'Cache-Control': 'no-store' } },
     )
   }
   ```
   `.has()` 검증 시맨틱은 기존과 완전히 동일하게 유지한다 (Set 기반 O(1) 조회, `raw &&` short-circuit 패턴 그대로) — 유일한 차이는 Set의 소스가 정적 배열에서 `getActiveSggCodes()` 동적 조회 결과로 바뀐 것뿐이다.
4. 응답 스키마(`{ ads }`, `Cache-Control: no-store` 헤더)는 전혀 변경하지 않는다.
  </action>
  <acceptance_criteria>
    - `grep -c "VALID_SGG_CODES" src/app/api/ads/sidebar/route.ts` == 0
    - `grep -c "getActiveSggCodes" src/app/api/ads/sidebar/route.ts` >= 1
    - `grep -c "activeSggCodes.has(raw)" src/app/api/ads/sidebar/route.ts` == 1
    - `grep -c "{ ads }" src/app/api/ads/sidebar/route.ts` == 1 (응답 스키마 불변 확인)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -i "api/ads/sidebar" | head -20</automated>
  </verify>
  <done>ads/sidebar/route.ts에서 VALID_SGG_CODES 정적 Set이 사라졌고, sgg_code 쿼리 파라미터가 regions 테이블 기반 동적 Set으로 검증되며, 응답 스키마는 변경 없이 tsc 에러가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| URL 쿼리 `sgg_code`(ads/sidebar) → DB 쿼리 필터 | 사용자가 임의 문자열 전달 가능, allowlist 대조 필수 |
| `/map` 검색어(`q`) → RPC 파라미터 | 기존 `searchComplexes`가 이미 정규화·RPC 바인딩 처리 (이번 plan에서 변경 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-12 | Tampering | ads/sidebar/route.ts `sgg_code` 쿼리 파라미터 | mitigate | `activeSggCodes.has(raw)` 검증 유지 (Decisions Log 2026-05-28 패턴 계승, 소스만 동적 regions 조회로 전환) |
| T-33-13 | Information Disclosure | map/page.tsx activeSggCodes | accept | 사용자 입력 경로 없음, 서버 컴포넌트 내부에서만 regions 테이블 조회 후 사용 |
</threat_model>

<verification>
```bash
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. map/page.tsx, ads/sidebar/route.ts에서 TARGET_SGG/VALID_SGG_CODES 정적 배열이 완전히 제거됨
2. 두 파일 모두 regions 테이블 기반 동적 조회(getActiveSggCodes)로 sgg_code 목록을 결정
3. `/map` 페이지 UI 구조·헤딩 문구, `/api/ads/sidebar` 응답 스키마 모두 변경 없음
4. tsc/lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-09-SUMMARY.md` 생성.
</output>
</output>
