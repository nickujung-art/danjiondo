---
phase: 33-db-1
plan: "04"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/lib/data/seo-hierarchy.test.ts
  - src/__tests__/school-ranking-regional.test.ts
autonomous: true
requirements:
  - REGION-05
must_haves:
  truths:
    - "구(區) 없는 경남 신규 시군구가 seo-hierarchy.ts의 hasGu 분기에서 김해시와 동일하게 dongList 경로로 처리됨이 테스트로 증명된다"
    - "school_ranking RPC가 창원 5개 구 패턴에 매칭되지 않는 시/군 데이터에 대해 gu=NULL을 반환함이 통합 테스트로 증명된다"
  artifacts:
    - path: "src/lib/data/seo-hierarchy.test.ts"
      provides: "경남 신규 시군구(구 없음) 케이스 회귀 테스트 추가"
    - path: "src/__tests__/school-ranking-regional.test.ts"
      provides: "school_ranking RPC 임의 si 파라미터 + gu NULL 폴백 통합 테스트 (신규 파일)"
  key_links: []
---

<objective>
RESEARCH.md Pattern 2/3이 코드베이스 감사로 이미 확인한 사실 — `seo-hierarchy.ts`의 `hasGu` 판별과 `school_ranking` SQL RPC의 `gu` CASE WHEN은 코드 변경 없이도 구(區) 없는 시군구(경남 신규 16개 포함)를 올바르게 처리한다 — 를 자동화된 회귀 테스트로 고정한다. CONTEXT.md의 "경남까지만 일반화" 요구사항은 실제로는 "코드 변경 불필요, 확인 + 회귀 테스트"로 범위가 축소됨(RESEARCH.md 명시).

Purpose: 향후 다른 plan이 이 두 함수를 실수로 창원/김해 전용으로 되돌리는 회귀를 방지한다. school_ranking RPC는 현재 전용 테스트가 전혀 없다는 갭(RESEARCH.md Wave 0 Gaps)도 메운다.
Output: seo-hierarchy.test.ts에 신규 케이스 1개 추가, school_ranking RPC 통합 테스트 신규 파일 1개.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md
@C:\Users\jung\coding\bds\src\lib\data\seo-hierarchy.ts
@C:\Users\jung\coding\bds\src\app\actions\education.ts

<interfaces>
<!-- seo-hierarchy.test.ts 기존 mock 헬퍼 패턴 (그대로 재사용) -->
```typescript
function makeSupabaseMock(tableData: { data: unknown; error: null }) {
  const chain = makeChainMock(tableData)
  return { from: vi.fn().mockReturnValue(chain) } as unknown as SupabaseClient
}
```

<!-- school_ranking RPC 시그니처 (supabase/migrations/20260616000004_school_ranking_rpc.sql) -->
```sql
CREATE OR REPLACE FUNCTION public.school_ranking(
  p_si          text,   -- 임의 문자열 허용, allowlist 없음
  p_school_type text,   -- 'elementary' | 'middle' | 'high'
  p_metric      text    -- 'students_per_class' | 'special' | 'univ_rate'
) RETURNS TABLE (rank bigint, school_name text, metric_value numeric, gu text)
-- gu: road_address가 '창원시 OO구' 5개 패턴에 매칭 안 되면 NULL (김해시 데이터로 이미 검증된 동작)
```

<!-- 기존 integration test 패턴 (src/__tests__/seed-region.test.ts 참고) -->
```typescript
import { describe, it, expect } from 'vitest'
import { SKEY, admin } from './helpers/db'

describe.skipIf(!SKEY)('...', () => { /* SKEY 없으면 스킵 */ })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: seo-hierarchy + school_ranking RPC 무구(無區) 시군구 회귀 테스트</name>
  <files>src/lib/data/seo-hierarchy.test.ts, src/__tests__/school-ranking-regional.test.ts</files>
  <read_first>src/lib/data/seo-hierarchy.test.ts, src/app/actions/education.ts</read_first>
  <behavior>
    - seo-hierarchy.test.ts: `getSiPageData('진주시', ...)` 호출 시 김해시와 동일하게 `guList.length === 0`, `dongList.length > 0` (dong별 집계 경로로 처리됨)
    - school-ranking-regional.test.ts (SKEY 있을 때만 실행):
      - `admin.rpc('school_ranking', { p_si: '없는가짜시군', p_school_type: 'elementary', p_metric: 'students_per_class' })` 호출 시 에러 없이 빈 배열 반환 (p_si에 allowlist가 없어 임의 문자열도 에러 없이 처리됨을 증명)
      - `admin.rpc('school_ranking', { p_si: '김해시', ... })` 호출 결과가 있다면(데이터 존재 시) 모든 행의 `gu`가 `null`임을 확인 (구 없는 시군의 기존 실증 데이터로 향후 경남 신규 시군구도 동일하게 처리될 것임을 방증)
  </behavior>
  <action>
**1. `src/lib/data/seo-hierarchy.test.ts`** — `describe('getSiPageData', ...)` 블록 안, 기존 "김해시 — dongList 반환" 테스트 다음에 신규 케이스 추가:

```typescript
it('구 없는 신규 시군구(경남 확장) — 김해시와 동일하게 dongList 처리 (Phase 33 회귀 방지)', async () => {
  const rows = [
    { gu: null, dong: '평거동', avg_sale_per_pyeong: 750 },
    { gu: null, dong: '충무공동', avg_sale_per_pyeong: 820 },
  ]
  const supabase = makeSupabaseMock({ data: rows, error: null })
  const result = await getSiPageData('진주시', supabase)
  expect(result).not.toBeNull()
  expect(result!.guList.length).toBe(0)
  expect(result!.dongList.length).toBe(2)
  expect(result!.totalComplexes).toBe(2)
})
```

**2. `src/__tests__/school-ranking-regional.test.ts` 신규 생성** — `seed-region.test.ts`의 `SKEY`/`admin` import 패턴을 그대로 사용:

```typescript
/**
 * school_ranking RPC 회귀 테스트 — Phase 33 (경남 전체 확장)
 * p_si가 임의 문자열을 받고, gu 컬럼이 창원 5개 구 패턴 외에는 NULL로 폴백함을 검증.
 * 코드 변경 없이 기존 SQL 함수가 구 없는 시군구를 이미 올바르게 처리함을 증명 (RESEARCH.md Pattern 3).
 */
import { describe, it, expect } from 'vitest'
import { SKEY, admin } from './helpers/db'

describe.skipIf(!SKEY)('school_ranking RPC: 무구(無區) 시군구 처리 (integration)', () => {
  it('임의의 si 문자열도 에러 없이 처리된다 (allowlist 없음)', async () => {
    const { data, error } = await admin.rpc('school_ranking', {
      p_si: '존재하지않는시군구_테스트',
      p_school_type: 'elementary',
      p_metric: 'students_per_class',
    })
    expect(error).toBeNull()
    expect(data).toEqual([])
  })

  it('김해시(구 없음) 데이터가 있다면 모든 행의 gu가 null이다', async () => {
    const { data, error } = await admin.rpc('school_ranking', {
      p_si: '김해시',
      p_school_type: 'elementary',
      p_metric: 'students_per_class',
    })
    expect(error).toBeNull()
    for (const row of data ?? []) {
      expect(row.gu).toBeNull()
    }
  })

  it('창원시(구 있음) 데이터가 있다면 gu가 5개 구 중 하나이거나 null이다', async () => {
    const { data, error } = await admin.rpc('school_ranking', {
      p_si: '창원시',
      p_school_type: 'elementary',
      p_metric: 'students_per_class',
    })
    expect(error).toBeNull()
    const validGu = ['의창구', '성산구', '마산합포구', '마산회원구', '진해구', null]
    for (const row of data ?? []) {
      expect(validGu).toContain(row.gu)
    }
  })
})
```
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/seo-hierarchy.test.ts` 통과 (기존 4개 + 신규 1개 = 5개)
    - `npx vitest run src/__tests__/school-ranking-regional.test.ts` 실행 시 에러 없음 (SKEY 없는 환경에서는 스킵되어 통과)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/data/seo-hierarchy.test.ts src/__tests__/school-ranking-regional.test.ts</automated>
  </verify>
  <done>seo-hierarchy.test.ts에 구 없는 신규 시군구 케이스가 추가되고, school_ranking RPC 통합 테스트 신규 파일이 존재하며 두 파일 모두 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| 테스트 코드 → RPC 호출 | CI/로컬 테스트 환경 전용, 프로덕션 트래픽 아님 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Tampering | school_ranking RPC p_si 임의 문자열 허용 | accept | 읽기 전용 SELECT RPC, SQL 인젝션 불가(파라미터 바인딩), 응답에 민감정보 없음 — 기존 설계 그대로 유지 |
</threat_model>

<verification>
```bash
npx vitest run src/lib/data/seo-hierarchy.test.ts src/__tests__/school-ranking-regional.test.ts
```
</verification>

<success_criteria>
1. seo-hierarchy.test.ts가 구 없는 신규 시군구 케이스를 커버함
2. school_ranking RPC에 대한 전용 통합 테스트가 신규로 존재함
3. 두 테스트 모두 코드 변경 없이 기존 로직이 올바름을 증명 (production code diff 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-04-SUMMARY.md` 생성.
</output>
