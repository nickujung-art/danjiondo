---
phase: 33-db-1
plan: "05"
type: execute
wave: 1
depends_on: ["33-00"]
files_modified:
  - src/components/invest/PredictionSection.tsx
  - src/app/api/invest/prediction-commentary/route.ts
  - src/components/admin/AdCreateForm.tsx
  - src/components/admin/AdEditForm.tsx
  - src/components/presale/EnrichedPresaleCard.tsx
  - src/components/admin/cardnews/BuilderOptionsPanel.tsx
  - src/app/api/admin/cardnews/data/route.ts
autonomous: true
requirements:
  - REGION-06
must_haves:
  truths:
    - "AI 예측 카드·분양 카드·광고 등록 폼·카드뉴스 빌더 등에서 경남 신규 시군구 sgg_code가 원본 코드(예: '48170') 그대로 노출되지 않고 한글 지역명으로 표시된다"
    - "UI 레이아웃·구조는 변경되지 않는다 (기존 정적 Record/배열에 항목만 추가)"
  artifacts: []
  key_links: []
---

<objective>
CONTEXT.md 2026-07-03 추가 결정("UI SGG_LABEL 정적 맵은 라벨만 추가하는 기계적 데이터 변경으로 처리")에 따라, 33-01~33-03에서 데이터 레이어의 allowlist를 동적으로 확장한 결과 새로 노출될 수 있는 경남 16개 sgg_code에 대해 UI 표시용 라벨을 정적 맵에 기계적으로 추가한다. 레이아웃/구조는 일절 변경하지 않는다 (사이트 재기획 회의 진행 중 — UI 동결 원칙).

CONTEXT.md가 나열한 8개 파일 중 6개(PredictionSection.tsx, prediction-commentary/route.ts, AdCreateForm.tsx, AdEditForm.tsx, EnrichedPresaleCard.tsx, BuilderOptionsPanel.tsx)와, RESEARCH.md 사전 조사 목록에는 없었으나 코드 감사로 추가 발견된 `src/app/api/admin/cardnews/data/route.ts`의 SGG_LABEL을 포함한다. `invest/page.tsx`와 `gap-analysis/page.tsx`의 SGG_LABEL은 33-01에서 이미 처리되므로 이 plan에서 제외한다. `src/app/invest/region/[sggCode]/page.tsx`의 SGG_LABEL도 33-01에서 함께 처리되므로 제외한다.

Purpose: 신규 지역 데이터가 UI에 노출될 때 사용자에게 원본 5자리 코드가 아닌 읽을 수 있는 지역명이 표시되게 한다.
Output: 7개 파일의 SGG_LABEL/SGG_OPTIONS/REGION_OPTIONS 정적 구조체에 경남 16개 항목 추가.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-RESEARCH.md

<interfaces>
<!-- 경남 16개 신규 라벨 — Record<string,string> 형식 파일(PredictionSection.tsx, prediction-commentary/route.ts, EnrichedPresaleCard.tsx, admin/cardnews/data/route.ts)에 추가할 항목 -->
```typescript
'48170': '진주시', '48220': '통영시', '48240': '사천시', '48270': '밀양시',
'48310': '거제시', '48330': '양산시', '48720': '의령군', '48730': '함안군',
'48740': '창녕군', '48820': '고성군', '48840': '남해군', '48850': '하동군',
'48860': '산청군', '48870': '함양군', '48880': '거창군', '48890': '합천군',
```

<!-- admin/cardnews/data/route.ts는 "구" 접미사 없는 짧은 라벨 형식 사용 (기존 '의창구' 스타일과 통일) -->
```typescript
'48170': '진주시', '48220': '통영시', ... // 동일 (이미 시/군 단위라 湖 접미사 이슈 없음)
```

<!-- SGG_OPTIONS 배열 형식 파일(AdCreateForm.tsx, AdEditForm.tsx)에 추가할 항목 -->
```typescript
{ code: '48170', label: '진주시' },
{ code: '48220', label: '통영시' },
{ code: '48240', label: '사천시' },
{ code: '48270', label: '밀양시' },
{ code: '48310', label: '거제시' },
{ code: '48330', label: '양산시' },
{ code: '48720', label: '의령군' },
{ code: '48730', label: '함안군' },
{ code: '48740', label: '창녕군' },
{ code: '48820', label: '고성군' },
{ code: '48840', label: '남해군' },
{ code: '48850', label: '하동군' },
{ code: '48860', label: '산청군' },
{ code: '48870', label: '함양군' },
{ code: '48880', label: '거창군' },
{ code: '48890', label: '합천군' },
```

<!-- BuilderOptionsPanel.tsx는 REGION_OPTIONS={label, sggCodes[]} 형식 — 각 신규 시군구를 개별 옵션으로 추가 (김해시 행과 동일 패턴, "전체" 그룹핑 없음) -->
```typescript
{ label: '진주시', sggCodes: ['48170'] },
{ label: '통영시', sggCodes: ['48220'] },
{ label: '사천시', sggCodes: ['48240'] },
{ label: '밀양시', sggCodes: ['48270'] },
{ label: '거제시', sggCodes: ['48310'] },
{ label: '양산시', sggCodes: ['48330'] },
{ label: '의령군', sggCodes: ['48720'] },
{ label: '함안군', sggCodes: ['48730'] },
{ label: '창녕군', sggCodes: ['48740'] },
{ label: '고성군', sggCodes: ['48820'] },
{ label: '남해군', sggCodes: ['48840'] },
{ label: '하동군', sggCodes: ['48850'] },
{ label: '산청군', sggCodes: ['48860'] },
{ label: '함양군', sggCodes: ['48870'] },
{ label: '거창군', sggCodes: ['48880'] },
{ label: '합천군', sggCodes: ['48890'] },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SGG_LABEL Record 4개 파일 라벨 추가</name>
  <files>src/components/invest/PredictionSection.tsx, src/app/api/invest/prediction-commentary/route.ts, src/components/presale/EnrichedPresaleCard.tsx, src/app/api/admin/cardnews/data/route.ts</files>
  <read_first>src/components/invest/PredictionSection.tsx, src/app/api/invest/prediction-commentary/route.ts, src/components/presale/EnrichedPresaleCard.tsx, src/app/api/admin/cardnews/data/route.ts</read_first>
  <action>
4개 파일 각각에 이미 존재하는 `const SGG_LABEL: Record<string, string> = { ... }` 객체 리터럴의 마지막 항목(`'48250': '김해시',`) 바로 다음 줄에, interfaces 블록의 경남 16개 항목을 그대로 삽입한다. 각 파일의 기존 라벨 스타일(전체명 "창원 의창구" 형식 vs 축약형 "의창구" 형식)에 맞춰 접두사 유무만 맞춘다:
- `PredictionSection.tsx`: 기존 스타일 "창원 의창구" — 신규 항목은 이미 시/군 단위라 접두사 불필요, `'48170': '진주시'` 그대로 사용
- `prediction-commentary/route.ts`: 동일 (기존 "창원 의창구" 스타일)
- `EnrichedPresaleCard.tsx`: 동일 (기존 "창원 의창구" 스타일)
- `api/admin/cardnews/data/route.ts`: 기존 스타일 "의창구"(축약형, 창원 접두사 없음) — 신규 항목도 그대로 `'48170': '진주시'` 형식 사용 (이미 축약형과 동일)

객체 리터럴 구조·타입·주변 코드는 변경하지 않는다. 항목 추가만 수행한다.
  </action>
  <acceptance_criteria>
    - `grep -c "'48170': '진주시'" src/components/invest/PredictionSection.tsx src/app/api/invest/prediction-commentary/route.ts src/components/presale/EnrichedPresaleCard.tsx src/app/api/admin/cardnews/data/route.ts` == 1 각각
    - `grep -c "'48890': '합천군'" src/components/invest/PredictionSection.tsx src/app/api/invest/prediction-commentary/route.ts src/components/presale/EnrichedPresaleCard.tsx src/app/api/admin/cardnews/data/route.ts` == 1 각각
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -iE "PredictionSection|prediction-commentary|EnrichedPresaleCard|cardnews/data" | head -20</automated>
  </verify>
  <done>4개 파일의 SGG_LABEL 객체 모두 경남 16개 항목이 추가되어 총 22개 키를 가지며 tsc 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: SGG_OPTIONS/REGION_OPTIONS 배열 3개 파일 라벨 추가</name>
  <files>src/components/admin/AdCreateForm.tsx, src/components/admin/AdEditForm.tsx, src/components/admin/cardnews/BuilderOptionsPanel.tsx</files>
  <read_first>src/components/admin/AdCreateForm.tsx, src/components/admin/AdEditForm.tsx, src/components/admin/cardnews/BuilderOptionsPanel.tsx</read_first>
  <action>
**1. `AdCreateForm.tsx`, `AdEditForm.tsx`** — 두 파일 모두 동일한 `const SGG_OPTIONS = [{ code: '48121', label: '창원시 의창구' }, ...]` 배열을 갖고 있다. 마지막 항목(`{ code: '48250', label: '김해시' },`) 다음에 interfaces 블록의 16개 `{ code, label }` 항목을 그대로 추가한다 (기존 항목은 "창원시 의창구" 형식이지만 신규 항목은 이미 독립 시/군이므로 "진주시" 그대로 — "창원시" 같은 접두사 불필요).

**2. `BuilderOptionsPanel.tsx`** — `const REGION_OPTIONS = [{ label: '창원 전체', sggCodes: [...] }, { label: '성산구', sggCodes: [...] }, ..., { label: '김해시', sggCodes: ['48250'] }]` 배열의 마지막 항목(`{ label: '김해시', sggCodes: ['48250'] },`) 다음에 interfaces 블록의 16개 `{ label, sggCodes: [...] }` 항목을 그대로 추가한다. 각 신규 시군구는 "전체" 그룹 없이 김해시와 동일하게 단독 옵션으로 추가한다 (창원 5구처럼 그룹핑할 하위 구가 없으므로).
  </action>
  <acceptance_criteria>
    - `grep -c "48170" src/components/admin/AdCreateForm.tsx src/components/admin/AdEditForm.tsx src/components/admin/cardnews/BuilderOptionsPanel.tsx` == 1 각각
    - `grep -c "48890" src/components/admin/AdCreateForm.tsx src/components/admin/AdEditForm.tsx src/components/admin/cardnews/BuilderOptionsPanel.tsx` == 1 각각
    - `grep -c "code: '48" src/components/admin/AdCreateForm.tsx` == 22
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -iE "AdCreateForm|AdEditForm|BuilderOptionsPanel" | head -20</automated>
  </verify>
  <done>3개 파일의 옵션 배열 모두 경남 16개 항목이 추가되어 어드민 광고 등록/수정 폼과 카드뉴스 빌더 지역 선택기에서 신규 시군구를 선택할 수 있고 tsc 에러가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| 어드민 폼(AdCreateForm/AdEditForm) 지역 선택 | 어드민 인증 사용자만 접근(requireAdmin), 정적 옵션 목록이라 인젝션 경로 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Information Disclosure | UI 라벨 노출 | accept | 지역명은 공개 정보, 민감정보 아님 |
</threat_model>

<verification>
```bash
npx tsc --noEmit --pretty false
npm run lint
```
</verification>

<success_criteria>
1. 7개 파일 모두 경남 16개 신규 시군구 라벨/옵션이 추가됨
2. 신규 지역 선택 시 어떤 UI에서도 원본 5자리 코드가 노출되지 않음
3. UI 레이아웃·구조 변경 없음 (재기획 동결 원칙 준수)
4. tsc/lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/33-db-1/33-05-SUMMARY.md` 생성.
</output>
