---
phase: 19-admin-ux
plan: "02"
type: execute
wave: 1
depends_on: ["19-00"]
files_modified:
  - src/app/admin/ads/page.tsx
  - src/app/admin/realtors/page.tsx
autonomous: true
requirements:
  - ADMIN-11

must_haves:
  truths:
    - "/admin/ads?status=pending 접속 시 검토 중인 광고 캠페인만 표시된다"
    - "/admin/ads?status=approved 접속 시 승인된 광고 캠페인만 표시된다"
    - "필터 없이 /admin/ads 접속 시 전체 상태 캠페인이 표시된다"
    - "/admin/realtors?q=홍길동 접속 시 이름 또는 회사명에 '홍길동'이 포함된 중개사만 표시된다"
    - "/admin/realtors?active=true 접속 시 활성 중개사만 표시된다"
    - "/admin/realtors?active=false 접속 시 비활성 중개사만 표시된다"
    - "광고/중개사 필터 폼이 URL GET 방식으로 동작한다"
  artifacts:
    - path: "src/app/admin/ads/page.tsx"
      provides: "광고 목록 — searchParams status 필터 (클라이언트 array filter)"
      contains: "await searchParams"
    - path: "src/app/admin/realtors/page.tsx"
      provides: "중개사 목록 — searchParams 검색(q) + 활성상태(active) 필터"
      contains: "await searchParams"
  key_links:
    - from: "src/app/admin/ads/page.tsx"
      to: "getAllAdCampaigns(adminClient)"
      via: "전체 조회 후 status 클라이언트 필터"
      pattern: "filter.*c.status"
    - from: "src/app/admin/realtors/page.tsx"
      to: "getAllRealtors(adminClient)"
      via: "전체 조회 후 q/active 클라이언트 필터"
      pattern: "filter.*r.name"
---

<objective>
광고 목록(/admin/ads)에 상태 필터를, 중개사 목록(/admin/realtors)에 이름/회사명 검색과 활성 상태 필터를 추가한다. 19-01과 files_modified가 겹치지 않아 Wave 1에서 병렬 실행 가능.

Purpose: 운영자가 광고 캠페인을 상태별로 필터링하고, 중개사를 이름/활성 상태로 검색할 수 있다.
Output: ads/page.tsx status 필터 추가, realtors/page.tsx q/active 필터 추가
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\PROJECT.md
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\19-admin-ux\19-00-SUMMARY.md
</context>

<interfaces>
<!-- getAllAdCampaigns 반환 타입 (ads/page.tsx에서 사용 중) -->
```typescript
// src/lib/data/ads.ts에서:
// getAllAdCampaigns(adminClient) → AdCampaign[]
// AdCampaign에는 .status: AdStatus 필드 포함

type AdStatus = 'draft' | 'pending' | 'approved' | 'ended' | 'rejected' | 'paused'

const STATUS_LABEL: Record<AdStatus, string> = {
  draft: '초안', pending: '검토 중', approved: '승인',
  ended: '종료', rejected: '거절', paused: '일시중지',
}
```

<!-- getAllRealtors 반환 타입 (realtors/page.tsx에서 사용 중) -->
```typescript
// src/lib/data/realtors.ts에서:
// getAllRealtors(adminClient) → Realtor[]
// Realtor에는 .name: string, .agency_name: string | null, .is_active: boolean 포함
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ads/page.tsx status 필터 추가</name>
  <files>
    src/app/admin/ads/page.tsx
  </files>
  <read_first>
    - src/app/admin/ads/page.tsx — 전체 파일 (STATUS_LABEL, STATUS_COLOR, getAllAdCampaigns 호출, 기존 테이블 JSX 구조)
    - src/lib/data/ads.ts — getAllAdCampaigns 시그니처 확인
  </read_first>
  <action>
함수 시그니처 변경:
```typescript
export default async function AdminAdsPage({
  searchParams,
}: {
  searchParams: Promise<{ status?: string }>
}) {
  const { status = '' } = await searchParams
```

데이터 조회 및 필터 (auth guard 이후) — 클라이언트 필터 방식 사용 (MVP 규모, 목록이 수백 건 미만):
```typescript
const adminClient = createSupabaseAdminClient()
const [allCampaigns, roiStats] = await Promise.all([
  getAllAdCampaigns(adminClient),
  getAdRoiStats(adminClient),
])

const campaigns = status
  ? allCampaigns.filter(c => c.status === status)
  : allCampaigns
```

JSX에 필터 폼 추가 (h1 + "새 광고 등록" 버튼 행 아래, ROI 테이블 위):
```tsx
<form method="get" style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap', alignItems: 'center' }}>
  <select name="status" defaultValue={status} className="input" style={{ minWidth: 130 }}>
    <option value="">상태 전체</option>
    {(Object.keys(STATUS_LABEL) as AdStatus[]).map(s => (
      <option key={s} value={s}>{STATUS_LABEL[s]}</option>
    ))}
  </select>
  <button type="submit" className="btn btn-sm btn-orange">필터</button>
  {status && (
    <a href="/admin/ads" className="btn btn-sm btn-secondary">초기화</a>
  )}
</form>
```

ROI 테이블은 `campaigns.length > 0` 조건이므로 필터 후 campaigns 배열 기준으로 자연스럽게 동작.

주의: `import Link from 'next/link'` — 19-00 Plan에서 header 제거 후에도 "+ 새 광고 등록" 버튼에서 Link를 사용하므로 import 유지 필요. 파일에 Link 사용처 확인 후 불필요하면 삭제.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    - ads/page.tsx: searchParams Promise 타입, status 클라이언트 필터 적용
    - 상태 필터 드롭다운 폼 추가 (STATUS_LABEL 기반 옵션 생성)
    - `npm run lint` PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: realtors/page.tsx 이름/활성상태 필터 추가</name>
  <files>
    src/app/admin/realtors/page.tsx
  </files>
  <read_first>
    - src/app/admin/realtors/page.tsx — 전체 파일 (getAllRealtors 호출, Realtor 타입, 기존 테이블 JSX 구조, header 구조)
    - src/lib/data/realtors.ts — getAllRealtors 반환 타입의 .name, .agency_name, .is_active 필드 확인
  </read_first>
  <action>
함수 시그니처 변경:
```typescript
export default async function AdminRealtorsPage({
  searchParams,
}: {
  searchParams: Promise<{ q?: string; active?: string }>
}) {
  const { q: rawQ = '', active = '' } = await searchParams
  const q = rawQ.trim().slice(0, 50)
```

데이터 조회 및 필터 (auth guard 이후) — 클라이언트 필터:
```typescript
const adminClient = createSupabaseAdminClient()
const allRealtors = await getAllRealtors(adminClient)

const realtors = allRealtors.filter(r => {
  if (q) {
    const nameMatch = r.name.toLowerCase().includes(q.toLowerCase())
    const agencyMatch = (r.agency_name ?? '').toLowerCase().includes(q.toLowerCase())
    if (!nameMatch && !agencyMatch) return false
  }
  if (active === 'true' && !r.is_active) return false
  if (active === 'false' && r.is_active) return false
  return true
})
```

JSX에 필터 폼 추가 (h1 + "새 중개사 등록" 버튼 행 아래, 테이블 위):
```tsx
<form method="get" style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap', alignItems: 'center' }}>
  <input
    type="search"
    name="q"
    defaultValue={q}
    placeholder="이름 / 회사명"
    className="input"
    style={{ minWidth: 180 }}
    maxLength={50}
  />
  <select name="active" defaultValue={active} className="input" style={{ minWidth: 110 }}>
    <option value="">상태 전체</option>
    <option value="true">활성</option>
    <option value="false">비활성</option>
  </select>
  <button type="submit" className="btn btn-sm btn-orange">검색</button>
  {(q || active) && (
    <a href="/admin/realtors" className="btn btn-sm btn-secondary">초기화</a>
  )}
</form>
```

필터 후 빈 목록 처리: 기존 `realtors.length === 0` 조건부 렌더링이 있다면 필터 결과가 0개일 때도 자연스럽게 처리됨. 없다면 추가:
```tsx
{realtors.length === 0 && (
  <div className="card" style={{ padding: 40, textAlign: 'center', font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
    {q || active ? '검색 결과가 없습니다.' : '등록된 중개사가 없습니다.'}
  </div>
)}
```

주의: `import Link from 'next/link'` — "새 중개사 등록" 버튼에서 사용하므로 유지 필요.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>
    - realtors/page.tsx: searchParams Promise 타입, q/active 클라이언트 필터 적용
    - 검색창 + 활성상태 드롭다운 필터 폼 추가
    - `npm run lint && npm run build` PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → 클라이언트 필터 | 사용자 입력이 배열 filter()에 사용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | ads/page.tsx status 파라미터 | accept | status 값이 STATUS_LABEL에 없으면 filter 결과 0개. 데이터 변경 없음 (조회 전용) |
| T-19-08 | Tampering | realtors/page.tsx q 파라미터 | mitigate | `.trim().slice(0, 50)` 최대 50자 + .toLowerCase().includes() 클라이언트 필터 (SQL 아님) |
| T-19-09 | Elevation of Privilege | ads/realtors 목록 | mitigate | layout auth guard (Wave 0) + 각 페이지 defense-in-depth auth guard 유지 |
</threat_model>

<verification>
1. `npm run lint && npm run build` — PASS
2. `/admin/ads?status=pending` → 검토 중 캠페인만 표시
3. `/admin/ads` → 전체 캠페인 표시
4. `/admin/realtors?q=홍` → 이름/회사명에 '홍' 포함된 중개사 표시
5. `/admin/realtors?active=true` → 활성 중개사만 표시
</verification>

<success_criteria>
- ads/page.tsx: searchParams status 필터, GET form 방식
- realtors/page.tsx: searchParams q/active 클라이언트 필터, GET form 방식
- `npm run lint && npm run build` PASS
- 19-01과 files_modified 겹침 없음 (Wave 1 병렬 실행 조건 충족)
</success_criteria>

<output>
완료 후 `.planning/phases/19-admin-ux/19-02-SUMMARY.md` 생성
</output>
