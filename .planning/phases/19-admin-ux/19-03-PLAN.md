---
phase: 19-admin-ux
plan: "03"
type: execute
wave: 2
depends_on: ["19-00", "19-01", "19-02"]
files_modified:
  - src/app/admin/ads/page.tsx
  - src/app/admin/members/page.tsx
  - src/app/admin/reports/page.tsx
  - src/app/admin/realtors/page.tsx
  - src/app/admin/ads/new/page.tsx
  - src/app/admin/ads/[id]/edit/page.tsx
  - src/app/admin/realtors/new/page.tsx
  - src/app/admin/realtors/[id]/edit/page.tsx
  - src/lib/auth/admin-actions.ts
  - src/lib/auth/ad-actions.ts
autonomous: true
requirements:
  - ADMIN-14

must_haves:
  truths:
    - "어드민 목록 페이지(ads/members/reports/realtors) 로드 시 DB auth.getUser() 1회만 호출된다 (layout 중복 제거)"
    - "?status=invalid 등 허용값 외 searchParam은 무시(빈값 처리)된다"
    - "DB 쿼리 오류 시 빈 목록 대신 에러 메시지가 표시된다"
    - "ads 목록에 status 필터 적용 시 DB 쿼리 WHERE 조건으로 처리된다 (in-memory filter 제거)"
    - "realtors 목록에 q/active 필터 적용 시 DB 쿼리 조건으로 처리된다"
    - "members/reports 목록에 페이지네이션이 적용된다 (limit 50, page 버튼)"
    - "Server Action(정지/승인/신고처리) 실행 후 사이드바 뱃지가 즉시 갱신된다"
    - "편집 페이지(ads/new, ads/edit, realtors/new, realtors/edit)에 어드민 사이드바가 표시된다"
    - "members 페이지 가입일/역할 컬럼 클릭으로 정렬 가능하다"
---

<objective>
Phase 19-00~02에서 구현된 어드민 레이아웃/필터를 기반으로 8개 에지케이스·에러 처리·고도화 이슈를 수정한다.

1. 중복 auth check 제거 (layout이 처리하므로 각 페이지 auth guard 불필요)
2. searchParams 화이트리스트 검증 (허용값 외 무시)
3. DB 에러 처리 (에러 시 명시적 메시지)
4. ads/realtors 인메모리 필터 → DB 레벨
5. members/reports 페이지네이션 (limit 50)
6. Server Action 후 revalidatePath('/admin', 'layout') 추가
7. 편집 페이지 standalone header 제거 (어드민 레이아웃 적용)
8. members 정렬 컬럼 (created_at/role)
</objective>

<tasks>

<task id="1">Wave 1A: ads/page.tsx — auth 제거 + 화이트리스트 + DB-level 필터 + 에러 처리</task>
<task id="2">Wave 1B: members/page.tsx — auth 제거 + 화이트리스트 + 페이지네이션 + 정렬 + 에러 처리</task>
<task id="3">Wave 1C: reports/page.tsx — auth 제거 + 화이트리스트 + 페이지네이션 + 에러 처리</task>
<task id="4">Wave 1D: realtors/page.tsx — auth 제거 + 화이트리스트 + DB-level 필터 + 에러 처리</task>
<task id="5">Wave 2A: Server Actions — revalidatePath('/admin', 'layout') 추가</task>
<task id="6">Wave 2B: 편집 페이지 4개 — auth 제거 + standalone header 제거</task>

</tasks>
