---
phase: 19-admin-ux
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/admin/layout.tsx
  - src/app/admin/page.tsx
  - src/components/admin/AdminSidebar.tsx
  - src/components/admin/AdminSidebarLinks.tsx
  - src/components/admin/AdminSidebarDrawer.tsx
  - src/app/globals.css
  - src/app/admin/status/page.tsx
  - src/app/admin/cardnews/page.tsx
  - src/app/admin/listing-prices/page.tsx
  - src/app/admin/redevelopment/page.tsx
  - src/app/admin/gps-requests/page.tsx
  - src/__tests__/admin-layout.test.ts
autonomous: true
requirements:
  - ADMIN-10
  - ADMIN-13

must_haves:
  truths:
    - "/admin URL 접속 시 /admin/status로 즉시 리다이렉트된다"
    - "비로그인 사용자가 /admin/* 접근 시 /login?next=/admin으로 리다이렉트된다"
    - "member 역할 사용자가 /admin/* 접근 시 / 로 리다이렉트된다"
    - "데스크톱에서 240px 고정 사이드바가 모든 어드민 페이지 왼쪽에 표시된다"
    - "사이드바에 9개 메뉴 항목이 텍스트만으로 표시된다"
    - "현재 경로와 일치하는 메뉴 항목이 굵게(font-weight 700) + 배경색으로 강조된다"
    - "768px 이하 모바일에서 사이드바가 숨겨지고 상단 60px 헤더에 햄버거 버튼이 나타난다"
    - "햄버거 버튼 클릭 시 240px drawer가 왼쪽에서 슬라이드인되며, 배경 클릭으로 닫힌다"
    - "기존 9개 어드민 페이지의 self-contained header 블록이 제거되고 레이아웃 사이드바로 대체된다"
  artifacts:
    - path: "src/app/admin/layout.tsx"
      provides: "공유 어드민 레이아웃 — auth guard + pending count 조회 + AdminSidebar 렌더"
      exports: ["default AdminLayout"]
    - path: "src/app/admin/page.tsx"
      provides: "/admin 루트 리다이렉트"
      exports: ["default AdminPage"]
    - path: "src/components/admin/AdminSidebar.tsx"
      provides: "RSC 사이드바 — 메뉴 구조 + buildNavItems 헬퍼 export"
      exports: ["AdminSidebar", "buildNavItems"]
    - path: "src/components/admin/AdminSidebarLinks.tsx"
      provides: "'use client' — usePathname active 상태 처리"
      exports: ["AdminSidebarLinks"]
    - path: "src/components/admin/AdminSidebarDrawer.tsx"
      provides: "'use client' — 모바일 overlay drawer"
      exports: ["AdminSidebarDrawer"]
    - path: "src/__tests__/admin-layout.test.ts"
      provides: "ADMIN-10, ADMIN-13 단위 테스트"
      contains: "requireAdminLayout, buildNavItems"
  key_links:
    - from: "src/app/admin/layout.tsx"
      to: "src/components/admin/AdminSidebar.tsx"
      via: "pendingCounts props 전달"
      pattern: "AdminSidebar.*pendingCounts"
    - from: "src/components/admin/AdminSidebar.tsx"
      to: "src/components/admin/AdminSidebarLinks.tsx"
      via: "buildNavItems(pendingCounts) → items props"
      pattern: "AdminSidebarLinks.*items"
    - from: "src/app/admin/layout.tsx"
      to: "src/components/admin/AdminSidebarDrawer.tsx"
      via: "pendingCounts props 전달"
      pattern: "AdminSidebarDrawer.*pendingCounts"
---

<objective>
공유 어드민 레이아웃(사이드바 네비게이션)을 구축하고, 기존 9개 어드민 페이지의 self-contained header 블록을 제거한다. 이 Plan이 완료되어야 Wave 1 (검색·필터 추가)이 의미 있다.

Purpose: 13개 어드민 기능이 URL 직접 입력 없이 사이드바로 접근 가능해진다. 공통 auth guard가 레이아웃 수준에서 동작한다.
Output: layout.tsx, page.tsx, AdminSidebar/Links/Drawer 컴포넌트, 기존 9개 페이지 header 제거, 테스트 파일
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\PROJECT.md
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\CLAUDE.md

<!-- 기존 admin 페이지 패턴 (auth guard + header 구조 확인용) -->
@C:\Users\jung\coding\bds\src\app\admin\ads\page.tsx
@C:\Users\jung\coding\bds\src\app\admin\members\page.tsx
@C:\Users\jung\coding\bds\src\app\admin\status\page.tsx
</context>

<interfaces>
<!-- layout.tsx에서 사용하는 기존 함수 시그니처 -->

From src/lib/supabase/server.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient>
```

From src/lib/supabase/admin.ts:
```typescript
export function createSupabaseAdminClient(): SupabaseClient
```

<!-- AdminSidebar → AdminSidebarLinks 인터페이스 (이 Plan이 정의) -->
```typescript
interface NavItem {
  label: string
  href: string
  pendingCount?: number
}

interface PendingCounts {
  reports: number
  ads: number
  gps: number
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 테스트 스캐폴드 + 공유 레이아웃 핵심 컴포넌트 (layout.tsx, page.tsx, AdminSidebar, AdminSidebarLinks)</name>
  <files>
    src/__tests__/admin-layout.test.ts,
    src/app/admin/layout.tsx,
    src/app/admin/page.tsx,
    src/components/admin/AdminSidebar.tsx,
    src/components/admin/AdminSidebarLinks.tsx
  </files>
  <behavior>
    - Test 1: requireAdminLayout — 비로그인(user=null) → redirect('/login?next=/admin')
    - Test 2: requireAdminLayout — role='member' → redirect('/')
    - Test 3: requireAdminLayout — role='admin' → 통과 (redirect 호출 없음)
    - Test 4: buildNavItems — pendingCount=150 → label '99+'
    - Test 5: buildNavItems — pendingCount=0 → 뱃지 숨김 (count > 0 조건)
    - Test 6: buildNavItems — 9개 항목 반환 확인
  </behavior>
  <read_first>
    - src/app/admin/ads/page.tsx — auth guard 패턴 (lines 38-51), header 구조 (lines 60-89)
    - src/app/admin/status/page.tsx — Promise.all 병렬 쿼리 패턴 (lines 53-97), header 구조 (lines 128-150)
    - src/app/admin/members/page.tsx — 전체 파일 (header 구조 확인)
    - src/lib/supabase/server.ts — createSupabaseServerClient 시그니처
    - src/lib/supabase/admin.ts — createSupabaseAdminClient 시그니처
    - src/__tests__/ 폴더의 기존 테스트 파일 1개 — vi.mock 패턴 확인
  </read_first>
  <action>
**1단계: 테스트 파일 먼저 작성 (RED)**

`src/__tests__/admin-layout.test.ts` 작성:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { redirect } from 'next/navigation'

vi.mock('next/navigation', () => ({ redirect: vi.fn() }))
vi.mock('@/lib/supabase/server', () => ({
  createSupabaseServerClient: vi.fn(),
}))
vi.mock('@/lib/supabase/admin', () => ({
  createSupabaseAdminClient: vi.fn(),
}))
```

테스트 6개를 위 behavior 기준으로 작성. `requireAdminLayout`은 layout.tsx에서 추출한 순수 함수로 테스트. `buildNavItems`는 AdminSidebar.tsx에서 export.

**2단계: 구현**

`src/app/admin/layout.tsx` (RSC, async):
- `export const revalidate = 0`
- `createSupabaseServerClient()` → auth.getUser() → user 없으면 `redirect('/login?next=/admin')`
- profiles.role 조회 → ['admin', 'superadmin'] 아니면 `redirect('/')`
- `createSupabaseAdminClient()` → Promise.all 3개 COUNT 쿼리:
  - `(adminClient as any).from('reports').select('*', { count: 'exact', head: true }).eq('status', 'pending')` — reports는 any 캐스트 필수 (기존 코드 패턴 준수)
  - `adminClient.from('ad_campaigns').select('*', { count: 'exact', head: true }).eq('status', 'pending')`
  - `adminClient.from('gps_verification_requests').select('*', { count: 'exact', head: true }).eq('status', 'pending')`
- pendingCounts = { reports: count ?? 0, ads: count ?? 0, gps: count ?? 0 }
- 반환 JSX:
```tsx
<div style={{ display: 'flex', minHeight: '100vh', background: 'var(--bg-canvas)' }}>
  <AdminSidebar pendingCounts={pendingCounts} />
  <AdminSidebarDrawer pendingCounts={pendingCounts} />
  <main style={{ flex: 1, minWidth: 0, fontFamily: 'var(--font-sans)' }}>
    {children}
  </main>
</div>
```

`src/app/admin/page.tsx`:
```typescript
import { redirect } from 'next/navigation'
export default function AdminPage() { redirect('/admin/status') }
```

`src/components/admin/AdminSidebar.tsx` (RSC, no 'use client'):
- `buildNavItems` 함수 export:
```typescript
export function buildNavItems(pendingCounts: PendingCounts): NavItem[] {
  return [
    { label: '대시보드', href: '/admin/status' },
    { label: '회원 관리', href: '/admin/members' },
    { label: '신고 관리', href: '/admin/reports', pendingCount: pendingCounts.reports },
    { label: '광고 관리', href: '/admin/ads', pendingCount: pendingCounts.ads },
    { label: '중개사 관리', href: '/admin/realtors' },
    { label: 'GPS 검증', href: '/admin/gps-requests', pendingCount: pendingCounts.gps },
    { label: '카드뉴스', href: '/admin/cardnews' },
    { label: '시세 입력', href: '/admin/listing-prices' },
    { label: '재개발 관리', href: '/admin/redevelopment' },
  ]
}
```
- `AdminSidebar` 컴포넌트: `className="admin-sidebar"` + inline style `{ width: 240, flexShrink: 0, background: '#fff', borderRight: '1px solid var(--line-default)', padding: '16px 8px', position: 'sticky', top: 0, height: '100vh', overflowY: 'auto' }`
- 상단 로고 영역: `<Link href="/" className="dj-logo"><span className="mark">단</span><span>단지온도</span></Link>` + `<span style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>어드민</span>` (기존 header의 dj-logo 패턴 그대로)
- `<AdminSidebarLinks items={buildNavItems(pendingCounts)} />`

`src/components/admin/AdminSidebarLinks.tsx` ('use client'):
- `usePathname()` → `pathname.startsWith(item.href)` active 판정
- active 스타일: `fontWeight: 700, color: 'var(--fg-pri)', background: 'var(--bg-surface-2)', borderRadius: 6`
- inactive 스타일: `fontWeight: 500, color: 'var(--fg-sec)', background: 'transparent'`
- 뱃지: `(item.pendingCount ?? 0) > 0` 조건 → `<span style={{ background: 'var(--fg-negative)', color: '#fff', minWidth: 20, height: 20, padding: '0 6px', borderRadius: 10, font: '700 11px/20px var(--font-sans)', textAlign: 'center' }}>{(item.pendingCount ?? 0) > 99 ? '99+' : item.pendingCount}</span>`
- AI 슬롭 금지: backdrop-blur, gradient, glow, 보라/인디고 없음
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/admin-layout.test.ts</automated>
  </verify>
  <done>
    - admin-layout.test.ts 6개 테스트 모두 PASS
    - layout.tsx 생성 완료 (revalidate=0, auth guard, Promise.all 3개 COUNT, AdminSidebar/Drawer 렌더)
    - AdminSidebar.tsx buildNavItems 9개 항목 export
    - AdminSidebarLinks.tsx 'use client' + usePathname active 스타일
  </done>
</task>

<task type="auto">
  <name>Task 2: AdminSidebarDrawer + globals.css @media + 기존 9개 페이지 header 제거</name>
  <files>
    src/components/admin/AdminSidebarDrawer.tsx,
    src/app/globals.css,
    src/app/admin/status/page.tsx,
    src/app/admin/cardnews/page.tsx,
    src/app/admin/listing-prices/page.tsx,
    src/app/admin/redevelopment/page.tsx,
    src/app/admin/gps-requests/page.tsx,
    src/app/admin/members/page.tsx,
    src/app/admin/reports/page.tsx,
    src/app/admin/ads/page.tsx,
    src/app/admin/realtors/page.tsx
  </files>
  <read_first>
    - src/app/globals.css — 기존 @media query 유무 확인, .card/.input/.btn 클래스 위치 확인 (파일 끝에 admin-specific 클래스 추가할 위치)
    - src/app/admin/status/page.tsx — header 블록 정확한 위치 (lines 127-150: `<div style={{ minHeight: '100vh'...}>` + `<header>` 블록, main으로 교체)
    - src/app/admin/members/page.tsx — lines 50-73 header 블록
    - src/app/admin/reports/page.tsx — lines 85-108 header 블록
    - src/app/admin/ads/page.tsx — lines 60-89 header 블록
    - src/app/admin/realtors/page.tsx — lines 30-60 header 블록
    - src/app/admin/gps-requests/page.tsx — header 없음 확인 (main 직접 사용), 수정 불필요
    - src/app/admin/cardnews/page.tsx — header 내 nav 포함 확인
    - src/app/admin/listing-prices/page.tsx — header 위치 확인
    - src/app/admin/redevelopment/page.tsx — header 위치 확인
  </read_first>
  <action>
**AdminSidebarDrawer.tsx ('use client') 작성:**
- `'use client'`
- `useState(false)` — open/close
- `usePathname()` — 링크 클릭 후 drawer 닫기 위한 경로 감지 (useEffect로 pathname 변경 시 setOpen(false))
- 모바일 상단바 (`className="admin-mobile-header"` — CSS로 제어):
  - 높이 60px, 흰 배경, borderBottom
  - 왼쪽: 햄버거 버튼 (`aria-label="메뉴 열기"`, `aria-expanded={open}`) — SVG path 사용 (이모지 금지):
    ```tsx
    <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
      <path d="M2 5h16M2 10h16M2 15h16" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/>
    </svg>
    ```
  - 오른쪽: `단지온도 어드민` 텍스트
- `open && <>backdrop + drawer</>`:
  - Backdrop: `position: fixed, inset: 0, background: rgba(0,0,0,0.4), zIndex: 200` onClick → setOpen(false)
  - Drawer: `position: fixed, top:0, left:0, bottom:0, width:240, background: #fff, borderRight: 1px solid var(--line-default), zIndex: 201, overflowY: auto, padding: 16px 8px`
  - Drawer 내부: AdminSidebarLinks items에 onClick={() => setOpen(false)} 전달 — AdminSidebarLinks에 `onItemClick?: () => void` prop 추가 필요. 각 Link에 `onClick={onItemClick}`
- buildNavItems는 AdminSidebar.tsx에서 import

**globals.css에 admin 레이아웃 CSS 추가 (파일 맨 끝에 추가):**
```css
/* Admin layout — sidebar responsive */
.admin-sidebar {
  display: flex;
  flex-direction: column;
}
.admin-mobile-header {
  display: none;
}
@media (max-width: 768px) {
  .admin-sidebar {
    display: none;
  }
  .admin-mobile-header {
    display: flex;
    align-items: center;
    padding: 0 16px;
    gap: 12px;
    height: 60px;
    background: #fff;
    border-bottom: 1px solid var(--line-default);
    position: sticky;
    top: 0;
    z-index: 100;
  }
}
```

**기존 9개 페이지 header 제거 — 각 파일 수정:**

공통 패턴: 각 페이지의 JSX return 최상위 `<div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>` wrapper와 내부 `<header>` 블록을 제거. 그 안의 콘텐츠 `<div>` 또는 `<main>`만 남긴다.

수정 후 각 페이지의 return은 바로 콘텐츠 래퍼로 시작:
```tsx
return (
  <div style={{ maxWidth: 1100, margin: '0 auto', padding: '28px 32px' }}>
    <h1 ...>페이지 제목</h1>
    ...
  </div>
)
```
또는 status 페이지처럼 `<main>`을 쓰는 경우 그대로 유지.

- `status/page.tsx`: lines 127-150 (`<div style={{ minHeight: '100vh'...}}>` + `<header>`) 제거, 마지막 `</div>` 닫는 태그 1개 제거. `<main style={{ maxWidth: 1100, margin: '0 auto', padding: '28px 32px' }}>` 직접 반환
- `members/page.tsx`: outer div + header (lines 50-73) 제거, 닫는 `</div>` 1개 제거
- `reports/page.tsx`: outer div + header (lines 85-108) 제거, 닫는 `</div>` 1개 제거
- `ads/page.tsx`: outer div (line 61-67) + header (lines 68-89) 제거, 닫는 `</div>` 1개 제거. `import Link` 제거 여부 확인 (다른 곳에서 사용하면 유지)
- `realtors/page.tsx`: outer div + header 제거
- `gps-requests/page.tsx`: header 없음 → 변경 불필요
- `cardnews/page.tsx`: header (nav 포함) 제거 — 사이드바가 nav 대체
- `listing-prices/page.tsx`: outer div + header 제거
- `redevelopment/page.tsx`: outer div + header 제거

각 페이지에서 `import Link from 'next/link'`가 header 제거 후 더 이상 사용되지 않는 경우 import 삭제 (lint 오류 방지).
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>
    - AdminSidebarDrawer.tsx 작성 완료 (햄버거 SVG, backdrop, 슬라이드인 drawer)
    - globals.css에 .admin-sidebar + .admin-mobile-header @media 추가
    - 기존 9개 어드민 페이지에서 self-contained header 블록 제거 완료 (gps-requests 제외)
    - `npm run lint && npm run build` 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → admin layout | 모든 /admin/* 요청이 layout.tsx를 통과 |
| layout.tsx → Supabase | 서버에서만 auth + role 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | layout.tsx auth guard | mitigate | requireAdminLayout: auth.getUser() + profiles.role in ['admin','superadmin'] 서버에서 검증. 미통과 시 redirect |
| T-19-02 | Elevation of Privilege | 개별 admin pages | mitigate | D-05: 기존 각 페이지 auth guard 유지 (defense in depth) — layout 우회 시에도 보호 |
| T-19-03 | Spoofing | 모바일 drawer open/close | accept | UI 상태만 변경, 데이터 접근 없음. client state이므로 서버 영향 없음 |
</threat_model>

<verification>
1. `npm run test -- --run src/__tests__/admin-layout.test.ts` — 6개 테스트 PASS
2. `npm run lint && npm run build` — 오류 없음
3. `/admin` 접속 → `/admin/status` 리다이렉트 확인
4. 사이드바 9개 메뉴 항목이 텍스트만으로 표시됨
5. 현재 경로 메뉴 항목이 bold + 배경색으로 active 표시
6. 768px 이하에서 사이드바 숨겨지고 햄버거 버튼 표시
</verification>

<success_criteria>
- layout.tsx 생성: revalidate=0 + auth guard + 3개 COUNT 병렬 조회 + AdminSidebar + AdminSidebarDrawer 렌더
- AdminSidebar(RSC) + AdminSidebarLinks('use client') + AdminSidebarDrawer('use client') 모두 생성
- /admin/page.tsx → redirect('/admin/status')
- 9개 기존 페이지 header 블록 제거 완료 (gps-requests 제외)
- globals.css @media 768px 규칙 추가
- admin-layout.test.ts 6개 테스트 PASS
- `npm run lint && npm run build` PASS
</success_criteria>

<output>
완료 후 `.planning/phases/19-admin-ux/19-00-SUMMARY.md` 생성
</output>
