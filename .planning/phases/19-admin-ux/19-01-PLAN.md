---
phase: 19-admin-ux
plan: "01"
type: execute
wave: 1
depends_on: ["19-00"]
files_modified:
  - src/app/admin/members/page.tsx
  - src/app/admin/reports/page.tsx
  - src/__tests__/admin-members-filter.test.ts
autonomous: true
requirements:
  - ADMIN-11
  - ADMIN-12

must_haves:
  truths:
    - "/admin/members?q=홍길동 접속 시 닉네임 또는 카페닉네임에 '홍길동'이 포함된 회원만 표시된다"
    - "/admin/members?role=admin 접속 시 admin 역할 회원만 표시된다"
    - "/admin/members?status=suspended 접속 시 정지된 회원만 표시된다"
    - "/admin/reports?status=accepted 접속 시 처리 완료된 신고만 표시된다"
    - "/admin/reports?target_type=review 접속 시 review 대상 신고만 표시된다"
    - "필터 없이 /admin/reports 접속 시 전체 신고(pending+accepted+rejected) 모두 표시된다"
    - "사이드바 신고 관리 메뉴에 pending 신고 건수 뱃지가 표시된다 (19-00에서 layout이 조회)"
    - "검색/필터 폼이 URL GET 방식으로 동작하여 결과 URL 공유가 가능하다"
  artifacts:
    - path: "src/app/admin/members/page.tsx"
      provides: "회원 목록 — searchParams 필터 (q, role, status)"
      contains: "await searchParams"
    - path: "src/app/admin/reports/page.tsx"
      provides: "신고 목록 — searchParams 필터 (status, target_type) + 전체 조회"
      contains: "await searchParams"
    - path: "src/__tests__/admin-members-filter.test.ts"
      provides: "ADMIN-11 단위 테스트"
      contains: "searchParams filter"
  key_links:
    - from: "src/app/admin/members/page.tsx"
      to: "Supabase profiles 테이블"
      via: "adminClient.from('profiles').or('nickname.ilike...').eq('role',...)"
      pattern: "ilike.*nickname"
    - from: "src/app/admin/reports/page.tsx"
      to: "Supabase reports 테이블"
      via: "(adminClient as any).from('reports').eq('status',...).eq('target_type',...)"
      pattern: "eq.*status.*target_type"
---

<objective>
회원 목록(/admin/members)과 신고 목록(/admin/reports)에 URL searchParams 기반 검색·필터를 추가한다. Wave 0에서 공유 레이아웃이 이미 사이드바의 뱃지 카운트(ADMIN-12)를 제공하므로 이 Plan은 목록 페이지 필터(ADMIN-11)에 집중한다.

Purpose: 운영자가 회원/신고 목록을 검색·필터링하여 빠르게 원하는 항목을 찾을 수 있다.
Output: members/page.tsx searchParams 필터 추가, reports/page.tsx 전체 조회 + 필터 추가, 테스트 파일
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\PROJECT.md
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\19-admin-ux\19-00-SUMMARY.md
</context>

<interfaces>
<!-- 기존 members/page.tsx 쿼리 패턴 (수정 전) -->
```typescript
// 현재 (필터 없음):
const { data: members } = await adminClient
  .from('profiles')
  .select('id, nickname, cafe_nickname, role, created_at, suspended_at, deleted_at, terms_agreed_at')
  .order('created_at', { ascending: false })

// 수정 후 (searchParams 필터 적용):
const params = await searchParams
const q = typeof params.q === 'string' ? params.q.trim().slice(0, 50) : ''
const role = typeof params.role === 'string' ? params.role : ''
const status = typeof params.status === 'string' ? params.status : ''
```

<!-- 기존 reports/page.tsx 쿼리 패턴 (수정 전 — status 필터 없음) -->
```typescript
// 현재 (status 필터 없음, 전체 조회):
const { data: reports } = await (adminClient as any)
  .from('reports')
  .select('id, target_type, target_id, reason, status, created_at')
  .order('status', { ascending: true })
  .order('created_at', { ascending: false })
```

<!-- MemberRow 타입 (members/page.tsx에 이미 정의됨) -->
```typescript
interface MemberRow {
  id: string
  nickname: string | null
  cafe_nickname: string | null
  role: string | null
  created_at: string
  suspended_at: string | null
  deleted_at: string | null
  terms_agreed_at: string | null
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 테스트 스캐폴드 + members/page.tsx searchParams 필터 추가</name>
  <files>
    src/__tests__/admin-members-filter.test.ts,
    src/app/admin/members/page.tsx
  </files>
  <behavior>
    - Test 1: q='홍길동' → Supabase query에 `.or('nickname.ilike.%홍길동%,cafe_nickname.ilike.%홍길동%')` 포함
    - Test 2: role='admin' → query에 `.eq('role', 'admin')` 포함
    - Test 3: status='active' → query에 `.is('suspended_at', null).is('deleted_at', null)` 포함
    - Test 4: status='suspended' → query에 `.not('suspended_at', 'is', null)` 포함
    - Test 5: status='deleted' → query에 `.not('deleted_at', 'is', null)` 포함
    - Test 6: q가 빈 문자열 → or() 조건 추가 안 됨 (불필요한 필터 방지)
  </behavior>
  <read_first>
    - src/app/admin/members/page.tsx — 전체 파일 (현재 쿼리 구조, MemberRow 타입, 기존 테이블 JSX)
    - src/__tests__/admin-layout.test.ts — vi.mock 패턴, 테스트 구조 참조
  </read_first>
  <action>
**1단계: 테스트 파일 먼저 작성 (RED)**

`src/__tests__/admin-members-filter.test.ts`:
- buildMembersQuery(params) 순수 함수를 테스트. 이 함수는 members/page.tsx에서 export하거나 별도 유틸로 분리.
- vi.mock으로 Supabase 클라이언트 모킹, 체인 호출(from→select→order→or→eq 등)을 추적.

**2단계: members/page.tsx 수정**

함수 시그니처 변경:
```typescript
export default async function AdminMembersPage({
  searchParams,
}: {
  searchParams: Promise<{ q?: string; role?: string; status?: string }>
}) {
  const { q: rawQ = '', role = '', status = '' } = await searchParams
  const q = rawQ.trim().slice(0, 50)  // 최대 50자 제한 (ilike injection 방어)
```

쿼리 빌드 (auth guard 이후):
```typescript
const adminClient = createSupabaseAdminClient()
let query = adminClient
  .from('profiles')
  .select('id, nickname, cafe_nickname, role, created_at, suspended_at, deleted_at, terms_agreed_at')

if (q) {
  query = query.or(`nickname.ilike.%${q}%,cafe_nickname.ilike.%${q}%`)
}
if (role) {
  query = query.eq('role', role)
}
if (status === 'active') {
  query = query.is('suspended_at', null).is('deleted_at', null)
} else if (status === 'suspended') {
  query = query.not('suspended_at', 'is', null)
} else if (status === 'deleted') {
  query = query.not('deleted_at', 'is', null)
}

const { data: members } = await query.order('created_at', { ascending: false })
```

JSX에 필터 폼 추가 (h1 바로 아래, 테이블 위):
```tsx
<form method="get" style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap', alignItems: 'center' }}>
  <input
    type="search"
    name="q"
    defaultValue={q}
    placeholder="닉네임 / 카페닉네임"
    className="input"
    style={{ minWidth: 180 }}
    maxLength={50}
  />
  <select name="role" defaultValue={role} className="input" style={{ minWidth: 100 }}>
    <option value="">역할 전체</option>
    <option value="admin">admin</option>
    <option value="member">member</option>
  </select>
  <select name="status" defaultValue={status} className="input" style={{ minWidth: 100 }}>
    <option value="">상태 전체</option>
    <option value="active">활성</option>
    <option value="suspended">정지</option>
    <option value="deleted">탈퇴</option>
  </select>
  <button type="submit" className="btn btn-sm btn-orange">검색</button>
</form>
```

참고: `defaultValue`를 사용해야 RSC에서 현재 필터 값이 폼에 반영된다. `value` 대신 `defaultValue` 사용 (controlled component가 아님).
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/admin-members-filter.test.ts</automated>
  </verify>
  <done>
    - admin-members-filter.test.ts 6개 테스트 PASS
    - members/page.tsx: searchParams Promise 타입, q/role/status 필터 쿼리 적용
    - 필터 폼 (검색창 + 역할 드롭다운 + 상태 드롭다운 + 검색 버튼) JSX 추가
  </done>
</task>

<task type="auto">
  <name>Task 2: reports/page.tsx searchParams 필터 추가 (전체 조회 + status/target_type 필터)</name>
  <files>
    src/app/admin/reports/page.tsx
  </files>
  <read_first>
    - src/app/admin/reports/page.tsx — 전체 파일 (현재 쿼리 구조, ReportRow 타입, STATUS_LABEL/STATUS_COLOR 맵, 기존 테이블 JSX)
  </read_first>
  <action>
함수 시그니처 변경:
```typescript
export default async function AdminReportsPage({
  searchParams,
}: {
  searchParams: Promise<{ status?: string; target_type?: string }>
}) {
  const { status = '', target_type = '' } = await searchParams
```

쿼리 수정 (auth guard 이후) — 기존 `.eq('status', 'pending')` 고정 제거, 조건부로 변경:
```typescript
const adminClient = createSupabaseAdminClient()
// eslint-disable-next-line @typescript-eslint/no-explicit-any
let query = (adminClient as any)
  .from('reports')
  .select('id, target_type, target_id, reason, status, created_at')

if (status) {
  query = query.eq('status', status)
}
if (target_type) {
  query = query.eq('target_type', target_type)
}

const { data: reports } = await query
  .order('status', { ascending: true })
  .order('created_at', { ascending: false })
```

JSX에 필터 폼 추가 (h1 바로 아래, 테이블 위):
```tsx
<form method="get" style={{ display: 'flex', gap: 8, marginBottom: 16, flexWrap: 'wrap', alignItems: 'center' }}>
  <select name="status" defaultValue={status} className="input" style={{ minWidth: 120 }}>
    <option value="">상태 전체</option>
    <option value="pending">대기</option>
    <option value="accepted">처리 완료</option>
    <option value="rejected">기각</option>
  </select>
  <select name="target_type" defaultValue={target_type} className="input" style={{ minWidth: 130 }}>
    <option value="">유형 전체</option>
    <option value="review">후기</option>
    <option value="user">회원</option>
    <option value="ad">광고</option>
    <option value="comment">댓글</option>
  </select>
  <button type="submit" className="btn btn-sm btn-orange">필터</button>
  {(status || target_type) && (
    <a href="/admin/reports" className="btn btn-sm btn-secondary">초기화</a>
  )}
</form>
```

주의: reports 테이블은 기존 코드에서 `(adminClient as any)` 캐스트를 사용 중. 동일 패턴 유지. eslint-disable 주석도 유지.
  </action>
  <verify>
    <automated>npm run lint && npm run test -- --run src/__tests__/admin-members-filter.test.ts</automated>
  </verify>
  <done>
    - reports/page.tsx: searchParams Promise 타입, status/target_type 조건부 필터
    - 기존 `.eq('status', 'pending')` 고정 제거 → 필터 없으면 전체 신고 표시
    - 필터 폼 (상태 + 유형 드롭다운 + 필터/초기화 버튼) 추가
    - `npm run lint` PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → Supabase query | 사용자 입력이 쿼리 조건에 반영됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | members/page.tsx q 파라미터 | mitigate | `.trim().slice(0, 50)` 최대 50자 + Supabase PostgREST 파라미터화 쿼리 (SQL injection 아님) |
| T-19-05 | Tampering | reports/page.tsx status 파라미터 | accept | 알 수 없는 status 값 → Supabase에서 매칭 행 0개 반환. 데이터 노출 없음 |
| T-19-06 | Elevation of Privilege | members/reports 목록 | mitigate | layout의 auth guard (Wave 0) + 각 페이지 defense-in-depth auth guard 유지 |
</threat_model>

<verification>
1. `npm run test -- --run src/__tests__/admin-members-filter.test.ts` — PASS
2. `npm run lint` — PASS
3. `/admin/members?q=test` → 닉네임/카페닉네임 필터 동작
4. `/admin/reports` → 전체 신고 목록 표시 (pending만 아님)
5. `/admin/reports?status=pending` → pending 신고만 표시
</verification>

<success_criteria>
- admin-members-filter.test.ts 6개 테스트 PASS
- members/page.tsx: q/role/status URL searchParams 필터 동작
- reports/page.tsx: status/target_type URL searchParams 필터, 기본값=전체 조회
- 필터 폼이 GET form 방식 (클라이언트 JS 불필요)
- `npm run lint` PASS
</success_criteria>

<output>
완료 후 `.planning/phases/19-admin-ux/19-01-SUMMARY.md` 생성
</output>
