---
phase: 14-dong-cluster-mid-level
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/map/DongClusterChip.tsx
autonomous: true
requirements:
  - MAP-10
  - MAP-11
  - MAP-12

must_haves:
  truths:
    - "DongChip 타입이 gu, dong, lat, lng, count, maxPrice, memberLats, memberLngs 필드를 모두 포함한다"
    - "dongChips 계산 함수가 complexes.dong 기반 groupBy로 동별 칩 배열을 반환한다"
    - "DongClusterChip 컴포넌트가 GuChip(구 모드)과 DongChip(동 모드) 두 인터페이스를 모두 수용한다"
    - "동 칩에 동 이름·단지 수·최고 실거래가가 표시된다"
    - "동 칩 클릭 시 map.setLevel(6) + setCenter(동 중심)가 호출된다"
  artifacts:
    - path: "src/components/map/DongClusterChip.tsx"
      provides: "GuChip 타입 + DongChip 타입 + DongClusterChip 컴포넌트(두 모드 지원)"
      exports: ["GuChip", "DongChip", "DongClusterChip"]
  key_links:
    - from: "DongClusterChip"
      to: "map.setLevel(6)"
      via: "동 모드 handleClick"
      pattern: "setLevel\\(6\\)"
    - from: "DongChip"
      to: "ComplexMapItem.dong"
      via: "KakaoMap.tsx dongChips useMemo"
      pattern: "dong.*groupBy|groupBy.*dong"
---

<objective>
DongChip 타입 정의, dongChips 계산 로직 설계, DongClusterChip 컴포넌트에 동 모드(level 7~8용) 추가.

Purpose: 현재 GuChip(구 단위)만 지원하는 DongClusterChip 컴포넌트를 DongChip(동 단위) 모드까지 수용하게 확장하여, KakaoMap이 level 7~8에서 동 칩을 렌더링할 수 있는 기반을 마련한다.

Output:
- DongChip 인터페이스 (DongClusterChip.tsx에 추가)
- DongClusterChip 컴포넌트 — mode='gu' | 'dong' 분기
- dongChips 계산 함수 (KakaoMap.tsx의 useMemo에서 호출할 수 있는 순수 계산 패턴 문서화)
</objective>

<execution_context>
@C:/Users/jung/coding/bds/.planning/phases/14-dong-cluster-mid-level/../../ROADMAP.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/ROADMAP.md
@C:/Users/jung/coding/bds/src/lib/data/complexes-map.ts
@C:/Users/jung/coding/bds/src/components/map/DongClusterChip.tsx
@C:/Users/jung/coding/bds/src/components/map/KakaoMap.tsx

<interfaces>
<!-- 현재 ComplexMapItem 필드 (complexes-map.ts에서 추출) -->
```typescript
export interface ComplexMapItem {
  id:                  string
  canonical_name:      string
  lat:                 number
  lng:                 number
  sgg_code:            string
  avg_sale_per_pyeong: number | null
  view_count:          number
  price_change_30d:    number | null
  tx_count_30d:        number
  status:              string          // 'pre_sale' 값 있음
  built_year:          number | null
  household_count:     number | null
  hagwon_grade:        string | null
  is_new_record_30d:   boolean
  si:                  string | null
  gu:                  string | null
  dong:                string | null   // Phase 14에서 groupBy 키로 사용
  recent_price:        number | null   // 만원 단위 — maxPrice 소스
  recent_date:         string | null
  recent_area_m2:      number | null
}
```

<!-- 현재 GuChip 타입 (DongClusterChip.tsx에서 추출) -->
```typescript
export interface GuChip {
  gu:         string
  lat:        number
  lng:        number
  maxPrice:   number | null
  memberLats: number[]
  memberLngs: number[]
}
```

<!-- 현재 DongClusterChip 컴포넌트 시그니처 -->
```typescript
export const DongClusterChip = memo(function DongClusterChip({
  gu, lat, lng, maxPrice,
}: GuChip)
// useMap('DongClusterChip') 사용
// 클릭 시 map.setLevel(7) — 동 모드에서는 setLevel(6)으로 변경
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DongChip 타입 추가 + DongClusterChip 동 모드 확장</name>
  <files>src/components/map/DongClusterChip.tsx</files>
  <read_first>
    - src/components/map/DongClusterChip.tsx (수정 대상 — 전체 읽기)
    - src/lib/data/complexes-map.ts (ComplexMapItem 인터페이스 확인)
  </read_first>
  <action>
DongClusterChip.tsx를 다음과 같이 확장한다.

**1. DongChip 인터페이스 추가 (GuChip 아래에)**

```typescript
export interface DongChip {
  gu:         string | null   // 그룹 키: c.gu ?? c.si ?? '기타'
  dong:       string          // 표시명: c.dong ?? '기타'
  lat:        number          // 그룹 내 단지 lat 평균
  lng:        number          // 그룹 내 단지 lng 평균
  count:      number          // 그룹 내 단지 수
  maxPrice:   number | null   // recent_price 최댓값 (만원 단위)
  memberLats: number[]
  memberLngs: number[]
}
```

**2. formatPrice 함수는 그대로 유지** (GuChip, DongChip 양쪽에서 사용).

**3. DongClusterChip 컴포넌트를 union props로 교체**

기존: `function DongClusterChip({ gu, lat, lng, maxPrice }: GuChip)`
신규: 두 모드를 union으로 수용

```typescript
type Props =
  | (GuChip  & { mode?: 'gu' })
  | (DongChip & { mode: 'dong' })
```

컴포넌트 내부 분기:
- `mode === 'dong'`일 때:
  - `label` = `${props.dong}` (동 이름)
  - `subLabel` = `${props.count}개 단지`
  - `handleClick` → `map.setLevel(6)` + `map.setCenter(new window.kakao.maps.LatLng(props.lat, props.lng))`
  - `aria-label` = `${props.dong} ${props.count}개 단지, 클릭하면 해당 동으로 확대됩니다`
- `mode`가 없거나 `'gu'`일 때 (기존 GuChip 동작):
  - `label` = `${props.gu}`
  - `subLabel` 없음
  - `handleClick` → 기존 `map.setLevel(7)` 유지 (GuChip 하위 호환 보존)
  - `aria-label` = `${props.gu} 클릭하면 해당 지역으로 확대됩니다`

**4. 동 칩 스타일** (AI 슬롭 금지 — backdrop-blur/gradient 금지):
- 기존 GuChip 스타일을 그대로 재사용
- 동 모드에서 subLabel(단지 수)은 기존 maxPrice 스타일(`color: '#F97316'`) 위에 추가로 표시
  - subLabel 먼저, maxPrice 그 다음 순으로 출력
  - subLabel 스타일: `fontSize: 11, fontWeight: 500, color: '#6B7280'`

**5. 내보내기**: `GuChip`, `DongChip` 둘 다 named export 유지.

**이모지 금지** — 텍스트만 사용.

**기존 GuChip 동작 하위 호환 필수**: KakaoMap.tsx의 `<DongClusterChip key={chip.gu} {...chip} />` 호출은 `mode` prop 없이 사용하므로, `mode` 미전달 시 기존 GuChip 동작이 그대로 유지되어야 한다.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/jung/coding/bds && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "DongClusterChip\|DongChip\|GuChip" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface DongChip" src/components/map/DongClusterChip.tsx` → 1건 이상 출력
    - `grep -n "mode.*dong\|dong.*mode" src/components/map/DongClusterChip.tsx` → 1건 이상 출력
    - `grep -n "setLevel(6)" src/components/map/DongClusterChip.tsx` → 1건 이상 출력
    - `grep -n "setLevel(7)" src/components/map/DongClusterChip.tsx` → 1건 이상 출력 (GuChip 하위 호환)
    - `grep -n "count.*개 단지\|개 단지.*count" src/components/map/DongClusterChip.tsx` → 1건 이상 출력
    - TypeScript 타입 오류 0건 (DongClusterChip.tsx 관련)
  </acceptance_criteria>
  <done>DongChip 타입이 export되고, DongClusterChip이 mode='dong' 시 동 이름+단지 수+최고가를 표시하며 level 6으로 드릴다운한다. GuChip 동작 하위 호환 유지.</done>
</task>

<task type="auto">
  <name>Task 2: dongChips 계산 헬퍼 함수 작성</name>
  <files>src/components/map/DongClusterChip.tsx</files>
  <read_first>
    - src/components/map/DongClusterChip.tsx (Task 1 완료 후 파일 상태)
    - src/lib/data/complexes-map.ts (ComplexMapItem 타입 임포트 확인)
  </read_first>
  <action>
DongClusterChip.tsx 하단에 `computeDongChips` 순수 함수를 추가하고 named export한다.

```typescript
import type { ComplexMapItem } from '@/lib/data/complexes-map'

/**
 * complexes 배열을 동(dong) 단위로 그룹화하여 DongChip[] 반환.
 * groupBy key = `${gu ?? si ?? '기타'}_${dong ?? '기타'}`
 * 중심 좌표 = 그룹 내 단지 lat/lng 평균
 * maxPrice = recent_price 최댓값 (null인 단지는 제외)
 */
export function computeDongChips(complexes: ComplexMapItem[]): DongChip[] {
  type AccEntry = {
    gu:       string | null
    dong:     string
    totalLat: number
    totalLng: number
    count:    number
    maxPrice: number | null
    lats:     number[]
    lngs:     number[]
  }

  const dongMap = new Map<string, AccEntry>()

  for (const c of complexes) {
    const guKey  = c.gu ?? c.si ?? '기타'
    const dongKey = c.dong ?? '기타'
    const key    = `${guKey}_${dongKey}`

    const entry = dongMap.get(key)
    if (!entry) {
      dongMap.set(key, {
        gu:       c.gu,
        dong:     dongKey,
        totalLat: c.lat,
        totalLng: c.lng,
        count:    1,
        maxPrice: c.recent_price,
        lats:     [c.lat],
        lngs:     [c.lng],
      })
    } else {
      entry.totalLat += c.lat
      entry.totalLng += c.lng
      entry.count    += 1
      entry.lats.push(c.lat)
      entry.lngs.push(c.lng)
      if (c.recent_price !== null) {
        entry.maxPrice = entry.maxPrice !== null
          ? Math.max(entry.maxPrice, c.recent_price)
          : c.recent_price
      }
    }
  }

  return Array.from(dongMap.values()).map(e => ({
    gu:         e.gu,
    dong:       e.dong,
    lat:        e.totalLat / e.count,
    lng:        e.totalLng / e.count,
    count:      e.count,
    maxPrice:   e.maxPrice,
    memberLats: e.lats,
    memberLngs: e.lngs,
  }))
}
```

**임포트 주의사항**:
- `ComplexMapItem` 임포트는 타입 임포트 `import type`을 사용한다 (`import type { ComplexMapItem } from '@/lib/data/complexes-map'`).
- 기존 임포트(`memo`, `CustomOverlayMap`, `useMap`)는 그대로 유지.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/jung/coding/bds && grep -n "computeDongChips\|export function computeDongChips" src/components/map/DongClusterChip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function computeDongChips" src/components/map/DongClusterChip.tsx` → 1건 출력
    - `grep -n "guKey.*si.*기타\|기타.*guKey" src/components/map/DongClusterChip.tsx` → 1건 이상 출력
    - `grep -n "import type.*ComplexMapItem" src/components/map/DongClusterChip.tsx` → 1건 출력
    - `grep -c "dongKey.*dong.*기타\|dong.*기타" src/components/map/DongClusterChip.tsx` → 1 이상
    - TypeScript 빌드: `npx tsc --noEmit` 오류 0건
  </acceptance_criteria>
  <done>computeDongChips 함수가 export되고, 동별 groupBy 계산(key=`${gu}_${dong}`, 평균 좌표, maxPrice)이 올바르게 동작한다. KakaoMap.tsx의 useMemo에서 즉시 호출 가능한 상태.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| complexes props → DongClusterChip | 서버에서 RSC가 조회한 데이터가 클라이언트 컴포넌트 props로 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | computeDongChips groupBy 키 | accept | 키는 `gu/dong` DB 값 기반이며 UI 표시용 — 인증/권한 없음, 저위험 |
| T-14-02 | Information Disclosure | DongChip.maxPrice | accept | 실거래가는 공개 데이터(국토부), 민감 정보 없음 |
| T-14-03 | Denial of Service | complexes 배열 크기 | accept | RSC에서 페이지네이션으로 제한 (complexes-map.ts PAGE=1000), 클라이언트 계산 O(n) |
</threat_model>

<verification>
Phase 14 Plan 01 완료 기준:

1. `src/components/map/DongClusterChip.tsx`에 `DongChip` 인터페이스가 export됨
2. `computeDongChips` 함수가 export됨
3. `DongClusterChip` 컴포넌트가 `mode='dong'` props를 수용하며 level 6 드릴다운 동작
4. `mode` 미전달 시 기존 GuChip 동작(`setLevel(7)`) 유지
5. TypeScript 타입 오류 0건
6. 이모지 없음, AI 슬롭 없음 (backdrop-blur/gradient 없음)
</verification>

<success_criteria>
- DongChip 타입이 GuChip와 별도로 named export됨
- computeDongChips(complexes) 호출 시 동별 DongChip[] 반환 — key=`${gu}_${dong}`, 평균 좌표, maxPrice=recent_price 최댓값
- DongClusterChip에 mode='dong' 전달 시: 동 이름 + "N개 단지" + 최고가 3줄 표시
- DongClusterChip에 mode='dong' 전달 시: 클릭 → map.setLevel(6) + setCenter(동 중심)
- mode 없이 GuChip props 전달 시: 기존 동작 완전 보존
</success_criteria>

<output>
완료 후 `.planning/phases/14-dong-cluster-mid-level/14-01-SUMMARY.md` 생성.
</output>
