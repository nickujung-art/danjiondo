---
phase: 40-changbuletter-prereq
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260731000001_complex_rankings_price_change.sql
  - src/lib/data/rankings.ts
  - src/__tests__/rankings.test.ts
  - src/__tests__/rankings-price-change.test.ts
  - scripts/measure-rankings.ts
autonomous: true
requirements: [CBL-0-5]

must_haves:
  truths:
    - "complex_rankings_rank_type_check가 price_change를 허용한다"
    - "rankings 크론 1회 실행 후 rank_type='price_change' 행이 실재한다"
    - "등락률 계산이 cancel_date IS NULL AND superseded_by IS NULL을 적용한다 — 코드와 테스트 양쪽에서"
    - "price_change 1위 행의 metadata에 지역명이 담겨 창부레터가 hotArea로 바로 쓸 수 있다"
    - "rankings 크론 실행 시간 증가분이 실측 숫자로 기록된다"
    - "기존 4종 aggregator의 산출 결과가 바뀌지 않는다"
    - "결과 행 수가 조회 상한에 닿으면 조용히 잘리지 않고 truncated 플래그로 드러난다"
  artifacts:
    - path: "supabase/migrations/20260731000001_complex_rankings_price_change.sql"
      provides: "rank_type CHECK 제약 확장 (4종 → 5종)"
      contains: "price_change"
    - path: "src/lib/data/rankings.ts"
      provides: "aggregatePriceChange + RankType 확장 + 소요시간 계측"
      exports: ["computeRankings", "getRankingsByType", "RankType"]
    - path: "src/__tests__/rankings-price-change.test.ts"
      provides: "취소·정정 필터 / 최소건수 임계 / metadata 지역명 / 정렬 / 절단 감지 단언"
      contains: "superseded_by"
  key_links:
    - from: "src/lib/data/rankings.ts"
      to: "public.transactions"
      via: "2개 창(현재 30일 / 직전 30일) 조회"
      pattern: "superseded_by"
    - from: "src/lib/data/rankings.ts"
      to: "public.complex_rankings"
      via: "upsert onConflict 'rank_type,complex_id,window_days'"
      pattern: "onConflict"
---

<objective>
0-5. `complex_rankings`에 `rank_type='price_change'`를 신설하고 **기존 랭킹 크론**에 aggregator 하나를 얹는다.
창부레터 홈 히어로의 `riseRank`·`avgRise`·`hotArea`와 `volumeRank`의 변동률이 전부 여기에 의존한다.

Purpose:
- ADR-005 §1(LOCKED): 실시간 RPC 집계는 **기각**됐다(PostgREST 8초 제한). 배치로 만든다.
- ADR-005의 *"`molit-daily`가 타임아웃 이력이 있으니 별도 스텝으로 분리하라"* 요구는
  **이미 충족돼 있다** — `rankings`는 `daily`와 **다른 실행 주체·다른 스케줄**이다.
  → `rankings` 크론에 얹기만 하면 된다. 🔴 **`daily`에 절대 얹지 않는다** (Scope Fence 3).
- ADR-005 §2가 "미결"로 남긴 `hotArea` 산출 방식을 40-CONTEXT D-05가 **MVP 근사로 확정**했다:
  *"등락률 1위 단지의 지역명"*. 그 값을 `metadata`에 담는다.

Output: 마이그레이션 1개 + aggregator 1개 + 테스트 + 크론 소요시간 실측.

⚠️ **이 plan은 40-01·40-03·40-04와 완전히 독립이다.** 0-4는 `card-news/` 전용이고 마이그레이션이
**0건**이다. 즉 40-CONTEXT가 우려한 "`db push` 충돌"은 **존재하지 않는다** — 이 phase에서
마이그레이션을 만드는 plan은 **이것 하나뿐**이다. 병렬 실행해도 안전하다.

---

## 🔴 착수 전 반드시 읽을 것 — 40-CONTEXT D-04의 사실 오류 (오케스트레이터가 정정)

40-CONTEXT D-04와 `<baseline>`의 **크론 표가 틀렸다.** 실측 정정:

| 크론 | 실제 |
|---|---|
| `/api/cron/daily` | `vercel.json` · `0 19 * * *` (1일 1회) ✅ CONTEXT 맞음 |
| ~~`/api/cron/rankings` `30 19 * * *`~~ | ❌ **틀림.** `30 19 * * *` 은 **`/api/cron/cafe-articles`** 다 |
| `/api/cron/rankings` | 🔴 **`vercel.json`에 없다.** `.github/workflows/rankings-cron.yml` 이 `cron: '0 * * * *'` — **매시 정각, 하루 24회** |

`rankings-cron.yml` 실측 세부:
```yaml
schedule: - cron: '0 * * * *'      # 매시 정각 UTC
timeout-minutes: 3
run: curl -sSf ... /api/cron/rankings   # -f → 비200이면 job 실패
```
`src/app/api/cron/rankings/route.ts`는 `export const runtime = 'nodejs'` 이고
**`maxDuration` 선언이 없다** → Vercel 기본 함수 타임아웃을 그대로 받는다.

**이 정정이 바꾸는 것 (plan이 반영한 부분)**:
1. 🔴 **새 aggregator는 하루 1회가 아니라 24회 돈다.** 5000행 `transactions` 조회 **2개 × 24회/일**
   이 추가 비용이다. SC9의 판단 기준을 "야간 배치 창"이 아니라
   **① Vercel 함수 타임아웃 ② GH Actions `timeout-minutes: 3`** 두 상한으로 잡는다.
2. `curl -sSf` 때문에 함수가 타임아웃되면 **GH Actions job이 빨갛게 실패**한다 (조용하지 않다 — 다행이다).
3. 배포 후 `price_change` 행은 **매시 갱신**된다. 배포 전에는 이번 수동 실행분이 그대로 남는다
   (배포된 빌드의 aggregator 목록에 `price_change`가 없어 덮이지 않는다).
4. ⛔ **"04:30 KST 배치" 같은 표현을 쓰지 않는다 — 그런 배치는 없다.** 다음 실행은 **정시(매시 00분 UTC)** 다.

D-04의 **결론 자체는 유효하다** — `rankings`가 `molit-daily`와 분리돼 있다는 사실이 ADR-005의
요구를 충족한다. 틀린 것은 스케줄·비용 분석이다.

🔴 **40-CONTEXT.md 본문도 이 Phase에서 정정됐다** (D-04 표 + `<baseline>` "크론 분리" 행).
CONTEXT와 이 plan이 어긋나 보이면 **정정된 CONTEXT와 이 절이 맞다.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-changbuletter-prereq/40-CONTEXT.md
@CLAUDE.md
@.planning/fix-loop/error-notes.md

<interfaces>
<!-- 전부 플래너가 프로덕션에서 직접 조회한 실측값(2026-07-31). -->

**`complex_rankings` 현재 상태 (`unique_indexes_for_table` RPC 실행 결과)**
```
complex_rankings_pkey                                  {id}                                   is_partial=false
complex_rankings_rank_type_complex_id_window_days_key  {complex_id,rank_type,window_days}     is_partial=false
```
→ 기존 `onConflict: 'rank_type,complex_id,window_days'`(rankings.ts:251)는 **비부분 UNIQUE와 정확히 일치**한다.
**`onConflict` 문자열을 바꾸지 않는다** → Phase 39 게이트에 새 위험을 만들지 않는다.

**현재 행 분포 (실측)**: `volume` 291 / `price_per_pyeong` 201 / `high_price` 198 / `interest` 1.
`window_days`는 **전부 30**. 최신 `computed_at` = `2026-07-31T08:28:38+00:00`.

**CHECK 제약 (`20260507000001_complex_rankings.sql:7`, 인라인 정의 → 자동 명명)**
```sql
rank_type text not null check (rank_type in ('high_price','volume','price_per_pyeong','interest'))
```
→ 제약 이름은 `complex_rankings_rank_type_check`. 🔴 **DROP 전에 카탈로그에서 확인할 것**
(Phase 39의 교훈: 마이그레이션 파일이 아니라 카탈로그가 진실이다).

**`complexes` 컬럼 (실측 샘플)**: `id`, `canonical_name`, `si`, `gu`, `sgg_code` 전부 존재.
예: `{si:'부산광역시', gu:'해운대구', sgg_code:'26350'}` → **지역명은 `gu ?? si`**.

**기존 aggregator 4종의 공통 골격 (`rankings.ts:73-205`)**
```
.from('transactions').select(…)
  .is('cancel_date', null).is('superseded_by', null)
  .eq('deal_type','sale').gte('deal_date', thirtyDaysAgo)
  .in('sgg_code', activeSggCodes).not('complex_id','is',null)
  .limit(2000|5000)
```
→ 신규 aggregator도 **같은 골격**을 따른다. 기존 4개는 건드리지 않는다(Scope Fence 10).

**`computeRankings` 업서트부 (`rankings.ts:237-256`)** — `metadata: row.metadata ?? null` 이미 지원.
`getRankingsByType`(`:51-60`)은 `metadata.area_m2`만 읽으므로 **키를 추가해도 안전**하다.

**🔴 테스트 목의 메서드 화이트리스트 함정 — `.planning/fix-loop/error-notes.md` #001 그대로**
`src/__tests__/rankings.test.ts:35`:
```ts
const methods = ['select', 'eq', 'is', 'in', 'not', 'gt', 'gte', 'order', 'limit']
```
**`lt`·`lte`가 없다.** 직전 30일 창을 만들려면 `.lt('deal_date', …)`가 필요한데, 목에 없으면
`TypeError: … .lt is not a function` 으로 **기존 테스트가 깨진다.** 이건 #001에서 이미 한 번
당한 실수다(`.range()` 누락). → **목록에 `'lt','lte'`를 먼저 추가하고 시작한다.**
같은 형태의 목이 `gap-analysis.test.ts:24`·`gap-stats.test.ts:37`에도 있으나
그 둘은 `computeRankings`를 호출하지 않는다 — **건드리지 않는다.**

**크론 라우트 (`src/app/api/cron/rankings/route.ts`)** — `computeRankings(supabase)` 반환값을
`results`로 그대로 내보내고 `rows_upserted` 합계를 `ingest_runs`에 기록한다.
반환 배열에 **필드를 추가하는 것은 안전**하다 (route는 `r.upserted`만 읽는다).

**TS 스크립트 실행기**: `tsx` 가 devDependency에 있다 (`package.json`: `"db:seed": "tsx scripts/seed.ts"`).
관용구: `npx tsx --env-file=.env.local scripts/<name>.ts` (`scripts/verify-cbl-rls.ts:2` 선례).

**마이그레이션 규약 (CLAUDE.md + Scope Fence 7)**
`supabase/migrations/` 파일 작성 → `npm run db:push`. `execute_sql`·MCP `apply_migration`·대시보드 **금지**.
적용 후 `npx supabase migration list --linked` 0/0 확인 필수.

**🔴 raw SQL 조회 수단 — 이것만 쓴다 (플래너가 CLI에서 존재 확인)**
```
npx supabase db query --linked "<SELECT …>"
```
⛔ `supabase-js`로는 raw SQL을 실행할 수 없다 (`.rpc()`는 미리 정의된 함수만 부른다).
⛔ MCP `execute_sql`은 Scope Fence 7이 금지한다.
⛔ `npx tsx -e "require(...)"` 는 이 저장소가 ESM(`"type"` 미지정이나 Next 15 + `.ts`)이라 동작하지 않는다.

**🔴 프로덕션 크론 실측 소요시간 — `ingest_runs`에 이미 남아 있다**
`route.ts`가 매 실행마다 `ingest_runs`에 `source_id='rankings'` 행을 만들고
`started_at`/`completed_at`을 기록한다. 즉 **현재 4종 기준 실제 소요시간이 DB에 있다.**
```
npx supabase db query --linked "select started_at, completed_at, extract(epoch from (completed_at - started_at)) as sec, status, rows_upserted from public.ingest_runs where source_id='rankings' order by started_at desc limit 24"
```
→ 이것이 SC9의 **진짜 BEFORE**다 (로컬 측정보다 정확하다 — Vercel→Supabase 경로).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHECK 제약 확장 마이그레이션 (사전 카탈로그 확인 → db push → 원장 확인)</name>

  <files>
    supabase/migrations/20260731000001_complex_rankings_price_change.sql
  </files>

  <read_first>
    - `supabase/migrations/20260507000001_complex_rankings.sql` — 원 CHECK 정의
    - `supabase/migrations/20260730000001_cbl_site_id_role_check.sql` — 이 저장소의
      **CHECK DROP→ADD 마이그레이션 선례**. 주석에 ACCESS EXCLUSIVE 락 경고를 남기는 톤을 그대로 따른다
    - `CLAUDE.md` `## 아키텍처 규칙` — `CREATE INDEX CONCURRENTLY` 항목 (이 마이그레이션은 인덱스를
      만들지 않으므로 해당 없음. 확인만 하고 넘어간다)
  </read_first>

  <action>
  **(1) 사전 카탈로그 확인 — 파일이 아니라 실제 제약을 본다**

  ```
  npx supabase db query --linked "select conname, pg_get_constraintdef(oid) from pg_constraint where conrelid = 'public.complex_rankings'::regclass and contype = 'c'"
  ```
  ⛔ `npx tsx -e` + `require()` 조합을 쓰지 않는다 — ESM 프로젝트라 동작하지 않는다.
  ⛔ `supabase-js`로는 raw SQL을 실행할 수 없다. ⛔ MCP `execute_sql` 금지(Scope Fence 7).
  🔴 기대: `complex_rankings_rank_type_check` 1건, 정의에 4종만.
  **이름이나 정의가 다르면 마이그레이션을 쓰지 말고 실제 출력과 함께 보고한다.**
  `rank >= 1` CHECK도 같이 나올 수 있다 — 그건 건드리지 않는다.

  **(2) 마이그레이션 작성** — `supabase/migrations/20260731000001_complex_rankings_price_change.sql`

  ```sql
  -- 창부레터 0단계 0-5: complex_rankings.rank_type 에 'price_change' 추가
  -- 근거: changbuletter/docs/adr/ADR-005 §1 (LOCKED), SPEC-002 0-5, 40-CONTEXT D-04
  --
  -- 왜 CHECK 확장인가: 홈 히어로의 riseRank·avgRise·hotArea 와 volumeRank 의 변동률이
  -- 전부 이 rank_type 에 의존한다. 실시간 RPC 집계는 ADR-005 가 기각했다
  -- (PostgREST authenticator 의 statement_timeout=8s).
  --
  -- ⚠️ ACCESS EXCLUSIVE 락: drop → add 순서라 complex_rankings 에 짧은 락이 걸리고
  --    add 시 기존 행 전체(2026-07-31 실측 691행)를 검증 스캔한다. 소규모라 실무상 무해하다.
  --
  -- ⛔ 허용값을 넓히기만 한다. 기존 4종을 제거하지 않으며 UPDATE/DELETE 도 없다.

  alter table public.complex_rankings
    drop constraint complex_rankings_rank_type_check;

  alter table public.complex_rankings
    add constraint complex_rankings_rank_type_check
    check (rank_type in ('high_price', 'volume', 'price_per_pyeong', 'interest', 'price_change'));
  ```
  ⛔ `if exists`를 붙이지 않는다 — 이름이 다르면 **조용히 통과하는 대신 시끄럽게 실패**해야 한다.
  ⛔ 이 파일에 다른 DDL을 섞지 않는다 (인덱스·컬럼·RLS 전부 불필요).

  **(3) 적용 + 원장 확인**
  ```
  npm run db:push
  npx supabase migration list --linked        # 🔴 0/0
  ```
  🔴 `db push` 가 실패하면 **임의로 우회하지 않는다.** `execute_sql`·MCP `apply_migration`·
  대시보드는 Scope Fence 7이 금지한다(Phase 37이 청소한 drift의 원인). 출력과 함께 보고한다.

  **(4) 적용 후 재확인 (읽기 전용)**
  ```
  npx supabase db query --linked "select pg_get_constraintdef(oid) from pg_constraint where conrelid='public.complex_rankings'::regclass and conname='complex_rankings_rank_type_check'"
  ```
  → 5종이 보여야 한다. 출력 전문을 SUMMARY에 인용한다.

  **(5) 🔴 현재 프로덕션 크론 소요시간 BEFORE 수집 — 코드를 고치기 전에 한다**
  ```
  npx supabase db query --linked "select started_at, extract(epoch from (completed_at - started_at)) as sec, status, rows_upserted from public.ingest_runs where source_id='rankings' order by started_at desc limit 24"
  ```
  최근 24회(=하루치)의 초 단위 소요시간을 표로 SUMMARY에 남긴다 — min/median/max를 계산한다.
  🔴 **이것이 SC9의 진짜 BEFORE다.** 배포된 빌드가 Vercel→Supabase 경로로 실제 측정한 값이라
  로컬 측정보다 정확하다. Task 3의 로컬 측정은 **보조 지표**로만 쓴다.
  행이 없으면(`data_sources`에 `rankings` 소스가 없어 FK 실패했을 수 있다 — `route.ts:35-38`)
  그 사실을 그대로 기록하고 Task 3의 로컬 측정만으로 간다.

  **(6) 커밋 + SHA 기록**
  ```
  git rev-parse HEAD                    # 🔴 PRE_40_02_SHA — 코드 변경 전 기준점. 반드시 기록
  git add supabase/migrations/20260731000001_complex_rankings_price_change.sql
  git commit -m "feat(rankings): complex_rankings.rank_type 에 price_change 추가 (창부레터 0-5)"
  ```
  ⛔ `git push` 하지 않는다.
  🔴 `PRE_40_02_SHA`(커밋 **직전** HEAD)를 SUMMARY에 기록한다 — Task 3의 회귀·성능 BEFORE가
  이 SHA를 참조한다. `git stash`로는 만들 수 없다(Task 2가 커밋을 남기기 때문).
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && npx supabase migration list --linked && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - **적용 전** `pg_constraint` 조회 출력이 SUMMARY에 인용됨 (제약 이름·정의)
    - 마이그레이션 파일 1개, DDL 문 정확히 **2개**(drop + add). `if exists` 미사용
    - `npm run db:push` exit 0
    - `npx supabase migration list --linked` **0/0**
    - **적용 후** 제약 정의에 5종이 전부 보임 — 출력 인용
    - 조회 수단이 `npx supabase db query --linked` — `npx tsx -e`·`supabase-js` raw SQL·MCP 미사용
    - 🔴 `ingest_runs` 최근 24회 소요시간 표(min/median/max)가 SUMMARY에 있음 — **SC9의 BEFORE**.
      행이 없으면 그 사실과 사유를 기록
    - 🔴 `PRE_40_02_SHA` 가 40자 hex로 SUMMARY에 기록됨 (Task 3이 이 값 없이는 수행 불가)
    - 🔴 `git status --porcelain supabase/` 빈 출력 (커밋 완료, 추가 파일 없음)
    - `execute_sql`·MCP `apply_migration`·대시보드 **미사용** — SUMMARY에 명시
    - `npm run lint` exit 0
  </acceptance_criteria>

  <done>
    `complex_rankings_rank_type_check`가 `price_change`를 허용하고, 그 사실이 카탈로그 조회로
    적용 전후 양쪽에서 확인됐으며, 마이그레이션 원장이 0/0이다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: aggregatePriceChange + metadata 지역명 + 절단 감지 (테스트 먼저)</name>

  <files>
    src/lib/data/rankings.ts
    src/__tests__/rankings-price-change.test.ts
    src/__tests__/rankings.test.ts
  </files>

  <read_first>
    - `src/lib/data/rankings.ts` **전문** — 특히 `WINDOW_DAYS`(:7), `RankType`(:9),
      `aggregateHighPrice`(:73-110)의 골격, `aggregators`(:217-225), 업서트부(:237-256)
    - `src/__tests__/rankings.test.ts` **31-55행** — `makeMockChain`의 메서드 화이트리스트.
      🔴 `lt`·`lte`가 **없다**
    - `.planning/fix-loop/error-notes.md` **#001 전문** — 이 함정을 한 번 밟은 기록.
      교훈 1(목 화이트리스트) + 교훈 3(루프 상한)
    - `card-news/scripts/fetch-data.js:515-572` — `fetchPriceChangeRanking`.
      **같은 계산을 이미 한 선례**다. `cur.count < 3` / `prev.count < 3` 제외 규칙을 여기서 가져온다.
      ⛔ 이 함수를 재사용하지 않는다 (card-news는 별도 패키지, 서비스롤 스탠드얼론)
  </read_first>

  <behavior>
  `aggregatePriceChange(supabase, activeSggCodes): Promise<AggRow[]>` 의 기대 동작.

  **창 정의 — `WINDOW_DAYS = 30` 유지 (40-CONTEXT D-04, 변경 금지)**
  - 현재 창: `deal_date >= today-30`
  - 직전 창: `deal_date >= today-60 AND deal_date < today-30`
  - 🔴 `complex_rankings`의 UNIQUE가 `(rank_type, complex_id, window_days)`라 창을 섞으면
    기존 4종과 같은 행 공간에서 의미가 흐려진다. **다른 창을 쓰지 않는다.**

  **필터 — 기존 4종과 동일**
  - 🔴 `.is('cancel_date', null).is('superseded_by', null)` **필수** (ADR-003·CLAUDE.md·Scope Fence 5)
  - `.eq('deal_type','sale')`, `.in('sgg_code', activeSggCodes)`, `.not('complex_id','is',null)`
  - `.order('deal_date', { ascending: false })` — 상한에 걸릴 때 **결정적으로** 잘리게 한다
  - `.limit(5000)` (기존 `aggregateVolume`·`aggregatePricePerPyeong`과 같은 값)

  **집계**
  - 단지별 평균가 = `sum(price) / count`
  - 📌 **최소 거래 건수 임계 = 3 (D-07 재량 결정)**, **양쪽 창 모두**에 적용.
    근거: `compute_gap_stats`의 `>= 3` 선례 + `fetch-data.js:555-557`이 이미 같은 규칙을 쓴다.
    1~2건짜리 단지는 등락률이 널뛰어 "이번 주 뜨거운 동네"를 왜곡한다.
  - `score` = `((cur - prev) / prev) * 100`, 소수 1자리 반올림
    (`Math.round(x * 10) / 10` — `complex_rankings.score`는 `numeric`이라 음수 허용)
  - 내림차순 정렬(1위 = 최대 상승) → `slice(0, 100)` (기존 4종과 동일 상한)

  **metadata — `hotArea` 근사 (40-CONTEXT D-05)**
  - 상위 100개 단지의 `complexes`를 **1회 조회**(`.in('id', ids).select('id,si,gu')`)
  - 각 행의 metadata:
    ```
    { region: gu ?? si ?? null, cur_avg, prev_avg, cur_count, prev_count }
    ```
  - 📌 **1위 행만이 아니라 전 행에 `region`을 넣는다** (D-07 재량). 40-CONTEXT D-05의 요구
    ("1위 행의 metadata에 지역명")를 포함하면서, 창부레터가 `riseRank[]` 각 항목의 지역도
    바로 쓸 수 있다. 비용은 조회 1회뿐이다.
  - 🔴 `getRankingsByType`(rankings.ts:51-60)은 `metadata.area_m2`만 읽는다 — 키 추가는 안전하다

  **절단 감지 (error-notes #001 교훈 3의 정신)**
  - 어느 창이든 반환 행 수가 `5000`이면 `console.warn`으로 남기고, `computeRankings`의 결과에
    `truncated: true`를 실어 보낸다. **throw 하지 않는다** — 던지면 정상 동작 중인 4종까지
    같이 죽는다(크론 라우트가 전체를 500으로 처리한다).
  - 🔴 조용한 절단이 최악이다. 로그도 없고 플래그도 없으면 등락률이 틀려도 아무도 모른다.

  **`computeRankings` 변경**
  - `RankType` 유니온에 `'price_change'` 추가
  - `aggregators` 배열 **끝에** `{ type: 'price_change', fn: aggregatePriceChange }` 추가
    (끝에 두는 이유: 기존 4종이 먼저 끝나 부분 실패 시 손실이 작다)
  - 반환 타입: `{ type, upserted, ms, truncated? }[]` — `ms`는 해당 aggregator+upsert 소요 ms.
    🔴 `route.ts`는 `r.upserted`만 읽으므로 필드 추가는 안전하다

  **테스트 (`src/__tests__/rankings-price-change.test.ts`, vitest, DB 불필요)**

  기존 `rankings.test.ts`의 `makeMockChain`/`makeMockSupabase` 패턴을 **복제**한다
  (import하지 않는다 — 그 파일은 route 테스트용이라 결합을 늘리지 않는다).
  🔴 목 메서드 목록에 `'lt'`·`'lte'`를 **반드시** 포함한다.

  **🔴 목을 그대로 복제하면 케이스 4~8이 원리적으로 통과할 수 없다 — 먼저 고친다**

  `makeMockSupabase`는 `overrides[table]`이 있으면 **매 `.from()` 호출에 같은 chain 객체**를
  돌려주고, `chain.limit`은 `mockResolvedValue(result)`로 **항상 같은 데이터**를 낸다.
  `aggregatePriceChange`는 `.from('transactions')`를 **두 번**(현재 창/직전 창) 부르므로
  두 창이 **동일 데이터**가 되어 등락률이 **언제나 0%** 다 → 케이스 4~8(임계·상승·하락)이
  구조적으로 불가능하다. 이건 error-notes #001과 **같은 부류의 목 함정**이다.

  해결 — 창별 데이터를 순서대로 돌려준다:
  ```ts
  const tx = makeMockChain({ data: [], error: null })
  ;(tx.limit as Mock).mockReset()
  ;(tx.limit as Mock)
    .mockResolvedValueOnce({ data: CUR_ROWS,  error: null })   // 1st .from('transactions')
    .mockResolvedValueOnce({ data: PREV_ROWS, error: null })   // 2nd
  const cx = makeMockChain({ data: COMPLEX_ROWS, error: null }) // complexes (지역명)
  const supabase = makeMockSupabase({ transactions: tx, complexes: cx, regions: … })
  ```
  ⚠️ 호출 **순서**가 결정적이어야 한다. 구현이 `Promise.all([fetchWindow(cur), fetchWindow(prev)])`
  이면 첫 인자의 동기 구간이 먼저 실행되므로 `.limit()` 호출 순서 = 배열 순서다.
  🔴 구현에서 두 창의 순서를 바꾸면 이 테스트가 조용히 반대로 측정한다 —
  **케이스 7(상승 +10)과 케이스 8(하락 −10)을 둘 다** 두는 이유가 그것이다. 부호가 뒤집히면 잡힌다.
  `regions`(`getActiveSggCodes`)도 override해 `sgg_code` 목록을 명시적으로 준다.

  | # | 케이스 | 기대 |
  |---|---|---|
  | 1 | 🔴 **취소·정정 필터** — `transactions` 체인에서 `.is('cancel_date', null)`·`.is('superseded_by', null)`가 각각 호출됨 | 두 호출 모두 인자까지 단언 |
  | 2 | 🔴 `deal_type='sale'` 필터 존재 | `.eq` 호출 인자 확인 |
  | 3 | 양쪽 창 모두 3건 이상인 단지가 결과에 포함 | 포함 |
  | 4 | 🔴 현재 창 2건 → 제외 | 미포함 |
  | 5 | 🔴 직전 창 2건 → 제외 | 미포함 |
  | 6 | 직전 창에 데이터 없음 → 제외 (0으로 나누지 않는다) | 미포함, 예외 없음 |
  | 7 | score 계산 — 평균 10000 → 11000 | `10` |
  | 8 | 하락 단지 — 평균 10000 → 9000 | `-10` |
  | 9 | 정렬: 상승률 큰 순 | rank 1 = 최대 상승 |
  | 10 | 🔴 metadata에 `region` 존재 (complexes 조회 결과 `gu`) | `'성산구'` |
  | 11 | `gu`가 null이면 `si` 폴백 | `si` 값 |
  | 12 | 🔴 5000행 반환 시 `truncated: true` + `console.warn` 호출 | 양쪽 단언 |
  | 13 | 결과 0건이면 upsert를 호출하지 않는다 | `upsert` 미호출 |
  | 14 | 🔴 `computeRankings` 결과 배열에 `price_change` 항목이 **존재**하고 기존 4종이 **전부 그대로** 있다 | 5개, 타입 순서 확인 |
  | 15 | 🔴 upsert 인자의 `onConflict`가 `'rank_type,complex_id,window_days'` **그대로** | 문자열 일치 |
  | 16 | upsert 행의 `window_days === 30` | `30` |
  </behavior>

  <action>
  **(1) 먼저 목 화이트리스트를 고친다 — 이걸 안 하면 기존 테스트가 깨진다**

  `src/__tests__/rankings.test.ts:35`:
  ```
  - const methods = ['select', 'eq', 'is', 'in', 'not', 'gt', 'gte', 'order', 'limit']
  + const methods = ['select', 'eq', 'is', 'in', 'not', 'gt', 'gte', 'lt', 'lte', 'order', 'limit']
  ```
  그 줄 위에 주석 1줄: `// lt/lte: aggregatePriceChange 의 직전 창 필터용 (error-notes #001 재발 방지)`
  ⛔ 이 파일의 다른 부분을 손대지 않는다.

  **(2) 테스트 먼저** — `src/__tests__/rankings-price-change.test.ts` 에 16케이스를 전부 쓴다.
  이 시점에 `npx vitest run src/__tests__/rankings-price-change.test.ts` 는 **실패**해야 한다(RED).
  실패 출력을 SUMMARY에 남긴다.

  케이스 1의 단언 형태(가장 중요):
  ```ts
  expect(txChain.is).toHaveBeenCalledWith('cancel_date', null)
  expect(txChain.is).toHaveBeenCalledWith('superseded_by', null)
  ```
  🔴 이 두 줄이 Success Criteria 7(코드+테스트)의 **테스트 쪽 본체**다.

  **(3) 구현** — `src/lib/data/rankings.ts`
  - `RankType` 유니온 확장
  - `AggRow` 인터페이스는 그대로 재사용 (`metadata?: Record<string, unknown>`)
  - `aggregatePriceChange`를 `aggregateInterest` **아래**에 추가 (기존 4개 사이에 끼워넣지 않는다)
  - 창 계산은 기존 `thirtyDaysAgo` 관용구를 따른다:
    ```
    new Date(Date.now() - N * 86_400_000).toISOString().split('T')[0]!
    ```
  - 두 창을 **`Promise.all`로 병렬** 조회한다 (크론 소요시간 증가를 절반으로)
  - `computeRankings`에 `ms` 계측 추가: 각 aggregator 앞뒤로 `Date.now()`
  ⛔ 기존 4개 aggregator의 본문을 **한 글자도 고치지 않는다** (Scope Fence 10).
  ⛔ `complexes.price_change_30d`를 **읽지 않는다** (Scope Fence 6 — 소유 배치가 다른 30일 롤링 컬럼).
  ⛔ 새 RPC를 만들지 않는다 (Scope Fence 4).

  **(4) GREEN 확인 + 회귀**
  ```
  npx vitest run src/__tests__/rankings-price-change.test.ts    # 16 passed
  npx vitest run src/__tests__/rankings.test.ts                 # 기존 전부 통과
  npm run lint                                                   # exit 0 (tsc 포함)
  ```
  🔴 `rankings.test.ts`가 깨지면 (1)의 목 수정을 빠뜨린 것이다.

  **(5) 커밋**
  ```
  git add src/lib/data/rankings.ts src/__tests__/rankings-price-change.test.ts src/__tests__/rankings.test.ts
  git commit -m "feat(rankings): 등락률 aggregator 추가 — price_change + hotArea 근사 metadata"
  ```
  ⛔ `git push` 하지 않는다.
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && npx vitest run src/__tests__/rankings-price-change.test.ts src/__tests__/rankings.test.ts && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `RankType`에 `'price_change'` 포함
    - `aggregators` 배열 길이 5, 기존 4종의 `type`·`fn` 매핑 불변
    - 🔴 `grep -c "superseded_by" src/lib/data/rankings.ts` — **변경 전 실측값은 3이다**
      (`aggregateHighPrice`·`aggregateVolume`·`aggregatePricePerPyeong` 3개.
      `aggregateInterest`는 `transactions`가 아니라 `favorites`를 조회하므로 없다).
      변경 후 값을 SUMMARY에 기록하고, **신규 경로에 필터가 있다는 사실**을 코드 인용으로 증명한다.
      ⛔ 특정 숫자를 목표로 맞추려 하지 않는다 — 두 창을 공통 헬퍼로 묶으면 4가 될 수도 있다
    - 🔴 테스트 케이스 1이 `cancel_date`·`superseded_by` 두 호출을 **인자까지** 단언
    - 🔴 목의 `transactions` chain이 `mockResolvedValueOnce` **2회 체인**으로 창별 데이터를 준다
      (`grep -c "mockResolvedValueOnce" src/__tests__/rankings-price-change.test.ts` ≥ 2).
      단일 `mockResolvedValue`면 두 창이 같아져 케이스 4~8이 무의미해진다
    - 케이스 7(+10)과 케이스 8(−10)이 **둘 다** 존재 — 창 순서가 뒤집히면 부호로 잡힌다
    - `npx vitest run src/__tests__/rankings-price-change.test.ts` → **16 passed / 0 skipped**
    - `npx vitest run src/__tests__/rankings.test.ts` 전부 통과 (목에 `lt`·`lte` 추가됨)
    - `grep -c "'lt', 'lte'" src/__tests__/rankings.test.ts` ≥ 1
    - upsert `onConflict` 문자열 **무변경** — `git diff` 에 `onConflict` 줄이 나타나지 않음
    - `grep -c "price_change_30d" src/lib/data/rankings.ts` == 0 (Scope Fence 6)
    - `computeRankings` 반환에 `ms` 포함, `route.ts` **무변경**
    - `npm run lint` exit 0
    - `git status --porcelain supabase/` 빈 출력 (Task 1 이후 새 마이그레이션 없음)
  </acceptance_criteria>

  <done>
    등락률 aggregator가 취소·정정 거래를 배제하고, 양쪽 창 3건 미만을 제외하며, 1위를 포함한 모든 행의
    metadata에 지역명을 담고, 절단이 조용히 일어나지 않으며, 기존 4종은 무접촉이다.
  </done>
</task>

<task type="auto">
  <name>Task 3: 라이브 1회 실행 + 소요시간 실측 + 결과 검증 + 회귀 측정</name>

  <files>
    scripts/measure-rankings.ts
  </files>

  <read_first>
    - `scripts/verify-cbl-rls.ts` **1-40행** — 이 저장소의 스탠드얼론 검증 스크립트 관용구
      (env 검증 → admin/anon 분리 → 대상 URL 로그)
    - `src/app/api/cron/rankings/route.ts` — 크론이 `computeRankings`를 어떻게 호출하는지
    - `.planning/fix-loop/error-notes.md` **#001 교훈 2** — `git stash` 베이스라인 실측
    - 40-CONTEXT `<baseline>` — 테스트 베이스라인 **17 failed / 657 passed / 2 skipped (677)**,
      **flaky(17↔18 관측)**
  </read_first>

  <action>
  **(1) 소요시간 계측 스크립트** — `scripts/measure-rankings.ts`

  ```
  npx tsx --env-file=.env.local scripts/measure-rankings.ts
  ```
  동작:
  - `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`로 admin 클라이언트 생성
    (`verify-cbl-rls.ts` 관용구 그대로. env 누락 시 명확히 종료)
  - 대상 URL을 **먼저 출력**한다. `127.0.0.1`이 아니면 "프로덕션 대상"이라고 경고 출력
  - `computeRankings(admin)` 1회 호출 → 반환 배열(`type`/`upserted`/`ms`/`truncated`)을 표로 출력
  - 전체 wall-clock을 출력

  ⚠️ 크론 엔드포인트를 외부 curl로 때리지 않는다 — 프로덕션에서 차단돼 있다.
  `computeRankings`를 직접 호출하는 것이 유일한 실측 경로다.

  🔴 **이 스크립트는 프로덕션 `complex_rankings`에 실제로 씁니다.** 이는 의도된 것이다
  (SC6: 배치 실행 후 `price_change` 행이 존재해야 한다). 다만:
  - `complex_rankings`는 **크론이 매일 통째로 덮어쓰는 파생 테이블**이므로 원본 손실이 없다
  - 기존 4종의 upsert도 같이 돌지만, 그건 크론이 하는 일과 **동일**하다
  - 🔴 실행 **전** 행 수 스냅샷을 찍어 SUMMARY에 남긴다:
    `volume 291 / price_per_pyeong 201 / high_price 198 / interest 1` (플래너 실측, 2026-07-31)

  **(2) 소요시간 before/after 실측 — SC9**

  🔴🔴 **`git stash`를 쓰지 않는다 — 이 시점에 stash는 no-op다.**
  Task 1·Task 2가 각각 커밋을 남겼으므로 워킹트리가 깨끗하다.
  `git stash push -u -- src/` 는 *"No local changes to save"* 를 출력하고 아무것도 되돌리지 않는다.
  그러면 "BEFORE"가 **5종 코드로 측정**되고 증가분이 **0으로 거짓 보고**된다.

  ```
  cd /c/Users/jung/coding/bds
  PRE=<Task 1 SUMMARY에 기록된 PRE_40_02_SHA>
  AFTER=$(git rev-parse HEAD)

  # ── BEFORE (aggregator 4종) ──
  git checkout $PRE -- src/lib/data/rankings.ts
  git diff --stat $PRE -- src/lib/data/rankings.ts     # 🔴 빈 출력 = 되돌아갔다 (양성 신호)
  grep -c "aggregatePriceChange" src/lib/data/rankings.ts   # 🔴 0 이어야 한다
  time npx tsx --env-file=.env.local scripts/measure-rankings.ts

  # ── 복원 → AFTER (5종) ──
  git checkout $AFTER -- src/lib/data/rankings.ts
  git status --porcelain src/lib/data/rankings.ts      # 🔴 빈 출력 = 복원 완료
  time npx tsx --env-file=.env.local scripts/measure-rankings.ts
  ```
  🔴 성공 신호는 `git status` 공백이 아니라 **`git diff --stat $PRE` 공백 + `grep -c` == 0** 두 가지다.
  🔴 BEFORE 실행 시 `measure-rankings.ts`는 `ms` 필드가 **없는** 반환값을 받는다
  (`scripts/`는 되돌리지 않으므로 스크립트 자체는 최신이다) → `r.ms ?? '-'` 로 견디게 쓴다. (1)에서 반영.

  **SC9 판정 기준 — 🔴 야간 배치 창이 아니라 두 상한이다**
  | 상한 | 값 | 근거 |
  |---|---|---|
  | Vercel 함수 타임아웃 | `route.ts`에 `maxDuration` 없음 → 프로젝트 기본값 | 초과 시 함수가 죽는다 |
  | GH Actions job | `timeout-minutes: 3` (`rankings-cron.yml`) | 초과 시 job 실패 |

  🔴 **그리고 이 크론은 하루 24회 돈다** (`cron: '0 * * * *'`). 추가 비용은
  `5000행 조회 × 2 × 24회/일` 이다. 이 사실을 SUMMARY의 SC9 절에 숫자로 적는다.

  ⚠️ **로컬 측정은 하한(lower bound)이다.** 로컬→Supabase 네트워크 경로가
  Vercel→Supabase와 다르다. 진짜 값은 Task 1 (5)에서 뽑은 **`ingest_runs` 최근 24회**이며,
  배포 후 다시 조회해야 AFTER 실측이 나온다.
  → SUMMARY에 세 값을 구분해 적는다: **① `ingest_runs` BEFORE(프로덕션 실측)
  ② 로컬 BEFORE ③ 로컬 AFTER**. ②③의 차이를 증가분 추정치로 쓰고 **"추정"이라고 명시**한다.
  ④ 프로덕션 AFTER는 **배포 후에만** 측정 가능 → "미확인"으로 이월한다.

  **(3) 결과 검증 (읽기 전용)**
  ```sql
  select rank_type, count(*), min(score), max(score), max(computed_at)
  from public.complex_rankings group by rank_type order by rank_type;

  select r.rank, r.score, r.metadata, c.canonical_name, c.gu, c.si
  from public.complex_rankings r join public.complexes c on c.id = r.complex_id
  where r.rank_type = 'price_change' and r.window_days = 30
  order by r.rank limit 5;
  ```
  확인 항목:
  - `price_change` 행 수 > 0 (**SC6**)
  - 🔴 rank 1 행의 `metadata->>'region'`이 그 단지의 `gu`(또는 `si`)와 **일치** (**SC8**)
  - 기존 4종의 행 수가 실행 전 스냅샷과 **같은 자릿수** — 급감했다면 무언가 깨진 것이다
  - `score` 범위가 상식적인가 (예: ±200% 밖 값이 있으면 이상치 유입 가능성을 SUMMARY에 기록.
    ⚠️ 이 aggregator는 `filterOutliers`를 쓰지 않는다 — **의도된 범위 밖**이며 이월 항목으로 남긴다)

  **(4) 회귀 측정 — 실패 이름 집합 비교 (총계 금지, `git stash` 금지)**

  🔴 여기서도 stash는 no-op다. SHA 되돌리기 + **신규 파일 임시 이동**을 함께 쓴다
  (`git checkout <sha> -- <path>` 는 그 커밋에 **없던 파일을 지우지 않으므로**,
  신규 테스트가 BEFORE 실행에도 그대로 돌아 측정이 오염된다).

  ```
  cd /c/Users/jung/coding/bds
  PRE=<PRE_40_02_SHA> ; AFTER=$(git rev-parse HEAD)

  npm run test 2>&1 | grep -E "^\s*(×|FAIL)" | sed 's/ *[0-9]*ms$//' | sort -u > /tmp/p40-02-after.txt

  git checkout $PRE -- src/
  mv src/__tests__/rankings-price-change.test.ts /tmp/p40-02-newtest.ts   # 신규 파일 격리
  git diff --stat $PRE -- src/          # 🔴 빈 출력 = 되돌아갔다
  git status --porcelain src/           # 🔴 빈 출력 (신규 파일을 옮겼으므로)
  npm run test 2>&1 | grep -E "^\s*(×|FAIL)" | sed 's/ *[0-9]*ms$//' | sort -u > /tmp/p40-02-before.txt

  mv /tmp/p40-02-newtest.ts src/__tests__/rankings-price-change.test.ts
  git checkout $AFTER -- src/
  git status --porcelain src/           # 🔴 빈 출력 = 복원 완료

  diff /tmp/p40-02-before.txt /tmp/p40-02-after.txt && echo "FAILSET_UNCHANGED"
  ```
  ⚠️ `supabase/` 는 되돌리지 않는다 — 마이그레이션은 **이미 DB에 적용**됐고 파일을 되돌려도
  DB가 되돌아가지 않아 의미가 없다. 그리고 vitest는 마이그레이션 파일을 읽지 않는다.

  🔴 베이스라인 17은 **flaky(17↔18)** 다. 총계로 판정하지 않는다.
  `failed` 이름 집합에 **새 이름이 하나라도 추가되면 회귀**다 — 중단하고
  `.planning/fix-loop/error-notes.md`에 항목을 추가한 뒤 보고한다.

  **(5) 마무리**
  ```
  npm run lint
  npx supabase migration list --linked      # 0/0
  git add scripts/measure-rankings.ts
  git commit -m "chore(rankings): 랭킹 배치 소요시간 실측 스크립트 (0-5 SC9)"
  ```
  ⛔ `git push` 하지 않는다. **배포 전까지 프로덕션 크론은 여전히 구 코드(4종)로 돈다.**

  🔴 SUMMARY에 이렇게 적는다 (문구를 정확히 지킬 것):
  > 미배포 상태다. `rankings` 크론은 `.github/workflows/rankings-cron.yml`의
  > `cron: '0 * * * *'` 로 **매시 정각** 돌지만, 배포 전까지 그 실행은 **4종 코드**다.
  > 따라서 이번에 수동 실행으로 만든 `price_change` 행은 갱신되지 않고 그대로 남는다.

  ⛔ **"04:30 KST 배치" 같은 표현을 쓰지 않는다 — `rankings`에 그런 배치는 없다.**
  (`0 19 * * *`는 `/api/cron/daily`, `30 19 * * *`는 `/api/cron/cafe-articles`다.)
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && npm run lint && npx supabase migration list --linked</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/measure-rankings.ts` 존재. env 누락 시 명확히 종료, 대상 URL을 먼저 출력
    - 스크립트가 `r.ms` 부재에도 동작 (`?? '-'`)
    - 실행 **전** `complex_rankings` 타입별 행 수 스냅샷이 SUMMARY에 기록됨
    - 🔴 `rank_type='price_change'` 행 수 **> 0** (SC6) — 조회 출력 인용
    - 🔴 rank 1 행의 `metadata->>'region'`이 해당 단지 `gu`/`si`와 일치 (SC8) — 조회 출력 인용
    - 기존 4종 행 수가 스냅샷 대비 급감하지 않음 (급감 시 그 사실과 수치를 기록)
    - 🔴 SC9 값 **4종 구분** 기록: ① `ingest_runs` BEFORE(프로덕션 실측, Task 1)
      ② 로컬 BEFORE ③ 로컬 AFTER ④ 프로덕션 AFTER = **"미확인(배포 후)"**.
      ②③ 차이는 **"추정 증가분"** 으로 표기 — 로컬 측정은 네트워크 경로가 달라 **하한**이다
    - 🔴 SC9 절에 **"하루 24회 실행(`cron: '0 * * * *'`)"** 과 두 상한
      (Vercel 함수 타임아웃 / GH Actions `timeout-minutes: 3`)이 명시됨
    - 5종 타입별 `ms` 표가 SUMMARY에 있음
    - `truncated` 값이 SUMMARY에 기록됨 (true면 위험으로 명시)
    - 🔴 성능·회귀 BEFORE가 **SHA 되돌리기**로 만들어짐. 양성 신호 인용:
      `git diff --stat $PRE -- <path>` 빈 출력 + `grep -c "aggregatePriceChange"` == 0
    - ⛔ `git stash` **미사용** (커밋 뒤라 no-op — 쓰면 BEFORE가 AFTER 코드로 측정된다)
    - 회귀 측정 시 신규 테스트 파일이 **BEFORE 실행에서 격리**됨 (`mv` 로 이동 후 복원)
    - 실패 이름 집합 `diff` 빈 출력 — 두 파일 경로·행수를 SUMMARY에 기록
    - `npm run lint` exit 0 / `migration list --linked` **0/0**
    - 🔴 SUMMARY의 미배포 이월 문구가 **"매시 정각 실행이지만 배포 전까지 4종"** 형태.
      ⛔ **"04:30 KST 배치" 표현 0건** (`rankings`에 그런 배치는 없다)
    - "아마도"·"문제없을 것"·"성공 추정" 류 표현 **0건**
  </acceptance_criteria>

  <done>
    등락률 배치가 프로덕션에서 1회 실제로 돌아 `price_change` 행이 생겼고, 1위 행의 metadata 지역명이
    실제 단지 지역과 일치함이 확인됐으며, 크론 소요시간 증가분이 숫자로 남았고, 배포 전이라
    다음 자동 배치는 아직 구 코드라는 사실이 명시적으로 인계됐다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `transactions` → 등락률 계산 | 취소·정정 거래가 새면 **틀린 랭킹이 조용히 발행**된다 |
| 랭킹 배치 → 창부레터 홈 히어로 | 창부레터는 **소비자일 뿐 생산자가 아니다**(ADR-005). 값이 이상해도 창부레터 코드에는 단서가 없다 |
| 마이그레이션 → 프로덕션 `complex_rankings` | ACCESS EXCLUSIVE 락 + 전체 행 검증 스캔 |
| `measure-rankings.ts` → 프로덕션 쓰기 | service_role. 파생 테이블 덮어쓰기라 원본 손실은 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Tampering | 취소·정정 거래가 등락률에 유입 | mitigate | 🔴 `.is('cancel_date',null).is('superseded_by',null)` + **테스트 케이스 1이 인자까지 단언**. Scope Fence 5 |
| T-40-02-02 | Repudiation | `limit(5000)` 조용한 절단으로 랭킹이 틀림 | mitigate | `.order('deal_date')`로 절단을 결정적으로 만들고 `truncated` 플래그 + `console.warn`. 테스트 케이스 12 |
| T-40-02-03 | Denial of Service | 새 aggregator가 `rankings` 크론을 타임아웃시킴 | mitigate | 🔴 이 크론은 **하루 24회**(`cron: '0 * * * *'`) 돈다 — 추가 비용은 5000행 조회 ×2 ×24회/일. 두 창 `Promise.all` 병렬 + 타입별 `ms` + `ingest_runs` 프로덕션 실측(BEFORE) + 로컬 before/after. 상한은 Vercel 함수 타임아웃(`maxDuration` 미선언)과 GH Actions `timeout-minutes: 3`. `curl -sSf` 라 초과 시 job이 **빨갛게 실패**한다(조용하지 않다) |
| T-40-02-11 | Repudiation | 🔴 **`git stash` BEFORE가 no-op라 증가분·회귀가 거짓으로 0** | mitigate | Task 1·2가 커밋을 남기므로 stash는 아무것도 되돌리지 않는다. `git checkout $PRE_40_02_SHA -- <path>` + 신규 테스트 `mv` 격리. 성공 신호를 `git diff --stat` 공백 + `grep -c aggregatePriceChange` == 0 **두 양성 신호**로 |
| T-40-02-12 | Repudiation | 목이 두 창에 같은 데이터를 줘 등락률이 항상 0% | mitigate | `chain.limit.mockResolvedValueOnce(cur).mockResolvedValueOnce(prev)`. 케이스 7(+10)·8(−10) 양방향으로 창 순서 뒤집힘까지 검출 |
| T-40-02-04 | Tampering | 마이그레이션이 기존 4종 허용값을 잃음 | mitigate | drop→add 를 **한 파일 2문**으로만 하고, 적용 전후 `pg_get_constraintdef` 출력을 양쪽 인용 |
| T-40-02-05 | Denial of Service | 목 화이트리스트 누락으로 기존 테스트 붕괴 | mitigate | 🔴 error-notes #001 재발. Task 2 (1)에서 `lt`·`lte`를 **구현보다 먼저** 추가 |
| T-40-02-06 | Repudiation | 1~2건 단지의 널뛰는 등락률이 hotArea를 왜곡 | mitigate | 양쪽 창 **3건 이상** 임계(테스트 4·5). `compute_gap_stats` 선례 |
| T-40-02-07 | Repudiation | 근사 `hotArea`가 본안으로 오인됨 | mitigate | metadata에 담아 교체 시 창부레터 코드 변경을 최소화. SUMMARY에 **근사임을 명시** (D-05). `changbuletter/docs/adr/ADR-005` "미결" 표기 갱신은 **크로스 레포라 범위 밖** — 사용자 판단으로 이월 |
| T-40-02-08 | Repudiation | "배치가 매일 돈다"는 과신 | mitigate | 🔴 `git push` 미실행 = 프로덕션 크론은 여전히 4종. Task 3 acceptance가 이 사실의 SUMMARY 기재를 강제 |
| T-40-02-09 | Information Disclosure | `metadata`에 불필요 정보 노출 | accept | `region`·평균가·건수뿐. `complex_rankings`는 이미 공개 읽기 랭킹 테이블이고 원자료(`transactions`)도 공개다 |
| T-40-02-10 | Tampering | 이상치 거래(단타·특수관계)가 등락률을 부풀림 | accept | 이 aggregator는 `filterOutliers`(200% 룰)를 쓰지 않는다. 기존 4종도 안 쓴다 — 일관성 유지. **이월 항목으로 명시**하고 score 범위를 SUMMARY에 기록 |
| T-40-02-SC | Tampering | npm/pip/cargo installs | N/A | 신규 의존성 **0건** |
</threat_model>

<verification>
1. 마이그레이션 적용 **전후** `pg_get_constraintdef` 출력 양쪽 인용, 후자에 5종
2. `npm run db:push` exit 0, `npx supabase migration list --linked` **0/0**
3. `execute_sql`·MCP `apply_migration`·대시보드 미사용 (Scope Fence 7)
4. `npx vitest run src/__tests__/rankings-price-change.test.ts` → **16 passed / 0 skipped**
5. 케이스 1이 `cancel_date`·`superseded_by` 호출을 인자까지 단언 (SC7 테스트 본체)
6. `src/__tests__/rankings.test.ts` 목에 `lt`·`lte` 추가, 기존 테스트 전부 통과
7. `onConflict` 문자열 무변경 — `git diff`에 해당 줄 미출현
8. `grep -c "price_change_30d" src/lib/data/rankings.ts` == 0
9. 라이브 1회 실행 후 `rank_type='price_change'` 행 수 > 0 (SC6)
9-b. raw SQL은 `npx supabase db query --linked` 로만 — `npx tsx -e`·supabase-js raw SQL·MCP 미사용
10. rank 1 `metadata->>'region'` == 해당 단지 `gu`/`si` (SC8)
11. SC9 4종 값(① ingest_runs BEFORE ② 로컬 BEFORE ③ 로컬 AFTER ④ 프로덕션 AFTER=미확인) +
    하루 24회 실행 사실 + 두 상한 명시. 로컬 측정은 **하한**임을 표기
12. SHA 되돌리기 회귀 실측 — 실패 **이름 집합** 불변 (총계 비교 금지, `git stash` 금지)
13. `npm run lint` exit 0
14. `git push` 미실행 — **매시 정각** 실행이 아직 4종임이 SUMMARY에 명시. "04:30 KST" 표현 0건
</verification>

<success_criteria>
1. `complex_rankings_rank_type_check`가 `price_change`를 허용한다 (카탈로그 확인)
2. 배치 1회 실행 후 `rank_type='price_change'` 행이 실재한다
3. 등락률 계산이 `cancel_date IS NULL AND superseded_by IS NULL`을 적용한다 — **코드 + 테스트 양쪽**
4. `price_change` 1위 행의 `metadata`에 지역명이 담긴다 (`hotArea` 근사)
5. `rankings` 크론 소요시간이 4종 구분(프로덕션 BEFORE / 로컬 BEFORE / 로컬 AFTER / 프로덕션 AFTER=미확인)으로
   기록되고, **하루 24회 실행**이라는 사실과 두 상한이 함께 적혔다
6. 기존 4종 aggregator가 무접촉이고 산출 결과가 유지된다
7. `daily` 크론 무변경 / 새 RPC 0개 / `price_change_30d` 미사용
8. lint exit 0 / `migration list --linked` 0/0 / 테스트 실패 이름 집합 불변
9. 근사임(D-05)·미배포·이상치 미필터가 SUMMARY에 정직하게 기록됐다
</success_criteria>

<output>
Create `.planning/phases/40-changbuletter-prereq/40-02-SUMMARY.md` when done.

SUMMARY에 반드시 포함:
- 마이그레이션 적용 **전후** `pg_get_constraintdef` 출력 2건
- `npm run db:push` + `migration list --linked` 출력
- 테스트 RED 출력 → GREEN 출력 (16 passed)
- 🔴 **취소·정정 필터 단언 코드 인용** (케이스 1) — SC7의 증거
- 실행 전 `complex_rankings` 타입별 행 수 스냅샷 + 실행 후 조회 결과
- 🔴 `price_change` rank 1~5 조회 출력 (score·metadata·단지명·gu) — SC8 증거
- 🔴 SC9 표: ① `ingest_runs` 최근 24회(프로덕션 BEFORE, min/median/max) ② 로컬 BEFORE
  ③ 로컬 AFTER ④ 프로덕션 AFTER **미확인(배포 후)** + 5종 타입별 `ms`
  + **하루 24회 실행** · Vercel 함수 타임아웃 · GH Actions `timeout-minutes: 3`
- 🔴 **40-CONTEXT D-04 크론 표 정정** — `30 19 * * *` 은 `cafe-articles` 이고 `rankings` 는
  `vercel.json` 에 없다(`rankings-cron.yml` 매시). D-04의 *결론*은 유효하나 스케줄·비용 분석이 틀렸다
- `truncated` 값. true면 위험으로 명시
- SHA 되돌리기 회귀 실측 — 양성 신호 2건 + 실패 이름 집합 diff (총계만 쓰지 말 것, stash 금지)
- `PRE_40_02_SHA` 40자 hex
- **이월 항목**:
  ① 🔴 **미배포** — `git push` 하지 않았다. `rankings` 크론은 `rankings-cron.yml` 의
     `cron: '0 * * * *'` 로 **매시 정각** 돌지만 배포 전까지 그 실행은 **4종 코드**다.
     따라서 `price_change` 행은 이번 수동 실행 1회분에서 갱신되지 않는다.
     ⛔ "04:30 KST" 표현 금지
  ①-b **프로덕션 AFTER 소요시간 미확인** — 배포 후 `ingest_runs` 재조회로만 얻을 수 있다
  ② `hotArea`는 **MVP 근사**(등락률 1위 단지의 지역명)다. `changbuletter/docs/adr/ADR-005` §2의
     "미결" 표기 갱신은 크로스 레포라 이 Phase 범위 밖 — 사용자 판단
  ③ 이 aggregator는 `filterOutliers`(200% 룰)를 적용하지 않는다 — 기존 4종과 동일한 정책.
     관측된 score 범위를 숫자로 남긴다
  ④ 창부레터가 소비할 조회 계약(`getRankingsByType('price_change')` 반환 형태)은 이 Phase에서
     문서화하지 않았다 — 창부레터 부트스트랩 시 필요
</output>
