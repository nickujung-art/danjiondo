---
phase: 40-changbuletter-prereq
plan: 05
type: execute
wave: 4
depends_on: ["40-01", "40-04"]
files_modified:
  - src/lib/cardnews/card-templates.ts
  - card-news/scripts/templates.js
  - card-news/scripts/generate.js
  - card-news/package.json
  - card-news/fixtures/golden/**
  - card-news/scripts/templates-golden.test.mjs
autonomous: false
requirements: [CBL-0-6]

must_haves:
  truths:
    - "두 카드 템플릿 코드베이스 어디에도 '창원부동산랩' 문자열이 남아 있지 않다"
    - "src/lib/cardnews/card-templates.ts 가 1080×1350 (4:5) 이다 — 어드민 빌더와 주간 자동화의 비율이 같다"
    - "리브랜딩으로 바뀐 HTML 부분이 골든 재생성 diff 로 정확히 특정돼 승인됐다 — 그 외에는 아무것도 안 바뀌었다"
    - "9:16 템플릿은 만들어지지 않았다"
    - "어드민 빌더 템플릿 5종이 1350 높이에서 레이아웃 붕괴 없이 렌더된다 (사람이 확인)"
  artifacts:
    - path: "src/lib/cardnews/card-templates.ts"
      provides: "1080×1350 통일 + 창부레터 브랜딩"
      contains: "1350"
  key_links:
    - from: "card-news/fixtures/golden"
      to: "card-news/scripts/templates.js"
      via: "골든 재생성 후 diff 범위 한정"
      pattern: "창부레터"
---

<objective>
0-6. 두 카드 템플릿 코드베이스를 **창부레터**로 리브랜딩하고 비율을 **1080×1350 (4:5)** 로 통일한다.

Purpose (ADR-004 §3·§4, LOCKED):
- 어느 도구로 생성해도 결과물이 일관되어야 한다. 현재 브랜드는 **창원부동산랩**이고,
  두 코드베이스의 비율이 **다르다** (`templates.js` 4:5 / `card-templates.ts` 1:1).
- ADR-004 §3: `card-templates.ts` **1080 → 1350** (`html,body` + 각 `.card`, **총 6곳**)
- ADR-004 §4: 리브랜딩. 🔴 *"착수 전 두 파일 전체를 다시 grep할 것 — 잔존 위치가 더 있을 수 있다."*
  1차 감사가 실제로 **두 번째 코드베이스를 통째로 놓쳤다.**
- ⛔ **9:16(1080×1920) 템플릿은 만들지 않는다.** ADR-004가 BRIEF §10의 관련 서술을 **폐기**했다.

## 🔴 이 plan은 드롭 가능하다

40-CONTEXT D-06: *"개발 블로킹이 아니므로 0-4·0-5가 위태로우면 잘라내고 별도 Phase로 미뤄도 된다."*
0-4(40-01·40-03·40-04)와 0-5(40-02)가 완료됐고 여유가 있을 때만 착수한다.

## 🔴 이 plan은 40-01의 무변경 게이트를 **의도적으로 깬다**

40-01의 골든 14개는 `창원부동산랩` 문자열을 포함한다. 리브랜딩은 그 문자열을 바꾸므로
`templates-golden.test.mjs`가 **반드시 실패한다.** 이건 회귀가 아니라 **의도된 변경**이다.

→ 절차: **골든을 재생성하고, 재생성 전후 diff가 브랜드 문자열 줄에만 국한되는지 확인**한다.
   diff에 브랜드 외의 줄이 하나라도 있으면 **중단**한다. 이것이 이 plan의 안전장치다.

⛔ 골든 테스트를 삭제하거나 skip하지 않는다. **재생성하고 diff를 검토**한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-changbuletter-prereq/40-CONTEXT.md
@.planning/phases/40-changbuletter-prereq/40-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- 플래너가 2026-07-31에 저장소 전체를 grep한 실측. 착수 시 반드시 재실행해 갱신할 것. -->

**`창원부동산랩` 잔존 위치 — 플래너 실측 (`.next/` 빌드 산출물 제외)**

| 파일 | 행 | 내용 | 조치 |
|---|---|---|---|
| `src/lib/cardnews/card-templates.ts` | 47 | `<span …>창원부동산랩</span>` (BrandLockup) | 교체 ✅ ADR-004가 지목 |
| `src/lib/cardnews/card-templates.ts` | 359 | `'…<br>창원부동산랩을 팔로우하세요.'` | 교체 ✅ ADR-004가 지목 |
| `card-news/scripts/templates.js` | 90 | `BrandLockup` | 교체 🔴 **ADR-004가 못 짚은 4건 중 1** |
| `card-news/scripts/templates.js` | 98 | `BrandLockupPreview` | 교체 🔴 |
| `card-news/scripts/templates.js` | 396 | `renderClosing` desc | 교체 🔴 |
| `card-news/scripts/templates.js` | 613 | `renderClosingPreview` desc | 교체 🔴 |
| `card-news/scripts/generate.js` | 141 | `console.log('\n창원부동산랩 카드뉴스 생성')` | 교체 (로그) |
| `card-news/package.json` | 4 | `"description"` | 교체 (메타) |

→ ADR-004는 **2곳**만 알고 있었다. 실제는 **8곳**이다. ADR의 경고가 옳았다.
`.next/server/…/route.js` 2건은 **빌드 산출물**이므로 손대지 않는다 (재빌드 시 자동 갱신).

**비율 — `src/lib/cardnews/card-templates.ts` 의 `1080` 실측 (정확히 6곳)**
```
:24   html, body { width: 1080px; height: 1080px; overflow: hidden; }
:118  .card { width:1080px; height:1080px; background:#fff; … }
:169  .card { width:1080px; height:1080px; background:var(--surface-2); … }
:256  .card { width:1080px; height:1080px; background:#fff; … }
:309  .card { width:1080px; height:1080px; background:var(--surface-2); … }
:362  .card { width:1080px; height:1080px; background:var(--ink); … }
```
🔴 각 줄에 `1080`이 **두 번** 나온다 (width, height). **height 만** 1350으로 바꾼다.
width는 1080 그대로다. ADR-004 §3 "총 6곳"은 **줄 기준**이다.
⛔ 무분별한 `sed 's/1080/1350/g'` 를 쓰면 width까지 바뀐다 — 절대 금지.

**`card-news/scripts/templates.js` 는 이미 1350이다** (`:35`, `:58`, 각 `.card`) — **비율 변경 대상 아님.**

**골든 하네스 (40-01)**
- `card-news/fixtures/snapshot-golden.json` — 4시리즈 data 동결본
- `card-news/fixtures/golden/**` — HTML **14개**
- `card-news/scripts/templates-golden.test.mjs` — 스냅샷 → 렌더 → 골든 바이트 대조
- 재생성: `node scripts/generate.js --data=fixtures/snapshot-golden.json --dry-run --out=fixtures/golden`

🔴 골든 14개 중 브랜드 문자열이 들어가는 것:
`01-cover`(BrandLockup) × 3, `04-closing`(BrandLockup + desc) × 3,
`district-champions/01-grid`(BrandLockup), `district-champions/02-closing`.
`02-highlight`·`03-ranking`에는 **브랜드 문자열이 없다** → 그 6개는 **바이트 무변경**이어야 한다.
이것이 강력한 대조 기준이다.

**어드민 빌더 (`src/lib/cardnews/card-templates.ts`) 사용처**
`src/app/api/admin/cardnews/generate-html/route.ts` 등. 템플릿 **5종**을 렌더한다
(ADR-004 Consequences: *"변경 후 기존 템플릿 5종을 전부 렌더 확인할 것"*).
🔴 1:1 → 4:5 는 세로가 **25% 늘어난다.** `justify-content`·`margin-top:auto` 기반 레이아웃은
대개 잘 늘어나지만, 절대 위치(`position:absolute; bottom:…`) 요소는 **간격이 벌어진다.**
자동 검증이 불가능하므로 Task 3에서 사람이 본다.

**브랜드 문자열 — 대체값**
`창원부동산랩` → `창부레터` (ADR-004 §4, 뷰어 `cbl-article.jsx:50`의 `창부레터` 와 일치)
⚠️ 인스타 계정(`@cb_letter`) 통일은 **코드가 아니라 계정 설정**이다 — 이 Phase 범위 밖.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 착수 전 전수 grep 재실행 + 리브랜딩 8곳 + 골든 재생성·diff 검토</name>

  <files>
    src/lib/cardnews/card-templates.ts
    card-news/scripts/templates.js
    card-news/scripts/generate.js
    card-news/package.json
    card-news/fixtures/golden/*
  </files>

  <read_first>
    - `changbuletter/docs/adr/ADR-004-cardnews-viewer-and-branding.md` **§4 + Consequences**
    - `src/lib/cardnews/card-templates.ts:40-55`, `:350-365` — 브랜드 문자열 2곳의 문맥
    - `card-news/scripts/templates.js:85-101`, `:390-400`, `:608-618` — 브랜드 문자열 4곳
    - `.planning/phases/40-changbuletter-prereq/40-01-SUMMARY.md` — 골든 하네스 사용법
  </read_first>

  <action>
  **(1) 🔴 착수 전 전수 grep 재실행 — ADR-004가 명시적으로 요구한 것**

  ```
  cd /c/Users/jung/coding/bds
  grep -rn "창원부동산랩" src/ card-news/scripts/ card-news/package.json
  grep -rn "창원부동산랩" .github/workflows/ e2e/ scripts/ 2>/dev/null
  ```
  🔴 위 `<interfaces>` 표(8곳)와 **대조**한다. 새 위치가 나오면 표를 갱신하고 함께 처리한다.
  ⛔ `.next/`·`node_modules/`·`.planning/`·`docs/`는 대상이 아니다
     (빌드 산출물 / 의존성 / 역사 기록).
  결과를 SUMMARY에 표로 남긴다 — "ADR이 2곳을 알았고 실제는 N곳이었다"가 이 태스크의 기록 가치다.

  **(2) 리브랜딩 — 발견된 모든 위치에서 `창원부동산랩` → `창부레터`**

  ⛔ `sed -i` 일괄 치환을 쓰지 않는다. 파일별로 문맥을 보고 교체한다.
  ⛔ 이 교체 외에 어떤 줄도 고치지 않는다. 폰트·색·간격 조정 금지.
  ⚠️ `card-news/package.json`의 `"description"`은 **동작에 영향이 없다** — 그래도 브랜드
     일관성을 위해 교체한다. `"name": "bds-card-news"` 는 **바꾸지 않는다**
     (`package-lock.json` 과 CI 캐시 키에 영향).

  **(3) 확인 — 잔존 0건**
  ```
  grep -rn "창원부동산랩" src/ card-news/scripts/ card-news/package.json ; echo "exit=$?"
  ```
  → 매치 없음(`exit=1`)이어야 한다.

  **(4) 🔴 골든 재생성 + diff 범위 검토 — 이 태스크의 안전장치**

  ```
  cd card-news
  cp -r fixtures/golden /tmp/p40-golden-before
  node scripts/generate.js --data=fixtures/snapshot-golden.json --dry-run --out=fixtures/golden
  diff -r /tmp/p40-golden-before fixtures/golden > /tmp/p40-golden.diff ; cat /tmp/p40-golden.diff
  ```

  판정 (전부 만족해야 통과):
  - 🔴 **브랜드 외 변경 0줄** — 기계적으로 판정한다. `diff -r` 은 헝크 헤더(`12c12`)·구분선(`---`)·
    `Only in …` 줄도 뱉으므로 *"모든 줄이 브랜드 문자열을 포함"* 은 **문자 그대로는 성립할 수 없다.**
    실제 내용 줄(`<`/`>`)만 뽑아서 본다:
    ```
    diff -r /tmp/p40-golden-before fixtures/golden | grep '^[<>]' | grep -Ev '창원부동산랩|창부레터'
    ```
    🔴 **이 명령의 출력이 비어야 한다.** 한 줄이라도 나오면 **즉시 중단**하고 그 줄을 그대로 보고한다.
    이것이 *의도된 변경*과 *무관한 회귀*를 가르는 유일한 게이트다.
  - 🔴 `diff -r … | grep '^Only in'` 도 **빈 출력**이어야 한다 (파일이 생기거나 사라지면 안 된다).
  - 🔴 `02-highlight.html`·`03-ranking.html` **6개 파일은 diff에 나타나지 않는다**
    (브랜드 문자열이 없는 카드다). 나타나면 뭔가 잘못됐다.
  - 변경된 파일 수는 **8개** 예상 (`01-cover` × 3, `04-closing` × 3,
    `district-champions/01-grid`, `district-champions/02-closing`). 다르면 사유를 규명한다.

  `/tmp/p40-golden.diff` 전문을 SUMMARY에 인용한다.

  **(5) 골든 테스트 재통과**
  ```
  node scripts/templates-golden.test.mjs        # 14 passed (재생성된 골든 기준)
  node scripts/build-slides.test.mjs            # 21 passed — containment 4건 유지
  node scripts/templates.test.mjs               # D-08 법적 고지 등 회귀 0
  node scripts/persist-contents.test.mjs        # 12 passed
  ```
  🔴 `build-slides.test.mjs`의 containment **4건**은 `fixtures/golden/**`를 읽는다.
  브랜드가 바뀌어도 `big`·`sub`는 랭킹 데이터라 **영향이 없어야 한다.** 깨지면
  리브랜딩이 데이터 렌더 부분까지 건드린 것이다 — 중단하고 보고한다.

  **(6) 커밋**
  ```
  cd /c/Users/jung/coding/bds
  npm run lint
  git add src/lib/cardnews/card-templates.ts card-news/scripts/templates.js \
          card-news/scripts/generate.js card-news/package.json card-news/fixtures/golden
  git commit -m "feat(cardnews): 카드 템플릿 리브랜딩 창원부동산랩 → 창부레터 (두 코드베이스)"
  ```
  ⛔ `git push` 하지 않는다.
  ⛔ 비율 변경은 Task 2다. 한 커밋에 섞지 않는다 — 섞으면 골든 diff의 원인이 흐려진다.
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && ! grep -rq "창원부동산랩" src/ card-news/scripts/ card-news/package.json && cd card-news && node scripts/templates-golden.test.mjs && node scripts/build-slides.test.mjs && node scripts/templates.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - 🔴 착수 전 전수 grep 결과가 표로 SUMMARY에 있음. `<interfaces>` 표(8곳)와 대조·갱신됨
    - `grep -rn "창원부동산랩" src/ card-news/scripts/ card-news/package.json` **매치 0건**
    - `grep -rc "창부레터" card-news/scripts/templates.js` == 4, `src/lib/cardnews/card-templates.ts` == 2
    - `card-news/package.json`의 `"name"` **무변경** (`git diff` 에 name 줄 미출현)
    - 🔴 `/tmp/p40-golden.diff` 전문이 SUMMARY에 인용됨
    - 🔴 **`diff -r … | grep '^[<>]' | grep -Ev '창원부동산랩|창부레터'` 출력이 빈 것**을 명령과 함께 인용.
      ⛔ "모든 줄이 브랜드 문자열을 포함" 같은 문자 그대로 성립 불가한 판정을 쓰지 않는다
      (`diff` 는 `12c12`·`---`·`Only in …` 도 출력한다)
    - 🔴 `diff -r … | grep '^Only in'` **빈 출력** (파일 생성·삭제 0건)
    - `02-highlight`·`03-ranking` **6개 파일이 diff에 미포함**
    - 변경된 골든 파일 수 **8개** (다르면 사유 규명)
    - `node card-news/scripts/templates-golden.test.mjs` **14 passed**
    - `node card-news/scripts/build-slides.test.mjs` **21 passed** (containment 4건 유지)
    - `node card-news/scripts/templates.test.mjs` 회귀 0 (D-08 법적 고지 유지)
    - 🔴 `grep -c "1920" src/lib/cardnews/card-templates.ts card-news/scripts/templates.js` == 0 (9:16 미신설)
    - `npm run lint` exit 0 / `git status --porcelain supabase/` 빈 출력
    - `git status --porcelain card-news/output/` 빈 출력
  </acceptance_criteria>

  <done>
    두 카드 템플릿 코드베이스 어디에도 `창원부동산랩`이 남아 있지 않고, 그로 인한 HTML 변경이
    브랜드 문자열 줄에만 국한됨이 골든 diff로 실증됐다.
  </done>
</task>

<task type="auto">
  <name>Task 2: card-templates.ts 비율 1080 → 1350 (height만, 6줄)</name>

  <files>
    src/lib/cardnews/card-templates.ts
  </files>

  <read_first>
    - `src/lib/cardnews/card-templates.ts` **:24, :118, :169, :256, :309, :362** — 변경 대상 6줄
    - `card-news/scripts/templates.js:35`, `:58` — **이미 1350인 참조 구현**.
      `html, body { width: 1080px; height: 1350px; … }` 형태를 그대로 맞춘다
    - `changbuletter/docs/adr/ADR-004…md` **§3 표 + Consequences 3번째 항목**
  </read_first>

  <action>
  **(1) 변경 전 실측 — 정확히 6줄인지 확인**
  ```
  grep -n "1080px" src/lib/cardnews/card-templates.ts
  grep -c "1080px" src/lib/cardnews/card-templates.ts
  ```
  🔴 6줄, 각 줄에 `1080px` 2회 → `grep -c` 는 **6**(줄 수), `grep -o … | wc -l` 은 **12**(출현 수).
  실측값이 다르면 표를 갱신하고 보고한다.

  **(2) height만 교체 — 6줄**

  각 줄에서 **`height:1080px` / `height: 1080px` 만** `1350px` 로 바꾼다.
  `width` 는 **1080 그대로**다.

  ⛔ `sed 's/1080/1350/g'` 절대 금지 — width까지 바뀌어 카드가 정사각형 1350×1350이 된다.
  ⛔ 다른 CSS 값(padding·font-size·gap)을 **조정하지 않는다.** ADR-004는 비율만 요구했다.
     레이아웃이 어색해도 이 태스크에서 손대지 않고 Task 3 체크포인트에서 사용자에게 보고한다.
     🔴 "보기 좋게 다듬었다"는 범위 확대다.

  **(3) 확인**
  ```
  grep -n "1080px" src/lib/cardnews/card-templates.ts     # width만 6줄에 남아야 한다
  grep -c "1350px" src/lib/cardnews/card-templates.ts     # 6
  grep -n "height:\s*1080px" src/lib/cardnews/card-templates.ts   # 0건
  grep -c "1920" src/lib/cardnews/card-templates.ts       # 0 — 9:16 미신설
  ```

  **(4) 렌더 스모크 — 자동 가능한 만큼만**
  ```
  npm run lint            # tsc 포함
  npm run build           # 어드민 라우트가 이 모듈을 import한다 — 빌드 실패 여부 확인
  ```
  🔴 빌드가 실패하면 중단한다. 레이아웃 육안 확인은 Task 3이다.

  **(5) 커밋**
  ```
  git add src/lib/cardnews/card-templates.ts
  git commit -m "feat(cardnews): 어드민 빌더 카드 비율 1:1 → 4:5 통일 (1080x1350)"
  ```
  ⛔ `git push` 하지 않는다.
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && test "$(grep -c '1350px' src/lib/cardnews/card-templates.ts)" = "6" && test "$(grep -c 'height:\s*1080px' src/lib/cardnews/card-templates.ts || true)" = "0" && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - 변경 전 `grep -n "1080px"` 출력(6줄)이 SUMMARY에 인용됨
    - `grep -c "1350px" src/lib/cardnews/card-templates.ts` == **6**
    - `grep -c "height:\s*1080px"` == **0** / `width:1080px` 은 6줄에 **잔존**
    - 🔴 `grep -c "1920" src/lib/cardnews/card-templates.ts` == **0** (9:16 미신설, ADR-004 §3)
    - `git diff HEAD~1 --numstat src/lib/cardnews/card-templates.ts` 추가 **6줄** / 삭제 **6줄**
      — 6줄 초과면 범위 확대다
    - padding·font-size·gap 등 다른 CSS 값 **무변경** (diff 육안 확인)
    - `card-news/scripts/templates.js` **무변경** (이미 1350)
    - `npm run lint` exit 0 / `npm run build` exit 0
    - `git status --porcelain supabase/` 빈 출력
  </acceptance_criteria>

  <done>
    어드민 빌더 카드가 1080×1350 (4:5)이고, 변경이 정확히 6줄의 height 값뿐이며,
    9:16 템플릿은 만들어지지 않았다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] 어드민 빌더 템플릿 5종 육안 확인 (1:1 → 4:5 레이아웃 붕괴)</name>

  <files>(파일 변경 없음 — 육안 확인·보고 전용)</files>

  <read_first>
    - `changbuletter/docs/adr/ADR-004…md` **Consequences 3번째 항목** —
      *"기존에 1:1로 생성한 카드가 있다면 레이아웃이 깨질 수 있다. 변경 후 기존 템플릿 5종을 전부 렌더 확인할 것"*
    - `src/app/admin/` 아래 카드뉴스 빌더 진입 경로 (어느 URL인지 실행 전 확인)
    - Task 1의 골든 diff 결과 + Task 2의 6줄 diff
  </read_first>

  <what-built>
    Task 1이 두 코드베이스에서 `창원부동산랩` → `창부레터` 교체를 완료하고 그 영향이 브랜드
    문자열 줄에만 국한됨을 골든 diff로 실증했다. Task 2가 `card-templates.ts`의 카드 높이를
    1080 → 1350으로 바꿨다(6줄). **세로가 25% 늘어난 레이아웃은 자동으로 검증할 수 없다** —
    이 태스크는 사람이 본다.
  </what-built>

  <how-to-verify>
    ### A. 어드민 빌더 렌더 확인 (템플릿 5종)

    1. `localhost:3000` 상태 확인 → 미실행이면 `npm run dev`
    2. 어드민 카드뉴스 빌더 페이지로 이동 (경로는 실행 전 `src/app/admin/` 에서 확인해 보고)
    3. 🔴 **템플릿 5종을 전부** 미리보기 렌더한다. 각각에서 확인:

    | 확인 항목 | 통과 기준 |
    |---|---|
    | 카드 비율 | 세로가 가로보다 길다 (4:5) |
    | 브랜드 문자열 | **창부레터** 로 표시. `창원부동산랩` 0건 |
    | 상단/하단 요소 | 잘리거나 카드 밖으로 넘치지 않음 |
    | 세로 여백 | 늘어난 250px가 어색하게 몰려 있지 않음 (몰려 있으면 **그대로 보고**) |
    | 텍스트 겹침 | 없음 |
    | 법적 고지(D-08) | 출처·면책 문구가 여전히 보임 |

    🔴 어긋난 항목은 **스크린샷 또는 구체적 서술**과 함께 보고한다.
    🔴 **실행자가 임의로 CSS를 조정하지 않는다.** ADR-004는 비율만 요구했다.
       조정이 필요하면 그것은 **별도 작업**이며 사용자 승인 사항이다.

    ### B. 주간 자동화 산출물 확인 (선택 — PNG 1장)

    `card-news/scripts/templates.js` 는 비율이 안 바뀌었고 브랜드만 바뀌었다.
    실제 PNG로 확인하려면:
    ```
    cd card-news
    node scripts/generate.js --data=fixtures/snapshot-golden.json --series=84-seongsan --out=/tmp/p40-brand-png
    ```
    ⚠️ `--dry-run` 없이 돌리면 Puppeteer가 뜬다(느림). 시간이 없으면 건너뛰고
    "미확인"으로 표기한다. **"괜찮을 것"이라고 쓰지 않는다.**

    ### C. 0-6 Success Criteria 대조

    | # | 40-CONTEXT SC10 | 증거 |
    |---|---|---|
    | a | `card-templates.ts` 가 1350 | Task 2 — `grep -c "1350px"` == 6 |
    | b | 두 파일에 `창원부동산랩` 잔존 **0건** | Task 1 — grep 매치 0 |
    | c | 9:16 템플릿 미신설 | `grep -c "1920"` == 0 |
    | d | 어드민 템플릿 5종 렌더 확인 | **이 태스크 A** |
    | e | 리브랜딩 외 HTML 무변경 | Task 1 — 골든 diff 범위 |

    ### D. 사용자에게 묻는 것

    **D-1.** 어드민 빌더 5종 중 레이아웃이 어색해진 것이 있다면, 지금 조정할지
    (= 이 Phase 범위 확대) 아니면 별도 작업으로 미룰지.

    **D-2.** 배포 여부. `git push` 하지 않았으므로 어드민 빌더 비율 변경과 리브랜딩은
    **아직 프로덕션에 반영되지 않았다.** 다음 주간 자동 생성분도 여전히 `창원부동산랩` 브랜드로 나간다.

    ### E. 최종 재실행
    ```
    npm run lint
    npm run build
    npx supabase migration list --linked
    node card-news/scripts/templates-golden.test.mjs
    node card-news/scripts/build-slides.test.mjs
    node card-news/scripts/templates.test.mjs
    ```
  </how-to-verify>

  <action>
  A~E를 순서대로 수행해 보고하고 승인을 받는다.
  🔴 템플릿 5종을 **전부** 본다. "1종 확인했고 나머지도 같을 것"이라고 쓰지 않는다.
  🔴 CSS를 임의 조정하지 않는다 — D-1에서 사용자가 결정한다.
  🔴 확인하지 못한 항목은 **"미확인"** 으로 표기한다.
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && npm run lint && npm run build && npx supabase migration list --linked</automated>
  </verify>

  <acceptance_criteria>
    - 어드민 빌더 **5종 전부**에 대한 확인 결과가 표로 보고됨. "나머지도 같을 것" 표현 0건
    - 각 템플릿에서 `창부레터` 표시 확인, `창원부동산랩` 0건
    - 레이아웃 이상이 있으면 구체적 서술/스크린샷과 함께 보고. 임의 CSS 조정 **0건**
    - 0-6 Success Criteria 표 a~e가 증거와 함께 작성됨. 증거 없는 "충족" 0건
    - B(PNG 확인)를 건너뛰었으면 **"미확인"** 명시
    - 🔴 D-1·D-2 두 질문이 사용자에게 제시됨
    - D-2에 "push 전까지 다음 주간 생성분은 구 브랜드로 나간다"가 명시됨
    - `npm run lint` exit 0 / `npm run build` exit 0 / `migration list --linked` 0/0
    - 테스트 3종 통과 숫자 기록
    - "아마도"·"괜찮을 것"·"문제없어 보임" 류 표현 **0건**
  </acceptance_criteria>

  <done>
    어드민 빌더 템플릿 5종이 4:5 비율과 창부레터 브랜드로 렌더됨을 사람이 확인했고,
    레이아웃 이상 여부와 배포 여부가 사용자에게 사실 그대로 제시돼 결정됐다.
  </done>

  <resume-signal>
    5종 확인 표와 0-6 Success Criteria 표를 확인한 뒤,
    D-1(레이아웃 조정 지금/나중) · D-2(배포 여부) 두 답을 함께 입력.
    레이아웃 이상이 있으면 어느 템플릿의 어느 부분인지 그대로.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브랜드 문자열 교체 → 렌더 HTML | 문자열 하나가 아니라 템플릿 함수를 건드리므로 의도치 않은 변경이 섞일 수 있다 |
| 1:1 → 4:5 → 어드민 빌더 5종 | 자동 검증 불가. **사람이 안 보면 아무도 모른다** |
| 40-01 골든 게이트 → 이 plan | 게이트를 의도적으로 깨는 유일한 plan. 무력화 유혹이 있다 |
| 일괄 치환(`sed`) → CSS 값 | `1080` 은 width에도 쓰인다. 무분별한 치환이 카드를 정사각형으로 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T-40-05-01 | Tampering | 리브랜딩이 브랜드 외 HTML까지 바꿈 | mitigate | 🔴 `diff -r … | grep '^[<>]' | grep -v '창원부동산랩\|창부레터'` **빈 출력**으로 기계 판정. ("모든 줄이 브랜드 포함" 은 헝크 헤더·`Only in` 때문에 문자 그대로 성립 불가하다.) 추가로 `grep '^Only in'` 빈 출력 + `02-highlight`·`03-ranking` 6개 파일 미포함 |
| T-40-05-02 | Repudiation | 잔존 브랜드 위치를 또 놓침 (1차 감사가 코드베이스 하나를 통째로 놓쳤다) | mitigate | 착수 전 전수 grep **재실행 필수**. 플래너 실측 8곳 표와 대조. ADR-004가 알던 2곳의 4배다 |
| T-40-05-03 | Tampering | `sed 's/1080/1350/g'` 가 width까지 바꿈 | mitigate | 🔴 일괄 치환 금지 명시 + `width:1080px` 6줄 잔존 단언 + diff 추가/삭제 **각 6줄** 상한 |
| T-40-05-04 | Repudiation | 골든 테스트를 삭제·skip해 "통과"로 만듦 | mitigate | ⛔ 삭제·skip 금지 명시. **재생성 + diff 검토**가 유일한 경로. 14 passed 재확인 |
| T-40-05-05 | Denial of Service | 4:5 전환으로 어드민 빌더 레이아웃 붕괴 | mitigate | Task 3 blocking 체크포인트에서 **5종 전부** 육안 확인. ADR-004 Consequences가 명시적으로 요구 |
| T-40-05-06 | Tampering | "보기 좋게" CSS를 함께 조정해 범위가 번짐 | mitigate | ⛔ 조정 금지. 이상은 보고만 하고 D-1에서 사용자가 결정. diff 6줄 상한이 기계적 방어 |
| T-40-05-07 | Tampering | 9:16 템플릿을 "있으면 좋으니" 추가 | mitigate | ADR-004 §3이 **폐기 결정**. `grep -c "1920"` == 0 단언 |
| T-40-05-08 | Repudiation | "리브랜딩 완료"라고 쓰지만 미배포 | mitigate | `git push` 미실행 = 다음 주간 생성분은 구 브랜드. D-2 질문 + acceptance가 명시를 강제 |
| T-40-05-09 | Tampering | `package.json`의 `name` 변경으로 lock/CI 캐시 깨짐 | mitigate | `"name"` 무변경 명시 + `git diff` 에 name 줄 미출현 단언 |
| T-40-05-SC | Tampering | npm/pip/cargo installs | N/A | 신규 의존성 **0건** |
</threat_model>

<verification>
1. 착수 전 전수 grep 재실행 결과 표 (플래너 실측 8곳과 대조)
2. `grep -rn "창원부동산랩" src/ card-news/scripts/ card-news/package.json` 매치 **0건**
3. 골든 재생성 diff — 🔴 `diff -r … | grep '^[<>]' | grep -Ev '창원부동산랩|창부레터'` **빈 출력** +
   `grep '^Only in'` **빈 출력** + `02-highlight`·`03-ranking` 6개 파일 미포함 + 변경 파일 **8개**
4. `templates-golden.test.mjs` **14 passed** / `build-slides.test.mjs` **20 passed** /
   `templates.test.mjs` 회귀 0 / `persist-contents.test.mjs` **12 passed**
5. `grep -c "1350px" src/lib/cardnews/card-templates.ts` == **6**
6. `grep -c "height:\s*1080px"` == **0**, `width:1080px` 6줄 잔존
7. `git diff --numstat src/lib/cardnews/card-templates.ts` 추가 6 / 삭제 6
8. `grep -c "1920"` == 0 (두 파일 모두)
9. `card-news/package.json` `"name"` 무변경
10. 어드민 빌더 **5종 전부** 육안 확인 표
11. `npm run lint` exit 0 / `npm run build` exit 0 / `migration list --linked` 0/0
12. `git status --porcelain supabase/ card-news/output/` 빈 출력
13. `git push` 미실행 — 미배포 사실이 SUMMARY에 명시
</verification>

<success_criteria>
1. 두 카드 템플릿 코드베이스에 `창원부동산랩` 잔존 **0건**
2. `src/lib/cardnews/card-templates.ts` 가 1080×1350 (4:5)
3. 리브랜딩으로 인한 HTML 변경이 **브랜드 문자열 줄에만** 국한됨이 골든 diff의
   `grep '^[<>]' | grep -v '브랜드'` 빈 출력으로 실증됨
4. 9:16 템플릿이 만들어지지 않았다
5. 어드민 빌더 템플릿 5종이 사람 확인을 통과했다 (또는 이상이 구체적으로 보고됐다)
6. 골든 게이트가 삭제·skip 없이 재생성으로 통과했다
7. lint / build exit 0 / 마이그레이션 0건
8. 미배포 사실이 정직하게 기록됐다
</success_criteria>

<output>
Create `.planning/phases/40-changbuletter-prereq/40-05-SUMMARY.md` when done.

SUMMARY에 반드시 포함:
- 🔴 착수 전 전수 grep 결과 표 — "ADR-004는 2곳을 알았고 실제는 N곳이었다"
- 🔴 골든 재생성 `diff -r` **전문** + 판정 4가지
  ① `grep '^[<>]' | grep -Ev '창원부동산랩|창부레터'` **빈 출력** (명령 그대로 인용)
  ② `grep '^Only in'` **빈 출력** ③ highlight·ranking 6개 미포함 ④ 변경 파일 8개
- `card-templates.ts` 변경 전 `grep -n "1080px"` 6줄 + 변경 후 `grep -c "1350px"` == 6
- `git diff --numstat src/lib/cardnews/card-templates.ts` (추가 6 / 삭제 6)
- 테스트 4종 결과 숫자
- **어드민 빌더 5종 육안 확인 표** — 이상이 있으면 구체 서술
- 0-6 Success Criteria a~e 대조 표 (증거 출처 포함)
- 체크포인트 D-1·D-2의 **사용자 답 그대로**
- **이월 항목**:
  ① 🔴 **미배포** — `git push` 미실행. 다음 주간 생성분은 여전히 구 브랜드로 나간다
  ② 인스타 계정(`@cb_letter`) 통일은 **계정 설정**이라 코드 범위 밖 (ADR-004 §4)
  ③ 어드민 빌더 레이아웃 조정이 필요하다면 **별도 작업** — D-1의 사용자 답을 그대로 기록
  ④ `.next/` 빌드 산출물의 구 브랜드 문자열 2건은 재빌드 시 자동 갱신 — 손대지 않았다
</output>
