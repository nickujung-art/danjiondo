---
phase: 40-changbuletter-prereq
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/lib/db/onconflict-audit.ts
  - src/lib/db/onconflict-audit.test.ts
  - src/__tests__/onconflict-constraint-gate.test.ts
  - card-news/scripts/persist-contents.js
  - card-news/scripts/persist-contents.test.mjs
  - card-news/scripts/generate.js
autonomous: true
requirements: [CBL-0-4b]

must_haves:
  truths:
    - "card-news/scripts 의 upsert 지점도 Phase 39 게이트의 감사 범위에 들어온다 — 신규 사이트가 게이트를 그냥 빠져나가지 않는다"
    - "contents 의 새 onConflict 가 실제 카탈로그의 비부분 UNIQUE 와 일치함이 EXPLAIN INSERT 로 사전 확인된다"
    - "같은 회차를 두 번 적재해도 contents 행이 중복되지 않는다 (멱등)"
    - "body.slides[*] 가 kicker·big·label·sub 정확히 4필드를 갖는다"
    - "실데이터가 없는 시리즈는 아예 적재되지 않는다 — placeholder 가 웹에 발행되지 않는다"
    - "--persist 없이 실행하면 DB 에 아무것도 쓰지 않는다"
  artifacts:
    - path: "card-news/scripts/persist-contents.js"
      provides: "buildContentRow(seriesId,data,to) + persistContents(rows) — contents upsert(onConflict slug)"
      exports: ["buildContentRow", "persistContents"]
    - path: "src/lib/db/onconflict-audit.ts"
      provides: "스캔 확장 — .js/.mjs 수집 + repo 루트 기준 다중 루트"
      contains: "mjs"
  key_links:
    - from: "card-news/scripts/persist-contents.js"
      to: "public.contents"
      via: "upsert onConflict 'slug'"
      pattern: "onConflict"
    - from: "src/__tests__/onconflict-constraint-gate.test.ts"
      to: "card-news/scripts"
      via: "collectUpsertSites 추가 루트"
      pattern: "card-news"
---

<objective>
0-4의 나머지 절반. 40-01이 만든 `buildSlides()` 결과를 `contents` 레코드로 적재한다.

Purpose:
- `contents`는 현재 **0행**이다(플래너 실측). 창부레터 홈 히어로와 콘텐츠 상세 카드뉴스가
  전부 빈 화면인 원인이 이것이다 (ADR-004 §2).
- 🔴 **Phase 39가 남긴 교훈이 이 plan의 절반이다.** 새 `onConflict`를 쓰는데, Phase 39의
  고장 4건이 **전부** "제약을 확인하지 않고 `onConflict`를 쓴 것"이었다. 그리고 Phase 39가
  세운 게이트는 `src/`만 스캔하므로 **`card-news/scripts/*.js`의 upsert를 못 본다** —
  즉 그냥 쓰면 게이트가 **공허하게 통과**한다. 게이트를 먼저 넓힌다.

Output: 게이트 확장 + `persist-contents.js` + 최근 1회차 실적재.
소급 3회차는 40-04 소관이다.

⛔ 이 plan은 **마이그레이션을 만들지 않는다.** `contents`는 Phase 36이 이미 만들었다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-changbuletter-prereq/40-CONTEXT.md
@.planning/phases/40-changbuletter-prereq/40-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- 전부 플래너 실측(2026-07-31). 프로덕션 직접 조회 포함. -->

**🟢 `contents`의 UNIQUE 인덱스 — 플래너가 `unique_indexes_for_table` RPC로 직접 확인**
```
contents_pkey      {id}     is_partial=false  is_expression=false  is_valid=true
contents_slug_key  {slug}   is_partial=false  is_expression=false  is_valid=true
```
→ **`onConflict: 'slug'`는 추론 가능하다.** (`20260730000002_cbl_content_schema.sql:11`
   `slug text not null unique` 가 만든 제약. **부분 인덱스가 아니다.**)
🔴 그래도 Task 2에서 `EXPLAIN INSERT ... ON CONFLICT` 를 **직접 실행해 재확인**한다 —
   Phase 39의 고장 4건이 전부 "파일을 믿고 카탈로그를 안 본 것"이었다.

**`contents` 스키마 (NOT NULL 인 것만)**
```
id uuid pk default gen_random_uuid()
site_id  text not null default 'changbuletter'     -- CHECK 없음 (실측)
slug     text not null UNIQUE
type     text not null check (type in ('card_news','article'))
category text not null
region_tags text[] not null default '{}'
title    text not null
status   text not null default 'draft' check (status in ('draft','scheduled','published'))
created_at / updated_at  not null default now()
```
nullable: `excerpt`, `body`(jsonb), `cover_image`, `read_minutes`, `scheduled_at`,
`published_at`, `cafe_post_url`, `vote_*`. `is_featured` not null default false.

**RLS (`20260730000003_cbl_content_rls.sql`)**
```
"contents: public read published"  select  to anon, authenticated
    using (status = 'published' and published_at <= now())
"contents: editor all"             all     to authenticated  (profiles.role in cbl_editor|superadmin)
```
→ 🔴 `published_at`이 **미래거나 NULL이면 anon에게 안 보인다.** 40-01의
   `publishedAt = ${to}T23:59:59+09:00`(과거 값)가 이 조건을 만족한다.
→ 쓰기 주체는 **service_role** — Supabase에서 RLS를 우회한다. `card-news/`는 서비스롤
   스탠드얼론이므로(`fetch-data.js:14-18`) 정책 추가가 필요 없다.

**현재 `contents` 사용처 (실측)**: `scripts/verify-cbl-rls.ts` **뿐**. 앱 코드에 `from('contents')` 없음.

**🔴 Phase 39 게이트의 사각지대 (실측 — 이 plan이 메워야 하는 것)**
`src/lib/db/onconflict-audit.ts:239`:
```ts
if (!/\.tsx?$/.test(entry.name)) continue    // ← .js/.mjs 를 통째로 건너뛴다
```
그리고 `src/__tests__/onconflict-constraint-gate.test.ts:91`:
```ts
const SRC_DIR = path.resolve(__dirname, '..')   // = <repo>/src  — card-news/ 를 안 본다
```
`src/lib/db/onconflict-audit.test.ts:258` 도 `path.resolve(__dirname,'../..')` = `<repo>/src`,
그리고 하한 상수 `MIN_SITES`(현재 17)로 스캐너 건전성을 단언한다.
→ **세 곳을 같이 고쳐야 한다.** 하나라도 빠지면 게이트가 조용히 새 사이트를 놓친다
   (Phase 39 위협등록부 T-39-03-02가 지목한 바로 그 실패 모드).

**게이트 모듈 계약 (`src/lib/db/onconflict-audit.ts`)**
```
toInferrable(rows: RawUniqueIndex[]): InferrableIndex[]        // is_partial|is_expression|!is_valid 제거
auditSites(sites, indexesByTable): AuditResult[]                // 컬럼 집합 완전 일치
collectUpsertSites(rootDir: string): UpsertSite[]               // 재귀 스캔, 주석 제거 후 매치
formatAuditTable(results): string
```
`collectUpsertSites`는 이미 `stripComments()`로 주석의 `onConflict` 를 걸러낸다.
정규식: `/onConflict\s*:\s*['"]([^'"]+)['"]/g`, 테이블은 역방향 최근접 `.from('<t>')`.
Phase 39 SUMMARY 기준 **라이브 대조 16/16 `ok`**, 단위 테스트 **20건**.

**`generate.js` 현재 상태 (40-01 적용 후)**
- 플래그: `--series` `--dry-run` `--from` `--to` `--month` `--out` `--dump-data` `--data`
- `generateCardSet(seriesId, data, dryRun)` 이 4장 렌더 (`:86-110`)
- district-champions는 `main()` 인라인 (`:199-226`)
- `buildSlides`/`buildChampionSlides`/`buildContentMeta`가 `./build-slides.js`에 있다 (40-01)

**⚠️ 주간 자동화 워크플로**: `.github/workflows/weekly-generate.yml` 이 매주 월 00:10 KST에
`node scripts/generate.js`를 인자 없이 실행한다(`SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY` 주입됨).
→ `--persist`를 여기에 붙이는 것은 **40-04 소관**이다. 이 plan에서 워크플로를 건드리지 않는다.

**card-news 테스트 관행**: 루트 vitest `include: ['src/**']` 밖. `node card-news/scripts/*.test.mjs` 수동 실행.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 39 게이트를 card-news/scripts 까지 확장 (새 사이트가 게이트를 빠져나가지 못하게)</name>

  <files>
    src/lib/db/onconflict-audit.ts
    src/lib/db/onconflict-audit.test.ts
    src/__tests__/onconflict-constraint-gate.test.ts
  </files>

  <read_first>
    - `src/lib/db/onconflict-audit.ts` **전문** — 특히 `walk()`(:224-244)의 확장자 필터,
      `toRepoRelative()`(:246-250), `collectUpsertSites()`(:262-299)
    - `src/lib/db/onconflict-audit.test.ts` **250-320행** — 실물 스캐너 건전성 케이스와 `MIN_SITES` 상수
    - `src/__tests__/onconflict-constraint-gate.test.ts` **전문** — `SRC_DIR`(:91),
      `gateNotReadyReason()`(:60-75), 전수 감사 테스트(:96-118)
    - `.planning/phases/39-onconflict-constraint-fix/39-03-PLAN.md` `<threat_model>` **T-39-03-02** —
      "스캐너가 조용히 지점을 놓침"이 주 실패 모드다
    - `.github/workflows/ci.yml` **44-49행** — `unit-test` 잡에 `env:` 블록이 없다.
      라이브 게이트는 CI에서 영구 skip된다는 사실을 눈으로 확인한다
  </read_first>

  <behavior>
  **`collectUpsertSites(rootDir)` 확장 — 파일 확장자**
  - 기존: `.ts` / `.tsx` (`.d.ts`·`.test.*`·`.spec.*` 제외)
  - 추가: `.js` / `.mjs` / `.cjs` (`.test.*`·`.spec.*` 제외)
  - 🔴 `SKIP_DIRS`에 `node_modules`가 이미 있다 — `card-news/node_modules` 는 자동 제외된다.
    `card-news/output`·`card-news/fixtures`도 추가로 제외한다 (스캔 낭비 + 골든 HTML 오탐 방지).
  - `stripComments()`는 언어 중립(JS 문법)이라 `.js`에도 그대로 동작한다. 수정 불필요.

  **스캔 루트 — 단일 진실 상수를 모듈에 둔다**
  ```
  export const AUDIT_ROOTS = ['src', 'card-news/scripts', 'scripts']
  export function collectAllUpsertSites(repoRoot: string): UpsertSite[]
  ```
  - `collectAllUpsertSites`는 각 루트에 `collectUpsertSites(path.join(repoRoot, root))`를 돌려 합친다
  - 🔴 **루트 목록을 세 파일에 흩어놓지 않는다.** Phase 39가 지목한 실패 모드가
    "한 군데만 고치고 다른 데는 안 고침"이다. 상수 하나 + 헬퍼 하나로 못 박는다.
  - `scripts`를 포함하는 이유: 저장소 루트의 `scripts/*.ts` 일회성 백필 스크립트에도
    upsert가 있을 수 있다. 실측으로 확인하고 결과를 SUMMARY에 남긴다.

  **단위 테스트 — `src/lib/db/onconflict-audit.test.ts` 에 케이스 **추가** (기존 20건 유지)**

  | # | 케이스 | 기대 |
  |---|---|---|
  | 21 | 🔴 임시 디렉터리의 `.mjs` 픽스처에서 upsert 지점을 수집한다 | 1건, 테이블 정확 |
  | 22 | 🔴 `.js` 픽스처의 **주석** `// onConflict — (a,b)` 는 수집하지 않는다 | 0건 |
  | 23 | 🔴 실물 `card-news/scripts` 스캔 결과에 `table === ''` 인 항목 **0건** | 실패 시 file:line 출력 |
  | 24 | 🔴 `collectAllUpsertSites(repoRoot)` 결과 ≥ `MIN_SITES` (기존 하한 유지) | `toBeGreaterThanOrEqual` |
  | 25 | 🔴 `AUDIT_ROOTS` 에 `'card-news/scripts'` 가 포함된다 | 명시 단언 — 루트 누락 회귀 방지 |

  🔴 케이스 23·25가 이 태스크의 핵심이다. 없으면 다음 사람이 루트를 지워도 아무도 모른다.
  기존 케이스 중 `MIN_SITES` 하한 단언은 **낮추지 않는다** (상한도 두지 않는다).

  **라이브 게이트 — `src/__tests__/onconflict-constraint-gate.test.ts`**
  - `SRC_DIR` → `REPO_ROOT = path.resolve(__dirname, '../..')` 로 바꾸고
    `collectAllUpsertSites(REPO_ROOT)` 를 쓴다
  - 나머지(`gateNotReadyReason`, `formatAuditTable`, 음성 대조)는 **그대로 둔다**
  - 테스트 이름을 `'src/ 의 …'` → `'저장소 전체의 …'` 로 갱신
  </behavior>

  <action>
  **(1) 실측 먼저** — 확장 전에 무엇이 잡히는지 본다:
  ```
  cd /c/Users/jung/coding/bds
  grep -rn "onConflict" card-news/scripts scripts --include=*.js --include=*.mjs --include=*.ts \
    | grep -v node_modules | grep -v '\.test\.'
  ```
  현재 기대: **0건**(40-03 이전이므로). 출력을 SUMMARY에 남긴다 — 이후 Task 2가 1건을 추가한다.

  **(2) 테스트 먼저** — 케이스 21~25를 `onconflict-audit.test.ts`에 추가한다.
  이 시점에 케이스 25(`AUDIT_ROOTS` 존재)와 21(`.mjs` 수집)이 **실패**해야 한다(RED).

  ⛔ 기존 20건을 재구성하지 않는다. **추가만** 한다.
  ⛔ 새 케이스에 `skipIf`를 쓰지 않는다 — 이 파일은 DB 없이 항상 실행되는 것이 존재 이유다
     (Phase 39 T-39-03-02). `ci.yml`에 `env:` 블록이 없어 라이브 게이트는 CI에서 영구 skip된다.

  **(3) 구현** — `onconflict-audit.ts`
  - `walk()`의 확장자 필터 확장 + `SKIP_DIRS`에 `output`·`fixtures` 추가
  - `AUDIT_ROOTS` + `collectAllUpsertSites` export 추가
  - `collectUpsertSites` 시그니처는 **유지**한다 (기존 20건이 그대로 통과해야 한다)
  - `toRepoRelative`는 `process.cwd()` 기준이라 `card-news/scripts/x.js` 형태로 잘 나온다 — 무변경

  **(4) 라이브 게이트 갱신** — `onconflict-constraint-gate.test.ts` 의 `SRC_DIR` → `REPO_ROOT`

  **(5) 실행**
  ```
  npx vitest run src/lib/db/onconflict-audit.test.ts      # 25 passed / 0 skipped
  npm run lint                                             # exit 0
  ```
  🔴 `TEST_SUPABASE_SKEY`를 **설정하지 않은 셸**에서 돌려 25건이 전부 실행되는지 확인한다.
  skip이 1건이라도 있으면 배치가 잘못된 것이다.

  라이브 대조는 Task 2에서 `contents` upsert를 만든 뒤 한 번에 돌린다 (지금 돌리면
  card-news에 upsert가 없어 결과가 Phase 39와 동일하다 — 그 사실만 확인하고 넘어가도 된다).

  **(6) 커밋**
  ```
  git add src/lib/db/onconflict-audit.ts src/lib/db/onconflict-audit.test.ts src/__tests__/onconflict-constraint-gate.test.ts
  git commit -m "fix(db): onConflict 감사 범위를 card-news/scripts·scripts 까지 확장 (.js/.mjs 포함)"
  ```
  ⛔ `git push` 하지 않는다.
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && npx vitest run src/lib/db/onconflict-audit.test.ts && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `onconflict-audit.ts`가 `AUDIT_ROOTS`·`collectAllUpsertSites` 를 export
    - `grep -c "card-news/scripts" src/lib/db/onconflict-audit.ts` ≥ 1
    - `walk()`가 `.js`·`.mjs`·`.cjs` 를 수집하고 `.test.*`·`.spec.*` 를 제외
    - `SKIP_DIRS`에 `output`·`fixtures` 추가
    - `npx vitest run src/lib/db/onconflict-audit.test.ts` → **25 passed / 0 skipped**
      (`TEST_SUPABASE_SKEY` 없는 셸에서도 동일)
    - `grep -c "skipIf" src/lib/db/onconflict-audit.test.ts` == 0
    - 🔴 케이스 25(`AUDIT_ROOTS` 에 `card-news/scripts` 포함) 존재 — 루트 누락 회귀 방지
    - `onconflict-constraint-gate.test.ts`가 `collectAllUpsertSites(REPO_ROOT)` 를 호출
      (`grep -c "collectAllUpsertSites" <file>` ≥ 1)
    - 기존 20건 무변경 — `git diff` 에서 삭제된 `it(` 블록 0개
    - `npm run lint` exit 0
    - `git status --porcelain supabase/` 빈 출력
  </acceptance_criteria>

  <done>
    Phase 39 게이트가 `card-news/scripts`의 `.js` upsert도 본다. 스캔 루트가 상수 하나에 모여 있고,
    그 상수에 `card-news/scripts`가 있다는 사실 자체가 DB 없이 실행되는 테스트로 고정됐다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: persist-contents.js + EXPLAIN INSERT 사전 검증 + --persist 플래그</name>

  <files>
    card-news/scripts/persist-contents.js
    card-news/scripts/persist-contents.test.mjs
    card-news/scripts/generate.js
  </files>

  <read_first>
    - `card-news/scripts/build-slides.js` (40-01 산출) — `buildSlides`·`buildChampionSlides`·`buildContentMeta`
    - `.planning/phases/40-changbuletter-prereq/40-01-SUMMARY.md` — D-07 재량 결정 3건
      (슬라이드 상한 5 / `big = item.price` / `publishedAt = to 23:59:59 KST`)
    - `card-news/scripts/fetch-data.js:1-18` — 서비스롤 클라이언트 생성 관용구
    - `supabase/migrations/20260730000002_cbl_content_schema.sql:9-33` — `contents` 컬럼·제약
    - `supabase/migrations/20260730000003_cbl_content_rls.sql:34-52` — `contents` RLS 2정책
    - 40-CONTEXT `<scope_fence>` **8번** — 새 `onConflict`는 `EXPLAIN INSERT` 로 먼저 검증
  </read_first>

  <behavior>
  **`buildContentRow(seriesId, data, to)` — 순수 함수, DB 비의존**

  `build-slides.js`의 `buildContentMeta`·`buildSlides`·`buildChampionSlides`를 조합해
  `contents` 행 하나를 만든다. `data.champions`가 있으면 champions 경로, 아니면 랭킹 경로.

  ```
  {
    site_id:  'changbuletter',
    slug:     meta.slug,                       // '2026-w24-84-seongsan'
    type:     'card_news',
    category: '주간실거래가',
    region_tags: [],                            // 📌 D-07: 비워둔다 (근거는 <action>)
    title:    meta.title,
    excerpt:  meta.excerpt,
    body:     { slides },                       // 🔴 { slides: [...] } — ADR-004 계약 그대로
    status:   'published',
    published_at: meta.publishedAt,
    is_featured: false,
  }
  ```
  - 🔴 `slides.length === 0` 이면 **`null`을 돌려준다.** 호출부가 그 시리즈를 건너뛴다.
    빈 아카이브 항목은 웹에서 "슬라이드 0장" 빈 화면이 된다 — 없느니만 못하다.
  - ⛔ `id`·`created_at`·`updated_at`을 넣지 않는다 (재실행 시 값이 흔들린다).
  - ⛔ `cover_image`·`read_minutes`·`cafe_post_url`·`vote_*`를 넣지 않는다 (스코프 밖).

  **`persistContents(client, rows)` → `{ upserted, skipped }`**
  - `rows`에서 `null`을 걸러낸다
  - `client.from('contents').upsert(rows, { onConflict: 'slug', ignoreDuplicates: false })`
    🔴 `onConflict: 'slug'` — `contents_slug_key`(비부분 UNIQUE, 플래너 실측)와 일치.
       Task (1)의 `EXPLAIN INSERT` 로 재확인한 뒤에만 쓴다.
  - `error`가 있으면 **던진다.** 조용히 삼키지 않는다 (Phase 39 F-03의 교훈).
  - 빈 배열이면 upsert를 호출하지 않고 `{upserted:0, skipped:n}`

  **`generate.js` — `--persist` 플래그 (opt-in)**
  - 없으면 **DB에 아무것도 쓰지 않는다.** 기본 동작 완전 무변경.
  - 📌 **왜 opt-in 인가 (D-07 재량 결정)**: 40-01의 무변경 실증과 40-04의 소급 작업이
    `--dry-run`을 **수십 번** 돌린다. 기본값이 on이면 그 실험이 전부 프로덕션 `contents`에
    발행 상태로 쓰인다. 주간 자동화에 `--persist`를 붙이는 것은 40-04에서 명시적으로 한다.
  - `--persist`가 있고 `--data`(스냅샷 모드)든 라이브든 상관없이 동작한다
  - 적재 대상 `to` 날짜는 `main()`이 이미 계산한 `to` 를 그대로 넘긴다
  - `--dump-data` 모드에서는 **적재하지 않는다** (렌더도 안 하는 모드다)
  - 실행 끝에 요약을 출력한다: `적재 N건 / 건너뜀 M건 (슬라이드 0장)`

  **테스트 (`persist-contents.test.mjs`, `templates.test.mjs` 러너 관용구, DB 불필요)**

  | # | 케이스 | 기대 |
  |---|---|---|
  | 1 | 랭킹 시리즈 행 형태 — 키 집합 | 위 11키 정확히 |
  | 2 | 🔴 `body`가 `{slides:[...]}` 이고 각 슬라이드가 **4키뿐** | `['big','kicker','label','sub']` |
  | 3 | 🔴 실데이터 0건 → `null` | `null` |
  | 4 | champions 시리즈 → `body.slides` 채워짐, title 분기 | `'구별 대장단지'` 포함 |
  | 5 | `slug` 소문자 + weekCode 접두 | `'2026-w24-84-seongsan'` |
  | 6 | `published_at` = `to` 23:59:59 KST | `'2026-06-20T23:59:59+09:00'` |
  | 7 | 🔴 `status === 'published'` · `site_id === 'changbuletter'` · `type === 'card_news'` | 각각 |
  | 8 | 🔴 `id`·`created_at`·`updated_at` 키 **부재** | `false` |
  | 9 | 🔴 `persistContents` 목 호출 인자의 `onConflict === 'slug'` | 문자열 일치 |
  | 10 | 🔴 upsert가 error를 돌려주면 **던진다** | `assert.rejects` |
  | 11 | `rows` 전부 null → upsert 미호출, `{upserted:0}` | 미호출 단언 |
  | 12 | 🔴 같은 입력 2회 → 두 행의 **모든 필드가 deepStrictEqual** (멱등의 근거) | 동일 |

  케이스 12가 멱등성(SC4)의 **구조적** 근거다. 같은 slug + 같은 내용 → upsert가 몇 번 돌아도
  결과 행이 하나이고 값도 안 바뀐다. 실제 DB 멱등은 (4)에서 라이브로 확인한다.
  </behavior>

  <action>
  **(1) 🔴 EXPLAIN INSERT 사전 검증 — 코드를 쓰기 전에 한다 (Scope Fence 8)**

  일회성 읽기 스크립트로 프로덕션에서 실행한다 (`EXPLAIN`은 **플래닝만** 하고 데이터를 쓰지 않는다):
  ```sql
  EXPLAIN INSERT INTO public.contents (site_id, slug, type, category, title, status)
  VALUES ('changbuletter', 'zz-p40-explain-probe', 'card_news', '주간실거래가', 'probe', 'draft')
  ON CONFLICT (slug) DO NOTHING;
  ```
  - 🔴 성공(플랜 출력) = 추론 가능. `42P10`(no unique or exclusion constraint matching) = 즉시 중단
  - 🔴 `EXPLAIN` 앞에 `ANALYZE`를 붙이지 않는다 — 붙이면 **실제로 INSERT 된다**
  - 실행 후 `select count(*) from public.contents where slug like 'zz-p40-%'` 로
    **잔여행 0건**을 확인한다 (Phase 39가 남긴 관행)
  - 플랜 출력 전문을 SUMMARY에 인용한다

  참고: 플래너가 `unique_indexes_for_table('contents')`로 이미 확인했다 —
  `contents_slug_key {slug} is_partial=false`. `EXPLAIN`은 그 확인의 **독립 2차 증거**다.

  **(2) 테스트 먼저** — `persist-contents.test.mjs` 에 12케이스. Supabase 클라이언트는
  최소 목(`{ from: () => ({ upsert: async (rows, opts) => {...} }) }`)으로 대체한다.
  ⛔ 실제 네트워크를 타지 않는다.

  **(3) 구현** — `persist-contents.js`
  - `import { buildSlides, buildChampionSlides, buildContentMeta } from './build-slides.js'`
  - `createClient`를 **이 파일에서 만들지 않는다** — `persistContents(client, rows)` 로 주입받는다
    (테스트 가능성 + `fetch-data.js`의 모듈 최상단 env 검증과의 결합 회피)
  - `generate.js`가 `fetch-data.js`의 클라이언트를 재사용할 수 없다면(export 안 됨),
    `persist-contents.js` 안에 **지연 생성 함수** `getClient()`를 두고 `--persist` 시에만 호출한다.
    env 검증 메시지는 `fetch-data.js:7-12` 형식을 따른다.

  📌 **`region_tags: []` 로 두는 이유 (D-07 재량 결정)**: 시리즈 정의의 `region`은
  `'창원 성산구'`·`'창원+김해'` 형태인데, ADR-002의 `region_tags`는 `'의창구'`·`'중동'` 같은
  **동/구 단위 태그**를 의도한다(SPEC-002 C절 "region_tags 초기 태그 목록"이 아직 미결).
  잘못된 태그를 넣으면 창부레터의 지역 추천(`&&` 교집합 인덱스)이 오작동한다.
  → **비워두고 이월**한다. 나중에 UPDATE로 채우는 편이 안전하다.

  **(4) `generate.js` 배선 + 라이브 1회차 적재**
  ```
  cd card-news
  # 드라이런으로 적재만 확인 (HTML 은 임시 디렉터리로)
  node scripts/generate.js --from=2026-06-21 --to=2026-06-27 --dry-run --persist --out=/tmp/p40-persist
  ```
  🔴 **`--out` 없이 돌리지 않는다** — `card-news/output/`의 과거 발행물을 덮는다.

  **멱등 확인 — 같은 명령을 한 번 더 돌린다:**
  ```
  node scripts/generate.js --from=2026-06-21 --to=2026-06-27 --dry-run --persist --out=/tmp/p40-persist2
  ```
  그리고 조회:
  ```sql
  select count(*) as total, count(distinct slug) as distinct_slug from public.contents;
  select slug, title, status, published_at,
         jsonb_array_length(body->'slides') as n_slides
  from public.contents order by slug;
  select body->'slides'->0 from public.contents order by slug limit 1;
  ```
  확인:
  - 🔴 `total === distinct_slug` (**SC4 멱등**)
  - 🔴 2회차 실행 후에도 행 수가 **1회차와 동일**
  - 🔴 `body->'slides'->0`의 키가 정확히 `kicker,big,label,sub` (**SC3**)
  - `n_slides` 가 전부 1 이상 (0장짜리가 적재되지 않음)
  - `published_at` = `2026-06-27T23:59:59+09:00` 계열

  **(5) 게이트 라이브 실행 — 새 `onConflict`가 실제로 통과하는지**
  ```
  TEST_SUPABASE_URL=<NEXT_PUBLIC_SUPABASE_URL> TEST_SUPABASE_SKEY=<SUPABASE_SERVICE_ROLE_KEY> \
    npx vitest run src/__tests__/onconflict-constraint-gate.test.ts
  ```
  🔴 감사 대상에 `card-news/scripts/persist-contents.js:<line> contents (slug)` 가 **나타나야** 한다.
  안 나타나면 Task 1의 루트 확장이 실패한 것이다 — 게이트가 공허하게 통과하고 있다.
  `verdict !== 'ok'` 0건 + **감사 표에 contents 행이 실재**하는 것을 둘 다 확인해 SUMMARY에 인용한다.
  실행하지 못했다면 "라이브 미검증" + 재실행 커맨드를 남긴다. **"통과 추정" 금지.**

  **(6) 테스트·lint·회귀**
  ```
  node card-news/scripts/persist-contents.test.mjs      # 12 passed
  node card-news/scripts/build-slides.test.mjs          # 20 passed (40-01)
  node card-news/scripts/templates-golden.test.mjs      # 14 passed — 🔴 HTML 무변경 재확인
  npx vitest run src/lib/db/onconflict-audit.test.ts    # 25 passed
  npm run lint
  ```
  루트 `npm run test` 실패 **이름 집합**을 `git stash push -u` 로 실측 비교한다
  (베이스라인 17은 flaky 17↔18 — 총계로 판정 금지).

  **(7) 커밋**
  ```
  git add card-news/scripts/persist-contents.js card-news/scripts/persist-contents.test.mjs card-news/scripts/generate.js
  git commit -m "feat(card-news): 슬라이드 데이터를 contents 로 적재 (--persist, onConflict slug)"
  ```
  ⛔ `git push` 하지 않는다.
  </action>

  <verify>
    <automated>cd /c/Users/jung/coding/bds && node card-news/scripts/persist-contents.test.mjs && node card-news/scripts/templates-golden.test.mjs && npx vitest run src/lib/db/onconflict-audit.test.ts && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - 🔴 `EXPLAIN INSERT ... ON CONFLICT (slug) DO NOTHING` **플랜 출력 전문**이 SUMMARY에 인용됨.
      `42P10` 아님. `EXPLAIN ANALYZE` 미사용
    - 프로브 잔여행 **0건** (`slug like 'zz-p40-%'`)
    - `node card-news/scripts/persist-contents.test.mjs` → **12 passed / 0 failed**
    - 🔴 케이스 2(슬라이드 4키)·3(0건 → null)·9(`onConflict==='slug'`)·10(error → throw)·12(멱등) 존재
    - `--persist` **없이** 실행 시 `contents` 행 수 불변 — 실측 확인
    - 🔴 `total === distinct_slug` (멱등, SC4). 2회 실행 후 행 수 동일 — 조회 출력 인용
    - 🔴 `body->'slides'->0` 키가 `kicker,big,label,sub` 4개 (SC3) — 조회 출력 인용
    - `n_slides >= 1` 인 행만 존재
    - 🔴 라이브 게이트 감사 표에 `card-news/scripts/persist-contents.js … contents … slug … ok` 행이
      **실재**하고 `verdict !== 'ok'` 0건 — 표 인용. 또는 "라이브 미검증" + 재실행 커맨드
    - 🔴 `node card-news/scripts/templates-golden.test.mjs` **14 passed** (HTML 무변경 유지)
    - 루트 `npm run test` 실패 이름 집합 불변 (`git stash push -u` 실측)
    - `npm run lint` exit 0 / `git status --porcelain supabase/` 빈 출력 (마이그레이션 0건)
    - `git status --porcelain card-news/output/` 빈 출력
    - `git push` 미실행
  </acceptance_criteria>

  <done>
    `contents`에 `type='card_news'`·`site_id='changbuletter'` 행이 실재하고, `body.slides[*]`가
    4필드를 정확히 지키며, 같은 회차를 두 번 적재해도 행이 늘지 않고, 새 `onConflict`가
    `EXPLAIN`과 확장된 라이브 게이트 **양쪽**에서 확인됐다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `card-news` 스탠드얼론(service_role) → `contents` | RLS 우회 쓰기. 발행 상태(`published`)로 즉시 공개된다 |
| 미래의 마이그레이션 → `contents` UNIQUE | 제약이 바뀌고 `onConflict`가 안 바뀌면 Phase 39가 재발한다 |
| 정적 스캐너 → `card-news/scripts` | 루트가 빠지면 **조용히** 감사 범위 밖이 된다 — 침묵이 주 실패 모드 |
| `buildSlides` 출력 → 공개 웹 | placeholder("단지명 입력")가 새면 발행물 신뢰가 깨진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Repudiation | 새 `onConflict`가 또 제약과 불일치 | mitigate | 🔴 3중: ① 플래너 실측(`contents_slug_key` 비부분) ② `EXPLAIN INSERT` 직접 실행 ③ 확장된 라이브 게이트 |
| T-40-03-02 | Repudiation | 게이트가 `card-news/scripts`를 안 봐서 **공허하게 통과** | mitigate | 🔴 Task 1에서 루트를 `AUDIT_ROOTS` 상수 하나로 모으고, 케이스 25가 그 상수의 내용을 DB 없이 단언. Task 2 (5)에서 감사 표에 `contents` 행이 **실재하는지** 확인 |
| T-40-03-03 | Tampering | 실험용 `--dry-run` 이 프로덕션 `contents`에 발행물을 씀 | mitigate | `--persist` **opt-in**. 기본 경로에서 DB 쓰기 0건. acceptance가 이를 실측 확인 |
| T-40-03-04 | Information Disclosure | placeholder·빈 슬라이드가 공개 발행됨 | mitigate | `buildContentRow`가 `slides.length===0` 이면 `null`. 테스트 3 + `n_slides>=1` 조회 확인 |
| T-40-03-05 | Tampering | 재실행이 중복 행을 만듦 | mitigate | `onConflict:'slug'` + 결정적 `publishedAt`. 2회 실행 후 `total===distinct_slug` 실측 |
| T-40-03-06 | Repudiation | upsert error를 조용히 삼킴 (Phase 39 F-03 형태) | mitigate | `persistContents`가 error를 던진다. 테스트 10이 이를 단언 |
| T-40-03-07 | Information Disclosure | `published_at`이 미래/NULL이라 anon에게 안 보임 (또는 draft가 새어 나감) | mitigate | `published_at = to 23:59:59 KST`(항상 과거) + `status='published'` 고정. RLS `published_at <= now()` 충족 확인 |
| T-40-03-08 | Tampering | 잘못된 `region_tags`가 창부레터 지역 추천을 오작동시킴 | mitigate | `[]`로 비워두고 이월. SPEC-002 C절의 태그 목록이 아직 미결이다 |
| T-40-03-09 | Tampering | `--out` 누락으로 과거 발행물(`card-news/output/`)을 덮어씀 | mitigate | 모든 실행 예시에 `--out` 포함. acceptance에 `git status --porcelain card-news/output/` 빈 출력 |
| T-40-03-10 | Repudiation | 게이트가 CI에서 안 돌아 "자동 방어됨" 착시 | accept | `ci.yml`에 `env:` 블록이 없어 라이브 대조는 영구 skip(Phase 39 확인). DB 불필요 25건만 자동 실행된다. 이 한계를 SUMMARY에 명시 |
| T-40-03-SC | Tampering | npm/pip/cargo installs | N/A | 신규 의존성 **0건** |
</threat_model>

<verification>
1. `npx vitest run src/lib/db/onconflict-audit.test.ts` → **25 passed / 0 skipped**
   (`TEST_SUPABASE_SKEY` 없는 셸에서도 동일)
2. `AUDIT_ROOTS`에 `card-news/scripts` 포함, 케이스 25가 이를 단언
3. `onconflict-constraint-gate.test.ts`가 `collectAllUpsertSites(REPO_ROOT)` 사용
4. 🔴 `EXPLAIN INSERT ... ON CONFLICT (slug) DO NOTHING` 플랜 출력 인용, `42P10` 아님, `ANALYZE` 미사용
5. 프로브 잔여행 0건
6. `node card-news/scripts/persist-contents.test.mjs` → **12 passed**
7. `--persist` 없이 실행 → `contents` 행 수 불변
8. 2회 적재 후 `total === distinct_slug`, 행 수 동일 (SC4)
9. `body->'slides'->0` 키가 `kicker,big,label,sub` (SC3)
10. 라이브 게이트 감사 표에 `persist-contents.js … contents … slug … ok` 행 실재 + 전체 `verdict!=='ok'` 0건
11. `node card-news/scripts/templates-golden.test.mjs` **14 passed** (HTML 무변경 유지)
12. 루트 `npm run test` 실패 이름 집합 불변 (총계 비교 금지)
13. `npm run lint` exit 0 / `git status --porcelain supabase/` 빈 출력
14. `git status --porcelain card-news/output/` 빈 출력 / `git push` 미실행
</verification>

<success_criteria>
1. `contents`에 `site_id='changbuletter'`·`type='card_news'` 행이 생겼다
2. `body.slides[*]`가 `kicker`·`big`·`label`·`sub` **정확히 4필드**를 갖는다
3. 같은 회차를 두 번 적재해도 중복 행이 생기지 않는다 (멱등)
4. 새 `onConflict`가 `EXPLAIN INSERT` + 확장된 라이브 게이트 양쪽에서 확인됐다
5. Phase 39 게이트가 `card-news/scripts`의 `.js` upsert를 본다 — 루트 상수가 테스트로 고정됐다
6. `--persist` 없이는 DB에 아무것도 쓰지 않는다
7. 실데이터 0건 시리즈는 적재되지 않는다
8. 40-01의 HTML 무변경이 유지된다 (골든 14건)
9. lint exit 0 / 마이그레이션 0건 / 테스트 실패 이름 집합 불변
</success_criteria>

<output>
Create `.planning/phases/40-changbuletter-prereq/40-03-SUMMARY.md` when done.

SUMMARY에 반드시 포함:
- 🔴 `EXPLAIN INSERT ... ON CONFLICT (slug)` **플랜 출력 전문** + 프로브 잔여행 0건 증거
- 🔴 라이브 게이트 감사 표 전문 — `persist-contents.js` 행이 **실재하는지** 눈으로 보이게.
  실행 못 했으면 "라이브 미검증" + 재실행 커맨드 (낙관적 표현 0건)
- 확장 전/후 `grep -rn "onConflict" card-news/scripts scripts` 출력 2건
- `contents` 조회 3종 출력: `total/distinct_slug`, slug·title·status·published_at·n_slides,
  `body->'slides'->0`
- 2회 적재 전후 행 수 (멱등 증거)
- `--persist` 미사용 실행에서 `contents` 행 수 불변 확인
- 테스트 결과 숫자 4종 (onconflict-audit 25 / persist-contents 12 / build-slides 20 / templates-golden 14)
- `git stash push -u` 회귀 실측 — 실패 **이름 집합** diff
- D-07 재량 결정 1건과 근거: `region_tags: []`
- **이월 항목**:
  ① `region_tags` 미입력 — SPEC-002 C절 "초기 태그 목록"이 미결. 확정 후 UPDATE로 채운다
  ② 소급 3회차는 **40-04 소관** — 이 plan은 최근 1회차만 적재했다
  ③ 주간 자동화(`weekly-generate.yml`)에 `--persist` **미적용** — 40-04에서 한다.
     그때까지 새 회차는 자동으로 적재되지 않는다
  ④ 🔴 라이브 대조 게이트는 CI에서 영구 skip된다(`ci.yml`에 `env:` 없음) — Phase 39와 동일 한계.
     자동 실행에서 단언되는 것은 DB 불필요 25건뿐이다
  ⑤ `card-news` 테스트 3종은 루트 `npm run test`에 잡히지 않는다 (수동 실행)
</output>
