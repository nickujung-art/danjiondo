---
phase: 28-hagwon-recommendation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260619000001_phase28_hagwon_system.sql
  - src/lib/hagwon-recommend.test.ts
  - src/app/actions/hagwon.test.ts
autonomous: false
requirements:
  - HAGWON-01
  - HAGWON-02
  - HAGWON-06
  - HAGWON-07

user_setup:
  - service: NEIS Open API
    why: "학원 전수 수집 (HAGWON-01)"
    env_vars:
      - name: NEIS_API_KEY
        source: "open.neis.go.kr → 로그인 → 인증키 신청 → '학원교습소정보' 서비스 활성화"
    dashboard_config:
      - task: "인증키 신청 후 Vercel 환경변수에 NEIS_API_KEY 추가"
        location: "Vercel Dashboard → Settings → Environment Variables"
  - service: Groq
    why: "AI 분류·코멘트 생성 (HAGWON-03, HAGWON-09) — MEMORY.md 재발급 필요"
    env_vars:
      - name: GROQ_API_KEY
        source: "console.groq.com → API Keys → Create API Key (재발급)"

must_haves:
  truths:
    - "hagwon_db 테이블이 DB에 존재하고 aca_asnum UNIQUE 제약이 있다"
    - "user_child_profiles 테이블에 RLS owner-all 정책이 활성화된다"
    - "recommend_hagwons RPC 함수가 DB에 존재하고 anon/authenticated EXECUTE 권한을 갖는다"
    - "hagwon_db.location 컬럼에 GiST 인덱스와 GIN(age_groups) 인덱스가 존재한다"
    - "테스트 스캐폴드 파일 2개가 생성된다 (RED 상태)"
  artifacts:
    - path: "supabase/migrations/20260619000001_phase28_hagwon_system.sql"
      provides: "hagwon_db + user_child_profiles + recommend_hagwons RPC + RLS + indexes"
      contains: "recommend_hagwons"
    - path: "src/lib/hagwon-recommend.test.ts"
      provides: "HAGWON-06 스코어 계산 테스트 스캐폴드"
    - path: "src/app/actions/hagwon.test.ts"
      provides: "HAGWON-09 Groq 코멘트 테스트 스캐폴드"
  key_links:
    - from: "hagwon_db"
      to: "recommend_hagwons RPC"
      via: "ST_DWithin + age_groups @> + subject_category"
      pattern: "recommend_hagwons"
    - from: "user_child_profiles"
      to: "auth.users"
      via: "user_id REFERENCES auth.users ON DELETE CASCADE"
      pattern: "auth.uid\\(\\) = user_id"
---

<objective>
Phase 28 학원 추천 시스템의 DB 스키마를 확립하고, 후속 Wave의 실행을 가능하게 하는 선행 작업을 완료한다.

Purpose: hagwon_db / user_child_profiles 테이블과 recommend_hagwons PostGIS RPC를 DB에 반영. 이 없이는 Wave 2 스크립트와 Wave 3~4 서버 코드를 작성할 수 없다.
Output:
  - supabase/migrations/20260619000001_phase28_hagwon_system.sql (DDL 전체)
  - npx supabase db push 완료 (DB에 실제 적용)
  - 테스트 스캐폴드 2개 파일 (RED)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-hagwon-recommendation/28-RESEARCH.md

<interfaces>
<!-- 기존 RLS 패턴 — supabase/migrations/20260528000001_phase18_realtors.sql -->
```sql
ALTER TABLE public.<table> ENABLE ROW LEVEL SECURITY;
CREATE POLICY "<table>: public read" ON public.<table> FOR SELECT USING (true);
CREATE POLICY "<table>: service_role write" ON public.<table> FOR ALL
  USING (auth.role() = 'service_role');
-- 사용자 소유 테이블 RLS 패턴:
CREATE POLICY "user_child_profiles: owner all"
  ON public.user_child_profiles FOR ALL
  USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
```

<!-- 기존 set_updated_at() 함수 — 이미 DB에 존재, 신규 생성 불필요 -->
```sql
CREATE TRIGGER hagwon_db_updated_at
  BEFORE UPDATE ON public.hagwon_db
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();
```

<!-- Vitest 테스트 스캐폴드 패턴 — src/lib/*.test.ts -->
```typescript
import { describe, it, expect, vi } from 'vitest'
// RED: 구현 전 실패하는 테스트
describe('hagwon recommend score', () => {
  it.todo('should weight distance 40%, popularity 30%, tier 30%')
})
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: NEIS API 키 신청 + GROQ API 키 재발급</name>
  <what-built>이 Phase에서 필요한 두 API 키를 준비한다.</what-built>
  <how-to-verify>
    1. NEIS_API_KEY 신청:
       - https://open.neis.go.kr 로그인 → 인증키 신청
       - 활용목적: "학원교습소정보(acaInsTiInfo) 수집"
       - 승인 후 Vercel Dashboard → Settings → Environment Variables → NEIS_API_KEY 추가
       - 로컬 .env.local에도 NEIS_API_KEY=<발급된키> 추가
    2. GROQ API 키 재발급:
       - https://console.groq.com → API Keys → Create API Key
       - Vercel 환경변수 GROQ_API_KEY 교체 + 로컬 .env.local 갱신
    3. 검증: `echo $NEIS_API_KEY | wc -c` 가 1 이상이면 OK
  </how-to-verify>
  <resume-signal>두 키 모두 준비되면 "ready"를 입력하세요</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Phase 28 DB 마이그레이션 SQL 작성</name>
  <files>supabase/migrations/20260619000001_phase28_hagwon_system.sql</files>
  <read_first>
    - supabase/migrations/20260528000001_phase18_realtors.sql (RLS + trigger 패턴)
    - supabase/migrations/20260430000009_rls.sql (RLS policy 패턴)
    - .planning/phases/28-hagwon-recommendation/28-RESEARCH.md (recommend_hagwons SQL, 테이블 컬럼 정의)
  </read_first>
  <action>
파일 `supabase/migrations/20260619000001_phase28_hagwon_system.sql`을 생성한다.
아래 DDL을 순서대로 포함한다 (CRITICAL: PostGIS extension은 이미 활성화, 신규 생성 불필요):

```sql
-- Phase 28: 학원 추천 시스템
-- hagwon_db + user_child_profiles + recommend_hagwons RPC

-- ── hagwon_db ──────────────────────────────────────────────────────────────
CREATE TABLE public.hagwon_db (
  id                uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  aca_asnum         text        UNIQUE NOT NULL,     -- 학원지정번호 (NEIS upsert 키)
  name              text        NOT NULL,             -- ACA_NM
  address           text,                             -- FA_RDNMA (도로명주소)
  address_detail    text,                             -- FA_RDNDA
  zipcode           text,                             -- FA_RDNZC
  phone             text,                             -- FA_TELNO
  realm_sc_nm       text,                             -- REALM_SC_NM (분야명)
  le_ord_nm         text,                             -- LE_ORD_NM (교습계열명)
  le_crse_nm        text,                             -- LE_CRSE_NM (교습과정명)
  fee_text          text,                             -- PSNBY_THCC_CNTNT 원본
  fee_amount        integer,                          -- 수강료 정수 (파싱, nullable)
  fee_tier          text        CHECK (fee_tier IN ('premium', 'standard', 'budget')),
  capacity          integer,                          -- TOFOR_SMTOT (정원)
  instructor_count  integer,                          -- NEIS 미제공 → null
  established_at    date,                             -- ESTBL_YMD
  is_active         boolean     NOT NULL DEFAULT true, -- REG_STTUS_NM='등록'
  location          geometry(Point, 4326),             -- Kakao geocoding 결과
  admst_zone_nm     text,                             -- 행정구역명 (창원시 성산구 등)
  age_groups        text[]      DEFAULT '{}',          -- Groq 분류
  subject_category  text        CHECK (subject_category IN ('academic','arts','sports','language')),
  teaching_style    text        CHECK (teaching_style IN ('exam_prep','enrichment','tutoring')),
  naver_blog_count  integer,
  popularity_score  numeric(5,4),
  created_at        timestamptz NOT NULL DEFAULT now(),
  updated_at        timestamptz NOT NULL DEFAULT now()
);

CREATE TRIGGER hagwon_db_updated_at
  BEFORE UPDATE ON public.hagwon_db
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

-- PostGIS GiST 인덱스 (ST_DWithin 성능 — Pitfall 6 방어)
CREATE INDEX idx_hagwon_db_location
  ON public.hagwon_db USING GIST (location);
CREATE INDEX idx_hagwon_db_is_active
  ON public.hagwon_db (is_active) WHERE is_active = true;
CREATE INDEX idx_hagwon_db_age_groups
  ON public.hagwon_db USING GIN (age_groups);

-- ── user_child_profiles ────────────────────────────────────────────────────
CREATE TABLE public.user_child_profiles (
  id             uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id        uuid        NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  nickname       text        NOT NULL DEFAULT '자녀',
  age_group      text        NOT NULL CHECK (age_group IN ('유아','유치','초등저','초등고','중등','고등')),
  subject_prefs  text[]      NOT NULL DEFAULT '{}',
  fee_tier_pref  text        CHECK (fee_tier_pref IN ('premium','standard','budget')),
  created_at     timestamptz NOT NULL DEFAULT now(),
  updated_at     timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX idx_user_child_profiles_user_id
  ON public.user_child_profiles(user_id);

CREATE TRIGGER user_child_profiles_updated_at
  BEFORE UPDATE ON public.user_child_profiles
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

-- ── RLS ───────────────────────────────────────────────────────────────────
ALTER TABLE public.hagwon_db ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.user_child_profiles ENABLE ROW LEVEL SECURITY;

-- hagwon_db: 공개 읽기, service_role 쓰기
CREATE POLICY "hagwon_db: public read"
  ON public.hagwon_db FOR SELECT USING (true);
CREATE POLICY "hagwon_db: service_role write"
  ON public.hagwon_db FOR ALL
  USING (auth.role() = 'service_role');

-- user_child_profiles: 소유자 전체 권한 (favorites 패턴 동일)
CREATE POLICY "user_child_profiles: owner all"
  ON public.user_child_profiles FOR ALL
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);

-- ── recommend_hagwons RPC ─────────────────────────────────────────────────
-- 거리(40%) + 인기도(30%) + tier 매칭(30%) 가중치 스코어링
CREATE OR REPLACE FUNCTION public.recommend_hagwons(
  p_lat       FLOAT,
  p_lng       FLOAT,
  p_age_group TEXT    DEFAULT NULL,
  p_subjects  TEXT[]  DEFAULT NULL,
  p_fee_tier  TEXT    DEFAULT NULL,
  p_limit     INT     DEFAULT 10
) RETURNS TABLE (
  id               UUID,
  name             TEXT,
  address          TEXT,
  distance_m       FLOAT,
  realm_sc_nm      TEXT,
  le_crse_nm       TEXT,
  fee_tier         TEXT,
  popularity_score NUMERIC,
  age_groups       TEXT[],
  subject_category TEXT,
  score            FLOAT
) LANGUAGE sql STABLE AS $$
  SELECT
    h.id,
    h.name,
    h.address,
    ST_Distance(h.location::geography, ST_Point(p_lng, p_lat)::geography) AS distance_m,
    h.realm_sc_nm,
    h.le_crse_nm,
    h.fee_tier,
    h.popularity_score,
    h.age_groups,
    h.subject_category,
    (
      0.4 * (1.0 - LEAST(
        ST_Distance(h.location::geography, ST_Point(p_lng, p_lat)::geography),
        2000.0
      ) / 2000.0)
      + 0.3 * COALESCE(h.popularity_score::FLOAT, 0.0)
      + 0.3 * CASE
          WHEN p_fee_tier IS NULL OR h.fee_tier = p_fee_tier THEN 1.0
          WHEN h.fee_tier IS NULL THEN 0.5
          ELSE 0.3
        END
    ) AS score
  FROM public.hagwon_db h
  WHERE h.is_active = TRUE
    AND h.location IS NOT NULL
    AND ST_DWithin(
      h.location::geography,
      ST_Point(p_lng, p_lat)::geography,
      2000
    )
    AND (p_age_group IS NULL OR h.age_groups @> ARRAY[p_age_group])
    AND (
      p_subjects IS NULL
      OR array_length(p_subjects, 1) = 0
      OR h.subject_category = ANY(p_subjects)
    )
  ORDER BY score DESC
  LIMIT p_limit;
$$;

GRANT EXECUTE ON FUNCTION public.recommend_hagwons
  TO anon, authenticated;
```
  </action>
  <verify>
    <automated>grep -c "recommend_hagwons" supabase/migrations/20260619000001_phase28_hagwon_system.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260619000001_phase28_hagwon_system.sql 파일이 존재한다
    - 파일에 "hagwon_db", "user_child_profiles", "recommend_hagwons" 문자열이 모두 포함된다
    - "ENABLE ROW LEVEL SECURITY" 구문이 2회 등장한다
    - "idx_hagwon_db_location" GiST 인덱스 정의가 포함된다
    - GRANT EXECUTE 구문이 포함된다
  </acceptance_criteria>
  <done>SQL 파일 작성 완료, 문법 오류 없음</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] supabase db push + 테스트 스캐폴드 생성</name>
  <files>
    src/lib/hagwon-recommend.test.ts
    src/app/actions/hagwon.test.ts
  </files>
  <read_first>
    - supabase/migrations/20260619000001_phase28_hagwon_system.sql (방금 작성한 파일)
    - .planning/phases/28-hagwon-recommendation/28-RESEARCH.md (Validation Architecture 섹션)
  </read_first>
  <action>
1. [BLOCKING] DB push 실행:
```bash
npx supabase db push
```
성공 메시지 확인. 오류 시 SQL 문법 수정 후 재실행.

2. 테스트 스캐폴드 파일 2개 생성 (RED 상태 — 구현 없이 실패하는 describe 블록):

`src/lib/hagwon-recommend.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'

// HAGWON-06: recommend_hagwons 가중치 스코어 계산 테스트
// Wave 4 (28-04)에서 실제 구현 후 채워짐
describe('hagwon recommend scoring', () => {
  it.todo('distance weight: 0.4 * (1 - dist/2000)')
  it.todo('popularity weight: 0.3 * popularity_score')
  it.todo('fee_tier match: 0.3 * 1.0 when tier matches')
  it.todo('fee_tier mismatch: 0.3 * 0.3 when tier differs')
  it.todo('fee_tier null hagwon: 0.3 * 0.5 penalty')
  it.todo('no age_group filter returns all active within 2km')
})
```

`src/app/actions/hagwon.test.ts`:
```typescript
import { describe, it, expect, vi } from 'vitest'

// HAGWON-09: Groq 코멘트 생성 테스트 (vi.mock groq-sdk)
// Wave 4 (28-04)에서 실제 구현 후 채워짐
vi.mock('groq-sdk', () => ({
  default: vi.fn().mockImplementation(() => ({
    chat: {
      completions: {
        create: vi.fn().mockResolvedValue({
          choices: [{ message: { content: '테스트 코멘트 문장1. 문장2. 문장3.' } }],
        }),
      },
    },
  })),
}))

describe('generateHagwonComment', () => {
  it.todo('returns 3-4 sentence Korean comment')
  it.todo('handles Groq API error gracefully with fallback message')
  it.todo('includes hagwon names in comment context')
})

describe('recommendHagwons action', () => {
  it.todo('validates lat range -90 to 90')
  it.todo('validates lng range -180 to 180')
  it.todo('validates ageGroup enum values')
})
```
  </action>
  <verify>
    <automated>npx supabase db diff --schema public 2>&1 | grep -c "hagwon_db\|No schema" || echo "check passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` 가 오류 없이 완료된다
    - `src/lib/hagwon-recommend.test.ts` 파일이 존재한다
    - `src/app/actions/hagwon.test.ts` 파일이 존재한다
    - `npm run test -- --run src/lib/hagwon-recommend.test.ts` 실행 시 "todo" 항목으로 처리된다 (실패 아님)
  </acceptance_criteria>
  <done>DB에 hagwon_db, user_child_profiles 테이블과 recommend_hagwons RPC가 존재하고, 테스트 스캐폴드 2개 파일이 생성된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → Supabase | DDL이 프로덕션 DB에 직접 적용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | user_child_profiles | mitigate | RLS `auth.uid() = user_id` WITH CHECK 적용 — 타 사용자 행 접근 불가 |
| T-28-02 | Elevation of Privilege | hagwon_db service_role write | mitigate | `auth.role() = 'service_role'` 정책으로 쓰기를 배치 스크립트 전용으로 제한 |
| T-28-03 | Tampering | recommend_hagwons RPC (ST_DWithin) | accept | 반경 2000m 하드코딩, p_limit 10 DEFAULT — 과도한 데이터 스캔 방지 |
</threat_model>

<verification>
```bash
# 1. 마이그레이션 파일 존재 확인
ls -la supabase/migrations/20260619000001_phase28_hagwon_system.sql

# 2. 주요 DDL 포함 확인
grep -c "recommend_hagwons\|hagwon_db\|user_child_profiles" supabase/migrations/20260619000001_phase28_hagwon_system.sql

# 3. RLS 정책 수 확인 (4개: hagwon_db 2개 + user_child_profiles 1개 + ... 최소 3개)
grep -v '^--' supabase/migrations/20260619000001_phase28_hagwon_system.sql | grep -c "CREATE POLICY"

# 4. 테스트 스캐폴드
ls src/lib/hagwon-recommend.test.ts src/app/actions/hagwon.test.ts
```
</verification>

<success_criteria>
1. supabase/migrations/20260619000001_phase28_hagwon_system.sql 존재하고 hagwon_db + user_child_profiles + recommend_hagwons RPC 모두 정의됨
2. npx supabase db push 성공
3. 테스트 스캐폴드 2개 파일 존재 (src/lib/hagwon-recommend.test.ts, src/app/actions/hagwon.test.ts)
4. npm run test 전체 suite PASS (스캐폴드는 todo로 처리됨)
</success_criteria>

<output>
완료 후 `.planning/phases/28-hagwon-recommendation/28-01-SUMMARY.md` 생성
</output>
