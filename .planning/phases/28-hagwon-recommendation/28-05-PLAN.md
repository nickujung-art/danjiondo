---
phase: 28-hagwon-recommendation
plan: 05
type: execute
wave: 4
depends_on: [28-04]
files_modified:
  - src/components/complex/HagwonRecommendSheet.tsx
  - src/components/complex/EducationCard.tsx
  - src/app/complexes/[id]/page.tsx
autonomous: false
requirements:
  - HAGWON-08

must_haves:
  truths:
    - "EducationCard 학원탭에 '내 아이에 맞는 학원 추천' 버튼이 표시된다"
    - "버튼 클릭 시 createPortal 기반 Sheet 팝업이 열린다"
    - "Step 1: 나이대 선택 (6종), Step 2: 과목 선택 (4종 다중), Step 3: tier 선택 → 결과 표시"
    - "추천 결과로 학원 카드 목록(최대 10개)과 Groq 코멘트가 표시된다"
    - "로그인 사용자는 자녀 프로필이 자동 저장·로드된다"
    - "lat/lng 없는 단지(null)에서는 버튼이 비활성화되거나 숨겨진다"
  artifacts:
    - path: "src/components/complex/HagwonRecommendSheet.tsx"
      provides: "createPortal 3단계 Sheet 팝업 + 결과 표시"
      contains: "createPortal"
    - path: "src/components/complex/EducationCard.tsx"
      provides: "'내 아이에 맞는 학원 추천' 버튼 추가"
      contains: "HagwonRecommendSheet"
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "FacilityEduSection에 lat/lng props 추가"
      contains: "complex.lat"
  key_links:
    - from: "EducationCard.tsx"
      to: "HagwonRecommendSheet.tsx"
      via: "lat, lng props"
      pattern: "HagwonRecommendSheet"
    - from: "HagwonRecommendSheet.tsx"
      to: "recommendHagwons Server Action"
      via: "useTransition + startTransition"
      pattern: "recommendHagwons"
    - from: "FacilityEduSection"
      to: "EducationCard"
      via: "lat={complex.lat ?? undefined} lng={complex.lng ?? undefined}"
      pattern: "complex\\.lat"
---

<objective>
학원 추천 팝업 UI를 구현하고 EducationCard에 연결한다. 사용자가 단지 교육 탭에서 "내 아이에 맞는 학원 추천" 버튼을 클릭하면 3단계 선택 후 맞춤 학원 리스트와 Groq 코멘트를 받을 수 있다.

Purpose: Phase 28의 최종 사용자 대면 기능. 이 플랜이 완료되면 학원 추천 시스템이 end-to-end로 동작한다.
Output:
  - src/components/complex/HagwonRecommendSheet.tsx (createPortal 3단계 Sheet)
  - src/components/complex/EducationCard.tsx 수정 (버튼 + lat/lng props)
  - src/app/complexes/[id]/page.tsx 수정 (FacilityEduSection에 lat/lng 전달)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-hagwon-recommendation/28-RESEARCH.md
@.planning/phases/28-hagwon-recommendation/28-04-SUMMARY.md

<interfaces>
<!-- EducationCard 현재 Props 인터페이스 -->
```typescript
// src/components/complex/EducationCard.tsx (현재)
interface Props {
  data: FacilityEduData
  si?: string
  gu?: string
}
export function EducationCard({ data, si, gu }: Props)

// 수정 후 Props:
interface Props {
  data: FacilityEduData
  si?:  string
  gu?:  string
  lat?: number   // 추가
  lng?: number   // 추가
}
```

<!-- FacilityEduSection 현재 시그니처 (src/app/complexes/[id]/page.tsx line 50) -->
```typescript
// 현재:
async function FacilityEduSection({ complexId, si, gu }: { complexId: string; si?: string; gu?: string }) {
  return <EducationCard data={data} si={si} gu={gu} />
}

// 수정 후:
async function FacilityEduSection({ complexId, si, gu, lat, lng }: {
  complexId: string; si?: string; gu?: string; lat?: number; lng?: number
}) {
  return <EducationCard data={data} si={si} gu={gu} lat={lat} lng={lng} />
}

// 호출부 (page.tsx line 849):
// 현재: <FacilityEduSection complexId={id} si={complex.si ?? undefined} gu={complex.gu ?? undefined} />
// 수정: <FacilityEduSection complexId={id} si={complex.si ?? undefined} gu={complex.gu ?? undefined}
//         lat={complex.lat ?? undefined} lng={complex.lng ?? undefined} />
```

<!-- SchoolDetailSheet createPortal 패턴 (EducationCard.tsx에서 확인됨) -->
```typescript
// 드래그 핸들, fixed overlay, borderRadius '20px 20px 0 0'
// maxHeight: '90vh', overflowY: 'auto'
// paddingBottom: 'env(safe-area-inset-bottom)'
// animation: transform opacity (compositor 속성만, CLAUDE.md 규칙)
import { createPortal } from 'react-dom'
```

<!-- Server Action 호출 패턴 -->
```typescript
import { useTransition } from 'react'
import { recommendHagwons } from '@/app/actions/hagwon'

const [isPending, startTransition] = useTransition()
// 제출:
startTransition(async () => {
  const result = await recommendHagwons({ lat, lng, ageGroup, subjects, feeTierPref })
  if ('error' in result) { setError(result.error); return }
  setResults(result.results)
  setComment(result.comment)
})
```

<!-- HagwonResult 타입 (src/services/neis-hagwon.ts) -->
```typescript
interface HagwonResult {
  id:               string
  name:             string
  address:          string | null
  distance_m:       number
  realm_sc_nm:      string | null
  le_crse_nm:       string | null
  fee_tier:         'premium' | 'standard' | 'budget' | null
  popularity_score: number | null
  age_groups:       string[]
  subject_category: string | null
  score:            number
}
```

<!-- UI 금지 사항 (CLAUDE.md) -->
// backdrop-blur, gradient-text, glow 애니메이션 금지
// 보라/인디고 브랜드색 금지
// 애니메이션: transform/opacity만 (compositor 속성)
// "Powered by AI" 배지 금지
// 이모지 금지 — SVG path만 사용
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HagwonRecommendSheet 팝업 컴포넌트 구현</name>
  <files>src/components/complex/HagwonRecommendSheet.tsx</files>
  <read_first>
    - src/components/complex/EducationCard.tsx (SchoolDetailSheet createPortal 패턴 — 전체 파일 읽기, 특히 하단 SchoolDetailSheet 구현부)
    - src/services/neis-hagwon.ts (HagwonResult, AgeGroup, SubjectCategory, FeeTier 타입)
    - src/app/actions/hagwon.ts (recommendHagwons, saveChildProfile, loadChildProfile 시그니처)
  </read_first>
  <action>
`src/components/complex/HagwonRecommendSheet.tsx` 파일을 생성한다. `'use client'` 지시어 필수.

**컴포넌트 Props**:
```typescript
interface Props {
  lat:     number
  lng:     number
  onClose: () => void
}
```

**3단계 스텝 상태**:
```typescript
type Step = 'age' | 'subject' | 'tier' | 'result'
const [step, setStep] = useState<Step>('age')
const [selectedAge, setSelectedAge] = useState<AgeGroup | undefined>()
const [selectedSubjects, setSelectedSubjects] = useState<SubjectCategory[]>([])
const [selectedTier, setSelectedTier] = useState<FeeTier | null>(null)
const [results, setResults] = useState<HagwonResult[]>([])
const [comment, setComment] = useState('')
const [error, setError] = useState<string | null>(null)
const [isPending, startTransition] = useTransition()
```

**자녀 프로필 자동 로드** (컴포넌트 마운트 시):
```typescript
useEffect(() => {
  loadChildProfile().then(profile => {
    if (profile) {
      setSelectedAge(profile.age_group)
      setSelectedSubjects(profile.subject_prefs as SubjectCategory[])
      setSelectedTier(profile.fee_tier_pref)
    }
  }).catch(() => {})
}, [])
```

**추천 제출 핸들러**:
```typescript
const handleSubmit = () => {
  startTransition(async () => {
    const res = await recommendHagwons({
      lat, lng,
      ageGroup:    selectedAge,
      subjects:    selectedSubjects.length > 0 ? selectedSubjects : undefined,
      feeTierPref: selectedTier,
    })
    if ('error' in res) { setError('추천을 불러오지 못했습니다.'); return }
    setResults(res.results)
    setComment(res.comment)
    setStep('result')
    // 프로필 저장 (fire & forget)
    if (selectedAge) {
      saveChildProfile({
        nickname: '자녀',
        age_group: selectedAge,
        subject_prefs: selectedSubjects,
        fee_tier_pref: selectedTier ?? null,
      }).catch(() => {})
    }
  })
}
```

**Step 화면 구성**:

Step 'age': 나이대 6종 버튼 그리드
```
['유아', '유치', '초등저', '초등고', '중등', '고등']
```
각 버튼 클릭 → setSelectedAge → setStep('subject')

Step 'subject': 과목 4종 다중 선택 체크박스
```
['학교교과 (academic)', '예술/음악 (arts)', '체육 (sports)', '어학 (language)']
```
"선택 없음(전체)" 옵션 포함. 다음 버튼 → setStep('tier')

Step 'tier': 수강료 tier 3종 + 전체
```
['전체', '프리미엄 (상위 30%)', '표준 (중간 50%)', '실속 (하위 20%)']
```
선택 후 handleSubmit() 자동 호출

Step 'result': 결과 화면
- isPending 중: 로딩 스피너 (간단한 SVG circle)
- 결과 있을 때: comment 텍스트 + 학원 카드 목록
- 학원 카드 각각: 이름, 거리(m), 교습계열, 수강료tier 뱃지, score

**학원 카드 거리 표시**:
```typescript
function fmtDist(m: number): string {
  return m >= 1000 ? `${(m / 1000).toFixed(1)}km` : `${Math.round(m)}m`
}
```

**fee_tier 뱃지 색상**:
```
premium: { color: '#92400e', bg: '#fef3c7' }
standard: { color: '#1e40af', bg: '#dbeafe' }
budget: { color: '#065f46', bg: '#d1fae5' }
```

**Sheet 구조** (createPortal, SchoolDetailSheet 패턴 동일):
```tsx
return createPortal(
  <>
    {/* 오버레이 */}
    <div
      onClick={onClose}
      style={{
        position: 'fixed', inset: 0,
        background: 'rgba(0,0,0,0.4)',
        zIndex: 1000,
      }}
    />
    {/* Sheet */}
    <div
      role="dialog"
      aria-modal="true"
      aria-label="학원 추천"
      style={{
        position:     'fixed',
        bottom:       0, left: 0, right: 0,
        background:   '#fff',
        borderRadius: '20px 20px 0 0',
        maxHeight:    '90vh',
        overflowY:    'auto',
        zIndex:       1001,
        paddingBottom:'env(safe-area-inset-bottom)',
      }}
    >
      {/* 드래그 핸들 */}
      <div style={{ display:'flex', justifyContent:'center', padding:'12px 0 4px' }}>
        <div style={{ width:36, height:4, background:'#e5e7eb', borderRadius:2 }} />
      </div>
      {/* 헤더 */}
      <div style={{ display:'flex', justifyContent:'space-between', alignItems:'center', padding:'0 20px 16px' }}>
        <h2 style={{ font:'700 16px/1.4 var(--font-sans)', margin:0 }}>
          내 아이에 맞는 학원 추천
        </h2>
        <button onClick={onClose} aria-label="닫기" style={{ background:'none', border:'none', cursor:'pointer', padding:4 }}>
          {/* X 아이콘 SVG */}
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
            <path d="M18 6 6 18M6 6l12 12" />
          </svg>
        </button>
      </div>
      {/* 컨텐츠 */}
      <div style={{ padding: '0 20px 24px' }}>
        {/* 스텝별 렌더링 */}
      </div>
    </div>
  </>,
  document.body,
)
```

**오류 처리**: error state 있으면 빨간 텍스트로 표시, "다시 시도" 버튼 제공.

**애니메이션 금지**: transform/opacity 이외 width/height/top/margin 등 레이아웃 속성 애니메이션 사용 금지 (CLAUDE.md).
  </action>
  <verify>
    <automated>grep -c "createPortal\|recommendHagwons\|useTransition" src/components/complex/HagwonRecommendSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/complex/HagwonRecommendSheet.tsx 파일이 존재한다
    - `grep -c "createPortal" src/components/complex/HagwonRecommendSheet.tsx` 결과 >= 1
    - `grep -c "useTransition" src/components/complex/HagwonRecommendSheet.tsx` 결과 >= 1
    - `grep -c "recommendHagwons\|saveChildProfile\|loadChildProfile" src/components/complex/HagwonRecommendSheet.tsx` 결과 >= 2
    - `grep -c "backdrop-blur\|gradient\|glow\|Powered by AI" src/components/complex/HagwonRecommendSheet.tsx` 결과 == 0 (UI 금지 사항)
    - npm run build PASS
  </acceptance_criteria>
  <done>HagwonRecommendSheet 컴포넌트 구현 완료. createPortal, 3단계 스텝, 결과 표시, 프로필 저장 포함.</done>
</task>

<task type="auto">
  <name>Task 2: EducationCard 버튼 추가 + FacilityEduSection lat/lng 연결</name>
  <files>
    src/components/complex/EducationCard.tsx
    src/app/complexes/[id]/page.tsx
  </files>
  <read_first>
    - src/components/complex/EducationCard.tsx (전체 파일 — Props interface, EducationCard 함수, 학원탭 렌더링 부분)
    - src/app/complexes/[id]/page.tsx (FacilityEduSection 정의 line 50, 호출부 line 849)
    - src/components/complex/HagwonRecommendSheet.tsx (방금 작성한 컴포넌트 Props)
  </read_first>
  <action>
**1. src/components/complex/EducationCard.tsx 수정:**

Props 인터페이스 수정 (lat, lng 추가):
```typescript
interface Props {
  data: FacilityEduData
  si?:  string
  gu?:  string
  lat?: number   // 추가 — HagwonRecommendSheet에 전달
  lng?: number   // 추가 — HagwonRecommendSheet에 전달
}
export function EducationCard({ data, si, gu, lat, lng }: Props)
```

상태 추가 (학원탭 내):
```typescript
const [showHagwonRecommend, setShowHagwonRecommend] = useState(false)
const [isMounted, setIsMounted] = useState(false)

// createPortal은 클라이언트에서만 동작
useEffect(() => { setIsMounted(true) }, [])
```

학원탭 (`tab === 'hagwon'`) 렌더링 블록에 버튼 추가 (학원 목록 위):
```tsx
{/* 학원 추천 버튼 — lat/lng 있을 때만 표시 */}
{lat != null && lng != null && (
  <div style={{ marginBottom: 16 }}>
    <button
      onClick={() => setShowHagwonRecommend(true)}
      style={{
        width:        '100%',
        padding:      '10px 16px',
        background:   '#f97316',  // 오렌지 (브랜드 색상)
        color:        '#fff',
        border:       'none',
        borderRadius: 8,
        font:         '600 14px/1.4 var(--font-sans)',
        cursor:       'pointer',
        display:      'flex',
        alignItems:   'center',
        justifyContent: 'center',
        gap:          8,
      }}
    >
      {/* 별 SVG 아이콘 (이모지 사용 금지, SVG path만) */}
      <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" stroke="none">
        <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
      </svg>
      내 아이에 맞는 학원 추천
    </button>
  </div>
)}

{/* HagwonRecommendSheet 팝업 */}
{isMounted && showHagwonRecommend && lat != null && lng != null && (
  <HagwonRecommendSheet
    lat={lat}
    lng={lng}
    onClose={() => setShowHagwonRecommend(false)}
  />
)}
```

파일 상단 import 추가:
```typescript
import { HagwonRecommendSheet } from '@/components/complex/HagwonRecommendSheet'
```

**2. src/app/complexes/[id]/page.tsx 수정 (2곳):**

FacilityEduSection 함수 시그니처 수정 (line ~50):
```typescript
// Before:
async function FacilityEduSection({ complexId, si, gu }: { complexId: string; si?: string; gu?: string }) {
  return <EducationCard data={data} si={si} gu={gu} />

// After:
async function FacilityEduSection({ complexId, si, gu, lat, lng }: {
  complexId: string; si?: string; gu?: string; lat?: number; lng?: number
}) {
  return <EducationCard data={data} si={si} gu={gu} lat={lat} lng={lng} />
```

FacilityEduSection 호출부 수정 (line ~849):
```tsx
// Before:
<FacilityEduSection complexId={id} si={complex.si ?? undefined} gu={complex.gu ?? undefined} />

// After:
<FacilityEduSection
  complexId={id}
  si={complex.si ?? undefined}
  gu={complex.gu ?? undefined}
  lat={complex.lat ?? undefined}
  lng={complex.lng ?? undefined}
/>
```
  </action>
  <verify>
    <automated>grep -c "HagwonRecommendSheet\|setShowHagwonRecommend" src/components/complex/EducationCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HagwonRecommendSheet" src/components/complex/EducationCard.tsx` 결과 >= 2 (import + JSX)
    - `grep -c "lat\|lng" src/components/complex/EducationCard.tsx` 결과 >= 4 (Props + 조건부 렌더)
    - `grep -c "complex\.lat\|complex\.lng" src/app/complexes/\[id\]/page.tsx` 결과 >= 2 (FacilityEduSection 전달)
    - `grep -c "isMounted" src/components/complex/EducationCard.tsx` 결과 >= 1 (createPortal SSR 방어)
    - npm run lint && npm run build PASS
  </acceptance_criteria>
  <done>EducationCard에 학원 추천 버튼 추가 완료. FacilityEduSection에 lat/lng 전달 경로 확립.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 학원 추천 UI 기능 검증</name>
  <what-built>
    - HagwonRecommendSheet 3단계 팝업 UI
    - EducationCard "내 아이에 맞는 학원 추천" 버튼
    - FacilityEduSection lat/lng 전달 경로
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 http://localhost:3000 접속
    2. 창원/김해 단지 상세 페이지로 이동 (예: 창원 아무 단지)
    3. 교육 환경 카드 → "학원·교육" 탭 클릭
    4. "내 아이에 맞는 학원 추천" 버튼이 탭 상단에 표시되는지 확인
    5. 버튼 클릭 → Sheet 팝업이 바텀에서 슬라이드업되는지 확인
    6. Step 1: 나이대 6종 버튼이 표시되는지 확인 (유아/유치/초등저/초등고/중등/고등)
    7. 나이대 선택 → Step 2: 과목 4종 다중 선택 화면으로 전환되는지 확인
    8. Step 3: tier 선택 후 추천 결과가 로딩되는지 확인
    9. 결과 화면: 학원 카드 목록과 코멘트 텍스트가 표시되는지 확인

    Note: hagwon_db에 데이터가 없으면 "추천 결과가 없습니다" 빈 상태로 표시될 수 있음 (Wave 2~3 스크립트 실행 선행 필요). UI 흐름(3단계 전환)이 동작하는지가 핵심.

    lat/lng 없는 단지 확인: complexes.lat IS NULL인 단지에서 버튼이 숨겨지는지 확인.
  </how-to-verify>
  <resume-signal>UI 흐름 정상 동작 시 "approved"를 입력하세요. 문제가 있으면 증상을 설명해주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | 클라이언트에서 오는 lat/lng, 나이/과목/tier 입력 신뢰 불가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-14 | Information Disclosure | 학원 추천 결과 (공개 데이터) | accept | hagwon_db는 공개 학원 정보, 개인 정보 없음 |
| T-28-15 | Tampering | HagwonRecommendSheet lat/lng (클라이언트 전달) | mitigate | Server Action zod 검증 (28-04에서 구현됨). UI는 complex.lat/lng만 전달하여 임의 좌표 불가 |
| T-28-16 | Information Disclosure | user_child_profiles 자동 저장 | mitigate | fire-and-forget이지만 saveChildProfile 내부에서 auth.getUser() guard 수행 (28-04) |
</threat_model>

<verification>
```bash
# 파일 존재 확인
ls -la src/components/complex/HagwonRecommendSheet.tsx

# 핵심 패턴 확인
grep -c "createPortal\|useTransition" src/components/complex/HagwonRecommendSheet.tsx
grep -c "HagwonRecommendSheet\|lat\|lng" src/components/complex/EducationCard.tsx
grep -c "complex\.lat\|complex\.lng" src/app/complexes/\[id\]/page.tsx

# UI 금지 사항 없는지 확인
grep -c "backdrop-blur\|gradient\|glow" src/components/complex/HagwonRecommendSheet.tsx

# 전체 빌드
npm run lint && npm run build && npm run test
```
</verification>

<success_criteria>
1. HagwonRecommendSheet.tsx: createPortal, 3단계 스텝, useTransition, 결과 표시, 프로필 저장
2. EducationCard.tsx: lat/lng props 추가, 버튼 추가, isMounted SSR 방어
3. page.tsx: FacilityEduSection에 complex.lat/lng 전달
4. 사람이 단지 교육탭에서 "내 아이에 맞는 학원 추천" 버튼을 클릭하고 3단계 팝업 흐름을 확인
5. npm run lint && npm run build && npm run test PASS
</success_criteria>

<output>
완료 후 `.planning/phases/28-hagwon-recommendation/28-05-SUMMARY.md` 생성
</output>
