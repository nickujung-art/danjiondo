---
phase: 28-hagwon-recommendation
plan: 04
type: execute
wave: 3
depends_on: [28-01]
files_modified:
  - src/services/neis-hagwon.ts
  - src/lib/data/hagwon-recommend.ts
  - src/app/actions/hagwon.ts
  - src/lib/hagwon-recommend.test.ts
  - src/app/actions/hagwon.test.ts
autonomous: true
requirements:
  - HAGWON-06
  - HAGWON-07
  - HAGWON-09

must_haves:
  truths:
    - "recommendHagwons Server Action이 lat/lng/ageGroup/subjects/feeTierPref 입력으로 10개 학원 배열을 반환한다"
    - "saveChildProfile Server Action이 로그인 사용자의 자녀 프로필을 user_child_profiles에 저장한다"
    - "loadChildProfile Server Action이 현재 사용자의 프로필을 반환하거나 null을 반환한다"
    - "generateHagwonComment 함수가 3-4문장 한국어 코멘트를 반환한다"
    - "테스트 파일 2개가 GREEN 상태로 통과한다"
  artifacts:
    - path: "src/services/neis-hagwon.ts"
      provides: "HagwonResult 타입 정의 + recommend 입력 타입"
      exports: ["HagwonResult", "RecommendInput", "ChildProfile"]
    - path: "src/lib/data/hagwon-recommend.ts"
      provides: "DB 쿼리 함수 (recommend_hagwons RPC wrapper)"
      exports: ["fetchHagwonRecommendations", "fetchChildProfile"]
    - path: "src/app/actions/hagwon.ts"
      provides: "Server Actions 3개 (recommend + profile save/load + Groq comment)"
      exports: ["recommendHagwons", "saveChildProfile", "loadChildProfile"]
  key_links:
    - from: "src/app/actions/hagwon.ts"
      to: "recommend_hagwons RPC"
      via: "supabase.rpc('recommend_hagwons', { p_lat, p_lng, ... })"
      pattern: "recommend_hagwons"
    - from: "src/app/actions/hagwon.ts"
      to: "Groq llama-3.1-8b-instant"
      via: "groq.chat.completions.create({ model: 'llama-3.1-8b-instant' })"
      pattern: "llama-3.1-8b-instant"
    - from: "src/app/actions/hagwon.ts"
      to: "user_child_profiles"
      via: "createSupabaseServerClient() + auth.uid() check"
      pattern: "user_child_profiles"
---

<objective>
학원 추천 Server Actions와 데이터 레이어를 구현한다. 이 플랜이 완료되면 Wave 4 UI(28-05)가 Server Action을 호출할 수 있게 된다.

Purpose: recommend_hagwons RPC를 호출하는 Server Action, 자녀 프로필 저장/로드, Groq 추천 코멘트 생성 함수를 제공한다.
Output:
  - src/services/neis-hagwon.ts (타입 계약 파일)
  - src/lib/data/hagwon-recommend.ts (DB 쿼리 레이어)
  - src/app/actions/hagwon.ts (Server Actions 3개)
  - 테스트 2개 파일 GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-hagwon-recommendation/28-RESEARCH.md
@.planning/phases/28-hagwon-recommendation/28-01-SUMMARY.md

<interfaces>
<!-- recommend_hagwons RPC 반환 타입 (28-01 마이그레이션 기준) -->
```typescript
// supabase.rpc('recommend_hagwons', params) 반환 타입
interface HagwonRpcRow {
  id:               string
  name:             string
  address:          string | null
  distance_m:       number
  realm_sc_nm:      string | null
  le_crse_nm:       string | null
  fee_tier:         'premium' | 'standard' | 'budget' | null
  popularity_score: number | null
  age_groups:       string[]
  subject_category: 'academic' | 'arts' | 'sports' | 'language' | null
  score:            number
}
```

<!-- createSupabaseServerClient 패턴 — src/app/actions/education.ts 기존 패턴 -->
```typescript
'use server'
import { createSupabaseServerClient } from '@/lib/supabase/server'

export async function myAction() {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  // user?.id 로 인증 확인
}
```

<!-- Groq 코멘트 생성 패턴 (scripts/generate-complex-commentary.ts 기존 패턴) -->
```typescript
import Groq from 'groq-sdk'
// GROQ_API_KEY는 서버 환경변수 — client 컴포넌트에서 접근 불가
const groq = new Groq({ apiKey: process.env.GROQ_API_KEY })
```

<!-- user_child_profiles 테이블 컬럼 (28-01 마이그레이션 기준) -->
```typescript
interface ChildProfile {
  id:            string
  user_id:       string
  nickname:      string
  age_group:     '유아' | '유치' | '초등저' | '초등고' | '중등' | '고등'
  subject_prefs: string[]
  fee_tier_pref: 'premium' | 'standard' | 'budget' | null
  created_at:    string
  updated_at:    string
}
```

<!-- zod 입력 검증 패턴 -->
```typescript
import { z } from 'zod'
const RecommendInputSchema = z.object({
  lat:         z.number().min(-90).max(90),
  lng:         z.number().min(-180).max(180),
  ageGroup:    z.enum(['유아','유치','초등저','초등고','중등','고등']).optional(),
  subjects:    z.array(z.enum(['academic','arts','sports','language'])).optional(),
  feeTierPref: z.enum(['premium','standard','budget']).nullable().optional(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 타입 어댑터 + DB 데이터 레이어</name>
  <files>
    src/services/neis-hagwon.ts
    src/lib/data/hagwon-recommend.ts
  </files>
  <read_first>
    - src/app/actions/education.ts (Server Action 패턴, createSupabaseServerClient)
    - src/lib/data/facility-edu.ts (데이터 레이어 패턴, interface export)
    - .planning/phases/28-hagwon-recommendation/28-RESEARCH.md (Pattern 3, recommend_hagwons RPC 시그니처)
  </read_first>
  <behavior>
    - fetchHagwonRecommendations(params) → HagwonResult[] 배열 반환 (RPC 에러 시 빈 배열)
    - fetchChildProfile(userId) → ChildProfile | null (미존재 시 null)
    - HagwonResult 타입이 HagwonRpcRow를 정확히 매핑한다
  </behavior>
  <action>
**src/services/neis-hagwon.ts** 생성 (타입 계약 파일):
```typescript
// NEIS 학원 추천 시스템 타입 계약
// CLAUDE.md: 외부 API 타입은 src/services/ 어댑터에 정의

export type AgeGroup = '유아' | '유치' | '초등저' | '초등고' | '중등' | '고등'
export type SubjectCategory = 'academic' | 'arts' | 'sports' | 'language'
export type TeachingStyle = 'exam_prep' | 'enrichment' | 'tutoring'
export type FeeTier = 'premium' | 'standard' | 'budget'

export interface HagwonResult {
  id:               string
  name:             string
  address:          string | null
  distance_m:       number
  realm_sc_nm:      string | null
  le_crse_nm:       string | null
  fee_tier:         FeeTier | null
  popularity_score: number | null
  age_groups:       AgeGroup[]
  subject_category: SubjectCategory | null
  score:            number
}

export interface RecommendInput {
  lat:         number
  lng:         number
  ageGroup?:   AgeGroup
  subjects?:   SubjectCategory[]
  feeTierPref?: FeeTier | null
}

export interface ChildProfile {
  id:            string
  user_id:       string
  nickname:      string
  age_group:     AgeGroup
  subject_prefs: string[]
  fee_tier_pref: FeeTier | null
  created_at:    string
  updated_at:    string
}
```

**src/lib/data/hagwon-recommend.ts** 생성 (DB 쿼리 레이어):
```typescript
// CLAUDE.md: Supabase 쿼리 → 서버 컴포넌트·API Route·Server Action 전용
import type { SupabaseClient } from '@supabase/supabase-js'
import type { HagwonResult, RecommendInput, ChildProfile } from '@/services/neis-hagwon'

export async function fetchHagwonRecommendations(
  supabase: SupabaseClient,
  input: RecommendInput,
): Promise<HagwonResult[]> {
  const { data, error } = await supabase.rpc('recommend_hagwons', {
    p_lat:       input.lat,
    p_lng:       input.lng,
    p_age_group: input.ageGroup ?? null,
    p_subjects:  input.subjects ?? null,
    p_fee_tier:  input.feeTierPref ?? null,
    p_limit:     10,
  })
  if (error) {
    console.error('[fetchHagwonRecommendations]', error)
    return []
  }
  return (data ?? []) as HagwonResult[]
}

export async function fetchChildProfile(
  supabase: SupabaseClient,
  userId: string,
): Promise<ChildProfile | null> {
  const { data, error } = await supabase
    .from('user_child_profiles')
    .select('*')
    .eq('user_id', userId)
    .order('created_at', { ascending: false })
    .limit(1)
    .maybeSingle()
  if (error) {
    console.error('[fetchChildProfile]', error)
    return null
  }
  return data as ChildProfile | null
}
```
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/hagwon-recommend.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/services/neis-hagwon.ts 파일이 존재하고 HagwonResult, RecommendInput, ChildProfile 타입을 export한다
    - src/lib/data/hagwon-recommend.ts 파일이 존재하고 fetchHagwonRecommendations, fetchChildProfile를 export한다
    - `grep -c "recommend_hagwons" src/lib/data/hagwon-recommend.ts` 결과 >= 1
    - `npm run test -- --run src/lib/hagwon-recommend.test.ts` 에서 오류 없음 (todo는 todo로 처리)
  </acceptance_criteria>
  <done>타입 계약 파일 + DB 쿼리 레이어 완성. export 타입이 마이그레이션 컬럼과 일치.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server Actions + 테스트 구현</name>
  <files>
    src/app/actions/hagwon.ts
    src/lib/hagwon-recommend.test.ts
    src/app/actions/hagwon.test.ts
  </files>
  <read_first>
    - src/app/actions/education.ts (Server Action 패턴 — createSupabaseServerClient, error handling)
    - src/services/neis-hagwon.ts (방금 작성한 타입 파일)
    - src/lib/data/hagwon-recommend.ts (방금 작성한 쿼리 레이어)
    - src/lib/hagwon-recommend.test.ts (Wave 1에서 만든 스캐폴드 — todo 항목 채워야 함)
    - src/app/actions/hagwon.test.ts (Wave 1에서 만든 스캐폴드 — todo 항목 채워야 함)
  </read_first>
  <behavior>
    - recommendHagwons: 유효하지 않은 lat/lng (범위 초과) → { error: 'invalid_input' }
    - recommendHagwons: 유효 입력 → { results: HagwonResult[], comment: string }
    - generateHagwonComment: Groq 오류 → fallback 문자열 반환 (빈 문자열 아님)
    - saveChildProfile: 비로그인 사용자 → { error: 'unauthorized' }
    - loadChildProfile: 비로그인 사용자 → null
  </behavior>
  <action>
**src/app/actions/hagwon.ts** 생성:
```typescript
'use server'

import { z } from 'zod'
import Groq from 'groq-sdk'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { fetchHagwonRecommendations, fetchChildProfile } from '@/lib/data/hagwon-recommend'
import type { HagwonResult, AgeGroup, SubjectCategory, FeeTier } from '@/services/neis-hagwon'

// ── 입력 검증 스키마 (HAGWON-06 + 보안 T-28-02) ────────────────────────────
const RecommendSchema = z.object({
  lat:         z.number().min(-90).max(90),
  lng:         z.number().min(-180).max(180),
  ageGroup:    z.enum(['유아','유치','초등저','초등고','중등','고등']).optional(),
  subjects:    z.array(z.enum(['academic','arts','sports','language'])).optional(),
  feeTierPref: z.enum(['premium','standard','budget']).nullable().optional(),
})

const ChildProfileSchema = z.object({
  nickname:      z.string().min(1).max(20),
  age_group:     z.enum(['유아','유치','초등저','초등고','중등','고등']),
  subject_prefs: z.array(z.string()).max(5),
  fee_tier_pref: z.enum(['premium','standard','budget']).nullable().optional(),
})

// ── Groq 코멘트 생성 (HAGWON-09) ──────────────────────────────────────────
const FALLBACK_COMMENT = '추천 학원 목록을 확인해보세요. 거리, 인기도, 수강료를 종합하여 선별한 학원들입니다.'

async function generateHagwonComment(
  results: HagwonResult[],
  ageGroup?: AgeGroup,
): Promise<string> {
  if (!process.env.GROQ_API_KEY || results.length === 0) return FALLBACK_COMMENT
  const topNames = results.slice(0, 3).map(r => r.name).join(', ')
  const groq = new Groq({ apiKey: process.env.GROQ_API_KEY })
  try {
    const res = await groq.chat.completions.create({
      model:      'llama-3.1-8b-instant',
      messages:   [{
        role:    'user',
        content: `아파트 단지 근처 추천 학원 목록입니다.\n학원: ${topNames}\n대상 연령: ${ageGroup ?? '전체'}\n\n이 학원들에 대해 학부모에게 도움이 되는 3~4문장 한국어 코멘트를 작성하세요. 숫자(가격, 거리)를 포함하지 마세요.`,
      }],
      max_tokens: 200,
      temperature: 0.5,
    })
    return res.choices[0]?.message?.content?.trim() ?? FALLBACK_COMMENT
  } catch {
    return FALLBACK_COMMENT
  }
}

// ── recommendHagwons (HAGWON-06, HAGWON-09) ──────────────────────────────
export async function recommendHagwons(input: {
  lat: number; lng: number
  ageGroup?: AgeGroup; subjects?: SubjectCategory[]
  feeTierPref?: FeeTier | null
}): Promise<{ results: HagwonResult[]; comment: string } | { error: string }> {
  const parsed = RecommendSchema.safeParse(input)
  if (!parsed.success) return { error: 'invalid_input' }

  const supabase = await createSupabaseServerClient()
  const results = await fetchHagwonRecommendations(supabase, parsed.data)
  const comment = await generateHagwonComment(results, parsed.data.ageGroup as AgeGroup | undefined)

  return { results, comment }
}

// ── saveChildProfile (HAGWON-07) ────────────────────────────────────────
export async function saveChildProfile(input: {
  nickname: string; age_group: AgeGroup
  subject_prefs: string[]; fee_tier_pref?: FeeTier | null
}): Promise<{ ok: true; id: string } | { error: string }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'unauthorized' }

  const parsed = ChildProfileSchema.safeParse(input)
  if (!parsed.success) return { error: 'invalid_input' }

  // upsert (user당 최신 1개 유지 — 기존 행 덮어쓰기)
  const existing = await fetchChildProfile(supabase, user.id)
  if (existing) {
    const { error } = await supabase.from('user_child_profiles')
      .update({ ...parsed.data, updated_at: new Date().toISOString() })
      .eq('id', existing.id)
    if (error) return { error: error.message }
    return { ok: true, id: existing.id }
  }

  const { data, error } = await supabase.from('user_child_profiles')
    .insert({ user_id: user.id, ...parsed.data })
    .select('id')
    .single()
  if (error) return { error: error.message }
  return { ok: true, id: data.id }
}

// ── loadChildProfile (HAGWON-07) ────────────────────────────────────────
export async function loadChildProfile(): Promise<{
  nickname: string; age_group: AgeGroup
  subject_prefs: string[]; fee_tier_pref: FeeTier | null
} | null> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return null
  const profile = await fetchChildProfile(supabase, user.id)
  if (!profile) return null
  return {
    nickname:      profile.nickname,
    age_group:     profile.age_group,
    subject_prefs: profile.subject_prefs,
    fee_tier_pref: profile.fee_tier_pref,
  }
}
```

**src/lib/hagwon-recommend.test.ts** 갱신 (todo → 실제 테스트):
```typescript
import { describe, it, expect } from 'vitest'

// RecommendSchema 검증 로직을 별도 함수로 추출하거나 action의 zod schema를 import
// 여기서는 독립 순수 함수 scoreHagwon을 테스트한다

function scoreHagwon(distanceM: number, popularityScore: number | null, feeTierMatch: boolean | null): number {
  const distScore = 0.4 * (1 - Math.min(distanceM, 2000) / 2000)
  const popScore  = 0.3 * (popularityScore ?? 0)
  const tierScore = 0.3 * (feeTierMatch === null ? 0.5 : feeTierMatch ? 1.0 : 0.3)
  return distScore + popScore + tierScore
}

describe('hagwon recommend scoring', () => {
  it('distance weight: 0m = 0.4 contribution', () => {
    expect(scoreHagwon(0, 0, null)).toBeCloseTo(0.4 + 0 + 0.15, 2)
  })
  it('distance weight: 2000m = 0 distance contribution', () => {
    expect(scoreHagwon(2000, 0, null)).toBeCloseTo(0 + 0 + 0.15, 2)
  })
  it('popularity weight: 0.3 * popularity_score', () => {
    expect(scoreHagwon(0, 1.0, null)).toBeCloseTo(0.4 + 0.3 + 0.15, 2)
  })
  it('fee_tier match: 0.3 * 1.0', () => {
    expect(scoreHagwon(0, 0, true)).toBeCloseTo(0.4 + 0 + 0.3, 2)
  })
  it('fee_tier mismatch: 0.3 * 0.3', () => {
    expect(scoreHagwon(0, 0, false)).toBeCloseTo(0.4 + 0 + 0.09, 2)
  })
  it('fee_tier null hagwon: 0.3 * 0.5', () => {
    expect(scoreHagwon(0, 0, null)).toBeCloseTo(0.4 + 0 + 0.15, 2)
  })
})
```

**src/app/actions/hagwon.test.ts** 갱신 (todo → 실제 테스트):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// vi.mock은 파일 상단에서만 동작 (hoisting)
vi.mock('groq-sdk', () => ({
  default: vi.fn().mockImplementation(() => ({
    chat: {
      completions: {
        create: vi.fn().mockResolvedValue({
          choices: [{ message: { content: '문장1. 문장2. 문장3. 문장4.' } }],
        }),
      },
    },
  })),
}))

vi.mock('@/lib/supabase/server', () => ({
  createSupabaseServerClient: vi.fn().mockResolvedValue({
    auth: { getUser: vi.fn().mockResolvedValue({ data: { user: null } }) },
    from: vi.fn().mockReturnValue({ select: vi.fn().mockReturnThis(), eq: vi.fn().mockReturnThis(), order: vi.fn().mockReturnThis(), limit: vi.fn().mockReturnThis(), maybeSingle: vi.fn().mockResolvedValue({ data: null, error: null }) }),
    rpc: vi.fn().mockResolvedValue({ data: [], error: null }),
  }),
}))

import { recommendHagwons, saveChildProfile, loadChildProfile } from './hagwon'

describe('recommendHagwons', () => {
  it('validates lat range -90 to 90', async () => {
    const result = await recommendHagwons({ lat: 91, lng: 128 })
    expect(result).toMatchObject({ error: 'invalid_input' })
  })
  it('validates lng range -180 to 180', async () => {
    const result = await recommendHagwons({ lat: 35, lng: 181 })
    expect(result).toMatchObject({ error: 'invalid_input' })
  })
  it('validates ageGroup enum values', async () => {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const result = await recommendHagwons({ lat: 35, lng: 128, ageGroup: '대학생' as any })
    expect(result).toMatchObject({ error: 'invalid_input' })
  })
  it('returns results and comment for valid input', async () => {
    const result = await recommendHagwons({ lat: 35.22, lng: 128.68 })
    expect(result).toHaveProperty('results')
    expect(result).toHaveProperty('comment')
  })
})

describe('saveChildProfile', () => {
  it('returns unauthorized for non-logged-in user', async () => {
    const result = await saveChildProfile({ nickname: '민준', age_group: '초등저', subject_prefs: [] })
    expect(result).toMatchObject({ error: 'unauthorized' })
  })
})

describe('loadChildProfile', () => {
  it('returns null for non-logged-in user', async () => {
    const result = await loadChildProfile()
    expect(result).toBeNull()
  })
})
```
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/hagwon-recommend.test.ts src/app/actions/hagwon.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/actions/hagwon.ts 파일이 존재하고 recommendHagwons, saveChildProfile, loadChildProfile를 export한다
    - `grep -c "recommend_hagwons\|createSupabaseServerClient" src/app/actions/hagwon.ts` 결과 >= 2
    - `grep -c "z.number().min(-90)" src/app/actions/hagwon.ts` 결과 >= 1 (lat 범위 검증)
    - `npm run test -- --run src/lib/hagwon-recommend.test.ts` PASS (6개 테스트)
    - `npm run test -- --run src/app/actions/hagwon.test.ts` PASS (4개 테스트)
  </acceptance_criteria>
  <done>Server Actions 3개 구현 완료. 테스트 GREEN. lat/lng zod 검증, 비로그인 guard, Groq fallback 포함.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HagwonRecommendSheet → Server Action | 클라이언트에서 오는 lat/lng/ageGroup 등 신뢰 불가 입력 |
| Server Action → user_child_profiles | 인증 user_id 확인 필수, RLS 이중 방어 |
| Server Action → Groq API | API key 서버 환경변수만, 응답 JSON 파싱 방어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Tampering | recommendHagwons lat/lng 입력 | mitigate | zod `z.number().min(-90).max(90)` / `.min(-180).max(180)` 검증 |
| T-28-11 | Elevation of Privilege | saveChildProfile 비인증 접근 | mitigate | `supabase.auth.getUser()` guard + RLS `auth.uid() = user_id` 이중 방어 |
| T-28-12 | Information Disclosure | GROQ_API_KEY | mitigate | `process.env.GROQ_API_KEY` 서버 전용, 'use server' 지시어로 클라이언트 번들 제외 |
| T-28-13 | Tampering | Groq 응답 | mitigate | try/catch + FALLBACK_COMMENT 반환, 모델 응답 타입 강제 없음 (text만 사용) |
</threat_model>

<verification>
```bash
# 파일 존재 확인
ls -la src/services/neis-hagwon.ts src/lib/data/hagwon-recommend.ts src/app/actions/hagwon.ts

# 테스트 실행
npm run test -- --run src/lib/hagwon-recommend.test.ts
npm run test -- --run src/app/actions/hagwon.test.ts

# export 확인
grep -c "export" src/services/neis-hagwon.ts
grep -c "export async function" src/app/actions/hagwon.ts

# 빌드 확인
npm run lint && npm run build
```
</verification>

<success_criteria>
1. src/services/neis-hagwon.ts: HagwonResult, RecommendInput, ChildProfile export
2. src/lib/data/hagwon-recommend.ts: fetchHagwonRecommendations, fetchChildProfile export
3. src/app/actions/hagwon.ts: recommendHagwons, saveChildProfile, loadChildProfile export
4. 테스트 10개 GREEN (6 scoring + 4 actions)
5. npm run lint && npm run build PASS
</success_criteria>

<output>
완료 후 `.planning/phases/28-hagwon-recommendation/28-04-SUMMARY.md` 생성
</output>
