---
phase: 22-ai-price-prediction
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260530000001_complex_price_predictions.sql
autonomous: true
requirements:
  - PRED-01
  - PRED-02

must_haves:
  truths:
    - "complex_price_predictions 테이블이 존재하고 complex_id + area_bucket + predicted_month UNIQUE 제약을 가진다"
    - "compute_predictions RPC가 단지별 area_bucket별 24개월 매매 실거래를 집계해서 반환한다"
    - "area_bucket 컬럼 CHECK 제약으로 '소형', '59', '84', '대형'만 허용된다"
    - "RLS 정책: authenticated + anon 읽기 가능, 서비스 롤만 쓰기 가능"
  artifacts:
    - path: "supabase/migrations/20260530000001_complex_price_predictions.sql"
      provides: "complex_price_predictions 테이블 + compute_predictions RPC + RLS + GRANT"
      contains: "complex_price_predictions"
  key_links:
    - from: "compute_predictions RPC"
      to: "transactions"
      via: "SQL WHERE cancel_date IS NULL AND superseded_by IS NULL"
      pattern: "cancel_date.*IS NULL.*superseded_by.*IS NULL"
    - from: "complex_price_predictions"
      to: "complexes"
      via: "FOREIGN KEY complex_id REFERENCES complexes(id)"
      pattern: "REFERENCES complexes"
---

<objective>
Phase 22 Wave 1-A: DB 마이그레이션.

complex_price_predictions 테이블과 compute_predictions RPC를 생성한다.
이 플랜은 Wave 1의 예측 엔진(22-01)과 Wave 2 배치(22-02) + UI(22-03)의 데이터 기반이다.

Purpose: 예측값은 DB에 pre-compute 방식으로 저장하고 Vercel RSC에서 조회 전용으로 읽는다.
Output: Supabase 마이그레이션 파일 1개
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/ROADMAP.md
@C:/Users/jung/coding/bds/CLAUDE.md
@C:/Users/jung/coding/bds/supabase/migrations/20260529000001_invest_price_history.sql

<interfaces>
<!-- Phase 21 RPC 패턴 참조 (invest_regional_price_history) -->
<!-- 동일한 area_m2 버킷 분류 로직을 compute_predictions에도 사용 -->
-- area_m2 → area_bucket 매핑:
--   area_m2 < 50  → '소형'
--   area_m2 < 66  → '59'
--   area_m2 < 95  → '84'
--   else          → '대형'
-- transactions 컬럼: complex_id(uuid), deal_type(deal_type enum), deal_date(date),
--   price(integer), area_m2(numeric 6,2), cancel_date(date NULL), superseded_by(uuid NULL)
-- complexes.id는 uuid (NOT integer)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: complex_price_predictions 테이블 + compute_predictions RPC + RLS</name>
  <files>supabase/migrations/20260530000001_complex_price_predictions.sql</files>
  <behavior>
    - 테이블 생성 시 누락 컬럼(ai_commentary, ai_cached_at) 포함
    - UNIQUE (complex_id, area_bucket, predicted_month) 제약 — upsert 충돌 키
    - RLS enabled: SELECT은 anon + authenticated, INSERT/UPDATE/DELETE는 service_role only
    - compute_predictions RPC는 STABLE로 선언하고 SECURITY DEFINER 사용 금지 (읽기 전용)
    - cancel_date IS NULL AND superseded_by IS NULL 필터 반드시 포함
    - TX count 10건 미만인 (complex_id, area_bucket) 조합은 RPC에서 제외 (per D-02)
  </behavior>
  <action>
마이그레이션 파일을 생성한다. 내용:

1. complex_price_predictions 테이블:
```sql
CREATE TABLE IF NOT EXISTS public.complex_price_predictions (
  id                   uuid          DEFAULT gen_random_uuid() PRIMARY KEY,
  complex_id           uuid          NOT NULL REFERENCES public.complexes(id) ON DELETE CASCADE,
  area_bucket          text          NOT NULL CHECK (area_bucket IN ('소형','59','84','대형')),
  predicted_month      date          NOT NULL,  -- 월 첫날 정규화: '2026-07-01'
  predicted_price_mean integer       NOT NULL,  -- 만원
  predicted_price_lower integer,               -- 80% CI 하한
  predicted_price_upper integer,               -- 80% CI 상한
  model_name           text          NOT NULL CHECK (model_name IN ('holt-winters','double-exp','linear','insufficient-data')),
  training_mape        real,                   -- 마지막 6개월 hold-out MAPE (0.0~1.0)
  training_count       integer,               -- 학습에 사용된 월 수
  ai_commentary        text,                  -- Claude Haiku 한국어 해설 (숫자 없음)
  ai_cached_at         timestamptz,           -- 해설 캐시 시각
  computed_at          timestamptz   NOT NULL DEFAULT now(),
  UNIQUE (complex_id, area_bucket, predicted_month)
);
```

2. RLS 정책:
```sql
ALTER TABLE public.complex_price_predictions ENABLE ROW LEVEL SECURITY;
CREATE POLICY "read_predictions" ON public.complex_price_predictions
  FOR SELECT USING (true);
```
(쓰기는 service_role이 RLS bypass하므로 별도 정책 불필요)

3. 인덱스:
```sql
CREATE INDEX IF NOT EXISTS idx_predictions_complex_month
  ON public.complex_price_predictions (complex_id, area_bucket, predicted_month);
```

4. compute_predictions RPC (배치 스크립트에서 원천 데이터 조회용):
```sql
CREATE OR REPLACE FUNCTION public.compute_predictions(
  p_complex_id  uuid,
  p_area_bucket text,
  p_months      int DEFAULT 30
)
RETURNS TABLE (year_month text, avg_price numeric, tx_count bigint)
LANGUAGE sql STABLE
AS $$
  SELECT
    to_char(deal_date, 'YYYY-MM') AS year_month,
    ROUND(AVG(price))             AS avg_price,
    COUNT(*)                      AS tx_count
  FROM public.transactions
  WHERE
    complex_id    = p_complex_id
    AND deal_type = 'sale'::public.deal_type
    AND deal_date >= (CURRENT_DATE - (p_months || ' months')::interval)::date
    AND cancel_date   IS NULL
    AND superseded_by IS NULL
    AND CASE
          WHEN area_m2 < 50 THEN '소형'
          WHEN area_m2 < 66 THEN '59'
          WHEN area_m2 < 95 THEN '84'
          ELSE '대형'
        END = p_area_bucket
  GROUP BY to_char(deal_date, 'YYYY-MM')
  ORDER BY year_month ASC
$$;

GRANT EXECUTE ON FUNCTION public.compute_predictions(uuid, text, int)
  TO authenticated, anon;
```

5. GRANT:
```sql
GRANT SELECT ON public.complex_price_predictions TO authenticated, anon;
GRANT ALL ON public.complex_price_predictions TO service_role;
```
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && npx supabase db push --local 2>&1 | tail -5 || echo "SKIP: local supabase not running; validate SQL syntax only" && node -e "const fs=require('fs');const sql=fs.readFileSync('supabase/migrations/20260530000001_complex_price_predictions.sql','utf8');const checks=['complex_price_predictions','holt-winters','ai_commentary','ai_cached_at','cancel_date','superseded_by','REFERENCES public.complexes','ENABLE ROW LEVEL SECURITY','compute_predictions'];let pass=true;checks.forEach(c=>{if(!sql.includes(c)){console.error('MISSING:',c);pass=false;}});if(pass)console.log('SQL syntax check: all keywords present');"</automated>
  </verify>
  <done>
    - supabase/migrations/20260530000001_complex_price_predictions.sql 파일 존재
    - 테이블에 id, complex_id, area_bucket, predicted_month, predicted_price_mean, predicted_price_lower, predicted_price_upper, model_name, training_mape, training_count, ai_commentary, ai_cached_at, computed_at 컬럼 모두 존재
    - UNIQUE (complex_id, area_bucket, predicted_month) 제약 존재
    - RLS ENABLE 및 SELECT 정책 존재
    - compute_predictions RPC 함수 정의 존재
    - cancel_date IS NULL AND superseded_by IS NULL 필터 포함
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배치 스크립트→DB | GitHub Actions가 service_role key로 INSERT — 비공개 시크릿 관리 필수 |
| API Route→DB | RSC가 readonly client로 SELECT — RLS로 쓰기 차단됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-00-01 | Tampering | complex_price_predictions INSERT | mitigate | RLS: SELECT only for anon/authenticated; service_role bypass는 GitHub Secrets에서만 관리 |
| T-22-00-02 | Information Disclosure | ai_commentary (Claude 해설) | accept | 예측 해설은 공개 정보; PII 없음 |
| T-22-00-03 | Denial of Service | compute_predictions RPC 대량 호출 | accept | STABLE 함수 + Supabase 연결 풀링; 배치에서만 호출 |
</threat_model>

<verification>
```bash
# SQL 파일 존재 확인
test -f supabase/migrations/20260530000001_complex_price_predictions.sql && echo "OK" || echo "MISSING"

# 필수 키워드 존재 확인
node -e "
const fs = require('fs');
const sql = fs.readFileSync('supabase/migrations/20260530000001_complex_price_predictions.sql', 'utf8');
const required = [
  'complex_price_predictions',
  'ai_commentary',
  'ai_cached_at',
  'model_name',
  'training_mape',
  'UNIQUE (complex_id, area_bucket, predicted_month)',
  'ENABLE ROW LEVEL SECURITY',
  'compute_predictions',
  'cancel_date   IS NULL',
  'superseded_by IS NULL',
];
const missing = required.filter(r => !sql.includes(r));
if (missing.length === 0) console.log('PASS');
else { console.error('FAIL — missing:', missing); process.exit(1); }
"
```
</verification>

<success_criteria>
1. supabase/migrations/20260530000001_complex_price_predictions.sql 파일이 존재한다
2. complex_price_predictions 테이블에 모든 컬럼(ai_commentary, ai_cached_at 포함)이 정의된다
3. UNIQUE (complex_id, area_bucket, predicted_month) 제약으로 upsert 충돌 처리가 가능하다
4. RLS가 활성화되어 있고 anon/authenticated는 SELECT만, service_role은 ALL 권한을 가진다
5. compute_predictions RPC에 cancel_date IS NULL AND superseded_by IS NULL 필터가 포함된다
6. npm run lint && npm run build 통과 (SQL만 추가이므로 TS 영향 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/22-ai-price-prediction/22-00-SUMMARY.md`를 생성한다.
</output>
