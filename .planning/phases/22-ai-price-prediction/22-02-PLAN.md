---
phase: 22-ai-price-prediction
plan: "02"
type: execute
wave: 2
depends_on:
  - "22-00"
  - "22-01"
files_modified:
  - scripts/compute-predictions.ts
  - .github/workflows/compute-predictions.yml
autonomous: true
requirements:
  - PRED-02

must_haves:
  truths:
    - "compute-predictions.ts 스크립트가 complexes 테이블 전체를 순회하며 예측값을 complex_price_predictions에 upsert한다"
    - "거래 10건 미만 (complex_id, area_bucket) 조합은 스킵된다 (per D-02)"
    - "compute_predictions RPC를 통해 원천 데이터를 조회한다"
    - "GitHub Actions 워크플로우가 매일 새벽 (KST 02:00 = UTC 17:00) 실행된다"
    - "스크립트 실패 시 exit code 1로 GitHub Actions 알림이 트리거된다"
  artifacts:
    - path: "scripts/compute-predictions.ts"
      provides: "전체 단지 예측 배치 스크립트"
      contains: "complex_price_predictions"
    - path: ".github/workflows/compute-predictions.yml"
      provides: "GitHub Actions 일배치 워크플로우"
      contains: "compute-predictions"
  key_links:
    - from: "scripts/compute-predictions.ts"
      to: "src/lib/prediction/engine.ts"
      via: "import { forecast } from '../src/lib/prediction/engine'"
      pattern: "forecast"
    - from: "scripts/compute-predictions.ts"
      to: "complex_price_predictions"
      via: "supabase.from('complex_price_predictions').upsert()"
      pattern: "onConflict.*complex_id.*area_bucket.*predicted_month"
---

<objective>
Phase 22 Wave 2-A: GitHub Actions 배치 스크립트.

모든 단지 × 4개 area_bucket 조합에 대해 예측값을 계산하고 complex_price_predictions 테이블에
upsert하는 TypeScript 스크립트를 작성하고, 매일 새벽 자동 실행하는 GitHub Actions 워크플로우를 추가한다.

Purpose: 예측 계산을 Vercel serverless가 아닌 GitHub Actions에서 실행해 번들 크기·실행시간 제약을 우회.
Output: scripts/compute-predictions.ts + .github/workflows/compute-predictions.yml
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/ROADMAP.md
@C:/Users/jung/coding/bds/CLAUDE.md
@C:/Users/jung/coding/bds/.planning/phases/22-ai-price-prediction/22-00-PLAN.md
@C:/Users/jung/coding/bds/.planning/phases/22-ai-price-prediction/22-01-PLAN.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/22-RESEARCH-prediction.md

<interfaces>
<!-- compute_predictions RPC 반환 타입 (22-00에서 생성) -->
-- compute_predictions(p_complex_id uuid, p_area_bucket text, p_months int)
-- RETURNS TABLE (year_month text, avg_price numeric, tx_count bigint)

<!-- complex_price_predictions upsert 필드 (22-00에서 생성) -->
-- onConflict: 'complex_id,area_bucket,predicted_month'
-- 필드: complex_id, area_bucket, predicted_month (date), predicted_price_mean (int),
--       predicted_price_lower (int), predicted_price_upper (int),
--       model_name (text), training_mape (real), training_count (int), computed_at

<!-- forecast() 반환 타입 (22-01에서 생성) -->
interface PredictionResult {
  forecasts: Array<{ yearMonth: string; mean: number; lower: number; upper: number }>
  modelName: 'holt-winters' | 'double-exp' | 'linear' | 'insufficient-data'
  trainingMape: number
  trainingCount: number
}

<!-- 기존 GitHub Actions 패턴 (.github/workflows/molit-daily.yml 참조) -->
-- setup-node v4, node-version '22', cache: 'npm'
-- npm ci → npx tsx scripts/...
-- secrets: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: compute-predictions.ts 배치 스크립트</name>
  <files>scripts/compute-predictions.ts</files>
  <action>
scripts/compute-predictions.ts를 작성한다.

**로직:**
1. `createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)` 로 Supabase 클라이언트 생성
2. complexes 테이블에서 전체 id 조회 (페이지네이션: limit 1000, range로 분할)
3. AREA_BUCKETS = ['소형', '59', '84', '대형'] 순회
4. 각 (complex_id, area_bucket) 쌍에 대해:
   a. `compute_predictions(complex_id, area_bucket, 30)` RPC 호출
   b. tx_count 합계 < 10건 이면 스킵 (D-02 준수)
   c. PricePoint[] 변환: `{ yearMonth: row.year_month, avgPrice: Number(row.avg_price), txCount: Number(row.tx_count) }`
   d. `forecast(pricePoints, 6)` 호출 → null이면 스킵
   e. forecasts 배열을 6개 upsert 행으로 변환:
      - predicted_month: `yearMonth + '-01'` (월 첫날 정규화)
      - predicted_price_mean: Math.round(f.mean)
      - predicted_price_lower: Math.round(f.lower)
      - predicted_price_upper: Math.round(f.upper)
   f. `.upsert(rows, { onConflict: 'complex_id,area_bucket,predicted_month' })`
5. 진행 상황 console.log: `[INFO] complex ${idx}/${total} — ${skipped} skipped`
6. 최종 요약 출력: `총 ${processed}개 단지 처리, ${skipped}개 스킵, ${errors.length}개 에러`
7. errors.length > 0 이면 `process.exit(1)` (GitHub Actions 알림 트리거)

**중요 제약:**
- `import 'server-only'` 금지 (Node.js CLI 환경)
- `.env.local` 대신 환경변수 직접 읽기: `process.env.NEXT_PUBLIC_SUPABASE_URL`
- 에러는 배열에 수집 후 마지막에 일괄 출력 (한 단지 실패 시 전체 중단하지 않음)
- 단지 간 병렬 처리: area_bucket 4개는 Promise.all()로 병렬, 단지는 순차 (Supabase 연결 풀 보호)

```typescript
// scripts/compute-predictions.ts
import { createClient } from '@supabase/supabase-js'
import { forecast } from '../src/lib/prediction/engine'

const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL!
const SERVICE_KEY  = process.env.SUPABASE_SERVICE_ROLE_KEY!
const AREA_BUCKETS = ['소형', '59', '84', '대형'] as const

// ... 위 로직 구현
```
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && node -e "
const fs = require('fs');
const code = fs.readFileSync('scripts/compute-predictions.ts', 'utf8');
const checks = [
  'SUPABASE_SERVICE_ROLE_KEY',
  'complex_price_predictions',
  'onConflict',
  'complex_id,area_bucket,predicted_month',
  'process.exit(1)',
  \"import { forecast }\",
  'tx_count',
  \"compute_predictions\",   // RPC 호출 필수 검증
  \"rows.reduce\",           // tx_count 합산 집계 방식 검증
];
let pass = true;
checks.forEach(c => {
  if (!code.includes(c)) { console.error('MISSING:', c); pass = false; }
});
if (pass) console.log('PASS: all required patterns present');
else process.exit(1);
"</automated>
  </verify>
  <done>
    - scripts/compute-predictions.ts 파일 존재
    - createClient with SERVICE_ROLE_KEY 사용
    - tx_count < 10 스킵 로직 존재 (D-02)
    - onConflict: 'complex_id,area_bucket,predicted_month' 사용
    - errors 수집 후 process.exit(1) 패턴 존재
    - 'server-only' 임포트 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions 워크플로우</name>
  <files>.github/workflows/compute-predictions.yml</files>
  <action>
.github/workflows/compute-predictions.yml을 작성한다.

**스펙:**
- 스케줄: `'0 17 * * *'` (KST 02:00 = UTC 17:00, 매일)
- workflow_dispatch 트리거도 추가 (수동 실행)
- ubuntu-latest, timeout-minutes: 30
- steps:
  1. `actions/checkout@v4`
  2. `actions/setup-node@v4` (node-version: '22', cache: 'npm')
  3. `npm ci`
  4. Run compute-predictions:
     ```
     env:
       NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
       SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
     run: npx tsx scripts/compute-predictions.ts
     ```

기존 `.github/workflows/molit-daily.yml` 패턴을 따른다.
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && node -e "
const fs = require('fs');
const code = fs.readFileSync('.github/workflows/compute-predictions.yml', 'utf8');
const checks = [
  '0 17 * * *',
  'NEXT_PUBLIC_SUPABASE_URL',
  'SUPABASE_SERVICE_ROLE_KEY',
  'compute-predictions.ts',
  'timeout-minutes',
  'workflow_dispatch',
];
let pass = true;
checks.forEach(c => {
  if (!code.includes(c)) { console.error('MISSING:', c); pass = false; }
});
if (pass) console.log('PASS');
else process.exit(1);
"</automated>
  </verify>
  <done>
    - .github/workflows/compute-predictions.yml 존재
    - cron: '0 17 * * *' (KST 02:00)
    - SUPABASE_SERVICE_ROLE_KEY secrets 참조
    - workflow_dispatch 트리거 존재
    - npx tsx scripts/compute-predictions.ts 실행
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions→Supabase | SUPABASE_SERVICE_ROLE_KEY — GitHub Secrets 보관 필수 |
| 배치 스크립트 출력 | console.log에 API 키, 가격 원본 데이터 로그 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | GitHub Secrets에만 저장; 스크립트 출력에 키 로그 금지 |
| T-22-02-02 | Tampering | complex_price_predictions upsert | mitigate | onConflict로 중복 방지; service_role만 쓰기 가능 (RLS 22-00) |
| T-22-02-03 | Denial of Service | 전체 단지 × 4 버킷 순회 | accept | 2,000단지 × 4 = 8,000 RPC 호출; 30분 timeout으로 처리 가능 |
</threat_model>

<verification>
```bash
# 파일 존재 확인
test -f scripts/compute-predictions.ts && echo "script OK"
test -f .github/workflows/compute-predictions.yml && echo "workflow OK"

# 스크립트 패턴 검증
node -e "
const fs = require('fs');
const code = fs.readFileSync('scripts/compute-predictions.ts', 'utf8');
['SUPABASE_SERVICE_ROLE_KEY', 'complex_price_predictions', \"onConflict\",
 'forecast', 'tx_count', 'process.exit'].forEach(c => {
  if (!code.includes(c)) { console.error('MISSING:', c); process.exit(1); }
});
console.log('PASS');
"

# TypeScript 타입 체크 (스크립트 파일 포함)
npm run lint 2>&1 | tail -10
```
</verification>

<success_criteria>
1. scripts/compute-predictions.ts: createClient with service role key 사용
2. tx_count 집계 < 10건 (complex_id, area_bucket 조합) 시 스킵 (D-02)
3. onConflict: 'complex_id,area_bucket,predicted_month' upsert
4. 에러 발생 시 process.exit(1) (GitHub Actions 알림)
5. .github/workflows/compute-predictions.yml: cron '0 17 * * *', timeout 30분
6. 환경변수 SUPABASE_SERVICE_ROLE_KEY GitHub Secrets 참조
7. npm run lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/22-ai-price-prediction/22-02-SUMMARY.md`를 생성한다.
</output>
