---
phase: 22-ai-price-prediction
plan: "03"
type: execute
wave: 2
depends_on:
  - "22-00"
  - "22-01"
files_modified:
  - src/lib/data/invest.ts
  - src/components/invest/RegionalPriceChart.tsx
  - src/components/invest/RegionalPriceChartWrapper.tsx
  - src/app/invest/page.tsx
  - src/app/api/invest/prediction-commentary/route.ts
autonomous: true
requirements:
  - PRED-03
  - PRED-04

must_haves:
  truths:
    - "RegionalPriceChart에서 기존 실거래 데이터는 실선으로, 예측 구간은 점선으로 구분 표시된다"
    - "예측 데이터가 없거나 모델이 null을 반환하면 차트는 예측선 없이 정상 표시된다 (D-07)"
    - "Claude Haiku 해설 카드가 /invest 페이지 차트 아래에 표시된다"
    - "AI 해설에 숫자(가격)가 포함되지 않는다 (hallucination 방지)"
    - "법적 면책 문구: 'AI 예측은 참고용이며 투자 판단의 근거로 사용 불가'가 UI에 존재한다"
    - "해설 ISR: revalidate 604800 (1주일)"
    - "거래 10건 미만 단지는 예측 구간이 숨겨진다 (D-02)"
  artifacts:
    - path: "src/lib/data/invest.ts"
      provides: "getRegionalPricePredictions() 함수 추가"
      exports: ["getRegionalPricePredictions", "PredictionPoint"]
    - path: "src/components/invest/RegionalPriceChart.tsx"
      provides: "점선 예측 구간 + 신뢰구간 영역 렌더링"
      contains: "strokeDasharray"
    - path: "src/app/invest/page.tsx"
      provides: "예측 데이터 fetch + AI 해설 카드 렌더"
      contains: "ai_commentary"
    - path: "src/app/api/invest/prediction-commentary/route.ts"
      provides: "Claude Haiku 해설 생성 API Route (ISR)"
      contains: "haiku"
  key_links:
    - from: "src/app/invest/page.tsx"
      to: "complex_price_predictions"
      via: "getRegionalPricePredictions() → Supabase SELECT"
      pattern: "getRegionalPricePredictions"
    - from: "src/app/api/invest/prediction-commentary/route.ts"
      to: "Anthropic claude-haiku"
      via: "@anthropic-ai/sdk messages.create"
      pattern: "haiku"
    - from: "RegionalPriceChart"
      to: "예측 데이터"
      via: "predictionData prop → dashed Line"
      pattern: "strokeDasharray"
---

<objective>
Phase 22 Wave 2-B: /invest 차트 예측선 + Claude Haiku 해설 카드 UI.

RegionalPriceChart에 점선 예측 구간을 추가하고, Claude Haiku를 사용한 한국어 트렌드 해설 카드를
/invest 페이지에 통합한다. AI 해설은 ISR로 1주일 캐싱한다.

Purpose: 통계 예측 수치를 시각화하고, LLM은 숫자 없는 트렌드 방향 해설만 제공.
Output: invest.ts 확장 + RegionalPriceChart 수정 + page.tsx 수정 + API Route 신규
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jung/coding/bds/.planning/ROADMAP.md
@C:/Users/jung/coding/bds/CLAUDE.md
@C:/Users/jung/coding/bds/.planning/phases/22-ai-price-prediction/22-00-PLAN.md
@C:/Users/jung/coding/bds/.planning/phases/22-ai-price-prediction/22-01-PLAN.md
@C:/Users/jung/coding/bds/.planning/phases/21-invest-analysis/22-RESEARCH-prediction.md

<interfaces>
<!-- 기존 RegionalPriceChart props (src/components/invest/RegionalPriceChart.tsx) -->
interface RegionalPriceChartProps {
  data:  RegionalPricePoint[]   // 실거래 이력 (기존)
  title: string
}
// 확장: predictionData?: PredictionPoint[]

<!-- 기존 RegionalPricePoint (src/lib/data/invest.ts) -->
interface RegionalPricePoint {
  yearMonth: string   // 'YYYY-MM'
  avgPrice: number
  txCount: number
}

<!-- 추가할 PredictionPoint -->
interface PredictionPoint {
  yearMonth: string   // 'YYYY-MM'
  mean: number        // 만원
  lower: number       // 80% CI 하한
  upper: number       // 80% CI 상한
  modelName: string
  trainingMape: number
}

<!-- complex_price_predictions 조회 패턴 (22-00에서 생성) -->
-- SELECT predicted_month, predicted_price_mean, predicted_price_lower,
--        predicted_price_upper, model_name, training_mape
-- FROM complex_price_predictions
-- WHERE complex_id = ? AND area_bucket = ? AND computed_at >= (now() - interval '2 days')
-- 지역 집계용: sgg_code 기반 중위값 집계 필요

<!-- Recharts AreaChart/LineChart 기존 패턴 -->
-- RegionalPriceChart.tsx: AreaChart + Area + XAxis + YAxis + Tooltip
-- 예측선 추가: Line 컴포넌트 (strokeDasharray="5 3"), ReferenceLine은 사용 금지

<!-- @anthropic-ai/sdk 패턴 (기존 사용 확인됨) -->
import Anthropic from '@anthropic-ai/sdk'
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
await client.messages.create({
  model: 'claude-haiku-4-5-20251001',
  max_tokens: 300,
  messages: [...]
})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: invest.ts 확장 + RegionalPriceChart 예측선 추가</name>
  <files>src/lib/data/invest.ts, src/components/invest/RegionalPriceChart.tsx</files>
  <action>
**src/lib/data/invest.ts 확장:**

파일 하단에 아래를 추가한다 (기존 코드 수정 없음):

```typescript
export interface PredictionPoint {
  yearMonth: string
  mean: number
  lower: number
  upper: number
  modelName: string
  trainingMape: number
}

/**
 * 지역 집계 예측값 조회.
 * complex_price_predictions 테이블에서 sgg_code 지역의 단지들 예측 중위값을 반환.
 * 예측 데이터가 없으면 빈 배열 반환 (graceful degradation, D-07).
 */
export async function getRegionalPricePredictions(
  supabase:   SupabaseClient<Database>,
  sggCode:    string | undefined,
  areaBucket: AreaBucket | undefined,
): Promise<PredictionPoint[]> {
  // complex_price_predictions는 단지별이므로, sgg_code 기반 집계 필요.
  // 먼저 해당 sgg_code 단지 id 목록 조회 후 예측 중위값 계산.
  // 구현 단순화: sgg_code가 없으면 빈 배열 반환 (지역 전체 예측은 미지원 — 단지별만)
  if (!sggCode || !areaBucket) return []

  const twoDaysAgo = new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString()

  // 1) 해당 sgg_code의 단지 id 조회
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: complexIds } = await (supabase as any)
    .from('complexes')
    .select('id')
    .eq('sgg_code', sggCode)
    .limit(500)

  if (!complexIds || complexIds.length === 0) return []
  const ids = (complexIds as { id: string }[]).map(c => c.id)

  // 2) 예측 데이터 조회 (최근 2일 내 computed)
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase as any)
    .from('complex_price_predictions')
    .select('predicted_month, predicted_price_mean, predicted_price_lower, predicted_price_upper, model_name, training_mape')
    .in('complex_id', ids)
    .eq('area_bucket', areaBucket)
    .gte('computed_at', twoDaysAgo)
    .order('predicted_month', { ascending: true })

  if (error || !data) return []

  // 3) 같은 predicted_month끼리 mean/lower/upper 각각 중위값 집계
  type MonthBucket = { means: number[]; lowers: number[]; uppers: number[] }
  const byMonth = new Map<string, MonthBucket>()
  for (const row of (data as Array<{
    predicted_month: string
    predicted_price_mean: number
    predicted_price_lower: number
    predicted_price_upper: number
    model_name: string
    training_mape: number
  }>)) {
    const ym = row.predicted_month.slice(0, 7) // 'YYYY-MM'
    if (!byMonth.has(ym)) byMonth.set(ym, { means: [], lowers: [], uppers: [] })
    const b = byMonth.get(ym)!
    b.means.push(row.predicted_price_mean)
    b.lowers.push(row.predicted_price_lower)
    b.uppers.push(row.predicted_price_upper)
  }

  // 중위값 계산 (DB 저장된 실제 Holt-Winters 신뢰구간 사용 — 하드코딩 ±5% 금지)
  function median(arr: number[]): number {
    const sorted = [...arr].sort((a, b) => a - b)
    const mid = Math.floor(sorted.length / 2)
    return sorted.length % 2 === 0
      ? Math.round((sorted[mid - 1]! + sorted[mid]!) / 2)
      : sorted[mid]!
  }

  return Array.from(byMonth.entries())
    .sort(([a], [b]) => a.localeCompare(b))
    .map(([ym, b]) => ({
      yearMonth:    ym,
      mean:         median(b.means),
      lower:        median(b.lowers),   // 실제 예측 하한 (22-00 Holt-Winters CI)
      upper:        median(b.uppers),   // 실제 예측 상한
      modelName:    'regional-median',
      trainingMape: 0,
    }))
}
```

**src/components/invest/RegionalPriceChart.tsx 수정:**

props 인터페이스에 `predictionData?: PredictionPoint[]` 추가.
컴포넌트 내부에 Recharts `Line` 컴포넌트로 예측선 추가:

1. import에 `Line, ComposedChart` 추가 (AreaChart → ComposedChart로 변경 또는 LineChart 오버레이).
   AreaChart 대신 ComposedChart 사용해 Area + Line 혼합 렌더링.
   ```tsx
   import { ComposedChart, Area, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
   ```

2. predictionData를 별도 data 배열로 처리하지 않고, 기존 data에 예측 키 추가:
   ```typescript
   const mergedData = [
     ...data.map(d => ({ ...d, type: 'actual' as const })),
     ...(predictionData ?? []).map(d => ({
       yearMonth: d.yearMonth,
       avgPrice: undefined,     // 실거래 없음
       txCount: 0,
       predMean:  d.mean,
       predLower: d.lower,
       predUpper: d.upper,
       type: 'prediction' as const,
     })),
   ].sort((a, b) => a.yearMonth.localeCompare(b.yearMonth))
   ```

3. ComposedChart data={mergedData}에서:
   - `<Area dataKey="avgPrice" ...>` (기존 실거래 영역)
   - `<Line dataKey="predMean" stroke={color} strokeWidth={2} strokeDasharray="5 3" dot={false} connectNulls />` (예측 점선)
   - 신뢰구간은 Area로 렌더링: `<Area dataKey="predUpper" stroke="none" fill={color} fillOpacity={0.08} connectNulls />`
     + `<Area dataKey="predLower" stroke="none" fill="white" fillOpacity={1} connectNulls />` (하한 아래 채우기 제거)

4. predictionData가 비어있으면 예측 관련 요소를 렌더하지 않음 (D-07).

5. MAPE 배지: predictionData 존재 시 "예측 참고선 — 평균 오차 약 N%" 텍스트 표시 (font 11px, color var(--fg-tertiary)).

**금지사항:**
- backdrop-blur, gradient-text, glow, 보라/인디고 색상 없음
- predictionData가 null/빈 배열일 때 에러 없이 차트만 표시
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>
    - invest.ts에 PredictionPoint 인터페이스와 getRegionalPricePredictions 함수 export
    - RegionalPriceChart에 predictionData?: PredictionPoint[] prop 추가
    - 예측선이 strokeDasharray="5 3" 점선으로 렌더링
    - predictionData 없을 때 차트 정상 표시 (D-07)
    - npm run lint 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: /invest page.tsx 통합 + Claude Haiku 해설 카드</name>
  <files>src/app/invest/page.tsx, src/app/api/invest/prediction-commentary/route.ts</files>
  <action>
**src/app/api/invest/prediction-commentary/route.ts 신규 생성:**

```typescript
// ISR Route: revalidate = 604800 (1주일)
export const revalidate = 604800

import Anthropic from '@anthropic-ai/sdk'
import { NextRequest } from 'next/server'

export async function GET(request: NextRequest): Promise<Response> {
  const { searchParams } = request.nextUrl
  const sggCode   = searchParams.get('sgg_code')   ?? ''
  const areaBucket = searchParams.get('area_bucket') ?? ''
  const trend      = searchParams.get('trend')       ?? 'neutral'  // 'up' | 'down' | 'neutral'
  const mape       = parseFloat(searchParams.get('mape') ?? '0')

  // 입력 검증
  const ALLOWED_SGG = ['48121','48123','48125','48127','48128','48129','48250','']
  const ALLOWED_AREA = ['소형','59','84','대형','']
  const ALLOWED_TREND = ['up','down','neutral']
  if (!ALLOWED_SGG.includes(sggCode)) return Response.json({ commentary: null }, { status: 400 })
  if (!ALLOWED_AREA.includes(areaBucket)) return Response.json({ commentary: null }, { status: 400 })
  if (!ALLOWED_TREND.includes(trend)) return Response.json({ commentary: null }, { status: 400 })

  const apiKey = process.env.ANTHROPIC_API_KEY
  if (!apiKey) return Response.json({ commentary: null })

  try {
    const client = new Anthropic({ apiKey })

    // 지역/타입 레이블
    const SGG_LABEL: Record<string, string> = {
      '48121': '창원 의창구', '48123': '창원 창원구', '48125': '창원 성산구',
      '48127': '창원 마산합포구', '48128': '창원 마산회원구', '48129': '창원 진해구',
      '48250': '김해시',
    }
    const regionLabel = SGG_LABEL[sggCode] ?? '창원·김해 전체'
    const areaLabel   = areaBucket ? `${areaBucket}㎡` : '전체'
    const trendLabel  = trend === 'up' ? '상승' : trend === 'down' ? '하락' : '보합'
    const mapeLabel   = mape > 0 ? `(평균 오차율 약 ${Math.round(mape * 100)}%)` : ''

    const message = await client.messages.create({
      model: 'claude-haiku-4-5-20251001',
      max_tokens: 200,
      system: `당신은 한국 부동산 시장 데이터 해설가입니다.
통계 모델 결과를 바탕으로 시장 맥락을 설명합니다.
규칙:
1. 구체적인 가격 숫자(만원, 억원 등)를 절대 언급하지 마세요
2. "오를 것입니다", "내릴 것입니다" 등 단정적 예측 표현 금지
3. 투자 조언 또는 매수/매도 권유 금지
4. 2문장 이내로 간결하게 작성
5. 한국어로만 응답`,
      messages: [{
        role: 'user',
        content: `${regionLabel} ${areaLabel} 아파트 매매 통계 분석 결과:
- 6개월 예측 추세: ${trendLabel} 방향 ${mapeLabel}
- 분석 기간: 최근 24개월 실거래 데이터 기반

위 결과를 바탕으로 시장 상황을 2문장으로 해설해주세요.`,
      }],
    })

    const text = message.content[0]?.type === 'text' ? message.content[0].text : null
    return Response.json({ commentary: text })
  } catch {
    return Response.json({ commentary: null })
  }
}
```

**src/app/invest/page.tsx 수정:**

1. import 추가:
   ```typescript
   import { getRegionalPricePredictions } from '@/lib/data/invest'
   import type { PredictionPoint } from '@/lib/data/invest'
   ```

2. parallel fetch에 예측 데이터 추가:
   ```typescript
   const [priceHistory, rows, predictions] = await Promise.all([
     getRegionalPriceHistory(supabase, sggCode, areaBucket, 24).catch((): RegionalPricePoint[] => []),
     getGapRankings({ sggCode, riskLevel }, supabase).catch((): GapRankingRow[] => []),
     getRegionalPricePredictions(supabase, sggCode, areaBucket).catch((): PredictionPoint[] => []),
   ])
   ```

3. RegionalPriceChartWrapper에 predictionData 전달:
   ```tsx
   <RegionalPriceChartWrapper
     data={priceHistory}
     title={chartTitle}
     predictionData={predictions}
   />
   ```

4. AI 해설 카드를 차트 섹션 아래(법적 면책 위)에 추가:
   - 서버에서 prediction-commentary API를 직접 fetch (ISR 캐시 활용):
   ```typescript
   // sggCode와 areaBucket이 있고 predictions 데이터가 있을 때만 해설 조회
   let aiCommentary: string | null = null
   if (sggCode && areaBucket && predictions.length > 0) {
     const trend = predictions.length >= 2
       ? (predictions[predictions.length - 1]!.mean > predictions[0]!.mean ? 'up' : 'down')
       : 'neutral'
     const avgMape = predictions.reduce((s, p) => s + p.trainingMape, 0) / predictions.length
     try {
       const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'
       const res = await fetch(
         `${siteUrl}/api/invest/prediction-commentary?sgg_code=${sggCode}&area_bucket=${encodeURIComponent(areaBucket)}&trend=${trend}&mape=${avgMape}`,
         { next: { revalidate: 604800 } }
       )
       if (res.ok) {
         const json = await res.json() as { commentary: string | null }
         aiCommentary = json.commentary
       }
     } catch { /* 해설 없이 차트만 표시 */ }
   }
   ```
   - 카드 UI:
   ```tsx
   {aiCommentary && (
     <div style={{
       marginTop: 12,
       padding: '12px 16px',
       background: 'var(--bg-surface-2)',
       borderRadius: 8,
       borderLeft: '3px solid var(--dj-orange)',
     }}>
       <p style={{
         font: '500 11px/1.4 var(--font-sans)',
         color: 'var(--fg-tertiary)',
         margin: '0 0 4px',
       }}>
         AI 시장 해설 (참고용)
       </p>
       <p style={{
         font: '400 13px/1.6 var(--font-sans)',
         color: 'var(--fg-sec)',
         margin: 0,
       }}>
         {aiCommentary}
       </p>
     </div>
   )}
   ```

5. 법적 면책 문구 강화 (D-06):
   기존 법적 면책 문구에 아래를 추가:
   ```
   * AI 예측은 참고용이며 투자 판단의 근거로 사용 불가.
   ```

**금지사항:**
- backdrop-blur, gradient-text, glow, 보라/인디고 색상 없음
- aiCommentary가 null이면 AI 해설 카드 미렌더 (D-07 graceful degradation)
- Claude Haiku 프롬프트에 숫자 예측값 전달 금지 (trend 방향과 MAPE만 전달)
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/app/api/invest/prediction-commentary/route.ts 존재 (revalidate = 604800)
    - route.ts에 model 'claude-haiku-4-5-20251001' 사용
    - route.ts 프롬프트에 숫자 금지 시스템 프롬프트 포함
    - route.ts 입력 검증(sgg_code, area_bucket, trend allowlist) 존재
    - invest/page.tsx에 getRegionalPricePredictions 임포트 및 호출
    - RegionalPriceChartWrapper에 predictionData prop 전달
    - AI 해설 카드 aiCommentary null 시 미렌더
    - 법적 면책 문구에 "AI 예측은 참고용이며 투자 판단의 근거로 사용 불가" 포함
    - npm run lint && npm run build 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams→API Route | sgg_code, area_bucket, trend — allowlist 검증 필수 |
| API Route→Anthropic SDK | ANTHROPIC_API_KEY — 환경변수에서만 로드 |
| Claude Haiku→클라이언트 | 해설 텍스트 — 숫자/투자조언 포함 금지 (시스템 프롬프트로 제어) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Injection | prediction-commentary searchParams | mitigate | sgg_code, area_bucket, trend allowlist 검증; 미허용 값은 400 반환 |
| T-22-03-02 | Information Disclosure | Claude Haiku 응답 | mitigate | 시스템 프롬프트: 숫자/투자조언/가격 언급 금지; 200토큰 제한 |
| T-22-03-03 | Spoofing | AI 해설을 사실로 오인 | mitigate | D-06 강화 면책 문구: "AI 예측은 참고용이며 투자 판단의 근거로 사용 불가" |
| T-22-03-04 | Denial of Service | Claude API 호출 급증 | mitigate | ISR revalidate=604800 (1주일 캐시); 지역+타입 조합은 14개 이하 |
</threat_model>

<verification>
```bash
# 파일 존재 확인
test -f src/lib/data/invest.ts && echo "invest.ts OK"
test -f src/components/invest/RegionalPriceChart.tsx && echo "Chart OK"
test -f src/app/invest/page.tsx && echo "page.tsx OK"
test -f src/app/api/invest/prediction-commentary/route.ts && echo "API Route OK"

# 핵심 패턴 검증
node -e "
const fs = require('fs');

// invest.ts: PredictionPoint export
const invest = fs.readFileSync('src/lib/data/invest.ts', 'utf8');
if (!invest.includes('PredictionPoint')) { console.error('FAIL: PredictionPoint missing'); process.exit(1); }
if (!invest.includes('getRegionalPricePredictions')) { console.error('FAIL: getRegionalPricePredictions missing'); process.exit(1); }

// RegionalPriceChart: predictionData prop + strokeDasharray
const chart = fs.readFileSync('src/components/invest/RegionalPriceChart.tsx', 'utf8');
if (!chart.includes('predictionData')) { console.error('FAIL: predictionData prop missing'); process.exit(1); }
if (!chart.includes('strokeDasharray')) { console.error('FAIL: strokeDasharray missing'); process.exit(1); }

// page.tsx: predictions fetch + aiCommentary
const page = fs.readFileSync('src/app/invest/page.tsx', 'utf8');
if (!page.includes('getRegionalPricePredictions')) { console.error('FAIL: getRegionalPricePredictions not called'); process.exit(1); }
if (!page.includes('aiCommentary')) { console.error('FAIL: aiCommentary missing'); process.exit(1); }
if (!page.includes('투자 판단의 근거로 사용 불가')) { console.error('FAIL: 법적 면책 문구 missing'); process.exit(1); }

// API Route: revalidate + haiku + system prompt
const route = fs.readFileSync('src/app/api/invest/prediction-commentary/route.ts', 'utf8');
if (!route.includes('604800')) { console.error('FAIL: ISR revalidate missing'); process.exit(1); }
if (!route.includes('haiku')) { console.error('FAIL: claude-haiku model missing'); process.exit(1); }
if (!route.includes('가격 숫자')) { console.error('FAIL: 숫자 금지 시스템 프롬프트 missing'); process.exit(1); }

console.log('PASS: all checks OK');
"

# 빌드
npm run lint && npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
1. getRegionalPricePredictions()와 PredictionPoint가 src/lib/data/invest.ts에서 export된다
2. RegionalPriceChart가 predictionData prop을 받아 strokeDasharray 점선으로 렌더링한다
3. predictionData 없을 때 차트가 에러 없이 정상 표시된다 (D-07)
4. /api/invest/prediction-commentary가 claude-haiku-4-5-20251001을 사용하고 ISR 1주일 캐싱된다
5. Haiku 시스템 프롬프트에 숫자/가격 언급 금지 규칙이 명시된다 (hallucination 방지)
6. /invest page.tsx 법적 면책 문구에 "AI 예측은 참고용이며 투자 판단의 근거로 사용 불가" 포함
7. sgg_code/area_bucket/trend 입력 검증 allowlist가 API Route에 존재한다
8. npm run lint && npm run build 모두 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/22-ai-price-prediction/22-03-SUMMARY.md`를 생성한다.
</output>
