---
phase: 29-mobile-optimization
plan: "04"
type: execute
wave: 3
depends_on:
  - "29-01"
files_modified:
  - src/app/presale/page.tsx
  - src/app/invest/page.tsx
autonomous: true
requirements:
  - MOB-05

must_haves:
  truths:
    - "분양 페이지가 모바일에서 단일 컬럼으로 표시되고 섹션이 세로로 쌓인다"
    - "분양 페이지에 인라인 헤더가 없다 (AppHeader가 대체)"
    - "투자 페이지가 모바일에서 단일 컬럼으로 표시된다"
    - "투자 페이지에 인라인 헤더가 없다 (AppHeader가 대체)"
    - "두 페이지의 필터 탭/칩이 44px 터치 타겟을 만족한다"
    - "두 파일에 max-sm: Tailwind 패턴이 없다"
    - "두 파일의 CSS 변수 색상이 inline style로 유지된다"
  artifacts:
    - path: "src/app/presale/page.tsx"
      provides: "분양 페이지 mobile-first 레이아웃"
      contains: "px-4"
    - path: "src/app/invest/page.tsx"
      provides: "투자 페이지 mobile-first 레이아웃"
      contains: "px-4"
  key_links:
    - from: "src/app/presale/page.tsx"
      to: "기존 Tailwind 그리드 (presale의 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3)"
      via: "이미 검증된 Tailwind 그리드 패턴 유지"
      pattern: "grid-cols-1 sm:"
    - from: "src/app/invest/page.tsx"
      to: "AppHeader (layout.tsx)"
      via: "인라인 헤더 삭제 — layout.tsx AppHeader 대체"
      pattern: "min-h-screen"
---

<objective>
분양 페이지 + 투자 페이지를 mobile-first Tailwind로 재작성한다. 인라인 헤더를 제거하고, inline style 레이아웃을 Tailwind 클래스로 전환하며, 터치 타겟을 44px로 보장한다.

Purpose: 남은 두 주요 페이지의 모바일 최적화를 완료하여 Phase 29 목표를 달성한다.
Output: presale/page.tsx, invest/page.tsx — 두 파일 모두 mobile-first Tailwind 레이아웃으로 재작성.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-PATTERNS.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-UI-SPEC.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-01-SUMMARY.md

<interfaces>
<!-- presale/page.tsx 현재 구조 요약 -->
```
src/app/presale/page.tsx
- export const dynamic = 'force-dynamic'  ← 반드시 유지 (ISR 아님, 동적 렌더링)
- 또는 export const revalidate = N (실제 파일 확인 필요)
- 'use client' 없음 — 유지
- lines 35–50: 인라인 헤더 ← 삭제 대상
- line 52: <main style={{ maxWidth: 1100, margin: '0 auto', padding: '32px 24px' }}>
  ← 변경: <main className="px-4 py-6 sm:px-6 sm:max-w-3xl sm:mx-auto">
- lines 92, 120: className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5"
  ← 이미 올바른 Tailwind 패턴. 변경 불필요.
- 3-tier 섹션 (분양 공고 / 재건축 예정 / 신축 최신순): 각 섹션의 padding/margin inline style을 Tailwind로
```

<!-- invest/page.tsx 현재 구조 요약 -->
```
src/app/invest/page.tsx
- export const revalidate = 3600  ← 반드시 유지 (ISR)
- 'use client' 없음 — 유지
- lines 143–165: 데스크탑 우선 헤더 블록 ← 삭제 대상
- line 143: <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
  ← 변경: <div className="min-h-screen"> (bg-canvas, font-sans는 globals.css에서 body에 이미 설정)
- tabStyle() 함수 (lines 130–140): inline CSSProperties 반환
  ← 변경: Tailwind 클래스 + inline style(색상만)로 전환, min-h-[44px] 추가
- main 컨테이너: style={{ maxWidth: N, margin: '0 auto', padding: '...' }}
  ← 변경: className="px-4 py-6 sm:max-w-screen-lg sm:mx-auto"
```

<!-- Tailwind 전환 패턴 레퍼런스 (PATTERNS.md Pattern G) -->
```tsx
// 컨텐츠 너비 (D-09):
// narrow pages: className="px-4 py-6 sm:max-w-3xl sm:mx-auto"
// wide pages: className="px-4 py-6 sm:max-w-screen-lg sm:mx-auto"

// tabStyle() 전환 패턴 (PATTERNS.md invest 섹션):
// Before: const tabStyle = (active: boolean): React.CSSProperties => ({ ... })
// After: className="inline-flex items-center px-3 rounded-[6px] text-xs font-medium min-h-[44px] whitespace-nowrap"
//        style={{ background: active ? 'var(--dj-orange)' : 'var(--bg-surface-2)', color: active ? '#fff' : 'var(--fg-sec)' }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 분양 페이지 (presale/page.tsx) mobile-first 재작성</name>
  <files>src/app/presale/page.tsx</files>
  <read_first>
    - C:\Users\jung\coding\bds\src\app\presale\page.tsx — 전체 파일 (현재 상태 파악 후 수정)
  </read_first>
  <action>
**반드시 먼저 전체 파일을 읽어 현재 구조를 파악한 후 수정한다.**

**1. 파일 상단 ISR/dynamic 설정 보존 — 절대 변경 금지:**
```tsx
// 파일에 있는 아래 중 하나 유지:
export const dynamic = 'force-dynamic'  // 또는
export const revalidate = N
// 'use client' 추가 절대 금지
```

**2. 인라인 헤더 블록 전체 삭제:**
- `<header>` 또는 헤더 역할 하는 블록 삭제 (로고, nav links 포함)
- UserMenu import 남아있으면 제거 (헤더에서만 사용한 경우)

**3. main 컨테이너 inline style → Tailwind:**
```tsx
// Before:
<main style={{ maxWidth: 1100, margin: '0 auto', padding: '32px 24px' }}>

// After (D-09 narrow page):
<main className="px-4 py-6 sm:px-6 sm:max-w-3xl sm:mx-auto">
```

**4. 기존 Tailwind 그리드 유지 (이미 올바른 패턴):**
```tsx
// presale/page.tsx lines 92, 120 — 이미 올바른 mobile-first Tailwind
className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5"
// 이 라인들은 변경하지 않는다.
```

**5. 섹션 구분 inline style → Tailwind:**
```tsx
// Before: style={{ marginBottom: 48 }} 또는 style={{ paddingTop: 32 }}
// After: className="mb-12" 또는 className="pt-8"
```

**6. 섹션 제목/헤딩 inline style → Tailwind:**
```tsx
// Before: style={{ fontSize: 20, fontWeight: 700, marginBottom: 16 }}
// After: className="text-xl font-bold mb-4"
```

**7. CSS 변수 색상은 inline style 유지 (D-08):**
```tsx
// 유지 (변경 금지):
style={{ color: 'var(--fg-sec)' }}
style={{ background: 'var(--bg-surface-2)' }}
```

금지 사항:
- 'use client' 추가 금지
- max-sm: prefix 사용 금지
- backdrop-blur, gradient-text, glow 사용 금지
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "dynamic\|revalidate" src/app/presale/page.tsx | head -5 && grep -c "max-sm:" src/app/presale/page.tsx && npx tsc --noEmit 2>&1 | grep "presale" | head -10</automated>
  </verify>
  <done>
    - src/app/presale/page.tsx: `dynamic` 또는 `revalidate` 설정 유지
    - 파일에 `'use client'` 없음
    - 파일에 인라인 `<header>` 블록 없음
    - main에 `px-4` 포함
    - 파일에 `max-sm:` 패턴 없음 (grep 결과 0건)
    - TypeScript 타입 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: 투자 페이지 (invest/page.tsx) mobile-first 재작성 + 44px 필터 탭</name>
  <files>src/app/invest/page.tsx</files>
  <read_first>
    - C:\Users\jung\coding\bds\src\app\invest\page.tsx — 전체 파일 (현재 상태 파악 후 수정)
  </read_first>
  <action>
**반드시 먼저 전체 파일을 읽어 현재 구조를 파악한 후 수정한다.**

**1. 파일 상단 보존 — 절대 변경 금지:**
```tsx
export const revalidate = 3600  // 이 라인 변경 금지 (ISR)
// 'use client' 추가 절대 금지
```

**2. 인라인 헤더/상단 데스크탑 블록 삭제:**
- 헤더 역할 블록 전체 삭제 (로고, nav 포함)
- UserMenu import 남아있으면 제거

**3. 최외곽 래퍼 div inline style → Tailwind:**
```tsx
// Before:
<div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>

// After (background, font-family는 globals.css body에 이미 적용됨):
<div className="min-h-screen">
```

**4. main 컨테이너 inline style → Tailwind:**
```tsx
// Before: style={{ maxWidth: N, margin: '0 auto', padding: '...' }}
// After (D-09 wide page):
<main className="px-4 py-6 sm:max-w-screen-lg sm:mx-auto">
```

**5. tabStyle() 함수 → Tailwind + inline style 전환 (핵심 작업):**

tabStyle() 함수를 제거하고 각 탭 Link/button에 직접 className + style 적용:

```tsx
// Before (함수 패턴):
const tabStyle = (active: boolean): React.CSSProperties => ({
  display: 'inline-block', padding: '5px 12px', borderRadius: 6,
  font: '500 12px/1 var(--font-sans)', textDecoration: 'none',
  background: active ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
  color: active ? '#fff' : 'var(--fg-sec)',
  border: '1px solid var(--line-subtle)',
})

// After (직접 className + style):
<Link
  href={...}
  className="inline-flex items-center px-3 rounded-[6px] text-xs font-medium whitespace-nowrap min-h-[44px]"
  style={{
    background: active ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
    color: active ? '#fff' : 'var(--fg-sec)',
    border: '1px solid var(--line-subtle)',
    textDecoration: 'none',
  }}
>
  {label}
</Link>
```

필터 탭 행에 가로 스크롤 처리 (모바일에서 탭이 많을 경우):
```tsx
<div className="flex gap-2 overflow-x-auto pb-1 -mx-4 px-4 mb-4">
  {/* 탭 items */}
</div>
```

**6. 차트 섹션 inline style → Tailwind:**
```tsx
// Before: style={{ marginTop: 32 }}
// After: className="mt-8"

// Before: style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}
// After: className="grid grid-cols-1 sm:grid-cols-2 gap-4"
```

**7. 제목/설명 텍스트 inline style → Tailwind:**
```tsx
// Before: style={{ fontSize: 20, fontWeight: 700 }}
// After: className="text-xl font-bold"

// Before: style={{ fontSize: 14, color: 'var(--fg-sec)' }}
// After: className="text-sm" + style={{ color: 'var(--fg-sec)' }}
```

금지 사항:
- 'use client' 추가 금지
- max-sm: prefix 사용 금지
- backdrop-blur, gradient-text, glow 사용 금지
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "export const revalidate" src/app/invest/page.tsx && grep -c "max-sm:" src/app/invest/page.tsx && grep -n "min-h-\[44px\]" src/app/invest/page.tsx | head -5 && npx tsc --noEmit 2>&1 | grep "invest" | head -10</automated>
  </verify>
  <done>
    - src/app/invest/page.tsx에 `export const revalidate = 3600` 유지
    - 파일에 `'use client'` 없음
    - 파일에 인라인 헤더 블록 없음
    - 필터 탭에 `min-h-[44px]` 포함
    - 파일에 `max-sm:` 패턴 없음 (grep 결과 0건)
    - TypeScript 타입 에러 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → ISR pages | presale, invest 페이지의 searchParams는 기존 검증 로직 유지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Tampering | ISR 캐시 (분양/투자) | mitigate | 'use client' 절대 추가 금지. dynamic/revalidate export 유지. |
| T-29-10 | Tampering | 필터 탭 URL 파라미터 | accept | 기존 searchParams 처리 로직 변경 없음. 레이아웃만 수정. |
</threat_model>

<verification>
Plan 04 완료 기준:

```bash
cd /c/Users/jung/coding/bds

# ISR/dynamic 유지 확인
grep -n "dynamic\|revalidate" src/app/presale/page.tsx | head -3
grep -n "export const revalidate" src/app/invest/page.tsx

# 'use client' 없음 확인
grep -n "use client" src/app/presale/page.tsx src/app/invest/page.tsx || echo "GOOD: no use client"

# max-sm 없음 확인
grep -c "max-sm:" src/app/presale/page.tsx src/app/invest/page.tsx

# 44px 터치 타겟 확인
grep -n "min-h-\[44px\]" src/app/invest/page.tsx

# px-4 모바일 패딩 확인
grep -n "px-4" src/app/presale/page.tsx src/app/invest/page.tsx | head -5

# TypeScript
npx tsc --noEmit 2>&1 | grep -E "presale|invest" | head -10 || echo "PASS"

# lint
npm run lint

# 빌드 전체 검증
npm run build
```
</verification>

<success_criteria>
1. `npm run build` 통과 — 빌드 에러 없음 (Phase 29 최종 게이트)
2. `npm run lint` 통과
3. presale/page.tsx: `dynamic` 또는 `revalidate` 유지, 인라인 헤더 없음, `px-4` 포함, `max-sm:` 없음
4. invest/page.tsx: `export const revalidate = 3600` 유지, 인라인 헤더 없음, 필터 탭에 `min-h-[44px]`, `max-sm:` 없음
5. 두 파일 모두 `'use client'` 없음
6. 전체 5개 페이지 (홈/랭킹/단지상세/분양/투자)에 더이상 인라인 헤더 없음 확인:
   ```bash
   grep -rn "dj-logo" src/app/page.tsx src/app/rankings/page.tsx "src/app/complexes/[id]/page.tsx" src/app/presale/page.tsx src/app/invest/page.tsx || echo "GOOD: all headers removed"
   ```
</success_criteria>

<output>
완료 후 `.planning/phases/29-mobile-optimization/29-04-SUMMARY.md` 생성.
</output>
