---
phase: 29-mobile-optimization
plan: "01"
type: execute
wave: 1
depends_on:
  - "29-00"
files_modified:
  - src/app/layout.tsx
  - src/components/layout/AppHeader.tsx
  - src/components/layout/BottomTabBar.tsx
  - src/components/ui/BottomSheet.tsx
  - src/components/complex/CompareFloatingBar.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - MOB-01
  - MOB-06

must_haves:
  truths:
    - "앱 상단에 로고 + 알림 아이콘만 있는 56px 헤더가 항상 표시된다"
    - "앱 하단에 홈/랭킹/분양/MY 4탭 네비게이션이 항상 고정 표시된다"
    - "현재 경로에 맞는 탭이 --dj-orange 색으로 강조된다"
    - "AppHeader와 BottomTabBar 모두 html2canvas 캡처에서 제외된다"
    - "iOS Safari에서 env(safe-area-inset-bottom)이 정상 동작한다 (viewport-fit=cover 적용)"
    - "공유 BottomSheet 컴포넌트가 open/close props로 제어된다"
    - "CompareFloatingBar가 BottomTabBar 위에 올바르게 위치한다"
  artifacts:
    - path: "src/components/layout/AppHeader.tsx"
      provides: "공유 상단 헤더 (로고 + 알림 버튼)"
      exports: ["AppHeader"]
    - path: "src/components/layout/BottomTabBar.tsx"
      provides: "공유 하단 탭바 (홈/랭킹/분양/MY)"
      exports: ["BottomTabBar"]
    - path: "src/components/ui/BottomSheet.tsx"
      provides: "공유 바텀시트 컴포넌트 (vaul 기반)"
      exports: ["BottomSheet"]
    - path: "src/app/layout.tsx"
      provides: "AppHeader + BottomTabBar 통합 전역 레이아웃"
      contains: "viewportFit: 'cover'"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/AppHeader.tsx"
      via: "import + JSX in body"
      pattern: "AppHeader"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/BottomTabBar.tsx"
      via: "import + JSX after Footer"
      pattern: "BottomTabBar"
    - from: "src/components/layout/BottomTabBar.tsx"
      to: "usePathname"
      via: "next/navigation"
      pattern: "usePathname"
---

<objective>
공유 레이아웃 인프라 구축: AppHeader + BottomTabBar + BottomSheet 신규 컴포넌트 생성, layout.tsx에 통합, CompareFloatingBar 위치 보정.

Purpose: 모든 페이지의 기반이 되는 네비게이션 셸을 먼저 확보한다. 이후 각 page.tsx 재작성(Plan 02~04)은 이 인프라 위에서 독립 진행된다.
Output: AppHeader.tsx, BottomTabBar.tsx, BottomSheet.tsx (신규), layout.tsx (수정), CompareFloatingBar.tsx (수정), vaul + embla-carousel-react 패키지 설치.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-PATTERNS.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-UI-SPEC.md

<interfaces>
<!-- layout.tsx 현재 상태 (lines 1–62) -->
```tsx
// src/app/layout.tsx — 현재 전체
import type { Metadata, Viewport } from 'next'
import localFont from 'next/font/local'
import { NuqsAdapter } from 'nuqs/adapters/next/app'
import { Footer } from '@/components/layout/Footer'
import './globals.css'

export const viewport: Viewport = {
  themeColor: '#ea580c',
  width: 'device-width',
  initialScale: 1,
  // viewportFit: 'cover' ← 미추가 상태 (Pitfall 3)
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko" className={pretendard.variable}>
      <body className="font-sans antialiased">
        <NuqsAdapter>{children}</NuqsAdapter>
        <Footer />
      </body>
    </html>
  )
}
```

<!-- CompareFloatingBar.tsx 핵심 변경 대상 (line 48) -->
```tsx
// src/components/complex/CompareFloatingBar.tsx — 현재 bottom 값
style={{ position: 'fixed', bottom: 80, right: 24, zIndex: 50, minHeight: 44 }}
// 변경 후:
style={{ position: 'fixed', bottom: 'calc(64px + env(safe-area-inset-bottom, 0px) + 16px)', right: 24, zIndex: 50, minHeight: 44 }}
```

<!-- AdminSidebarLinks 패턴 (BottomTabBar의 정확한 아날로그) -->
```tsx
// src/components/admin/AdminSidebarLinks.tsx — 핵심 패턴
'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
// pathname.startsWith(item.href) 로 active 감지
// style={{ color: isActive ? 'var(--fg-pri)' : 'var(--fg-sec)' }} 로 색상 전환
```

<!-- lucide-react 아이콘 — package.json 확인됨 -->
// lucide-react ^0.487.0 이미 설치됨
// 사용 아이콘: Home, BarChart2, Building2, User (탭바), Bell (헤더)
// strokeWidth={1.75} 프로젝트 표준
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vaul + embla-carousel-react 설치</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - C:\Users\jung\coding\bds\package.json — 현재 dependencies 확인 (vaul/embla 미설치 상태 검증)
  </read_first>
  <action>
프로젝트 루트에서 아래 명령을 실행한다:

```bash
cd /c/Users/jung/coding/bds && npm install vaul@1.1.2 embla-carousel-react@8.6.0
```

설치 완료 후 package.json의 dependencies에 아래 두 항목이 추가되어야 한다:
- `"vaul": "1.1.2"`
- `"embla-carousel-react": "8.6.0"`

설치 후 빌드 타입 에러 없음을 확인: `npm run build` (혹은 `npx tsc --noEmit`)
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && node -e "const p = require('./package.json'); console.log(p.dependencies.vaul, p.dependencies['embla-carousel-react'])"</automated>
  </verify>
  <done>package.json에 vaul@1.1.2, embla-carousel-react@8.6.0이 있고 node_modules에 설치됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppHeader + BottomTabBar + BottomSheet 신규 컴포넌트 생성</name>
  <files>
    src/components/layout/AppHeader.tsx
    src/components/layout/BottomTabBar.tsx
    src/components/ui/BottomSheet.tsx
  </files>
  <read_first>
    - C:\Users\jung\coding\bds\src\components\admin\AdminSidebarLinks.tsx — usePathname active 패턴 (정확한 아날로그)
    - C:\Users\jung\coding\bds\src\components\complex\HagwonRecommendSheet.tsx — BottomSheet 추출 기준 (lines 407–467)
    - C:\Users\jung\coding\bds\src\app\page.tsx — 기존 로고 패턴 lines 126–129 (dj-logo CSS class)
    - C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-UI-SPEC.md — 컴포넌트 인벤토리 섹션
  </read_first>
  <behavior>
    - AppHeader: sticky top-0 z-50 h-14 (56px), logo (dj-logo CSS class) + Bell 버튼 (w-11 h-11, aria-label="알림"), data-capture-hide="true"
    - BottomTabBar: fixed bottom-0 z-40, 4탭 (홈/랭킹/분양/MY), active=var(--dj-orange), inactive=var(--fg-sec), data-capture-hide="true", height=calc(64px + env(safe-area-inset-bottom, 0px))
    - BottomSheet: vaul Drawer.Root/Portal/Overlay/Content, props={open, onClose, title, children}, overlay z-[200], content z-[201] rounded-t-[20px] max-h-[90dvh]
  </behavior>
  <action>
**AppHeader.tsx 생성** (`src/components/layout/AppHeader.tsx`):
```tsx
'use client'

import Link from 'next/link'
import { Bell } from 'lucide-react'

export function AppHeader() {
  return (
    <header
      aria-label="상단 헤더"
      className="sticky top-0 z-50 h-14 flex items-center justify-between px-4 bg-white border-b border-[var(--line-default)]"
      data-capture-hide="true"
    >
      <Link href="/" className="dj-logo">
        <span className="mark">단</span>
        <span>단지온도</span>
      </Link>
      <button
        className="flex items-center justify-center w-11 h-11"
        aria-label="알림"
        style={{ color: 'var(--fg-pri)', background: 'none', border: 'none', cursor: 'pointer' }}
      >
        <Bell size={22} strokeWidth={1.75} />
      </button>
    </header>
  )
}
```

주의:
- 알림 버튼은 현재 단계에서 클릭 동작 없이 아이콘만 표시 (href="/notifications"로 Link 전환도 가능)
- `dj-logo` 와 `mark` 는 globals.css 기존 CSS 클래스. 새로 정의하지 말 것.
- 로고 텍스트 19px/800w 는 브랜드 아이덴티티 예외 — 타입 스케일 규칙 적용 금지

**BottomTabBar.tsx 생성** (`src/components/layout/BottomTabBar.tsx`):
```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Home, BarChart2, Building2, User } from 'lucide-react'

const TABS = [
  { href: '/',         label: '홈',   Icon: Home },
  { href: '/rankings', label: '랭킹', Icon: BarChart2 },
  { href: '/presale',  label: '분양', Icon: Building2 },
  { href: '/profile',  label: 'MY',   Icon: User },
] as const

export function BottomTabBar() {
  const pathname = usePathname()

  return (
    <nav
      aria-label="하단 탭 네비게이션"
      className="fixed bottom-0 left-0 right-0 z-40 bg-white border-t border-[var(--line-default)]"
      style={{
        height: 'calc(64px + env(safe-area-inset-bottom, 0px))',
        paddingBottom: 'env(safe-area-inset-bottom, 0px)',
      }}
      data-capture-hide="true"
    >
      <div className="flex h-16 items-stretch">
        {TABS.map(({ href, label, Icon }) => {
          const active = href === '/' ? pathname === '/' : pathname.startsWith(href)
          return (
            <Link
              key={href}
              href={href}
              className="flex flex-1 flex-col items-center justify-center gap-1 min-h-[44px]"
              style={{ color: active ? 'var(--dj-orange)' : 'var(--fg-sec)', textDecoration: 'none' }}
            >
              <Icon size={22} strokeWidth={1.75} />
              <span className="text-xs font-bold">{label}</span>
            </Link>
          )
        })}
      </div>
    </nav>
  )
}
```

**BottomSheet.tsx 생성** (`src/components/ui/BottomSheet.tsx`):
vaul 1.1.2의 Drawer API 사용:
```tsx
'use client'

import { Drawer } from 'vaul'
import { X } from 'lucide-react'

interface BottomSheetProps {
  open: boolean
  onClose: () => void
  title: string
  children: React.ReactNode
}

export function BottomSheet({ open, onClose, title, children }: BottomSheetProps) {
  return (
    <Drawer.Root open={open} onOpenChange={(v) => !v && onClose()}>
      <Drawer.Portal>
        <Drawer.Overlay className="fixed inset-0 bg-black/45 z-[200]" />
        <Drawer.Content
          className="fixed bottom-0 left-0 right-0 z-[201] rounded-t-[20px] bg-white max-h-[90dvh] overflow-y-auto"
          style={{
            paddingBottom: 'env(safe-area-inset-bottom, 20px)',
            boxShadow: '0 -8px 40px rgba(0,0,0,0.15)',
          }}
          aria-label={title}
        >
          <div className="flex justify-center pt-3">
            <div className="w-10 h-1 rounded-full bg-[var(--line-default)]" />
          </div>
          <div className="flex items-center justify-between px-5 pt-3 pb-4 border-b border-[var(--line-subtle)]">
            <h2 className="text-xl font-bold">{title}</h2>
            <button
              onClick={onClose}
              aria-label="닫기"
              className="flex items-center justify-center w-11 h-11 rounded-full"
              style={{ background: 'var(--bg-surface-2)', border: 'none', cursor: 'pointer', color: 'var(--fg-sec)' }}
            >
              <X size={20} strokeWidth={1.75} />
            </button>
          </div>
          <div>{children}</div>
        </Drawer.Content>
      </Drawer.Portal>
    </Drawer.Root>
  )
}
```

금지 사항:
- backdrop-blur, gradient-text, glow, 보라/인디고 일체 금지
- max-sm: Tailwind 패턴 사용 금지 (D-06)
- 이모지 아이콘 사용 금지 (lucide-react 사용)
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - src/components/layout/AppHeader.tsx: 파일 존재, 'use client' + data-capture-hide="true" + dj-logo class 포함
    - src/components/layout/BottomTabBar.tsx: 파일 존재, 'use client' + usePathname + 4개 탭 + data-capture-hide="true" 포함
    - src/components/ui/BottomSheet.tsx: 파일 존재, vaul Drawer.Root + open/onClose/title/children props 포함
    - TypeScript 타입 에러 없음
    - Plan 00에서 작성한 세 테스트 파일(AppHeader.test.tsx, BottomTabBar.test.tsx, BottomSheet.test.tsx)이 GREEN으로 전환됨
  </done>
</task>

<task type="auto">
  <name>Task 3: layout.tsx 업데이트 + CompareFloatingBar bottom 보정</name>
  <files>
    src/app/layout.tsx
    src/components/complex/CompareFloatingBar.tsx
  </files>
  <read_first>
    - C:\Users\jung\coding\bds\src\app\layout.tsx — 현재 전체 파일 (수정 기준)
    - C:\Users\jung\coding\bds\src\components\complex\CompareFloatingBar.tsx — line 48–50 (bottom: 80)
  </read_first>
  <action>
**layout.tsx 수정** — 아래 4가지 변경 적용:

1. viewport export에 `viewportFit: 'cover'` 추가:
```tsx
export const viewport: Viewport = {
  themeColor: '#ea580c',
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',  // iOS env(safe-area-inset-bottom) 활성화
}
```

2. AppHeader, BottomTabBar import 추가:
```tsx
import { AppHeader } from '@/components/layout/AppHeader'
import { BottomTabBar } from '@/components/layout/BottomTabBar'
```

3. body className에 bottom padding 추가 (탭바 공간 확보):
```tsx
<body className="font-sans antialiased pb-[calc(64px+env(safe-area-inset-bottom,0px))]">
```

4. body 내 컴포넌트 순서 — AppHeader를 NuqsAdapter 앞에, BottomTabBar를 Footer 뒤에, Footer는 모바일 숨김:
```tsx
<body className="font-sans antialiased pb-[calc(64px+env(safe-area-inset-bottom,0px))]">
  <AppHeader />
  <NuqsAdapter>
    {children}
  </NuqsAdapter>
  <div className="hidden sm:block">
    <Footer />
  </div>
  <BottomTabBar />
</body>
```

**CompareFloatingBar.tsx 수정** — bottom 값만 변경 (line 49):
```tsx
// Before:
bottom: 80,

// After:
bottom: 'calc(64px + env(safe-area-inset-bottom, 0px) + 16px)',
```
다른 코드는 일체 변경하지 않는다.

**중요 제약:**
- layout.tsx는 RSC (Server Component) 유지 — 'use client' 추가 절대 금지
- AppHeader, BottomTabBar는 자체적으로 'use client'이므로 layout.tsx는 RSC로 유지 가능
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "viewportFit" src/app/layout.tsx && grep -n "AppHeader\|BottomTabBar" src/app/layout.tsx && grep -n "calc(64px" src/components/complex/CompareFloatingBar.tsx</automated>
  </verify>
  <done>
    - layout.tsx: viewportFit: 'cover' 포함, AppHeader + BottomTabBar import 및 JSX 포함, body에 pb-[calc(64px+env(safe-area-inset-bottom,0px))] 포함, Footer가 hidden sm:block 래퍼 안에 있음
    - CompareFloatingBar.tsx: bottom 값이 calc(64px + env(safe-area-inset-bottom, 0px) + 16px) 로 변경됨
    - npm run build 또는 npx tsc --noEmit 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → navigation | BottomTabBar의 MY 탭이 /profile로 링크 — 기존 /profile/page.tsx auth guard가 미인증 시 /login으로 리다이렉트 (기존 동작 유지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Spoofing | BottomTabBar /profile 링크 | accept | /profile/page.tsx의 기존 auth guard가 처리. 탭바는 UI 진입점만 제공 |
| T-29-02 | Tampering | vaul Drawer 포털 | accept | vaul은 MIT 라이선스 공개 패키지. 네트워크 호출 없음. DOM 조작만 수행 |
| T-29-03 | Information Disclosure | data-capture-hide 속성 | accept | CSS 속성으로 캡처 제외 처리. 보안 민감 정보 숨김 목적 아님 (UX 목적) |
</threat_model>

<verification>
Phase 29 Plan 01 완료 기준:

```bash
# 1. 패키지 설치 확인
cd /c/Users/jung/coding/bds
node -e "const p=require('./package.json'); console.log('vaul:', p.dependencies.vaul, 'embla:', p.dependencies['embla-carousel-react'])"

# 2. 신규 파일 존재 확인
ls src/components/layout/AppHeader.tsx src/components/layout/BottomTabBar.tsx src/components/ui/BottomSheet.tsx

# 3. 핵심 속성 확인
grep -n "data-capture-hide" src/components/layout/AppHeader.tsx src/components/layout/BottomTabBar.tsx
grep -n "viewportFit" src/app/layout.tsx
grep -n "calc(64px" src/components/complex/CompareFloatingBar.tsx

# 4. TypeScript 타입 검사
npx tsc --noEmit 2>&1 | head -20

# 5. 빌드 확인
npm run lint
```
</verification>

<success_criteria>
1. `npm run lint` 통과 — lint 에러 없음
2. `npx tsc --noEmit` 통과 — 타입 에러 없음
3. AppHeader에 `data-capture-hide="true"` 존재 (grep으로 확인)
4. BottomTabBar에 `data-capture-hide="true"` + `usePathname` 존재 (grep으로 확인)
5. layout.tsx에 `viewportFit: 'cover'` + `AppHeader` + `BottomTabBar` + `pb-[calc(64px+env(safe-area-inset-bottom,0px))]` 모두 존재
6. CompareFloatingBar.tsx의 bottom이 `calc(64px + env(safe-area-inset-bottom, 0px) + 16px)` 로 변경됨
7. BottomSheet.tsx가 vaul `Drawer.Root` + `open`, `onClose`, `title`, `children` props 포함
8. `npm run test` — Plan 00에서 작성한 세 테스트 파일(AppHeader, BottomTabBar, BottomSheet)이 모두 GREEN (PASS)
</success_criteria>

<output>
완료 후 `.planning/phases/29-mobile-optimization/29-01-SUMMARY.md` 생성.
</output>
