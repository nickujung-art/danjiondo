---
phase: 29-mobile-optimization
plan: "03"
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - src/app/complexes/[id]/page.tsx
  - src/components/complex/DealTypeTabs.tsx
  - src/components/complex/HagwonRecommendSheet.tsx
  - src/components/complex/EducationCard.tsx
  - src/components/complex/RedevelopmentSheet.tsx
autonomous: true
requirements:
  - MOB-04
  - MOB-06

must_haves:
  truths:
    - "단지상세 페이지가 모바일에서 단일 컬럼으로 표시되고 데스크탑에서 1fr+360px 사이드바 레이아웃으로 전환된다"
    - "단지상세 탭이 모바일에서 좌우 스와이프로 전환된다 (Embla Carousel)"
    - "단지상세 페이지에 인라인 헤더가 없다 (AppHeader가 대체)"
    - "HagwonRecommendSheet가 공유 BottomSheet 컴포넌트를 사용한다"
    - "단지상세 페이지의 export const revalidate = 86400이 유지된다 (ISR 보장)"
    - "DealTypeTabs의 스와이프가 nuqs URL 상태와 동기화된다"
    - "탭바가 sticky top-14(56px)에 위치하여 AppHeader 바로 아래에 고정된다"
    - "EducationCard의 SchoolDetailSheet와 학교순위 시트가 공유 BottomSheet 컴포넌트를 사용한다 (D-11)"
    - "재건축 타임라인이 RedevelopmentSheet 클라이언트 컴포넌트를 통해 BottomSheet로 제공된다 (D-11)"
  artifacts:
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "단지상세 페이지 mobile-first 레이아웃"
      contains: "export const revalidate = 86400"
    - path: "src/components/complex/DealTypeTabs.tsx"
      provides: "Embla 기반 스와이프 탭 컴포넌트"
      contains: "useEmblaCarousel"
    - path: "src/components/complex/HagwonRecommendSheet.tsx"
      provides: "공유 BottomSheet 컴포넌트를 사용하는 학원 추천 시트"
      contains: "BottomSheet"
    - path: "src/components/complex/EducationCard.tsx"
      provides: "학교 상세/순위 팝업을 공유 BottomSheet로 전환한 교육 카드"
      contains: "BottomSheet"
    - path: "src/components/complex/RedevelopmentSheet.tsx"
      provides: "재건축 타임라인을 BottomSheet로 감싸는 클라이언트 컴포넌트 (D-11)"
      contains: "BottomSheet"
  key_links:
    - from: "src/components/complex/DealTypeTabs.tsx"
      to: "nuqs useQueryState"
      via: "emblaApi.on('select', ...) → nuqs 상태 업데이트"
      pattern: "useEmblaCarousel"
    - from: "src/components/complex/HagwonRecommendSheet.tsx"
      to: "src/components/ui/BottomSheet.tsx"
      via: "import + JSX 래핑"
      pattern: "BottomSheet"
---

<objective>
단지상세 페이지 mobile-first 재작성 + DealTypeTabs 스와이프 확장 + HagwonRecommendSheet를 공유 BottomSheet로 전환한다.

Purpose: 가장 복잡한 단지상세 페이지를 모바일 최적화하고, 탭 스와이프 인터랙션으로 네이티브 앱 수준의 UX를 제공한다.
Output: complexes/[id]/page.tsx (모바일 레이아웃), DealTypeTabs.tsx (Embla 스와이프), HagwonRecommendSheet.tsx (BottomSheet 전환), EducationCard.tsx (BottomSheet 전환).
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-PATTERNS.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-UI-SPEC.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-01-SUMMARY.md

<interfaces>
<!-- 단지상세 page.tsx 핵심 구조 -->
```
src/app/complexes/[id]/page.tsx
- export const revalidate = 86400  ← 반드시 유지 (ISR)
- 'use client' 절대 추가 금지
- lines 380–417: 인라인 <header> (nav links + action buttons) ← 삭제 대상
  - 삭제 후 action buttons (ShareButton, FavoriteButton, CompareAddButton, 알림 설정)는
    page body의 hero 영역 아래 이동
- lines 420–428: desktop 2-column <main>:
    style={{ padding: '24px 32px', display: 'grid', gridTemplateColumns: '1fr 360px', gap: 24, maxWidth: 1280, margin: '0 auto' }}
    → className="px-4 py-4 sm:px-6 sm:py-6 max-w-screen-xl mx-auto grid grid-cols-1 gap-4 lg:grid-cols-[1fr_360px] lg:gap-6"
- DealTypeTabs: 현재 탭 버튼 행 구조. Embla로 content 영역 스와이프 추가 필요.
  - 탭바 자체는 sticky top-14 (AppHeader 56px 아래)
```

<!-- DealTypeTabs 현재 구조 -->
```tsx
// src/components/complex/DealTypeTabs.tsx
// 'use client' 이미 존재 (탭 전환 인터랙션)
// nuqs를 사용하여 URL에 탭 상태 동기화
// lines 78–93: 탭 버튼 행 (div 또는 nav 형태)
// lines 100+: 탭 콘텐츠 영역 (현재 단순 조건 렌더링 또는 이미 있는 구조)
// → Embla wrapping: 탭 버튼 행은 그대로, content 영역만 Embla flex 트랙으로 감싸기
```

<!-- Embla Carousel API (RESEARCH.md Pattern 5) -->
```tsx
import useEmblaCarousel from 'embla-carousel-react'
import { useCallback, useEffect } from 'react'

const [emblaRef, emblaApi] = useEmblaCarousel({ startIndex: activeTabIndex })

// 스와이프 완료 시 nuqs 상태 업데이트
useEffect(() => {
  if (!emblaApi) return
  const onSelect = () => {
    const index = emblaApi.selectedScrollSnap()
    // nuqs setTab(tabs[index].key) 호출
  }
  emblaApi.on('select', onSelect)
  return () => { emblaApi.off('select', onSelect) }
}, [emblaApi])
```

<!-- BottomSheet 컴포넌트 인터페이스 (Plan 01 생성) -->
```tsx
// src/components/ui/BottomSheet.tsx (Plan 01에서 생성됨)
interface BottomSheetProps {
  open: boolean
  onClose: () => void
  title: string
  children: React.ReactNode
}
export function BottomSheet({ open, onClose, title, children }: BottomSheetProps): JSX.Element
```

<!-- HagwonRecommendSheet 현재 구조 (파일 경로 주의) -->
```
src/components/complex/HagwonRecommendSheet.tsx  ← 실제 경로
- createPortal 방식으로 fixed overlay + sheet div 직접 구현 (lines 407–467)
- 내부 step 로직(useState, step machine, form chips): lines 312–405
- 제거 대상: lines 407–430 (createPortal 래퍼 전체)
- 대체: <BottomSheet open={open} onClose={onClose} title="AI 맞춤 학원 추천">
           {/* step content: lines 469–745 */}
         </BottomSheet>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 단지상세 page.tsx mobile-first 재작성 + 인라인 헤더 제거</name>
  <files>src/app/complexes/[id]/page.tsx</files>
  <read_first>
    - C:\Users\jung\coding\bds\src\app\complexes\[id]\page.tsx — 전체 파일 (현재 상태 파악 후 수정)
  </read_first>
  <action>
**반드시 먼저 전체 파일을 읽어 현재 구조를 파악한 후 수정한다.**

**1. 파일 상단 보존 — 절대 변경 금지:**
```tsx
export const revalidate = 86400  // 이 라인 변경 금지 (ISR 핵심)
// 'use client' 추가 절대 금지
```

**2. 인라인 헤더 블록 전체 삭제:**
- `<header>` 태그 블록 전체 삭제 (nav links 포함)
- 헤더 내 action buttons (ShareButton, FavoriteButton, CompareAddButton, 알림 설정 링크)는
  `<main>` 내부의 hero section 아래로 이동. 버튼 row를 별도 `<div>`로 감싸기:
```tsx
{/* Hero 섹션 아래 action buttons */}
<div className="flex flex-wrap gap-2 mb-4">
  <ShareButton ... />
  <FavoriteButton ... />
  {/* 기타 action buttons */}
</div>
```

**3. main 컨테이너 → mobile-first 그리드:**
```tsx
// Before:
<main style={{
  padding: '24px 32px',
  display: 'grid',
  gridTemplateColumns: '1fr 360px',
  gap: 24,
  maxWidth: 1280,
  margin: '0 auto',
}}>

// After (mobile single-column, desktop 2-column):
<main className="px-4 py-4 sm:px-6 sm:py-6 max-w-screen-xl mx-auto grid grid-cols-1 gap-4 lg:grid-cols-[1fr_360px] lg:gap-6">
```

**4. 탭바 sticky 위치 — AppHeader 바로 아래:**
탭 버튼 행을 감싸는 컨테이너에 sticky 포지션 적용:
```tsx
// 탭 버튼 행 컨테이너 (DealTypeTabs 내부 또는 page.tsx에서 감싸는 div):
<div className="sticky top-14 z-30 bg-white border-b border-[var(--line-default)]">
  {/* DealTypeTabs 탭 버튼 row */}
</div>
```
- `top-14` = 56px = AppHeader 높이 (h-14)
- `z-30` = AppHeader(z-50)보다 낮고 BottomTabBar(z-40)보다도 낮음

**5. 기타 inline style → Tailwind 전환:**
- 각 섹션의 padding/margin/gap inline style을 Tailwind 클래스로 전환
- CSS 변수 색상은 inline style 유지 (D-08)
- 섹션 내 카드 div의 `style={{ borderRadius: 16, padding: 24 }}` → `className="rounded-2xl p-6"`

**6. DealTypeTabs에 스와이프가 아직 없다면 wrapper 준비:**
page.tsx 수준에서 DealTypeTabs 주변 구조 정리. 탭 스와이프는 Task 2에서 DealTypeTabs.tsx 직접 수정.

금지 사항:
- 'use client' 추가 금지
- max-sm: prefix 사용 금지
- backdrop-blur, gradient-text, glow 사용 금지
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "export const revalidate" "src/app/complexes/[id]/page.tsx" && grep -c "max-sm:" "src/app/complexes/[id]/page.tsx" && npx tsc --noEmit 2>&1 | grep "\[id\]" | head -10</automated>
  </verify>
  <done>
    - src/app/complexes/[id]/page.tsx에 `export const revalidate = 86400` 유지
    - 파일에 `'use client'` 없음
    - 파일에 인라인 `<header>` 블록 없음
    - main에 `lg:grid-cols-[1fr_360px]` 포함
    - 파일에 `max-sm:` 패턴 없음
    - TypeScript 타입 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2a: DealTypeTabs.tsx Embla 스와이프 확장 + 44px 탭 터치 타겟</name>
  <files>
    src/components/complex/DealTypeTabs.tsx
  </files>
  <read_first>
    - C:\Users\jung\coding\bds\src\components\complex\DealTypeTabs.tsx -- 전체 파일 (현재 탭 구조 파악)
    - C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-RESEARCH.md -- Pattern 5 (Embla 사용법), Pitfall 6 (nuqs 동기화)
  </read_first>
  <action>
**먼저 파일 전체를 읽어 현재 구조를 파악한 후 수정한다.**

**현재 구조 파악 포인트:**
- 어떤 state/URL 파라미터로 현재 탭을 관리하는지 (nuqs? useState?)
- 탭 버튼 행과 탭 콘텐츠가 어떻게 구성되어 있는지
- 어떤 탭 items가 있는지 (거래내역/시설/관리비/AI코멘트 등)

**Embla 추가 패턴 (RESEARCH.md Pattern 5 + Pitfall 6):**

```tsx
import useEmblaCarousel from "embla-carousel-react"
import { useCallback, useEffect } from "react"

// 컴포넌트 내부:
// activeTabIndex: 현재 탭의 배열 인덱스 (URL 상태 또는 prop에서 도출)
const [emblaRef, emblaApi] = useEmblaCarousel({
  startIndex: activeTabIndex,  // URL 직접 진입 시 올바른 탭으로 시작
})

// 스와이프 완료 → URL 상태 동기화 (Pitfall 6 해결)
useEffect(() => {
  if (!emblaApi) return
  const onSelect = () => {
    const newIndex = emblaApi.selectedScrollSnap()
    setActiveTabByIndex(newIndex)  // 실제 함수명으로 교체
  }
  emblaApi.on("select", onSelect)
  return () => { emblaApi.off("select", onSelect) }
}, [emblaApi])

// URL 탭 인덱스 변경 시 Embla 동기화 (탭 버튼 클릭 시)
useEffect(() => {
  if (emblaApi && emblaApi.selectedScrollSnap() !== activeTabIndex) {
    emblaApi.scrollTo(activeTabIndex)
  }
}, [emblaApi, activeTabIndex])
```

**콘텐츠 영역 Embla 래핑:**
```tsx
{/* Embla 컨테이너 — overflow-hidden 필수 */}
<div ref={emblaRef} className="overflow-hidden">
  {/* Embla 트랙 — flex 레이아웃 */}
  <div className="flex">
    {tabs.map((tab) => (
      <div key={tab.key} className="min-w-full">
        {/* 탭 콘텐츠 */}
        {tab.content}
      </div>
    ))}
  </div>
</div>
```

**탭 버튼 행 — 44px 터치 타겟 보장 (D-10):**
기존 탭 버튼이 `minHeight: 32` 이면 `min-h-[44px]` 로 업그레이드:
```tsx
// Before: style={{ minHeight: 32 }}
// After: className="min-h-[44px]" (기존 스타일과 병합)
```

**중요 주의사항:**
- DealTypeTabs.tsx는 이미 `"use client"` — 그대로 유지
- Embla는 content 영역만 감싼다. 탭 버튼 행은 Embla 외부에 유지.
- 탭 버튼 클릭 시: `emblaApi.scrollTo(index)` 호출하여 Embla와 동기화
- 파일의 기존 로직(nuqs 연동, 탭 데이터 등)은 최소한으로 변경
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "useEmblaCarousel" src/components/complex/DealTypeTabs.tsx && grep -n "min-h-\[44px\]" src/components/complex/DealTypeTabs.tsx && npx tsc --noEmit 2>&1 | grep "DealTypeTabs" | head -10</automated>
  </verify>
  <done>
    - DealTypeTabs.tsx에 `useEmblaCarousel` import + 사용 존재
    - DealTypeTabs.tsx에 `emblaRef` 및 Embla 콘텐츠 래퍼 존재 (`overflow-hidden` + `flex` + `min-w-full`)
    - DealTypeTabs.tsx 탭 버튼에 `min-h-[44px]` 적용 (D-10)
    - TypeScript 타입 에러 없음 (DealTypeTabs 관련)
  </done>
</task>

<task type="auto">
  <name>Task 2b: HagwonRecommendSheet.tsx 공유 BottomSheet로 전환</name>
  <files>
    src/components/complex/HagwonRecommendSheet.tsx
  </files>
  <read_first>
    - C:\Users\jung\coding\bds\src\components\complex\HagwonRecommendSheet.tsx -- 전체 파일 (createPortal 래퍼 위치 확인)
  </read_first>
  <action>
**먼저 파일 전체를 읽어 createPortal 래퍼 구조를 파악한 후 수정한다.**

**목표:** 기존 createPortal 래퍼 (fixed overlay + sheet div) 를 제거하고 공유 BottomSheet로 대체.

**삭제 대상:** createPortal 호출 + fixed overlay div + sheet container div (약 lines 407-468)
**유지 대상:** 내부 step 상태 머신, form 컴포넌트 등 모든 비즈니스 로직

**변환 패턴:**
```tsx
// 상단에 import 추가:
import { BottomSheet } from "@/components/ui/BottomSheet"

// createPortal 래퍼 제거 후:
return (
  <BottomSheet open={open} onClose={onClose} title="AI 맞춤 학원 추천">
    {/* 기존 step content (drag handle, header는 BottomSheet가 제공하므로 제거) */}
    {step === 0 && <Step0Content />}
    {step === 1 && <Step1Content />}
    {/* etc. */}
  </BottomSheet>
)
```

**주의:**
- BottomSheet 내부에 이미 drag handle + title + close button이 있으므로 기존 중복 요소 제거
- `open` prop이 없는 경우 (early return 패턴이면) BottomSheet의 open prop으로 전환
- 기존 `if (!open) return null` 패턴 → `open={open}` prop으로 vaul이 처리
- step 로직, form 상태 등 내부 로직은 전혀 변경하지 않음
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "BottomSheet" src/components/complex/HagwonRecommendSheet.tsx && (grep -n "createPortal" src/components/complex/HagwonRecommendSheet.tsx || echo "GOOD: no createPortal") && npx tsc --noEmit 2>&1 | grep "HagwonRecommend" | head -10</automated>
  </verify>
  <done>
    - HagwonRecommendSheet.tsx에 `BottomSheet` import + 사용 존재
    - HagwonRecommendSheet.tsx에 `createPortal` 직접 호출 없음
    - TypeScript 타입 에러 없음 (HagwonRecommendSheet 관련)
  </done>
</task>

<task type="auto">
  <name>Task 3: EducationCard.tsx SchoolDetailSheet + 학교순위 시트 BottomSheet 전환</name>
  <files>
    src/components/complex/EducationCard.tsx
  </files>
  <read_first>
    - C:\Users\jung\coding\bds\src\components\complex\EducationCard.tsx -- 전체 파일 (두 createPortal 위치 확인: SchoolDetailSheet ~line 355, 학교순위 시트 ~line 817)
  </read_first>
  <action>
**반드시 먼저 전체 파일을 읽어 두 createPortal 블록의 정확한 위치를 확인한 후 수정한다.**

EducationCard.tsx에는 두 개의 createPortal 기반 바텀시트가 있다:
1. **SchoolDetailSheet** (함수, ~line 355): 학교 상세 정보 시트
2. **학교순위 시트** (함수 내부, ~line 817): 학교 순위 팝업

두 곳 모두 공유 `BottomSheet` 컴포넌트로 전환한다 (D-11 locked decision).

**1. import 추가 (파일 상단):**
```tsx
import { BottomSheet } from "@/components/ui/BottomSheet"
```

**2. SchoolDetailSheet 함수 내부 전환 패턴:**

현재 구조:
```tsx
function SchoolDetailSheet({ school, si, onClose }: {...}) {
  // ... 데이터 계산 로직 ...

  return createPortal(
    <>
      <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.45)", zIndex: 200 }} />
      <div style={{ position: "fixed", bottom: 0, ... zIndex: 201, ... }}>
        {/* drag handle */}
        {/* 헤더 */}
        {/* 학교 상세 콘텐츠 */}
      </div>
    </>,
    document.body
  )
}
```

변환 후:
```tsx
function SchoolDetailSheet({ school, si, onClose }: {...}) {
  // ... 데이터 계산 로직 유지 ...

  return (
    <BottomSheet open={true} onClose={onClose} title={school.school_name}>
      {/* 헤더 내용 (drag handle + 최상위 div 제거, BottomSheet가 제공) */}
      {/* 학교 상세 콘텐츠: 타입 배지, 거리, 기본정보, 품질지표 등 */}
    </BottomSheet>
  )
}
```

**주의:** SchoolDetailSheet는 열릴 때만 렌더링되므로 `open={true}` 고정이 올바름.
호출 측(SchoolList)에서 `{selectedSchool && <SchoolDetailSheet ... />}` 패턴으로 제어.

**3. 학교순위 시트 함수 내부 전환 패턴:**

현재 구조 (~line 817):
```tsx
return createPortal(
  <>
    <div onClick={onClose} style={{ position: "fixed", inset: 0, ... }} />
    <div style={{ position: "fixed", bottom: 0, ... }}>
      {/* drag handle */}
      <div>{/* 헤더: "{si} {schoolType} 순위" */}</div>
      {/* 순위 목록 */}
    </div>
  </>,
  document.body
)
```

변환 후:
```tsx
return (
  <BottomSheet open={true} onClose={onClose} title={`${si} ${SCHOOL_TYPE_LABEL[schoolType]} 순위`}>
    {/* 구 필터 버튼 행 */}
    {/* 순위 목록 */}
  </BottomSheet>
)
```

**공통 주의사항:**
- `createPortal` import를 제거하거나 다른 곳에서 사용하지 않으면 unused import 제거
- BottomSheet 내 drag handle/헤더/닫기 버튼은 vaul이 제공하므로 중복 요소 제거
- 기존 콘텐츠(학교 정보, 순위 목록) 로직은 전혀 변경하지 않음
- `backdrop-blur`, gradient 일체 금지
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && grep -n "BottomSheet" src/components/complex/EducationCard.tsx && grep -v "^[ ]*[//]" src/components/complex/EducationCard.tsx | grep -c "createPortal" || echo "0 createPortal" && npx tsc --noEmit 2>&1 | grep "EducationCard" | head -10</automated>
  </verify>
  <done>
    - EducationCard.tsx에 `BottomSheet` import + 2회 이상 사용 존재 (SchoolDetailSheet + 학교순위 시트)
    - EducationCard.tsx에 `createPortal` 직접 호출 없음 (주석 제외 후 0건)
    - TypeScript 타입 에러 없음 (EducationCard 관련)
  </done>
</task>


<task type="auto">
  <name>Task 4: RedevelopmentSheet.tsx 신규 생성 + complexes/[id]/page.tsx 통합 (D-11)</name>
  <files>
    src/components/complex/RedevelopmentSheet.tsx
    src/app/complexes/[id]/page.tsx
  </files>
  <read_first>
    - C:\Users\jung\coding\bds\src\components\complex\RedevelopmentTimeline.tsx -- 현재 컴포넌트 파악 (RSC 여부, props 구조)
    - C:\Users\jung\coding\bds\src\app\complexes\[id]\page.tsx -- 현재 RedevelopmentTimeline 사용 위치 확인
  </read_first>
  <action>
**배경:** D-11 locked decision — "재건축 타임라인은 모바일에서 바텀시트로 제공". RedevelopmentTimeline은 현재 page.tsx(RSC)에 인라인 렌더링되므로, 클라이언트 래퍼를 신규 생성해야 한다.

**문제:** page.tsx는 RSC (`export const revalidate = 86400`). BottomSheet와 useState는 클라이언트 전용. 따라서 직접 page.tsx에서 `useState` 사용 불가.

**해결책:** `RedevelopmentSheet.tsx` 클라이언트 컴포넌트 신규 생성 — 트리거 버튼 + 열기 상태 + BottomSheet 래핑을 담당.

**1. 신규 파일 생성 — `src/components/complex/RedevelopmentSheet.tsx`:**

```tsx
'use client'

import { useState } from 'react'
import { BottomSheet } from '@/components/ui/BottomSheet'
import RedevelopmentTimeline from './RedevelopmentTimeline'

interface RedevelopmentSheetProps {
  complexId: string
  // RedevelopmentTimeline이 필요로 하는 props를 파악 후 전달 (파일 읽기 후 확정)
}

export function RedevelopmentSheet({ complexId, ...props }: RedevelopmentSheetProps) {
  const [open, setOpen] = useState(false)

  return (
    <>
      <button
        onClick={() => setOpen(true)}
        className="w-full min-h-[44px] flex items-center justify-between px-4 py-3 rounded-xl border border-[var(--line-default)] text-left"
        aria-label="재건축 타임라인 보기"
      >
        <span className="text-[16px] font-medium">재건축 진행 현황</span>
        <span className="text-[14px]" style={{ color: 'var(--fg-sec)' }}>자세히 보기 ›</span>
      </button>

      <BottomSheet
        open={open}
        onClose={() => setOpen(false)}
        title="재건축 진행 단계"
      >
        <div className="px-5 pb-6">
          <RedevelopmentTimeline complexId={complexId} {...props} />
        </div>
      </BottomSheet>
    </>
  )
}
```

**주의사항:**
- RedevelopmentTimeline의 실제 props 구조를 `read_first`에서 확인한 후 정확히 전달
- 트리거 버튼에 `min-h-[44px]` 필수 (D-10)
- 색상은 CSS 변수 inline style 사용 (브랜드 색상)

**2. `complexes/[id]/page.tsx` 수정:**
- 기존 RedevelopmentTimeline 직접 렌더링 위치를 RedevelopmentSheet로 교체
- RSC → Client 경계 안전: RedevelopmentSheet는 'use client', page.tsx는 RSC 유지 (이 패턴은 Next.js 15 App Router에서 안전함)
- complexId 등 필요한 props를 page.tsx에서 전달

```tsx
// Before (기존):
<RedevelopmentTimeline complexId={complex.id} ... />

// After:
<RedevelopmentSheet complexId={complex.id} ... />
```

**금지:**
- page.tsx에 'use client' 추가 금지
- max-sm: 사용 금지
  </action>
  <verify>
    <automated>cd /c/Users/jung/coding/bds && test -f src/components/complex/RedevelopmentSheet.tsx && grep -n "BottomSheet" src/components/complex/RedevelopmentSheet.tsx && grep -n "RedevelopmentSheet" "src/app/complexes/[id]/page.tsx" && grep -n "export const revalidate" "src/app/complexes/[id]/page.tsx" && npx tsc --noEmit 2>&1 | grep -E "Redevelopment|\[id\]" | head -10 || echo "PASS"</automated>
  </verify>
  <done>
    - src/components/complex/RedevelopmentSheet.tsx 생성됨
    - RedevelopmentSheet.tsx에 `BottomSheet` import + 사용 존재
    - RedevelopmentSheet.tsx에 트리거 버튼 `min-h-[44px]` 존재
    - complexes/[id]/page.tsx에 `RedevelopmentSheet` import + 사용 존재 (RedevelopmentTimeline 직접 렌더링 제거)
    - page.tsx에 `export const revalidate = 86400` 유지
    - TypeScript 타입 에러 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → ISR page | complexes/[id]/page.tsx는 RSC. 'use client' 추가 시 ISR 파괴됨. |
| Embla swipe → nuqs URL | 스와이프 완료 후 URL 상태가 업데이트되어야 공유/북마크 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Tampering | ISR 캐시 (단지상세) | mitigate | 'use client' 추가 절대 금지. revalidate = 86400 유지. |
| T-29-07 | Tampering | Embla + nuqs 동기화 | mitigate | emblaApi.on('select', cb) 패턴 준수. startIndex를 URL 상태에서 도출하여 초기 동기화. |
| T-29-08 | Denial of Service | BottomSheet 포커스 트랩 | accept | vaul 내장 포커스 트랩 (WCAG 2.4.3). 추가 구현 불필요. |
</threat_model>

<verification>
Plan 03 완료 기준:

```bash
cd /c/Users/jung/coding/bds

# ISR 유지 확인
grep -n "export const revalidate" "src/app/complexes/[id]/page.tsx"

# 'use client' 없음 확인 (page.tsx)
grep -n "use client" "src/app/complexes/[id]/page.tsx" || echo "GOOD: no use client in page.tsx"

# max-sm 없음 확인
grep -c "max-sm:" "src/app/complexes/[id]/page.tsx" || echo "0"

# Embla 추가 확인
grep -n "useEmblaCarousel" src/components/complex/DealTypeTabs.tsx

# BottomSheet 전환 확인
grep -n "BottomSheet" src/components/complex/HagwonRecommendSheet.tsx
grep -n "createPortal" src/components/complex/HagwonRecommendSheet.tsx || echo "GOOD: no createPortal"

# RedevelopmentSheet 생성 + 통합 확인
test -f src/components/complex/RedevelopmentSheet.tsx && grep -n "BottomSheet" src/components/complex/RedevelopmentSheet.tsx
grep -n "RedevelopmentSheet" "src/app/complexes/[id]/page.tsx"

# TypeScript
npx tsc --noEmit 2>&1 | grep -E "\[id\]|DealTypeTabs|HagwonRecommend|Redevelopment" | head -10 || echo "PASS"

# lint
npm run lint
```
</verification>

<success_criteria>
1. `npm run lint` 통과
2. `npx tsc --noEmit` 통과
3. complexes/[id]/page.tsx: `export const revalidate = 86400` 유지, 인라인 헤더 없음, `lg:grid-cols-[1fr_360px]` 포함, `max-sm:` 없음
4. DealTypeTabs.tsx: `useEmblaCarousel` import 및 사용, Embla 콘텐츠 래퍼 (`overflow-hidden` + `flex`), 탭 버튼 `min-h-[44px]`
5. HagwonRecommendSheet.tsx: `BottomSheet` import 및 사용, `createPortal` 직접 호출 없음
6. EducationCard.tsx: `BottomSheet` import + 2회 이상 사용 (SchoolDetailSheet + 학교순위 시트), `createPortal` 직접 호출 없음 (D-11)
7. RedevelopmentSheet.tsx 생성됨, `BottomSheet` 사용, 트리거 버튼 `min-h-[44px]` 존재 (D-11)
8. complexes/[id]/page.tsx에 `RedevelopmentSheet` import 및 사용 존재 (`RedevelopmentTimeline` 직접 인라인 렌더링 제거)
</success_criteria>

<output>
완료 후 `.planning/phases/29-mobile-optimization/29-03-SUMMARY.md` 생성.
</output>
