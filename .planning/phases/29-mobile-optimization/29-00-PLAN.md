---
phase: 29-mobile-optimization
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/components/layout/AppHeader.test.tsx
  - src/components/layout/BottomTabBar.test.tsx
  - src/components/ui/BottomSheet.test.tsx
autonomous: true
requirements:
  - MOB-01
  - MOB-06

must_haves:
  truths:
    - "AppHeader 테스트 파일이 존재하고 RED 상태로 실패한다 (컴포넌트 미구현)"
    - "BottomTabBar 테스트 파일이 존재하고 RED 상태로 실패한다 (컴포넌트 미구현)"
    - "BottomSheet 테스트 파일이 존재하고 RED 상태로 실패한다 (컴포넌트 미구현)"
  artifacts:
    - path: "src/components/layout/AppHeader.test.tsx"
      provides: "AppHeader 컴포넌트 RED 테스트 스텁"
      exports: []
    - path: "src/components/layout/BottomTabBar.test.tsx"
      provides: "BottomTabBar 컴포넌트 RED 테스트 스텁"
      exports: []
    - path: "src/components/ui/BottomSheet.test.tsx"
      provides: "BottomSheet 컴포넌트 RED 테스트 스텁"
      exports: []
  key_links:
    - from: "src/components/layout/AppHeader.test.tsx"
      to: "src/components/layout/AppHeader.tsx"
      via: "import — 아직 파일 없음, Plan 01에서 생성됨"
      pattern: "from '@/components/layout/AppHeader'"
    - from: "src/components/layout/BottomTabBar.test.tsx"
      to: "src/components/layout/BottomTabBar.tsx"
      via: "import — 아직 파일 없음, Plan 01에서 생성됨"
      pattern: "from '@/components/layout/BottomTabBar'"
    - from: "src/components/ui/BottomSheet.test.tsx"
      to: "src/components/ui/BottomSheet.tsx"
      via: "import — 아직 파일 없음, Plan 01에서 생성됨"
      pattern: "from '@/components/ui/BottomSheet'"
---

<objective>
CLAUDE.md의 TDD 규칙("테스트 먼저 작성 후 구현")에 따라 Plan 01이 생성하는 세 컴포넌트(AppHeader, BottomTabBar, BottomSheet)의 RED 테스트 스텁을 먼저 작성한다.

Purpose: 컴포넌트 구현 전에 기대 동작을 코드로 명세한다. Plan 01 실행 시 이 테스트들이 GREEN으로 전환되어야 완료 기준이 충족된다.
Output: 세 개의 테스트 파일 — 각 파일은 컴포넌트 import 실패로 RED 상태.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\29-mobile-optimization\29-RESEARCH.md

<interfaces>
<!-- 프로젝트 Vitest 설정 확인 -->
```
// vitest.config.ts — @/ alias 설정 확인 필요 (기존 테스트 파일 패턴 참조)
// 기존 테스트: src/components/complex/ListingPriceSection.test.tsx
```

<!-- Plan 01이 생성할 컴포넌트 인터페이스 (사전 명세) -->
```tsx
// AppHeader — 기대 인터페이스
export function AppHeader(): JSX.Element
// aria-label="상단 헤더", data-capture-hide="true", dj-logo 포함, Bell 아이콘 포함

// BottomTabBar — 기대 인터페이스
export function BottomTabBar(): JSX.Element
// aria-label="하단 탭 네비게이션", data-capture-hide="true"
// 4개 탭: 홈/랭킹/분양/MY

// BottomSheet — 기대 인터페이스
interface BottomSheetProps {
  open: boolean
  onClose: () => void
  title: string
  children: React.ReactNode
}
export function BottomSheet(props: BottomSheetProps): JSX.Element
// open=true 시 children 렌더링, overlay 클릭 시 onClose 호출
```
</interfaces>
</context>

<feature>
  <name>신규 레이아웃 컴포넌트 RED 테스트 스텁</name>
  <files>
    src/components/layout/AppHeader.test.tsx
    src/components/layout/BottomTabBar.test.tsx
    src/components/ui/BottomSheet.test.tsx
  </files>
  <behavior>
    AppHeader:
    - renders without crashing
    - has aria-label="상단 헤더"
    - has data-capture-hide="true" attribute
    - contains Bell icon (aria-label="알림")

    BottomTabBar:
    - renders without crashing
    - has aria-label="하단 탭 네비게이션"
    - has data-capture-hide="true" attribute
    - renders exactly 4 tab links (홈/랭킹/분양/MY)

    BottomSheet:
    - renders children when open=true
    - does NOT render children when open=false (vaul handles this)
    - calls onClose when overlay is clicked
    - displays title prop
  </behavior>
  <implementation>
아래 세 파일을 생성한다. 컴포넌트 파일이 아직 없으므로 import 에러로 RED 상태가 된다.
Plan 01 실행 후 컴포넌트가 생성되면 GREEN으로 전환되어야 한다.

**1. src/components/layout/AppHeader.test.tsx**

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { AppHeader } from '@/components/layout/AppHeader'

describe('AppHeader', () => {
  it('renders without crashing', () => {
    render(<AppHeader />)
  })

  it('has aria-label "상단 헤더"', () => {
    render(<AppHeader />)
    expect(screen.getByRole('banner', { name: '상단 헤더' })).toBeDefined()
  })

  it('has data-capture-hide attribute', () => {
    const { container } = render(<AppHeader />)
    const header = container.querySelector('[data-capture-hide="true"]')
    expect(header).not.toBeNull()
  })

  it('contains 알림 button', () => {
    render(<AppHeader />)
    expect(screen.getByRole('button', { name: '알림' })).toBeDefined()
  })
})
```

**2. src/components/layout/BottomTabBar.test.tsx**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { BottomTabBar } from '@/components/layout/BottomTabBar'

// usePathname mock — BottomTabBar 내부에서 호출
vi.mock('next/navigation', () => ({
  usePathname: () => '/',
}))

describe('BottomTabBar', () => {
  it('renders without crashing', () => {
    render(<BottomTabBar />)
  })

  it('has aria-label "하단 탭 네비게이션"', () => {
    render(<BottomTabBar />)
    expect(screen.getByRole('navigation', { name: '하단 탭 네비게이션' })).toBeDefined()
  })

  it('has data-capture-hide attribute', () => {
    const { container } = render(<BottomTabBar />)
    const nav = container.querySelector('[data-capture-hide="true"]')
    expect(nav).not.toBeNull()
  })

  it('renders exactly 4 tab links', () => {
    render(<BottomTabBar />)
    const links = screen.getAllByRole('link')
    expect(links).toHaveLength(4)
  })

  it('renders 홈/랭킹/분양/MY labels', () => {
    render(<BottomTabBar />)
    expect(screen.getByText('홈')).toBeDefined()
    expect(screen.getByText('랭킹')).toBeDefined()
    expect(screen.getByText('분양')).toBeDefined()
    expect(screen.getByText('MY')).toBeDefined()
  })
})
```

**3. src/components/ui/BottomSheet.test.tsx**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { BottomSheet } from '@/components/ui/BottomSheet'

describe('BottomSheet', () => {
  it('renders title when open', () => {
    render(
      <BottomSheet open={true} onClose={vi.fn()} title="테스트 시트">
        <p>콘텐츠</p>
      </BottomSheet>
    )
    expect(screen.getByText('테스트 시트')).toBeDefined()
  })

  it('renders children when open=true', () => {
    render(
      <BottomSheet open={true} onClose={vi.fn()} title="시트">
        <p>자식 콘텐츠</p>
      </BottomSheet>
    )
    expect(screen.getByText('자식 콘텐츠')).toBeDefined()
  })

  it('calls onClose when close button is clicked', () => {
    const onClose = vi.fn()
    render(
      <BottomSheet open={true} onClose={onClose} title="시트">
        <p>콘텐츠</p>
      </BottomSheet>
    )
    const closeBtn = screen.getByRole('button', { name: '닫기' })
    fireEvent.click(closeBtn)
    expect(onClose).toHaveBeenCalledOnce()
  })
})
```

주의:
- `@testing-library/react`와 Vitest 환경 설정이 프로젝트에 이미 있음 (기존 테스트 파일 기준)
- 컴포넌트가 아직 없으므로 `npm run test` 실행 시 import 에러로 RED 상태가 정상
- vaul의 Drawer.Root는 jsdom 환경에서 완전한 동작이 안 될 수 있으므로 BottomSheet 테스트는 open=true 조건에서 title/children 렌더링만 검증
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → component imports | 컴포넌트 미구현 시 import 에러 발생 — 의도된 RED 상태 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-00 | Tampering | 테스트 파일 | accept | 테스트 파일은 CI 환경에서 실행됨. 프로덕션 번들에 포함되지 않음. |
</threat_model>

<verification>
Plan 00 완료 기준:

```bash
cd /c/Users/jung/coding/bds

# 테스트 파일 존재 확인
ls src/components/layout/AppHeader.test.tsx
ls src/components/layout/BottomTabBar.test.tsx
ls src/components/ui/BottomSheet.test.tsx

# RED 상태 확인 — 컴포넌트가 없으므로 import 에러로 실패해야 정상
npm run test -- --reporter=verbose 2>&1 | grep -E "FAIL|cannot find|Cannot find|AppHeader|BottomTabBar|BottomSheet" | head -20
```
</verification>

<success_criteria>
1. 세 테스트 파일 모두 존재
2. `npm run test` 실행 시 세 파일이 RED (실패) 상태 — 컴포넌트 import 불가로 실패하는 것이 정상
3. 각 테스트 파일이 컴포넌트의 기대 인터페이스를 명세하고 있음 (aria-label, data-capture-hide, 4탭, open/onClose/title props)
4. Plan 01 완료 후 동일 테스트가 GREEN으로 전환되어야 함
</success_criteria>

<output>
완료 후 `.planning/phases/29-mobile-optimization/29-00-SUMMARY.md` 생성.
</output>
