---
phase: 27-rankings-page
plan: "03"
type: execute
wave: 2
depends_on:
  - "27-01"
  - "27-02"
files_modified:
  - src/app/rankings/page.tsx
  - src/app/rankings/[date]/page.tsx
  - src/app/page.tsx
  - src/app/map/page.tsx
autonomous: true
requirements:
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-04
  - RANK-05
  - RANK-06
  - RANK-07
  - RANK-08

must_haves:
  truths:
    - "/rankings 페이지가 ISR revalidate=3600으로 렌더되고 404가 없다"
    - "대장단지 카드 3개(창원/마산/김해)가 상단 섹션에 렌더된다"
    - "일별 실거래 피드 탭이 7일 날짜를 ?date= searchParam으로 전환한다"
    - "신고가 거래에 오렌지(#ea580c) NEW HIGH 뱃지가 표시된다"
    - "지역 랭킹 탭 7개가 ?region= searchParam으로 전환한다"
    - "흥미 지표 섹션(이번 주 최고가/이번 달 거래량/급등 단지) 3열이 렌더된다"
    - "OG 메타 og:title에 날짜 포함 형식이 사용된다"
    - "카페 공유하기 버튼과 하단 고정 카페 CTA가 존재한다"
    - "홈 페이지 nav에 '랭킹' Link가 추가된다 (/rankings)"
    - "지도 페이지 nav에 '랭킹' Link가 추가된다 (/rankings)"
    - "/rankings/[date] 날짜별 퍼머링크 페이지가 존재하고 generateStaticParams가 최근 30일을 반환한다"
  artifacts:
    - path: "src/app/rankings/page.tsx"
      provides: "ISR 랭킹 메인 페이지 (모든 섹션 포함)"
      contains: "revalidate = 3600"
    - path: "src/app/rankings/[date]/page.tsx"
      provides: "날짜별 퍼머링크 SSG 페이지"
      contains: "generateStaticParams"
  key_links:
    - from: "src/app/rankings/page.tsx"
      to: "src/lib/data/rankings-page.ts"
      via: "import { getRecentDailyFeed, getRegionalPriceRanking, getChampionComplexes, getWeeklyHighlights, REGION_TABS }"
      pattern: "rankings-page"
    - from: "src/app/rankings/page.tsx"
      to: "src/lib/supabase/readonly.ts"
      via: "createReadonlyClient()"
      pattern: "createReadonlyClient"
---

<objective>
`/rankings` 페이지(ISR, revalidate=3600)와 `/rankings/[date]` 퍼머링크 페이지를 구현한다.
홈·지도 페이지 네비게이션에 "랭킹" 링크를 추가한다.

Purpose: 바이럴 핵심 콘텐츠 페이지 제공. 대장단지·신고가·지역순위·흥미 지표가 한 화면에 보여야 카페 공유욕구를 자극한다 (D-08).
Output: rankings/page.tsx + rankings/[date]/page.tsx + 홈/지도 nav 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-rankings-page/27-CONTEXT.md
@src/app/invest/page.tsx
@src/app/map/page.tsx
@src/lib/format.ts
@.planning/phases/27-rankings-page/27-02-SUMMARY.md
</context>

<interfaces>
<!-- rankings-page.ts에서 import할 타입과 함수 (27-02 결과물) -->
```typescript
// src/lib/data/rankings-page.ts exports:
import {
  getRecentDailyFeed,      // (supabase, days=7) => Promise<DailyFeedGroup[]>
  getRegionalPriceRanking, // (supabase, sggCodes: string[]) => Promise<RegionalRankingRow[]>
  getChampionComplexes,    // (supabase) => Promise<ChampionComplexes>
  getWeeklyHighlights,     // (supabase) => Promise<WeeklyHighlights>
  REGION_TABS,             // 7개 탭: { key, label, sggCodes }[]
  type DailyFeedGroup,     // { date: string, transactions: DailyFeedTransaction[] }
  type RegionalRankingRow, // { rank, complexId, complexName, urlSlug, si, gu, avgPricePerPyeong, recentTradePrice, txCount30d }
  type ChampionComplexes,  // { chanwon, masan, gimhae } — each ChampionComplex | null
  type WeeklyHighlights,   // { topPriceThisWeek, topVolumeThisMonth, priceSurgeLastMonth }
} from '@/lib/data/rankings-page'

// src/lib/format.ts exports (기존):
import { formatPrice, complexHref, formatPyeong } from '@/lib/format'
// formatPrice(price: number): string  — "5억 5000" 형식 (만원 입력)
// complexHref(id, urlSlug): string    — "/슬러그" 또는 "/complexes/id"
// formatPyeong(area_m2: number): string — "25평"

// src/lib/supabase/readonly.ts:
import { createReadonlyClient } from '@/lib/supabase/readonly'

// 기존 nav 패턴 (src/app/page.tsx 136~143줄):
// <nav style={{ display: 'flex', gap: 24, font: '600 14px/1 var(--font-sans)' }}>
//   <Link href="/" style={{ color: 'var(--dj-orange)', textDecoration: 'none' }}>홈</Link>
//   <Link href="/map" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>지도</Link>
//   <Link href="/presale" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>분양</Link>
//   <Link href="/favorites" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>관심단지</Link>
// </nav>
// → "관심단지" Link 다음에 아래를 추가:
// <Link href="/rankings" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>랭킹</Link>

// 기존 map nav 패턴 (src/app/map/page.tsx 59~75줄):
// <Link href="/favorites" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>관심단지</Link>
// → "관심단지" Link 다음에 동일하게 추가

// CSS 변수:
// var(--dj-orange)   = #ea580c (active 색상)
// var(--fg-sec)      = 비활성 링크 색상
// var(--fg-pri)      = 주요 텍스트
// var(--fg-tertiary) = 보조 텍스트
// var(--bg-canvas)   = 페이지 배경
// var(--bg-surface-2) = 탭 비활성 배경
// var(--line-default) = 테두리
// var(--line-subtle)  = 연한 테두리

// 카드/테이블 CSS class (globals.css 정의):
// className="card" — 흰 배경 + border + border-radius
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: /rankings/page.tsx — ISR 랭킹 메인 페이지 (전체 섹션)</name>
  <files>src/app/rankings/page.tsx</files>
  <read_first>
    - src/app/invest/page.tsx — 전체 파일 (헤더 패턴, 탭 링크 스타일, 테이블 레이아웃, searchParams 처리 패턴)
    - src/app/map/page.tsx — 헤더/nav 패턴 (줄 35~90)
    - .planning/phases/27-rankings-page/27-CONTEXT.md — D-01 ~ D-08 전체 (구체적 요구사항)
    - src/lib/format.ts — formatPrice, complexHref, formatPyeong 시그니처
  </read_first>
  <action>
`src/app/rankings/page.tsx`를 새로 작성한다. 아래 구조를 정확히 따른다.

**파일 구조:**
```
import 'server-only' — 없음 (page.tsx는 RSC이므로 불필요)
import type { Metadata } from 'next'
import Link from 'next/link'
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getRecentDailyFeed, getRegionalPriceRanking, getChampionComplexes, getWeeklyHighlights, REGION_TABS } from '@/lib/data/rankings-page'
import type { ChampionComplexes, WeeklyHighlights } from '@/lib/data/rankings-page'
import { formatPrice, complexHref, formatPyeong } from '@/lib/format'
import { Suspense } from 'react'
import { UserMenu } from '@/components/auth/UserMenu'
```

**ISR 설정:**
```typescript
export const revalidate = 3600  // D-01: ISR 1시간
```

**Metadata (D-07):**
```typescript
export async function generateMetadata({ searchParams }: Props): Promise<Metadata> {
  const { date } = await searchParams
  const today = new Date().toISOString().split('T')[0]!
  const displayDate = (typeof date === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(date)) ? date : today

  // D-07: og:title = "[날짜] 창원·김해 실거래 TOP 10"
  const SITE = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://danjiondo.kr'
  return {
    title: `${displayDate} 창원·김해 실거래 랭킹 | 단지온도`,
    description: '창원·김해 아파트 일별 실거래 피드, 신고가, 지역 순위, 대장단지 확인.',
    openGraph: {
      title: `[${displayDate}] 창원·김해 실거래 TOP 10`,
      description: '창원·김해 아파트 오늘의 신고가·대장단지·지역 순위 실거래 랭킹.',
      url: `${SITE}/rankings`,
      siteName: '단지온도',
      locale: 'ko_KR',
      type: 'website',
    },
    alternates: { canonical: `${SITE}/rankings` },
  }
}
```

**Props 타입:**
```typescript
interface Props {
  searchParams: Promise<{ date?: string; region?: string }>
}
```

**페이지 컴포넌트 (`export default async function RankingsPage`):**

1. searchParams 파싱 및 입력 검증:
```typescript
const { date: rawDate, region: rawRegion } = await searchParams

// 날짜 검증: YYYY-MM-DD 형식 + 최근 7일 이내
const today = new Date().toISOString().split('T')[0]!
const validDates = Array.from({ length: 7 }, (_, i) => {
  const d = new Date(Date.now() - i * 86_400_000)
  return d.toISOString().split('T')[0]!
})
const activeDate = (typeof rawDate === 'string' && validDates.includes(rawDate))
  ? rawDate
  : today

// 지역 검증: REGION_TABS의 key 목록에 있는 값만
const validRegionKeys = REGION_TABS.map(t => t.key)
const activeRegion = (typeof rawRegion === 'string' && validRegionKeys.includes(rawRegion as (typeof REGION_TABS)[number]['key']))
  ? rawRegion
  : 'all'

const activeTab = REGION_TABS.find(t => t.key === activeRegion) ?? REGION_TABS[0]!
```

2. 병렬 데이터 fetch:
```typescript
const supabase = createReadonlyClient()
const [champions, feed, ranking, highlights] = await Promise.all([
  getChampionComplexes(supabase).catch((): ChampionComplexes => ({ chanwon: null, masan: null, gimhae: null })),
  getRecentDailyFeed(supabase, 7).catch(() => []),
  getRegionalPriceRanking(supabase, [...activeTab.sggCodes]).catch(() => []),
  getWeeklyHighlights(supabase).catch((): WeeklyHighlights => ({ topPriceThisWeek: [], topVolumeThisMonth: [], priceSurgeLastMonth: [] })),
])

const activeDateFeed = feed.find(g => g.date === activeDate)
```

3. 헬퍼 함수:
```typescript
// 탭 링크 스타일 (invest/page.tsx와 동일 패턴)
const tabStyle = (active: boolean): React.CSSProperties => ({
  display: 'inline-block',
  padding: '5px 12px',
  borderRadius: 6,
  font: '500 12px/1 var(--font-sans)',
  textDecoration: 'none',
  background: active ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
  color: active ? '#fff' : 'var(--fg-sec)',
  border: '1px solid var(--line-subtle)',
  whiteSpace: 'nowrap' as const,
})
```

4. JSX 구조:
```
<div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>

  {/* 헤더 — map/page.tsx 패턴과 동일 */}
  <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)', ... }}>
    <Link href="/" className="dj-logo"><span className="mark">단</span><span>단지온도</span></Link>
    <nav style={{ display: 'flex', gap: 24, font: '600 14px/1 var(--font-sans)' }}>
      <Link href="/" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>홈</Link>
      <Link href="/map" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>지도</Link>
      <Link href="/presale" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>분양</Link>
      <Link href="/favorites" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>관심단지</Link>
      <Link href="/rankings" style={{ color: 'var(--dj-orange)', textDecoration: 'none' }}>랭킹</Link>
    </nav>
    <div style={{ marginLeft: 'auto' }}>
      <Suspense><UserMenu /></Suspense>
    </div>
  </header>

  <main style={{ maxWidth: 1040, margin: '0 auto', padding: '24px 32px' }}>

    {/* 페이지 제목 */}
    <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 24px' }}>
      오늘의 실거래 랭킹
    </h1>

    {/* ── 섹션 1: 대장단지 (D-05) ── */}
    <section aria-labelledby="champion-heading" style={{ marginBottom: 32 }}>
      <h2 id="champion-heading" style={{ font: '700 16px/1.3 var(--font-sans)', margin: '0 0 12px', letterSpacing: '-0.01em' }}>
        지역 대장단지
      </h2>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 12 }}>
        {[
          { label: '창원 대장', data: champions.chanwon },
          { label: '마산 대장', data: champions.masan },
          { label: '김해 대장', data: champions.gimhae },
        ].map(({ label, data }) => (
          <div key={label} className="card" style={{ padding: 16 }}>
            <p style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 6px' }}>{label}</p>
            {data ? (
              <>
                <Link
                  href={complexHref(data.complexId, data.urlSlug)}
                  style={{ font: '700 14px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none', display: 'block', marginBottom: 8 }}
                >
                  {data.complexName}
                </Link>
                <p style={{ font: '700 16px/1 var(--font-sans)', color: 'var(--dj-orange)', margin: '0 0 4px' }}>
                  {data.avgPricePerPyeong.toLocaleString()}만/평
                </p>
                {data.priceChange30d != null && (
                  <p style={{ font: '500 12px/1 var(--font-sans)', color: data.priceChange30d >= 0 ? '#16a34a' : '#dc2626', margin: '0 0 4px' }}>
                    {data.priceChange30d >= 0 ? '+' : ''}{(data.priceChange30d * 100).toFixed(1)}% (전월비)
                  </p>
                )}
                <p style={{ font: '400 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
                  30일 거래 {data.txCount30d}건 · {[data.si, data.gu].filter(Boolean).join(' ')}
                </p>
              </>
            ) : (
              <p style={{ font: '400 13px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
                최근 거래 없음
              </p>
            )}
          </div>
        ))}
      </div>
    </section>

    {/* ── 섹션 2: 일별 실거래 피드 (D-02, D-03, D-08) ── */}
    <section aria-labelledby="feed-heading" style={{ marginBottom: 32 }}>
      <h2 id="feed-heading" style={{ font: '700 16px/1.3 var(--font-sans)', margin: '0 0 12px', letterSpacing: '-0.01em' }}>
        일별 실거래 피드
      </h2>

      {/* 날짜 탭 */}
      <div style={{ display: 'flex', gap: 4, flexWrap: 'wrap', marginBottom: 12 }}>
        {validDates.map(d => (
          <Link key={d} href={`/rankings?date=${d}&region=${activeRegion}`} style={tabStyle(d === activeDate)}>
            {d.slice(5).replace('-', '.')}
          </Link>
        ))}
      </div>

      {/* 카페 공유 버튼 (D-08) */}
      <div style={{ display: 'flex', justifyContent: 'flex-end', marginBottom: 8 }}>
        <a
          href="https://cafe.naver.com/chanwonbds"
          target="_blank"
          rel="noopener noreferrer"
          style={{
            display: 'inline-block',
            padding: '6px 14px',
            borderRadius: 6,
            font: '600 12px/1 var(--font-sans)',
            color: '#fff',
            background: '#03c75a',
            textDecoration: 'none',
            border: 'none',
          }}
        >
          카페에 공유하기
        </a>
      </div>

      {/* 거래 테이블 */}
      {!activeDateFeed || activeDateFeed.transactions.length === 0 ? (
        <div className="card" style={{ padding: 32, textAlign: 'center' }}>
          <p style={{ font: '500 14px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
            {activeDate} — 창원·김해 아파트 거래가 없었습니다.
          </p>
        </div>
      ) : (
        <div className="card" style={{ overflow: 'hidden' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse' }}>
            <thead>
              <tr style={{ background: 'var(--bg-surface-2)', borderBottom: '1px solid var(--line-default)' }}>
                {['단지명', '전용면적', '층', '거래가', '동'].map(h => (
                  <th key={h} style={{ padding: '8px 12px', font: '600 11px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left', whiteSpace: 'nowrap' }}>
                    {h}
                  </th>
                ))}
              </tr>
            </thead>
            <tbody>
              {activeDateFeed.transactions.map(tx => (
                <tr key={tx.id} style={{ borderBottom: '1px solid var(--line-subtle)' }}>
                  <td style={{ padding: '10px 12px' }}>
                    <Link
                      href={complexHref(tx.complexId, tx.urlSlug)}
                      style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none' }}
                    >
                      {tx.complexName}
                    </Link>
                    {tx.dong && (
                      <span style={{ font: '400 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginLeft: 6 }}>
                        {tx.dong}
                      </span>
                    )}
                    {tx.is_new_high && (
                      <span
                        style={{
                          display: 'inline-block',
                          marginLeft: 6,
                          padding: '2px 6px',
                          borderRadius: 4,
                          font: '700 10px/1 var(--font-sans)',
                          background: '#ea580c',
                          color: '#fff',
                          verticalAlign: 'middle',
                        }}
                      >
                        NEW HIGH
                      </span>
                    )}
                  </td>
                  <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', whiteSpace: 'nowrap' }}>
                    {formatPyeong(tx.area_m2)} ({tx.area_m2.toFixed(0)}㎡)
                  </td>
                  <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', whiteSpace: 'nowrap' }}>
                    {tx.floor != null ? `${tx.floor}층` : '—'}
                  </td>
                  <td className="tnum" style={{ padding: '10px 12px', font: '700 14px/1 var(--font-sans)', color: 'var(--fg-pri)', whiteSpace: 'nowrap' }}>
                    {formatPrice(tx.price)}
                  </td>
                  <td style={{ padding: '10px 12px', font: '400 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                    {tx.dong ?? '—'}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </section>

    {/* ── 섹션 3: 지역 랭킹 (D-04) ── */}
    <section aria-labelledby="ranking-heading" style={{ marginBottom: 32 }}>
      <h2 id="ranking-heading" style={{ font: '700 16px/1.3 var(--font-sans)', margin: '0 0 12px', letterSpacing: '-0.01em' }}>
        지역 평당가 랭킹
      </h2>
      <div style={{ display: 'flex', gap: 4, flexWrap: 'wrap', marginBottom: 12 }}>
        {REGION_TABS.map(tab => (
          <Link
            key={tab.key}
            href={`/rankings?date=${activeDate}&region=${tab.key}`}
            style={tabStyle(tab.key === activeRegion)}
          >
            {tab.label}
          </Link>
        ))}
      </div>
      {ranking.length === 0 ? (
        <div className="card" style={{ padding: 24, textAlign: 'center' }}>
          <p style={{ font: '500 13px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
            해당 지역 최근 30일 거래 데이터가 없습니다.
          </p>
        </div>
      ) : (
        <div className="card" style={{ overflow: 'hidden' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse' }}>
            <thead>
              <tr style={{ background: 'var(--bg-surface-2)', borderBottom: '1px solid var(--line-default)' }}>
                {['#', '단지명', '평당가', '최근거래가', '30일거래'].map(h => (
                  <th key={h} style={{ padding: '8px 12px', font: '600 11px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: h === '#' || h === '30일거래' ? 'center' : 'left', whiteSpace: 'nowrap' }}>
                    {h}
                  </th>
                ))}
              </tr>
            </thead>
            <tbody>
              {ranking.map(row => (
                <tr key={row.complexId} style={{ borderBottom: '1px solid var(--line-subtle)' }}>
                  <td className="tnum" style={{ padding: '10px 12px', font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)', textAlign: 'center', width: 36 }}>
                    {row.rank}
                  </td>
                  <td style={{ padding: '10px 12px' }}>
                    <Link
                      href={complexHref(row.complexId, row.urlSlug)}
                      style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none' }}
                    >
                      {row.complexName}
                    </Link>
                    {(row.si ?? row.gu) && (
                      <div style={{ font: '400 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginTop: 2 }}>
                        {[row.si, row.gu].filter(Boolean).join(' ')}
                      </div>
                    )}
                  </td>
                  <td className="tnum" style={{ padding: '10px 12px', font: '700 14px/1 var(--font-sans)', color: 'var(--dj-orange)', whiteSpace: 'nowrap' }}>
                    {row.avgPricePerPyeong.toLocaleString()}만/평
                  </td>
                  <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)', whiteSpace: 'nowrap' }}>
                    {row.recentTradePrice != null ? formatPrice(row.recentTradePrice) : '—'}
                  </td>
                  <td className="tnum" style={{ padding: '10px 12px', font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)', textAlign: 'center' }}>
                    {row.txCount30d}건
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </section>

    {/* ── 섹션 4: 이번 주 흥미 지표 (D-06) ── */}
    <section aria-labelledby="highlights-heading" style={{ marginBottom: 40 }}>
      <h2 id="highlights-heading" style={{ font: '700 16px/1.3 var(--font-sans)', margin: '0 0 16px', letterSpacing: '-0.01em' }}>
        이번 주 흥미 지표
      </h2>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 16 }}>

        {/* 이번 주 최고가 TOP 3 */}
        <div className="card" style={{ padding: 16 }}>
          <h3 style={{ font: '600 13px/1.3 var(--font-sans)', margin: '0 0 10px', color: 'var(--fg-pri)' }}>
            이번 주 최고가 TOP 3
          </h3>
          {highlights.topPriceThisWeek.length === 0 ? (
            <p style={{ font: '400 12px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>데이터 없음</p>
          ) : highlights.topPriceThisWeek.map((item, idx) => (
            <div key={item.complexId} style={{ marginBottom: idx < highlights.topPriceThisWeek.length - 1 ? 10 : 0 }}>
              <span style={{ font: '600 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginRight: 6 }}>{idx + 1}</span>
              <Link href={complexHref(item.complexId, item.urlSlug)} style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none' }}>
                {item.complexName}
              </Link>
              <div style={{ font: '700 14px/1 var(--font-sans)', color: 'var(--dj-orange)', margin: '3px 0 2px' }}>
                {formatPrice(item.price)}
              </div>
              <div style={{ font: '400 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                {formatPyeong(item.area_m2)} · {item.deal_date.slice(5).replace('-', '.')}
              </div>
            </div>
          ))}
        </div>

        {/* 이번 달 거래량 TOP 5 */}
        <div className="card" style={{ padding: 16 }}>
          <h3 style={{ font: '600 13px/1.3 var(--font-sans)', margin: '0 0 10px', color: 'var(--fg-pri)' }}>
            이번 달 거래량 TOP 5
          </h3>
          {highlights.topVolumeThisMonth.length === 0 ? (
            <p style={{ font: '400 12px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>데이터 없음</p>
          ) : highlights.topVolumeThisMonth.map((item, idx) => (
            <div key={item.complexId} style={{ display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: idx < highlights.topVolumeThisMonth.length - 1 ? 8 : 0 }}>
              <span style={{ font: '600 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', minWidth: 14 }}>{idx + 1}</span>
              <Link href={complexHref(item.complexId, item.urlSlug)} style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none', flex: 1 }}>
                {item.complexName}
              </Link>
              <span className="tnum" style={{ font: '700 14px/1 var(--font-sans)', color: 'var(--dj-orange)', whiteSpace: 'nowrap' }}>
                {item.txCount30d}건
              </span>
            </div>
          ))}
        </div>

        {/* 전월 대비 급등 TOP 3 (20%+) */}
        <div className="card" style={{ padding: 16 }}>
          <h3 style={{ font: '600 13px/1.3 var(--font-sans)', margin: '0 0 10px', color: 'var(--fg-pri)' }}>
            전월 대비 급등 TOP 3
          </h3>
          <p style={{ font: '400 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 8px' }}>
            평당가 20% 이상 상승 단지
          </p>
          {highlights.priceSurgeLastMonth.length === 0 ? (
            <p style={{ font: '400 12px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>해당 단지 없음</p>
          ) : highlights.priceSurgeLastMonth.map((item, idx) => (
            <div key={item.complexId} style={{ marginBottom: idx < highlights.priceSurgeLastMonth.length - 1 ? 10 : 0 }}>
              <span style={{ font: '600 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginRight: 6 }}>{idx + 1}</span>
              <Link href={complexHref(item.complexId, item.urlSlug)} style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none' }}>
                {item.complexName}
              </Link>
              <div style={{ font: '700 14px/1 var(--font-sans)', color: '#16a34a', margin: '3px 0 0' }}>
                +{(item.changeRatio * 100).toFixed(1)}%
              </div>
            </div>
          ))}
        </div>

      </div>
    </section>

    {/* 데이터 주석 */}
    <p style={{ font: '400 11px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 16 }}>
      국토부 실거래 기준 · deal_type=sale · 매일 새벽 갱신
    </p>

  </main>

  {/* 하단 고정 카페 CTA (D-08) */}
  <footer style={{ borderTop: '1px solid var(--line-default)', background: '#fff', padding: '16px 32px', textAlign: 'center' }}>
    <a
      href="https://cafe.naver.com/chanwonbds"
      target="_blank"
      rel="noopener noreferrer"
      style={{ font: '600 14px/1 var(--font-sans)', color: 'var(--dj-orange)', textDecoration: 'none' }}
    >
      단지온도 카페에서 더 많은 정보 보기
    </a>
  </footer>

</div>
```

**금지 패턴 (CLAUDE.md):**
- backdrop-blur 없음
- gradient-text 없음
- glow 애니메이션 없음
- 보라/인디고 색상 없음
- "Powered by AI" 배지 없음
- 이모지 없음

**네이버 카페 URL:** `https://cafe.naver.com/chanwonbds` (placeholder — 실제 카페 URL로 교체 가능하며 일단 이 값 사용)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:/Users/jung/coding/bds/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/rankings/page.tsx` 파일이 존재한다
    - `export const revalidate = 3600`이 파일 최상단에 있다
    - `generateMetadata`가 og:title에 날짜 포함 형식(`[날짜] 창원·김해 실거래 TOP 10`)을 반환한다
    - 대장단지 섹션에 `{ chanwon, masan, gimhae }` 3개 카드가 렌더된다
    - 신고가 배지가 `background: '#ea580c'`이고 텍스트가 `NEW HIGH`이다 (grep 확인 가능)
    - 거래 없는 날 empty state 메시지가 `창원·김해 아파트 거래가 없었습니다`를 포함한다
    - 카페 공유 버튼이 `href="https://cafe.naver.com/chanwonbds"`를 포함한다
    - 지역 랭킹 탭이 `REGION_TABS`를 map해서 렌더된다
    - 흥미 지표 3열 그리드(이번 주 최고가/거래량/급등)가 존재한다
    - `backdrop-blur`, `gradient`, `보라`, `인디고` CSS가 없다 (grep 확인)
    - TypeScript 컴파일 오류 없음
  </acceptance_criteria>
  <done>
    /rankings/page.tsx가 완성되어 npm run build가 통과한다.
    5개 섹션(헤더/대장단지/피드/지역랭킹/흥미지표) 모두 렌더된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: /rankings/[date]/page.tsx — 날짜 퍼머링크 + 홈/지도 nav 업데이트</name>
  <files>
    src/app/rankings/[date]/page.tsx
    src/app/page.tsx
    src/app/map/page.tsx
  </files>
  <read_first>
    - src/app/rankings/page.tsx — Task 1이 작성한 파일 (헤더 패턴, 테이블 스타일 재사용)
    - src/app/page.tsx — nav 섹션 위치 확인 (130~143줄 근처 nav 태그 검색)
    - src/app/map/page.tsx — nav 섹션 위치 확인 (58~76줄 근처)
    - .planning/phases/27-rankings-page/27-CONTEXT.md — D-01 날짜별 퍼머링크, D-07 OG 메타
  </read_first>
  <action>
**A. `src/app/rankings/[date]/page.tsx` 작성:**

```typescript
import type { Metadata } from 'next'
import Link from 'next/link'
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getRecentDailyFeed, REGION_TABS } from '@/lib/data/rankings-page'
import { formatPrice, complexHref, formatPyeong } from '@/lib/format'
import { Suspense } from 'react'
import { UserMenu } from '@/components/auth/UserMenu'

interface Props {
  params: Promise<{ date: string }>
}

// 최근 30일 SSG 생성
export async function generateStaticParams() {
  return Array.from({ length: 30 }, (_, i) => {
    const d = new Date(Date.now() - i * 86_400_000)
    return { date: d.toISOString().split('T')[0]! }
  })
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { date } = await params
  const SITE = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://danjiondo.kr'
  return {
    title: `${date} 창원·김해 실거래 랭킹 | 단지온도`,
    description: `${date} 창원·김해 아파트 실거래 피드. 신고가·최고가 거래 확인.`,
    openGraph: {
      title: `[${date}] 창원·김해 실거래 TOP 10`,
      description: `${date} 창원·김해 아파트 실거래 랭킹.`,
      url: `${SITE}/rankings/${date}`,
      siteName: '단지온도',
      locale: 'ko_KR',
      type: 'website',
    },
    alternates: { canonical: `${SITE}/rankings/${date}` },
  }
}

export default async function DateRankingsPage({ params }: Props) {
  const { date } = await params

  // 날짜 검증: YYYY-MM-DD 형식
  if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) {
    return (
      <div style={{ padding: 48, textAlign: 'center', fontFamily: 'var(--font-sans)' }}>
        <p style={{ font: '500 14px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)' }}>유효하지 않은 날짜입니다.</p>
        <Link href="/rankings" style={{ color: 'var(--dj-orange)' }}>랭킹으로 돌아가기</Link>
      </div>
    )
  }

  const supabase = createReadonlyClient()
  const feed = await getRecentDailyFeed(supabase, 60).catch(() => [])  // 60일 범위로 날짜 커버
  const dateGroup = feed.find(g => g.date === date)

  const tabStyle = (active: boolean): React.CSSProperties => ({
    display: 'inline-block',
    padding: '5px 12px',
    borderRadius: 6,
    font: '500 12px/1 var(--font-sans)',
    textDecoration: 'none',
    background: active ? 'var(--dj-orange)' : 'var(--bg-surface-2)',
    color: active ? '#fff' : 'var(--fg-sec)',
    border: '1px solid var(--line-subtle)',
    whiteSpace: 'nowrap' as const,
  })

  return (
    <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
      <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)', display: 'flex', alignItems: 'center', padding: '0 32px', gap: 32, position: 'sticky', top: 0, zIndex: 50 }}>
        <Link href="/" className="dj-logo"><span className="mark">단</span><span>단지온도</span></Link>
        <nav style={{ display: 'flex', gap: 24, font: '600 14px/1 var(--font-sans)' }}>
          <Link href="/" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>홈</Link>
          <Link href="/map" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>지도</Link>
          <Link href="/rankings" style={{ color: 'var(--dj-orange)', textDecoration: 'none' }}>랭킹</Link>
        </nav>
        <div style={{ marginLeft: 'auto' }}><Suspense><UserMenu /></Suspense></div>
      </header>

      <main style={{ maxWidth: 1040, margin: '0 auto', padding: '24px 32px' }}>
        <div style={{ marginBottom: 16 }}>
          <Link href="/rankings" style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', textDecoration: 'none' }}>
            ← 랭킹 전체 보기
          </Link>
        </div>
        <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 20px' }}>
          {date} 실거래 피드
        </h1>

        {/* 카페 공유 버튼 */}
        <div style={{ display: 'flex', gap: 8, marginBottom: 16 }}>
          <a
            href="https://cafe.naver.com/chanwonbds"
            target="_blank"
            rel="noopener noreferrer"
            style={{ display: 'inline-block', padding: '6px 14px', borderRadius: 6, font: '600 12px/1 var(--font-sans)', color: '#fff', background: '#03c75a', textDecoration: 'none' }}
          >
            카페에 공유하기
          </a>
        </div>

        {!dateGroup || dateGroup.transactions.length === 0 ? (
          <div className="card" style={{ padding: 48, textAlign: 'center' }}>
            <p style={{ font: '500 14px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
              {date} — 창원·김해 아파트 거래가 없었습니다.
            </p>
          </div>
        ) : (
          <div className="card" style={{ overflow: 'hidden' }}>
            <table style={{ width: '100%', borderCollapse: 'collapse' }}>
              <thead>
                <tr style={{ background: 'var(--bg-surface-2)', borderBottom: '1px solid var(--line-default)' }}>
                  {['단지명', '전용면적', '층', '거래가', '동'].map(h => (
                    <th key={h} style={{ padding: '8px 12px', font: '600 11px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left', whiteSpace: 'nowrap' }}>
                      {h}
                    </th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {dateGroup.transactions.map(tx => (
                  <tr key={tx.id} style={{ borderBottom: '1px solid var(--line-subtle)' }}>
                    <td style={{ padding: '10px 12px' }}>
                      <Link href={complexHref(tx.complexId, tx.urlSlug)} style={{ font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)', textDecoration: 'none' }}>
                        {tx.complexName}
                      </Link>
                      {tx.is_new_high && (
                        <span style={{ display: 'inline-block', marginLeft: 6, padding: '2px 6px', borderRadius: 4, font: '700 10px/1 var(--font-sans)', background: '#ea580c', color: '#fff', verticalAlign: 'middle' }}>
                          NEW HIGH
                        </span>
                      )}
                    </td>
                    <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', whiteSpace: 'nowrap' }}>
                      {formatPyeong(tx.area_m2)}
                    </td>
                    <td className="tnum" style={{ padding: '10px 12px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
                      {tx.floor != null ? `${tx.floor}층` : '—'}
                    </td>
                    <td className="tnum" style={{ padding: '10px 12px', font: '700 14px/1 var(--font-sans)', color: 'var(--fg-pri)', whiteSpace: 'nowrap' }}>
                      {formatPrice(tx.price)}
                    </td>
                    <td style={{ padding: '10px 12px', font: '400 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                      {tx.dong ?? '—'}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}
      </main>
    </div>
  )
}
```

**B. `src/app/page.tsx` nav 업데이트:**

홈 페이지의 `<nav>` 태그 내 `<Link href="/favorites" ...>관심단지</Link>` 다음에 아래 링크를 추가:
```tsx
<Link href="/rankings" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>
  랭킹
</Link>
```

**C. `src/app/map/page.tsx` nav 업데이트:**

지도 페이지의 `<nav>` 태그 내 `<Link href="/favorites" ...>관심단지</Link>` 다음에 동일한 링크 추가:
```tsx
<Link href="/rankings" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>
  랭킹
</Link>
```

단, `src/app/map/page.tsx`에 `href="#"` 로 되어 있는 분양 링크가 있다면 `/presale`로 수정한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:/Users/jung/coding/bds/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/rankings/[date]/page.tsx` 파일이 존재한다
    - `generateStaticParams`가 export되고 30일 배열을 반환한다
    - `generateMetadata`가 `og:title: "[date] 창원·김해 실거래 TOP 10"` 형식을 반환한다
    - `src/app/page.tsx`에 `href="/rankings"`인 Link가 추가되었다 (grep: `href="/rankings"`)
    - `src/app/map/page.tsx`에 `href="/rankings"`인 Link가 추가되었다
    - TypeScript 컴파일 오류 없음
    - `npm run build` 통과 (또는 최소 `tsc --noEmit` 오류 없음)
  </acceptance_criteria>
  <done>
    날짜 퍼머링크 페이지가 존재하고 generateStaticParams가 30일을 커버한다.
    홈·지도 페이지 nav에 "랭킹" 링크가 추가된다.
    npm run build 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searchParams → 페이지 | date, region 파라미터는 allowlist 검증 필수 |
| RSC → Supabase | createReadonlyClient() (anon key), 쿼기 결과 공개 데이터 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | searchParams.date | mitigate | `validDates.includes(rawDate)` — 7일 이내 날짜만 허용, 그 외는 today로 fallback |
| T-27-03-02 | Tampering | searchParams.region | mitigate | `REGION_TABS.map(t => t.key)`에 있는 값만 허용, 그 외는 'all' fallback |
| T-27-03-03 | Information Disclosure | /rankings/[date] URL | accept | 공개 실거래 데이터. 비공개 정보 없음 |
| T-27-03-04 | Spoofing | 카페 링크 | accept | 외부 링크는 `target="_blank" rel="noopener noreferrer"` 적용 |
</threat_model>

<verification>
```bash
# 파일 존재 확인
ls C:/Users/jung/coding/bds/src/app/rankings/page.tsx
ls C:/Users/jung/coding/bds/src/app/rankings/[date]/page.tsx

# ISR 설정 확인
grep "revalidate = 3600" C:/Users/jung/coding/bds/src/app/rankings/page.tsx

# NEW HIGH 뱃지 색상 확인 (#ea580c)
grep "ea580c" C:/Users/jung/coding/bds/src/app/rankings/page.tsx

# 홈 nav 업데이트 확인
grep -n "href=\"/rankings\"" C:/Users/jung/coding/bds/src/app/page.tsx

# 지도 nav 업데이트 확인
grep -n "href=\"/rankings\"" C:/Users/jung/coding/bds/src/app/map/page.tsx

# generateStaticParams 확인
grep "generateStaticParams" "C:/Users/jung/coding/bds/src/app/rankings/[date]/page.tsx"

# TypeScript 검사
cd C:/Users/jung/coding/bds && npx tsc --noEmit 2>&1 | head -30

# 빌드 검사
cd C:/Users/jung/coding/bds && npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
1. `/rankings` 페이지 200 OK, ISR revalidate=3600
2. `/rankings/[date]` SSG 페이지 최근 30일 정적 생성
3. 대장단지 섹션 3개 카드 (창원/마산/김해)
4. 일별 피드에 NEW HIGH 뱃지 (#ea580c 오렌지)
5. 지역 랭킹 7탭 (창원전체/의창/성산/마산합포/마산회원/진해/김해)
6. 흥미 지표 3열 (최고가TOP3/거래량TOP5/급등TOP3)
7. 카페 공유 버튼 + 하단 카페 CTA
8. 홈·지도 nav에 "랭킹" 링크 추가
9. OG 메타 og:title에 날짜 포함
10. `npm run build && npm run lint` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/27-rankings-page/27-03-SUMMARY.md` 생성.
포함 내용: 생성된 파일 목록, 주요 섹션 구조, OG 메타 형식, nav 변경 내역, 빌드 결과.
</output>
