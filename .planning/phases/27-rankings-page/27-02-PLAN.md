---
phase: 27-rankings-page
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/rankings-page.ts
autonomous: true
requirements:
  - RANK-02
  - RANK-04
  - RANK-05
  - RANK-08

must_haves:
  truths:
    - "getRecentDailyFeed()가 최근 7일 실거래를 날짜별 그룹핑해 반환한다 (고가순, 최대 50건/일)"
    - "각 거래 행에 is_new_high boolean이 포함된다 (같은 단지 ±5㎡ 면적대 역대 최고가 비교 — TypeScript 2-query 방식, D-03)"
    - "getRegionalPriceRanking()이 sggCodes 필터로 평당가 TOP 20 단지를 반환한다"
    - "getChampionComplexes()가 창원/마산/김해 지역별 대장단지 1위를 반환한다 (90일 거래량 × 평당가 점수, D-05)"
    - "getWeeklyHighlights()가 이번 주 최고가 TOP 3·이번 달 거래량 TOP 5·급등 TOP 3를 반환한다"
    - "모든 쿼리에 cancel_date IS NULL AND superseded_by IS NULL 필터가 적용된다"
  artifacts:
    - path: "src/lib/data/rankings-page.ts"
      provides: "랭킹 페이지 4개 데이터 함수 + TypeScript 타입 정의"
      exports:
        - "getRecentDailyFeed"
        - "getRegionalPriceRanking"
        - "getChampionComplexes"
        - "getWeeklyHighlights"
        - "REGION_TABS"
        - "DailyFeedGroup"
        - "RegionalRankingRow"
        - "ChampionComplexes"
        - "WeeklyHighlights"
  key_links:
    - from: "src/lib/data/rankings-page.ts"
      to: "supabase transactions table"
      via: "SupabaseClient (createReadonlyClient()로 전달)"
      pattern: "cancel_date.*null.*superseded_by.*null"
    - from: "getRecentDailyFeed"
      to: "transactions (historical lookup)"
      via: "2차 쿼리 histByComplex Map — ±5㎡ TypeScript 비교"
      pattern: "histByComplex"
---

<objective>
`/rankings` 페이지의 4개 데이터 함수와 TypeScript 타입을 `src/lib/data/rankings-page.ts`에 구현한다. 이 파일은 wave 2의 UI 페이지(27-03)에서 import한다.

Purpose: RSC 페이지에서 import할 서버 전용 데이터 레이어. `createReadonlyClient()`를 파라미터로 주입받아 ISR 호환.
Output: `src/lib/data/rankings-page.ts` (4개 함수 + 타입 + REGION_TABS 상수)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-rankings-page/27-CONTEXT.md
@src/lib/data/rankings.ts
@src/lib/data/homepage.ts
@src/lib/format.ts
</context>

<interfaces>
<!-- 기존 패턴에서 추출 — rankings.ts -->
```typescript
// 기존 패턴 (rankings.ts)
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

const ACTIVE_SGG_CODES = ['48121', '48123', '48125', '48127', '48129', '48250'] as const

// supabase 쿼리 패턴:
await supabase
  .from('transactions')
  .select(`price, area_m2, complexes!inner (id, canonical_name, si, gu, url_slug)`)
  .is('cancel_date', null)
  .is('superseded_by', null)
  .eq('deal_type', 'sale')
  .gte('deal_date', thirtyDaysAgo)
  .in('complexes.sgg_code', ACTIVE_SGG_CODES)
  .order('price', { ascending: false })
  .limit(200)

// complexes 테이블 주요 컬럼:
// avg_sale_per_pyeong integer   -- 만원/평 (refresh_complex_price_stats가 설정)
// tx_count_30d integer          -- 30일 거래건수 (champion 점수는 90d 별도 쿼리 사용)
// price_change_30d numeric(5,4) -- 30일 vs 이전30일 비율 (0.20 = 20%)
// url_slug text                 -- SEO URL 슬러그
// NOTE: all_time_high 컬럼 없음 — is_new_high는 TypeScript 2-query ±5㎡ 방식으로 계산

// format.ts
import { formatPrice, complexHref, formatPyeong } from '@/lib/format'
// formatPrice(price: number): string — 만원 입력 → "N억 M만" 형식
// complexHref(id, urlSlug): string   — "/슬러그" 또는 "/complexes/id"
// formatPyeong(area_m2: number): string — "N평" 형식
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 타입 정의 + getRecentDailyFeed + getChampionComplexes</name>
  <files>src/lib/data/rankings-page.ts</files>
  <read_first>
    - src/lib/data/rankings.ts — 전체 파일 (supabase 쿼리 패턴, ACTIVE_SGG_CODES 참조)
    - src/lib/data/homepage.ts — getRecentHighRecords 패턴 (transactions + complexes join)
    - .planning/phases/27-rankings-page/27-CONTEXT.md — D-02, D-03, D-05 결정사항
  </read_first>
  <action>
`src/lib/data/rankings-page.ts` 파일을 생성한다. 아래 구현을 정확히 따른다.

```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

// ── 창원·김해 활성 SGG 코드 ─────────────────────────────────────────────────
const ACTIVE_SGG_CODES = ['48121', '48123', '48125', '48127', '48129', '48250'] as const

// 지역 그룹 (대장단지 champion 계산용, D-05)
const CHANWON_SGG = ['48121', '48123'] as const           // 의창 + 성산
const MASAN_SGG   = ['48125', '48127', '48129'] as const  // 마산합포 + 마산회원 + 진해
const GIMHAE_SGG  = ['48250'] as const                    // 김해

// ── 지역 랭킹 탭 정의 (D-04 결정사항) ──────────────────────────────────────
export const REGION_TABS = [
  { key: 'all',    label: '창원 전체',  sggCodes: ['48121', '48123', '48125', '48127', '48129'] },
  { key: '48121',  label: '의창구',     sggCodes: ['48121'] },
  { key: '48123',  label: '성산구',     sggCodes: ['48123'] },
  { key: '48125',  label: '마산합포구', sggCodes: ['48125'] },
  { key: '48127',  label: '마산회원구', sggCodes: ['48127'] },
  { key: '48129',  label: '진해구',     sggCodes: ['48129'] },
  { key: '48250',  label: '김해시',     sggCodes: ['48250'] },
] as const

// ── 타입 정의 ────────────────────────────────────────────────────────────────

export interface DailyFeedTransaction {
  id: string
  price: number          // 만원
  area_m2: number
  floor: number | null
  deal_date: string      // YYYY-MM-DD
  dong: string | null
  is_new_high: boolean   // 같은 단지 ±5㎡ 면적대 역대 최고가 비교 (D-03)
  complexId: string
  complexName: string
  urlSlug: string | null
}

export interface DailyFeedGroup {
  date: string           // YYYY-MM-DD
  transactions: DailyFeedTransaction[]
}

export interface RegionalRankingRow {
  rank: number
  complexId: string
  complexName: string
  urlSlug: string | null
  si: string | null
  gu: string | null
  avgPricePerPyeong: number   // 만원/평
  recentTradePrice: number | null  // 만원
  txCount30d: number
}

export interface ChampionComplex {
  complexId: string
  complexName: string
  urlSlug: string | null
  si: string | null
  gu: string | null
  avgPricePerPyeong: number   // 만원/평
  priceChange30d: number | null  // 비율 (0.105 = +10.5%)
  txCount90d: number             // 최근 90일 거래 건수 (D-05: 3개월 기준)
}

export interface ChampionComplexes {
  chanwon: ChampionComplex | null
  masan: ChampionComplex | null
  gimhae: ChampionComplex | null
}

export interface WeeklyHighlights {
  topPriceThisWeek: Array<{
    complexId: string
    complexName: string
    urlSlug: string | null
    si: string | null
    gu: string | null
    price: number     // 만원
    area_m2: number
    deal_date: string
  }>
  topVolumeThisMonth: Array<{
    complexId: string
    complexName: string
    urlSlug: string | null
    si: string | null
    gu: string | null
    txCount30d: number
  }>
  priceSurgeLastMonth: Array<{
    complexId: string
    complexName: string
    urlSlug: string | null
    si: string | null
    gu: string | null
    changeRatio: number  // 0.20 = +20%
  }>
}

// ── 헬퍼 ────────────────────────────────────────────────────────────────────

function nDaysAgo(n: number): string {
  return new Date(Date.now() - n * 86_400_000).toISOString().split('T')[0]!
}

// ── 1. 일별 실거래 피드 (D-02, D-03) ─────────────────────────────────────────

/**
 * 최근 N일 실거래 피드를 날짜별 그룹핑해 반환한다.
 * - deal_type='sale' 전용, 취소·정정 제외
 * - 날짜 내림차순, 각 날짜 내 price 내림차순
 * - 날짜당 최대 50건
 * - is_new_high: 같은 단지 ±5㎡ 면적대 역대 최고가와 비교 (D-03)
 *   TypeScript 2-query 방식 — complexes.all_time_high 컬럼 불필요.
 *   단지 전체 최고가 비교 시 면적대 오류(59㎡ vs 134㎡) 방지.
 */
export async function getRecentDailyFeed(
  supabase: SupabaseClient<Database>,
  days = 7,
): Promise<DailyFeedGroup[]> {
  const since = nDaysAgo(days)

  // ── Query 1: 최근 피드 거래 ────────────────────────────────────────────────
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: feedData, error } = await (supabase as any)
    .from('transactions')
    .select(`
      id, price, area_m2, floor, deal_date,
      complexes!inner (id, canonical_name, dong, url_slug, sgg_code)
    `)
    .in('complexes.sgg_code', [...ACTIVE_SGG_CODES])
    .gte('deal_date', since)
    .eq('deal_type', 'sale')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .order('deal_date', { ascending: false })
    .order('price', { ascending: false })
    .limit(days * 50 + 50)  // 여유분 포함

  if (error) {
    console.error('[getRecentDailyFeed] failed:', error.message)
    return []
  }

  const feedRows = (feedData ?? []) as Record<string, unknown>[]
  if (feedRows.length === 0) return []

  // ── Query 2: 역대 이력 거래 (is_new_high ±5㎡ 비교용) ──────────────────────
  // 피드에 등장한 단지들의 전체 이력 거래를 가져와 TypeScript에서 ±5㎡ 비교
  const feedComplexIds = [...new Set(
    feedRows.map(r => {
      const c = Array.isArray(r['complexes'])
        ? (r['complexes'] as Record<string, unknown>[])[0]
        : r['complexes'] as Record<string, unknown>
      return c?.['id'] as string | undefined
    }).filter((id): id is string => Boolean(id))
  )]

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: histData } = await (supabase as any)
    .from('transactions')
    .select('id, complex_id, area_m2, price')
    .in('complex_id', feedComplexIds)
    .eq('deal_type', 'sale')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .limit(10000)  // 소규모 시장(창원·김해) 기준 충분한 한도

  // Map<complexId, {id, area_m2, price}[]>
  const histByComplex = new Map<string, Array<{ id: string; area_m2: number; price: number }>>()
  for (const h of (histData ?? []) as Record<string, unknown>[]) {
    const cid = h['complex_id'] as string
    if (!histByComplex.has(cid)) histByComplex.set(cid, [])
    histByComplex.get(cid)!.push({
      id:      h['id'] as string,
      area_m2: h['area_m2'] as number,
      price:   h['price'] as number,
    })
  }

  // ── 그룹핑 + is_new_high 계산 ───────────────────────────────────────────────
  const grouped = new Map<string, DailyFeedTransaction[]>()

  for (const row of feedRows) {
    const c = Array.isArray(row['complexes'])
      ? (row['complexes'] as Record<string, unknown>[])[0]
      : row['complexes'] as Record<string, unknown>
    if (!c) continue

    const date      = row['deal_date'] as string
    const price     = row['price'] as number
    const area_m2   = row['area_m2'] as number
    const txId      = row['id'] as string
    const complexId = c['id'] as string

    // ±5㎡ 같은 면적대 역대 최고가 비교 (자기 자신 제외)
    const history     = histByComplex.get(complexId) ?? []
    const comparables = history.filter(h => h.id !== txId && Math.abs(h.area_m2 - area_m2) <= 5)
    const maxPrice    = comparables.length > 0
      ? Math.max(...comparables.map(h => h.price))
      : 0
    // 비교 대상 없으면 is_new_high = false (첫 거래는 신고가 마크 보수적 처리)
    const is_new_high = comparables.length > 0 && price >= maxPrice

    if (!grouped.has(date)) grouped.set(date, [])
    const group = grouped.get(date)!
    if (group.length >= 50) continue  // 날짜당 최대 50건

    group.push({
      id:          txId,
      price,
      area_m2,
      floor:       row['floor'] as number | null,
      deal_date:   date,
      dong:        c['dong'] as string | null,
      is_new_high,
      complexId,
      complexName: c['canonical_name'] as string,
      urlSlug:     c['url_slug'] as string | null,
    })
  }

  // 날짜 내림차순 정렬 후 반환
  return Array.from(grouped.entries())
    .sort(([a], [b]) => b.localeCompare(a))
    .map(([date, transactions]) => ({ date, transactions }))
}

// ── 2. 대장단지 (D-05) ───────────────────────────────────────────────────────

/**
 * 지역별 대장단지 (창원/마산/김해) 각 1위를 반환한다.
 * 점수 = tx_count_90d × avg_sale_per_pyeong (D-05: 최근 3개월 거래량 기준)
 * avg_sale_per_pyeong IS NOT NULL 조건 필수. tx_count_90d = 0이면 점수 0 (제외).
 */
export async function getChampionComplexes(
  supabase: SupabaseClient<Database>,
): Promise<ChampionComplexes> {
  // ── Query 1: 활성 단지 목록 ───────────────────────────────────────────────
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase as any)
    .from('complexes')
    .select('id, canonical_name, url_slug, si, gu, sgg_code, avg_sale_per_pyeong, price_change_30d')
    .in('sgg_code', [...ACTIVE_SGG_CODES])
    .not('avg_sale_per_pyeong', 'is', null)
    .eq('status', 'active')

  if (error) {
    console.error('[getChampionComplexes] failed:', error.message)
    return { chanwon: null, masan: null, gimhae: null }
  }

  const rows = (data ?? []) as Record<string, unknown>[]
  if (rows.length === 0) return { chanwon: null, masan: null, gimhae: null }

  // ── Query 2: 최근 90일 거래 건수 (D-05: 3개월 기준) ──────────────────────
  const complexIds  = rows.map(r => r['id'] as string)
  const ninetyDaysAgo = nDaysAgo(90)

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: txData } = await (supabase as any)
    .from('transactions')
    .select('complex_id')
    .in('complex_id', complexIds)
    .eq('deal_type', 'sale')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .gte('deal_date', ninetyDaysAgo)

  // Map<complexId, count> — 90일 거래 건수
  const countMap = new Map<string, number>()
  for (const tx of (txData ?? []) as Record<string, unknown>[]) {
    const cid = tx['complex_id'] as string
    countMap.set(cid, (countMap.get(cid) ?? 0) + 1)
  }

  function bestInGroup(sggList: readonly string[]): ChampionComplex | null {
    const candidates = rows
      .filter(r => sggList.includes(r['sgg_code'] as string))
      .map(r => {
        const id           = r['id'] as string
        const tx_count_90d = countMap.get(id) ?? 0
        const avgPPyeong   = r['avg_sale_per_pyeong'] as number
        return {
          complexId:         id,
          complexName:       r['canonical_name'] as string,
          urlSlug:           r['url_slug'] as string | null,
          si:                r['si'] as string | null,
          gu:                r['gu'] as string | null,
          avgPricePerPyeong: avgPPyeong,
          priceChange30d:    r['price_change_30d'] as number | null,
          txCount90d:        tx_count_90d,
          score:             tx_count_90d * avgPPyeong,
        }
      })
      .filter(c => c.score > 0)  // tx_count_90d > 0 필터
      .sort((a, b) => b.score - a.score)

    if (candidates.length === 0) return null
    const { score: _score, ...rest } = candidates[0]!
    return rest
  }

  return {
    chanwon: bestInGroup(CHANWON_SGG),
    masan:   bestInGroup(MASAN_SGG),
    gimhae:  bestInGroup(GIMHAE_SGG),
  }
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:/Users/jung/coding/bds/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>getRecentDailyFeed (±5㎡ 2-query), getChampionComplexes (90일 거래량) 함수가 구현되고 TypeScript 오류 없음</done>
</task>

<task type="auto">
  <name>Task 2: getRegionalPriceRanking + getWeeklyHighlights 구현</name>
  <files>src/lib/data/rankings-page.ts</files>
  <read_first>
    - src/lib/data/rankings-page.ts — Task 1이 작성한 현재 상태 (이어서 추가)
    - .planning/phases/27-rankings-page/27-CONTEXT.md — D-04 지역 랭킹, D-06 흥미 지표
  </read_first>
  <action>
`src/lib/data/rankings-page.ts` 파일에 아래 두 함수를 추가한다 (기존 내용 유지, 파일 끝에 append).

```typescript
// ── 3. 지역 랭킹 (D-04) ──────────────────────────────────────────────────────

/**
 * 특정 sgg_code 목록 내 평당가 TOP 20 단지를 반환한다.
 * - tx_count_30d > 0 (최근 거래 있는 단지만)
 * - avg_sale_per_pyeong 내림차순
 * - 최근 거래가 = 30일 이내 최신 거래 price (2차 쿼리로 가져옴, N=20 이하이므로 허용)
 */
export async function getRegionalPriceRanking(
  supabase: SupabaseClient<Database>,
  sggCodes: string[],
): Promise<RegionalRankingRow[]> {
  if (sggCodes.length === 0) return []

  // 1차: TOP 20 단지 조회
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: complexList, error: e1 } = await (supabase as any)
    .from('complexes')
    .select('id, canonical_name, url_slug, si, gu, avg_sale_per_pyeong, tx_count_30d')
    .in('sgg_code', sggCodes)
    .gt('tx_count_30d', 0)
    .not('avg_sale_per_pyeong', 'is', null)
    .eq('status', 'active')
    .order('avg_sale_per_pyeong', { ascending: false })
    .limit(20)

  if (e1) {
    console.error('[getRegionalPriceRanking] complexes query failed:', e1.message)
    return []
  }

  const rows = (complexList ?? []) as Record<string, unknown>[]
  if (rows.length === 0) return []

  const ids = rows.map(r => r['id'] as string)
  const thirtyDaysAgo = nDaysAgo(30)

  // 2차: 해당 단지들의 최근 거래가 (30일 이내 가장 최신 거래)
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: txData } = await (supabase as any)
    .from('transactions')
    .select('complex_id, price, deal_date')
    .in('complex_id', ids)
    .gte('deal_date', thirtyDaysAgo)
    .eq('deal_type', 'sale')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .order('deal_date', { ascending: false })
    .limit(ids.length * 10)

  // 단지별 최신 거래가 맵
  const latestPriceMap = new Map<string, number>()
  for (const tx of (txData ?? []) as Record<string, unknown>[]) {
    const cid = tx['complex_id'] as string
    if (!latestPriceMap.has(cid)) {
      latestPriceMap.set(cid, tx['price'] as number)
    }
  }

  return rows.map((r, idx) => ({
    rank:              idx + 1,
    complexId:         r['id'] as string,
    complexName:       r['canonical_name'] as string,
    urlSlug:           r['url_slug'] as string | null,
    si:                r['si'] as string | null,
    gu:                r['gu'] as string | null,
    avgPricePerPyeong: r['avg_sale_per_pyeong'] as number,
    recentTradePrice:  latestPriceMap.get(r['id'] as string) ?? null,
    txCount30d:        r['tx_count_30d'] as number,
  }))
}

// ── 4. 이번 주 흥미 지표 (D-06) ──────────────────────────────────────────────

/**
 * 주간/월간 흥미 지표를 반환한다:
 * - topPriceThisWeek: 이번 주(7일) 최고가 거래 TOP 3
 * - topVolumeThisMonth: tx_count_30d 기준 TOP 5 단지
 * - priceSurgeLastMonth: price_change_30d >= 0.20 (20% 이상) TOP 3
 */
export async function getWeeklyHighlights(
  supabase: SupabaseClient<Database>,
): Promise<WeeklyHighlights> {
  const sevenDaysAgo = nDaysAgo(7)

  const [priceResult, volumeResult, surgeResult] = await Promise.all([
    // 이번 주 최고가 TOP 3
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase as any)
      .from('transactions')
      .select(`
        price, area_m2, deal_date,
        complexes!inner (id, canonical_name, url_slug, si, gu, sgg_code)
      `)
      .in('complexes.sgg_code', [...ACTIVE_SGG_CODES])
      .gte('deal_date', sevenDaysAgo)
      .eq('deal_type', 'sale')
      .is('cancel_date', null)
      .is('superseded_by', null)
      .order('price', { ascending: false })
      .limit(3),

    // 이번 달 거래량 TOP 5
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase as any)
      .from('complexes')
      .select('id, canonical_name, url_slug, si, gu, tx_count_30d')
      .in('sgg_code', [...ACTIVE_SGG_CODES])
      .gt('tx_count_30d', 0)
      .eq('status', 'active')
      .order('tx_count_30d', { ascending: false })
      .limit(5),

    // 전월 대비 급등 TOP 3 (20% 이상)
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase as any)
      .from('complexes')
      .select('id, canonical_name, url_slug, si, gu, price_change_30d')
      .in('sgg_code', [...ACTIVE_SGG_CODES])
      .gte('price_change_30d', 0.20)
      .gt('tx_count_30d', 0)
      .eq('status', 'active')
      .order('price_change_30d', { ascending: false })
      .limit(3),
  ])

  const topPriceThisWeek: WeeklyHighlights['topPriceThisWeek'] = []
  for (const row of ((priceResult.data ?? []) as Record<string, unknown>[])) {
    const c = Array.isArray(row['complexes']) ? row['complexes'][0] : row['complexes']
    if (!c) continue
    topPriceThisWeek.push({
      complexId:   (c as Record<string, unknown>)['id'] as string,
      complexName: (c as Record<string, unknown>)['canonical_name'] as string,
      urlSlug:     (c as Record<string, unknown>)['url_slug'] as string | null,
      si:          (c as Record<string, unknown>)['si'] as string | null,
      gu:          (c as Record<string, unknown>)['gu'] as string | null,
      price:       row['price'] as number,
      area_m2:     row['area_m2'] as number,
      deal_date:   row['deal_date'] as string,
    })
  }

  const topVolumeThisMonth: WeeklyHighlights['topVolumeThisMonth'] = (
    (volumeResult.data ?? []) as Record<string, unknown>[]
  ).map(r => ({
    complexId:   r['id'] as string,
    complexName: r['canonical_name'] as string,
    urlSlug:     r['url_slug'] as string | null,
    si:          r['si'] as string | null,
    gu:          r['gu'] as string | null,
    txCount30d:  r['tx_count_30d'] as number,
  }))

  const priceSurgeLastMonth: WeeklyHighlights['priceSurgeLastMonth'] = (
    (surgeResult.data ?? []) as Record<string, unknown>[]
  ).map(r => ({
    complexId:   r['id'] as string,
    complexName: r['canonical_name'] as string,
    urlSlug:     r['url_slug'] as string | null,
    si:          r['si'] as string | null,
    gu:          r['gu'] as string | null,
    changeRatio: r['price_change_30d'] as number,
  }))

  return { topPriceThisWeek, topVolumeThisMonth, priceSurgeLastMonth }
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:/Users/jung/coding/bds/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>4개 데이터 함수와 모든 타입이 rankings-page.ts에 구현되고 TypeScript 오류 없음</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → Supabase | createReadonlyClient()로만 접근, anon key 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | rankings-page.ts | accept | 공개 실거래 데이터. anon key로 읽기 가능한 데이터만 노출 |
| T-27-02-02 | Denial of Service | getRecentDailyFeed 2차 쿼리 | mitigate | feedComplexIds IN 쿼리 + limit(10000) 하드캡. 소규모 시장(창원·김해) 기준 충분 |
| T-27-02-03 | Denial of Service | getChampionComplexes 2차 쿼리 | mitigate | 90일 거래 IN 쿼리 (complexIds 목록 한정). N+1 아님 |
| T-27-02-04 | Denial of Service | getRegionalPriceRanking N+1 | mitigate | TOP 20 단지 ID 목록으로 IN 쿼리 1회 (N+1 아님), limit(20) 하드캡 |
</threat_model>

<verification>
```bash
# 파일 존재 확인
ls C:/Users/jung/coding/bds/src/lib/data/rankings-page.ts

# 4개 함수 모두 export 확인
grep -v '^//' C:/Users/jung/coding/bds/src/lib/data/rankings-page.ts | grep "^export async function"

# cancel_date IS NULL 필터 존재 확인
grep -c "cancel_date.*null" C:/Users/jung/coding/bds/src/lib/data/rankings-page.ts

# is_new_high 2-query 방식 확인 (histByComplex)
grep -c "histByComplex" C:/Users/jung/coding/bds/src/lib/data/rankings-page.ts

# 90일 거래 쿼리 확인
grep -c "ninetyDaysAgo\|90" C:/Users/jung/coding/bds/src/lib/data/rankings-page.ts

# TypeScript 컴파일 검사
cd C:/Users/jung/coding/bds && npx tsc --noEmit 2>&1 | head -30
```
</verification>

<success_criteria>
1. `src/lib/data/rankings-page.ts` 파일 존재
2. `import 'server-only'` 최상단 포함
3. export 4개: `getRecentDailyFeed`, `getRegionalPriceRanking`, `getChampionComplexes`, `getWeeklyHighlights`
4. export 상수: `REGION_TABS` (7개 항목)
5. export 타입: `DailyFeedGroup`, `RegionalRankingRow`, `ChampionComplexes`, `WeeklyHighlights`
6. `getRecentDailyFeed`: 2차 쿼리로 `histByComplex` Map 구성, ±5㎡ 비교 (complexes.all_time_high 없음)
7. `getChampionComplexes`: 90일 거래 별도 쿼리 + `tx_count_90d` 필드 사용
8. 모든 쿼리에 `cancel_date IS NULL AND superseded_by IS NULL` 적용
9. `tsc --noEmit` 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/27-rankings-page/27-02-SUMMARY.md` 생성.
포함 내용: 함수별 쿼리 전략, 타입 인터페이스 목록, is_new_high 2-query 방식 요약, 주요 상수(REGION_TABS), TypeScript 컴파일 결과.
</output>
