---
phase: 27-rankings-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260617000001_phase27_all_time_high.sql
autonomous: false
requirements:
  - RANK-03

must_haves:
  truths:
    - "complexes 테이블에 all_time_high INTEGER 컬럼이 존재한다 (만원 단위, nullable)"
    - "refresh_complex_price_stats() 실행 시 모든 active 단지의 all_time_high가 갱신된다"
    - "daily cron이 refresh_complex_price_stats() RPC를 호출하므로 all_time_high는 매일 자동 갱신된다"
  artifacts:
    - path: "supabase/migrations/20260617000001_phase27_all_time_high.sql"
      provides: "all_time_high 컬럼 추가 + refresh_complex_price_stats() 업데이트"
      contains: "all_time_high"
  key_links:
    - from: "src/app/api/cron/daily/route.ts"
      to: "refresh_complex_price_stats()"
      via: "supabase.rpc('refresh_complex_price_stats')"
      pattern: "refresh_complex_price_stats"
---

<objective>
`complexes` 테이블에 `all_time_high INTEGER` 컬럼을 추가하고, 기존 `refresh_complex_price_stats()` 함수를 업데이트해 역대 최고 실거래가를 daily cron에서 자동 갱신한다.

Purpose: RANK-03 신고가 마크를 위한 DB 기반 구축. 랭킹 페이지의 `NEW HIGH` 뱃지는 `transaction.price >= complex.all_time_high`로 판단.
Output: 마이그레이션 파일 1개 (컬럼 추가 + 함수 업데이트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-rankings-page/27-CONTEXT.md

<!-- 기존 refresh_complex_price_stats() 최신 버전 (20260521000001_badge_is_new_record.sql) -->
<!-- 신규 all_time_high 컬럼을 기존 함수에 추가해야 함 — 전체 함수를 CREATE OR REPLACE -->
</context>

<interfaces>
<!-- 기존 refresh_complex_price_stats() 현재 업데이트 대상 컬럼 (20260521000001_badge_is_new_record.sql 기준) -->
<!--
  UPDATE public.complexes c SET
    avg_sale_per_pyeong = (...1년 평균 평당가 만원/평...),
    price_change_30d    = (...30일 vs 이전30일 비율...),
    tx_count_30d        = (...30일 거래건수...),
    is_new_record_30d   = (...30일 최고단가 > 이전최고 × 1.03...),
    updated_at          = now()
  WHERE c.status = 'active';

  새로 추가할 컬럼:
    all_time_high = (SELECT max(t.price)::integer FROM transactions t
                     WHERE t.complex_id = c.id AND t.deal_type='sale'
                     AND t.cancel_date IS NULL AND t.superseded_by IS NULL)
-->

<!-- daily/route.ts 호출 방식 (변경 없음) -->
<!--
  await supabase.rpc('refresh_complex_price_stats')
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 마이그레이션 파일 작성 (all_time_high + 함수 업데이트)</name>
  <files>supabase/migrations/20260617000001_phase27_all_time_high.sql</files>
  <read_first>
    - supabase/migrations/20260521000001_badge_is_new_record.sql — 현재 refresh_complex_price_stats() 전체 함수 본문 (CREATE OR REPLACE 시 모든 컬럼 유지 필수)
    - supabase/migrations/20260516000001_phase11_map_columns.sql — avg_sale_per_pyeong 원본 구현 패턴
  </read_first>
  <action>
파일 경로: `supabase/migrations/20260617000001_phase27_all_time_high.sql`

아래 내용을 정확히 작성한다:

```sql
-- Phase 27: 신고가 마크 (RANK-03, D-03)
-- complexes.all_time_high: 해당 단지 역대 최고 실거래가 (만원 단위, nullable)
-- 비교 기준: deal_type='sale', cancel_date IS NULL, superseded_by IS NULL 필터 적용

ALTER TABLE public.complexes
  ADD COLUMN IF NOT EXISTS all_time_high INTEGER;

COMMENT ON COLUMN public.complexes.all_time_high IS
  '역대 최고 실거래가 만원 단위 (transactions.price 동일 단위). daily cron refresh_complex_price_stats()가 매일 갱신. NULL=거래 이력 없음.';

-- refresh_complex_price_stats() 업데이트: all_time_high 집계 추가
-- CRITICAL: cancel_date IS NULL AND superseded_by IS NULL 필터 유지 (CLAUDE.md)
-- 기존 컬럼 (avg_sale_per_pyeong, price_change_30d, tx_count_30d, is_new_record_30d) 모두 그대로 유지
CREATE OR REPLACE FUNCTION public.refresh_complex_price_stats()
RETURNS void
LANGUAGE sql
AS $$
  UPDATE public.complexes c
  SET
    avg_sale_per_pyeong = (
      SELECT (avg(t.price / NULLIF(t.area_m2 / 3.3058, 0)) / 10000)::integer
      FROM public.transactions t
      WHERE t.complex_id = c.id
        AND t.deal_type = 'sale'
        AND t.deal_date >= CURRENT_DATE - INTERVAL '1 year'
        AND t.cancel_date IS NULL
        AND t.superseded_by IS NULL
        AND t.area_m2 > 0
    ),
    price_change_30d = (
      WITH recent AS (
        SELECT avg(t.price) AS avg_p
        FROM public.transactions t
        WHERE t.complex_id = c.id
          AND t.deal_type = 'sale'
          AND t.deal_date >= CURRENT_DATE - INTERVAL '30 days'
          AND t.cancel_date IS NULL
          AND t.superseded_by IS NULL
      ),
      prev AS (
        SELECT avg(t.price) AS avg_p
        FROM public.transactions t
        WHERE t.complex_id = c.id
          AND t.deal_type = 'sale'
          AND t.deal_date >= CURRENT_DATE - INTERVAL '60 days'
          AND t.deal_date <  CURRENT_DATE - INTERVAL '30 days'
          AND t.cancel_date IS NULL
          AND t.superseded_by IS NULL
      )
      SELECT CASE
        WHEN prev.avg_p IS NULL OR prev.avg_p = 0 THEN NULL
        ELSE round(((recent.avg_p - prev.avg_p) / prev.avg_p)::numeric, 4)
      END
      FROM recent, prev
    ),
    tx_count_30d = (
      SELECT count(*)::integer
      FROM public.transactions t
      WHERE t.complex_id = c.id
        AND t.deal_type = 'sale'
        AND t.deal_date >= CURRENT_DATE - INTERVAL '30 days'
        AND t.cancel_date IS NULL
        AND t.superseded_by IS NULL
    ),
    is_new_record_30d = (
      WITH recent_max AS (
        SELECT max(t.price / NULLIF(t.area_m2, 0)) AS max_unit_price
        FROM public.transactions t
        WHERE t.complex_id = c.id
          AND t.deal_type = 'sale'
          AND t.deal_date >= CURRENT_DATE - INTERVAL '30 days'
          AND t.cancel_date IS NULL
          AND t.superseded_by IS NULL
          AND t.area_m2 > 0
      ),
      hist_max AS (
        SELECT max(t.price / NULLIF(t.area_m2, 0)) AS max_unit_price
        FROM public.transactions t
        WHERE t.complex_id = c.id
          AND t.deal_type = 'sale'
          AND t.deal_date < CURRENT_DATE - INTERVAL '30 days'
          AND t.cancel_date IS NULL
          AND t.superseded_by IS NULL
          AND t.area_m2 > 0
      )
      SELECT CASE
        WHEN recent_max.max_unit_price IS NULL THEN false
        WHEN hist_max.max_unit_price  IS NULL THEN false
        WHEN recent_max.max_unit_price > hist_max.max_unit_price * 1.03 THEN true
        ELSE false
      END
      FROM recent_max, hist_max
    ),
    all_time_high = (
      SELECT max(t.price)::integer
      FROM public.transactions t
      WHERE t.complex_id = c.id
        AND t.deal_type = 'sale'
        AND t.cancel_date IS NULL
        AND t.superseded_by IS NULL
    ),
    updated_at = now()
  WHERE c.status = 'active';
$$;
```

주의사항:
- `transactions.price`는 만원 단위. `all_time_high`도 만원 단위로 저장 (나누기 없음).
- `deal_type = 'sale'` 전용. 전월세 제외.
- `cancel_date IS NULL AND superseded_by IS NULL` 필수 (CLAUDE.md 규칙).
- 기존 4개 컬럼의 서브쿼리는 변경 없이 그대로 유지.
  </action>
  <verify>
    <automated>grep -c "all_time_high" C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase27_all_time_high.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260617000001_phase27_all_time_high.sql` 파일이 존재한다
    - 파일 내에 `ALTER TABLE public.complexes ADD COLUMN IF NOT EXISTS all_time_high INTEGER` 구문이 포함된다
    - `refresh_complex_price_stats()` 함수가 `all_time_high` 업데이트를 포함한다
    - 기존 `avg_sale_per_pyeong`, `price_change_30d`, `tx_count_30d`, `is_new_record_30d` 컬럼 업데이트가 그대로 유지된다
    - `cancel_date IS NULL AND superseded_by IS NULL` 필터가 all_time_high 서브쿼리에 적용된다
  </acceptance_criteria>
  <done>마이그레이션 파일이 완성되어 `supabase db push` 적용 준비가 됨</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: supabase db push 실행</name>
  <what-built>Task 1이 마이그레이션 파일을 생성했다. 이제 DB에 적용이 필요하다.</what-built>
  <how-to-verify>
    1. 터미널에서 프로젝트 루트로 이동
    2. `npx supabase db push` 실행
    3. `complexes` 테이블에 `all_time_high` 컬럼이 추가되었는지 확인:
       ```
       npx supabase db query "SELECT column_name, data_type FROM information_schema.columns WHERE table_name='complexes' AND column_name='all_time_high';"
       ```
    4. 결과에 `all_time_high | integer` 행이 나타나면 성공
  </how-to-verify>
  <resume-signal>supabase db push 완료 후 "done" 입력</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → DB | refresh_complex_price_stats() RPC는 service_role로만 호출 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | refresh_complex_price_stats() | accept | SECURITY DEFINER 없음 — cron endpoint가 service_role로만 호출, 외부 노출 없음 |
| T-27-01-02 | Information Disclosure | all_time_high column | accept | 공개 데이터 (실거래가). RLS anon 읽기 허용이 기존 패턴과 동일 |
</threat_model>

<verification>
```bash
# 마이그레이션 파일 존재 확인
ls C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase27_all_time_high.sql

# all_time_high 컬럼 언급 횟수 (최소 3회: ALTER + COMMENT + 서브쿼리)
grep -v '^--' C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase27_all_time_high.sql | grep -c "all_time_high"

# cancel_date IS NULL 필터가 all_time_high 서브쿼리에 있는지 확인
grep -A5 "all_time_high = " C:/Users/jung/coding/bds/supabase/migrations/20260617000001_phase27_all_time_high.sql | grep "cancel_date"
```
</verification>

<success_criteria>
1. `supabase/migrations/20260617000001_phase27_all_time_high.sql`이 존재하고 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS all_time_high INTEGER` 포함
2. `refresh_complex_price_stats()` 함수에 `all_time_high = (SELECT max(t.price)::integer ...)` 서브쿼리가 추가됨
3. 기존 4개 컬럼 업데이트 로직이 변경 없이 유지됨
4. `supabase db push` 완료 후 `complexes.all_time_high` 컬럼이 DB에 존재함
</success_criteria>

<output>
완료 후 `.planning/phases/27-rankings-page/27-01-SUMMARY.md` 생성.
포함 내용: 마이그레이션 파일명, all_time_high 컬럼 정의, supabase db push 결과.
</output>
