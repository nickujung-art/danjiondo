---
phase: 27-rankings-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - RANK-03

must_haves:
  truths:
    - "transactions 테이블에 id, area_m2, complex_id, price, deal_type, cancel_date, superseded_by, deal_date 컬럼이 존재한다"
    - "is_new_high는 DB 컬럼 추가 없이 쿼리 타임에 TypeScript 2-query 방식으로 ±5㎡ 비교 계산된다"
    - "complexes.all_time_high 컬럼은 추가하지 않는다 (59㎡ 거래가 134㎡ 역대 최고가와 비교되는 오류 방지)"
  artifacts: []
  key_links: []
---

<objective>
신고가 마크(RANK-03) 구현 방식 확정 및 스키마 검증: `complexes.all_time_high` 컬럼 없이, 쿼리 타임에 ±5㎡ 면적대 TypeScript 2-query 방식으로 `is_new_high`를 계산한다.

Purpose: 면적대별 정확한 신고가 비교를 보장한다. 단지 전체 역대 최고가 비교(이전 설계)는 59㎡ 거래가 134㎡ 역대 최고가와 비교되는 구조적 오류가 있어 제거한다. DB 마이그레이션 불필요.
Output: 스키마 변경 없음. 이 플랜은 기존 transactions 컬럼 충족 여부를 확인하고 Plan 02에 구현 접근법을 전달한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-rankings-page/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: transactions 스키마 확인 (±5㎡ 쿼리 지원 검증)</name>
  <files></files>
  <action>
Supabase MCP 도구 또는 아래 CLI로 `transactions` 테이블에 필요한 컬럼이 모두 존재하는지 확인한다.

**필요 컬럼 체크리스트 (±5㎡ TypeScript 2-query 방식에 필요):**
- `id` — 자기 자신 제외 (`h.id !== txId`) 용
- `complex_id` — 단지 필터링
- `area_m2` — ±5 비교 (`Math.abs(h.area_m2 - area_m2) <= 5`)
- `price` — 최고가 비교
- `deal_type` — 'sale' 필터
- `cancel_date` — IS NULL 필터
- `superseded_by` — IS NULL 필터
- `deal_date` — 피드 날짜 필터

확인 쿼리 (Supabase MCP `execute_sql` 또는 CLI):
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'transactions'
  AND column_name IN ('id','complex_id','area_m2','price','deal_type','cancel_date','superseded_by','deal_date')
ORDER BY column_name;
```

8개 컬럼이 모두 존재하면 DB 스키마 변경 불필요. Plan 02로 진행.

**Plan 02 executor에게 전달하는 구현 접근법:**

`getRecentDailyFeed`에서 `is_new_high`를 2-query TypeScript 방식으로 계산한다:
1. Query 1: 최근 N일 피드 거래 조회 (`all_time_high` 없이)
2. Query 2: 피드에 등장한 `complex_id`들의 전체 이력 거래 조회 (`id, complex_id, area_m2, price`, limit 10000)
3. TypeScript에서 `histByComplex` Map 구성
4. 각 거래마다: `comparables = history.filter(h => h.id !== txId && Math.abs(h.area_m2 - area_m2) <= 5)`
5. `is_new_high = comparables.length > 0 && price >= Math.max(...comparables.map(h => h.price))`
6. 비교 대상 없으면(첫 거래) `is_new_high = false` (보수적 처리)
  </action>
  <verify>
    <automated>echo "Schema check: run SQL above via Supabase MCP or npx supabase db query. Expected: 8 rows returned."</automated>
  </verify>
  <done>
    transactions 테이블 8개 컬럼 존재 확인. DB 마이그레이션 파일 생성 없음.
    Plan 02 executor는 TypeScript 2-query 방식으로 is_new_high를 구현한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 쿼리 타임 계산 → DB | is_new_high 계산은 서버 사이드 TypeScript에서만 발생 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Information Disclosure | is_new_high 계산 | accept | 공개 실거래 데이터. TypeScript 서버 레이어에서만 처리됨 |
</threat_model>

<verification>
```bash
# transactions 컬럼 확인 (8개 모두 존재 여부)
npx supabase db query "SELECT column_name FROM information_schema.columns WHERE table_name='transactions' AND column_name IN ('id','complex_id','area_m2','price','deal_type','cancel_date','superseded_by','deal_date') ORDER BY column_name;"
# 기대 결과: 8개 행 반환

# 마이그레이션 파일 미생성 확인 (새 파일 없어야 함)
ls supabase/migrations/ | grep 20260617
```
</verification>

<success_criteria>
1. transactions 테이블에 ±5㎡ 쿼리에 필요한 8개 컬럼이 모두 존재한다
2. DB 마이그레이션 파일이 생성되지 않는다 (스키마 변경 불필요)
3. Plan 02 executor가 TypeScript 2-query 방식으로 is_new_high를 구현할 수 있다
</success_criteria>

<output>
완료 후 `.planning/phases/27-rankings-page/27-01-SUMMARY.md` 생성.
포함 내용: 스키마 확인 결과 (8개 컬럼 목록), is_new_high 계산 방식 요약, Plan 02로 전달되는 구현 명세.
</output>
