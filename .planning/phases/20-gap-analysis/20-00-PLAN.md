---
phase: 20-gap-analysis
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260528000001_complex_gap_stats.sql
autonomous: false
requirements:
  - GAP-D05

must_haves:
  truths:
    - "complex_gap_stats 테이블이 DB에 존재하고 RLS public read 정책이 적용된다"
    - "compute_gap_stats() SQL 함수가 호출되면 매매/전세 각 3건 이상인 단지의 갭 통계를 반환한다"
    - "data_sources 테이블에 'gap-stats' 행이 존재한다"
  artifacts:
    - path: "supabase/migrations/20260528000001_complex_gap_stats.sql"
      provides: "complex_gap_stats 테이블 + compute_gap_stats RPC + RLS + data_sources 행"
      contains: "CREATE TABLE public.complex_gap_stats"
  key_links:
    - from: "compute_gap_stats SQL 함수"
      to: "transactions 테이블"
      via: "PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY price)"
      pattern: "PERCENTILE_CONT.*jeonse"
    - from: "complex_gap_stats"
      to: "complexes"
      via: "REFERENCES public.complexes(id) ON DELETE CASCADE"
      pattern: "REFERENCES.*complexes"
---

<objective>
`complex_gap_stats` 테이블, `compute_gap_stats` SQL RPC 함수, RLS 정책, data_sources 등록을 마이그레이션으로 생성한다.

Purpose: Wave 1 집계 로직이 의존하는 DB 구조를 먼저 확립한다. `supabase db push` 적용 후 Wave 1 구현이 가능하다.
Output: 단일 마이그레이션 SQL 파일
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\PROJECT.md
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
</context>

<interfaces>
<!-- 참조: 기존 complex_rankings 테이블 패턴 (supabase/migrations/20260507000001_complex_rankings.sql) -->
<!-- complex_gap_stats는 같은 설계 원칙을 따른다: regular table, UPSERT-safe UNIQUE, RLS public read -->

<!-- transactions 테이블 컬럼 (verified) -->
<!-- complex_id uuid, deal_type text ('sale'|'jeonse'|'monthly'), price bigint, deal_date date -->
<!-- cancel_date date NULL, superseded_by uuid NULL, area_m2 numeric -->

<!-- data_sources 테이블 컬럼 (verified from cron-status.ts 사용 패턴) -->
<!-- id text PK, cadence text, expected_freshness_hours integer, ui_label text -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: complex_gap_stats 마이그레이션 SQL 작성</name>
  <files>supabase/migrations/20260528000001_complex_gap_stats.sql</files>
  <action>
아래 내용을 정확히 구현하는 마이그레이션 파일을 작성한다.

**1. complex_gap_stats 테이블:**
```sql
CREATE TABLE public.complex_gap_stats (
  id                  uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  complex_id          uuid NOT NULL REFERENCES public.complexes(id) ON DELETE CASCADE,
  median_sale_price   bigint NOT NULL,      -- 매매가 중위값 (만원)
  median_jeonse_price bigint NOT NULL,      -- 전세보증금 중위값 (만원)
  gap_amount          bigint NOT NULL,      -- 갭 금액 = 매매가 - 전세가 (만원)
  gap_ratio           numeric(5,1) NOT NULL,-- 갭 비율 % (소수 1자리)
  jeonse_ratio        numeric(5,1) NOT NULL,-- 전세가율 % (= 100 - gap_ratio)
  risk_level          text NOT NULL CHECK (risk_level IN ('safe', 'caution', 'danger')),
  sale_count          integer NOT NULL,     -- 매매 거래 건수 (12개월)
  jeonse_count        integer NOT NULL,     -- 전세 거래 건수 (12개월)
  window_months       integer NOT NULL DEFAULT 12,
  computed_at         timestamptz NOT NULL DEFAULT now(),
  UNIQUE (complex_id)
);

COMMENT ON TABLE public.complex_gap_stats IS
  '단지별 갭투자 통계 캐시. daily-batch cron이 12개월 중위값으로 일 1회 UPSERT.';
```

**2. 인덱스 3개:**
- `complex_gap_stats_gap_ratio_idx ON (gap_ratio DESC)` — 랭킹 페이지 정렬용
- `complex_gap_stats_risk_level_idx ON (risk_level)` — 위험도 필터용
- `complex_gap_stats_complex_id_idx ON (complex_id)` — FK 조회용

**3. RLS:**
```sql
ALTER TABLE public.complex_gap_stats ENABLE ROW LEVEL SECURITY;
CREATE POLICY "gap_stats_public_read"
  ON public.complex_gap_stats FOR SELECT
  USING (true);
```

**4. compute_gap_stats SQL 함수:**

CRITICAL 주의사항:
- `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY col)` 만 사용 — `OVER()` 절 금지
- deal_type = 'sale' (매매), deal_type = 'jeonse' (전세) — 'trade'/'lease' 사용 금지
- cancel_date IS NULL AND superseded_by IS NULL 필터 필수
- complex_id IS NOT NULL 필터 필수
- sale_count >= 3 AND jeonse_count >= 3 조건으로 데이터 희박 단지 제외

```sql
CREATE OR REPLACE FUNCTION public.compute_gap_stats(
  p_window_months integer DEFAULT 12
)
RETURNS TABLE (
  complex_id          uuid,
  median_sale_price   bigint,
  median_jeonse_price bigint,
  gap_amount          bigint,
  gap_ratio           numeric,
  jeonse_ratio        numeric,
  sale_count          integer,
  jeonse_count        integer
)
LANGUAGE sql
STABLE
AS $$
  WITH sale_stats AS (
    SELECT
      complex_id,
      PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY price) AS median_sale_price,
      COUNT(*)::integer AS sale_count
    FROM public.transactions
    WHERE
      deal_type = 'sale'
      AND deal_date >= CURRENT_DATE - (p_window_months || ' months')::interval
      AND cancel_date IS NULL
      AND superseded_by IS NULL
      AND complex_id IS NOT NULL
      AND price IS NOT NULL
    GROUP BY complex_id
  ),
  jeonse_stats AS (
    SELECT
      complex_id,
      PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY price) AS median_jeonse_price,
      COUNT(*)::integer AS jeonse_count
    FROM public.transactions
    WHERE
      deal_type = 'jeonse'
      AND deal_date >= CURRENT_DATE - (p_window_months || ' months')::interval
      AND cancel_date IS NULL
      AND superseded_by IS NULL
      AND complex_id IS NOT NULL
      AND price IS NOT NULL
    GROUP BY complex_id
  )
  SELECT
    s.complex_id,
    s.median_sale_price::bigint,
    j.median_jeonse_price::bigint,
    (s.median_sale_price - j.median_jeonse_price)::bigint AS gap_amount,
    ROUND((1.0 - j.median_jeonse_price / s.median_sale_price) * 100, 1) AS gap_ratio,
    ROUND((j.median_jeonse_price / s.median_sale_price) * 100, 1) AS jeonse_ratio,
    s.sale_count,
    j.jeonse_count
  FROM sale_stats s
  JOIN jeonse_stats j ON s.complex_id = j.complex_id
  WHERE s.sale_count >= 3 AND j.jeonse_count >= 3;
$$;
```

**5. data_sources 등록:**
```sql
INSERT INTO public.data_sources (id, cadence, expected_freshness_hours, ui_label)
VALUES ('gap-stats', 'daily', 28, '갭투자 통계 (일배치 재계산)')
ON CONFLICT (id) DO NOTHING;
```
  </action>
  <behavior>
    - compute_gap_stats()가 매매 3건 미만 단지를 결과에서 제외한다
    - compute_gap_stats()가 deal_type='monthly' 거래를 포함하지 않는다 (jeonse만 포함)
    - gap_ratio + jeonse_ratio = 100 이 성립한다
    - cancel_date IS NOT NULL 거래는 집계에서 제외된다
  </behavior>
  <verify>
    <automated>npm run db:push -- --dry-run 2>&1 | head -20 || echo "마이그레이션 파일 문법 검증"</automated>
  </verify>
  <done>
    supabase/migrations/20260528000001_complex_gap_stats.sql 파일이 존재하고,
    complex_gap_stats 테이블 DDL, compute_gap_stats 함수, RLS 정책, data_sources INSERT를 모두 포함한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron→DB | compute_gap_stats는 서비스 롤(admin client)만 호출. anon key로는 호출 불가 |
| 공개 조회 | complex_gap_stats SELECT는 anon key로 가능 (공개 부동산 정보) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | compute_gap_stats RPC | mitigate | 함수는 STABLE (읽기 전용 집계). SECURITY DEFINER 불필요 — 서비스 롤로만 호출 |
| T-20-02 | Information Disclosure | complex_gap_stats public read | accept | 갭투자 통계는 공개 부동산 정보; 개인정보 미포함 |
| T-20-03 | Tampering | deal_type 필터 오기 | mitigate | 마이그레이션에 'sale'/'jeonse' 명시 + CHECK constraint 없음이지만 필터 오기 방지는 애플리케이션 계층에서 처리 |
</threat_model>

<verification>
1. 파일이 존재한다: `ls supabase/migrations/20260528000001_complex_gap_stats.sql`
2. complex_gap_stats CREATE TABLE 포함: `grep -c "CREATE TABLE public.complex_gap_stats" supabase/migrations/20260528000001_complex_gap_stats.sql`
3. compute_gap_stats 함수 포함: `grep -c "CREATE OR REPLACE FUNCTION public.compute_gap_stats" supabase/migrations/20260528000001_complex_gap_stats.sql`
4. RLS 정책 포함: `grep -c "gap_stats_public_read" supabase/migrations/20260528000001_complex_gap_stats.sql`
5. data_sources 등록 포함: `grep -c "gap-stats" supabase/migrations/20260528000001_complex_gap_stats.sql`
6. 'sale'/'jeonse' 올바른 deal_type 사용 확인: `grep -v "^--" supabase/migrations/20260528000001_complex_gap_stats.sql | grep -c "deal_type = 'sale'"`
</verification>

<success_criteria>
- supabase/migrations/20260528000001_complex_gap_stats.sql 존재
- complex_gap_stats 테이블: id, complex_id, median_sale_price, median_jeonse_price, gap_amount, gap_ratio, jeonse_ratio, risk_level, sale_count, jeonse_count, window_months, computed_at 컬럼 포함
- UNIQUE (complex_id) 제약 포함 (UPSERT onConflict 대상)
- RLS 활성화 + gap_stats_public_read SELECT 정책
- compute_gap_stats() 함수: deal_type='sale'/'jeonse', cancel_date IS NULL, superseded_by IS NULL, complex_id IS NOT NULL, sale_count >= 3, jeonse_count >= 3 조건 모두 포함
- data_sources에 'gap-stats' row INSERT
</success_criteria>

<checkpoint>
## CHECKPOINT: supabase db push 적용 필요

마이그레이션 파일 작성 완료 후 개발자가 직접 실행해야 한다:

```bash
npm run db:push
# 또는
npx supabase db push
```

Wave 1 구현은 이 마이그레이션이 적용된 후 진행한다.

Wave 1 진행 전 DB에서 확인:
```sql
SELECT COUNT(*) FROM information_schema.tables WHERE table_name = 'complex_gap_stats';
SELECT routine_name FROM information_schema.routines WHERE routine_name = 'compute_gap_stats';
```
</checkpoint>

<output>
완료 후 `.planning/phases/20-gap-analysis/20-00-SUMMARY.md` 생성.
</output>
