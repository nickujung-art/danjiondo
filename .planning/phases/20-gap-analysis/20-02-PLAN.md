---
phase: 20-gap-analysis
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/lib/data/gap-analysis.ts
  - src/components/complex/GapAnalysisCard.tsx
  - src/app/complexes/[id]/page.tsx
  - src/__tests__/gap-analysis.test.ts
autonomous: true
requirements:
  - GAP-D01
  - GAP-D02
  - GAP-D06

must_haves:
  truths:
    - "단지 상세 페이지에 갭투자 분석 카드가 표시된다 (complex_gap_stats 데이터가 있는 단지)"
    - "갭투자 카드는 갭 금액(억/만원), 갭 비율(%), 전세가율(%)을 표시한다"
    - "위험도 배지가 CSS dot으로 표시된다 (이모지 금지): 안전=#16a34a, 주의=#d97706, 위험=#dc2626"
    - "데이터가 없는 단지에서 갭투자 카드가 렌더링되지 않는다 (null 반환)"
    - "기존 Promise.all 데이터 패칭 구조를 유지한다 (page.tsx)"
  artifacts:
    - path: "src/lib/data/gap-analysis.ts"
      provides: "getComplexGapStats(complexId, supabase) 읽기 함수"
      contains: "from('complex_gap_stats')"
    - path: "src/components/complex/GapAnalysisCard.tsx"
      provides: "갭투자 카드 RSC 컴포넌트"
      contains: "GapAnalysisCardProps"
    - path: "src/__tests__/gap-analysis.test.ts"
      provides: "GAP-06, GAP-07 단위 테스트"
      contains: "getComplexGapStats"
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "갭 카드 통합 (Promise.all에 추가)"
      contains: "GapAnalysisCard"
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/lib/data/gap-analysis.ts"
      via: "getComplexGapStats(id, supabase) in Promise.all"
      pattern: "getComplexGapStats"
    - from: "src/components/complex/GapAnalysisCard.tsx"
      to: "formatPrice (page.tsx 내 로컬 함수)"
      via: "prop으로 포맷된 값 전달 또는 컴포넌트 내 직접 포맷"
      pattern: "formatPrice|gapAmount"
---

<objective>
단지 상세 페이지에 갭투자 분석 카드를 추가한다. `getComplexGapStats`로 `complex_gap_stats` 테이블을 조회하고, `GapAnalysisCard` RSC 컴포넌트로 렌더링한다.

Purpose: D-01 결정 — 단지 상세 페이지에 갭투자 요약 카드 표시. D-02 결정 — 숫자 + CSS 신호등 배지.
Output: gap-analysis.ts (조회 함수), GapAnalysisCard.tsx (RSC), page.tsx 수정, gap-analysis.test.ts
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\PROJECT.md
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\20-gap-analysis\20-01-SUMMARY.md
@C:\Users\jung\coding\bds\src\app\complexes\[id]\page.tsx
</context>

<interfaces>
<!-- page.tsx 기존 formatPrice 함수 (재사용, 컴포넌트 내 인라인 또는 lib으로 이동 불필요) -->
```typescript
function formatPrice(price: number): string {
  const uk = Math.floor(price / 10000)
  const man = price % 10000
  if (uk > 0 && man > 0) return `${uk}억 ${man.toLocaleString()}`
  if (uk > 0) return `${uk}억`
  return `${price.toLocaleString()}만`
}
```

<!-- page.tsx 기존 Promise.all 패턴 — 끝 부분에 추가 -->
```typescript
const [
  saleData,
  complexRealtors,
  // ... 기존 14개 항목 ...
  rawJeonseData,
  // ↓ 여기에 추가
  gapStats,
] = await Promise.all([
  // ... 기존 항목들 ...
  getComplexGapStats(id, supabase).catch(() => null),
])
```

<!-- createReadonlyClient 패턴 — 단지 상세 페이지는 readonly client 사용 -->
```typescript
import { createReadonlyClient } from '@/lib/supabase/readonly'
const supabase = createReadonlyClient()
```

<!-- GapAnalysisCardProps 인터페이스 -->
```typescript
interface GapAnalysisCardProps {
  gapAmount: number       // 만원 단위
  gapRatio: number        // % (소수 1자리)
  jeonseRatio: number     // % (소수 1자리)
  riskLevel: 'safe' | 'caution' | 'danger'
  saleCount: number
  jeonseCount: number
}
```

<!-- D-02 위험도 배지 CSS (이모지 금지 — CSS dot 사용) -->
```typescript
const BADGE_STYLES = {
  safe:    { background: '#16a34a', color: '#fff', label: '안전' },
  caution: { background: '#d97706', color: '#fff', label: '주의' },
  danger:  { background: '#dc2626', color: '#fff', label: '위험' },
}
```

<!-- complex_gap_stats 테이블 컬럼 (Wave 0에서 생성) -->
```
id, complex_id, median_sale_price, median_jeonse_price, gap_amount,
gap_ratio, jeonse_ratio, risk_level, sale_count, jeonse_count, window_months, computed_at
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gap-analysis.ts 조회 함수 + 테스트 (GAP-06, GAP-07)</name>
  <files>src/lib/data/gap-analysis.ts, src/__tests__/gap-analysis.test.ts</files>
  <behavior>
    - GAP-06: getComplexGapStats('nonexistent-id', mockSupabase) → null 반환 (데이터 없는 단지)
    - GAP-06: getComplexGapStats('valid-id', mockSupabase) → GapStatsRow 형태 객체 반환
    - GAP-07: GapAnalysisCard에 null 데이터가 전달되면 null을 렌더링한다 (컴포넌트 레벨 테스트는 Wave 2 Task 1에서 커버)
  </behavior>
  <action>
**Step 1 — 테스트 파일 작성 (RED):**

`src/__tests__/gap-analysis.test.ts`:

```typescript
import { describe, it, expect, vi, beforeAll } from 'vitest'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

vi.mock('server-only', () => ({}))
vi.mock('next/headers', () => ({
  cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
}))

beforeAll(() => {
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', 'http://127.0.0.1:54321')
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', 'test-anon-key')
})

function makeMockChain(result: { data: unknown; error: unknown }) {
  const chain: Record<string, unknown> = {}
  const terminal = vi.fn().mockResolvedValue(result)
  const methods = ['select', 'eq', 'is', 'in', 'not', 'order', 'limit']
  for (const m of methods) chain[m] = vi.fn().mockReturnValue(chain)
  chain['maybeSingle'] = terminal
  chain['single'] = terminal
  ;(chain['limit'] as ReturnType<typeof vi.fn>).mockResolvedValue(result)
  return chain
}

function makeMockSupabase(overrides: Record<string, ReturnType<typeof makeMockChain>> = {}) {
  return {
    from: vi.fn((table: string) => overrides[table] ?? makeMockChain({ data: null, error: null })),
  } as unknown as SupabaseClient<Database>
}

describe('getComplexGapStats', () => {
  it('데이터 없는 단지 → null 반환', async () => {
    const { getComplexGapStats } = await import('@/lib/data/gap-analysis')
    const mockSupabase = makeMockSupabase()
    const result = await getComplexGapStats('nonexistent-id', mockSupabase)
    expect(result).toBeNull()
  })

  it('데이터 있는 단지 → GapStatsRow 반환', async () => {
    const { getComplexGapStats } = await import('@/lib/data/gap-analysis')
    const mockRow = {
      complex_id: 'valid-id',
      median_sale_price: 35000,
      median_jeonse_price: 22000,
      gap_amount: 13000,
      gap_ratio: '37.1',
      jeonse_ratio: '62.9',
      risk_level: 'safe',
      sale_count: 5,
      jeonse_count: 4,
      window_months: 12,
      computed_at: '2026-05-28T00:00:00Z',
    }
    const chain = makeMockChain({ data: mockRow, error: null })
    const mockSupabase = makeMockSupabase({ complex_gap_stats: chain })
    const result = await getComplexGapStats('valid-id', mockSupabase)
    expect(result).not.toBeNull()
    expect(result?.riskLevel).toBe('safe')
    expect(result?.gapRatio).toBeCloseTo(37.1, 1)
    expect(result?.saleCount).toBe(5)
  })
})
```

**Step 2 — `src/lib/data/gap-analysis.ts` 구현 (GREEN):**

```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'
import type { RiskLevel } from './gap-stats'

export interface ComplexGapStatsResult {
  complexId: string
  medianSalePrice: number
  medianJeonsePrice: number
  gapAmount: number
  gapRatio: number
  jeonseRatio: number
  riskLevel: RiskLevel
  saleCount: number
  jeonseCount: number
  computedAt: string
}

/**
 * 단지 상세 페이지에서 complex_gap_stats 테이블을 단순 조회한다.
 * createReadonlyClient()로 생성한 supabase를 전달한다.
 * 데이터 없으면 null 반환 (섹션 숨김 처리용).
 */
export async function getComplexGapStats(
  complexId: string,
  supabase: SupabaseClient<Database>,
): Promise<ComplexGapStatsResult | null> {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase as any)
    .from('complex_gap_stats')
    .select('complex_id, median_sale_price, median_jeonse_price, gap_amount, gap_ratio, jeonse_ratio, risk_level, sale_count, jeonse_count, computed_at')
    .eq('complex_id', complexId)
    .maybeSingle()

  if (error || !data) return null

  const row = data as {
    complex_id: string
    median_sale_price: number
    median_jeonse_price: number
    gap_amount: number
    gap_ratio: string | number
    jeonse_ratio: string | number
    risk_level: string
    sale_count: number
    jeonse_count: number
    computed_at: string
  }

  return {
    complexId:          row.complex_id,
    medianSalePrice:    row.median_sale_price,
    medianJeonsePrice:  row.median_jeonse_price,
    gapAmount:          row.gap_amount,
    gapRatio:           Number(row.gap_ratio),
    jeonseRatio:        Number(row.jeonse_ratio),
    riskLevel:          row.risk_level as RiskLevel,
    saleCount:          row.sale_count,
    jeonseCount:        row.jeonse_count,
    computedAt:         row.computed_at,
  }
}
```
  </action>
  <verify>
    <automated>npm run test -- gap-analysis</automated>
  </verify>
  <done>
    - GAP-06 테스트 PASS: null 반환 + 정상 데이터 반환 두 케이스 모두
    - src/lib/data/gap-analysis.ts에 getComplexGapStats, ComplexGapStatsResult export
    - npm run lint 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: GapAnalysisCard.tsx 컴포넌트 + page.tsx 통합</name>
  <files>src/components/complex/GapAnalysisCard.tsx, src/app/complexes/[id]/page.tsx</files>
  <action>
**1. `src/components/complex/GapAnalysisCard.tsx` 작성:**

CRITICAL 규칙:
- `'use client'` 금지 — RSC (서버 컴포넌트)
- 이모지 금지 — CSS dot (`border-radius: 50%`) 또는 인라인 `<span>` 색상 원형 사용
- backdrop-blur, gradient-text, glow, 보라/인디고, gradient orb 금지
- 기존 page.tsx의 card 스타일 (`className="card"`, `padding: 20`) 준용

```typescript
import type { ComplexGapStatsResult } from '@/lib/data/gap-analysis'

function formatPrice(price: number): string {
  const uk = Math.floor(price / 10000)
  const man = price % 10000
  if (uk > 0 && man > 0) return `${uk}억 ${man.toLocaleString()}`
  if (uk > 0) return `${uk}억`
  return `${price.toLocaleString()}만`
}

const RISK_BADGE: Record<string, { bg: string; label: string }> = {
  safe:    { bg: '#16a34a', label: '안전' },
  caution: { bg: '#d97706', label: '주의' },
  danger:  { bg: '#dc2626', label: '위험' },
}

interface GapAnalysisCardProps {
  data: ComplexGapStatsResult | null
}

export function GapAnalysisCard({ data }: GapAnalysisCardProps) {
  if (!data) return null

  const badge = RISK_BADGE[data.riskLevel] ?? RISK_BADGE['caution']!

  return (
    <div className="card" style={{ padding: 20 }}>
      {/* 헤더 행 */}
      <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 16 }}>
        <h3 style={{ font: '700 15px/1.4 var(--font-sans)', margin: 0 }}>갭투자 분석</h3>
        {/* CSS dot + 텍스트 배지 (이모지 금지) */}
        <span style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 5,
          background: badge.bg,
          color: '#fff',
          font: '600 12px/1 var(--font-sans)',
          borderRadius: 6,
          padding: '4px 10px',
        }}>
          <span style={{
            display: 'inline-block',
            width: 7,
            height: 7,
            borderRadius: '50%',
            background: 'rgba(255,255,255,0.7)',
            flexShrink: 0,
          }} />
          {badge.label}
        </span>
      </div>

      {/* 3열 숫자 */}
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 12, marginBottom: 14 }}>
        {[
          { label: '갭 금액',  value: formatPrice(data.gapAmount) },
          { label: '갭 비율',  value: `${data.gapRatio.toFixed(1)}%` },
          { label: '전세가율', value: `${data.jeonseRatio.toFixed(1)}%` },
        ].map(item => (
          <div key={item.label}>
            <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 4 }}>
              {item.label}
            </div>
            <div className="tnum" style={{ font: '700 16px/1.2 var(--font-sans)' }}>
              {item.value}
            </div>
          </div>
        ))}
      </div>

      {/* 기준 메타 */}
      <div style={{ font: '500 11px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', borderTop: '1px solid var(--line-subtle)', paddingTop: 10 }}>
        기준: 최근 12개월 거래 중위값 · 매매 {data.saleCount}건 / 전세 {data.jeonseCount}건
      </div>
    </div>
  )
}
```

**2. `src/app/complexes/[id]/page.tsx` 수정:**

상단 import에 추가:
```typescript
import { getComplexGapStats } from '@/lib/data/gap-analysis'
import type { ComplexGapStatsResult } from '@/lib/data/gap-analysis'
import { GapAnalysisCard } from '@/components/complex/GapAnalysisCard'
```

Promise.all 배열의 끝(rawJeonseData 항목 다음)에 추가:
```typescript
    // 갭투자 분석 (데이터 없으면 null)
    getComplexGapStats(id, supabase).catch(() => null),
```

Promise.all 구조분해 배열의 끝에 `gapStats,` 추가 (타입: `ComplexGapStatsResult | null`).

JSX에서 `<ManagementCostCard>` 바로 앞(시설 카드와 관리비 카드 사이)에 추가:
```tsx
          {/* 갭투자 분석 (D-01: 단지 상세 카드) */}
          <GapAnalysisCard data={gapStats} />
```

데이터 없는 단지는 GapAnalysisCard가 null을 반환하므로 조건부 렌더링 불필요.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/components/complex/GapAnalysisCard.tsx 존재, 이모지 미포함, 'use client' 미포함
    - GapAnalysisCard({ data: null }) → null 렌더링
    - page.tsx에 GapAnalysisCard import + 렌더링 포함
    - npm run build 성공 (TypeScript 오류 없음)
    - npm run lint 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC→DB | getComplexGapStats는 readonly client (anon key) 사용 — RLS public read 정책으로 허용 |
| 사용자→페이지 | URL의 id 파라미터는 기존 getComplexById에서 검증 (notFound() 처리) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | complexId URL 파라미터 | accept | Supabase .eq('complex_id', complexId)는 파라미터화 쿼리 — SQL injection 불가 |
| T-20-08 | Information Disclosure | complex_gap_stats 공개 조회 | accept | 공개 부동산 통계 데이터, 개인정보 미포함, RLS public read |
| T-20-09 | Spoofing | GapAnalysisCard 클라이언트 렌더 | mitigate | RSC — 'use client' 금지로 클라이언트 사이드 조작 불가 |
</threat_model>

<verification>
1. 테스트 통과: `npm run test -- gap-analysis` — GAP-06 PASS
2. 빌드 성공: `npm run build` 오류 없음
3. 컴포넌트 이모지 미포함: `grep -v "^//" src/components/complex/GapAnalysisCard.tsx | grep -c "🟢\|🟡\|🔴"` → 0
4. RSC 확인: `grep -c "use client" src/components/complex/GapAnalysisCard.tsx` → 0
5. page.tsx 통합 확인: `grep -c "GapAnalysisCard" src/app/complexes/[id]/page.tsx` → 2 이상 (import + 사용)
6. gap-analysis.ts server-only 확인: `grep -c "server-only" src/lib/data/gap-analysis.ts` → 1
</verification>

<success_criteria>
- src/__tests__/gap-analysis.test.ts: GAP-06 2개 케이스(null/정상) PASS
- src/lib/data/gap-analysis.ts: getComplexGapStats, ComplexGapStatsResult export
- src/components/complex/GapAnalysisCard.tsx: RSC, 이모지 없음, CSS dot 배지, null data → null 반환
- src/app/complexes/[id]/page.tsx: gapStats 추가, GapAnalysisCard 렌더링
- npm run lint && npm run build 통과
</success_criteria>

<output>
완료 후 `.planning/phases/20-gap-analysis/20-02-SUMMARY.md` 생성.
</output>
