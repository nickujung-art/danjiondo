---
phase: 20-gap-analysis
plan: "01"
type: execute
wave: 1
depends_on: ["20-00"]
files_modified:
  - src/lib/data/gap-stats.ts
  - src/app/api/cron/daily/route.ts
  - src/__tests__/gap-stats.test.ts
autonomous: true
requirements:
  - GAP-D03
  - GAP-D04
  - GAP-D05

must_haves:
  truths:
    - "computeRiskLevel(39) === 'safe', computeRiskLevel(50) === 'caution', computeRiskLevel(61) === 'danger'"
    - "computeGapStats()가 실행되면 compute_gap_stats RPC를 호출하고 결과를 complex_gap_stats에 UPSERT한다"
    - "daily cron 엔드포인트 실행 시 gap-stats도 함께 계산되고 'gap-stats' data_source status가 갱신된다"
    - "CRON_SECRET 없이 /api/cron/daily 호출 시 401 반환 (기존 동작 유지)"
  artifacts:
    - path: "src/lib/data/gap-stats.ts"
      provides: "computeGapStats(), computeRiskLevel(), RiskLevel, GapStatsRow"
      contains: "import 'server-only'"
    - path: "src/__tests__/gap-stats.test.ts"
      provides: "GAP-01 ~ GAP-05 단위 테스트"
      contains: "computeRiskLevel"
    - path: "src/app/api/cron/daily/route.ts"
      provides: "gap-stats 계산을 daily 배치에 통합"
      contains: "computeGapStats"
  key_links:
    - from: "src/lib/data/gap-stats.ts"
      to: "Supabase compute_gap_stats RPC"
      via: "supabase.rpc('compute_gap_stats', { p_window_months: 12 })"
      pattern: "rpc.*compute_gap_stats"
    - from: "src/lib/data/gap-stats.ts"
      to: "complex_gap_stats 테이블"
      via: "supabase.from('complex_gap_stats').upsert(rows, { onConflict: 'complex_id' })"
      pattern: "complex_gap_stats.*upsert"
    - from: "src/app/api/cron/daily/route.ts"
      to: "src/lib/data/gap-stats.ts"
      via: "import { computeGapStats }"
      pattern: "computeGapStats"
---

<objective>
갭 통계 집계 함수(`computeGapStats`)와 `computeRiskLevel` 유틸리티를 구현하고, 일배치 cron에 통합한다.

Purpose: Vercel Hobby 플랜은 cron job 2개 한도(현재 `/api/cron/daily`, `/api/cron/cafe-articles` 사용 중)이므로 새 cron 엔드포인트 대신 기존 `/api/cron/daily/route.ts`에 gap-stats 계산을 추가한다(D-05, RESEARCH.md Open Question 1 해소).
Output: gap-stats.ts (집계+UPSERT), gap-stats.test.ts (GAP-01~GAP-05), daily/route.ts 수정
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\PROJECT.md
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\20-gap-analysis\20-00-SUMMARY.md
@C:\Users\jung\coding\bds\src\lib\data\rankings.ts
@C:\Users\jung\coding\bds\src\app\api\cron\daily\route.ts
@C:\Users\jung\coding\bds\src\__tests__\rankings.test.ts
</context>

<interfaces>
<!-- src/lib/data/rankings.ts에서 따르는 패턴 -->
```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'
// computeRankings(supabase) → { type, upserted }[]
// rankings.ts처럼 서버 전용, admin client로만 집계 함수 호출
```

<!-- src/lib/data/cron-status.ts 인터페이스 -->
```typescript
export async function markCronSuccess(supabase, sourceId: string): Promise<void>
export async function markCronFailed(supabase, sourceId: string): Promise<void>
export async function markCronStatus(supabase, sourceId: string, status: 'success'|'partial'|'failed'): Promise<void>
```

<!-- src/__tests__/rankings.test.ts mock 패턴 재사용 -->
```typescript
vi.mock('server-only', () => ({}))
function makeMockChain(result: { data: unknown; error: unknown }) { ... }
function makeMockSupabase(overrides = {}) { ... }
```

<!-- daily/route.ts 통합 위치: refresh_complex_price_stats 호출 직후, markCronStatus 직전 -->
<!-- 현재 마지막 부분: -->
```typescript
  try {
    await supabase.rpc('refresh_complex_price_stats')
  } catch (err) {
    errors.push(`refresh_complex_price_stats: ${...}`)
  }

  await markCronStatus(supabase, 'daily-batch', errors.length === 0 ? 'success' : 'partial')
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gap-stats.test.ts 작성 (RED) → gap-stats.ts 구현 (GREEN)</name>
  <files>src/__tests__/gap-stats.test.ts, src/lib/data/gap-stats.ts</files>
  <behavior>
    - GAP-01: computeRiskLevel(0) → 'safe', computeRiskLevel(39.9) → 'safe', computeRiskLevel(40) → 'caution', computeRiskLevel(60) → 'caution', computeRiskLevel(60.1) → 'danger', computeRiskLevel(100) → 'danger'
    - GAP-02: computeGapStats mock에서 RPC 결과가 빈 배열이면 upsert를 호출하지 않는다 (0 rows skipped)
    - GAP-03: computeGapStats 내부에서 deal_type='monthly'는 집계에 포함되지 않는다 (SQL RPC에 위임되므로 RPC 호출 인자만 검증)
    - GAP-04: GET /api/cron/daily에 Authorization 헤더 없으면 401 반환
    - GAP-05: GET /api/cron/daily에 올바른 CRON_SECRET 헤더 → 200 + ok 필드 포함 응답
  </behavior>
  <action>
**Step 1 — 테스트 파일 작성 (RED):**

`src/__tests__/gap-stats.test.ts`를 생성한다. rankings.test.ts의 mock 패턴을 그대로 재사용한다:

```typescript
import { describe, it, expect, vi, beforeAll } from 'vitest'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

vi.mock('server-only', () => ({}))
vi.mock('@/lib/supabase/admin', () => ({ createSupabaseAdminClient: vi.fn() }))

beforeAll(() => {
  vi.stubEnv('CRON_SECRET', 'test-cron-secret')
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', 'http://127.0.0.1:54321')
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', 'test-anon-key')
  vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', 'test-service-role-key')
})

function makeMockChain(result: { data: unknown; error: unknown }) {
  const chain: Record<string, unknown> = {}
  const terminal = vi.fn().mockResolvedValue(result)
  const methods = ['select', 'eq', 'is', 'in', 'not', 'gt', 'gte', 'order', 'limit']
  for (const m of methods) chain[m] = vi.fn().mockReturnValue(chain)
  chain['upsert'] = vi.fn().mockResolvedValue({ error: null })
  chain['single'] = terminal
  ;(chain['limit'] as ReturnType<typeof vi.fn>).mockResolvedValue(result)
  return chain
}

function makeMockSupabase(overrides: Record<string, ReturnType<typeof makeMockChain>> = {}) {
  const rpc = vi.fn().mockResolvedValue({ data: [], error: null })
  return {
    from: vi.fn((table: string) => overrides[table] ?? makeMockChain({ data: [], error: null })),
    rpc,
  } as unknown as SupabaseClient<Database> & { rpc: typeof rpc }
}
```

GAP-01 computeRiskLevel 테스트:
```typescript
describe('computeRiskLevel', () => {
  it('갭 비율 40% 미만 → safe', async () => {
    const { computeRiskLevel } = await import('@/lib/data/gap-stats')
    expect(computeRiskLevel(0)).toBe('safe')
    expect(computeRiskLevel(39.9)).toBe('safe')
  })
  it('갭 비율 40~60% → caution', async () => {
    const { computeRiskLevel } = await import('@/lib/data/gap-stats')
    expect(computeRiskLevel(40)).toBe('caution')
    expect(computeRiskLevel(60)).toBe('caution')
  })
  it('갭 비율 60% 초과 → danger', async () => {
    const { computeRiskLevel } = await import('@/lib/data/gap-stats')
    expect(computeRiskLevel(60.1)).toBe('danger')
    expect(computeRiskLevel(100)).toBe('danger')
  })
})
```

GAP-02 빈 RPC 결과 테스트:
```typescript
describe('computeGapStats', () => {
  it('RPC 결과 빈 배열이면 upsert 호출 없음', async () => {
    const { computeGapStats } = await import('@/lib/data/gap-stats')
    const mockSupabase = makeMockSupabase()
    ;(mockSupabase as unknown as { rpc: ReturnType<typeof vi.fn> }).rpc.mockResolvedValue({ data: [], error: null })
    const chainSpy = makeMockChain({ data: null, error: null })
    const upsertSpy = chainSpy['upsert'] as ReturnType<typeof vi.fn>
    const result = await computeGapStats(mockSupabase as unknown as SupabaseClient<Database>)
    expect(upsertSpy).not.toHaveBeenCalled()
    expect(result.complexesUpdated).toBe(0)
  })
})
```

GAP-04, GAP-05 cron 테스트 (rankings.test.ts 패턴 준용):
```typescript
describe('GET /api/cron/daily — gap-stats 통합', () => {
  it('Authorization 없음 → 401', async () => {
    const { GET } = await import('@/app/api/cron/daily/route')
    const res = await GET(new Request('http://localhost/api/cron/daily'))
    expect(res.status).toBe(401)
  })
  it('올바른 CRON_SECRET → 200 + ok 필드', async () => {
    const { createSupabaseAdminClient } = await import('@/lib/supabase/admin')
    vi.mocked(createSupabaseAdminClient).mockReturnValue(makeMockSupabase() as ReturnType<typeof createSupabaseAdminClient>)
    const { GET } = await import('@/app/api/cron/daily/route')
    const res = await GET(new Request('http://localhost/api/cron/daily', {
      headers: { authorization: 'Bearer test-cron-secret' },
    }))
    expect(res.status).toBe(200)
    const body = await res.json() as { ok: boolean }
    expect(body).toHaveProperty('ok')
  })
})
```

**Step 2 — `src/lib/data/gap-stats.ts` 구현 (GREEN):**

```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

const WINDOW_MONTHS = 12

export type RiskLevel = 'safe' | 'caution' | 'danger'

/** D-02: 갭 비율 기준 위험도 분류 */
export function computeRiskLevel(gapRatio: number): RiskLevel {
  if (gapRatio < 40) return 'safe'
  if (gapRatio <= 60) return 'caution'
  return 'danger'
}

export interface GapStatsRow {
  complexId: string
  medianSalePrice: number
  medianJeonsePrice: number
  gapAmount: number
  gapRatio: number
  jeonseRatio: number
  riskLevel: RiskLevel
  saleCount: number
  jeonseCount: number
}

interface ComputeGapStatsResult {
  complexesUpdated: number
  complexesSkipped: number
  errors: string[]
}

/**
 * compute_gap_stats SQL RPC를 호출해 갭 통계를 계산하고
 * complex_gap_stats 테이블에 UPSERT한다.
 * createSupabaseAdminClient()로 생성한 supabase만 전달할 것.
 */
export async function computeGapStats(
  supabase: SupabaseClient<Database>,
): Promise<ComputeGapStatsResult> {
  const errors: string[] = []

  // SQL RPC로 전체 집계 (DB에서 PERCENTILE_CONT 계산)
  const { data: rpcRows, error: rpcError } = await supabase.rpc('compute_gap_stats', {
    p_window_months: WINDOW_MONTHS,
  })

  if (rpcError) {
    return { complexesUpdated: 0, complexesSkipped: 0, errors: [`compute_gap_stats RPC: ${rpcError.message}`] }
  }

  const rows = (rpcRows ?? []) as Array<{
    complex_id: string
    median_sale_price: number
    median_jeonse_price: number
    gap_amount: number
    gap_ratio: number
    jeonse_ratio: number
    sale_count: number
    jeonse_count: number
  }>

  if (rows.length === 0) {
    return { complexesUpdated: 0, complexesSkipped: 0, errors: [] }
  }

  const computedAt = new Date().toISOString()
  const upsertRows = rows.map(row => ({
    complex_id:           row.complex_id,
    median_sale_price:    row.median_sale_price,
    median_jeonse_price:  row.median_jeonse_price,
    gap_amount:           row.gap_amount,
    gap_ratio:            row.gap_ratio,
    jeonse_ratio:         row.jeonse_ratio,
    risk_level:           computeRiskLevel(Number(row.gap_ratio)),
    sale_count:           row.sale_count,
    jeonse_count:         row.jeonse_count,
    window_months:        WINDOW_MONTHS,
    computed_at:          computedAt,
  }))

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error: upsertError } = await (supabase as any)
    .from('complex_gap_stats')
    .upsert(upsertRows, { onConflict: 'complex_id', ignoreDuplicates: false })

  if (upsertError) {
    errors.push(`complex_gap_stats UPSERT: ${upsertError.message}`)
    return { complexesUpdated: 0, complexesSkipped: rows.length, errors }
  }

  return { complexesUpdated: upsertRows.length, complexesSkipped: 0, errors }
}
```

**Step 3 — `src/app/api/cron/daily/route.ts` 수정:**

파일 상단 import에 추가:
```typescript
import { computeGapStats } from '@/lib/data/gap-stats'
import { markCronSuccess, markCronFailed } from '@/lib/data/cron-status'
```

(markCronStatus는 이미 import되어 있으므로 markCronSuccess/markCronFailed 추가가 필요 없을 수 있음 — 기존 import 확인)

`refresh_complex_price_stats` try-catch 블록 직후, `markCronStatus(supabase, 'daily-batch', ...)` 호출 바로 앞에 삽입:

```typescript
  // ── 갭투자 통계 재계산 (GAP-D05) ──────────────────────────────────────────
  let gapUpdated = 0
  try {
    const gapResult = await computeGapStats(supabase)
    gapUpdated = gapResult.complexesUpdated
    if (gapResult.errors.length > 0) {
      errors.push(...gapResult.errors)
      await markCronFailed(supabase, 'gap-stats')
    } else {
      await markCronSuccess(supabase, 'gap-stats')
    }
  } catch (err) {
    errors.push(`computeGapStats: ${err instanceof Error ? err.message : String(err)}`)
    await markCronFailed(supabase, 'gap-stats').catch(() => {/* gap-stats source 미등록이면 무시 */})
  }
```

Response.json() 반환 객체에 `gapUpdated` 필드 추가:
```typescript
  return Response.json({
    ok: errors.length === 0,
    totalUpserted,
    // ... 기존 필드들 ...
    gapUpdated,
    errors,
  })
```

주의: markCronFailed를 이미 임포트했는지 확인한다. `cron-status.ts`에서 `markCronSuccess`, `markCronFailed`를 named export로 제공하므로 필요 시 import 추가.
  </action>
  <verify>
    <automated>npm run test -- gap-stats</automated>
  </verify>
  <done>
    - GAP-01~GAP-05 테스트 모두 PASS
    - src/lib/data/gap-stats.ts: computeRiskLevel, computeGapStats, GapStatsRow, RiskLevel 모두 export
    - src/app/api/cron/daily/route.ts: computeGapStats import 및 호출 포함
    - npm run lint 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron caller→/api/cron/daily | CRON_SECRET Bearer 토큰으로 인증된 요청만 허용 |
| /api/cron/daily→DB | createSupabaseAdminClient() (서비스 롤) 사용 — RLS 우회 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | /api/cron/daily 무단 호출 | mitigate | 기존 CRON_SECRET 헤더 검증 패턴 유지 (daily/route.ts 첫 줄) |
| T-20-05 | Denial of Service | compute_gap_stats RPC 타임아웃 | accept | daily 배치 내 try-catch로 격리; 실패 시 다른 배치 작업 영향 없음 |
| T-20-06 | Tampering | computeRiskLevel 경계값 오류 | mitigate | GAP-01 단위 테스트가 40.0/60.0 경계값 명시적 검증 |
</threat_model>

<verification>
1. 테스트 통과: `npm run test -- gap-stats` — GAP-01 ~ GAP-05 모두 PASS
2. TypeScript 컴파일: `npm run lint` 오류 없음
3. 함수 export 확인: gap-stats.ts에 `export function computeRiskLevel`, `export async function computeGapStats`, `export type RiskLevel`, `export interface GapStatsRow` 포함
4. daily route 통합 확인: `grep -v "^//" src/app/api/cron/daily/route.ts | grep -c "computeGapStats"`
5. markCronSuccess('gap-stats') 호출 확인: `grep -c "gap-stats" src/app/api/cron/daily/route.ts`
</verification>

<success_criteria>
- src/__tests__/gap-stats.test.ts: 5개 테스트(GAP-01~GAP-05) 모두 PASS
- src/lib/data/gap-stats.ts: server-only 임포트, computeRiskLevel(gapRatio), computeGapStats(supabase) 구현
- computeRiskLevel: 40 미만=safe, 40~60=caution, 60 초과=danger
- computeGapStats: RPC 호출 → UPSERT, onConflict='complex_id'
- daily/route.ts: computeGapStats 통합 + gap-stats cron status 갱신
- npm run lint && npm run test 통과
</success_criteria>

<output>
완료 후 `.planning/phases/20-gap-analysis/20-01-SUMMARY.md` 생성.
</output>
