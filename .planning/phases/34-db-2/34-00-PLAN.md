---
phase: 34-db-2
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/seed.ts
  - src/__tests__/seed-region.test.ts
autonomous: true
requirements:
  - REGION-12
must_haves:
  truths:
    - "regions 테이블에 부산광역시 16개 구·군이 존재하고 is_active=true이며 gu 값이 채워져 있다 (창원과 동일한 '구 있는' 패턴)"
    - "부산 16개 sgg_code가 국토부 실거래가 API에서 유효한 코드로 확인된다 (빈 응답만 반복되는 오탐 코드가 아님)"
    - "seed-region.test.ts가 부산 확장(총 38행) 기준으로 갱신되어 회귀를 방지한다"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "REGIONS 배열에 부산 16개 구·군 행 추가"
      contains: "26350"
    - path: "src/__tests__/seed-region.test.ts"
      provides: "부산 16개 구 전용 assertion (count>=38, gu NOT NULL 16건)"
      contains: "BUSAN_EXPANSION_SGG_CODES"
  key_links:
    - from: "scripts/seed.ts"
      to: "regions 테이블"
      via: "supabase.from('regions').upsert(REGIONS, { onConflict: 'sgg_code' })"
      pattern: "upsert.*regions"
---

<objective>
`regions` 테이블에 부산광역시 16개 구·군을 시딩하고, 시딩 직후 국토부 API로 신규 코드의 유효성을 단발 검증한다. 이 phase의 모든 후속 plan(34-01~34-10)이 의존하는 지역 마스터 데이터를 Wave 0에서 확정한다. Phase 33에서 이미 범용화된 `getActiveSggCodes()`/`getActiveCityNames()`/`getActiveRegionAddrs()` 공용 헬퍼(`src/lib/data/regions.ts`)는 그대로 재사용하므로 코드 변경 없이 regions 시딩만으로 대부분의 동적 조회 파이프라인이 자동으로 부산을 포함한다.

Purpose: 부산 확장의 단일 진입점(지역 마스터 데이터) 확정 + 잘못된 법정동코드로 대규모 백필(34-06)이 낭비되는 것을 방지.
Output: regions 테이블 38행(기존 22 + 부산 16), 갱신된 seed-region 통합 테스트, 부산 코드 유효성 검증 로그.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-00-PLAN.md

<interfaces>
<!-- scripts/seed.ts 현재 REGIONS 배열 끝(라인 18~34, 경남 22행). 이 배열 끝에 부산 16행 추가 -->
```typescript
const REGIONS = [
  { sgg_code: '48121', sgg_name: '창원시 의창구', si: '창원시', gu: '의창구' },
  // ... 경남 22행 (기존, 유지) ...
  { sgg_code: '48890', sgg_name: '합천군', si: '합천군', gu: null },
  // 부산 16개 추가 (구 있음 — 창원과 동일 패턴, gu 값 채움)
] as const
```

<!-- regions 테이블 컬럼 타입 (src/types/database.ts) -->
```typescript
regions.Row = { created_at: string; gu: string | null; is_active: boolean; sgg_code: string; sgg_name: string; si: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 부산 16개 구·군 regions 시딩</name>
  <files>scripts/seed.ts</files>
  <read_first>scripts/seed.ts</read_first>
  <action>
scripts/seed.ts의 `REGIONS` 배열 끝(현재 라인 34, `{ sgg_code: '48890', ... }` 다음)에 아래 16개 부산 행을 추가한다. 형식은 기존 경남 행과 동일하게 유지하고 `as const`를 유지한다. **부산은 창원처럼 '구 있는' 광역시이므로 gu 값을 반드시 채운다** (경남 확장 16개가 gu=null이었던 것과 반대):

```typescript
  { sgg_code: '26110', sgg_name: '중구',     si: '부산광역시', gu: '중구' },
  { sgg_code: '26140', sgg_name: '서구',     si: '부산광역시', gu: '서구' },
  { sgg_code: '26170', sgg_name: '동구',     si: '부산광역시', gu: '동구' },
  { sgg_code: '26200', sgg_name: '영도구',   si: '부산광역시', gu: '영도구' },
  { sgg_code: '26230', sgg_name: '부산진구', si: '부산광역시', gu: '부산진구' },
  { sgg_code: '26260', sgg_name: '동래구',   si: '부산광역시', gu: '동래구' },
  { sgg_code: '26290', sgg_name: '남구',     si: '부산광역시', gu: '남구' },
  { sgg_code: '26320', sgg_name: '북구',     si: '부산광역시', gu: '북구' },
  { sgg_code: '26350', sgg_name: '해운대구', si: '부산광역시', gu: '해운대구' },
  { sgg_code: '26380', sgg_name: '사하구',   si: '부산광역시', gu: '사하구' },
  { sgg_code: '26410', sgg_name: '금정구',   si: '부산광역시', gu: '금정구' },
  { sgg_code: '26440', sgg_name: '강서구',   si: '부산광역시', gu: '강서구' },
  { sgg_code: '26470', sgg_name: '연제구',   si: '부산광역시', gu: '연제구' },
  { sgg_code: '26500', sgg_name: '수영구',   si: '부산광역시', gu: '수영구' },
  { sgg_code: '26530', sgg_name: '사상구',   si: '부산광역시', gu: '사상구' },
  { sgg_code: '26710', sgg_name: '기장군',   si: '부산광역시', gu: '기장군' },
```

⚠ 이 16개 법정동코드는 RESEARCH.md에서 MEDIUM confidence(2개 독립 웹소스 교차검증, 정부 원본 표 미확인). 스키마 제약(`sgg_code ~ '^\d{5}$'`)은 형식만 검증하므로 실제 유효성은 Task 2에서 API 단발 검증으로 확인한다.

수정 후 `npm run db:seed` 실행 (내부적으로 `tsx scripts/seed.ts` → `regions` upsert, `onConflict: 'sgg_code'`라 기존 22행은 유지되고 부산 16행만 추가됨 — 순수 데이터 upsert, 스키마 변경 없으므로 마이그레이션 불필요).
  </action>
  <acceptance_criteria>
    - `grep -c "sgg_code: '26" scripts/seed.ts` == 16
    - `grep -c "sgg_code:" scripts/seed.ts` == 38
    - `npm run db:seed` 실행 로그에 `regions:` 카운트가 38 이상으로 출력됨
    - 부산 행 16개 모두 `si: '부산광역시'`이고 `gu`가 non-null (grep으로 `gu: null`이 부산 블록에 없음 확인)
  </acceptance_criteria>
  <verify>
    <automated>npm run db:seed 2>&1 | grep "regions:"</automated>
  </verify>
  <done>regions 테이블에 38행이 존재하고 부산 16행이 is_active=true, gu non-null이다.</done>
</task>

<task type="auto">
  <name>Task 2: 부산 16개 법정동코드 단발 검증 (Pitfall 4 방어)</name>
  <files>없음 (검증 전용, 코드 변경 없음)</files>
  <read_first>scripts/backfill-realprice.ts</read_first>
  <action>
부산 16개 sgg_code가 실제로 유효한 국토부 LAWD_CD인지, 10년 전체 백필(34-06)을 트리거하기 전에 최근 1개월치만으로 확인한다. 국토부 API는 잘못된 코드에도 200 OK + 빈 items를 반환할 수 있으므로(Pitfall 4), HTTP 에러가 아니라 `ingest_runs.rows_upserted` 실측으로 판단한다.

```bash
npx tsx scripts/backfill-realprice.ts \
  --sgg=26110,26140,26170,26200,26230,26260,26290,26320,26350,26380,26410,26440,26470,26500,26530,26710 \
  --from=202606 --to=202606
```

실행 후 `ingest_runs`에서 확인 (Supabase MCP execute_sql 또는 admin 클라이언트):
```sql
select sgg_code, status, rows_upserted from ingest_runs
where source_id in ('molit_trade','molit_villa_trade')
  and year_month = '202606'
  and sgg_code like '26%'
order by sgg_code;
```

판단 기준:
- 부산은 전 지역이 도심 밀집지이므로 **거의 모든 구에서 rows_upserted > 0**이 기대됨 (경남 농어촌 군 지역과 달리 0건이 드묾)
- 특히 해운대구(26350)·부산진구(26230)·사하구(26380)·북구(26320)는 인구 밀집지라 반드시 > 0
- **전체 16개 코드에서 전부 0건이면 시스템적 오류(코드 형식·API 파라미터)일 가능성이 높음 — Task 1로 돌아가 land.koreacharts.com/land/list/<code>.html로 코드를 재검증한다**

코드를 수정하지 않는다. 검증 결과는 실행 로그로 남기고 34-06 백필 판단 근거로 사용한다.
  </action>
  <acceptance_criteria>
    - `ingest_runs`에 부산 16개 sgg_code × 202606 조합의 status='success' 레코드가 존재
    - 해운대구(26350)·부산진구(26230)·사하구(26380) 중 최소 2곳이 rows_upserted > 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/backfill-realprice.ts --sgg=26350,26230,26380 --from=202606 --to=202606 --apt 2>&1 | tail -5</automated>
  </verify>
  <done>부산 16개 sgg_code에 대한 최근 1개월 백필이 실행되었고, 주요 구에서 0건이 아닌 실거래 데이터가 확인되어 법정동코드가 유효함이 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: seed-region.test.ts 부산 확장 assertion 추가</name>
  <files>src/__tests__/seed-region.test.ts</files>
  <read_first>src/__tests__/seed-region.test.ts</read_first>
  <behavior>
    - `count >= 38` (기존 `count >= 22` 하한을 38로 상향 — upsert 재확장 대비 `>=` 유지)
    - `BUSAN_EXPANSION_SGG_CODES` 16개 신규 상수로 `.in()` 조회 시 모든 행 `gu !== null` (경남 확장 케이스가 `gu === null`을 검증한 것과 반대 — 부산은 전부 구가 있음)
    - 부산 16개 행 모두 `si === '부산광역시'`
  </behavior>
  <action>
`src/__tests__/seed-region.test.ts`를 읽고, 기존 경남 확장 테스트 구조를 그대로 확장한다:

1. 파일 상단에 신규 상수 추가:
```typescript
const BUSAN_EXPANSION_SGG_CODES = [
  '26110','26140','26170','26200','26230','26260','26290','26320',
  '26350','26380','26410','26440','26470','26500','26530','26710',
] as const
```

2. 기존 "경남 전체 22개 시군구 존재" 테스트의 `count >= 22`를 `count >= 38`로 상향한다 (부산 16 추가 반영, `>=` 유지).

3. 신규 테스트 추가 (SKEY 있을 때만 실행되는 integration describe 블록 안):
```typescript
it('regions: 부산 16개 구가 존재하고 모두 gu가 채워져 있다 (구 있는 광역시 패턴)', async () => {
  const { data, error } = await admin
    .from('regions')
    .select('sgg_code, si, gu')
    .in('sgg_code', [...BUSAN_EXPANSION_SGG_CODES])
  expect(error).toBeNull()
  expect(data?.length).toBe(16)
  for (const row of data ?? []) {
    expect(row.si).toBe('부산광역시')
    expect(row.gu).not.toBeNull()  // 부산은 전부 구가 있음 — 경남 무구 시군과 반대
  }
})
```
  </action>
  <acceptance_criteria>
    - `grep -c "BUSAN_EXPANSION_SGG_CODES" src/__tests__/seed-region.test.ts` >= 2
    - `grep -c "toBeGreaterThanOrEqual(22)" src/__tests__/seed-region.test.ts` == 0 (38로 상향됨)
    - `grep -c "toBeGreaterThanOrEqual(38)" src/__tests__/seed-region.test.ts` >= 1
    - `npx vitest run src/__tests__/seed-region.test.ts` 통과 (SKEY 없는 환경에서는 integration describe 스킵되어 통과)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/seed-region.test.ts</automated>
  </verify>
  <done>seed-region.test.ts가 부산 확장(38행, gu non-null 16건) 기준으로 갱신되고 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 인자(`--sgg=`) → 국토부 API 파라미터 | 로컬/CI 실행자만 트리거, 사용자 입력 아님 |
| regions.sgg_code → 하위 plan들의 allowlist 소스 | DB 값이 이후 모든 지역 필터링의 신뢰 기준점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | scripts/seed.ts REGIONS 배열 | accept | git 관리 파일, service_role 키로만 실행, RLS write 정책은 service_role 전용 |
| T-34-02 | Information Disclosure | regions public read | accept | 지역 코드는 공개 데이터, 민감정보 아님. 부산 코드도 `sgg_code ~ '^\d{5}$'` CHECK 제약 통과 (V5) |
</threat_model>

<verification>
```bash
npm run db:seed 2>&1 | grep "regions:"
npx vitest run src/__tests__/seed-region.test.ts
```
</verification>

<success_criteria>
1. regions 테이블에 38개 시군구가 존재하고 부산 16개는 is_active=true, gu non-null
2. 부산 주요 구(해운대·부산진·사하 등)에서 국토부 API 실거래 데이터가 확인됨
3. seed-region.test.ts가 부산 확장을 반영하여 갱신됨
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-00-SUMMARY.md` 생성.
</output>
</content>
</invoke>
