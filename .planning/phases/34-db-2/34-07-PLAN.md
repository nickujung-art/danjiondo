---
phase: 34-db-2
plan: "07"
type: execute
wave: 3
depends_on: ["34-05", "34-01"]
files_modified:
  - scripts/collect-facility-edu.ts
autonomous: true
requirements:
  - REGION-19
must_haves:
  truths:
    - "부산 단지에 관리비(K-apt)·학군·POI enrichment 파이프라인이 실행되어 데이터가 채워진다"
    - "각 enrichment 스크립트의 complexes 조회가 PostgREST 1,000행 캡에 걸리지 않고(페이지네이션 확인) 부산 전체 단지를 처리한다"
    - "처리 건수 로그가 예상 부산 단지 수와 대조되어 조용한 누락이 없음이 확인된다 (Pitfall 4)"
  artifacts:
    - path: "scripts/collect-facility-edu.ts"
      provides: "complexes 조회 페이지네이션 확인/보강 (1,000행 캡 방어)"
      contains: "range"
  key_links:
    - from: "enrichment 스크립트"
      to: "complexes 테이블 (부산)"
      via: "regions 동적 조회 → 좌표 기반 매칭 → facility/management_cost upsert"
      pattern: "is_active|range"
---

<objective>
34-05(지오코딩) 완료 후 부산 단지에 기존 enrichment 파이프라인(관리비·학군·POI)을 실행한다. 34-01에서 이미 `collect-school-stats.ts`/`fetch-sports-facilities.ts`를 regions 동적 조회로 전환했으므로 부산이 자동 포함된다. **핵심 리스크는 PostgREST 1,000행 캡(Pitfall 4)** — 부산 추가로 `complexes` 총 행 수가 1,000을 확실히 초과하므로, 각 스크립트의 단지 조회 쿼리에 `.range()` 페이지네이션이 있는지 실행 전 재확인하고, 처리 건수 로그를 예상치와 대조한다.

Purpose: 부산 단지의 데이터 완성도를 경남 수준으로 끌어올리고, 1,000행 캡으로 인한 조용한 누락을 예방.
Output: 부산 단지 관리비/학군/POI 데이터 채움, 페이지네이션 검증 로그.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-PATTERNS.md
@C:\Users\jung\coding\bds\scripts\collect-school-stats.ts
@C:\Users\jung\coding\bds\scripts\fetch-sports-facilities.ts
@C:\Users\jung\coding\bds\scripts\collect-facility-edu.ts

<interfaces>
<!-- PostgREST 1,000행 캡 페이지네이션 템플릿 (검증된 패턴, 커밋 11d5952 / kapt-code-lookup.ts:52-64) -->
```typescript
const PAGE = 1000
for (let offset = 0; ; offset += PAGE) {
  const { data, error } = await supabase.from('complexes').select('id, lat, lng')
    .not('lat', 'is', null).not('lng', 'is', null).range(offset, offset + PAGE - 1)
  if (error) { console.error(error.message); process.exit(1) }
  if (!data || data.length === 0) break
  rows.push(...data)
  if (data.length < PAGE) break
}
```
<!-- 페이지네이션 이미 존재(확인만): kapt-code-lookup.ts, import-management-cost.ts, kapt-facility-enrich.ts (Phase 33에서 수정 완료) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: enrichment 스크립트 PostgREST 1,000행 캡 재확인 + collect-facility-edu.ts 보강</name>
  <files>scripts/collect-facility-edu.ts</files>
  <read_first>scripts/collect-facility-edu.ts, scripts/kapt-facility-enrich.ts</read_first>
  <action>
부산 추가로 `complexes` 총 행 수가 1,000을 확실히 넘으므로, enrichment 실행 전 각 스크립트의 `complexes` 조회 쿼리에 페이지네이션이 있는지 확인한다.

**1. 이미 페이지네이션 있음(확인만, 수정 불필요)** — Phase 33에서 수정 완료:
- `scripts/kapt-code-lookup.ts` (라인 52-64, `.range()` 있음)
- `scripts/import-management-cost.ts` (라인 436-450, `.range()` 있음)
- `scripts/kapt-facility-enrich.ts` (라인 74-81, `.range()` 있음)

**2. `scripts/collect-facility-edu.ts` 페이지네이션 확인 + 보강** — 이 스크립트는 페이지네이션 패턴이 확인되지 않았다. 파일을 읽고 `complexes` 전체를 조회하는 지점을 찾아, `.range()`/`while` 페이지네이션이 없거나 `.limit()`으로만 잘려 있으면 위 인터페이스의 템플릿으로 교체한다. **`fetch-sports-facilities.ts`(34-01에서 동적 전환)의 페이지네이션 루프도 유지됐는지 확인**한다.
- 페이지네이션이 이미 있으면 코드 변경 없이 확인만 하고 SUMMARY에 "collect-facility-edu.ts 페이지네이션 확인됨"으로 기록.
- 없으면 보강 후 `npm run lint` 통과 확인.
  </action>
  <acceptance_criteria>
    - `grep -c "\.range(" scripts/collect-facility-edu.ts` >= 1 (페이지네이션 존재 또는 보강됨)
    - `npm run lint` 통과
    - SUMMARY.md에 4개 enrichment 스크립트(kapt-code-lookup/import-management-cost/kapt-facility-enrich/collect-facility-edu)의 페이지네이션 상태표 기록
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\.range(" scripts/collect-facility-edu.ts</automated>
  </verify>
  <done>모든 enrichment 스크립트의 complexes 조회가 1,000행 캡을 넘어 전체 부산 단지를 처리하도록 페이지네이션이 확인·보강된다.</done>
</task>

<task type="auto">
  <name>Task 2: 부산 관리비(K-apt) enrichment 실행</name>
  <files>없음 (기존 스크립트 실행)</files>
  <read_first>scripts/import-management-cost.ts, scripts/kapt-facility-enrich.ts</read_first>
  <action>
부산 단지 관리비 및 K-apt 부대시설 enrichment를 실행한다:
```bash
npx tsx --env-file=.env.local scripts/kapt-facility-enrich.ts
npx tsx --env-file=.env.local scripts/import-management-cost.ts
```
실행 후 **처리 건수를 예상치와 대조**(Pitfall 4 — 로그 건수가 1,000의 배수 근처에서 끊기면 캡 의심):
```sql
select count(*) as busan_total from complexes where sgg_code like '26%';
-- 관리비/시설 채워진 부산 단지 수와 비교 (예: management_cost_kapt, facility_kapt 조인)
```
로그 처리 건수가 부산 전체 단지 수에 부합하는지 확인하고 SUMMARY에 기록. 1,000행 배수 근처에서 끊겼으면 Task 1로 돌아가 페이지네이션을 재점검한다.
  </action>
  <acceptance_criteria>
    - 실행 로그의 처리 건수가 `select count(*) from complexes where sgg_code like '26%'` 예상치에 부합 (1,000 배수에서 끊기지 않음)
    - 부산 단지 중 관리비/시설 데이터가 채워진 행이 존재 (실행 후 SQL 카운트 > 0)
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --env-file=.env.local scripts/kapt-facility-enrich.ts 2>&1 | tail -15</automated>
  </verify>
  <done>부산 단지 관리비/K-apt 시설 데이터가 채워지고 처리 건수가 예상 단지 수에 부합한다.</done>
</task>

<task type="auto">
  <name>Task 3: 부산 학군·POI enrichment 실행</name>
  <files>없음 (기존 스크립트 실행, 34-01에서 동적 전환된 스크립트)</files>
  <read_first>scripts/collect-school-stats.ts, scripts/fetch-sports-facilities.ts</read_first>
  <action>
34-01에서 regions 동적 조회로 전환된 학군/POI 스크립트를 실행한다(부산 자동 포함). 좌표 기반 매칭이 필요하므로 34-05 지오코딩 완료가 전제:
```bash
npx tsx --env-file=.env.local scripts/collect-school-stats.ts
npx tsx --env-file=.env.local scripts/fetch-sports-facilities.ts
npx tsx --env-file=.env.local scripts/collect-facility-edu.ts   # POI(어린이집/유치원/학원 등)
```
- `collect-school-stats.ts`는 sidoCode를 각 지역 sgg_code 앞 2자리(부산=26)로 동적 계산하도록 34-01에서 전환됨 — 부산 학교알리미 데이터가 수집된다.
- 각 스크립트 실행 후 처리 건수를 예상 부산 단지/학교 수와 대조(Pitfall 4).
- ⚠ 부산 학군 랭킹의 구별 라벨은 34-04에서 확인한 대로 `school_ranking` RPC가 gu=NULL을 반환한다(범위 밖, 회귀 테스트로만 고정) — enrichment 데이터 자체는 정상 수집되며, 랭킹 리스트도 정상 반환된다(구 세부 구분만 미표시).
  </action>
  <acceptance_criteria>
    - `collect-school-stats.ts` 실행 로그에 부산(sidoCode 26) 처리 항목이 나타남
    - `fetch-sports-facilities.ts` 주소 필터에 '부산광역시'가 포함되어 부산 체육시설이 매칭됨 (로그 확인)
    - 부산 단지 중 학군/POI 데이터가 채워진 행이 존재 (실행 후 SQL 카운트 > 0)
    - 처리 건수가 1,000 배수에서 끊기지 않음
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --env-file=.env.local scripts/collect-school-stats.ts 2>&1 | tail -15</automated>
  </verify>
  <done>부산 단지 학군/POI 데이터가 채워지고 처리 건수가 예상치에 부합한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 API(학교알리미·K-apt·행안부 체육도장) → 배치 스크립트 | 서버 전용 시크릿, src/services/ 어댑터 경유(CLAUDE.md 준수) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-15 | Information Disclosure (조용한 데이터 누락) | PostgREST 1,000행 캡 | mitigate | 실행 전 페이지네이션 확인 + 처리 건수를 예상 부산 단지 수와 대조, 1,000 배수 끊김 감지 |
| T-34-16 | Tampering | enrichment upsert | accept | service_role 키 필요, 좌표+이름 복합 매칭(Golden Record 원칙), 사용자 입력 경로 없음 |
</threat_model>

<verification>
```bash
grep -c "\.range(" scripts/collect-facility-edu.ts
npx tsx --env-file=.env.local scripts/collect-school-stats.ts 2>&1 | tail -15
```
</verification>

<success_criteria>
1. 부산 단지 관리비·학군·POI 데이터가 채워짐
2. 모든 enrichment 스크립트가 1,000행 캡 없이 전체 부산 단지 처리
3. 처리 건수가 예상 단지 수와 대조되어 조용한 누락 없음 확인
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-07-SUMMARY.md` 생성. enrichment 스크립트 페이지네이션 상태표 + 처리 건수 대조 결과 포함.
</output>
</content>
