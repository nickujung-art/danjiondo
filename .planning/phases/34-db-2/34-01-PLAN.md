---
phase: 34-db-2
plan: "01"
type: execute
wave: 1
depends_on: ["34-00"]
files_modified:
  - src/app/admin/region-expansion/page.tsx
  - scripts/collect-school-stats.ts
  - scripts/fetch-sports-facilities.ts
autonomous: true
requirements:
  - REGION-13
must_haves:
  truths:
    - "Phase 33 종료 후 신규로 추가된 하드코딩 지역 배열이 재스윕으로 식별되고 부산을 포함하도록 전환된다"
    - "enrichment 스크립트(collect-school-stats/fetch-sports-facilities)의 경남 전용 하드코딩 배열이 regions 동적 조회로 전환되어 부산이 자동 포함된다"
    - "admin/region-expansion 대시보드가 부산 확장 진행 상황을 추적하도록 갱신된다"
  artifacts:
    - path: "scripts/collect-school-stats.ts"
      provides: "GYEONGNAM_SGG 정적 배열 → regions 동적 조회 전환"
      contains: "is_active"
    - path: "scripts/fetch-sports-facilities.ts"
      provides: "ADDRESS_KEYWORDS 정적 배열 → regions 동적 조회 전환"
      contains: "is_active"
    - path: "src/app/admin/region-expansion/page.tsx"
      provides: "NEW_CODES/OLD_CODES를 부산 추적 기준으로 갱신"
      contains: "26350"
  key_links:
    - from: "scripts/collect-school-stats.ts"
      to: "regions 테이블"
      via: "supabase.from('regions').select().eq('is_active', true)"
      pattern: "is_active"
---

<objective>
Phase 33에서 20건 이상 발견됐던 "하드코딩 지역 배열" 버그 클래스는 Phase 33 종료 후에도 신규로 재발했다(RESEARCH.md Runtime State Inventory: `collect-school-stats.ts`의 `GYEONGNAM_SGG`, `fetch-sports-facilities.ts`의 `ADDRESS_KEYWORDS`, `admin/region-expansion`의 `NEW_CODES`/`OLD_CODES` — 모두 커밋 `11d5952`/`61d8623`로 추가됨). 이번 plan은 (1) 표준 grep 패턴 + 리터럴 코드값으로 저장소 전체를 재스윕하고, (2) 발견된 경남 전용 하드코딩을 regions 동적 조회로 전환하며, (3) admin 진행 대시보드를 부산 추적 기준으로 갱신한다.

Purpose: 부산 착수 전 하드코딩 재발분을 제거해 "특정 스크립트/페이지에서만 부산이 누락되는" 조용한 실패를 예방.
Output: enrichment 스크립트 2개 동적 전환, admin 대시보드 부산 추적 전환, grep 재스윕 결과 로그.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-PATTERNS.md
@C:\Users\jung\coding\bds\scripts\collect-school-stats.ts
@C:\Users\jung\coding\bds\scripts\fetch-sports-facilities.ts
@C:\Users\jung\coding\bds\src\app\admin\region-expansion\page.tsx

<interfaces>
<!-- regions 동적 조회 헬퍼 (src/lib/data/regions.ts, 변경 없이 재사용) — 단, 스크립트는 자체 supabase 클라이언트가 있으므로 인라인 쿼리가 더 단순함 -->
```typescript
// scripts/seed-complexes.ts:32-39 의 검증된 인라인 패턴 (그대로 복사)
async function getActiveRegions(): Promise<Array<{ sgg_code: string; si: string; gu: string | null }>> {
  const { data, error } = await supabase
    .from('regions').select('sgg_code, si, gu').eq('is_active', true).order('sgg_code')
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  return data ?? []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 저장소 전체 하드코딩 지역 배열 재스윕 + admin 대시보드 부산 추적 전환</name>
  <files>src/app/admin/region-expansion/page.tsx</files>
  <read_first>src/app/admin/region-expansion/page.tsx</read_first>
  <action>
**1. 3-pass grep 재스윕 실행** (결과를 SUMMARY.md에 기록):
```bash
# 1차 — 표준 변수명
grep -rn "ALLOWED_SGG_CODES\|ACTIVE_SGG_CODES\|TARGET_SGG\|VALID_SGG_CODES\|LAWD_CODES\|offiSggCodes" src/ scripts/
# 2차 — Phase 33에서 grep 사각지대였던 변수명
grep -rn "CHANGWON_GU_MAP\|SGG_TO_ADDR\|GYEONGNAM_SGG\|ADDRESS_KEYWORDS\|NEW_CODES\|OLD_CODES" src/ scripts/
# 3차 — 리터럴 코드값 (신규 변수명은 grep 1/2가 못 잡음)
grep -rn "'481[0-9][0-9]'\|'482[0-9][0-9]'" src/ scripts/   # 경남 코드 (재확인)
grep -rn "'26[0-9][0-9][0-9]'" src/ scripts/                 # 부산 코드 (시딩 후 신규 유입 여부)
```
발견된 각 항목을 (a) 이미 동적화됨 / (b) 이 plan에서 처리 / (c) 다른 plan에서 처리(collect-school-stats·fetch-sports-facilities는 Task 2, UI 라벨은 34-02, KAPT는 34-03) / (d) 프로덕션 경로 아님 으로 분류해 SUMMARY.md에 표로 남긴다. **분류 (b)에 해당하는 새로운 미분류 하드코딩이 있으면 이 Task에서 regions 동적 조회로 전환한다.**

**2. `src/app/admin/region-expansion/page.tsx` 부산 추적 전환** (Open Question 3 결정 — 이 대시보드는 확장 진행 추적용으로 실측 유용하므로 삭제하지 않고 부산 추적으로 갱신한다):
- `OLD_CODES`를 경남 전체 22개(창원 5구 + 김해 + 경남 확장 16)로 갱신:
```typescript
const OLD_CODES = [
  '48121','48123','48125','48127','48129','48250',
  '48170','48220','48240','48270','48310','48330',
  '48720','48730','48740','48820','48840','48850','48860','48870','48880','48890',
]
```
- `NEW_CODES`를 부산 16개로 교체:
```typescript
const NEW_CODES = [
  '26110','26140','26170','26200','26230','26260','26290','26320',
  '26350','26380','26410','26440','26470','26500','26530','26710',
]
```
- 파일 상단 주석의 "Phase 33 완료 후 삭제 예정" 및 metadata title `'Phase 33'`을 `'Phase 34 (부산)'`로 갱신.
- `WAVE_PLANS` 배열은 Phase 33 항목이므로 부산 진행 상황을 반영하도록 갱신하거나(선택) 최소한 title/주석만 갱신 — 대시보드가 렌더 에러 없이 부산 코드로 동작하는 것이 핵심.
  </action>
  <acceptance_criteria>
    - `grep -c "'26350'" src/app/admin/region-expansion/page.tsx` >= 1 (NEW_CODES가 부산 코드)
    - `grep -c "'48890'" src/app/admin/region-expansion/page.tsx` >= 1 (OLD_CODES에 경남 확장 포함)
    - `grep -c "Phase 33 완료 후 삭제 예정" src/app/admin/region-expansion/page.tsx` == 0
    - `npm run lint` 통과 (tsc 포함)
    - SUMMARY.md에 3-pass grep 재스윕 결과 분류표가 기록됨
  </acceptance_criteria>
  <verify>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>grep 3-pass 재스윕이 완료되어 결과가 분류·기록되고, admin 대시보드가 부산 추적 기준(NEW_CODES=부산 16, OLD_CODES=경남 22)으로 갱신되어 tsc/lint를 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: enrichment 스크립트 region 배열 → regions 동적 조회 전환</name>
  <files>scripts/collect-school-stats.ts, scripts/fetch-sports-facilities.ts</files>
  <read_first>scripts/collect-school-stats.ts, scripts/fetch-sports-facilities.ts</read_first>
  <action>
두 스크립트의 경남 전용 정적 배열을 regions 테이블 동적 조회로 전환한다 (부산뿐 아니라 향후 울산 확장까지 재작업 없이 자동 대응 — A그룹 패턴 일관성). 두 스크립트 모두 이미 자체 `supabase` 클라이언트(`createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)`)를 갖고 있으므로 인라인 쿼리를 추가한다.

**1. `scripts/collect-school-stats.ts`** — 라인 30~53의 `GYEONGNAM_SGG` 정적 배열(경남 22개, `{name, sggCode, schulKndCodes}`)을 런타임 동적 조회로 교체한다. `schulKndCodes`는 현재 전 지역이 `['02','03','04']`로 동일하므로 모든 활성 지역에 동일 적용:
```typescript
async function loadTargetRegions(): Promise<Array<{ name: string; sggCode: string; schulKndCodes: string[] }>> {
  const { data, error } = await supabase
    .from('regions').select('sgg_code, sgg_name').eq('is_active', true).order('sgg_code')
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  return (data ?? []).map(r => ({ name: r.sgg_name, sggCode: r.sgg_code, schulKndCodes: ['02','03','04'] }))
}
```
- `SIDO_CODE`는 현재 `'48'`(경남) 하드코딩 → 부산은 `'26'`. 학교알리미 API는 sidoCode + sggCode를 함께 요구하므로, `SIDO_CODE` 상수를 제거하고 각 지역의 `sggCode` 앞 2자리(`sggCode.slice(0,2)`)를 sidoCode로 사용하도록 변경한다.
- `GYEONGNAM_SGG`를 사용하던 라인 123·153의 `for (const area of GYEONGNAM_SGG)`를 `for (const area of targetRegions)`로 바꾸고, `main()`(또는 실행 진입점) 시작 시 `const targetRegions = await loadTargetRegions()`를 호출한다.

**2. `scripts/fetch-sports-facilities.ts`** — 라인 14~18의 `ADDRESS_KEYWORDS`(si 이름 배열)를 regions 동적 조회로 교체한다:
```typescript
async function loadAddressKeywords(): Promise<string[]> {
  const { data, error } = await supabase
    .from('regions').select('si').eq('is_active', true)
  if (error) throw new Error(`regions 조회 실패: ${error.message}`)
  return [...new Set((data ?? []).map(r => r.si))]  // '부산광역시','창원시','김해시',...
}
```
- `ADDRESS_KEYWORDS`를 참조하던 라인 90 등의 사용처를 `main()` 시작 시 계산한 `const addressKeywords = await loadAddressKeywords()`로 교체. 주소 필터가 `si` 문자열(예 '부산광역시')로 매칭되므로 부산 체육도장이 자동 포함된다.
- ⚠ 부산은 도심 밀집지라 `complexes` 조회가 1,000행을 넘을 수 있다 — 이 스크립트는 이미 `.range()` 페이지네이션 패턴(커밋 `11d5952`)이 적용돼 있으니 그 루프를 유지·확인만 하고 제거하지 않는다.

두 스크립트의 **실제 실행(enrichment)은 34-07에서 좌표 지오코딩 완료 후 수행**한다. 이 Task는 코드 전환 + tsc/lint 통과까지만 담당한다.
  </action>
  <acceptance_criteria>
    - `grep -c "GYEONGNAM_SGG" scripts/collect-school-stats.ts` == 0
    - `grep -c "is_active" scripts/collect-school-stats.ts` >= 1
    - `grep -c "is_active" scripts/fetch-sports-facilities.ts` >= 1
    - `grep -c "'창원시', '김해시'" scripts/fetch-sports-facilities.ts` == 0 (정적 배열 제거됨)
    - `npm run lint` 통과 (tsc 포함, scripts/ tsconfig exclude 대상이면 `npx tsc --noEmit -p tsconfig.scripts.json` 등 스크립트 전용 체크 수행)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>collect-school-stats.ts와 fetch-sports-facilities.ts가 regions 동적 조회로 전환되어 부산을 자동 포함하고, tsc/lint를 통과한다 (실행은 34-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regions 테이블 → 스크립트/대시보드 지역 필터 | DB 값이 지역 필터링의 신뢰 기준점, 하드코딩 제거로 단일 소스화 |
| admin/region-expansion → 서버 컴포넌트 | createSupabaseAdminClient 경유, 어드민 권한 미들웨어로 보호(기존) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | 하드코딩 지역 배열 잔존 | mitigate | 3-pass grep(변수명 + 리터럴 코드값)으로 재스윕, 발견분 동적 전환 — 조용한 부산 누락 방지 |
| T-34-04 | Elevation of Privilege | admin/region-expansion 페이지 | accept | 기존 어드민 미들웨어 권한 검증 유지, 읽기 전용 대시보드(쓰기 경로 없음) |
</threat_model>

<verification>
```bash
grep -rn "GYEONGNAM_SGG\|ADDRESS_KEYWORDS" src/ scripts/   # 잔존 없어야 함
npm run lint 2>&1 | tail -5
```
</verification>

<success_criteria>
1. 3-pass grep 재스윕 완료 + 결과 분류표 SUMMARY 기록
2. collect-school-stats.ts / fetch-sports-facilities.ts가 regions 동적 조회로 전환됨
3. admin/region-expansion 대시보드가 부산 추적 기준으로 갱신되고 tsc/lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-01-SUMMARY.md` 생성. grep 재스윕 결과 분류표 포함.
</output>
</content>
