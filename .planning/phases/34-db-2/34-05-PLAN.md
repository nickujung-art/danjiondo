---
phase: 34-db-2
plan: "05"
type: execute
wave: 2
depends_on: ["34-03"]
files_modified:
  - scripts/detect-busan-dup-candidates.ts
  - src/lib/data/complexes-map.ts
autonomous: true
requirements:
  - REGION-16
  - REGION-15
must_haves:
  truths:
    - "부산 16개 구 complexes의 lat/lng가 카카오 지오코딩으로 채워진다"
    - "좌표 확보 후 좌표+이름유사 중복 후보가 병합 없이 CSV 로그(busan-dup-candidates.csv)로만 기록된다 (D-10 병합 금지, D-11 로그만) — 이때 lat이 채워진 부산 row를 실제로 순회하므로 탐지가 실제 수행됨"
    - "complexes-map.ts의 좌표 범위 BBOX가 부산 동쪽 끝(기장군·해운대 lng ~129.3)을 포함하도록 확장되어 지도에서 부산 단지가 걸러지지 않는다"
  artifacts:
    - path: "scripts/detect-busan-dup-candidates.ts"
      provides: "지오코딩 후 부산 실좌표 row 순회 → detectPotentialDuplicate 호출 → busan-dup-candidates.csv append (log-only)"
      contains: "detectPotentialDuplicate"
    - path: "src/lib/data/complexes-map.ts"
      provides: "부산 실측 좌표 범위를 포함하도록 lat/lng BBOX 상한 확장"
      contains: "129.4"
  key_links:
    - from: "scripts/detect-busan-dup-candidates.ts"
      to: "find_nearby_similar_complexes RPC (via detectPotentialDuplicate)"
      via: "complexes WHERE sgg_code LIKE '26%' AND lat IS NOT NULL 순회 → detectPotentialDuplicate"
      pattern: "detectPotentialDuplicate"
    - from: "src/lib/data/complexes-map.ts"
      to: "complexes 좌표 필터"
      via: ".gte('lng', ...).lte('lng', ...) 범위 필터"
      pattern: "lte\\('lng'"
---

<objective>
부산 16개 구 complexes(34-03에서 시딩)에 카카오 지오코딩으로 lat/lng를 채우고, 좌표가 채워진 뒤 D-11 좌표+이름유사 중복 후보를 탐지해 로그(CSV)로만 남기며, `complexes-map.ts`의 좌표 범위 BBOX를 부산을 포함하도록 확장한다.

**왜 dup-detection이 여기(34-05)인가:** KAPT 단지목록 API 응답에는 좌표가 없어(`KaptComplex`에 coordX/coordY 필드 부재) 34-03 시딩 시점에는 좌표기반 중복 탐지가 불가능하다. `complexes`의 좌표는 이 plan의 Task 1 카카오 지오코딩으로 비로소 채워지므로, 좌표+이름유사 탐지는 반드시 지오코딩 **후**(Task 2)에 실행되어야 한다. 34-03은 RPC(`find_nearby_similar_complexes`)와 헬퍼(`detectPotentialDuplicate`)만 준비했고, 이 plan이 실좌표 row를 순회하며 실제 탐지를 수행한다. 경남에서 발견된 111쌍/303건 중복 좌표 패턴의 부산 규모를 파악해 후속 병합 phase 규모 산정에 쓴다(D-11: 로그만, D-10: 병합 금지).

RESEARCH.md Pitfall 2가 지목한 대로 현재 BBOX 상한(`lng 129.3`)은 부산 기장군·해운대구 동쪽 해안(lng ~129.29~129.32)과 거의 맞닿아 있어, 지오코딩 오차나 해안 인접 단지가 지도에서 걸러질 위험이 있다. **주의(33-CONTEXT 교훈): BBOX 수정은 좌표가 실제로 채워지기 전까지 효과가 없다 — 지오코딩이 반드시 선행되어야 한다.**

Purpose: 부산 단지가 지도에 정상 표시되고, 네이버 매핑 BBOX 산출(34-08)의 기반이 되는 실측 좌표를 확보하며, 후속 중복 병합 phase 규모를 dup-detection 로그로 파악.
Output: 부산 단지 좌표 채움, `scripts/busan-dup-candidates.csv` 잠재 중복 로그, complexes-map.ts BBOX 부산 포함 확장.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-PATTERNS.md
@C:\Users\jung\coding\bds\scripts\geocode-complexes.ts
@C:\Users\jung\coding\bds\src\lib\data\complex-matching.ts
@C:\Users\jung\coding\bds\src\lib\data\complexes-map.ts

<interfaces>
<!-- 34-03이 준비한 dup-detection 헬퍼 (src/lib/data/complex-matching.ts) — 이 plan Task 2가 소비 -->
```typescript
export async function detectPotentialDuplicate(
  supabase: SupabaseClient,
  candidate: { coordX?: number /*lng*/; coordY?: number /*lat*/; nameNormalized: string; kaptCode: string },
): Promise<Array<{ id: string; canonical_name: string; kapt_code: string; dist_m: number }>>
// coordX/coordY null이면 [] (스킵). 좌표 있으면 find_nearby_similar_complexes RPC 호출.
```

<!-- nameNormalize (src/lib/data/name-normalize.ts:14) -->
```typescript
export function nameNormalize(raw: string): string
```

<!-- complexes-map.ts 현재 좌표 필터 (라인 87-88, b59bd01로 경남 전체 커버) -->
```typescript
.gte('lat', 34.7).lte('lat', 35.8)
.gte('lng', 127.7).lte('lng', 129.3)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 부산 단지 카카오 지오코딩 실행</name>
  <files>없음 (기존 스크립트 실행)</files>
  <read_first>scripts/geocode-complexes.ts, scripts/enrich-apt-unmatched.ts</read_first>
  <action>
34-03에서 시딩된 부산 단지 중 lat/lng가 비어있는 단지를 카카오 지오코딩으로 채운다. `KAKAO_REST_API_KEY`가 필요하다(기존 크론/스크립트에서 사용 중). 스크립트 헤더 주의사항대로 환경변수(`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `KAKAO_REST_API_KEY`)를 프로덕션 대상으로 명시 설정 후 실행:

```bash
npx tsx scripts/geocode-complexes.ts
```

RESEARCH.md/33-CONTEXT의 "데이터 완성도 점검" 교훈: 카카오 키워드 검색만으로 부족한 단지는 주소 검색 API 폴백이 필요할 수 있다(경남에서 100% 도달까지 겪은 함정). 지오코딩 후 잔여 미좌표 단지가 있으면 `scripts/enrich-apt-unmatched.ts`(도로명주소 기반 보강)를 추가 실행해 커버리지를 높인다:
```bash
npx tsx --env-file=.env.local scripts/enrich-apt-unmatched.ts
```

실행 후 부산 좌표 커버리지를 확인한다:
```sql
select count(*) filter (where lat is not null) as geocoded,
       count(*) as total
from complexes where sgg_code like '26%';
```
목표는 경남과 동일한 사실상 100% 커버리지(소수 폐단지 제외).
  </action>
  <acceptance_criteria>
    - `select count(*) from complexes where sgg_code like '26%' and lat is not null` 이 실행 후 > 0이고, 전체 부산 단지 대비 커버리지가 95% 이상
    - 지오코딩 후 좌표가 부산 실제 범위(lat 34.9~35.4, lng 128.7~129.35) 안에 분포 (SQL min/max 확인)
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/geocode-complexes.ts 2>&1 | tail -15</automated>
  </verify>
  <done>부산 단지의 lat/lng가 카카오 지오코딩으로 채워지고 커버리지가 95% 이상이다.</done>
</task>

<task type="auto">
  <name>Task 2: [D-11] 좌표 확보 후 부산 중복 후보 탐지 로그 (실좌표 row 순회)</name>
  <files>scripts/detect-busan-dup-candidates.ts</files>
  <read_first>src/lib/data/complex-matching.ts, scripts/seed-complexes.ts</read_first>
  <action>
**⚠ 이 Task는 Task 1(지오코딩)이 완료되어 부산 complexes의 lat/lng가 채워진 뒤에만 유효하다** — 좌표가 없으면 `detectPotentialDuplicate`가 전건 스킵한다. 초기 계획은 이 로직을 34-03 시딩 루프에 넣었으나 KAPT API에 좌표가 없어 항상 스킵되는 결함이 있었고, 좌표가 실제로 존재하는 이 시점으로 이관됐다.

**신규 스크립트 `scripts/detect-busan-dup-candidates.ts` 작성** — 다른 배치 스크립트(`scripts/geocode-complexes.ts` 등)의 헤더 패턴(환경변수 로드 + service_role supabase 클라이언트 생성)을 그대로 따른다. 로직:
1. `complexes`에서 `sgg_code LIKE '26%' AND lat IS NOT NULL AND lng IS NOT NULL` row를 조회한다 (Supabase 1,000행 캡 대비 `.range()` 페이지네이션 — 33-06 선례).
2. 각 row에 대해 34-03의 헬퍼를 호출한다 (import: `import { detectPotentialDuplicate } from '../src/lib/data/complex-matching'`, `import { nameNormalize } from '../src/lib/data/name-normalize'`):
```typescript
const dupCandidates = await detectPotentialDuplicate(supabase, {
  coordX: row.lng, coordY: row.lat, nameNormalized: nameNormalize(row.canonical_name), kaptCode: row.kapt_code,
})
```
3. 후보가 있으면 `busan-dup-candidates.csv`에 append (DB 변경 없음, log-only):
```typescript
for (const d of dupCandidates) {
  dupRows.push(`"${row.sgg_code}","${row.kapt_code}","${row.canonical_name}","${d.kapt_code}","${d.canonical_name}",${d.dist_m.toFixed(1)}`)
}
```
   - 시작 시 `const dupRows: string[] = ['sgg,kapt,name,dup_kapt,dup_name,dist_m']`, 종료 시 `writeFileSync('scripts/busan-dup-candidates.csv', dupRows.join('\n'))`.
4. 종료 시 **처리한 좌표 보유 row 수**와 **잠재 중복 건수**를 콘솔에 출력한다: `console.log(\`좌표 보유 부산 단지 N건 검사, 잠재 중복 M건\`)`. N==0이면 지오코딩이 선행되지 않았다는 뜻이므로 경고를 남기고 중단 코드로 종료(결함 재발 방지 가드).

실행:
```bash
npx tsx --env-file=.env.local scripts/detect-busan-dup-candidates.ts
```
잠재 중복 건수를 SUMMARY.md에 "잠재 중복 M건"으로 기록한다(후속 병합 phase 규모 파악용, D-11).
  </action>
  <acceptance_criteria>
    - `grep -c "detectPotentialDuplicate" scripts/detect-busan-dup-candidates.ts` >= 1 (import + 호출)
    - `grep -c "lat" scripts/detect-busan-dup-candidates.ts` >= 1 (좌표 보유 row만 순회 — WHERE lat IS NOT NULL)
    - 실행 로그에 "좌표 보유 부산 단지 N건 검사" 출력이 있고 N > 0 (지오코딩 선행 확인 — 결함 재발 시 N==0 가드가 잡음)
    - `scripts/busan-dup-candidates.csv`가 생성되고 잠재 중복 건수가 SUMMARY.md에 기록됨
    - `npm run lint` 통과
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --env-file=.env.local scripts/detect-busan-dup-candidates.ts 2>&1 | tail -10</automated>
  </verify>
  <done>지오코딩으로 좌표가 채워진 부산 단지를 순회하며 좌표+이름유사 중복 후보를 병합 없이 CSV로 기록하고, 처리 건수(N>0)로 좌표 선행이 확인된다.</done>
</task>

<task type="auto">
  <name>Task 3: complexes-map.ts BBOX 부산 실측 범위로 확장</name>
  <files>src/lib/data/complexes-map.ts</files>
  <read_first>src/lib/data/complexes-map.ts</read_first>
  <action>
Task 1의 지오코딩 완료 후 부산 실측 좌표 범위를 조회한다:
```sql
select min(lat), max(lat), min(lng), max(lng) from complexes where si='부산광역시' and lat is not null;
```

`src/lib/data/complexes-map.ts`의 라인 87-88 좌표 필터를 실측 결과에 맞춰 확장한다. 부산 동쪽 끝(기장군·해운대 lng ~129.32, 양산 인접 lat ~35.4)을 여유있게 포함하도록, **실측 max가 현재 상한을 넘으면 여유 padding을 더해** 상향한다. 예상 목표값(실측으로 확인 후 조정):
```typescript
.gte('lat', 34.7).lte('lat', 35.8)      // lat 상한은 이미 부산(max ~35.4) 커버 — 실측이 35.8 초과 시에만 조정
.gte('lng', 127.7).lte('lng', 129.4)    // lng 상한 129.3 → 129.4 (부산 기장군·해운대 동쪽 해안 포함)
```
- lng 하한 127.7은 경남 서쪽(하동 등) 기준이라 부산은 그 안에 들어오므로 하한은 유지.
- **실측값이 위 예시와 다르면 실측 기준으로 조정**하되, 상한은 항상 실측 max + 최소 0.05 여유를 둔다.
- 이 파일은 "잘못된 지오코딩 결과 제외" 안전장치이므로 과도하게 넓히지 않는다(예: 129.4 정도, 130까지 확장 금지 — 오지오코딩 필터 기능 유지).
  </action>
  <acceptance_criteria>
    - `grep -c "129.4" src/lib/data/complexes-map.ts` >= 1 (또는 실측 기반 상한값이 129.32 이상)
    - `grep -c "129.3'" src/lib/data/complexes-map.ts` == 0 이 아니어도 되지만, lng 상한이 부산 실측 max를 포함함이 SUMMARY에 실측값과 함께 기록됨
    - `npm run lint` 통과
    - `/map` 페이지에서 기장군·해운대 동쪽 끝 단지가 걸러지지 않음 (부산 lng max < 새 상한 확인)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>complexes-map.ts BBOX가 부산 실측 좌표 범위를 포함하도록 확장되고 tsc/lint를 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KAKAO_REST_API_KEY → 카카오 지오코딩 API | 서버 전용 시크릿, 배치 스크립트 경유 |
| dup-detection RPC 파라미터 | DB 좌표/정규화 이름(서버 산출값), 사용자 직접 입력 경로 없음 |
| complexes-map.ts BBOX → 지도 쿼리 필터 | 오지오코딩 결과 제외 안전장치, 서버 컴포넌트/RSC 전용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-09 | Information Disclosure | busan-dup-candidates.csv | accept | 공개 단지명/좌표만 기록, git ignore 대상 여부는 실행자가 확인(운영 로그) |
| T-34-11 | Tampering | complexes-map BBOX 과확장 | mitigate | 실측 max + 최소 여유(0.05)까지만 확장, 130 이상 금지 — 오지오코딩 필터 기능 유지 |
| T-34-12 | Information Disclosure | 좌표 데이터 | accept | 공개 단지 좌표, 민감정보 아님 |
</threat_model>

<verification>
```bash
npx tsx scripts/geocode-complexes.ts 2>&1 | tail -15
npx tsx --env-file=.env.local scripts/detect-busan-dup-candidates.ts 2>&1 | tail -10
npm run lint 2>&1 | tail -5
```
</verification>

<success_criteria>
1. 부산 단지 좌표 커버리지 95% 이상
2. 좌표 확보 후 좌표+이름유사 잠재 중복이 병합 없이 CSV 로그로만 기록됨 (D-10/D-11), 처리 건수 N>0으로 좌표 선행 확인
3. complexes-map.ts BBOX가 부산 실측 범위 포함, 지도에서 부산 동쪽 끝 단지가 걸러지지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-05-SUMMARY.md` 생성. 부산 실측 좌표 min/max + "잠재 중복 M건"(검사한 좌표 보유 단지 N건) 기록.
</output>
</content>
</output>
