---
phase: 34-db-2
plan: "10"
type: execute
wave: 4
depends_on: ["34-08"]
files_modified:
  - .github/workflows/map-naver-self-hosted.yml
autonomous: false
requirements:
  - REGION-21
must_haves:
  truths:
    - "부산 네이버 매핑이 --new-only로 실제 실행되어 부산 단지의 naver_complex_no가 채워진다 (D-05)"
    - "로컬 프로세스 불안정 근본 원인이 self-hosted runner PoC로 시간박스 재조사되고, 시간 내 미해결이면 restart-loop 폴백으로 phase가 막히지 않는다 (D-06, D-07)"
    - "self-hosted runner는 public 저장소 fork PR 침해를 막도록 workflow_dispatch 전용 라벨링 + --ephemeral로 격리된다"
  artifacts:
    - path: ".github/workflows/map-naver-self-hosted.yml"
      provides: "self-hosted runner 전용 workflow_dispatch 매핑 워크플로 (PoC)"
      contains: "workflow_dispatch"
  key_links:
    - from: "map-naver-complexes-playwright.ts --new-only"
      to: "complexes.naver_complex_no"
      via: "flushMatches() 증분 저장 (존 경계/5중심점마다)"
      pattern: "new-only"
---

<objective>
부산 네이버 매핑을 실제로 실행한다(D-05). 동시에 로컬 크롤러 프로세스가 불투명하게 죽는 문제(harness 레벨 백그라운드 작업 수명 제약으로 추정, OS/백신 흔적 없음)를 self-hosted runner PoC로 **시간박스 재조사**한다(D-06). self-hosted runner는 실제 사용자 Windows PC에서 실행되므로 네이버 IP 차단을 우회하고(로컬과 동일 네트워크 경로) Claude 세션과 독립된 프로세스 트리를 타므로 kill 문제를 회피할 이론적 근거가 있다(RESEARCH.md §3, LOW confidence — 미검증 가설). **PoC가 시간 내 해결되지 않으면 검증된 restart-loop로 즉시 폴백(D-07) — 이 문제가 phase 전체를 blocking하지 않는다.**

**보안(RESEARCH.md Security Domain):** 이 저장소는 public이므로 self-hosted runner를 fork PR 침해로부터 격리해야 한다 — `workflow_dispatch` 전용 워크플로에만 self-hosted 라벨 사용, `pull_request`/`pull_request_target` 트리거에는 절대 미사용, `--ephemeral`로 잡 후 자동 등록 해제, Fork PR 승인 설정 확인.

Purpose: 부산 매핑 완성 + 로컬 프로세스 불안정 근본 원인의 구조적 재조사(단, phase blocking 방지).
Output: 부산 naver_complex_no 매핑, self-hosted runner PoC 결과(성공/폴백) 리포트.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-PATTERNS.md
@C:\Users\jung\coding\bds\scripts\map-naver-complexes-playwright.ts
@C:\Users\jung\coding\bds\.github\workflows\map-naver-complexes-once.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
34-08에서 부산 BBOX·OLD_ZONE_NAMES 갱신·쿠키 유효성·--diagnose가 준비됐다. 이제 부산 네이버 매핑을 실제 실행한다. 로컬 프로세스 불안정 종료 문제 때문에 매핑이 도중에 죽을 수 있으나, `flushMatches()`가 존 경계/5중심점마다 즉시 DB 반영하므로(라인 384-418) 유실은 최소화된다. self-hosted runner PoC는 Task 2에서 병행 시도하되, 이 Task는 검증된 restart-loop 경로로 부산 매핑을 완주하는 것이 목표다.
  </what-built>
  <how-to-verify>
1. **매핑 대상 기준선 확인:**
   ```sql
   select count(*) filter (where naver_complex_no is not null) as mapped,
          count(*) as total from complexes where sgg_code like '26%';
   ```
2. **부산 실제 매핑 실행(D-05)** — OLD_ZONE_NAMES 갱신으로 --new-only가 부산만 스윕:
   ```bash
   npx tsx scripts/map-naver-complexes-playwright.ts --new-only
   ```
3. **프로세스가 죽으면 restart-loop 폴백(D-07)** — 검증된 패턴:
   - 좀비 Playwright/Chromium 프로세스 정리
   - `--new-only` 재시작 (flushMatches가 이미 저장한 매핑은 건너뜀, 증분 재개)
   - 부산 미매핑 잔여가 수렴할 때까지 반복
4. **완료 판정:** 부산 매핑 커버리지가 경남 수준(사실상 대부분 매핑, 미매핑은 `complex_match_queue` 검수 대상)에 도달. 미매핑 잔여는 34-08 diagnose 버킷(이름불일치/커버리지밖)과 대조해 정상 잔여인지 확인.
  </how-to-verify>
  <resume-signal>부산 매핑 커버리지가 목표에 도달하면 "부산 매핑 완료, N/M 매핑됨"으로 응답. 진행 중이면 "N회차 restart-loop, M개 남음"으로 알려주면 이어서 재개한다. 프로세스 안정성 관련 관찰(몇 분/몇 zone 후 죽는지)을 Task 2 PoC 판단 근거로 함께 기록한다.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
로컬 프로세스 불안정 근본 원인을 self-hosted runner로 시간박스 재조사하는 PoC 준비물(워크플로 파일)을 구성한다. **시간 제한(예: 반나절)을 두고, 실패 시 Task 1의 restart-loop로 이미 부산 매핑이 진행되므로 phase는 막히지 않는다(D-07).**
  </what-built>
  <how-to-verify>
1. **self-hosted runner 전용 워크플로 작성** — `.github/workflows/map-naver-self-hosted.yml`:
   - 트리거: `on: workflow_dispatch` **전용** (절대 `pull_request`/`pull_request_target` 없음 — public repo fork PR 침해 차단)
   - `runs-on: [self-hosted, windows, naver-map]` 커스텀 라벨 (기본 라벨과 분리)
   - `map-naver-complexes-once.yml`의 스텝 구조(checkout → setup → `npx tsx scripts/map-naver-complexes-playwright.ts --new-only`) 참고, 단 러너만 self-hosted로 교체
   - `NAVER_COOKIE`는 GitHub Secret에서 주입(값 로그 금지)
2. **러너 등록(사용자 수행, 시간박스):**
   - 사용자 Windows PC에서 GitHub Settings → Actions → Runners → New self-hosted runner
   - `--labels naver-map` + **`--ephemeral`** (잡 1회 실행 후 자동 등록 해제 — 상시 서비스보다 안전)
   - Settings → Actions → "Fork pull request workflows from outside collaborators" 승인 요구 설정 확인
3. **PoC 실행 + 성공 기준(Pitfall 5 — 성급한 "해결됨" 판단 금지):**
   ```bash
   gh workflow run map-naver-self-hosted.yml
   ```
   성공 기준 = **실제 매핑 대상 규모에 준하는 장시간 실행 1회를 처음부터 끝까지 중단 없이 완료**. 1~2회 짧은 성공은 "해결"과 동치가 아니다.
4. **판정:**
   - **성공(장시간 무중단 완주)** → self-hosted runner가 kill 문제를 회피함을 확인, 향후 매핑 경로로 채택 여부를 SUMMARY에 권고. 단 PoC 후 러너는 `--ephemeral`로 해제되므로 상시 유지 시 보안 재검토 필요.
   - **실패 또는 시간 초과** → D-07 restart-loop 폴백 확정(Task 1이 이미 완주 중). 러너 등록 해제(`config.cmd remove`)로 공격 표면 제거. 근본 원인은 미해결로 SUMMARY에 기록하고 phase는 restart-loop로 완료.
5. **[필수] PoC 종료 후 정리:** 성공/실패 무관하게 러너가 상시 등록 상태로 방치되지 않도록 `--ephemeral` 자동 해제 또는 수동 `config.cmd remove` 수행(RESEARCH.md Runtime State Inventory: 방치 시 불필요한 공격 표면 잔존).
  </how-to-verify>
  <resume-signal>PoC 결과를 "self-hosted runner 성공(장시간 완주) — 채택 권고" 또는 "PoC 실패/시간초과 — restart-loop 폴백 확정, 러너 등록 해제함"으로 응답. 러너 정리 완료 여부를 함께 확인한다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-hosted runner ← public repo fork PR | fork PR이 러너에서 임의 코드 실행 가능 — 최대 위험 경계 |
| self-hosted runner OS 등록 상태 | 이 phase에서 처음 생기는 신규 OS 레벨 등록, 방치 시 공격 표면 |
| NAVER_COOKIE (GitHub Secret) → 러너 | 실사용자 세션 쿠키, 로그 노출 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-20 | Elevation of Privilege / Tampering | public repo self-hosted runner (fork PR 임의 코드 실행) | mitigate | `workflow_dispatch` 전용 라벨(`naver-map`)에만 사용, `pull_request`/`pull_request_target` 트리거 워크플로에 절대 미사용, `--ephemeral` 자동 해제, Fork PR 승인 설정 확인 (RESEARCH.md Security Domain) |
| T-34-21 | Information Disclosure | NAVER_COOKIE 워크플로 로그 노출 | mitigate | GitHub Secret 주입, 값 로그 금지(이름만) — 기존 스크립트 관행 유지 |
| T-34-22 | Elevation of Privilege | 방치된 러너 등록 상태 | mitigate | PoC 종료 후 `--ephemeral` 자동 해제 또는 `config.cmd remove` 필수 — 공격 표면 제거 |
</threat_model>

<verification>
```bash
test -f .github/workflows/map-naver-self-hosted.yml && grep -c "pull_request" .github/workflows/map-naver-self-hosted.yml
# 기대: 0 (workflow_dispatch 전용, pull_request 트리거 없음)
```
</verification>

<success_criteria>
1. 부산 naver_complex_no 매핑이 실제 실행되어 커버리지 확보 (restart-loop로라도 완주)
2. self-hosted runner PoC가 시간박스로 시도되고 성공/폴백이 명확히 판정됨
3. self-hosted runner가 fork PR 침해 방어책으로 격리되고, PoC 후 러너 정리됨
4. PoC 실패 시에도 restart-loop 폴백으로 phase가 막히지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-10-SUMMARY.md` 생성. 매핑 커버리지 + self-hosted runner PoC 결과(성공/폴백) + 근본 원인 조사 관찰 기록.
</output>
</content>
