---
phase: 34-db-2
plan: "04"
type: execute
wave: 1
depends_on: ["34-00"]
files_modified:
  - src/__tests__/school-ranking-regional.test.ts
autonomous: true
requirements:
  - REGION-18
must_haves:
  truths:
    - "school_ranking RPC가 p_si='부산광역시' 호출에 에러 없이 응답하고 모든 행의 gu가 null임이 통합 테스트로 고정된다 (현재 동작 회귀 방지)"
    - "부산 구별 라벨 미지원은 코드 수정 대상이 아니라 회귀 테스트로 현재 동작을 의도적으로 고정하는 것임이 명확히 기록된다"
  artifacts:
    - path: "src/__tests__/school-ranking-regional.test.ts"
      provides: "부산광역시 케이스 통합 테스트 추가 (gu 전부 null)"
      contains: "부산광역시"
  key_links: []
---

<objective>
RESEARCH.md Pattern 3이 코드 감사로 확정한 사실 — `school_ranking` SQL RPC의 `gu` 출력 컬럼은 `road_address LIKE '%창원시 OO구%'` 5-CASE 하드코딩이라 부산 학교(`'부산광역시 해운대구 ...'`)는 이 패턴에 매칭되지 않아 항상 `gu=NULL`을 반환한다(에러 없음, 김해시와 동일한 안전 폴백 경로) — 를 자동화된 회귀 테스트로 고정한다. 부산은 16개 구 전부가 구 있는 지역이라 이 제약이 창원보다 크게 체감되지만, **코드 수정(전국형 gu 라벨 일반화)은 CONTEXT.md 범위 밖(deferred)** 이므로 이번 phase는 "현재 동작이 안전하게 유지됨"만 테스트로 고정한다(33-04 패턴 재사용).

Purpose: 향후 다른 작업이 이 RPC를 실수로 깨뜨리는 회귀 방지 + 부산 구별 라벨 지원이 별도 과제(`project_school_ranking_next.md`)로 이관됨을 문서화.
Output: school-ranking-regional.test.ts에 부산 케이스 1개 추가.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\33-db-1\33-04-PLAN.md
@C:\Users\jung\coding\bds\src\__tests__\school-ranking-regional.test.ts

<interfaces>
<!-- school_ranking RPC 시그니처 (supabase/migrations/20260616000004_school_ranking_rpc.sql) -->
```sql
school_ranking(p_si text, p_school_type text, p_metric text)
  RETURNS TABLE (rank bigint, school_name text, metric_value numeric, gu text)
-- gu: road_address가 '창원시 OO구' 5-CASE에 매칭 안 되면 NULL (부산은 항상 NULL)
```

<!-- 기존 테스트 구조 (33-04에서 생성, 이 describe 블록에 부산 it 추가) -->
```typescript
import { describe, it, expect } from 'vitest'
import { SKEY, admin } from './helpers/db'
describe.skipIf(!SKEY)('school_ranking RPC: 무구(無區) 시군구 처리 (integration)', () => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: school_ranking RPC 부산광역시 케이스 회귀 테스트 추가</name>
  <files>src/__tests__/school-ranking-regional.test.ts</files>
  <read_first>src/__tests__/school-ranking-regional.test.ts</read_first>
  <behavior>
    - admin.rpc('school_ranking', { p_si:'부산광역시', p_school_type:'elementary', p_metric:'students_per_class' }) 호출 시 error가 null (에러 없이 처리)
    - 반환된 모든 행의 gu가 null (부산은 gu 추출 5-CASE 패턴 미지원 — 항상 NULL)
  </behavior>
  <action>
`src/__tests__/school-ranking-regional.test.ts`의 기존 `describe.skipIf(!SKEY)('school_ranking RPC: 무구(無區) 시군구 처리 (integration)', ...)` 블록 안, 김해시/창원시 케이스 다음에 부산 케이스를 추가한다:

```typescript
it('부산광역시(구 있으나 RPC gu 라벨 미대응) — 에러 없이 응답, 모든 행 gu=null (현재 동작 고정)', async () => {
  const { data, error } = await admin.rpc('school_ranking', {
    p_si: '부산광역시',
    p_school_type: 'elementary',
    p_metric: 'students_per_class',
  })
  expect(error).toBeNull()
  // 부산 학교 road_address는 창원 5-CASE 패턴에 매칭 안 됨 → gu 항상 NULL
  // ⚠ 구별 라벨 지원은 CONTEXT.md 범위 밖(deferred) — 이 테스트는 현재 안전 동작을 고정만 함
  for (const row of data ?? []) {
    expect(row.gu).toBeNull()
  }
})
```

**⚠ 33-04 Deviations 주의 계승:** 김해시 테스트에서 실제로는 소수 오염 행(facility_school↔complexes 매칭 버그로 gu가 잘못 채워진 사전 존재 데이터)이 있었다. 부산도 프로덕션 데이터로 실행 시 non-null 예외가 나오면, 이는 신규 회귀가 아니라 사전 존재 데이터 오염이므로 assertion을 "gu가 null이거나(대다수) non-null이면 부산 16개 구 중 하나"로 완화하되, **완화 사유를 테스트 주석과 SUMMARY.md에 명시**한다. 완화 없이 통과하면 그대로 둔다.

코드 변경 없음 — 테스트 파일만 수정한다(production diff 0).
  </action>
  <acceptance_criteria>
    - `grep -c "부산광역시" src/__tests__/school-ranking-regional.test.ts` >= 1
    - `npx vitest run src/__tests__/school-ranking-regional.test.ts` 실행 시 에러 없음 (SKEY 없는 환경은 스킵되어 통과, SKEY 있으면 부산 케이스 통과)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/school-ranking-regional.test.ts</automated>
  </verify>
  <done>school-ranking-regional.test.ts에 부산 케이스가 추가되고 통과하며, 구별 라벨 미지원이 deferred임이 주석으로 기록된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 코드 → RPC 호출 | CI/로컬 테스트 전용, 프로덕션 트래픽 아님 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10 | Tampering | school_ranking RPC p_si 임의 문자열 허용 | accept | 읽기 전용 SELECT RPC, 파라미터 바인딩(SQL 인젝션 불가), 응답에 민감정보 없음 — 기존 설계 유지 |
</threat_model>

<verification>
```bash
npx vitest run src/__tests__/school-ranking-regional.test.ts
```
</verification>

<success_criteria>
1. school_ranking RPC 부산 케이스가 통합 테스트로 존재
2. 현재 동작(gu 전부 null)이 회귀 테스트로 고정됨
3. production code diff 0 — 구별 라벨 지원은 deferred로 명시
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-04-SUMMARY.md` 생성.
</output>
</content>
