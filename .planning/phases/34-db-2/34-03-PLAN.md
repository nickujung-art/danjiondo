---
phase: 34-db-2
plan: "03"
type: execute
wave: 1
depends_on: ["34-00"]
files_modified:
  - supabase/migrations/20260708000001_find_nearby_similar_complexes.sql
  - src/lib/data/complex-matching.ts
  - src/lib/data/complex-matching.test.ts
  - scripts/seed-complexes.ts
autonomous: true
requirements:
  - REGION-15
must_haves:
  truths:
    - "부산 16개 구의 Golden Record(complexes)가 KAPT 단지목록 API로 시딩되고 si/gu/household_count/built_year가 채워진다"
    - "신규 dup-detection RPC(find_nearby_similar_complexes)가 라이브 Supabase에 적용되어 시딩 루프에서 호출 가능하다"
    - "KAPT 시딩 시 좌표+이름유사 중복 후보가 병합 없이 로그(CSV)로만 기록된다 (D-10 병합 금지, D-11 로그만)"
  artifacts:
    - path: "supabase/migrations/20260708000001_find_nearby_similar_complexes.sql"
      provides: "ST_DWithin + similarity 기반 log-only 중복 탐지 RPC"
      contains: "find_nearby_similar_complexes"
    - path: "src/lib/data/complex-matching.ts"
      provides: "detectPotentialDuplicate 헬퍼 (RPC 호출, log-only)"
      exports: ["detectPotentialDuplicate"]
    - path: "scripts/seed-complexes.ts"
      provides: "시딩 루프 내 dup-check 삽입 + busan-dup-candidates.csv append"
      contains: "detectPotentialDuplicate"
  key_links:
    - from: "scripts/seed-complexes.ts"
      to: "find_nearby_similar_complexes RPC"
      via: "detectPotentialDuplicate() 호출 (seedComplex 직후)"
      pattern: "detectPotentialDuplicate"
    - from: "scripts/seed-complexes.ts"
      to: "complexes 테이블"
      via: "getSggCodes (regions.is_active 동적 조회) → fetchComplexList → seedComplex upsert"
      pattern: "getSggCodes"
---

<objective>
34-06(국토부 실거래가 백필) 전에 부산 16개 구의 Golden Record(`complexes`)를 KAPT 단지목록 API로 먼저 시딩한다. `scripts/seed-complexes.ts`/`scripts/kapt-enrich.ts`는 이미 `regions.is_active=true`를 동적 조회하므로(코드 변경 없이 34-00 시딩만으로 부산 자동 포함), 이 plan의 신규 작업은 **D-11의 좌표+이름유사 중복 후보 탐지 로그**뿐이다. 경남 기존 데이터에서 111쌍/303건의 중복 좌표 Golden Record가 발견됐고(원인: KAPT 이름 변형체가 동일 좌표로 중복 시딩됨), 부산도 동일 메커니즘으로 중복이 쌓일 수 있으므로 시딩 시점에 후보를 탐지해 **병합 없이 로그만** 남긴다(D-10: 병합은 별도 후속 phase로 defer).

**왜 이 순서인가:** CLAUDE.md Golden Record 원칙상 실거래가는 complexes에 좌표+이름 매칭되고, 매칭 실패는 `complex_match_queue`로 쌓인다(자동 승인 금지). 부산 complexes가 없는 상태로 34-06 백필을 먼저 실행하면 부산 거래 전량이 매칭 실패로 큐에 쌓여 검수 부담만 늘린다. 따라서 Golden Record 시딩이 백필보다 먼저 완료돼야 한다.

Purpose: 부산 실거래가가 최대한 자동 매칭되도록 Golden Record 기반 확보 + 후속 중복 병합 phase 규모 파악용 로그 확보.
Output: 부산 complexes 시딩, dup-detection RPC 적용, `scripts/busan-dup-candidates.csv` 잠재 중복 로그.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\CLAUDE.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-CONTEXT.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\34-db-2\34-PATTERNS.md
@C:\Users\jung\coding\bds\scripts\seed-complexes.ts
@C:\Users\jung\coding\bds\src\lib\data\complex-matching.ts

<interfaces>
<!-- seedComplex 입력 타입 (src/lib/data/complex-matching.ts:8) — coordY=lat, coordX=lng -->
```typescript
export interface SeedComplexInput { kaptCode: string; kaptName: string; sggCode: string; coordX?: number /*lng*/; coordY?: number /*lat*/; ... }
export async function seedComplex(raw: SeedComplexInput, supabase: SupabaseClient): Promise<...>
```

<!-- 기존 좌표 매칭 RPC 패턴 (complex-matching.ts:163) — 신규 dup-RPC도 동일 SupabaseClient.rpc 형식 -->
```typescript
const { data, error } = await supabase.rpc('match_complex_by_coord', { p_lng, p_lat, p_name_normalized })
```

<!-- seed-complexes.ts 시딩 루프 (라인 60-67) — dup-check 삽입 지점 -->
```typescript
for (const c of complexes) {
  try { await seedComplex({ ...c, sggCode }, supabase); sggUpserted++
    // [NEW] dup-check 삽입 위치
  } catch (err) { failures.push({ sgg_code: sggCode, raw_name: c.kaptName, reason: String(err) }) }
}
```

<!-- nameNormalize (src/lib/data/name-normalize.ts:14) -->
```typescript
export function nameNormalize(raw: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] dup-detection RPC 마이그레이션 작성 + 라이브 Supabase 적용</name>
  <files>supabase/migrations/20260708000001_find_nearby_similar_complexes.sql</files>
  <read_first>supabase/migrations/20260707000000_area_type_ambiguity_guard.sql</read_first>
  <action>
**이 Task는 [BLOCKING]이다 — 마이그레이션 파일을 작성한 뒤 반드시 라이브 Supabase 프로젝트에 적용해야 하며, Task 3(시딩)이 이 RPC를 호출하므로 Task 3 전에 완료되어야 한다.**

**1. 마이그레이션 파일 작성** `supabase/migrations/20260708000001_find_nearby_similar_complexes.sql` — 기존 PostGIS `location`(geography) 컬럼 + `pg_trgm` GIN 인덱스(`name_normalized`)를 재사용하는 log-only(SELECT) 함수. 병합/쓰기 없음:
```sql
-- D-11: 좌표+이름유사 중복 후보 탐지 (log-only, 병합 없음)
-- 기존 complexes.location(geography) + name_normalized(pg_trgm GIN) 인덱스 재사용
create or replace function find_nearby_similar_complexes(
  p_lat double precision,
  p_lng double precision,
  p_name_normalized text,
  p_exclude_kapt_code text,
  p_radius_m double precision,
  p_similarity_threshold real
) returns table(id uuid, canonical_name text, kapt_code text, dist_m double precision)
language sql stable as $$
  select c.id, c.canonical_name, c.kapt_code,
         ST_Distance(c.location, ST_SetSRID(ST_MakePoint(p_lng, p_lat), 4326)::geography) as dist_m
  from complexes c
  where c.kapt_code is distinct from p_exclude_kapt_code
    and c.location is not null
    and ST_DWithin(c.location, ST_SetSRID(ST_MakePoint(p_lng, p_lat), 4326)::geography, p_radius_m)
    and similarity(c.name_normalized, p_name_normalized) > p_similarity_threshold
  order by dist_m;
$$;
```

**2. 라이브 적용** — 이 프로젝트는 `mcp__supabase__apply_migration` MCP 도구로 마이그레이션을 직접 적용한다(이번 세션 선례: `20260707000000_area_type_ambiguity_guard.sql`을 이 방식으로 적용). 위 SQL을 `apply_migration`(name: `find_nearby_similar_complexes`)로 실행한다.
- MCP 도구를 쓸 수 없으면 폴백: `SUPABASE_ACCESS_TOKEN` 환경변수를 설정하고 `supabase db push`로 적용한다.
- ⚠ `p_exclude_kapt_code`에 `is distinct from`을 쓴 이유: 신규 시딩 단지 자신(같은 kapt_code)을 결과에서 제외하되, NULL kapt_code 비교 함정을 피하기 위함.

**3. 적용 검증** — RPC가 존재하고 호출 가능한지 확인:
```sql
select find_nearby_similar_complexes(35.16, 129.16, 'test', 'K_NONEXIST', 30, 0.4);
```
(결과가 0행이어도 정상 — 함수가 에러 없이 실행되면 적용 성공)
  </action>
  <acceptance_criteria>
    - 파일 `supabase/migrations/20260708000001_find_nearby_similar_complexes.sql`이 존재하고 `grep -c "find_nearby_similar_complexes" supabase/migrations/20260708000001_find_nearby_similar_complexes.sql` >= 1
    - 라이브 Supabase에서 `select find_nearby_similar_complexes(35.16,129.16,'test','K_NONEXIST',30,0.4)` 호출이 에러 없이 실행됨 (RPC 적용 확인)
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/20260708000001_find_nearby_similar_complexes.sql && grep -c "ST_DWithin" supabase/migrations/20260708000001_find_nearby_similar_complexes.sql</automated>
  </verify>
  <done>dup-detection RPC 마이그레이션 파일이 존재하고 라이브 Supabase에 적용되어 호출 가능하다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: detectPotentialDuplicate 헬퍼 + seed-complexes.ts 배선 + 유닛 테스트</name>
  <files>src/lib/data/complex-matching.ts, src/lib/data/complex-matching.test.ts, scripts/seed-complexes.ts</files>
  <read_first>src/lib/data/complex-matching.ts, scripts/seed-complexes.ts</read_first>
  <behavior>
    - detectPotentialDuplicate: coordX 또는 coordY가 null이면 빈 배열 반환 (좌표 없으면 탐지 스킵)
    - detectPotentialDuplicate: RPC가 후보 행을 반환하면 그대로 반환, RPC error 시 빈 배열 + console.warn (시딩을 중단시키지 않음 — log-only)
    - detectPotentialDuplicate: RPC 호출 시 p_radius_m=30, p_similarity_threshold=0.4로 호출됨 (좌표 미세 오차 허용)
  </behavior>
  <action>
**1. `src/lib/data/complex-matching.ts`에 신규 export 함수 추가** (기존 `matchByCoordinate` 패턴 재사용, 단 이건 log-only라 throw하지 않음):
```typescript
export async function detectPotentialDuplicate(
  supabase: SupabaseClient,
  candidate: { coordX?: number; coordY?: number; nameNormalized: string; kaptCode: string },
): Promise<Array<{ id: string; canonical_name: string; kapt_code: string; dist_m: number }>> {
  if (candidate.coordX == null || candidate.coordY == null) return []
  const { data, error } = await supabase.rpc('find_nearby_similar_complexes', {
    p_lat: candidate.coordY,
    p_lng: candidate.coordX,
    p_name_normalized: candidate.nameNormalized,
    p_exclude_kapt_code: candidate.kaptCode,
    p_radius_m: 30,
    p_similarity_threshold: 0.4,
  })
  if (error) { console.warn(`[dup-check] RPC 실패: ${error.message}`); return [] }
  return (data ?? []) as Array<{ id: string; canonical_name: string; kapt_code: string; dist_m: number }>
}
```

**2. `scripts/seed-complexes.ts` 시딩 루프(라인 60~67) 배선** — `seedComplex` 성공 직후 dup-check 호출, 후보가 있으면 콘솔 로그 + `scripts/busan-dup-candidates.csv`에 append (DB 변경 없음). `nameNormalize`는 이미 import되어 있다(라인 16):
```typescript
await seedComplex({ ...c, sggCode }, supabase); sggUpserted++
const dupCandidates = await detectPotentialDuplicate(supabase, {
  coordX: c.coordX, coordY: c.coordY, nameNormalized: nameNormalize(c.kaptName), kaptCode: c.kaptCode,
})
if (dupCandidates.length > 0) {
  for (const d of dupCandidates) {
    dupRows.push(`"${sggCode}","${c.kaptCode}","${c.kaptName}","${d.kapt_code}","${d.canonical_name}",${d.dist_m.toFixed(1)}`)
  }
}
```
- 파일 상단에 `detectPotentialDuplicate` import 추가, `runWithApi` 시작 시 `const dupRows: string[] = ['new_sgg,new_kapt,new_name,existing_kapt,existing_name,dist_m']`, 종료 시 `dupRows.length>1`이면 `scripts/busan-dup-candidates.csv`에 writeFileSync + `console.log("잠재 중복 N건")`.

**3. `src/lib/data/complex-matching.test.ts` 신규 생성** — behavior의 3개 케이스를 mock supabase(`rpc` mock)로 유닛 테스트:
- coordX undefined → `[]` 반환 (rpc 미호출)
- rpc가 `{data:[{id,canonical_name,kapt_code,dist_m}], error:null}` → 해당 배열 반환
- rpc가 `{data:null, error:{message}}` → `[]` 반환 (throw 안 함)
- rpc 호출 인자에 `p_radius_m:30, p_similarity_threshold:0.4` 포함 확인 (mock의 호출 인자 assert)
  </action>
  <acceptance_criteria>
    - `grep -c "export async function detectPotentialDuplicate" src/lib/data/complex-matching.ts` == 1
    - `grep -c "detectPotentialDuplicate" scripts/seed-complexes.ts` >= 2 (import + 호출)
    - `grep -c "busan-dup-candidates.csv" scripts/seed-complexes.ts` >= 1
    - `npx vitest run src/lib/data/complex-matching.test.ts` 통과
    - `npm run lint` 통과
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/data/complex-matching.test.ts</automated>
  </verify>
  <done>detectPotentialDuplicate 헬퍼가 존재하고 유닛 테스트를 통과하며, seed-complexes.ts가 시딩 루프에서 이를 호출해 CSV로 로그를 남긴다 (병합 없음).</done>
</task>

<task type="auto">
  <name>Task 3: 부산 KAPT Golden Record 시딩 + kapt-enrich 실행</name>
  <files>없음 (기존 스크립트 실행, Task 1·2 완료 후)</files>
  <read_first>scripts/seed-complexes.ts, scripts/kapt-enrich.ts</read_first>
  <action>
Task 1(RPC 적용)·Task 2(dup-check 배선)가 완료된 상태에서 시딩을 실행한다. `getSggCodes()`가 regions에서 `is_active=true` 전체(38개, 부산 포함)를 동적 조회하므로 코드 수정 없이 부산이 포함된다:

```bash
npx tsx scripts/seed-complexes.ts       # KAPT 단지목록 → complexes upsert + dup-check 로그
npx tsx scripts/kapt-enrich.ts          # si/gu/household_count/built_year 보강
```

- `KAPT_API_KEY`가 반드시 `.env.local`에 설정돼 있어야 한다(없으면 bootstrap 모드로 전환됨 — 로그에 "Bootstrap 모드"가 뜨면 즉시 중단하고 키 설정). KAPT는 기존 크론에서 사용 중이라 설정돼 있을 것으로 예상.
- 기존 22개 지역은 `onConflict: kapt_code` upsert로 idempotent 재실행(안전), 부산 16개만 실질 신규.
- ⚠ 부산은 도심 밀집지라 `kapt-enrich.ts`가 Supabase 1,000행 쿼리 캡에 걸릴 수 있다 — 33-06 선례처럼 **여러 번 반복 실행해 `si IS NULL` 잔여가 0으로 수렴할 때까지** 재실행한다.
- 실행 후 `scripts/busan-dup-candidates.csv`의 잠재 중복 건수를 확인하고 SUMMARY.md에 "잠재 중복 N건"으로 기록(후속 병합 phase 규모 파악용, D-11).
- `scripts/seed-failures.csv`에 전 지역 0건 같은 시스템적 실패가 없는지 확인.
  </action>
  <acceptance_criteria>
    - 실행 로그에 "Bootstrap 모드" 문구가 없음 (API 모드 실행)
    - `select count(*) from complexes where sgg_code like '26%'` > 0 (부산 단지 시딩됨)
    - `select count(*) from complexes where sgg_code like '26%' and si is null` 이 반복 enrich 후 0에 수렴
    - `scripts/busan-dup-candidates.csv`가 생성되고 잠재 중복 건수가 SUMMARY.md에 기록됨
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/seed-complexes.ts 2>&1 | tail -20</automated>
  </verify>
  <done>부산 16개 구 complexes가 KAPT API로 시딩되고 si/gu/household_count/built_year가 채워지며, 잠재 중복 로그가 CSV로 기록된다 (병합 없음).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KAPT_API_KEY → K-apt 외부 API | 서버 전용 시크릿, `src/services/kapt.ts` 어댑터 경유(CLAUDE.md 준수) |
| dup-detection RPC 파라미터 | 모두 KAPT API 응답에서 산출된 서버 사이드 값, 사용자 직접 입력 경로 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering (injection) | find_nearby_similar_complexes RPC | accept | 파라미터가 KAPT API 응답 좌표/이름(서버 산출값), 사용자 입력 아님. 파라미터 바인딩(SQL 인젝션 불가), 읽기 전용 SELECT (RESEARCH.md Security Domain 확인) |
| T-34-08 | Tampering | seed-complexes upsert | accept | `onConflict: kapt_code` upsert, service_role 키 필요, 사용자 입력 경로 없음 |
| T-34-09 | Information Disclosure | busan-dup-candidates.csv | accept | 공개 단지명/좌표만 기록, git ignore 대상 여부는 실행자가 확인(운영 로그) |
</threat_model>

<verification>
```bash
test -f supabase/migrations/20260708000001_find_nearby_similar_complexes.sql
npx vitest run src/lib/data/complex-matching.test.ts
npx tsx scripts/seed-complexes.ts 2>&1 | tail -20
```
</verification>

<success_criteria>
1. dup-detection RPC가 라이브 Supabase에 적용됨 (시딩 전 [BLOCKING] 완료)
2. 부산 16개 구 complexes가 KAPT API로 시딩되고 enrich됨
3. 좌표+이름유사 잠재 중복이 병합 없이 CSV 로그로만 기록됨 (D-10/D-11)
</success_criteria>

<output>
완료 후 `.planning/phases/34-db-2/34-03-SUMMARY.md` 생성. "잠재 중복 N건" 명시.
</output>
</content>
