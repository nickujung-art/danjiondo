---
phase: 13-presale-redevelopment-dashboard
plan: 02
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - src/services/cheongyak/client.ts
  - src/services/cheongyak/client.test.ts
  - src/app/api/cron/daily/route.ts
autonomous: true
requirements: [PRESALE-01, PRESALE-02]

must_haves:
  truths:
    - "fetchCheongyakList(sggCode) ?¨ìˆ˜ê°€ ì²?•½??API 3???¸ì¶œ?˜ì—¬ CheongyakItem[] ë¥?ë°˜í™˜?œë‹¤"
    - "fetchCompetitionRate(pblancNo) ?¨ìˆ˜ê°€ ì²?•½??API 2ë¥??¸ì¶œ?˜ì—¬ numeric ê²½ìŸë¥ ì„ ë°˜í™˜?œë‹¤ (?†ìœ¼ë©?null)"
    - "daily cron??ì°½ì›(4812500000)Â·ê¹€??4825000000) ??sgg_code ë¥??œíšŒ?˜ì—¬ new_listings ??pblanc_no ê¸°ì? upsert ?œë‹¤"
    - "daily cron??upsert ??pblanc_no ëª©ë¡???€??competition_rate ë¥?update ?œë‹¤"
    - "daily cron??rcept_endde < current_date ??ì²?•½???‰ì„ is_active=false ë¡??ë™ ê°±ì‹ ?œë‹¤"
    - "MOLIT_API_KEY ë¯¸ì„¤????throw + ?¤ë¥¸ cron ë¸”ë¡?€ ê³„ì† ?¤í–‰ (errors[] ?„ì )"
    - "AbortSignal.timeout(15_000) + withRetry ë¡??¸ë? ?¸ì¶œ ?ˆì •???•ë³´"
    - "client.test.ts 6ê°?test GREEN"
    - "daily cron ?‘ë‹µ JSON??cheongyakUpserted ?„ë“œ ì¶”ê?"
  artifacts:
    - path: "src/services/cheongyak/client.ts"
      provides: "fetchCheongyakList, fetchCompetitionRate, CHEONGYAK_SGG_CODES"
      exports: ["fetchCheongyakList", "fetchCompetitionRate", "CHEONGYAK_SGG_CODES"]
    - path: "src/app/api/cron/daily/route.ts"
      provides: "ì²?•½???˜ì§‘ ë¸”ë¡ + ê²½ìŸë¥?ë³‘í•© ë¸”ë¡ + is_active ë§Œë£Œ ì²˜ë¦¬"
      contains: "fetchCheongyakList"
  key_links:
    - from: "src/app/api/cron/daily/route.ts"
      to: "src/services/cheongyak/client.ts"
      via: "import"
      pattern: "from ['\"]@/services/cheongyak/client['\"]"
    - from: "src/app/api/cron/daily/route.ts"
      to: "src/services/cheongyak/normalize.ts"
      via: "import"
      pattern: "from ['\"]@/services/cheongyak/normalize['\"]"
    - from: "src/services/cheongyak/client.ts"
      to: "src/services/cheongyak/types.ts"
      via: "import schemas + types"
      pattern: "from ['\"]\\./types['\"]"
---

<objective>
ì²?•½??API 3 (ë¶„ì–‘?•ë³´) + API 2 (ê²½ìŸë¥? ?´ëŒ‘?°ë? êµ¬í˜„?˜ê³ , ê¸°ì¡´ daily cron ???µí•©?œë‹¤. RESEARCH Pattern 1Â·2 + 13-01??Zod ?¤í‚¤ë§?normalize ?¨ìˆ˜ë¥?ê·¸ë?ë¡??œìš©.

Purpose: PRESALE-01 (?¼ë°°ì¹?ë¶„ì–‘ ê³µê³  ?˜ì§‘) + PRESALE-02 (ê²½ìŸë¥?ë³‘í•©) ???¤ì œ ?°ì´???Œì´?„ë¼?¸ì„ ?‘ë™?œí‚¨?? 13-04 ??/presale Tier 1 ?¹ì…˜???˜ì????°ì´?°ê? ë§¤ì¼ 04:00 KST ê°±ì‹ ??

Output:
- src/services/cheongyak/client.ts (fetchCheongyakList + fetchCompetitionRate)
- src/services/cheongyak/client.test.ts (6ê°?test GREEN ??13-01 todo ?„í™˜)
- src/app/api/cron/daily/route.ts ?•ì¥ (ì²?•½???˜ì§‘ + ê²½ìŸë¥?+ is_active ë§Œë£Œ)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-presale-redevelopment-dashboard/13-CONTEXT.md
@.planning/phases/13-presale-redevelopment-dashboard/13-RESEARCH.md
@.planning/phases/13-presale-redevelopment-dashboard/13-PATTERNS.md
@.planning/phases/13-presale-redevelopment-dashboard/13-01-SUMMARY.md
@CLAUDE.md

# 13-01?ì„œ ?•ì •??contract ??ë³?plan?ì„œ ê·¸ë?ë¡??¬ìš©
@src/services/cheongyak/types.ts
@src/services/cheongyak/normalize.ts

# ?¨í„´ ë³µì‚¬ ?€??@src/services/molit.ts
@src/services/molit-presale.ts
@src/app/api/cron/daily/route.ts
@src/lib/api/retry.ts

<interfaces>
<!-- ë³?plan?ì„œ ?ˆë¡œ ë§Œë“œ??contract -->

From src/services/cheongyak/client.ts (this plan creates):
```typescript
import type { CheongyakItem, CompetitionRateItem } from './types'

export const CHEONGYAK_SGG_CODES = ['4812500000', '4825000000'] as const  // ì°½ì›, ê¹€??
export async function fetchCheongyakList(sggCode: string): Promise<CheongyakItem[]>
// ?˜ì´ì§€?¤ì´???ë™ ì²˜ë¦¬ ??totalCount ê¸°ë°˜ 1~N ?˜ì´ì§€
// API ???„ë½ ??throw('MOLIT_API_KEY not set')
// res.ok=false ??throw(`Cheongyak API ${status}`)

export async function fetchCompetitionRate(pblancNo: string): Promise<number | null>
// API 2 ?¸ì¶œ ??ì£¼íƒ?•ë³„ ?‰ì„ ì§‘ê³„?˜ì—¬ ìµœëŒ“ê°?ìµœê³  ê²½ìŸë¥? ë°˜í™˜
// ?°ì´???†ìœ¼ë©?null
```

From src/app/api/cron/daily/route.ts (this plan modifies ??? ê·œ ?‘ë‹µ ?„ë“œ):
```typescript
return Response.json({
  ok: errors.length === 0,
  totalUpserted,
  kaptUpserted,
  presaleUpserted,
  cheongyakUpserted,         // NEW
  competitionUpdated,        // NEW
  expiredDeactivated,        // NEW
  errors,
})
```

From src/services/cheongyak/types.ts (13-01 ??already exists):
- CheongyakItemSchema, CompetitionRateItemSchema, CheongyakItem, CompetitionRateItem, NewListingCheongyakRow

From src/services/cheongyak/normalize.ts (13-01 ??already exists):
- normalizeCheongyakItem, parseDateStr
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/services/cheongyak/client.ts êµ¬í˜„ + client.test.ts GREEN ?„í™˜</name>
  <files>src/services/cheongyak/client.ts, src/services/cheongyak/client.test.ts</files>
  <read_first>
    - src/services/molit.ts (lines 63-119 ??fetchPage ?¨í„´ ?•í™•??ë³µì‚¬)
    - src/services/cheongyak/types.ts (Zod ?¤í‚¤ë§?import ?•ì •)
    - src/services/cheongyak/client.test.ts (13-01?ì„œ ë§Œë“  it.todo 6ê°?
    - src/lib/api/retry.ts (withRetry ?œê·¸?ˆì²˜ ?•ì¸)
    - 13-01-SUMMARY.md (Task 0 ê²€ì¦?ê²°ê³¼ ???¤ì œ ?‘ë‹µ ?„ë“œëª?
  </read_first>
  <behavior>
    Test 1 (GREEN ?„í™˜): `fetchCheongyakList('4812500000')` when MOLIT_API_KEY missing ??throws 'MOLIT_API_KEY not set'
    Test 2: vi.stubGlobal('fetch', mockFetch) ???¸ì¶œ?˜ë©´ URL??`RoadNmSggCd=4812500000`, `_type=json`, `numOfRows=100`, `pageNo=1`, `ServiceKey=<key>` ëª¨ë‘ ?¬í•¨
    Test 3: mock ?‘ë‹µ??`{ response: { body: { totalCount: 2, items: { item: [{PBLANC_NO:'A',PBLANC_NM:'X'},{PBLANC_NO:'B',PBLANC_NM:'Y'}] } } } }` ????returns 2 CheongyakItem
    Test 4: mock fetchê°€ `Response.ok=false` ????throws with message including 'Cheongyak API'
    Test 5: `fetchCompetitionRate('2026000123')` when API returns `items.item: []` ??returns null
    Test 6: `fetchCompetitionRate('2026000123')` when API returns 3 rows with CMPET_RATE values [12.3, 45.6, 7.8] ??returns 45.6 (max)
  </behavior>
  <action>
    ë¨¼ì? `src/services/cheongyak/client.test.ts` ?ì„œ `it.todo(` ë¥?`it(` ë¡??„í™˜?˜ë©´????6ê°?testë¥??‘ì„±. molit-presale.test.ts??vi.stubGlobal('fetch', ...) ?¨í„´ ì°¸ê³  (`grep -rn "vi.stubGlobal" src/services/*.test.ts` ë¡?ì°¾ì•„ ?¬ìš©).

    ê·??¤ìŒ `src/services/cheongyak/client.ts` êµ¬í˜„:

    ```typescript
    import { z } from 'zod/v4'
    import { withRetry } from '@/lib/api/retry'
    import {
      CheongyakItemSchema,
      CompetitionRateItemSchema,
      type CheongyakItem,
      type CompetitionRateItem,
    } from './types'

    const BASE_LIST_URL = 'https://apis.data.go.kr/B552555/APTLttotPblancDetail/getAPTLttotPblancList'
    const BASE_RATE_URL = 'https://apis.data.go.kr/B552555/APTRcritPblancDetail/getAPTRcritPblancList'
    const ROWS_PER_PAGE = 100
    const MAX_PAGES = 5  // ê²½ë‚¨ 2ê°?sgg Ã— 100rows Ã— 5pages = 1000ê±??í•œ (?œë„ ë³´í˜¸)

    // per CONTEXT.md specifics (verified)
    export const CHEONGYAK_SGG_CODES = ['4812500000', '4825000000'] as const  // ì°½ì›, ê¹€??
    function normalizeItems(raw: unknown): unknown[] {
      if (!raw || typeof raw !== 'object') return []
      const item = (raw as Record<string, unknown>).item
      if (!item) return []
      return Array.isArray(item) ? item : [item]
    }

    async function fetchListPage(
      sggCode: string,
      page: number,
    ): Promise<{ items: CheongyakItem[]; totalCount: number }> {
      const apiKey = process.env.MOLIT_API_KEY
      if (!apiKey) throw new Error('MOLIT_API_KEY not set')

      const url = new URL(BASE_LIST_URL)
      url.searchParams.set('ServiceKey', apiKey)
      url.searchParams.set('RoadNmSggCd', sggCode)
      url.searchParams.set('pageNo', String(page))
      url.searchParams.set('numOfRows', String(ROWS_PER_PAGE))
      url.searchParams.set('_type', 'json')
      // HOUSE_SECD=01 (APT) ??13-01-SUMMARY?ì„œ ê²€ì¦?ê²°ê³¼ ?•ì •??ê°’ìœ¼ë¡??¤ì •
      // SUMMARYê°€ HOUSE_SECD ë¯¸ì??ì´?¼ê³  ë³´ê³ ?˜ë©´ ??ì¤„ì„ ?œê±°
      url.searchParams.set('HOUSE_SECD', '01')

      return withRetry(async () => {
        const res = await fetch(url.toString(), {
          headers: { Accept: 'application/json', 'User-Agent': 'Mozilla/5.0' },
          signal: AbortSignal.timeout(15_000),
        })
        if (!res.ok) {
          throw Object.assign(new Error(`Cheongyak API ${res.status}`), { status: res.status })
        }
        const json: unknown = await res.json()
        const body = (json as { response?: { body?: unknown } })?.response?.body
        const totalCount = (body as { totalCount?: number } | undefined)?.totalCount ?? 0
        const rawItems = normalizeItems((body as { items?: unknown } | undefined)?.items)
        const items: CheongyakItem[] = []
        for (const raw of rawItems) {
          const parsed = CheongyakItemSchema.safeParse(raw)
          if (parsed.success) items.push(parsed.data)
        }
        return { items, totalCount }
      })
    }

    export async function fetchCheongyakList(sggCode: string): Promise<CheongyakItem[]> {
      const all: CheongyakItem[] = []
      const first = await fetchListPage(sggCode, 1)
      all.push(...first.items)
      const totalPages = Math.min(MAX_PAGES, Math.ceil(first.totalCount / ROWS_PER_PAGE))
      for (let page = 2; page <= totalPages; page++) {
        const result = await fetchListPage(sggCode, page)
        all.push(...result.items)
      }
      return all
    }

    export async function fetchCompetitionRate(pblancNo: string): Promise<number | null> {
      const apiKey = process.env.MOLIT_API_KEY
      if (!apiKey) throw new Error('MOLIT_API_KEY not set')

      const url = new URL(BASE_RATE_URL)
      url.searchParams.set('ServiceKey', apiKey)
      url.searchParams.set('PBLANC_NO', pblancNo)
      url.searchParams.set('numOfRows', '50')
      url.searchParams.set('_type', 'json')

      return withRetry(async () => {
        const res = await fetch(url.toString(), {
          headers: { Accept: 'application/json', 'User-Agent': 'Mozilla/5.0' },
          signal: AbortSignal.timeout(15_000),
        })
        if (!res.ok) {
          throw Object.assign(new Error(`Cheongyak API ${res.status}`), { status: res.status })
        }
        const json: unknown = await res.json()
        const body = (json as { response?: { body?: unknown } })?.response?.body
        const rawItems = normalizeItems((body as { items?: unknown } | undefined)?.items)
        if (rawItems.length === 0) return null
        const items: CompetitionRateItem[] = []
        for (const raw of rawItems) {
          const parsed = CompetitionRateItemSchema.safeParse(raw)
          if (parsed.success) items.push(parsed.data)
        }
        const rates = items.map(i => i.CMPET_RATE).filter((r): r is number => typeof r === 'number')
        if (rates.length === 0) return null
        return Math.max(...rates)
      })
    }
    ```

    ê²½ìŸë¥?ì§‘ê³„ ë°©ì‹?€ RESEARCH Open Question 2 ??Claude's Discretion: **ìµœê³  ê²½ìŸë¥?(max)** ?¬ìš©. ì¹´ë“œ??"ìµœê³  ê²½ìŸë¥?X:1" ?¼ë¡œ ?œì‹œ. CONTEXT.md decision?€ ?¨ì¼ numeric ì»¬ëŸ¼ ëª…ì‹œ ??maxê°€ ê°€???¨ìˆœ?˜ê³  ?¬ìš©?ì—ê²?ëª…í™•??
  </action>
  <verify>
    <automated>npm run test -- --run src/services/cheongyak/client.test.ts</automated>
    6ê°?test ëª¨ë‘ pass.
  </verify>
  <acceptance_criteria>
    - src/services/cheongyak/client.ts ì¡´ì¬
    - `grep -c "export.*fetchCheongyakList" src/services/cheongyak/client.ts` ??1
    - `grep -c "export.*fetchCompetitionRate" src/services/cheongyak/client.ts` ??1
    - `grep -c "export const CHEONGYAK_SGG_CODES" src/services/cheongyak/client.ts` ??1
    - `grep -c "AbortSignal.timeout" src/services/cheongyak/client.ts` ??2 (list + rate ëª¨ë‘)
    - `grep -c "withRetry" src/services/cheongyak/client.ts` ??2
    - `grep -v "^[[:space:]]*//" src/services/cheongyak/client.ts | grep -c "MOLIT_API_KEY not set"` ??2
    - client.test.ts ??it.todo ê°€ it ë¡?6ê°?ëª¨ë‘ ?„í™˜ (`grep -c "it.todo" src/services/cheongyak/client.test.ts` === 0, `grep -c "it(" src/services/cheongyak/client.test.ts` ??6)
    - npm run test ?µê³¼
  </acceptance_criteria>
  <done>
    client.ts êµ¬í˜„ + 6ê°?unit test GREEN + withRetry/AbortSignal/Zod safeParse ?¨í„´ ?ìš© ?„ë£Œ
  </done>
</task>

<task type="auto">
  <name>Task 2: daily cron??ì²?•½???˜ì§‘Â·ê²½ìŸë¥ Â·ë§Œë£?ì²˜ë¦¬ ?µí•©</name>
  <files>src/app/api/cron/daily/route.ts</files>
  <read_first>
    - src/app/api/cron/daily/route.ts (?„ì²´ ??ê¸°ì¡´ êµ¬ì¡° + ?‘ë‹µ ?•ì‹)
    - src/services/cheongyak/client.ts (Task 1 ?„ë£Œë¬?
    - src/services/cheongyak/normalize.ts (13-01 ?„ë£Œë¬?
  </read_first>
  <action>
    `src/app/api/cron/daily/route.ts` ë¥??˜ì •. ê¸°ì¡´ MOLIT ë¶„ì–‘ê¶Œì „ë§?ë¸”ë¡(lines 56-102) ?¤ìŒ, refresh_complex_price_stats ë¸”ë¡(line 104) ì§ì „??ì²?•½??ë¸”ë¡ 3ê°œë? ?½ì….

    Import ì¶”ê? (?Œì¼ ?ë‹¨):
    ```typescript
    import { fetchCheongyakList, fetchCompetitionRate, CHEONGYAK_SGG_CODES } from '@/services/cheongyak/client'
    import { normalizeCheongyakItem } from '@/services/cheongyak/normalize'
    ```

    ?‘ë‹µ ë³€??ì¶”ê? (line 19 `let totalUpserted = 0` ?¤ìŒ):
    ```typescript
    let cheongyakUpserted = 0
    let competitionUpdated = 0
    let expiredDeactivated = 0
    ```

    MOLIT ë¸”ë¡ ??line 102) ê³?refresh_complex_price_stats ë¸”ë¡(line 104) ?¬ì´???¤ìŒ 3ê°?ë¸”ë¡???•í™•???½ì…:

    ```typescript
    // ?€?€ ì²?•½??ë¶„ì–‘ ê³µê³  ?˜ì§‘ (PRESALE-01, per D-2, D-6) ?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€
    const cheongyakPblancNos: string[] = []
    for (const sggCode of CHEONGYAK_SGG_CODES) {
      try {
        const items = await fetchCheongyakList(sggCode)
        for (const item of items) {
          const row = normalizeCheongyakItem(item)
          // sgg_code??5?ë¦¬ prefix (RoadNmSggCd 10?ë¦¬ ??5?ë¦¬). normalize?ì„œ SUBSCRPT_AREA_CODE.slice(0,5)
          // ì²?•½???‰ë§Œ ?œì •??upsert (pblanc_no ??
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          const { error } = await (supabase as any)
            .from('new_listings')
            .upsert(
              {
                name:                row.name,
                region:              row.region,
                pblanc_no:           row.pblanc_no,
                pblanc_nm:           row.pblanc_nm,
                sgg_code:            row.sgg_code,
                supply_region:       row.supply_region,
                supply_count:        row.supply_count,
                rcept_bgnde:         row.rcept_bgnde,
                rcept_endde:         row.rcept_endde,
                przwner_presnatn_de: row.przwner_presnatn_de,
                mvn_prearnge_ym:     row.mvn_prearnge_ym,
                hssply_adres:        row.hssply_adres,
                is_active:           true,
                fetched_at:          row.fetched_at,
              },
              { onConflict: 'pblanc_no' },
            )
          if (!error) {
            cheongyakUpserted++
            cheongyakPblancNos.push(row.pblanc_no)
          } else {
            errors.push(`cheongyak upsert pblanc_no=${row.pblanc_no}: ${error.message}`)
          }
        }
      } catch (err) {
        errors.push(`cheongyak sgg=${sggCode}: ${err instanceof Error ? err.message : String(err)}`)
      }
    }
    totalUpserted += cheongyakUpserted

    // ?€?€ ì²?•½??ê²½ìŸë¥?ë³‘í•© (PRESALE-02, per D-2) ?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€?€
    for (const pblancNo of cheongyakPblancNos) {
      try {
        const rate = await fetchCompetitionRate(pblancNo)
        if (rate == null) continue
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const { error } = await (supabase as any)
          .from('new_listings')
          .update({ competition_rate: rate })
          .eq('pblanc_no', pblancNo)
        if (!error) competitionUpdated++
        else errors.push(`competition update pblanc_no=${pblancNo}: ${error.message}`)
      } catch (err) {
        errors.push(`competition pblanc_no=${pblancNo}: ${err instanceof Error ? err.message : String(err)}`)
      }
    }

    // ?€?€ ì²?•½??ë§Œë£Œ ê³µê³  ë¹„í™œ?±í™” (per RESEARCH Pitfall 3) ?€?€?€?€?€?€?€?€?€?€?€?€?€
    try {
      const today = new Date().toISOString().slice(0, 10)  // YYYY-MM-DD
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const { data: expired, error } = await (supabase as any)
        .from('new_listings')
        .update({ is_active: false })
        .lt('rcept_endde', today)
        .not('pblanc_no', 'is', null)
        .eq('is_active', true)
        .select('id')
      if (!error) expiredDeactivated = (expired as { id: string }[] | null)?.length ?? 0
      else errors.push(`expired deactivation: ${error.message}`)
    } catch (err) {
      errors.push(`expired deactivation: ${err instanceof Error ? err.message : String(err)}`)
    }
    ```

    ë§ˆì?ë§‰ìœ¼ë¡??‘ë‹µ (line 111) ???¤ìŒ?¼ë¡œ êµì²´:

    ```typescript
    return Response.json({
      ok: errors.length === 0,
      totalUpserted,
      kaptUpserted,
      presaleUpserted,
      cheongyakUpserted,
      competitionUpdated,
      expiredDeactivated,
      errors,
    })
    ```

    ì¤‘ìš” ?œì•½:
    - CLAUDE.md CRITICAL: ?¸ë? API ?¸ì¶œ?€ services/ ?´ëŒ‘?°ë§Œ ??ì§ì ‘ fetch ?¬ìš© ê¸ˆì? (ì§€?? fetchCheongyakList/fetchCompetitionRate ë§??¸ì¶œ)
    - CRON_SECRET ?¤ë” ê²€ì¦ì? ê¸°ì¡´ lines 13-15 ê·¸ë?ë¡?? ì? (?˜ì • ê¸ˆì?)
    - ê¸°ì¡´ MOLIT ë¸”ë¡ (lines 56-102) ?ˆë? ?˜ì • ê¸ˆì? ??onConflict: 'name,region' ? ì?
    - refresh_complex_price_stats RPC ?¸ì¶œ (lines 104-109) ?ˆë? ?˜ì • ê¸ˆì?
    - cheongyakUpserted 0?´ì–´???¤ìŒ ë¸”ë¡ ?¤í–‰ (errors.push ??continue)
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -E "(daily/route|error)" | head -20</automated>
    + `npm run build 2>&1 | grep -E "(daily/route|error|Compiled successfully)" | head -10` ??ë¹Œë“œ ?±ê³µ
    + ?˜ë™: ë¡œì»¬ dev ?œë²„?ì„œ `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/daily` ?¸ì¶œ ???‘ë‹µ??`cheongyakUpserted` ?„ë“œ ?¬í•¨ + ?ëŸ¬ ?†ìŒ (API ???¤ì • ??
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchCheongyakList" src/app/api/cron/daily/route.ts` ??2 (import + ?¸ì¶œ)
    - `grep -c "fetchCompetitionRate" src/app/api/cron/daily/route.ts` ??2
    - `grep -c "normalizeCheongyakItem" src/app/api/cron/daily/route.ts` ??2
    - `grep -c "onConflict: 'pblanc_no'" src/app/api/cron/daily/route.ts` ??1
    - `grep -c "cheongyakUpserted" src/app/api/cron/daily/route.ts` ??3 (let + ì¦ê? + ?‘ë‹µ)
    - `grep -c "competitionUpdated" src/app/api/cron/daily/route.ts` ??3
    - `grep -c "expiredDeactivated" src/app/api/cron/daily/route.ts` ??2
    - `grep -c "refresh_complex_price_stats" src/app/api/cron/daily/route.ts` ??1 (ê¸°ì¡´ ë³´ì¡´)
    - `grep -c "onConflict: 'name,region'" src/app/api/cron/daily/route.ts` ??1 (ê¸°ì¡´ MOLIT ë³´ì¡´)
    - npm run lint ?µê³¼
    - npm run build ?±ê³µ
    - ?‘ë‹µ JSON ??9ê°? ok, totalUpserted, kaptUpserted, presaleUpserted, cheongyakUpserted, competitionUpdated, expiredDeactivated, errors
  </acceptance_criteria>
  <done>
    daily cron ??ì²?•½??3ë¸”ë¡ ?µí•© ?„ë£Œ + ê¸°ì¡´ MOLITÂ·KAPTÂ·RPC ë¸”ë¡ ë³´ì¡´ + ?‘ë‹µ ?„ë“œ 3ì¢?ì¶”ê?
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron ??/api/cron/daily | CRON_SECRET Bearer ? í° ê²€ì¦?|
| daily route ??ì²?•½??API | services/cheongyak ?´ëŒ‘?°ë§Œ ê²½ìœ  (CLAUDE.md) |
| ì²?•½???‘ë‹µ ??DB | Zod safeParse + normalize ë³€??|
| daily route ??DB | createSupabaseAdminClient (RLS ?°íšŒ, ê¸°ì¡´ ?¨í„´) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | daily cron ë¬´ì¸ì¦??¸ì¶œ | mitigate | `CRON_SECRET` Bearer ?¤ë” ê²€ì¦?(ê¸°ì¡´ ?¨í„´ ? ì?, lines 13-15) |
| T-13-05 | Information Disclosure | MOLIT_API_KEY | mitigate | process.env ë§??¬ìš©, errors[] ë©”ì‹œì§€????ê°??¬í•¨ ê¸ˆì? |
| T-13-06 | DoS | API ?œë„ ì´ˆê³¼ | mitigate | MAX_PAGES=5 ?˜ì´ì§€ ?í•œ + AbortSignal.timeout(15s) |
| T-13-07 | Tampering | ë§Œë£Œ ê³µê³ ê°€ Tier 1???¸ì¶œ | mitigate | daily cron ë§ˆë‹¤ `rcept_endde < today AND pblanc_no IS NOT NULL` ??is_active=false ?ë™ ê°±ì‹  |
| T-13-08 | Tampering | MOLIT/ì²?•½????ì¶©ëŒ | mitigate | 13-01 partial unique index + ì²?•½??upsert??`onConflict: 'pblanc_no'` ë§??¬ìš© (name/region ?œì•½ ë¬´ê?) |
</threat_model>

<verification>
- src/services/cheongyak/client.ts ì¡´ì¬ + 3ê°?export
- src/services/cheongyak/client.test.ts 6ê°?GREEN
- src/app/api/cron/daily/route.ts ì²?•½??ë¸”ë¡ 3ê°??µí•©
- npm run test ?µê³¼
- npm run lint ?µê³¼
- npm run build ?±ê³µ
- MOLIT_API_KEY ?¤ì • + dev ?œë²„ ?¸ì¶œ ???‘ë‹µ??cheongyakUpserted ?„ë“œ ?¬í•¨
</verification>

<success_criteria>
1. fetchCheongyakList(sggCode) ?˜ì´ì§€?¤ì´??+ Zod ê²€ì¦?+ withRetry/timeout ?™ì‘
2. fetchCompetitionRate(pblancNo) ê°€ ì£¼íƒ??ë³µìˆ˜ ?‰ì„ max ì§‘ê³„ ??numeric ë°˜í™˜ (?†ìœ¼ë©?null)
3. daily cron 1???¤í–‰?¼ë¡œ ì°½ì›Â·ê¹€??ë¶„ì–‘ ê³µê³ ê°€ new_listings ??pblanc_no ??upsert ??4. ?™ì¼ cron ?¤í–‰?ì„œ upsert??pblanc_no ??ê²½ìŸë¥ ì´ update ??5. ë§ˆê°??(rcept_endde < today) ì²?•½???‰ì´ is_active=false ë¡??ë™ ë¹„í™œ?±í™”
6. MOLIT ê¸°ì¡´ ?‰ì? ?í–¥ ë°›ì? ?ŠìŒ (pblanc_no IS NOT NULL ?„í„° ë³´ì¥)
7. MOLIT_API_KEY ë¯¸ì„¤????ì²?•½??ë¸”ë¡ë§?fail (errors[] ?„ì ), ?¤ë¥¸ ë¸”ë¡?€ ?•ìƒ ?¤í–‰
</success_criteria>

<output>
After completion, create `.planning/phases/13-presale-redevelopment-dashboard/13-02-SUMMARY.md` documenting:
- client.ts ?œê·¸?ˆì²˜ + URL ?Œë¼ë¯¸í„°
- ê²½ìŸë¥?ì§‘ê³„ ë°©ì‹ (max) ??Open Question 2 ê²°ì •
- daily cron ?‘ë‹µ ?„ë“œ ë³€ê²?(3ê°?ì¶”ê?)
- HOUSE_SECD=01 ?¬ìš© ?¬ë? (Task 0 ê²€ì¦?ê²°ê³¼ ê¸°ë°˜)
</output>
