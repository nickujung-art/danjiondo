---
phase: 16-ad-platform
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ads/AdBannerCarousel.tsx
  - src/app/page.tsx
  - src/__tests__/ad-banner-carousel.test.ts
autonomous: true
requirements:
  - AD-01

must_haves:
  truths:
    - "홈페이지 상단에 banner_top 광고가 표시된다 (승인된 광고가 있을 때)"
    - "광고가 없을 때 홈페이지는 빈 상태로 정상 렌더링된다 (에러 없음)"
    - "복수 광고가 있을 때 자동 로테이션이 작동한다 (4초 간격)"
    - "각 광고 렌더링 시 impression 이벤트가 /api/ads/events에 POST된다"
    - "광고 클릭 시 click 이벤트가 /api/ads/events에 POST된다"
    - "홈페이지 ISR revalidate=60이 유지된다"
  artifacts:
    - path: "src/components/ads/AdBannerCarousel.tsx"
      provides: "복수 광고 자동 로테이션 클라이언트 컴포넌트"
      exports: ["AdBannerCarousel"]
    - path: "src/app/page.tsx"
      provides: "AdBannerCarousel을 hero 섹션 위에 삽입한 홈페이지"
      contains: "AdBannerCarousel"
    - path: "src/__tests__/ad-banner-carousel.test.ts"
      provides: "AdBannerCarousel 단위 테스트"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/data/ads.ts"
      via: "getActiveAds('banner_top', supabase) 서버 사이드 호출"
      pattern: "getActiveAds"
    - from: "src/app/page.tsx"
      to: "src/components/ads/AdBannerCarousel.tsx"
      via: "ads prop으로 AdCampaign[] 전달"
      pattern: "AdBannerCarousel"
    - from: "src/components/ads/AdBannerCarousel.tsx"
      to: "src/components/ads/AdBanner.tsx"
      via: "현재 인덱스의 ad를 AdBanner에 전달"
      pattern: "AdBanner"
---

<objective>
AD-01: 홈페이지 배너 광고 표시

`banner_top` placement의 승인된 활성 광고를 홈페이지 최상단에 노출한다.
복수 광고가 있을 때 자동 로테이션하는 클라이언트 컴포넌트를 추가하고, 홈페이지 RSC에서 광고를 fetch하여 전달한다.

Purpose: 광고주 수익화의 핵심 노출면 활성화. `AdBanner`와 이벤트 API는 이미 완성됐으므로 연결 작업만 남아 있다.

Output:
- src/components/ads/AdBannerCarousel.tsx (신규: 로테이션 wrapper)
- src/app/page.tsx (수정: AdBannerCarousel 삽입 + getActiveAds 호출)
- src/__tests__/ad-banner-carousel.test.ts (신규: 단위 테스트)
</objective>

<execution_context>
@/c/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ad-platform/16-CONTEXT.md

<interfaces>
<!-- 기존 코드 인터페이스 — executor가 탐색 없이 바로 사용할 것 -->

<!-- src/lib/data/ads.ts — AdCampaign 타입 + getActiveAds -->
<!--
export type AdCampaign = Database['public']['Tables']['ad_campaigns']['Row']
// Row 필드: id, advertiser_id, advertiser_name, title, image_url, link_url,
//           placement, starts_at, ends_at, status, budget_won, created_at

export async function getActiveAds(
  placement: 'banner_top' | 'sidebar' | 'in_feed',
  supabase: SupabaseClient<Database>,
): Promise<AdCampaign[]>
// CRITICAL: 내부적으로 status='approved' AND now() BETWEEN starts_at AND ends_at 조건 포함
-->

<!-- src/components/ads/AdBanner.tsx — 단일 광고 렌더링 컴포넌트 -->
<!--
'use client'
interface Props {
  ad: AdCampaign
}
export function AdBanner({ ad }: Props)
// - useEffect로 mount 시 impression POST /api/ads/events
// - onClick으로 click POST /api/ads/events
// - <a href={ad.link_url} target="_blank" rel="noopener noreferrer">
// - 이미지 + 타이틀 + advertiser_name 렌더링
// - "광고" 레이블 배지 (절대 위치)
-->

<!-- src/app/page.tsx — 현재 구조 요약 -->
<!--
export const revalidate = 60  // ← 반드시 유지

export default async function HomePage() {
  const supabase = createReadonlyClient()

  const [highRecords, rankHighPrice, ...] = await Promise.all([
    getRecentHighRecords(supabase, 4).catch(() => []),
    ...
  ])

  return (
    <div style={{ minHeight: '100vh', background: '#fff', ... }}>
      <header>...</header>
      <main style={{ flex: 1, padding: '32px 48px', maxWidth: 1280, margin: '0 auto', width: '100%' }}>
        <h1>오늘 신고가</h1>    ← 이 h1 위에 AdBannerCarousel 삽입
        ...
      </main>
    </div>
  )
}
-->

<!-- src/lib/supabase/readonly.ts — 읽기 전용 클라이언트 (ISR 안전) -->
<!--
export function createReadonlyClient(): SupabaseClient<Database>
// cookies() 미호출 → ISR 캐시와 충돌 없음
-->
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- Task 1: TDD RED — AdBannerCarousel 테스트 스캐폴드          -->
<!-- ============================================================ -->
<task type="auto" tdd="true">
  <name>Task 1: TDD RED — AdBannerCarousel 테스트 스캐폴드 (failing)</name>
  <files>
    src/__tests__/ad-banner-carousel.test.ts
  </files>

  <behavior>
    - ads=[] → 아무것도 렌더링하지 않는다 (null 반환)
    - ads=[단일광고] → AdBanner를 인덱스 0으로 렌더링한다, 내비게이션 버튼 없음
    - ads=[광고1, 광고2] → 초기에 인덱스 0 광고가 표시된다
    - ads=[광고1, 광고2] → 4초 후 인덱스 1로 자동 전환된다 (vi.useFakeTimers)
  </behavior>

  <action>
`src/__tests__/ad-banner-carousel.test.ts`를 작성한다.
구현 파일이 아직 없으므로 import가 실패하여 RED 상태여야 한다.

```typescript
/**
 * AdBannerCarousel 테스트 — Phase 16 Plan 01
 */
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, act } from '@testing-library/react'
import type { AdCampaign } from '@/lib/data/ads'

vi.mock('server-only', () => ({}))

// AdBanner는 fetch를 호출하므로 mock 처리
vi.mock('@/components/ads/AdBanner', () => ({
  AdBanner: ({ ad }: { ad: AdCampaign }) => (
    <div data-testid="ad-banner" data-id={ad.id}>{ad.title}</div>
  ),
}))

function makeAd(id: string, title: string): AdCampaign {
  return {
    id,
    advertiser_id: null,
    advertiser_name: '테스트광고주',
    title,
    image_url: 'https://example.com/img.jpg',
    link_url: 'https://example.com',
    placement: 'banner_top',
    starts_at: new Date(Date.now() - 86400_000).toISOString(),
    ends_at: new Date(Date.now() + 86400_000).toISOString(),
    status: 'approved',
    budget_won: null,
    created_at: new Date().toISOString(),
  } as AdCampaign
}

describe('AdBannerCarousel', () => {
  beforeEach(() => {
    vi.useFakeTimers()
  })
  afterEach(() => {
    vi.useRealTimers()
  })

  it('ads=[] → 아무것도 렌더링하지 않는다', async () => {
    const { AdBannerCarousel } = await import('@/components/ads/AdBannerCarousel')
    const { container } = render(<AdBannerCarousel ads={[]} />)
    expect(container.firstChild).toBeNull()
  })

  it('ads=[단일광고] → 인덱스 0 광고를 렌더링한다', async () => {
    const { AdBannerCarousel } = await import('@/components/ads/AdBannerCarousel')
    const ads = [makeAd('ad-1', '광고 하나')]
    const { getByTestId } = render(<AdBannerCarousel ads={ads} />)
    expect(getByTestId('ad-banner').dataset.id).toBe('ad-1')
  })

  it('ads=[광고1, 광고2] → 초기에 인덱스 0이 표시된다', async () => {
    const { AdBannerCarousel } = await import('@/components/ads/AdBannerCarousel')
    const ads = [makeAd('ad-1', '광고 하나'), makeAd('ad-2', '광고 둘')]
    const { getByTestId } = render(<AdBannerCarousel ads={ads} />)
    expect(getByTestId('ad-banner').dataset.id).toBe('ad-1')
  })

  it('ads=[광고1, 광고2] → 4초 후 인덱스 1로 전환된다', async () => {
    const { AdBannerCarousel } = await import('@/components/ads/AdBannerCarousel')
    const ads = [makeAd('ad-1', '광고 하나'), makeAd('ad-2', '광고 둘')]
    const { getByTestId } = render(<AdBannerCarousel ads={ads} />)
    await act(async () => { vi.advanceTimersByTime(4000) })
    expect(getByTestId('ad-banner').dataset.id).toBe('ad-2')
  })
})
```

이 상태에서 `npm run test -- ad-banner-carousel`은 import 실패로 RED 상태여야 한다.
  </action>

  <verify>
    <automated>npm run test -- ad-banner-carousel 2>&1 | tail -15</automated>
  </verify>

  <done>테스트 파일이 존재하고, 구현 파일 미존재로 RED 상태(실패)이다. 4개 테스트 케이스가 모두 정의되어 있다.</done>
</task>

<!-- ============================================================ -->
<!-- Task 2: AdBannerCarousel 구현 + homepage 연결 (GREEN)       -->
<!-- ============================================================ -->
<task type="auto" tdd="true">
  <name>Task 2: AdBannerCarousel 구현 + 홈페이지 연결 (GREEN)</name>
  <files>
    src/components/ads/AdBannerCarousel.tsx
    src/app/page.tsx
  </files>

  <action>
**1. src/components/ads/AdBannerCarousel.tsx 생성**

복수 광고를 4초 인터벌로 자동 로테이션하는 클라이언트 컴포넌트.
`AdBanner`는 impression 이벤트를 자체적으로 처리하므로 AdBannerCarousel은 로테이션만 담당한다.

```typescript
'use client'

import { useEffect, useState } from 'react'
import { AdBanner } from '@/components/ads/AdBanner'
import type { AdCampaign } from '@/lib/data/ads'

const ROTATION_INTERVAL_MS = 4000

interface Props {
  ads: AdCampaign[]
}

export function AdBannerCarousel({ ads }: Props) {
  const [index, setIndex] = useState(0)

  useEffect(() => {
    if (ads.length <= 1) return
    const timer = setInterval(() => {
      setIndex(prev => (prev + 1) % ads.length)
    }, ROTATION_INTERVAL_MS)
    return () => clearInterval(timer)
  }, [ads.length])

  if (ads.length === 0) return null

  const ad = ads[index]
  if (!ad) return null

  return (
    <div style={{ marginBottom: 28 }}>
      <AdBanner ad={ad} />
    </div>
  )
}
```

**2. src/app/page.tsx 수정**

기존 파일에 최소 변경만 적용한다:

추가할 import (기존 import 블록 끝에 추가):
```typescript
import { AdBannerCarousel } from '@/components/ads/AdBannerCarousel'
import { getActiveAds } from '@/lib/data/ads'
```

Promise.all 배열에 추가 (기존 항목 뒤에):
```typescript
const [...기존..., bannerAds] = await Promise.all([
  ...기존...,
  getActiveAds('banner_top', supabase).catch(() => []),
])
```

JSX 삽입 — `<main>` 태그 바로 안에서 `<h1>오늘 신고가</h1>` 위에 추가:
```tsx
{bannerAds.length > 0 && <AdBannerCarousel ads={bannerAds} />}
```

`export const revalidate = 60` 유지 (변경 금지).

구현 완료 후 `npm run test -- ad-banner-carousel`로 GREEN 확인.
  </action>

  <verify>
    <automated>npm run test -- ad-banner-carousel</automated>
  </verify>

  <done>
    AdBannerCarousel 컴포넌트가 구현되고, 홈페이지에 banner_top 광고가 연결된다.
    테스트 4개 모두 GREEN. 광고 없을 때 null 반환, 복수 광고 시 4초 로테이션.
    홈페이지 revalidate=60 유지.
  </done>
</task>

<!-- ============================================================ -->
<!-- Task 3: 빌드 검증                                           -->
<!-- ============================================================ -->
<task type="auto">
  <name>Task 3: 전체 테스트 + 빌드 검증</name>
  <files></files>

  <action>
전체 테스트 suite 실행 후 프로덕션 빌드를 검증한다.

```bash
npm run test
npm run build
```

타입 에러, lint 에러 발생 시 수정한다.
  </action>

  <verify>
    <automated>npm run test && npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
    `npm run test` 전체 통과.
    `npm run build` 에러 없음.
    `grep "AdBannerCarousel" src/app/page.tsx` → import + 사용 확인.
    `grep "revalidate = 60" src/app/page.tsx` → 값 유지 확인.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → AdBannerCarousel | 서버에서 fetch한 AdCampaign[]을 클라이언트 컴포넌트에 prop으로 전달 |
| AdBanner → /api/ads/events | 클라이언트에서 impression/click 이벤트 POST — 이미 rate limit + IP hash 구현됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Information Disclosure | getActiveAds 쿼리 | mitigate | CLAUDE.md 강제: `status='approved' AND now() BETWEEN starts_at AND ends_at` 조건 내장. 미승인·만료 광고 노출 불가 |
| T-16-01-02 | Tampering | AdBannerCarousel ads prop | accept | props는 서버가 쿼리한 DB 데이터. 클라이언트에서 위조 불가 (렌더 전용) |
| T-16-01-03 | Elevation of Privilege | 이벤트 API (impression/click) | accept | /api/ads/events는 이미 IP rate limit + anomaly 감지 구현됨. 추가 조치 불필요 |
</threat_model>

<verification>
## AD-01 완료 기준

```bash
# 1. 컴포넌트 존재 확인
ls src/components/ads/AdBannerCarousel.tsx

# 2. 클라이언트 컴포넌트 선언
grep "'use client'" src/components/ads/AdBannerCarousel.tsx

# 3. 홈페이지에 연결됨
grep "AdBannerCarousel" src/app/page.tsx
grep "getActiveAds" src/app/page.tsx

# 4. ISR 설정 유지
grep "revalidate = 60" src/app/page.tsx

# 5. 광고 없을 때 null 반환
grep "return null" src/components/ads/AdBannerCarousel.tsx

# 6. 테스트 통과
npm run test -- ad-banner-carousel

# 7. 빌드 성공
npm run build
```
</verification>

<success_criteria>
1. `npm run test -- ad-banner-carousel` 통과 (4개 테스트 GREEN)
2. `npm run build` 에러 없음
3. 홈페이지에서 banner_top 승인 광고가 있을 때 AdBannerCarousel 렌더링
4. 광고 없을 때 홈페이지가 정상 렌더링됨 (null 반환)
5. 복수 광고 시 4초 자동 로테이션
6. `export const revalidate = 60` 유지
7. AdBanner의 impression/click 이벤트 로깅은 기존 구현 그대로 동작
</success_criteria>

<output>
완료 후 `.planning/phases/16-ad-platform/16-01-SUMMARY.md` 생성.
포함 내용: 생성/수정된 파일 목록, 테스트 결과, 빌드 결과.
</output>
