# 골든 픽스처는 렌더 결과와 **바이트 단위**로 대조된다.
# 이 저장소는 core.autocrlf=true 라서, 이 선언이 없으면 체크아웃 때 LF→CRLF 변환이 일어나
# templates-golden.test.mjs 가 새 클론에서 14건 전부 실패한다 (코드 회귀가 아닌데도).
# 따라서 이 디렉터리 아래에서는 개행 변환을 전면 금지한다.
* -text
