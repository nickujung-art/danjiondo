name: Weekly Regional Commentary

# 지역별 주간 실거래 AI 코멘트 생성 → regional_commentary 테이블 upsert.
# realtrade-story 홈 피드가 이 결과를 읽어 지역별 실거래 요약 카드로 노출한다
# (그쪽 저장소는 외부 AI API를 직접 호출하지 않는 아키텍처라 생성은 여기서 한다).
#
# ⚠️ 2026-09-23: 대상 주가 "직전 완결 주"에서 "3주 전 완결 주"로 바뀌었다.
# 실거래 신고가 최대 30일 걸려서 갓 끝난 주는 60%만 차 있고, 그 상태로 증감을 계산하면
# 주간 증감 방향이 39% 확률로 반대로 나왔다(54표본 실측). 상세는 스크립트의
# REPORTING_LAG_WEEKS 주석 참고. 카드 제목이 "이번 주"라면 그쪽도 같이 고쳐야 한다.
#
# 구조는 monthly-ai-commentary.yml 을 그대로 따랐고 주기만 주간으로 바꿨다.

on:
  schedule:
    # 매주 월요일 06:00 KST (UTC 일요일 21:00).
    # molit-daily(매일 04:00 KST)가 주말 신고분까지 적재한 뒤에 돌도록 여유를 둔다.
    - cron: '0 21 * * 0'
  workflow_dispatch:
    inputs:
      week_start:
        description: '집계 시작일 YYYY-MM-DD (비우면 3주 전 완결 주 — REPORTING_LAG_WEEKS)'
        default: ''
      dry_run:
        description: 'DB 쓰지 않고 출력만 (true/false)'
        default: 'false'

jobs:
  generate-regional-commentary:
    name: 지역별 주간 AI 코멘트 생성
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Regional Commentary
        # 파이프(| tee)가 스크립트 종료 코드를 삼키지 않도록 pipefail 을 켠다.
        # GitHub 기본 셸은 `bash -e {0}` 라 pipefail 이 없어, 스크립트가 exit 1 로 죽어도
        # tee 의 0 이 최종 종료 코드가 됐다(2026-08-03 발견 — 배치 전량 실패에도 초록불).
        shell: bash
        run: >-
          npx tsx scripts/generate-regional-commentary.ts
          ${{ github.event.inputs.week_start && format('--week-start={0}', github.event.inputs.week_start) || '' }}
          ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}
          2>&1 | tee regional-commentary-run.log
        env:
          GROQ_API_KEY:                ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY:              ${{ secrets.GEMINI_API_KEY }}
          NEXT_PUBLIC_SUPABASE_URL:    ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY:   ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Upload run log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regional-commentary-log-${{ github.run_id }}
          path: regional-commentary-run.log
          retention-days: 14
